<!--
이 템플릿은 "형식 갖추기"가 아니라 이 저장소가 **실제로 반복해서 뚫린 지점**만 묻는다.
칸을 비워 두지 말 것 — 해당 없으면 "해당 없음"이 아니라 *왜* 해당 없는지를 쓴다.
(CLAUDE.md: 측정 없는 기계 게이트 금지 · 변별력 없는 검증 스텝 금지 · 침묵 실패 금지)
-->

## 무엇을 · 왜

<!-- 바꾼 것과 그 이유 2~4줄. 태스크 id(예: OPS-10-...)가 있으면 함께. -->

## 검증 — 무엇을 돌렸고 결과가 무엇인가

<!-- 실제 실행한 명령과 **수치**. "테스트 통과"가 아니라 "204 passed in 31s" 형태로.
     돌리지 못한 검증이 있으면 아래 '정직한 공백'에 옮긴다. -->

- [ ] CI 잡이 이 변경을 실제로 실행한다 (신규 테스트 디렉터리·워크플로 추가 시 특히)

## 변별력 — 무엇을 *깨뜨려* 확인했는가

<!-- 이 검증이 **실패 상태에서 실제로 실패**함을 어떻게 확인했는지. 결함 주입/돌연변이 내역을
     "무엇을 깨뜨림 → 어떤 신호가 났는가 → 복원함" 형태로. 성공/실패 양쪽에서 같은 값을 내는
     검사는 검증이 아니라 위장이다(2026-07-17 logconfig 사고).
     증빙이 없다면 "해당 없음"으로 넘기지 말고 *왜 없어도 되는지*를 쓴다. -->

## 정직한 공백

<!-- 측정하지 못한 것 · 미확인 전제 · 알면서 남긴 한계 · 후속 후보.
     "없음"이라고 쓰기 전에 한 번 더 볼 것 — 대개 있다. -->

## 영향 범위 · 롤백

<!-- 7계층 중 어디를 건드렸는지, 마이그레이션·설정·시크릿 변경 유무, 되돌리는 방법. -->

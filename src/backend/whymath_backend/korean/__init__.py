"""한국어 어문 규칙 유틸 — 과목과 무관한 *언어적 사실*만 다루는 순수 함수 패키지 (EOS-69).

## 왜 별도 패키지인가 (`l3/equivalent/`에서 옮겨온 이유)

`josa.py`는 모듈 docstring이 스스로 밝히듯 **한국어 조사 받침 판별**이다 — 수학을 전혀 모른다.
그런데 수학 어댑터 패키지(`l3.equivalent`, EOS-65 배정 ADAPTER) 안에 살고 있었기 때문에,
과목 중립 모듈인 `l3.render.adapters`(CORE)가 조사를 쓰려면 **CORE → ADAPTER import**가 될
수밖에 없었다(경계 문서 §4 B분류 3건). 경계 문제가 아니라 *파일 위치* 문제였다.

옮겨서 얻는 것은 위반 3건 소멸만이 아니다. 이 패키지는 **어느 계층에서도 부를 수 있는 어문
유틸의 좌석**이다 — 수학 발문 생성기(`l3.equivalent.*`)와 교수법 렌더(`l3.render`)가 조사
판별을 각자 재발명하지 않고 한 곳을 쓴다(재발명 금지는 옮기기 전부터의 규약이었고, 옮기면서
그 규약이 계층 경계와도 충돌하지 않게 됐다).

## 계약상의 위치

- **7계층**: `pyproject.toml`의 `layers` 계약에서 **최하위**다(`schema`보다 아래). 즉 이
  패키지는 `whymath_backend`의 어느 계층도 import할 수 없고(정적 강제), 실제로 표준 라이브러리
  외 의존이 0이다. 위치를 옮기면서 "아무것도 import하지 않는다"는 사실을 기계가 지키게 했다.
- **EOS 경계**: `BOUNDARY_MAP`에서 **CORE**다(과목 무관). Physics 어댑터를 붙여도 이 패키지는
  손대지 않는다 — 한국어 조사 규칙은 과목이 바뀌어도 그대로다. 나아가 baseline 0 계약
  (`l1`·`l2`·`schema`와 동급)의 source에 등재해, 이 패키지가 수학을 새로 끌어오면 즉시 적색이
  되게 했다.

## 범위

한국어 *어문* 사실만 둔다 — 조사 받침, (후속이 생긴다면) 존댓말 어미·수 읽기 같은 것. 교수학
문구·프롬프트 문면·UI 문자열은 여기 두지 않는다(그건 각 계층의 콘텐츠다).
"""

from __future__ import annotations

"""L5 상호작용 — OCR·시각화·대화 등 학생과 코어 사이의 *입출력 인식·표현* 계층.

CLAUDE.md 7계층의 L5(상호작용). L1~L4(독립 수학 코어) 위에서 학생의 손글씨 풀이·발화를
*구조*(AST/JSON)로 인식해 코어에 넘기고, 코어의 결정을 학생이 보는 표현으로 되돌린다.
L5는 아래 계층을 **호출(소비)**할 수 있으나 *구현하지 않으며*(경계 침범 금지), 자신을 쓰는
HTTP 표면(api/)·상위 응용(L6/L7)을 *알지 못한다*(역방향 의존 금지).

입주자(이 슬라이스):
  - OCR 서브시스템(`ocr/`) — 손글씨 풀이 이미지 → 영역 검출·라우팅(텍스트/수식)·인식·
    조립·검증 → `schema.ocr.OcrResult`(구조). LLM/VLM 호출은 *반드시 L3 라우터 경유*
    (CLAUDE.md "LLM 호출은 항상 라우터 경유")이며, 결정론 검증(SymPy)은 L3 패턴 재사용.

표현 ≠ 의미(CLAUDE.md): OCR 산출은 화면 문자열이 아니라 *구조*다 — 렌더는 클라이언트가
각자 한다(L5 인식 결과는 좌표·유형·LaTeX·신뢰도의 구조). 미성년 풀이 데이터의 저장계층
보호는 L5 서버 책임(평문 장기 저장·외부 공유 금지).
"""

from __future__ import annotations

"""운영 도구 패키지 — 라이브 배포 직후 수동 실행하는 진단·검증 스크립트.

`ops`는 7계층(l4→…→schema) import-linter 계약 *밖*의 단독 진입점 모듈이다.
계층에 편입되지 않으므로 config·l3 등을 자유롭게 import할 수 있으나(무제약),
반대로 어느 계층도 `ops`를 import해서는 안 된다(단독 진입점 — CLI로만 실행).
"""

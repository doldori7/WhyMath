"""L1 데이터 기반 — 백엔드 영속/적재 인프라 좌석.

7계층 L1(성취기준·검정교과서·평가원·EBS·OER + 개념그래프). 이 백엔드 패키지는 L1 자산의
*영속/적재 인프라*만 담는다(데이터 수집·정형화는 `data-pipeline` 패키지 소관). 의미검색·
모델 로직(L2/L3/L4)은 이 계층을 *호출*하되 여기서 구현하지 않는다(경계: L_n→L_{n-1}).
"""

from __future__ import annotations

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: S3-17-suneung-prefilter-persona-fit-widen
title: 수능 SQL 사전필터 persona_fit 조건 확장
track: math-completion
stage: S3
subject: math
layer: backend
status: done
priority: 3
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "api/me.py 수능 SQL where절에 세 번째 OR 조건(Problem.persona_fit[persona].as_float() >= SUNEUNG_DEFAULT_MIN_FIT) 추가"
  - "SUNEUNG_DEFAULT_MIN_FIT를 l6/suneung/gating.py에 신설·is_suneung_eligible·select_suneung_items의 인라인 min_fit 기본값 2곳이 이를 참조(SQL과 L6 진실 게이트 단일 권위)"
  - "api/me.py 관련 docstring을 현재 상태(구현 완료)로 갱신 — '별도 태스크로 분리'가 아니라 'S3-17로 구현됨'"
  - "변별력 통합테스트 2건(persona_fit-only 적격 포함/임계미달 배제) 포팅 — 실PG 필요, sandbox는 skip만 확인"
  - "성능 캐비엇 정직 기록: persona_fit JSONB 표현식 비교는 GIN 인덱스 미활용(순차 스캔)이나 코퍼스 규모(2,647건)에서 감내 가능, 실 트래픽 재검증은 범위 밖"
artifacts:
  - "미병합 고립 브랜치 education-os-architecture-mr0fbq 커밋 a20c851에서 검증된 로직만 정밀\n포팅(REC-02와 동일 방법론) — SQL 조건·SUNEUNG_DEFAULT_MIN_FIT 상수·2개 통합테스트는 그대로\n가져오되, 이후 main에서 재작성된 docstring/문서 텍스트는 원문을 베끼지 않고 현재 상태로\n직접 다시 씀. 컴파일 검증: Problem.persona_fit[persona].as_float() >= SUNEUNG_DEFAULT_MIN_FIT\n→ \"CAST((problem.persona_fit ->> 'A_일반고고3') AS FLOAT) >= 0.5\" (postgresql dialect 컴파일\n직접 확인).\n\n포팅한 통합테스트 2건은 a20c851 작성 시점(PB-03 이전)엔 없던 review_status=approved 픽스처\n필드가 필요해(PB-03의 is_review_cleared fail-closed 게이트) 추가해서 포팅 — 원문 그대로\n붙였으면 새 게이트에 막혀 깨졌을 것.\n\n실측: pytest tests/backend/api/test_me.py tests/backend/api/test_me_integration.py\ntests/backend/l6/suneung/ --asyncio-mode=auto -q → 218 passed, 29 skipped(신규 2건 포함 —\n라이브 PG 없어 sandbox에서는 skip만 확인, 실통과는 CI 확정), 0 failed.\nbacklog.py validate green(태스크 223건)."
paths:
  - src/backend/whymath_backend/api/me.py
session: null
notes: "구현 후보: 고립 브랜치 education-os-architecture-mr0fbq 커밋 a20c851. 이 태스크(PB-01)는 등록만 하고 구현은 범위 밖 — 엔드포인트 실트래픽·성능 영향을 함께 봐야 하는 별개 변경(원 커밋 판단 승계)."
updated: 2026-08-09

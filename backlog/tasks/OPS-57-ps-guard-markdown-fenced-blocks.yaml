# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: OPS-57-ps-guard-markdown-fenced-blocks
title: "PowerShell 가드를 런북 마크다운 코드펜스까지 확장 — 자동 변수·UseBasicParsing·실패 원인 보존 규칙 추가"
track: infra-debt
stage: S3
subject: math
layer: infra
status: todo
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① check_ps_scripts.py 검사 대상에 docs/**/*.md의 powershell 코드펜스 편입 — 현재 scripts/**/*.ps1만 훑어 Kiki에게 건네는 PowerShell 대부분이 미검사 상태(2026-08-31 실측: 같은 블록에서 결함 3건 연속 발생, 전건 미검출)"
  - "② 규칙 3종 신설 — (a) 자동 변수 대입 금지($home·$host·$input·$error·$args·$pwd·$matches·$profile 등: $home 대입은 거부되면서 후속 if($home)이 참이 되어 '실패가 성공처럼 보이는' 신호를 낸다) (b) Invoke-WebRequest에 -UseBasicParsing 필수(PS 5.1 IE 파싱 대화상자로 절차 정지) (c) catch가 $_.Exception.Response를 존재 확인 없이 참조 금지(전송 계층 오류에서 원인 유실)"
  - "③ 변별력 실측 — 각 규칙마다 위반 샘플이 exit 1, 정상 샘플이 exit 0을 내는지 tests/infra로 동결. 기존 .ps1 자산 전건이 새 규칙에서 green인지 먼저 확인한 뒤 CI 차단으로 승격"
artifacts: []
paths:
  - scripts/ops/check_ps_scripts.py
  - "tests/infra/**"
session: null
notes: "출처: 2026-08-31 LIC-02 siyavula URL 프로브. 원격 세션은 PowerShell 실행 검증이 구조적으로 불가(Windows 부재)하므로 정적 가드가 유일한 방어선인데, 그 가드가 런북 코드펜스를 안 본다. CLAUDE.md '측정·수집 도구를 성공 경로만 보고 설계 금지'·'변별력 없는 검증 스텝 금지'의 도구화. 2026-08-31 추가 관측(4번째 결함): 대화형 붙여넣기에서 `if {...}` / `else {...}`가 여러 입력으로 나뉘면 PS가 if를 완결 문으로 파싱해 else가 고아가 되고(CommandNotFoundException) 검증 스텝이 통째로 미실행된다 — 규칙 (d)로 추가 검토: 런북 코드펜스의 if/else 다중행 구조 경고. acceptance 수정 CLI(HARN-24)가 다른 세션 진행 중이라 notes로 남긴다."
updated: 2026-08-31

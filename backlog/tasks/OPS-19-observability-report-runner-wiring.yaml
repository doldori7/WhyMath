# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: OPS-19-observability-report-runner-wiring
title: "관측 리포트 실행 배선 — 리포트 11개 중 10개 러너 0건(산출 0회·'미측정'이 '문제 없음'으로 읽힘)"
track: infra-debt
stage: S4
subject: cross
layer: infra
status: todo
priority: 3
eos_priority: P1
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 현행 실측 고정: 리포트 CLI(*_report.py) 전수를 러너 후보(.github/·docker-compose*.yml·scripts/·infra/)와 대조해 러너 0건 목록을 확정한다(2026-08-04 실측 10/11 — 주장 확인 또는 반증, 반증되면 범위 재조정)"
  - "② 진입점 1개 배선 + 신규 로직 0: 기존 CLI를 호출만 한다(SEC-12 retention-purge compose 서비스 선례 = 앱 이미지 재사용·CLI 호출만). 리포트 내용 수정·신규 리포트 추가·게이트화는 범위 밖"
  - "③ 두 부류 분리: harness/*(코퍼스 JSON 결정론 — CI 실행 가능)와 ops/*(실 DB 필요 — 운영 진입점)를 한 진입점에 섞지 않는다. 섞으면 'DB 없으니 0건'이 되어 이 태스크가 고치려는 위장을 그대로 만든다"
  - "④ 실행 실패 ≠ 0건: 산출물에서 '리포트가 0을 보고함'과 '리포트가 돌지 못함'이 서로 다른 값으로 구분된다(ops/cost_probe 이중 회계 승계). 관측 전용이므로 수치가 0이어도 exit 0, 실행 오류만 비-0"
  - "⑤ 배선 실재성 동결: tests/infra/test_test_suite_wiring.py의 Wiring/_all_wirings를 import 재사용해 확인한다(새 YAML 파서 금지 — tests/infra/test_pedagogy_content_slot_pipeline_ci_wiring.py:22-25 원칙). '저장소에 존재함'과 '돌아감'은 다르다(OPS-03·08·10·11 계열)"
  - "⑥ 변별력: 성공 판정은 '배선했다'가 아니라 산출물 파일이 실제로 생기고 그 값이 실행 전/후로 달라지는지다. 리포트 1개를 의도적으로 깨뜨렸을 때 '실행 실패'가 보이는지 실측 → 복원해 정상 실측"
artifacts: []
paths:
  - ".github/workflows/**"
  - "docker-compose*.yml"
  - "tests/infra/**"
  - "infra/**"
session: null
notes: "docs/architecture/nlp_module_gap_review_r2.md §3 D5. NLP 축 재점검에서 발견했으나 근본 원인이 횡단이라 모듈별 중복 등재 대신 1건으로 등재(Kiki 결정 2026-08-04). 실측: 리포트 11개 중 러너 보유는 cost_report 1개뿐이고, NLP-02가 만든 attempt_grading_shadow_report(412줄·테스트 488줄·분모 없는 0 금지까지 완비)를 포함해 10개가 실데이터 산출 0회. 운영 런북도 없다(docs/ 전체에서 이 CLI들을 언급하는 것은 그들을 만든 갭 리뷰 3개뿐). CI의 python -m 스텝 6종은 전부 게이트 성격(qa_pipeline·defect_detection_eval·coach_prose_leak_eval·pedagogy_pack_fidelity_eval·notation_coverage·provenance_audit)이고 리포트 실행은 없다. '완비된 소비 경로 + 미도달 공급원' 계열 4회차이며, 앞의 셋과 달리 피해자가 관측 계층 자신이라 다음 회차를 발견할 수단이 사라진다.인접 대조(2026-08-11 병합 시점·nlp_module_gap_review_r2.md §8.11-③): OPS-34(추천 r3 §3 D8·main 신규)와 인접하나 비중복 — OPS-34는 declared_unwired_audit._OFFLINE_REPORT 면제의 수취인·만료 선언이고 이 태스크는 러너 실배선이다. **순서는 OPS-34가 먼저다**: 면제가 정당한지(=사람이 돌리는 것이 맞는지) 먼저 정해야 이 태스크가 배선할 러너의 범위가 정해진다. 거꾸로 하면 면제 대상까지 CI에 넣는다. 또한 리포트 수는 2026-08-11 재측정에서 11개 → 12개로 늘었다(harness/standard_attainment_report.py 신설·ASM-05/S4-19). acceptance ①의 '10/11'은 '11/12'로 갱신해 재확인한다.\n\n[정정 2026-09-01] eos_priority null → P1: 판정표 R/계측 — 12월 검증 관여(KPI·계약·앵커 자산). EOS-80 일괄 백필"
updated: 2026-09-01

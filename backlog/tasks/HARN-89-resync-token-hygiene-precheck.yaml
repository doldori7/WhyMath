# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: HARN-89-resync-token-hygiene-precheck
title: "pr-auto-resync 토큰 위생 사전 검사 — 시크릿 값 오염(개행·공백·비ASCII)을 행동 가능한 메시지로 진단"
track: infra-debt
stage: S3
subject: math
layer: infra
status: todo
priority: 2
eos_priority: P2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 토큰 위생 사전 검사 — pr_auto_resync.sh가 gh를 호출하기 전에 GH_TOKEN 값을 검사한다. 앞뒤 공백·개행은 제거하고 그 사실을 로그에 남긴다(조용히 고치지 않는다 — 침묵 실패 금지). 남은 이상(값 내부의 공백·제어문자·비ASCII, 비정상 길이, github_pat_ 접두 부재)은 fail-closed로 exit 1하되 **무엇이 잘못됐는지 이름을 붙여** 보고한다. 토큰 값 자체는 절대 출력하지 않는다(길이·불리언만 — CLAUDE.md 시크릿 자가검증 규칙 준수)."
  - "② 변별력 — 정상 토큰에서 초록인 것은 보호의 증거가 아니다(2026-09-01 등재 규칙). 오염 형태를 실제로 주입해 각각 다른 메시지가 나오는지 확인한다: (a)끝 개행 (b)끝 공백 (c)앞 공백 (d)값 내부 개행 (e)비ASCII 문자 (f)빈 문자열 (g)정상값(대조군 — 아무 경고도 나오지 않아야 한다). (a)~(c)는 제거 후 통과 + 고지, (d)~(f)는 exit 1이어야 한다. 대조군 (g)가 핵심이다 — 모든 입력에서 경고가 나오면 그 경고는 신호가 아니라 배경 소음이다."
  - "③ 계약 테스트를 기존 tests/infra/test_pr_auto_resync.py에 편입하고, 그 파일이 실제로 CI 잡에서 수집되는지 확인한다(\"저장소에 존재함\"과 \"돌아감\"은 다르다 — OPS-10 배선 동결 축)."
artifacts: []
paths:
  - ".github/scripts/pr_auto_resync.sh"
  - tests/infra/test_pr_auto_resync.py
session: null
notes: "[경위] 2026-09-08 게이트 G-pr-auto-resync-token 집행 중 발생. Kiki가 시크릿에 붙여넣은 PAT 값 끝에 개행이 딸려 들어가, 첫 수동 실행(run #1 · id 34187171965)이 `Post \"https://api.github.com/graphql\": net/http: invalid header field value for \"Authorization\"` 로 실패했다. 원인 규명에 필요했던 결정적 단서는 Actions 로그에서 `GH_TOKEN: ***` 다음 줄에 타임스탬프 접두가 없는 빈 줄이 있다는 것 — 즉 시크릿 값이 두 줄이라는 사실이었고, 이것은 사람 운영자가 스스로 도달하기 어렵다.\n\n[왜 태스크인가] 스크립트의 증거 설계 자체는 제대로 작동했다(실패 응답 본문을 남기도록 돼 있어 진단이 1분 만에 끝났다). 그러나 그 메시지는 Go net/http의 내부 오류 문면이라 **처방으로 번역되지 않는다**. 측정·수집 도구는 \"실패 원인이 남는가\"뿐 아니라 그 원인이 **행동 가능한 형태**여야 한다는 축(CLAUDE.md 2026-08-22)의 미충족 사례다. 이 결함은 PAT를 재발급할 때마다(만료 주기) 재현 가능하다.\n\n[한계 — 의도적 제외] 이 태스크는 토큰의 *형태*만 검사한다. 권한 충분성(Contents/Pull requests write)은 실제 쓰기를 해 봐야 알 수 있으므로 이 검사의 대상이 아니며, 그 축은 HARN-87 acceptance ①②가 가진다."
updated: 2026-09-08

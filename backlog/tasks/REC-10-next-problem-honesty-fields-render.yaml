# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: REC-10-next-problem-honesty-fields-render
title: "추천 응답 정직 표기 렌더 착지 — Flutter가 REC-01·REC-04 신규 5필드를 파싱 0, 기존 2필드도 reader 0"
track: math-completion
stage: S3
subject: math
layer: mobile
status: todo
priority: 3
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 현행 실측 고정(결함 재현 먼저) — candidate_zero_reason이 어떤 값이어도 화면 텍스트가 동일함을 재현하는 widget test를 먼저 세운다. 이 테스트가 red가 아니면 갭 진단 자체가 틀린 것이므로 범위를 재조정한다"
  - "② 정합 설계 본체 — NextProblemResponse(problem_models.dart)에 weight_axes_applied·candidate_pool_size·weak_concept_signal_count·candidate_zero_reason·band_calibrated를 선언하고, problemId==null일 때 candidate_zero_reason 값별로 서로 다른 중립 문구를 표기한다. 모델 주석의 '측정 충분·또는 미시딩' 뭉개기를 '서버가 사유를 구분해 보낸다'는 사실로 교체. src/backend 0줄·신규 API 0·신규 상태 0·네트워크 호출 추가 0·기존 5필드 회귀 0"
  - "③ 정직 신호 전수 도달 + 변별력(양방향) — 7신호(신규 5 + standardError·measurementSufficient)가 전건 파싱되고 그중 학생 대면 대상이 화면에 도달함을 확인하며, candidate_zero_reason 두 값에서 위젯 트리 텍스트가 실제로 갈리는지와 정상 경로(problemId!=null)에서는 그 문구가 나타나지 않는지를 둘 다 assert한다. 신호 목록은 사람이 옮겨 적지 않고 기계가 세게 해 다음 필드 추가 때 또 하나가 조용히 누락되지 않게 한다"
  - "④ 어조·반게임화 동결 + 범위 밖 — 문구는 학생의 결손이 아니라 데이터·시스템 한계를 말한다(부정적 피드백의 정서적 강화 금지). candidate_pool_size·weak_concept_signal_count 같은 계측 수치를 학생에게 숫자로 노출하지 않는다(판단은 사유 라벨로만 — PATH-10 선례). 랭킹·스트릭·카운트다운·보상 연출 0을 테스트로 동결(전역 UI 불변식 2·ARCH-26). 서버 응답 변경·개인화 기본값 on 전환·attempt POST 배선·개념 추천 화면 신설은 범위 밖"
artifacts: []
paths:
  - "src/mobile/lib/features/problems/**"
  - "src/mobile/test/**"
session: null
notes: "docs/architecture/ai_recommendation_module_gap_review_r3.md §3 D7. problem_models.dart:33-54의 NextProblemResponse가 원래 5필드만 선언 — REC-01이 추가한 4필드와 REC-04의 band_calibrated 전건 미파싱이고, 파싱하는 standardError·measurementSufficient조차 src/mobile/lib 전역 reader 0. 서버는 candidate_zero_reason으로 '후보 조회 0건'과 '게이팅 전부 부적격'을 이미 구분해 보내는데 클라가 버린 뒤 주석으로 '측정 충분·또는 미시딩'이라 뭉갠다. learning_path_module_gap_review_r2.md §9 9회차 원칙('정직 표기는 그것을 만든 응답 밖으로 나갈 때 함께 가야 한다')의 추천축 재생산이며 PATH-10보다 한 단계 이르다(렌더 0이 아니라 파싱 0). REC-01·REC-04 모두 paths가 backend만이라 렌더 좌석을 볼 수 없었다 — 어느 태스크의 실패도 아닌 두 태스크 사이의 무소유 경계. 백로그 전수 grep에서 이 필드명은 REC-04 YAML에만 등장 = 소유 태스크 0."
updated: 2026-08-11

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: PATH-12-me-tab-review-queue-consumption
title: "복습 큐 학생 도달 0 — GET /v1/me/review-queue를 부르는 클라이언트가 없다(S4-18 착지분이 화면에 못 감)"
track: math-completion
stage: S3
subject: math
layer: mobile
status: todo
priority: 3
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "①현행 실측 고정 — src/mobile/lib/** 전수에서 review-queue 참조 0건임을 재현하는 테스트를 먼저 세운다(결함 재현 선행)"
  - "②본체 — Me 탭에 복습 섹션 신설. GET /v1/me/review-queue의 decayed_mastery 오름차순 서버 순서를 그대로 렌더하고 클라 재정렬·재계산 0(전역 UI 불변식 1 표현≠의미). 신규 백엔드 엔드포인트 0·src/backend 0줄을 리뷰 게이트로 확인"
  - "③정직 표기 동반 이동 — calibrated=false·decay_model을 삼키지 않고 학생 어조로 표기한다(r2 §9 원칙: 정직 표기는 응답 밖으로 나갈 때 함께 가야 한다). 표기 누락 시 red가 나는 테스트를 건다"
  - "④반게임화 동결 — due_rank 숫자 미노출(순서로만)·days_since_practice를 '며칠 방치'류 부정 강화로 쓰지 않음. 랭킹·스트릭·카운트다운·보상 연출이 이 섹션에 0임을 테스트로 동결(전역 UI 불변식 2)"
  - "⑤변별력 — 빈 큐 / 항목 있는 큐 / 401 세 상태에서 위젯 트리 텍스트가 실제로 갈리는지 widget test로 assert한다. 갈리지 않으면 실패다"
  - "⑥범위 밖 명시 — 서버 응답 변경 · λ 캘리브레이션(r2 P4) · 복습 '일정'(달력·SM-2/FSRS는 v1 §2-④ 영구 미채택) · 목표축 노출(PATH-13 소관)은 이 태스크 범위 밖"
artifacts: []
paths:
  - "src/mobile/lib/features/profile/**"
  - "src/mobile/lib/features/problems/data/**"
  - "src/mobile/test/**"
session: null
notes: "docs/architecture/learning_path_module_gap_review_r3.md §3 D7. 틀의 fan-in 종단(… → 학생 대시보드) 4갈래 중 무소유 2건의 하나. S4-18(done)이 라이브로 착지시켰으나 Flutter 소비 0건이고, OPS-22 감사기의 reached가 '테스트 호출'을 포함해 어떤 게이트도 이 공백을 보지 못한다. 재사용 좌석: me_screen.dart _LearningPathSection 구조·_MessageRow(dense:true)·me_tab_state.dart SectionStatus(401 분리)."
updated: 2026-08-11

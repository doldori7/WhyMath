# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: PATH-04-learning-path-corpus-ingestion
title: "7주체 S1~S7 학습경로 코퍼스 677행 적재 추적 복원 — 자매 데이터셋 누락 해소"
track: math-completion
stage: S4
subject: math
layer: data-pipeline
status: blocked
priority: 4
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 원본 자산 확보 **선행**(Kiki 소유 행동) — Path xlsx는 저장소에 없다(저장소 내 .xlsx는 ai_llm_inventory_2026-07.xlsx 1건뿐). 제공받은 파일의 sha256 앞 16자리가 f4ee650b734ac854와 일치하는지 검증하고, 불일치면 코드가 아니라 데이터 카드(docs/data/concept_content_corpus_v1.md B.1)부터 정정한다. 자산 없이 store만 만들면 dead table"
  - "② 코퍼스 추출 → data/corpus/learning_paths_v1/. 불변식 검증(데이터 카드 §C): 전체요약 677행 · 트랙 합계 일치(초119+중60+고기본34+고일반93+고진로127+재수130+영재114=677) · concept_id 전건 해소(고아 0)"
  - "③ store·로더는 자매 데이터셋 ③의 l1/concept_content/populate.py 패턴 미러 — 멱등 적재·provenance 기록·라이선스 필드(와이매스 자체 저작). 적재 대상 = S1 진단임계·S5 숙달임계(L2 mastery 게이트 파라미터) + 소단원×트랙 경로 메타(concept_id·성취기준·이론틀 EIS·대상특화)"
  - "④ **S7 간격복습은 적재 범위에서 제외**하고 그 사실을 로더 docstring·데이터 카드에 명시 — 코퍼스 간격 설계를 store에 넣으면 BKT p_forget 단일 권위(S4-18)와 두 번째 복습 권위가 생긴다(이중 진실원천 회피)"
  - "⑤ 축 혼동 방지: 코퍼스 트랙 7종(초·중·고기본·고일반·고진로·재수·영재)과 코드 Persona A~E(시장 세그먼트)의 **직교**를 테스트로 동결 — 둘을 조인하는 코드가 생기면 실패(데이터 카드 B.3 '혼동 금지')"
  - "⑥ 범위 밖: L4 교수학 소비(S2~S4·S6 단계)·학생 노출 화면·경로 추천 결합(PATH-01~03의 알고리즘 축)은 하지 않는다. 이 태스크는 **적재까지**다 — PATH-01~03(위상정렬·정직 표기·전이 엣지)과 축이 다르다(데이터 자산 vs 알고리즘). 중복 없음"
artifacts: []
paths:
  - "data/corpus/learning_paths_v1/**"
  - "src/backend/whymath_backend/l1/**"
  - docs/data/concept_content_corpus_v1.md
session: null
notes: "착수 전 현실 점검(2026-08-10) — acceptance①이 명시한 전제가 이 세션에서 그대로 성립: 저장소 전체를 검색해도 Path xlsx(sha256 앞 f4ee650b734ac854)나 그 추출본(data/corpus/learning_paths_v1/**)이 존재하지 않는다(저장소 내 .xlsx는 ai_llm_inventory_2026-07.xlsx 1건뿐, 나머지 'path' 매치는 전부 pathlib류 무관 코드). concept_content_corpus_v1.md B.1도 '2026-08-03 정정'에서 이 사실을 이미 기록해 뒀다 — 자매 데이터셋(7계층 본문 437개념)은 착지했는데 이 §B(7주체 경로 677행)만 코퍼스 파일·store·로더가 전부 0인 채 원본조차 없다. 원본 자산 확보(Kiki 소유 행동 — 2026-06-20 업로드했던 Path xlsx 재제공 또는 소재 확인)가 acceptance①의 명시적 선결 조건이며, 자산 없이 store/로더만 만들면 태스크 자신이 경고한 'dead table'이 된다. 재개 조건: Kiki가 원본 Path xlsx(또는 동등 재추출본)를 제공 → sha256 앞 16자리가 f4ee650b734ac854와 일치 확인 → 그때 착수."
updated: 2026-08-10

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: COLLAB-07-trigger-axis-completion-corpus-schemas
title: "COLLAB-04 완결 — data/corpus·schemas가 backend 필터 밖이라 래칫 게이트가 자기 입력 변경에 안 깨어남 + corpus 플래그 무작동 + 가드 소비판정 강화"
track: infra-debt
stage: S3
subject: math
layer: infra
status: done
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 현행 실측 고정(결함 재현 선행): ⓐ ci.yml:88 backend 필터에 data/corpus/·schemas/가 없음 ⓑ 그런데 backend 잡의 상시 게이트 스텝(:319-320 표기 커버리지 notation_coverage)이 data/corpus/problem_bank_*/problems.jsonl을 읽고, tests/backend/test_failure_prevention_manifest.py:96-98이 schemas/v1.1/*.yaml 존재를 하드 단언한다 ⓒ schemas/는 web(:110)·mobile(:102)에는 있는데 backend에만 없다. 코퍼스에 신규 표기를 넣으면 게이트가 exit 1을 내는데 그 PR은 backend 잡을 skip함을 재현하는 검사를 먼저 세운다"
  - "② corpus 플래그 무작동 해소: corpus 플래그(:46·:64)의 유일한 소비처 :209는 data_pipeline 필터로 게이트된 잡 내부에 있어 코퍼스 단독 PR에서 잡 통째 skip → 그 스텝도 미실행이며 continue-on-error까지 걸려 있다. COLLAB-04 주석이 'data/corpus/는 별도 축(corpus 플래그)이 담당'이라고 정당화했는데 그 전제가 사실이 아니다 — 플래그를 실제로 발화시키거나 backend 필터에 흡수하거나, 둘 중 근거 있는 쪽을 택하고 주석의 틀린 정당화를 정정한다"
  - "③ 변별력: 각 축마다 수정 전 red·수정 후 green을 실측한다. data/corpus만 변경한 경우와 schemas만 변경한 경우를 합성해 backend 필터가 be=true를 내는지 확인. 갈리지 않으면 실패다. 비용 근거 병기 — 이력 전체에서 data/corpus 변경은 7커밋뿐이라 과트리거 비용이 낮음을 재실측해 기록한다"
  - "④ 가드 소비판정 강화: tests/infra/test_ci_contract_fixture_trigger_wiring.py의 _backend_test_consumers가 파일 전체 substring 매칭이라 docstring에 이름만 언급해도 '소비'로 통과한다(실증됨). 읽기 호출 기준(AST 또는 경로 리터럴)으로 강화하고, 대상을 .json suffix에서 최상위 전 파일로 확장한다 — 현재 data/policy_contract.yaml 같은 비-json 계약은 계약으로 인식조차 못 해 가드 green·CI skip이 성립한다(실증됨)"
  - "⑤ 가드 잡 축 확장: _ENFORCED_OUTPUTS가 backend·web 하드코딩이라 신규 잡·mobile을 검사하지 않는다. mobile 필터는 현재 나열 4종이 실측과 일치해 구멍은 없으나(src/mobile/test/ 읽기 전수 확인) 구조적 취약은 남는다 — 검사 대상 잡을 하드코딩이 아니라 ci.yml에서 파생시키거나, 최소한 mobile을 편입한다"
  - "⑥ 범위 밖·정직 표기: git core.quotePath 발산(비ASCII 계약 파일명이면 CI grep은 인용·8진 이스케이프된 경로를 받아 SKIP인데 가드는 원문자열로 RUN — false-green)은 현재 해당 파일 0건이라 이 태스크에서 고치지 않되 가드 docstring의 전제에 명시한다. 계약 내용 정합(COLLAB-05 소관)·새 CI 잡 신설·data_pipeline 필터 확장은 범위 밖"
artifacts:
  - "PR #817 (https://github.com/doldori7/WhyMath/pull/817) — 커밋 9ae1f617.\n\nci.yml 기능 4줄: backend 필터에 data/[^/]+$·data/corpus/·schemas/ 편입 · web/mobile을 같은 접두\n패턴으로 통일 · data-pipeline 잡 if에 corpus OR 추가(죽은 플래그 소생).\ntests/infra/test_ci_contract_fixture_trigger_wiring.py +518/-74(가드 강화).\n\nacceptance 6항: ①현행 실측 고정 — CI와 동일 엔진(grep -qE)으로 필터 추출해 합성 diff 판정,\n코퍼스만/schemas만 = be:false 재현. 배경 대비 반증 3건(policy_contract.yaml은 합성 예시라 실재\n안 함 → 잠재 결함으로 취급 / data/corpus 참조는 63개가 아니라 59개(33파일) / 기존\ntest_qa_pipeline_wiring.py:117 docstring이 틀린 전제를 적어놨는데 검사는 output 존재만 봤음)\n②corpus 플래그를 잡 게이트 OR로 소생 — ARCH-21 설계 의도 보존(스텝 if 유지), backend 이전은\ntimeout 35분 리스크로 기각. backend 필터 흡수는 별개로 병행(qa_pipeline과 notation_coverage는\n서로 대체 불가). COLLAB-04의 틀린 정당화 주석 교체 ③변별력 — 가드 3 failed EXIT=1 → 17 passed\nEXIT=0, 필터 4표적 false→true·음성대조(docs) false 유지. 비용 지표 교체: shallow 창 발견 후\n마진 과트리거 비용 재측정 = 0(코퍼스 7·schemas 1커밋 전건이 이미 be=true였음 — 결함이 무증상\n이었던 이유이기도 함) ④소비판정 AST화(비-docstring 문자열 리터럴만) + 대상을 data/ 최상위 전\n파일로 확장, docstring/주석 언급이 구 방식에선 통과·신 방식에선 미인정 실증 ⑤_ENFORCED_OUTPUTS\n하드코딩 제거 → ci.yml changes.outputs에서 파생, mobile 자동 편입, fake_job 주입 시 EXIT=1 실증\n⑥core.quotePath 미수정(범위 밖)하되 비ASCII 계약 0건 감시 테스트 부착.\n\n신규 계약 2건: ⑦test_no_dead_changes_flag(스텝 if가 참조하는 플래그는 잡 if도 참조해야 함 —\n죽은 corpus 지목) · ⑥의 등재 사유를 문자열이 아니라 실물(게이트 스텝 실재·CLI 코퍼스 glob 실재·\ntests/backend의 schemas 코드 리터럴 실재)로 검증 — r3 §8-⑥ '사유의 진실성을 안 보는' 결함 클래스 회피.\n\n검증(부모 세션 독립 재실행): pytest tests/infra 325 passed EXIT=0 · tests/harness 343 passed\nEXIT=0 · ruff/black CI 원문 명령 EXIT=0 · backlog validate EXIT=0(320건) · 독립 뮤테이션(backend\n필터를 COLLAB-04 형태로 회귀) EXIT=1 후 cp 백업 원복·md5 바이트 동일 확인(git 계열 미사용).\n\n정직한 잔여: backend 전체 스위트 미실행(의존성 부재·CI가 최종 판정) · mobile 필터를 MATH-01(#791)의\n개별 나열에서 접두 패턴으로 바꿈(타 세션 착지분 수정·커버리지 손실 0·과트리거만 마진 2커밋) ·\n계약 ⑦은 잡 if의 불리언 의미론 미모델링(참조 유무만·역참조도 참조로 계상·현행 0건) · 코퍼스 단독\nPR에서 data-pipeline 잡 전체 약 10분 추가(잡 분할은 신규 잡 금지 범위 밖) · data/ncic/ 미편입\n(backend 미참조 실측) · 로컬 3.11 vs CI 3.12 black 마이너 차이 이론적 여지."
paths:
  - ".github/workflows/ci.yml"
  - "tests/infra/**"
session: null
notes: "2026-08-11 COLLAB-04 착지 직후 자체 재검증에서 발견(서브에이전트 적대적 검증 + 부모 세션 재확인). COLLAB-04의 변경 자체는 옳고 방향도 맞으나, 같은 태스크가 세운 논리('계약 파일이 트리거 축에 있는가는 테스트 파일 축과 다르다')를 data/corpus 축에 적용하지 않았고 그 미적용을 정당화한 근거(corpus 플래그가 담당)가 실측상 작동하지 않는다. 규모: data/corpus를 참조하는 tests/backend 파일 63개 — COLLAB-04가 봉합한 최상위 계약 소비 테스트 10개의 6배. 반복 실수 10회차(트리거 축 부분 배선 — 연결이 조건부라 정상 상태에서는 완전히 정상으로 보인다)가 더 큰 표면에 그대로 남아 있다. 정본: docs/architecture/collaboration_module_gap_review_r3.md §3 D5 + §8 사후 정정."
updated: 2026-08-11

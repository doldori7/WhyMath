# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: S3-39-math-notation-rendering
title: "수식렌더링 flutter_math_fork 전환(모바일) — S3-24 버킷B⑤(최대 diff)"
track: math-completion
stage: S3
subject: math
layer: mobile
status: blocked
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "[재구현 여부 자체가 별도 미결 — 아래 notes 참조] flutter_math_fork 기반 수식렌더링을 도입할지부터 재확인 필요 — main이 MOB-09에서 이 패키지를 '사용처 0'으로 이미 제거하며 '수식 입력은 MathLive WebView로 이미 커버 — 별도 렌더 라이브러리 미필요'라고 명시한 바 있다(pubspec.yaml:44-46). 이 결정을 유지한 채 이 기능이 다른 방식(예: MathLive 자체의 수식 표시 기능 재사용)으로 가능한지, 아니면 그 결정을 뒤집고 flutter_math_fork를 재도입할지 Kiki 확인 후 착수"
  - "재구현 시: flutter analyze 0 · flutter test green"
artifacts: []
paths:
  - "src/mobile/lib/**"
session: null
notes: "S3-24-shadow-recovery-bucket-b(버킷B⑤)에서 분리 등록 — 9건 중 diff가 가장 크다고 명시된 항목.
  [2026-08-09 사전조사 발견] pubspec.yaml:44-46 주석: flutter_math_fork는 MOB-09가 '사용처 0·선언만 있던 상태'로 이미 제거했고 '기능 착수 시 재도입'·'수식 입력은 MathLive WebView 임베드로 이미 커버 — 별도 렌더 라이브러리 미필요 상태가 유지되고 있다'고 명시. 즉 이 항목은 main이 이미 내린 아키텍처 결정과 정면으로 상충할 수 있다.
  [2026-08-09 재조사] 원 소스 브랜치 claude/shadow-data-s3-pilot-nh5kbz가 origin에서 완전히 삭제됨(git fetch·ls-remote 전부 실패, 원 커밋 75c3dda도 fetch 불가) — '원본 diff 재적용'은 이제 불가능.
  [2026-08-09 Kiki '재구현' 승인 이후에도 별도 미결 — 의도적으로 unblock 보류] S3-24/S3-25 나머지 15개 항목은 '원본 소실 → 재구현' 승인으로 unblock했으나, 이 항목엔 그 승인이 다루지 않은 추가 질문(main의 기존 아키텍처 결정을 뒤집을지)이 걸려 있어 같은 승인으로 자동 해소됐다고 보지 않는다. Kiki에게 이 항목만 별도로 한 번 더 확인 후 unblock할 것."
updated: 2026-08-09

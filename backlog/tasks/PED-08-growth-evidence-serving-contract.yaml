# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: PED-08-growth-evidence-serving-contract
title: "성장 증거 서빙 경계 집행 — 학생 안전 엔드포인트 신설 + 노출 계약 단일 판정 경로 강제"
track: math-completion
stage: S3
subject: math
layer: backend
status: todo
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 계약 정본화와 집행 지점을 분리 기술 — growth_evidence_exposure.py는 이미 정본이므로 이 태스크는 '집행 지점'만 담당한다(gap_review_r2 §4-④ 규칙 첫 적용). 계약 로직 재구현 금지"
  - "② GET /v1/me/growth-evidence 신설 — classify_metric_exposure(growth_evidence_exposure.py:115)·narrate_calibration_brier(:81)를 경유해서만 응답 생성. 재구현하면 ':122 유일한 노출 판정 경로'가 깨진다"
  - "③ 내부 전용 2종(diagnosis_agreement_rate·tokens_per_turn)은 런타임 필터가 아니라 응답 스키마에 필드 자체를 두지 않는다(구조적 배제 — 필터는 꺼질 수 있으나 부재는 안 꺼진다)"
  - "④ help_reduction_validated/GAMING_SUSPECT는 어떤 형태로도 필드가 되지 않는다. hint_depth_reached는 exposable_now=False면 값 대신 suppressed_reason 서술만"
  - "⑤ calibration_brier는 원 스칼라 대신 3버킷 서술만(역방향 스칼라 오독 방지). 확신도 입력 UI가 0이라 당분간 NO_DATA 서술이 정상 — 실패로 읽지 않는다"
  - "⑥ 비교·서열·순위 파생 0 — 백분위·평균대비·타 학생 필드 미도입(5원칙 #2 · 계약 모듈 :15-17의 '부재가 계약'을 API 층에서도 유지)"
  - "⑦ 도달 관측 — _growth_evidence_state.py 카운터를 신 라우트에도 배선하되 원시 표면(/harness-metrics) 호출과 구분해 센다(섞이면 도달 판정이 위장됨). PED-06 3상태 리포트 계속 성립"
  - "⑧ 거버넌스 테스트 — 학생 대면 라우트 응답 모델에 금지 필드명 재등장 시 red. test_anti_gamification_governance.py 기법 답습(경로 스코프·무력화 하한 assert·금지 필드 주입 시 실제 red 나는지 변별력 양방향 실측)"
  - "⑨ CI 배선 실재 확인(OPS-03·OPS-10 선례 — 저장소에 존재함과 돌아감은 다르다)"
  - "⑩ 범위 밖 동결 — /harness-metrics 응답 변경(원시 표면 유지)·모바일 화면 신설(D6)·보호자 대시보드·확신도 수집 UI"
artifacts: []
paths:
  - src/backend/whymath_backend/api/me.py
  - src/backend/whymath_backend/harness/growth_evidence_exposure.py
  - src/backend/whymath_backend/api/_growth_evidence_state.py
  - "tests/backend/api/test_me_growth_evidence*.py"
session: null
notes: "docs/architecture/gamification_module_gap_review_r2.md §6 D5. 계약 모듈이 스스로 ':122 이 함수가 유일한 노출 판정 경로가 되게 한다'고 선언했으나 importer가 CLI 리포트(surrogate_baseline_report.py:39-41,206)와 자기 테스트뿐 — 학생 토큰(ConsentedUser)으로 부르는 GET /v1/me/harness-metrics(api/me.py:2380-2392)는 원시 11지표를 그대로 반환한다. api/me.py:2412-2414가 노출을 '클라이언트가 소비할 때의 몫'으로 위임하나 전역 UI 불변식 #1(00_index.md:41)은 클라에 판정을 두는 것을 금지 — 계약이 하필 그것을 가질 수 없는 계층에 위임됐다. v1 8회차 '규정하고 기계화 안 함'의 재발이되 규정 주체가 직전 슬라이스 자신(1일 만의 재발). 형태 신설=Kiki 확정(기존 라우트 수정 아닌 학생 안전 엔드포인트 신설)."
updated: 2026-08-04

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: OPS-42-client-reach-axis
title: "학생 도달 축 부재 — declared_unwired_audit의 reached가 '테스트 호출'을 포함해 학생 대면 미배선을 구조적으로 못 본다(감사기 자인 잔여·소유자 0)"
track: infra-debt
stage: S3
subject: cross
layer: backend
status: todo
priority: 3
eos_priority: P2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "①현행 실측 고정 — 백엔드 테스트만 호출하고 dart 클라 호출이 0인 학생 대면 라우트(GET /v1/me/review-queue·/v1/me/target-progress·/v1/me/growth-evidence 등)가 현재 reached로 통과함을 재현하는 테스트를 먼저 세운다"
  - "②본체 — AXIS_HTTP에 dart 호출만 세는 파생 축을 추가한다. 판정 3분류(client-reached / by-design:<사유> / pending-task:<id>)는 OPS-22의 기존 계약을 그대로 재사용하고 신규 대장 파일 0(backlog/가 단일 진실 원천). 라우트 추출은 route_paths() 재사용·재구현 0"
  - "③그랜드파더 만료 계약 승계 — pending-task:<id>는 backlog/tasks/<id>.yaml이 실재하고 status!=done일 때만 유효하며, 태스크 소멸·done 전환 시 expired-waiver로 exit 1(ARCH-25 GrandfatherEntry 패턴)"
  - "④변별력 양방향 — 학생 대면 라우트의 dart 호출을 떼면 exit 1, 배선하면 exit 0. 수집기가 파손되면(정규식 무력화) exit 2로 '0건 통과 위장'을 차단한다"
  - "⑤CI 배선 실재 확인 — tests/infra/에 이 축이 실제로 CI에서 실행되는지 동결하는 테스트를 둔다(OPS-03·OPS-10·test_test_suite_wiring.py 선례 — 저장소에 존재함과 돌아감은 다르다)"
  - "⑥범위 밖 명시 — 기존 4축 정의 변경 · 미도달 라우트의 실제 클라 배선(각 mobile 태스크 소관) · 기존 축별 리포트 대체는 이 태스크 범위 밖"
artifacts: []
paths:
  - src/backend/whymath_backend/ops/declared_unwired_audit.py
  - "tests/backend/ops/**"
  - "tests/infra/**"
session: null
notes: "docs/architecture/learning_path_module_gap_review_r3.md §3 D9. 감사기 자신이 ops/declared_unwired_audit.py:892-897에서 '이 축의 reached는 dart 클라 호출 ∪ 백엔드 테스트 호출이므로 … 클라 소비 공백을 보려면 dart 호출만 따로 세는 별도 축이 필요하고, 그것은 이 태스크 범위 밖'이라고 자인했고 OPS-25 리포트에 잔여로 기록됐으나 소유 태스크가 0이었다. 실측 귀결: PED-15가 layer=mobile·acceptance=[]인 채 backend 테스트 추가만으로 done 처리되고 '모바일 클라이언트는 여전히 0건'을 정직한 잔여로 남겼다. CLAUDE.md '만료 없는 유예·제외 금지'(2026-08-03) 근거. 번호 이력: 최초 OPS-33은 add 시점에 CLI가 거부(원격 claude/whymath-knowledge-design-4rxrax의 OPS-33-yaml-spec-unwired-audit-axis와 충돌), 재배정 OPS-34는 add 시점엔 통과했으나 PR 시점에 main의 OPS-34-offline-report-recipient-contract와 충돌 발견(병렬 세션 경합 창) → OPS-42로 재등재. 같은 파일(declared_unwired_audit.py)을 만지는 인접 태스크: OPS-33(YAML 스펙 축·원격)·OPS-34(offline-report·main) — 착수 시 병합 순서 조율 필요.\n\n[정정 2026-09-01] eos_priority null → P2: 판정표 N/이월 — 2027 이월(폐기 아님·선언 §6-5). EOS-80 일괄 백필"
updated: 2026-09-01

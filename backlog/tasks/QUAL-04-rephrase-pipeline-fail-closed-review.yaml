# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: QUAL-04-rephrase-pipeline-fail-closed-review
title: "재서술 파이프라인 fail-closed 설계 재검토 — 실패 레코드가 원문 그대로 코퍼스에 편입되는 구조 (QUAL-03 근본원인)"
track: infra-debt
stage: S4
subject: math
layer: backend
status: done
priority: 3
owner: claude
depends_on:
  - QUAL-03-rephrase-noop-defect-measurement
requires_gates: []
acceptance:
  - "QUAL-03이 확정한 근본원인(problem_corpus_rephrase.run_corpus_rephrase가 실패 사유 불문 outcome.rephrased=False 레코드를 원문 그대로 코퍼스에 편입 — reason_code는 표본 10건에만 남고 레코드 자체엔 미영속)을 실측 재확인하고, 실패 사유 분포(수식추출 실패/provider 예외/수치불변 위반/위생 위반/무변화 각각 몇 건)를 실제로 진단(가능하면 Phaiakes9 라이브 재실행·불가하면 코드 경로 정적 분석으로 사유별 발생 가능성만이라도)"
  - "설계 변경 결정 — (a)실패 레코드를 코퍼스에서 드롭 (b)rephrase_status 필드 신설로 영속화 후 소비측이 필터 중 판정, 근거를 기록. 어느 쪽이든 신규 마이그레이션 최소화 원칙 확인"
  - "결정 반영 후 QUAL-03 무변화 282건(재실행 시점 재측정치로 갱신) 처리는 이 태스크가 실행하지 않는다 — 콘텐츠 재생성은 Phaiakes9 라이브 LLM 필요·비용 판단이라 owner=kiki 후속 결정 또는 별도 태스크"
  - "범위 밖: QUAL-02(실중복 9쌍 자체 판정)·기존 429건 중 무변화 레코드의 실제 재생성 실행"
artifacts:
  - 55a473a6
  - "검증(전건 exit code, 독립 재실행 — CI 3개 잡 명령 그대로): backend 잡(ruff/black/mypy --strict 485파일/lint-imports 7계층 KEPT, 전체 스위트 9412 passed/304 skipped/0 failed·599s) + infra-contracts 잡(python3 -m pytest tests/infra -q → 303 passed/0 failed, 신규 5건 포함) + harness-integrity 잡(ruff/black scripts tests/harness → 0/0). 근본원인 재확정: 무변화 282건의 실위험은 run_corpus_rephrase가 아니라 backfill_rephrase_lineage_s4_18.py였다(실측: 커밋된 429건 전량이 이미 identity_id 보유·392건 재슬러그 — 무변화 레코드가 populate.py 적재 시 가짜 중복 DB 행이 됨, QUAL-01의 8쌍보다 큰 위험). run_corpus_rephrase의 fail-closed=원문유지는 기존 테스트가 고정한 의도된 계약이라 무변경 — 대신 backfill 단계에서 무변화면 slug/identity/relations 전부 미부여로 수정(JSONL 드롭이 아니라 DB 별도행 생성을 막는 방식). (b)status필드 신설은 '선언≠소비' 반복 패턴 위험으로 기각(근거 기록). cp 기반 뮤테이션(git checkout 미사용)으로 신규 테스트 5건 중 3건 실제 red 확인 후 원복. --check 드라이런: already_done=429·noop_skipped=0(전량 이미 1회 처리돼 예상대로 already_done 단락). 코퍼스 무수정(실행 전후 확인)"
paths:
  - scripts/backfill_rephrase_lineage_s4_18.py
  - tests/infra/test_backfill_rephrase_lineage_s4_18.py
session: null
notes: "QUAL-03(harness/problem_duplication_audit.py §7.1, 2026-08-10) 권고 ②를 등재 — 무변화 비율 65.7%(429건 중 282건)가 개별 결함이 아니라 파이프라인 구조(드롭 옵션 부재)의 산물이라는 근본원인은 코드 추적으로 확정했으나(run_corpus_rephrase가 실패 사유 불문 원문 그대로 편입), 실 LLM 재실행 기반 사유별 분포 진단과 설계 변경 자체는 QUAL-03 범위 밖으로 명시적으로 분리했다. QUAL-03이 추가한 위생 게이트 ⑦축(무변화 검출)은 *신규* 생성만 방어 — 기존 429건·이 구조적 결함 자체는 무변경."
updated: 2026-08-11

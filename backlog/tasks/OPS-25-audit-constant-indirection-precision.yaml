# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: OPS-25-audit-constant-indirection-precision
title: "감사기 상수 간접참조 맹점 해소 — 유령 태스크 생성 차단(PED-15·S4-22 막힘 실측 오탐 2건)"
track: infra-debt
stage: S4
subject: cross
layer: infra
status: done
priority: 3
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "event_consumers 축: 모듈 레벨 상수(frozenset/set/tuple 등)에 담긴 EventType.X가 Compare에 이름으로 쓰이면 소비로 인정 — 실측 오탐: EventType.막힘은 l2/learning_metrics_rollup.py:279(파이썬 필터)·:563(SQL 필터)로 이미 소비 중인데 _SOCRATIC_EVENT_TYPES 상수 경유라 미탐지"
  - "http_routes 축: 모듈 레벨 경로 리터럴 상수를 호출부에서 이름으로 써도 도달로 인정 — 실측 오탐: /v1/me/growth-evidence는 test_me_growth_evidence.py:48 _ENDPOINT 상수로 이미 호출 중이었다(PED-15가 이 오탐 때문에 등재됐다)"
  - "추적 범위는 같은 모듈 1홉으로 한정 — 전역 콜그래프 금지. 기존 _insert_helpers(timeseries 축 헬퍼 1홉 추적) 선례와 동일 절제"
  - "변별력 양방향 필수 — (양성) 고친 뒤 막힘·growth-evidence가 도달로 잡히는지, (음성 대조) 상수를 실제로 안 쓰는 미도달 항목이 여전히 미도달로 남는지. 음성 대조가 없으면 '전부 도달'로 뭉개는 반대 방향 오탐이 된다"
  - "감사기 exit 0 유지 + 도달 수가 오탐 해소분만큼 정확히 증가(막힘 1건 + 기존 도달분). 대장에서 불필요해진 유예 항목 제거"
artifacts:
  - "커밋 8523509 — 감사기의 상수 간접참조 맹점을 두 축에서 해소. event 축: 모듈 최상위 컨테이너 상수(frozenset/set/tuple/list)를 이름→EventType 집합으로 해석해 Compare 피연산자가 그 이름이면 소비 인정. http 축: 모듈 최상위 경로 문자열 상수 + f-string 접두 조합까지 도달 인정. 범위는 _insert_helpers 선례대로 같은 모듈 최상위 1홉(전역 콜그래프 금지).\n\n변별력 양방향 실측: (양성) 막힘 소비 전환·POST /v1/ocr/pages 도달 전환 (음성 대조) 답입력·시각화조작은 수정 후에도 미도달 유지·GET /v1/gating/gifted 미도달 유지·지역/import 상수·tuple(EventType)·+ 연결은 미인정. 서브에이전트가 수정을 임시 무력화해 검사 자체의 변별력도 확인(신규 양성 14건 전부 실패·음성 11건은 양쪽 통과). PR 작성자 독립 뮤테이션: _SOCRATIC_EVENT_TYPES에서 막힘을 빼자 다시 미도달로 잡히고 원복 시 복귀(diff 0) — 상수 내용을 실제로 읽는 것이지 이름만 보고 통과시키는 게 아님을 확인.\n\n수치: 감사기 exit 0 유지, HTTP 도달 80→87(+7)·EventType 소비 3→4(+1), 증가분 8건 전부 오탐 해소분이고 신규 배선 0(호출부 전건 육안 대조로 과탐 아님 확인). 대장 유예 8건 제거(도달 후 방치 시 stale-waiver로 exit 1). 테스트 42→67건, 백엔드 전체 9260 passed·302 skipped·exit 0. 독립 재검증: ruff/black 통과, tests/backend/ops+tests/infra 556 passed.\n\n부수 처리: S4-22를 3종→2종으로 축소(막힘이 오탐이었으므로) + MEMORY 결정 로그 등재(동일 원인 2회·유령 태스크 1건 실발생 — CLAUDE.md 반복 실수 재발방지 의무).\n\n정직한 잔여: 이 축의 reached는 'dart 클라 호출 ∪ 백엔드 테스트 호출'이라, 해제된 7건이 reached라는 건 테스트가 관통한다는 뜻이지 학생 앱이 쓴다는 뜻이 아니다(시각화 3종·speech·assemble의 모바일 소비는 여전히 0건 — 대장 주석에 명시) · SQL .in_(상수) 단독 소비는 Compare가 아니라 여전히 미탐 · 모듈 최상위 밖 상수(클래스 속성·조건부 재대입·import)는 설계상 미해석(미도달로 남아 등재를 강제받는 안전 방향)."
paths:
  - src/backend/whymath_backend/ops/declared_unwired_audit.py
  - tests/backend/ops/test_declared_unwired_audit.py
session: null
notes: "OPS-22(#754) 회수·PED-15(#755) 처리 중 실측 발견한 체계적 맹점. 두 오탐이 같은 원인(상수 간접참조)이며, 방치하면 실재하지 않는 미도달을 계속 태스크로 등재해 유령 작업을 만든다 — PED-15가 그 첫 사례이고 S4-22가 두 번째(3종 중 막힘 1종이 오탐). 감사기 자신의 정밀도 문제이므로 축별 bespoke report와 무관."
updated: 2026-08-10

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ASM-04-blueprint-test-set-assembly
title: "평가 청사진/테스트셋 조립 — 표준×난이도×문항수×배점×시간 선언적 명세 (미병합 브랜치 jkwdzn 재작성)"
track: math-completion
stage: S4
subject: math
layer: backend
status: done
priority: 4
owner: claude
depends_on:
  - ASM-03-assessment-writer-persist
requires_gates: []
acceptance:
  - "①선언적 blueprint 명세(성취기준×난이도 밴드×유형 비율·문항 수·배점 배분·시험시간)를 만족하는 세트를 자체 동등문제 코퍼스에서 조립 ②후보 필터는 `l6/_shared.py`의 `is_exposable`(저작권·법적 축)과 `is_review_cleared`(검수·운영 축)를 **각각 독립된 if 2개**로 통과시킨다(PB-03 설계 — 두 축을 합치지 않는다)"
  - "①CAT vs blueprint 역할 분담을 주석·테스트로 동결 — 학습 중 노출은 CAT 단건(`GET /v1/me/next-problem`)이 정본이고 blueprint는 '단원 마감 측정'에만 쓰는 예외, 세트가 CAT을 대체하지 않는다 ②좌석 증설 회피: 새 테이블 없이 `Assessment` 행 + 문항 id 배열로 표현(`AssessmentType.실전모의고사` 첫 발화) ③**신규 마이그레이션 0**"
  - "배점·총점 — `points`(`schema/problem.py:266`) 첫 소비처로 총점을 합산하되, **코퍼스 2,647건 전량 `points=NULL`이 실측 확인된 상태**(2026-08-10)라 NULL을 0으로 접어 가짜 총점을 만들지 않는다. 배점 미부여 문항이 섞이면 총점을 `None`으로 두고 미부여 건수를 함께 보고한다(날조 금지·침묵 실패 금지). 부분점수는 신규 채점기 없이 기존 `l3/verify_solution.py`의 `first_incorrect_index` 재사용"
  - "시험시간 제약은 **선언 측에서만** 표현 — `Problem`에 `estimated_time` 류 필드가 없음을 실측 확인(2026-08-10). 문항별 소요시간을 추정해 채우지 말고(날조 금지), blueprint가 선언한 총 시험시간을 메타로 보존하는 데 그친다"
  - "변별력 — 제약을 만족하는 코퍼스와 만족 못 하는 코퍼스가 **서로 다른 결과**를 내는지 실측(예: 요구 문항수 > 적격 후보수면 조립 실패가 명시적으로 드러나야 하고, 조용히 부족한 세트를 반환하면 안 된다). `is_exposable`/`is_review_cleared` 각각을 뮤테이션해 실제로 후보가 빠지는지 확인"
  - "반-스코프 동결: 평가원·EBS 본문 0건(구조 메타만·자체 동등문제 대체) · 교사용 시험지 빌더 UI·인쇄·PDF 출력 미포함(Phase 3+) · 루브릭·서술형 채점 미포함"
artifacts:
  - "커밋 e1b867c — l6/blueprint/assembly.py(순수 조립 로직 571줄)·l6/blueprint/__init__.py 신설,\napi/me.py에 POST /v1/me/assessments/assemble 추가(+230/-0 완전 추가만·기존 함수 무수정),\n테스트 2파일(hermetic 56건).\n\nacceptance 6항: ①선언 명세(성취기준×난이도밴드×유형×문항수) 조립 + is_exposable·\nis_review_cleared를 and로 묶지 않고 순차 if 2개(PB-03 설계 준수) ②CAT 경계 동결\n(BLUEPRINT_USE_CASE 상수 + recommend_next_problem 소스가 이 모듈을 참조하지 않음을\ninspect.getsource로 확인하는 테스트) · 세트는 Assessment 행 + pattern_diagnosis JSONB\n(AssessmentType.실전모의고사 첫 발화) · 신규 마이그레이션 0(alembic 무변경 확인)\n③배점 정직 회계 — declared_total_points(청사진 선언)와 corpus_total_points(Problem.points\n첫 소비처)를 별도 축으로 분리, 미부여 1건이라도 섞이면 None + points_missing_count 보고\n(코퍼스 2,647건 전량 NULL 실측 반영·NULL→0 접기 없음). partial_credit은 first_incorrect_index\n재사용하되 인정 구간에 unverifiable이 있으면 점수를 만들지 않음(0점과 판정불가 구분)\n④시험시간은 선언값 보존만(결과 스키마에 time/duration 필드 부재를 테스트가 기계 확인)\n⑤변별력 실행 — 요구 5문항 vs 적격 2건이면 satisfied=False·shortfall=3·api 적재 0(commit 0)\n확인, is_exposable/is_review_cleared 각각 뮤테이션 시 eligible_count 3→2로 감소·원복 시 복구\n⑥반-스코프 — 평가원/EBS/교과서만인 풀은 세트 0, 게임화 금기 4필드 전부 None.\n\n검증(이 세션 독립 재실행): ruff/black 통과 · alembic 변경 0건 확인 · me.py가 +230/-0으로\n완전 추가만임을 확인(타 세션 공유 파일 충돌 위험 0) · 회귀 스코프 tests/backend/{l6,api}\n1590 passed·120 skipped·0 failed. 서브에이전트 보고: 전체 스위트 9182 passed·302 skipped·\n0 failed, mypy --strict(481파일)·lint-imports 통과.\n\n정직한 잔여: 실PG 통합테스트를 skip 가드로 작성한 게 아니라 아예 만들지 않았다(샌드박스에\nPG 부재) — 새 엔드포인트의 실 DB 왕복은 CI 실PG 잡이 최종 판정 · 난이도 밴드 경계\n(2.5/3.75)는 코퍼스 분포 보정 아닌 선언값 · 배점 우선순위 가중은 points 전량 NULL이라\n현재 실질 무신호 · 세트 시행·채점 폐루프는 범위 밖(구성까지)·partial_credit은 호출자 없음 ·\n멱등 창 미설정(같은 청사진 2회 POST 시 행 2건 — 서로 다른 청사진을 같은 날 조립하는 게\n정상이라 창을 잘못 걸면 막히기 때문)."
paths:
  - "src/backend/whymath_backend/l6/**"
  - src/backend/whymath_backend/api/me.py
  - "tests/backend/l6/blueprint/**"
  - tests/backend/api/test_me_blueprint_assemble.py
session: null
notes: "브랜치 jkwdzn의 원 ASM-03/D4 재작성. CAT은 학습시간 기본값으로 유지하고, 이 태스크는 '단원 마감 측정' 예외 상황에서만 쓰는 청사진 기반 테스트셋 조립. is_exposable() 게이트 통과 문항만 후보."
updated: 2026-08-10

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: PB-03-exposure-path-gates
title: "학생 노출 경로 게이트 3축 — 기본 CAT 저작권 게이트 미배선 · 검수 축 미집행 · L6 후보 1000건 절단"
track: math-completion
stage: S3
subject: math
layer: backend
status: done
priority: 2
owner: claude
depends_on:
  - PB-01-unmerged-completion-recovery
requires_gates: []
acceptance:
  - "저작권 게이트를 주 노출 경로에 — 기본 CAT 후보 SQL(api/me.py:1813)에 Problem.source_type.notin_(METADATA_ONLY_SOURCES) 추가. 수능 분기(me.py:1757)와 정확히 같은 상수를 재사용해 판정 기준 이원화를 회피(단일 권위). l6/_shared.py:141이 'L6의 모든 응용 모드가 차단한다'고 선언하는데 is_exposable 호출자에 api/me.py가 0건이고, 학생 앱이 실제로 쓰는 경로가 그 기본 분기다. 현 코퍼스 100% 자체생성이라 실손실은 0 — 결함 수정이 아니라 규약 대칭·defense-in-depth이며 그 사실을 과장 없이 기록한다"
  - "검수 축 활성화(신규 필드 0) — corpus_audit_eval 판정(PASS/FAIL/min-n 미달)을 코퍼스 단위로 Problem.review_status에 결정론 각인. 현재 실측 None 2,647/2,647이며 읽는 코드도 쓰는 코드도 0건이라, Wilson PASS 코퍼스와 min-n 미달 killer(120<200)가 런타임에서 무변별이다. 각인 값은 사람의 인상이 아니라 측정 게이트 판정만(사람 입력 경로 0 — 검증 권위 서열)"
  - "is_review_cleared를 is_exposable와 별도 함수로 신설해 L6 6모드 + 기본 CAT에 배선 — 두 함수를 합치지 않는 것이 설계의 핵심이다. is_exposable는 법적 축(협상 불가·의사결정 우선순위 #2)이고 검수는 운영 축(정책·완화 가능)이라, 하나로 뭉치면 나중에 운영 사유로 법적 게이트를 느슨하게 하는 압력이 생긴다"
  - "L6 후보 절단 해소 — api/gating.py:70 _CANDIDATE_FETCH_LIMIT=1000 < 코퍼스 2,647이고 created_at DESC 정렬 후 절단이라 최신 1,000건 밖 1,647문은 어떤 L6 모드에서도 후보가 될 수 없다. 최소한 절단 발생 자체를 관측(잘린 건수)하고, 상한 상향 또는 SQL 사전축소(수능 분기 선례) 중 성능 실측에 근거해 택한다. 조용한 절단을 남기지 않는 것이 요건"
  - "변별력 실측 — 메타데이터 전용 출처(평가원/EBS/교과서) 행 1건과 검수 FAIL 코퍼스 문항 1건을 주입해 후보에서 실제로 빠지는지 확인. 주입 없이 통과하는 테스트는 위장이다"
artifacts:
  - "3축 구현 완료(축① api/me.py 기본 CAT SQL에 METADATA_ONLY_SOURCES 저작권 게이트 추가·수능분기와 동일 상수 재사용, 축② l6/_shared.py is_review_cleared 신설+L6 6모드 gating.py 및 api/me.py candidate_stmt에 독립 조건 배선+problem_corpus_review_status_backfill.py CLI로 코퍼스 7종 2647건 전건 review_status 백필(approved 2489/pending 158), 축③ api/gating.py _CANDIDATE_FETCH_LIMIT 1000→3000 상향+절단 관측 로깅). 실측 테스트: tests/backend/l6/ 240 passed, tests/backend/api/test_me_review_status_gate.py+test_gating.py 56 passed, tests/backend/api/test_me.py+test_me_integration.py 167 passed/27 skipped/2 failed(TestSessionEndConceptSnapshots, git stash로 클린 HEAD에서도 동일 재현 확인된 기존 pytest-asyncio 격리 문제·본 태스크 무관), tests/backend/api/test_e2e_*.py 2 skipped(라이브 PG 필요), tests/backend/harness/ 1023 passed/10 skipped/62 failed(test_wh1_evaluation.py 전체·동일하게 클린 HEAD 재현 확인된 기존 환경 문제·본 태스크 무관). 정직한 잔여: L6 성능 실측 미실행(라이브 PG 없음), probability_finite_v0·problem_bank_v1은 감사 라벨 부재로 pending 고정, populate_problem_bank 실DB 재적재 미실행(코퍼스 JSONL만 갱신), docs/data/review_status_backfill_report.md가 언급한 test_problem_corpus_review_status_backfill.py·test_review_status_gate.py·test_gating_candidate_limit.py 3개 파일은 실제로는 생성되지 않았고 해당 커버리지는 test_shared.py(TestIsReviewCleared)·L6 6개 test_gating.py(TestReviewStatusGateIndependentFromCopyrightGate)·test_me_review_status_gate.py로 대체 편입되어 있음(문서 기술과 실물 불일치, 별도 정정 필요)."
paths:
  - src/backend/whymath_backend/api/me.py
  - src/backend/whymath_backend/api/gating.py
  - "src/backend/whymath_backend/l6/**"
  - "tests/backend/api/**"
session: null
notes: "정본: docs/architecture/problem_bank_gap_review_r2.md §0-②-다·라·마 · §3 R4 · §2-⑦(review_status를 사람 검수 기록용 수동 필드로 쓰지 않는 판정). 1차 문서 §0이 세운 '노출 4단'(게이트 통과→코퍼스 편입→노출 적격=is_exposable+검수→실노출)의 3단이 코드에 없다. PB-04(L6 6모드 도달)가 착지하면 절단 축이 즉시 발화하므로 이 태스크가 선행 또는 동반이어야 한다."
updated: 2026-08-08

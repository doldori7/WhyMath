# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ASM-06-distractor-misconception-reverse-link
title: "오답 선택지→오개념 역방향 배선 — 학생이 실제로 고른 보기를 오개념 ID로 (미병합 브랜치 jkwdzn 재작성)"
track: math-completion
stage: S4
subject: math
layer: backend
status: blocked
priority: 4
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "선행 확인: NLP-02 서버 채점 shadow 경로가 학생이 고른 MC 인덱스를 이미 포착하는지 실측 확인 — 포착 안 하면 이 태스크는 그 확장부터 선행해야 하므로 스코프를 그에 맞게 재정의하고 착수"
  - "포착된 선택 인덱스를 오개념 ID로 매핑 — 오개념을 초기 context에 preload하지 않고 reactive retrieval만 사용(CLAUDE.md 원칙)"
artifacts: []
paths:
  - "src/backend/whymath_backend/l4/misconception/**"
  - src/backend/whymath_backend/schema/problem.py
session: null
notes: "브랜치 jkwdzn의 원 ASM-04/D5 재작성. REC-02(오개념 표적 프로브 공급)가 정방향(가설→프로브 선택)만 다룸 — 이건 역방향(학생이 고른 실제 오답 인덱스→오개념 ID). 선행조건: 현재 student_answer가 자유텍스트라 '어느 보기를 골랐는지' 자체가 채점 레이어에 없음 — NLP-02 shadow 채점이 선택 인덱스를 이미 포착하는지 먼저 확인 후 스코프 확정.\n\n[차단 2026-08-11] 선행 조건 불충족 확정 + 스코프 재정의 필요 (2026-08-11 r2 §3 D4 실측). acceptance ①은 'NLP-02 shadow가 학생이 고른 MC 인덱스를 이미 포착하는지 실측 확인 — 포착 안 하면 스코프를 그에 맞게 재정의하고 착수'를 선행으로 걸었다. 실측 답 = **포착하지 않는다**. ⑴shadow(harness/attempt_grading_shadow_report.py)는 student_answer 자유텍스트를 verify_answer에 넘길 뿐 선지 개념 자체가 없다 ⑵애초에 그 shadow의 코퍼스 파생 가능 비율이 0.00%다(ASM-08). 진짜 차단 요인은 채점 권위가 아니라 AttemptSubmitRequest(api/me.py:641-657)에 선택 인덱스 슬롯이 없다는 것 — model_config가 extra='forbid'라 클라가 임의 필드를 실어 보낼 수도 없다. 재정의 방향: 요청 슬롯 selected_choice_index 신설 + 적재 + 클라 변경 + 마이그레이션으로 스코프를 다시 잡고, 사장 규모 관측(ASM-09) 착지 후 해제한다. 승계할 제약 = 오개념 reactive retrieval만(preload 금지)·오답을 오개념으로 단정 금지(distractor_map은 가설 근거)·기존 misconception_hypothesis 좌석으로 흘리고 별도 판정 경로 신설 금지(이중 진실원천)·점수/오답률 노출 금지."
updated: 2026-08-11

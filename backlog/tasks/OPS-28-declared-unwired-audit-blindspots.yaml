# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: OPS-28-declared-unwired-audit-blindspots
title: "선언≠배선 감사기 사각 3종 해소 — CLI 축 전 패키지 확장·HTTP 서버/클라 도달 분리·리터럴 한계 자기 선언 (OPS-22 후속)"
track: infra-debt
stage: S4
subject: cross
layer: backend
status: todo
priority: 3
eos_priority: P2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① CLI 축을 _CLI_PACKAGES=(harness,ops) 비재귀 glob(declared_unwired_audit.py:560,568)에서 main() 보유 전 패키지(l1·l2·l3·l4·l5·whs 하위 포함)로 확장 — 신규 편입 CLI(약 30종: l4/misconception 11종·l3/pregenerate·l2/calibrate_items·whs 3종·l1 populate 계열 등) 전건 3분류 강제(미분류 exit 1·기존 그랜드파더 규약 유지)"
  - "② 확장 첫 적용으로 실호출자 0 실측 2건(l4/misconception/judge_shadow_harvest.py·crosslink_triage.py — 테스트 외 호출자 0)의 분류 확정"
  - "③ HTTP 축에 서버 도달(백엔드 테스트 포함)과 클라 도달(dart)의 분리 컬럼 추가 — 테스트 합집합 reached 판정(:998)은 data_platform_module_gap_review.md §2가 기록한 의도적 결정이므로 뒤집지 않고 클라 축을 별도 판정으로 확장(POST /v1/generate·GET /v1/jobs가 서버 테스트만으로 reached인 실사례)"
  - "④ dart 경로 리터럴 매칭 한계(verify_api.dart에 리터럴은 있으나 lib/ 소비 화면 0인 client-side 선언≠배선 미검출)를 모듈 docstring에 자기 선언 — AST 수준 해소는 범위 밖 동결"
artifacts: []
paths:
  - src/backend/whymath_backend/ops/declared_unwired_audit.py
  - "tests/backend/ops/**"
session: null
notes: "ai_integration_gap_review.md D5(2026-08-10). dead-write 테이블 사각(dsl_integration_gap_review.md·PED-16 notes가 관찰)은 별개 4축째 — 본 태스크 범위 밖(PED-16 판정 결과에 따라 후속). 감사기 자신이 '최소 6회 반복' 사고 계열의 방어 장치이므로 사각 확장은 우선 방어선 보강.\n\n[정정 2026-09-01] eos_priority null → P2: 판정표 N/이월 — 2027 이월(폐기 아님·선언 §6-5). EOS-80 일괄 백필"
updated: 2026-09-01

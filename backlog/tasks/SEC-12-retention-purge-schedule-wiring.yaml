# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: SEC-12-retention-purge-schedule-wiring
title: "보존 파기 정기 실행 배선 — 로직·CLI 완비인데 스케줄 0(정책 미집행 상태)"
track: infra-debt
stage: S4
subject: cross
layer: infra
status: in_progress
priority: 3
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "스케줄 진입점 1개 배선(Celery beat 또는 compose cron — 착수 시 docker-compose.prod.yml 실측 후 확정) + 신규 로직 0(retention_purge_cli 호출만) + cleanup_stale_devices 동반 검토"
  - "배선 실재성 테스트 동결 — 스케줄 정의를 지우면 테스트가 깨진다(OPS-03/08/10/11 선례: '존재함 ≠ 돌아감')"
  - "dry-run 실행 증거 + 파기 0건('0건 통과')과 실행 실패('측정 실패')가 구분되는 출력 동결(이중 회계 금기)"
artifacts: []
paths:
  - src/backend/whymath_backend/privacy/retention_purge_cli.py
  - docker-compose.prod.yml
  - "infra/**"
session: claude/whymath-account-security-973iv1
notes: "2026-07-30 account_security_gap_review.md D6. 실측: privacy/retention.py·retention_purge_cli.py 완비이고 CLI docstring:8이 '둘 다 실행 진입점이 없어 retention이 집행되지 않았다 — 이 CLI가 그 표면이다'라고 쓰여 있는데, 그 CLI를 부르는 스케줄 정의가 grep 0(cron·Celery beat·CronJob). 즉 보존 정책은 여전히 집행되지 않는 상태 — CLI가 생긴 것으로 문제가 해결됐다고 읽히기 쉬운 형태. config.py:632 device_credential_max_idle_days도 '운영은 Celery beat·cron 권장' 상태. 배포 형태(OPS-03) 확정 시 배선 위치가 바뀔 수 있어 priority 3."
updated: 2026-08-03

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: OPS-34-offline-report-recipient-contract
title: "오프라인 리포트 면제의 수취인·만료 선언 계약 — _OFFLINE_REPORT 면제 항목에 런북·주기 0건"
track: infra-debt
stage: S4
subject: cross
layer: infra
status: todo
priority: 3
owner: claude
depends_on:
  - OPS-29-ci-enforcement-declaration-contract
requires_gates: []
acceptance:
  - "① 현행 실측 고정 — ops/declared_unwired_audit.py의 _OFFLINE_REPORT('빌드타임 관측 리포트 — 수치를 보려고 사람이 돌린다') 면제 항목 수와, 그 '사람'에게 돌리라고 지시하는 런북·주기 문서가 0건임을 재확인한다(docs/ 전수 grep). 반증되면 범위를 재조정한다"
  - "② 수취인 선언 계약 — 각 면제 항목이 수취인을 선언하게 한다: runbook:<경로#앵커> / report-schedule:<주기> / pending-task:<id>. 선언 없는 항목은 미분류로 exit 1. pending-task:<id>는 backlog/tasks/<id>.yaml이 실재하고 status != done일 때만 유효하고 done인데 남아 있으면 red(면제를 걷으라는 신호)"
  - "③ 계약 코드 재사용(재구현 0) — OPS-29가 ci.yml 비차단 스텝에 이식하는 분류·만료 계약(ARCH-25 GrandfatherEntry 유래)을 재구현하지 않고 그대로 재사용한다. 같은 만료 판정을 세 곳에서 굴리면 이중 진실원천이다. OPS-29 착지 후 대상 축(면제 사전)만 추가하는 것이 이 태스크의 실체다"
  - "④ 변별력(양방향) + 범위 밖 동결 — 수취인 선언 없는 항목 1건을 주입하면 red, 되돌리면 green을 둘 다 실측한다. 착지는 tests/infra/(infra-contracts 잡이 전량 실행하므로 CI 배선 확인은 그 사실 확인으로 충족). 범위 밖: 면제된 리포트들을 CI 상시 실행으로 승격하지 않는다(비용·라이브 DB 의존). OPS-29(ci.yml 스텝 축)를 대신하지 않는다"
artifacts: []
paths:
  - src/backend/whymath_backend/ops/declared_unwired_audit.py
  - "tests/infra/**"
session: null
notes: "docs/architecture/ai_recommendation_module_gap_review_r3.md §3 D8. REC-01 acceptance ④가 요구한 'CI 배선 실재 확인'의 답이 by-design 면제(_OFFLINE_REPORT)였는데 면제의 전제인 수취인이 실재하지 않는다 — ops.recommendation_reach_report를 포함한 면제 항목 전부에 런북·주기·만료가 0이다(argparse CLI는 실재하나 그것을 돌리라고 적힌 문서가 없다). CLAUDE.md '만료 없는 유예·제외 금지'(2026-08-03) 직접 저촉. 추천축이 아니라 횡단 인프라축이라(면제 항목 중 추천 관련 2건뿐) 추천 전용의 좁은 태스크를 만들지 않고 infra-debt로 등재한다. 운영 r3 D9('정직 표기는 침묵 통과는 막지만 영구 미상환은 막지 못한다')의 면제 사전 평면 재발. 등재 경위: OPS-33으로 시도했으나 CLI가 원격 OPS-33-yaml-spec-unwired-audit-axis와 번호 충돌로 거부(HARN-10) → 우회하지 않고 제안 번호 OPS-34 수용."
updated: 2026-08-11

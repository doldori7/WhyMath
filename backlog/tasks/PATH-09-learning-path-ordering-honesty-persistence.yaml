# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: PATH-09-learning-path-ordering-honesty-persistence
title: "학습 경로 정렬 근거 영속 정직 표기 — assessment.recommended_path가 steps만 저장해 96.4% tiebreak_only가 스냅샷에서 판독 불가"
track: math-completion
stage: S3
subject: math
layer: backend
status: done
priority: 3
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "①현행 실측 고정 — capture가 저장한 recommended_path에서 정렬 근거를 복원할 수 없음을 재현하는 테스트를 먼저 세운다(결함 재현)"
  - "②정합 설계 본체 — capture 좌석이 path.ordering_basis·ordering_edge_count·has_cycle을 recommended_path JSONB 안에 동반 기록한다. 신규 컬럼·마이그레이션 0을 리뷰 게이트로 확인한다"
  - "③변별력 — steps가 완전히 동일하고 ordering_basis만 다른 fixture 2종에서 영속 산출물이 갈리는지 assert한다. 갈리지 않으면 실패다"
  - "④계약 안정·날조 방지 — 정렬 근거는 build_learning_path가 산출한 값을 그대로 옮길 뿐 재계산·보정하지 않는다. LearningPath 재호출 0을 확인한다"
  - "⑤노출 정합 — GET /v1/me/assessments(StudentAssessment) 응답에서도 그 근거가 판독 가능함을 확인하고, 예측 5필드가 여전히 응답 스키마에 부재함을 회귀로 동결한다"
  - "⑥범위 밖 명시 — 과거 행 백필·신규 컬럼·모바일 렌더(D5 태스크 소관)·PATH-03 전이 엣지·전역 경로는 이 태스크 범위 밖이다"
artifacts:
  - 2501f6ad
  - "https://github.com/doldori7/WhyMath/pull/818"
paths:
  - src/backend/whymath_backend/api/me.py
  - "tests/backend/api/**"
session: null
notes: "docs/architecture/learning_path_module_gap_review_r2.md §3 D4. v1 페이퍼 갭 P3('writer가 reader보다 먼저 가지 않는다')이 ASM-03 writer 착지로 넘어선 뒤의 상환. is_cycle_residual(자식 필드·발생률 0%)은 생존하고 ordering_basis(부모 필드·96.4%)만 소실되는 비대칭이 근거. SM-2/FSRS·next_review_at 컬럼 불채택(v1 §2-④), 점수·등급 예측 불채택(v1 §2-⑤·ASM-07)."
updated: 2026-08-11

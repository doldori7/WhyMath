# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ADMIN-01-operator-seat-grant-audit
title: "운영자 좌석 발급 경로 + 역할 변경 감사 — CONTENT_ADMIN 부여 경로 0건 해소"
track: infra-debt
stage: S4
subject: cross
layer: backend
status: done
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 현행 실측 고정 — CONTENT_ADMIN 사용자 0명 상태에서 CUD 6라우터 전건 403 재현(주장 확인 또는 반증 — 반증되면 범위 재조정)"
  - "② ops CLI(role_grant_cli) grant/revoke/list 동작 + 존재하지 않는 user_id·잘못된 역할명 거부(비0 종료코드+사유). HTTP 미노출"
  - "③ 부여/회수와 privacy_audit.role_change 감사 1행이 동일 트랜잭션임을 동결(강제 롤백 시 감사 행도 함께 사라짐을 실측)"
  - "④ CI 배선 실재 확인 — 신규 테스트가 실제로 CI에서 실행되는지 확인(OPS-03·OPS-10)"
  - "⑤ 변별력 — 부여 전 403 → 부여 후 200/201 → 회수 후 다시 403의 3상태 왕복 실측(같은 값이면 위장)"
  - "⑥ 범위 밖 동결 — 관리자 HTTP API(/v1/admin/*)·콘솔 UI·SYSTEM_ADMIN 역할 신설은 포함하지 않는다(§5-①)"
artifacts:
  - "#786"
  - 8924a2e2
  - e6c7d9a1
paths:
  - "src/backend/whymath_backend/ops/*"
  - src/backend/whymath_backend/privacy/audit.py
  - src/backend/whymath_backend/schema/enums.py
  - "tests/backend/ops/**"
session: null
notes: "docs/architecture/operations_platform_gap_review.md §3 D1. require_content_admin(api/_auth.py:123)이 CUD 6라우터를 지키나 Role.CONTENT_ADMIN 부여 경로가 저장소 전체 0건(.role= 대입 grep 무일치, api/auth.py:146 resolve_user가 role kwarg 없이 생성). AuditEventKind(schema/enums.py:1114)에 role_change 어휘 없어 부여를 만들어도 기록할 곳이 없음. 반복 실수 7회차(§6) — 만들고 인가 통로를 잇지 않음."
updated: 2026-08-11

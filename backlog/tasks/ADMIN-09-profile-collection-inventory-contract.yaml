# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ADMIN-09-profile-collection-inventory-contract
title: "프로필 수집 항목 대장 부재 — user_profile PII 컬럼의 수집·목적·소비처 대장 + ORM↔대장 동기 게이트"
track: infra-debt
stage: S4
subject: cross
layer: backend
status: todo
priority: 2
eos_priority: P1
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 현행 실측 고정 — data/access_matrix.json(COLLAB-01)은 학습 데이터 9항목의 노출 해상도만 계약하고 user_profile의 수집 항목은 다루지 않음을 재현. gender(user.py:87 sa.String(32))·school_region(:92)·school_id(:93)가 ORM·Pydantic(schema/user.py:126,139,145)에 실재하는데 ⑴ API 수집 경로 0 ⑵ 런타임 read/write 0 ⑶ pipa_data_matrix.md 미등재 ⑷ access_matrix.json 미등재임을 grep으로 고정"
  - "② data/collection_inventory.json 신설 — user_profile 전 컬럼 × {수집 여부·수집 경로(엔드포인트 또는 none)·목적·런타임 소비처·보존 계획·PII 등급}. 값을 날조하지 않는다: 소비처가 없으면 none으로 적고 그것이 정직한 상태다(가짜 목적 문구 금지)"
  - "③ 거버넌스 테스트 — ORM UserProfile의 컬럼 집합 ↔ 대장 키 집합 동기 강제(tests/backend/schema/test_access_matrix.py 패턴 답습). 신규 컬럼이 대장 등재 없이 추가되면 실패한다. 대장에만 있고 ORM에 없는 유령 항목도 실패"
  - "④ 변별력 — 컬럼을 하나 추가(또는 대장에서 하나 제거)했을 때 그 테스트가 실제로 실패하는지 양방향 확인. 성공/실패 양쪽에서 같은 결과면 검증이 아니라 위장이다(2026-07-17 delay:true 선례). 뮤테이션 원복은 cp 백업으로만"
  - "⑤ CI 배선 실재성 — 신규 테스트가 실제로 CI backend 잡에서 실행되는지 확인(OPS-10 — '저장소에 존재함'과 '돌아감'은 다르다)"
  - "⑥ 범위 밖 — 법적 근거·목적 문구의 법률적 확정은 MGMT-02(변호사 검토) 소관이다(CLAUDE.md 법령 유래 절차의 기계 대체 금지). 이번은 기계 대장 + 동기 강제까지. 컬럼 드롭·수집 시작·pipa_data_matrix §2.2 9항목 표 확장도 범위 밖(전자는 ADMIN-02, 후자는 학습 데이터 축)"
artifacts: []
paths:
  - data/collection_inventory.json
  - src/backend/whymath_backend/db/models/user.py
  - "tests/backend/schema/*"
  - docs/legal/pipa_data_matrix.md
session: null
notes: "docs/architecture/operations_platform_gap_review_r2.md §4 D7 — v1이 보지 않은 평면. pipa_data_matrix.md §3.2가 '동의 시 수집 항목·이용 목적·보유 기간·제3자 제공 여부를 명확히 고지(PIPA 고지 의무)'를 명령하는데 그 수집 항목 목록의 진실 원천이 코드·문서 어디에도 없다. §1 '데이터 최소 수집' 원칙이 기계로 검증되지 않는 상태이며, MGMT-02(처리방침 문안·blocked)가 참조할 대장이 없다. ADMIN-02 (c)(school_region·gender 처분)의 선결 — 처분 근거는 이 대장이 있어야 성립한다.\n\n[정정 2026-09-01] eos_priority null → P1: 판정표 R/I3 — 12월 검증 관여(KPI·계약·앵커 자산). EOS-80 일괄 백필"
updated: 2026-09-01

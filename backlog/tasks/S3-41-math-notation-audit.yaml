# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: S3-41-math-notation-audit
title: "수식 표기 전수감사(모바일+shared/math) — S3-24 버킷B⑦"
track: math-completion
stage: S3
subject: math
layer: mobile
status: done
priority: 2
owner: claude
depends_on:
  - S3-39-math-notation-rendering
requires_gates: []
acceptance:
  - "[회수 — 원본 SHA는 GitHub API로 접근 가능(2026-08-10 전제 정정)] 앱 전체에서 수식 표기가 렌더러 미지원 문법으로 깨지는 곳이 없는지 전수 점검하는 감사 로직(정적 스캔 또는 런타임 검증)을 현재 main 기준으로 새로 설계한다. 착수 전 필수: NS-03(notation_support_manifest.json, Stage1에서 이미 회수 완료)이 이 항목과 얼마나 겹치는지 먼저 확인 — 겹치면 이 태스크는 NS-03 위에 얹는 증분만, 안 겹치면 독립 신규"
  - flutter analyze 0 · flutter test green
  - "감사 결과가 빌드/CI에서 실제로 확인 가능한 형태(테스트 또는 게이트)로 남는지 확인 — 감사만 하고 강제 없는 산출물 금지"
artifacts:
  - "수식 표기 전수감사 회수 완료 — 원 커밋 bcb5eeb(원 S3-23)를 GitHub API full_patch로 받아 이식. NS-03과의 경계 확인: NS-03은 커버리지 *게이트*(manifest 소비측), 이 태스크는 정규화 *로직*(생산측) — 겹침 없음. 내용: ①math_notation.dart에 flutter_math_fork 0.7.4 미지원 매크로 정규화 8종(\\doubleprime/\\dprime/\\backdoubleprime→{\\prime\\prime}, \\trprime→3중, \\differentialD→\\mathrm{d}, \\mleft/\\mright→\\left/\\right, \\abs/\\norm, \\placeholder 제거, 괄호형 강세 16종 복원 — 전부 (?![a-zA-Z]) 경계가드) + normalizeUnicodeMath 신설(유니코드 상첨자 ⁰~ⁿ·프라임 ′″‴→캐럿 LaTeX — 실기기 Kiki #2 '이계도함수 f″ 깨짐'의 코퍼스측 원인 해소) ②math_text.dart 세그먼트 전 normalizeUnicodeMath 선적용 ③test/math_notation_audit_test.dart 202줄(원본 그대로) — 매크로/유니코드 순수함수 + 팔레트 4탭 위젯 렌더 + fail-closed 폴백 + 한글 무회귀. acceptance③(빌드/CI 강제): 신규 테스트가 flutter test 스위트에 포함돼 CI mobile 잡이 상시 실행. 검증: 구현 diff를 원본 패치와 정규식·치환 순서까지 직접 대조(일치), 결함주입 5종 실행(doubleprime 정규화·유니코드 프라임/상첨자·미지원 매크로 불변·네거티브 컨트롤 isNot 반증) 전부 변별력 확인, flutter analyze 0, flutter test 352→378 all passed(+26, 원본 보고 신규 26건과 일치). 참고: 검증을 워크플로 독립 에이전트에 맡겼으나 세션 재개로 중단돼 최종 검증은 메인 세션이 직접 수행(구현 에이전트 자기보고 '원본과 정확히 일치'를 diff 대조로 재확인)."
paths:
  - "src/mobile/lib/**"
  - "src/mobile/lib/shared/math/**"
session: null
notes: "[2026-08-09 재조사] 원 소스 브랜치 claude/shadow-data-s3-pilot-nh5kbz가 origin에서 완전히 삭제됨(git fetch origin <브랜치명> → 'couldn't find remote ref', git ls-remote --heads origin 전체 43건 스캔에도 부재). 원 커밋 해시(fa08081·73b77a1·75c3dda 등) 직접 fetch도 전부 실패 — GitHub가 reachable하지 않은 SHA는 노출하지 않음. 즉 '원본 diff를 main 위에 수동 재적용'이라는 acceptance 전제 자체가 이제 불가능 — 실행 가능한 대안은 title/notes의 짧은 설명만으로 처음부터 다시 설계·구현하는 것뿐인데, 이는 '포팅'이 아니라 '재설계'라 원 기능의 정확한 범위·UX 디테일을 보장할 수 없다(디자인 판단 위험). 이 기능들을 그대로 재구현할지, 범위를 다시 정의할지, 혹은 지금 시점에 더는 필요 없다고 보고 폐기할지는 Kiki 판단이 필요 — /drive 안전장치(acceptance 판단 불가·모호성에서 정지)에 해당해 차단. [2026-08-10 전제 정정 — 원본은 접근 가능하다] 이 태스크의 acceptance를 '원본 diff 없음 → 재설계'로 쓴 것은 **내 오판이었다**. 원 브랜치가 origin에서 삭제된 것은 사실이나, 그로부터 git fetch가 실패한다는 이유만으로 '원본 접근 전면 불가'로 단정했고 다른 접근 경로를 시험하지 않았다. 실제로는 **GitHub API로 원 커밋에 완전히 접근된다**(repos/<owner>/<repo>/commits/<sha> — 메시지·파일목록, detail=full_patch로 유니파이드 diff까지). 2026-08-10 실측 확인했고, main도 #748에서 '회수 경로는 GitHub API 고정'으로 정본화했다. ⑦ 원S3-23 원 커밋 SHA: bcb5eeb · c8fff19. 따라서 이 항목은 '처음부터 재설계'가 아니라 **원본을 읽고 현재 main 위에 이식하는 회수**로 다루라 — 원본의 UX 디테일·테스트를 그대로 살릴 수 있다."
updated: 2026-08-10

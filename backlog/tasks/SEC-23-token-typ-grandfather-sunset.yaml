# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: SEC-23-token-typ-grandfather-sunset
title: "토큰 typ 그랜드파더 만료 계약 — 'typ 없는 액세스 토큰 허용'이 일몰 조건 없이 열려 있음"
track: infra-debt
stage: S4
subject: cross
layer: backend
status: todo
priority: 4
eos_priority: P2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① _decode_token(security.py:75)에서 액세스 경로도 typ == 'access'를 요구하도록 필수화 — 'typ 없는 기존 액세스 토큰은 허용'(security.py:29 주석·:75-80 docstring) 제거."
  - "② 만료가 이미 자명함을 근거로 남긴다 — 액세스 TTL 24h(config.py:437)이므로 typ 도입 커밋 + 24시간이면 typ 없는 토큰은 물리적으로 존재할 수 없다. 이 근거를 주석으로 남겨 미래 세션이 '호환 깨뜨림'으로 오인하지 않게 한다."
  - "③ 동결 테스트 — typ 없는 토큰이 액세스로 거부됨(401)을 회귀로 고정하고, 리프레시 오용 거부 기존 계약이 유지됨을 함께 확인."
  - "④ 변별력 — 필수화를 되돌리면 그 테스트가 실제로 실패하는지 확인."
  - "⑤ 범위 밖: 액세스 TTL 15분 단축·토큰 denylist(v1 §5-⑧ — r2 §5에서 선결 조건 MOB-12 충족은 확인됐으나 발화는 D8·D9 착지 후 재판정)."
artifacts: []
paths:
  - src/backend/whymath_backend/security.py
  - tests/backend/test_security.py
session: null
notes: "docs/architecture/account_security_gap_review_r2.md G4→D11. 실측: security.py:29 '후방호환: typ 없는 토큰은 액세스로 간주' + _decode_token(:75-80) docstring 'expected_type==access면 typ가 refresh만 거부한다(typ 없는 기존 액세스 토큰은 허용)'. 일몰 조건이 없다. 실질 위험은 낮다(우리가 발급하는 모든 토큰에 typ가 있다) — 그래서 priority 4다. 그러나 CLAUDE.md '만료 없는 유예·제외 금지'(2026-08-03: 유예는 반드시 만료 또는 재확인 지점을 동반한다)에 정면 해당하고, 그 규칙의 1차 집행은 산문이 아니라 코드여야 한다(PB-02 그랜드파더 만료 계약 선례).\n\n[정정 2026-09-01] eos_priority null → P2: 판정표 N/이월 — 2027 이월(폐기 아님·선언 §6-5). EOS-80 일괄 백필"
updated: 2026-09-01

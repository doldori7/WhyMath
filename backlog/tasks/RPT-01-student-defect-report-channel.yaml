# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: RPT-01-student-defect-report-channel
title: "학생 결함 신고 채널 — 문항·AI응답 오류를 학생이 알릴 경로가 스키마·API·UI 전부 0"
track: infra-debt
stage: S3
subject: cross
layer: backend
status: in_progress
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "append-only 신고 테이블 1건 적재(카테고리 enum 3~6종 + problem_id 등 대상 참조만 — user_id·자유서술 미보유를 시그니처·테스트로 동결, db/models/audit.py의 DeletionAudit/PrivacyAudit append-only 패턴 답습, attempt_event 재사용 금지)."
  - "Flutter 버튼 1개(유일한 생산자) → POST 1개 → 적재 왕복을 실측(버튼 1회→1행 적재, 철회 경로 있으면 0행 복귀)."
  - "CLI 리포트가 '수집 경로 미배선'과 '0건 접수'를 다른 값으로 표시(이중 회계) — harness/qa_pipeline.py(ARCH-21) 기존 7축과 별개로 합류하는 8번째 입력으로 설계."
  - "범위 밖 동결: 자유서술·첨부·티켓·배정·SLA·챗봇·회신·만족도·admin 콘솔은 만들지 않는다(docs/architecture/service_operations_gap_review.md D1 §2-⑤~⑦ 참조)."
artifacts: []
paths:
  - "src/backend/whymath_backend/db/models/**"
  - "src/backend/whymath_backend/api/**"
  - "src/backend/whymath_backend/harness/**"
  - "src/mobile/lib/features/**"
  - "src/backend/alembic/versions/**"
session: claude/rpt-01-student-defect-report
notes: "docs/architecture/service_operations_gap_review.md §3 D1 — 외부 EOS 틀 모듈 94(고객지원·오류신고) 대조 최대 갭. 학생이 문항·AI응답·수식 오류를 신고할 경로가 라우터 16종·DDL 28테이블 전수에 0건(2026-08-03 실측)."
updated: 2026-08-03

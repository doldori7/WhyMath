# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: MOB-10-diagnosis-evidence-render
title: "진단 결과 렌더 착지 — 이미 클라 메모리에 적재된 개념 진단을 숙달 상태 라벨로 노출(서버 신규 계산 0)"
track: math-completion
stage: S3
subject: math
layer: mobile
status: done
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 서버 — GET /v1/me/diagnosis/concepts 응답에 숙달 상태 라벨 필드 추가. mastery_to_level(l4/lthc/adapt.py:127, '초보'/'발전 중'/'숙달') 재사용(신규 계산 0). 클라가 임계값(0.4·0.8)을 계산하면 전역 UI 불변식 #1(표현≠의미) 위반이므로 라벨은 반드시 서버 산출"
  - "② 클라 — me_screen.dart:34-38 '진단 결과' 타일 placeholder(:82 리터럴 '준비 중') 해소. DiagnosisState.diagnoses(diagnosis_state.dart:14-23·이미 적재됨)를 개념명+상태 라벨로 렌더. 신규 엔드포인트 호출 0"
  - "③ 원시 BKT 확률·θ 숫자 노출 금지 — 상태 라벨과 자기 대비 서술만(숫자는 서열 신호로 읽힘·5원칙 #2). θ는 학생에게 의미 전달 안 됨"
  - "④ 정렬을 순위로 제시하지 않는다 — '약한 개념부터'는 학습 순서이지 등수가 아니므로 번호·등급·상위 N% 표기 금지"
  - "⑤ anti_gamification_governance_test.dart(금지 8정규식·:29-38) 통과 + 스캔 무력화 방지 하한 유지"
  - "⑥ 차트 금지(v0) — fl_chart는 MOB-09로 제거됐고 재도입은 pubspec_dependency_usage_governance_test.dart:134가 선언↔사용 동시 착지를 강제한다. v0은 텍스트·라벨로만 착지하고 차트는 별도 슬라이스"
  - "⑦ CI 배선 실재 확인 — 모바일 잡이 실제로 이 테스트를 수집하는지(ci.yml changes 필터 src/mobile/ 경로) 확인"
  - "⑧ 범위 밖 동결 — '학습 경로' 타일(PATH-02가 정렬 근거 정직 표기를 선행 소유)·/growth-evidence 클라 배선(PED-08 선행)·problem_screen 재설계·설정 타일"
artifacts:
  - 5988c5bd
paths:
  - src/mobile/lib/features/profile/presentation/me_screen.dart
  - "src/mobile/lib/features/problems/**"
  - src/backend/whymath_backend/api/me.py
  - "src/mobile/test/**"
session: null
notes: "docs/architecture/gamification_module_gap_review_r2.md §6 D6. 반복실수 9회차 '도달했는데 렌더 0' 신규 형태 — DiagnosisController.load()가 문제 로드마다 3콜(next-problem→problems/{id}→diagnosis/concepts, diagnosis_controller.dart:7-8,32,59)을 하고 결과가 DiagnosisState에 전량 담기는데(nextProblem: θ·standard_error·measurement_sufficient / diagnoses: ConceptDiagnosisItem 9필드·problem_models.dart:63-91), problem_screen.dart:60-83의 _buildBody는 isLoading·error·noCandidate·problem 4개만 읽는다. v1 §6 7회차 '결정 부재' 판정의 정밀화(§4-③) — 네트워크 갭이 아니라 렌더 갭이라 서버 신규 계산 0·신규 엔드포인트 0으로 착지 가능한 유일한 성장 증거 축."
updated: 2026-08-08

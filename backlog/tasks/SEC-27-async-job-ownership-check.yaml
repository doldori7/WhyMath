# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: SEC-27-async-job-ownership-check
title: "비동기 작업(/v1/jobs/{id}) 폴링에 사용자 소유권 검사 추가"
track: infra-debt
stage: S3
subject: cross
layer: backend
status: todo
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "job 테이블 또는 작업 메타데이터에 user_id(또는 actor_id) 소유자 필드를 추가하고, 작업 생성 시점에 기록한다"
  - "GET /v1/jobs/{id} 폴링 엔드포인트에서 인증된 사용자가 해당 job의 소유자인지 서버측에서 검사한다"
  - "매핑이 없거나 소유자 불일치 시 404(리소스 존재 자체도 노출하지 않음) 또는 403으로 거부한다"
  - "인가 회귀 테스트를 추가한다: 소유자 ALLOW, 다른 사용자 DENY, 미인증 DENY"
artifacts: []
paths:
  - src/backend/whymath_backend/app.py
  - src/backend/whymath_backend/l3/queue/celery_job_queue.py
  - "src/backend/whymath_backend/db/models/job*.py"
  - "src/backend/alembic/versions/**"
  - tests/backend/test_app.py
  - "tests/backend/api/test_jobs*.py"
  - "tests/infra/**"
session: null
notes: "48_보안 §P0 객체 수준 인가 갭. 실제 job 생성/폴링 경로는 app.py와 celery_job_queue.py에 있으며, 현재 인증은 있으나 job ↔ user 매핑 및 소유권 검사가 없다. acceptance의 구체적 모듈은 착수 첫 단계에서 현행 코드 실측 후 재조정해도 된다."
updated: 2026-08-25

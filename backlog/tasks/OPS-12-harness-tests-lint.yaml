# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: OPS-12-harness-tests-lint
title: "tests/harness lint 배선 — 선재 ruff 오류 2건 미노출 (OPS-11의 harness 판박이)"
track: infra-debt
stage: S4
subject: cross
layer: infra
status: todo
priority: 4
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "harness-integrity 잡에 ruff·black 스텝 추가 — 현재 tests/harness를 lint하는 CI 잡이 없다"
  - "선재 위반 정리 — ruff F541 2건(test_cli.py:704·722 f-string 자리표시자 없음) 실측"
  - "배선 동결 — 스텝 제거 시 실패하는지 변별력 실측 (OPS-11의 test_infra_lint_wiring 패턴 재사용)"
artifacts: []
paths:
  - ".github/workflows/ci.yml"
  - "tests/harness/**"
session: null
notes: "HARN-08 구현 중 발견. OPS-11이 tests/infra에 대해 고친 것과 동일 구조가 tests/harness에 남아 있다 — backend 잡은 tests/backend만 보고 harness-integrity 잡엔 lint 스텝이 없다. CLAUDE.md '검증 장치를 만들고 배선 확인 없이 완료 선언 금지'의 여섯 번째 사례."
updated: 2026-07-27

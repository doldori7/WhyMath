# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: OPS-52
title: "ROCm 7.2 / standalone llama.cpp 시도 — Ollama 번들 대비 +20% 이상이면 배선 변경 검토"
track: infra-debt
stage: S3
subject: math
layer: backend
status: todo
priority: 2
eos_priority: P2
owner: claude
depends_on:
  - OPS-51
requires_gates: []
acceptance:
  - "① ROCm 7.2 SDK + standalone llama-server 빌드/설치 경로를 Windows Phaiakes9에서 실측 — 가능한 경로: (a) AMD 공식 ROCm 7.2 Windows installer (b) llama.cpp release 바이너리 + HIP SDK (c) WSL2 ROCm(메모리 제약 문서화)"
  - "② standalone llama-server를 OpenAI 호환 API로 띄우고, WhyMath L3 라우터를 경유하지 않는 isolated 벤치로 dense 27B / MoE 30B-A3B의 t/s·prefill·왕복 지연 측정"
  - "③ Ollama 번들(현 확정) 대비 standalone이 왕복 지연에서 +20% 이상 이득이면 라우터에 신규 provider 경로 설계 태스크로 승격 — 미만이면 Ollama 유지 결정 문서화"
  - "④ 시도 불가(설치 실패·gfx1151 미지원·WSL 메모리 한계 등) 시에도 '불가'의 근거와 재현 명령을 docs/ops/amd395_local_llm_performance.md §6 또는 별도 런북에 기록"
artifacts: []
paths:
  - "scripts/ops/**"
  - docs/ops/amd395_local_llm_performance.md
  - src/backend/whymath_backend/l3/providers/ollama.py
  - src/backend/whymath_backend/l3/router.py
session: null
notes: "docs/ops/amd395_local_llm_performance.md §3 L4·§6 Phase 6 후속. 현재 Ollama 번들 ROCm 사용 중. ROCm 7.2 문서가 §8에 인용돼 있으나 실제 시도는 안 됐다. WSL2의 .wslconfig 메모리 제약은 이미 문서에 언급됨. +20% 규칙은 §3 L4에서 정의됨.\n\n[정정 2026-09-01] eos_priority null → P2: 판정표 N/이월 — 2027 이월(폐기 아님·선언 §6-5). EOS-80 일괄 백필"
updated: 2026-09-01

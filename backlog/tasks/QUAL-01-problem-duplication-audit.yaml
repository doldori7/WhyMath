# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: QUAL-01-problem-duplication-audit
title: "문항 수준 중복 감사 — 파일럿 풀 실중복 1쌍 + 은행 간 slug 충돌 392행 가시화"
track: infra-debt
stage: S3
subject: math
layer: backend
status: todo
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 3계층 판정 신설(T1 은행 간 slug 충돌·T2 발문 완전 동일·T3 verify 서명 동일). T3는 **게이트 금지·분포 관측만**이며, 반례(wm-skel-f50f96b5a691 이차방정식 ↔ wm-calc-ext-8c193941df77 삼차함수 극값 — 도함수 공유로 verify.conditions가 동일)를 모듈 도크스트링에 slug와 함께 박아 둔다. 이게 없으면 다음 세션이 'SymPy 동치로 중복 잡자'는 틀린 탐지기를 만든다"
  - "② 파일럿 실중복 1쌍 명명 보고: wm-quad-eq-larger-root(problem_bank_v1) ↔ wm-skel-92cd1ba2bbf5(generated_v0) — 발문·answer='3'·difficulty=2.0 동일인데 slug가 달라 seed_demo._CORPORA가 양쪽을 다 적재한다. 해소(어느 쪽 은퇴)는 콘텐츠 판정이라 범위 밖 — **가시화까지**"
  - "③ 기존 392(generated_v0↔rephrased_v0)·278(발문 동일 중 같은 근원)은 pending-task:S4-14 유예. 신규 충돌·유예 만료(S4-14가 done/부재)는 exit 1"
  - "④ seed_demo._CORPORA에 코퍼스 추가 시 ON CONFLICT(slug) DO UPDATE 덮어쓰기 예상 건수를 사전 보고(리포트로 끝내지 않는 소비처). populate.py는 배치 내부만 dedup하고 배치 간 덮어쓰기는 침묵한다"
  - "⑤ qa_pipeline 8번째 축(problem_duplication)으로 배선 — 새 잡·새 스텝 신설 0(ARCH-21 좌석 재사용·in-process import·subprocess 금지). CI가 실제로 실행하는지 확인('존재함 ≠ 돌아감')"
  - "⑥ 변별력 양방향 실측: 현 코퍼스 그대로 exit 0(전부 유예) / 실중복 1쌍을 유예에서 빼면 exit 1 / 임의 문항 slug를 타 은행 slug로 바꾸면 exit 1 / S4-14를 done으로 바꾸면 exit 1. 성공·실패가 같은 값을 내면 위장이다"
artifacts: []
paths:
  - src/backend/whymath_backend/harness/problem_duplication_audit.py
  - src/backend/whymath_backend/harness/qa_pipeline.py
  - scripts/demo/seed_demo.py
  - tests/backend/harness/test_problem_duplication_audit.py
  - "tests/infra/*"
session: null
notes: "docs/architecture/data_platform_module_gap_review.md §3 D3(외부 틀 모듈 85① '중복 문제' 대조). 코퍼스 전수 집계 실측(2026-08-03): 7종 2647문 중 은행 간 slug 충돌 392·발문 완전 동일 279그룹/566레코드(278 generated↔rephrased·1 v1↔generated). 은행 내부 중복은 0. 생성 시점 가드(find_near_duplicates→l3/equivalent/orchestrator.py:273)는 생성 오케스트레이터 경로만 덮고 은행 간·populate 적재 시점은 무방비. 초고가 이를 '✅ 충족'으로 오판했다가 전수 집계로 정정 — '방어 코드 존재'를 '위험 부재'로 읽은 오류."
updated: 2026-08-03

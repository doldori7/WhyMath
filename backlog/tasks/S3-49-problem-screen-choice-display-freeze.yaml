# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: S3-49-problem-screen-choice-display-freeze
title: "객관식 세로 번호목록 UI(모바일) — S3-24 버킷B①"
track: math-completion
stage: S3
subject: math
layer: mobile
status: done
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "[재구현 — 원본 diff 없음, Kiki 승인 2026-08-09] 객관식 문제의 세로 번호목록 UI(1·2·3·4·5 형태로 세로 나열되는 선택지 표시)를 현재 main 코드 기준으로 새로 설계·구현한다. 원본 커밋(73b77a1/5481086, 소실)의 정확한 시각 디자인은 재현 대상이 아니다 — '세로 번호목록'이라는 UX 골자만 승계, 세부 스타일은 현재 main의 디자인 토큰(MOB-09~13)에 맞춰 새로 결정"
  - flutter analyze 0 · flutter test green
  - "정답·정오 강조 없음(CLAUDE.md 절대 금기) — 선택 여부만 표시, 정오 색상/아이콘 등 금지"
  - "S3-45(버킷C④, 객관식 선택지 버튼 UX)와 범위 겹침 가능 — 착수 전 그 태스크와 조정"
artifacts:
  - "판정: 객관식 세로 번호목록 UI는 이미 main에 완전히 구현돼 있었다 — problem_screen.dart _ProblemView가 choices를 for 루프로 각각 독립 Padding+Text에 세로로 쌓고, _circledNumber(i+1)로 ①~⑨ 라벨(10 이상은 괄호 숫자)을 붙이며, 정오 강조 없이 중립 bodyLarge로 렌더한다. 즉 acceptance①의 UX 골자(세로 번호목록)·③(정오 강조 없음)이 코드상 이미 충족 — S3-37과 동일하게 '원본 소실로 재설계 필요'라 봤던 항목이 실제로는 main에 이미 들어와 있던 사례(2건째). 다만 **어떤 테스트도 이 UI를 동결하지 않아** 가로 배치(Wrap/Row) 전환·번호 라벨 제거·정오 색 차등이 전부 조용히 통과할 수 있는 상태였으므로, 회귀 방지 테스트 4건을 problem_screen_test.dart에 신설: ①①②③ 번호 부착 ②세로 배치(y좌표 증가 단언 — Wrap/Row 회귀를 실제로 잡는다) ③보기 없는 주관식엔 번호 없음(변별력 반대 사례) ④보기끼리 색이 갈리지 않고 error 롤도 아님(정오 차등 강조 금기). 검증 과정 실측: M3 textTheme.bodyLarge는 기본 onSurface 색을 이미 갖고 있어 'style.color == null' 단언은 성립하지 않는다 → '보기 간 색 동일성'이라는 실질 불변식으로 교정(레드 1회 경유). S3-45(버킷C④)와의 경계 확정: S3-35=보기의 *표시 방식*(세로 번호목록·본 태스크 완료), S3-45=보기의 *선택 상호작용*(탭 가능 버튼화) — 서로 다른 축이라 중복 아님, S3-45 notes에 기록. 검증: flutter analyze 0 issues, flutter test 295/295 all passed(신규 4건 포함, 직전 기준선 291에서 순증)."
paths:
  - "src/mobile/lib/**"
session: null
notes: "[2026-08-09 재조사] 원 소스 브랜치 claude/shadow-data-s3-pilot-nh5kbz가 origin에서 완전히 삭제됨(git fetch origin <브랜치명> → 'couldn't find remote ref', git ls-remote --heads origin 전체 43건 스캔에도 부재). 원 커밋 해시(fa08081·73b77a1·75c3dda 등) 직접 fetch도 전부 실패 — GitHub가 reachable하지 않은 SHA는 노출하지 않음. 즉 '원본 diff를 main 위에 수동 재적용'이라는 acceptance 전제 자체가 이제 불가능 — 실행 가능한 대안은 title/notes의 짧은 설명만으로 처음부터 다시 설계·구현하는 것뿐인데, 이는 '포팅'이 아니라 '재설계'라 원 기능의 정확한 범위·UX 디테일을 보장할 수 없다(디자인 판단 위험). 이 기능들을 그대로 재구현할지, 범위를 다시 정의할지, 혹은 지금 시점에 더는 필요 없다고 보고 폐기할지는 Kiki 판단이 필요 — /drive 안전장치(acceptance 판단 불가·모호성에서 정지)에 해당해 차단."
updated: 2026-08-10

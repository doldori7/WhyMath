# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: OPS-11-infra-tests-lint
title: "tests/infra lint·format CI 미배선 상환 (OPS-10 부수 발견·같은 부류 누수)"
track: infra-debt
stage: S4
subject: cross
layer: infra
status: todo
priority: 4
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "infra-contracts 잡에 ruff·black 스텝 추가 — 현재 tests/infra를 lint/format하는 CI 잡이 없다"
  - "기존 위반 정리 — black --check가 test_backup_script.py 1건에서 실패(선재·실측)"
  - "배선 자체를 동결 — 스텝 제거 시 실패하는지 변별력 실측"
artifacts: []
paths:
  - ".github/workflows/ci.yml"
  - "tests/infra/**"
session: null
notes: "OPS-10 구현 중 발견. tests/infra는 OPS-03이 infra-contracts로 '실행'은 배선했으나 lint/format은 여전히 어떤 잡도 하지 않는다 — backend 잡은 ../../tests/backend만 본다. '장치는 있는데 일부만 배선됨'의 또 다른 사례."
updated: 2026-07-27

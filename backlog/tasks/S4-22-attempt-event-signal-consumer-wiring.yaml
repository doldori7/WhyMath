# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: S4-22-attempt-event-signal-consumer-wiring
title: "attempt_event 관측 신호 소비 배선 — 답입력·시각화조작 producer-only 2종(OPS-22 발견·OPS-25로 막힘 제외)"
track: math-completion
stage: S4
subject: math
layer: backend
status: todo
priority: 3
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "답입력·시각화조작 2종 EventType은 S3-16/슬96-J에서 build_event_data 생산자가 배선됐으나(attempt_event에 실제로 적재됨), 그 값을 필터링해 읽는 소비자가 0건이다(OPS-22 declared_unwired_audit AST 실측 + OPS-25 상수 간접참조 해석 후 재실측으로 확정). 각 신호를 실제로 소비하는 지표/리포트/API 좌석을 최소 1곳 배선한다 — 답입력(server_latency_ms 분포 리포트), 시각화조작(visualization_reach_report 소비 축 추가)"
  - "새 EventType enum 신설 0(기존 2종의 소비자만 배선) — 학생 대면 응답 페이로드 무변경 회귀"
  - "완료 후 OPS-22 declared_unwired_audit 재실행 시 이 2종이 reached로 전환됨을 확인(그랜드파더 만료 계약상 이 태스크가 done되면 대장의 pending-task 항목도 함께 제거 — 방치 시 expired-waiver가 잡는다). 실 저장소 음성 대조 테스트(test_declared_unwired_audit.py::TestRealRepositoryConstantIndirectionDiscrimination)도 같은 사실을 단언하므로 함께 갱신한다"
artifacts: []
paths:
  - src/backend/whymath_backend/harness/wh1_evaluation.py
  - src/backend/whymath_backend/harness/visualization_reach_report.py
  - src/backend/whymath_backend/api/me.py
  - src/backend/whymath_backend/api/coach.py
session: null
notes: "OPS-22(선언≠배선 일반 탐지기) 구현 중 실측 발견 — S3-16이 생산자만 소생시키고(관측 신호로 적재) 소비자는 범위 밖으로 남겼다(S3-16 acceptance ②는 힌트요청/힌트제공만 지표 ⑫로 소비). 반복 실수(생산 좌석 완비 + 소비 미배선) 패턴의 축소판 인스턴스 — 즉시 해소보다 추적이 우선이라 유예(pending-task) 대상으로 등재. **2026-08-10 OPS-25 정정: 등재 당시 3종이었으나 막힘은 처음부터 오탐이었다** — l2/learning_metrics_rollup.py가 _SOCRATIC_EVENT_TYPES 상수로 파이썬 필터(:279)와 SQL 필터(:563)를 이미 걸고 있었고, 감사기가 ast.Compare 피연산자에 EventType.X가 직접 나타나야만 소비로 인정하던 한계로 못 봤다. 상수 1홉 해석을 넣어 해소했고 남은 실제 범위는 답입력·시각화조작 2종이다(음성 대조로 미도달 유지 확인)."
updated: 2026-08-10

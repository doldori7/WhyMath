# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ASM-12-export-prediction-seal-bypass
title: "GET /v1/me/export가 ASM-07 봉인 5필드를 학생에게 원시 반환 — 봉인이 엔드포인트 1개만 덮고 두 번째 학생 표면은 양방향 무가드"
track: infra-debt
stage: S3
subject: math
layer: backend
status: in_progress
priority: 1
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 현행 실측 고정(결함 재현 선행): privacy/export.py:93의 _EXPORT_PLAN이 내부 정본 Assessment를 싣고 _row_to_json(:190)이 to_schema().model_dump()로 필터 0임을, GET /v1/me/export(api/me.py:3017·ConsentedUser — /v1/me/assessments와 동일 학생 토큰 클래스)가 STUDENT_HIDDEN_PREDICTION_FIELDS 5필드를 페이로드에 싣음을 재현하는 테스트를 먼저 세운다. state_snapshots(UserStateSnapshot.estimated_percentile) 경로도 함께 고정 — 학생 도달 경로가 2개다"
  - "② 판정 선행(코드보다 먼저): 단순 버그가 아니라 법적 열람권 대 교수학적 비노출의 미조정 충돌이다. export.py 주석은 추론치 포함을 GDPR Art.15 본인 열람·이동권 근거로 의도적이라 명시하고, pipa_data_matrix §2.2 #8은 학생 본인조차 ◐ 요약만이며 §2.4가 제품 차원 차단까지 못박는다. 저장소에 이 충돌을 조정한 흔적이 0건이므로 어느 쪽이 이기는지를 근거와 함께 문서로 먼저 판정한다. 법령 판단 부분은 단정하지 말고 변호사 검토 대상으로 표기(기계 대체 금지)"
  - "③ 집행 지점(②와 별항): 판정 결과를 코드로 집행한다. 집행 형태는 런타임 필터가 아니라 ASM-07이 확립한 방식(필드의 부재 — 필터는 데코레이터 인자 한 줄이 빠지면 조용히 무력화되지만 필드 부재는 꺼지지 않는다·PED-08 ③ 선례)을 우선 검토한다. 열람권 유지로 판정되면 노출을 유지하되 그 사실이 access_matrix #8과 매트릭스에 반영돼야 한다 — 코드와 정본 중 하나만 고치고 끝내지 않는다"
  - "④ 변별력·양방향 가드: 봉인 5필드에 값을 넣은 fixture로 export 페이로드를 검사해 red 실측 후 조치 → green. test_prediction_field_sealing.py에 export 축을 편입하거나 export 테스트에 봉인 축을 편입해 양방향 무가드(현재 각각 참조 0건 — 실측)를 해소한다. 새 학생 표면이 생길 때 red가 나야 한다"
  - "⑤ 표면 전수 확인: /v1/me 라우트 전수에서 봉인 5필드를 나르는 다른 경로가 더 있는지 OpenAPI 스키마 도달성으로 확인한다(중첩 ref 추적 — test_prediction_field_sealing.py가 이미 쓰는 방식 재사용). export·assessments 외 0건임을 실측 기록하거나 있으면 함께 처리한다"
  - "⑥ 범위 밖: Assessment(내부 정본)에서 필드 폐기(ASM-02가 (d) 미채택)·예측 필드 writer 신설·교사/부모 노출 판정(Phase 3+)·pipa_data_matrix §2.2 표 기호 변경(§2.4 절차 소관)은 범위 밖. 단 ②의 판정이 표 개정을 요구하면 그 사실을 게이트로 등재한다"
artifacts: []
paths:
  - src/backend/whymath_backend/privacy/export.py
  - "tests/backend/privacy/**"
  - "tests/backend/api/**"
session: claude/unresolved-long-term-branches-ph1ad7
notes: "2026-08-11 협업 r3 자체 재검증에서 발견(서브에이전트 독립 실측 + 부모 세션 재확인: _EXPORT_PLAN:93 · _row_to_json:190 필터 0 · 봉인테스트의 export 참조 0건 · export테스트의 봉인필드 참조 0건). ASM-07(#732, 08-09)이 학생 대면 예측 5필드를 3층 봉인했으나 export는 그보다 먼저(#668, 08-06) 내부 정본 Assessment를 실었고 나중 봉인이 이 경로를 덮지 않았다 — r3 D7이 계약 파일에서 지적한 형태(먼저 있던 배선을 나중 결정이 무효화했는데 아무도 red로 못 받음)가 코드 축에서 반복. 현재 유출값은 null(estimated_percentile writer 0건)이라 라이브 피해 0의 잠재 결함이나, 봉인 설계 취지가 'writer가 붙는 순간'을 막는 것이므로 값 부재를 안전 근거로 쓸 수 없다. ★COLLAB-05 선행: r3 D7 설계 ②가 계약 #8을 summary→none으로 좁히라 하는데 export 실태가 full이라 그대로 집행하면 계약-실태 어긋남이 위험한 방향으로 벌어진다 — 이 태스크 ②의 판정이 COLLAB-05보다 먼저 와야 한다."
updated: 2026-08-11

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: S4-19-live-step-verification-event-persist
title: "라이브 3상태 단계 검증 결과 적재 — 검산결과 이벤트 additive 확장 + 지표 ① 이중 회계(binary 병기)"
track: math-completion
stage: S4
subject: math
layer: backend
status: todo
priority: 4
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 현행 실측 고정(2026-08-10): 검산결과 이벤트는 binary passed·error_kind만 적재(coach.py _log_verify_event :983-1042 — arithmetic_validator 재검산)·verify_solution 3상태(SolutionVerificationResult, l3/verify_solution.py:49-88 — n_correct/n_incorrect/n_unverifiable/n_transitions/unverified_ratio/first_incorrect_index·steps에 reason 텍스트 포함)는 응답에만 싣고 유실. attempt_event.event_data는 JSONB(activity.py:265)·계약 강제는 build_event_data 단일 seam(extra=forbid) — 신규 테이블·마이그레이션 0 성립. [등재 전제 정정] '핸들러가 이미 쥔 solution_verification 운반만'은 실측과 다름 — _build_response_payload(coach.py:596-599)가 코칭 노출 게이트(arithmetic_error 또는 focus∈{consolidate,retrieval})로 sol을 걸러 None을 반환하므로, 전단계-correct 제출·ocr_gated 케이스가 핸들러에 도달하지 않는다. 게이트 통과분만 적재하면 불합격 편향 회계가 되므로 적재는 게이트 이전 값(sol.solution_verification·sol.verification_ocr_gated — 후자는 verification 객체가 아닌 형제 필드, solution_coaching.py:227-247)을 운반한다. 재계산 0 원칙은 유지"
  - "② 계약 확장(additive): VerifyEventData(event_data_contract.py:36-62)에 n_correct·n_incorrect·n_unverifiable(int|None)·unverified_ratio(float|None)·first_incorrect_index(int|None)·ocr_gated(bool|None) — 전건 default None, S3-03 mode/persona 선례 3요소(Field(default=None)·description 'None=미지정·기존 동작 불변'·docstring 태스크 문단) 답습. None=구판 미기록 vs 0=실측 0 구분(S3-07 규약). n_transitions는 미적재 — 세 카운트 합==n_transitions 보장(verify_solution.py:53-54)으로 재구성 가능함을 주석 명시. 정확 dict 동등성 테스트 3건(test_event_data_contract.py:101-118) 갱신 + S3-16형 producer 정합 테스트 신설(:81-92 선례)"
  - "③ writer: 풀이 제출 턴은 verification 유무와 무관하게 기존 binary passed·error_kind 불변(이중 회계), verification이 있으면 3상태 카운트를 additive로 병기 적재(전이 0회 제출도 0으로 기록 — None과 구분). 비제출 턴 무적재 가드 불변 + '비제출 턴 payload 키 불변' 테스트 신설(현재 부재 실측). _build_response_payload 반환 확장 시 **마지막 원소=solution_coaching 불변식 보존**(7n9n72/S3-32 브랜치가 언패킹 전제를 명문화 — 끝이 아닌 위치에 삽입)·coach.py 변경은 두 핸들러 호출부 최소 표면으로 억제. 학생 풀이 원문·단계 텍스트·steps.reason 미포함 — model_dump 통짜 금지, 비식별 정수/비율/인덱스/불리언만(wh1_shadow.py:59-68 규약 동형)"
  - "④ reader 병기: verify_pass_rate 값·계산식 불변 + 3상태 파생(step_decision_rate·step_incorrect_rate — 정의는 solution_module_gap_review.md §5-3 D6) — 신판(3카운트 non-None)만 분모·구판 건수 분리 계상(S3-07 정직 회계, wh1_shadow_harvest.py:300-312 동형)·표본 0이면 NO_DATA(value None·'가짜 0 아님' note). 지표 ① note의 'unverifiable 미구분' 한계 서술을 갱신하고 동결 테스트(test_wh1_evaluation.py:329-330)를 짝으로 갱신"
  - "⑤ 노출 집행(별항 — 2026-08-04 헌법): '학생 대면 표면 0'과 SurrogateMetrics 필드 추가는 양립 불가임을 실측 고정 — GET /v1/me/harness-metrics(api/me.py:3063-3067)가 response_model=SurrogateMetrics로 전 필드를 학생 토큰에 자동 서빙하며 _STATIC_TIER는 이 라우트를 막지 않는다. 따라서 파생 지표는 SurrogateMetrics에 넣지 않고 별도 내부 산출(3상태 회계 dataclass — harvest transition_counts 선례 동형)로 병기하고 surrogate_baseline_report 내부 섹션으로만 렌더(정확한 함수 배치는 구현 재량·원칙: SurrogateMetrics와 학생 라우트 무변경). 학생 라우트 응답 페이로드 신규 키 0을 실바디 테스트로 동결(ASM-07 3층 동결 패턴)·growth_evidence_exposure 13필드 정확 집합 테스트(:102-122) green 유지"
  - "⑥ 변별력·CI·회귀: 신구 혼재 픽스처에서 파생 분모가 신판만 세고 구판 건수가 분리 표기되는지·게이트 이전 운반의 변별력(전단계-correct 제출과 ocr_gated 케이스가 실제 적재됨 — 게이트 이후 값으로 구현하면 red)·경계 케이스(전이 0 제출=카운트 0 적재). 검사는 CI 명령 그대로(ruff/black --line-length 100/mypy --strict/lint-imports/pytest)·판정은 exit code. 전체 스위트를 돌린 뒤 회귀 없음을 말한다 — 못 돌렸으면 '전체는 확인하지 못했다' 명시(CI 최종 판정). 테스트 경로는 기존 배선(testpaths) — 신규 배선 불요"
  - "⑦ 범위 밖: BKT 부분 크레딧 반영(D6-2단계 — S3-01/S3-04 측정 후 판정)·record_problem_attempt_mastery 무변경·학생 대면 지표 노출(⑤로 구조 차단)·S3-32(7n9n72) 서버검증 완료 판정 축과의 결합(별 태스크 — coach.py 병합 인접 충돌만 notes 기록)·신규 테이블/마이그레이션"
artifacts: []
paths:
  - src/backend/whymath_backend/schema/event_data_contract.py
  - src/backend/whymath_backend/api/coach.py
  - src/backend/whymath_backend/harness/wh1_evaluation.py
session: null
notes: "docs/architecture/solution_module_gap_review.md §5-3 D6(2026-08-03 2차 재검증) — 외부 EOS 틀 문서의 종단 엣지(AI 채점→학생 모델)가 1비트로만 닫혀 있던 갭. S3-07(shadow 전이 카운트)의 결정론 라이브 미러. NLP-02(답안 채점 shadow)·S4-11(힌트 콘텐츠)와 축 상이·S4-09 비의존. D6-2단계(BKT 부분 크레딧 승격)는 S3-01/S3-04 측정 후 판정(설계만·태스크 0). [2026-08-10 acceptance 정리 — 착수 전 선행 정리(세션 remaining-track-34zvse, ASM-05 완료 대기 중이라 1세션=1태스크 준수로 claim 보류)] 지형 실측으로 1줄 acceptance를 ①~⑦ 분해, 등재 전제 2건이 실측과 달라 정정: (a)'핸들러가 이미 쥔 값 운반'은 코칭 노출 게이트(coach.py:596-599)에 걸려 전단계-correct·ocr_gated 표본이 유실되는 구조 — 게이트 이전 값 운반으로 정정 (b)'학생 대면 표면 0'은 SurrogateMetrics 확장과 양립 불가(GET /v1/me/harness-metrics가 전 필드 자동 서빙 실측) — 파생 지표는 내부 산출로 분리. 겹침 실측: 7n9n72(S3-32)가 coach.py +416/-7 수정 중 — _build_response_payload 시그니처 영역·두 핸들러의 _log_verify_event 호출 인접 라인이라 병합 텍스트 충돌 예상, 그 브랜치가 '마지막 원소=solution_coaching' 언패킹 전제를 명문화하므로 반환 확장 시 그 위치 보존 필수. S4-16(vafylb)은 대상 3파일 무접촉 실측. 설계 문서의 행 번호는 stale(coach.py:523→실측 :582 등) — acceptance 행 번호가 2026-08-10 실측 정본"
updated: 2026-08-10

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ADMIN-08-operator-seat-grant-recovery
title: "고립된 ADMIN-01(운영자 좌석 발급 CLI + role_change 감사) 완료분 회수 — admin-01 브랜치 815줄 trunk 이식 + 전체 스위트 재검증"
track: infra-debt
stage: S4
subject: cross
layer: backend
status: todo
priority: 1
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 고립 실측 고정(2026-08-11): origin/claude/admin-01-operator-seat-grant-audit의 8924a2e2가 ADMIN-01 acceptance 전건을 구현했고(5파일 815줄 — ops/role_grant_cli.py 231줄·privacy/audit.py record_role_change_audit +52/-5·schema/enums.py AuditEventKind.role_change·테스트 20건=hermetic 16+integration 4) 53a06c4f가 그 브랜치 YAML을 done으로 전이했으나 main에는 전건 부재하다(role_grant_cli.py 없음·enums.py에 role_change 0건·.role= 대입 저장소 전수 0건). 브랜치는 main 대비 50커밋 뒤쳐졌으므로 merge가 아니라 이식(re-port)이다 — PB-01·SOL-01·S3-17·VIZ-06·REC-02·S4-18이 확립한 회수 패턴을 따른다"
  - "② 이식 범위: 8924a2e2의 5파일만. 같은 브랜치 53a06c4f(backlog YAML done 전이)는 이식하지 않고 main에서 CLI(done --artifact)로 다시 낸다 — 대장 전이는 이식 대상이 아니라 이 태스크의 산출이다"
  - "③ 재조정 — schema/enums.py는 main에서 드리프트가 큰 파일이다(#752·#768 등 enum 추가 빈번). AuditEventKind 값 추가가 기존 값·docstring·db/models/audit.py 주석·schema/audit.py와 정합하는지 확인하고, admin_access의 '현재 호출부 0곳' 자인 문구가 role_change 추가로 stale이 되지 않는지 함께 본다"
  - "④ 검증 재수행 필수 — 원 커밋의 통과 증적을 승계하지 않는다(50커밋 드리프트). CI가 실제로 쓰는 명령을 대상 경로·플래그까지 그대로 재실행하고 판정은 exit code로 한다(-q·tail 금지·EXIT 병기 — 2026-08-09 규칙). 백엔드 소스를 건드리므로 전체 스위트를 돌린 뒤 회귀 없음을 말한다 — 못 돌렸으면 '전체는 확인하지 못했다'를 명시하고 CI를 최종 판정으로 넘긴다"
  - "⑤ 변별력(원 acceptance ⑤ 승계) — 부여 전 403 → 부여 후 200/201 → 회수 후 다시 403의 3상태 왕복을 이식 후 main 코드에서 재실측한다. 같은 값을 내면 이식이 실패한 것이다. CI 배선 실재성도 확인(신규 tests/backend/ops/**가 실제로 backend 잡에서 실행되는지 — '저장소에 존재함 ≠ 돌아감')"
  - "⑥ 완료 조건: ADMIN-01 status를 main에서 done으로 전이(artifacts에 이식 커밋 증적) + 원 브랜치 claude/admin-01-operator-seat-grant-audit을 정리 대상으로 기록. 뮤테이션 검증을 한다면 원복은 cp 백업으로만 한다(git checkout --/restore/stash 금지 — 2026-08-10 OPS-24 규칙)"
  - "⑦ 범위 밖: 관리자 HTTP API(/v1/admin/*)·콘솔 UI·SYSTEM_ADMIN 역할 신설(원 acceptance ⑥ 승계·§7-①) · ADMIN-02 컬럼 처분 · access_matrix.json 소비 좌석 배선"
artifacts: []
paths:
  - "src/backend/whymath_backend/ops/**"
  - src/backend/whymath_backend/privacy/audit.py
  - src/backend/whymath_backend/schema/enums.py
  - "tests/backend/ops/**"
session: null
notes: "docs/architecture/operations_platform_gap_review_r2.md §4 D5. 미병합 고립 5회차. operations_module_gap_review_r3.md §6이 'ADMIN-01이 이미 그 좌석'이라며 태스크 신설 없이 회수 권고만 했고 아무도 실행하지 않았다(2회차 권고). SOL-01 선례대로 회수는 원 태스크와 acceptance가 다르므로(드리프트 흡수·증적 재수행·충돌 해소) 별도 좌석으로 분리한다 — r3 §6의 '좌석 이미 있음' 판정을 이 근거로 정정. D1(인가 데드락: CUD 6라우터 전건 403, 봉인과 좌석 부재가 같은 상태코드)이 9일째 열려 있다. 번호: 최초 ADMIN-04 시도를 CLI가 원격 브랜치 claude/whymath-webpage-plan-8pma1f의 인플라이트 ADMIN-04-module-registry와 충돌로 거부하고 ADMIN-08을 제안(HARN-10/15)."
updated: 2026-08-11

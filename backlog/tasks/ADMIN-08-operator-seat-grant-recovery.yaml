# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ADMIN-08-operator-seat-grant-recovery
title: "고립된 ADMIN-01(운영자 좌석 발급 CLI + role_change 감사) 완료분 회수 — admin-01 브랜치 815줄 이식 + 전체 스위트 재검증"
track: infra-debt
stage: S4
subject: cross
layer: backend
status: cancelled
priority: 1
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 고립 실측 고정(2026-08-11) — claude/admin-01-operator-seat-grant-audit(head 53a06c4f)의 8924a2e2가 ADMIN-01 acceptance ①~⑥을 5파일 815줄로 구현했으나(ops/role_grant_cli.py 231줄·privacy/audit.py record_role_change_audit +52/-5·schema/enums.py AuditEventKind.role_change·테스트 20건=hermetic 16+integration 4) main에는 전건 부재하고 ADMIN-01은 status: todo·artifacts: []다(role_grant_cli.py 없음·enums.py에 role_change 0건·.role= 대입 저장소 전수 0건). **merge가 아니라 이식(re-port)** — 브랜치가 main보다 낡아(main 대비 44,081줄 삭제) **통째 머지하면 main 완료분을 되돌린다**. PB-01·SOL-01·S3-17·VIZ-06·REC-02·S4-18이 확립한 회수 패턴을 따른다"
  - "② 이식 범위는 8924a2e2가 건드린 파일만 — ops/role_grant_cli·privacy/audit.py role_change·schema/enums.py AuditEventKind·tests/backend/ops. **태스크 paths에서 뽑은 목록은 불완전하므로 커밋의 실제 변경 파일 목록으로 대조한다**(NLP-04 acceptance② 자기정정). 같은 브랜치의 53a06c4f(backlog YAML done 전이)는 **이식하지 않는다** — 대장 전이는 이식 대상이 아니라 이 태스크의 산출이며, main에서 CLI(done --artifact)로 다시 낸다"
  - "③ 재조정 — schema/enums.py는 main에서 드리프트가 큰 파일이다(#752·#768 등 enum 추가 빈번). AuditEventKind 값 추가가 기존 값·docstring·db/models/audit.py 주석·schema/audit.py와 정합하는지 확인하고, admin_access의 '현재 호출부 0곳' 자인 문구가 role_change 추가로 stale이 되지 않는지 함께 본다"
  - "④ 검증 재수행 필수 — 원 커밋의 통과 증적을 **승계하지 않는다**(main 드리프트). CI가 실제로 쓰는 명령을 대상 경로·플래그까지 그대로 재실행하고 판정은 **exit code**로 한다(-q·| tail 금지·EXIT 병기 — 2026-08-09 규칙). 백엔드 소스를 건드리므로 **전체 스위트**를 돌린 뒤 회귀 없음을 말한다 — 못 돌렸으면 '전체는 확인하지 못했다'를 명시하고 CI를 최종 판정으로 넘긴다"
  - "⑤ 변별력 — ADMIN-01 acceptance ⑤의 3상태 왕복(부여 전 403 → 부여 후 200/201 → 회수 후 다시 403)을 이식 후 main 코드에서 재실측한다. 같은 값이면 위장이고 이식이 실패한 것이다"
  - "⑥ CI 배선 실재 확인 — 신규 tests/backend/ops/**가 실제로 CI backend 잡에서 실행되는지 확인(OPS-03·OPS-10 — '저장소에 존재함'과 '돌아감'은 다르다)"
  - "⑦ 완료 조건 — main의 ADMIN-01을 done으로 전이(artifacts에 이식 커밋 증적) + 원 브랜치를 .github/branch-cleanup-request.txt 대상으로 기록. 뮤테이션 검증을 한다면 원복은 cp 백업으로만 한다(git checkout --/restore/stash 금지 — 2026-08-10 OPS-24 규칙)"
  - "⑧ 범위 밖 — 관리자 HTTP API(/v1/admin/*)·콘솔 UI·SYSTEM_ADMIN 역할 신설(ADMIN-01 원 acceptance ⑥ 승계) · ADMIN-02 컬럼 처분 · access_matrix.json 소비 좌석 배선"
artifacts: []
paths:
  - "src/backend/whymath_backend/ops/**"
  - src/backend/whymath_backend/privacy/audit.py
  - src/backend/whymath_backend/schema/enums.py
  - "tests/backend/ops/**"
session: null
notes: "정본 2개(병합됨): docs/reviews/unmerged_branch_verdict_2026-08-11.md(#785) + docs/architecture/operations_platform_gap_review_r2.md §4 D5·§9-ⓒ. 원 브랜치 claude/admin-01-operator-seat-grant-audit head=53a06c4f·구현 커밋=8924a2e2 — 브랜치 삭제 후에도 GitHub API commits/<sha>·contents?ref=<sha>로 회수 가능(MEMORY 2026-08-09 실증 경로). 미병합 고립 5회차.\n\n[병합 2026-08-11] 이 파일은 **두 병렬 세션이 같은 ID·같은 슬러그·같은 목적으로 동시 등재**한 것을 한 좌석으로 합친 결과다(#785 claude/unresolved-long-term-branches-ph1ad7 9d002a1a ↔ claude/whymath-operations-platform-i5iu61). 판정 충돌이 아니라 **수렴**(같은 근거 d42fbd40/8924a2e2에서 독립 도달 = 상호 교차검증)이며, 원인은 backlog.py add의 번호 가드가 *이미 쓰인* 번호는 막아도 **자신이 방금 제안한 번호를 예약하지 않는** 것 — main HARN-22-id-number-suggestion-race가 등재한 결함이 그 태스크 머지 전에 재발한 실사례다. 나중에 머지되는 쪽(i5iu61)이 새 번호를 만들지 않고 흡수했다(PB-01 편집자 부기 선례). priority는 1을 채택 — 회수 태스크 선례(PB-01·SOL-01 모두 1)를 따르며, #785 원본의 2에서 올린 것이므로 되돌리려면 이 부기를 근거로 하면 된다.\n\n[취소 2026-08-11] 중복 좌석 흡수 — 회수 대상 커밋(8924a2e2)이 하나뿐이라 좌석도 하나여야 한다(operations_platform_gap_review_r2.md §해소 규칙이 정한 '나중 머지 쪽이 흡수'). 그 회수를 ADMIN-01이 실제로 실행·완료했다(artifacts: 8924a2e2 원구현 + e6c7d9a1 회수·계약등재·docstring동기화). 실측 증적: hermetic 16 + 실PG 통합 4 통과(3상태 왕복 403->201->403·트랜잭션 원자성), 전체 백엔드 스위트 9428 passed/0 failed, declared_unwired_audit EXIT 1->0 양방향. 3중 착수였고(이 세션·operations-platform-i5iu61·unresolved-long-term-branches-ph1ad7) 코드를 실제로 착지시킨 것은 ADMIN-01 경로다. 이 태스크를 todo로 두면 이미 착지한 일을 대장이 미완으로 말하게 된다(r2가 지적한 '대장-코드 분기'의 재생산)."
updated: 2026-08-11

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: OPS-46-bench-environment-capture
title: "벤치 결과에 환경 조건 미기록 — 측정 2건이 냉각·CMOS 상태 불명으로 재해석 불가가 됐다"
track: infra-debt
stage: S3
subject: math
layer: backend
status: todo
priority: 2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① detect_machine()이 현재 platform·python_version·machine 3개만 기록(:147-152)하는 것을 확장 — GPU 백엔드(ROCm/DirectML/Vulkan)·Ollama 버전·VRAM 할당·관련 OLLAMA_* 환경변수·측정 중 온도/클럭(획득 가능한 범위)·CMOS/전력 프로파일(수기 입력 필드 허용)을 결과 JSON에 기록"
  - "② 획득 불가 항목은 가짜 0/빈문자 금지 — wh1_evaluation.py의 MetricStatus 선례대로 value=None + 사유 note로 갭을 가시화(날조 0 원칙)"
  - "③ 조건 미기록 결과와 기록 결과를 구분할 수 있게 스키마 버전 필드 부여 — 과거 results/*.json이 '비교 불가'임이 기계로 드러나야 한다"
  - "④ 가짜 클라이언트 주입 테스트로 신규 필드 계약 동결(tests/infra) + CI 실행 배선 확인 후 완료 선언"
artifacts: []
paths:
  - "infra/phaiakes9/benchmark/**"
  - "tests/infra/**"
session: null
notes: "2026-08-21 발견. Kiki 보고로 기존 성능 측정 2건(2026-05-16 9.22 tok/s·2026-08-14 0.5~1 tok/s)이 모두 냉각 부족 구간·CMOS 상태 불명임이 드러났는데, 벤치가 환경을 하나도 기록하지 않아 사후 보정도 비교도 불가능했다. 그 결과 '9~18배 회귀'라는 판정을 회수해야 했다. OPS-45(프리필/생성 분리)와 같은 파일을 건드리므로 함께 처리 권장 — 둘은 다른 결함이다(45=진단 해상도, 46=시계열 비교가능성). 정본: infra/phaiakes9/LLM_STACK_TUNING.md §1.3"
updated: 2026-08-21

# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: SEC-19-generate-rate-limit
title: "POST /v1/generate 레이트리밋 — 유일한 무제한 LLM 비용 표면 봉인 (SEC-08 선례 이식)"
track: infra-debt
stage: S4
subject: cross
layer: backend
status: todo
priority: 3
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① POST /v1/generate(app.py:962)에 RateLimited 의존성 부착 — api/_rate_limit.py 기존 리미터 이식(SEC-08 /v1/auth/* 선례·재구현 0)·429 실측·Redis 부재 시 기존 fail-safe 거동 유지"
  - "② 인증(SEC-07 CurrentUser 봉인)과 별개 축임을 명문화 — 인증 사용자라도 단일 계정의 LLM 비용 남용 방어. 현재 /v1/generate는 RateLimited* 의존성이 0건인 유일한 LLM 호출 표면(visualization·scene·coach는 리미터 보유)"
  - "③ GET /v1/jobs 무인증·소유권 축은 SEC-15(in_progress) 소유 — 범위 밖 동결"
artifacts: []
paths:
  - src/backend/whymath_backend/app.py
  - tests/backend/test_app.py
session: null
notes: "ai_integration_gap_review.md D3(2026-08-10, 축소판 — jobs 축은 SEC-15로 승계). functional_security_audit_2026-08-08.md(미머지 브랜치 claude/whymath-issues-review-k20m0w)는 generate를 CurrentUser 봉인으로만 기록 — 레이트리밋 부재는 그 감사 미커버 축. SEC-15와 app.py paths 의도적 겹침(겹침 경고는 대기 신호) — SEC-15 착지 후 착수."
updated: 2026-08-10

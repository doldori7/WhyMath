# 아키텍처 감사 5회차 (ARCH-05) — 4회차 초점 4항목 판정·AI 검수 전체 코퍼스 확장 감사·생성 파이프라인 계통 결함 상환

> **감사일**: 2026-07-12 (4회차 `arch_audit_2026-07-12_r4.md` 이후 델타: **없음** — HEAD `dc976fb`가 r4 감사 PR #509 그 자체. r4 이후 신규 커밋 0)
> **태스크**: `backlog/tasks/ARCH-05-playbook-audit.yaml` | **방법**: 정적 불변식 grep·config diff + 게이트 대장 실측 + AI 검수 첫 배치 품질 대조
> **한 줄 결론**: r4 이후 **코드/PR 델타 0**(HEAD=r4). 4회차 초점 ①②③④는 전부 **kiki 소유 태스크·라이브 실측·pending 게이트 대기**로 정상 이월. **이번 회차 핵심 = AI 검수 첫 배치 30문 품질·기계 게이트 선행 실측 + 생성 파이프라인 계통 결함 3종 판정.** 정적 불변식 회귀 0·NEVER 위반 0. **단, 계통 결함 3종(조사 받침·난이도 앵커 인플레·op-code 방향)이 상환 태스크로 미추적임을 위반으로 판정 → 상환 태스크 1건 추가**(§5·수용기준 ③).

---

## 1. 4회차 초점 4항목 이행 판정 (arch_audit_2026-07-12_r4.md §5)

| # | 4회차 초점 | 5회차 판정 |
|---|---|---|
| ① | S1-12 라이브 실측 완료 시: 첫 실측(#505)→전면 실측 교체·비용 로컬:클라우드 비율·verify verdict 분포·S1 게이트② 판정 | ⏸ **대기(무변화)** — `S1-12-phaiakes9-live-verify`(owner=**kiki**·status=todo) 미완. r4 이후 라이브 실측 신규 기록 없음(델타 0). **추정→전면 실측 교체 미도달·S1 게이트② 미판정 유지.** 클라우드 컨테이너가 완료 불가한 라이브 태스크 = auto-drive 제외 유지(정상 추적) |
| ② | S1-11 승격 — shadow 데이터 확보 후 학생-대면 primary 승격 가부·"측정 없는 도입 없음" 준수 | ⏸ **대기(무변화)** — `S1-11-coach-harness-converge`(owner=claude·todo)는 S1-12 shadow 선행. S1-12 미완으로 승격 판정 진입 불가. **성급 primary 승격 안 함 = 원칙 준수.** MEMORY 2026-07-10 실측(코치 결정론 한계) 이후 신규 판정 근거 없음 |
| ③ | ARCH-12(QuizMode) 결정 이행·웹 게이트 화이트리스트 봉인 해제 판정 | ⏸ **kiki 대기(무변화)** — `ARCH-12-quizmode-grading-decision`(owner=kiki·todo). 데모 예외 공식화 vs 백엔드 verify 리팩터 결정 미도래. 웹 게이트 화이트리스트 2건 **봉인 유지**(신규 유입 red 가동·ARCH-10 게이트 존치). 회귀 유발 신규 클라이언트 수학로직 0 |
| ④ | S2-01 착수 시(G-crosswalk-approval 후): 저작권 게이트 재확인 + AI 검수 전체 코퍼스(513문) 확장 감사 | 🔄 **착수 전·확장 감사 §3에서 선행 실측** — `S2-01-equiv-problems-100`(owner=claude·todo, depends_on S1-12·requires G-crosswalk-approval). `G-crosswalk-approval` **pending 유지**(4일 경과)로 미착수. 전체 코퍼스(513문) 확장은 미개시이나, **첫 배치 30문 검수 품질·기계 게이트 선행·⑥ 잔여 리스크를 §3에서 실측**하고 **선행 수정 대상(계통 결함)을 §5로 상환** |

**요지**: 4개 초점 전부 라이브 실측(S1-12)·Kiki 게이트 이행 후에야 실익 발생 — 태스크 notes 예측과 정확히 일치. 이번 회차가 새로 판정 가능한 실질은 §3(AI 검수 확장 감사)에 집중.

---

## 2. 신규 코드 델타 점검 (r4 이후 = HEAD)

| 항목 | 결과 |
|---|---|
| 델타 창 | ✅ **HEAD `dc976fb` = r4 감사 PR #509** — `git log dc976fb..HEAD` **빈 결과**. r4 이후 신규 커밋·PR 0. 본 감사는 순수 재확인+AI 검수층 신규 감사 |
| 신규 수학코어 코드 | ✅ **0** — 델타 자체가 없음 |
| 금지 관계 토큰 | ✅ `grep -rn "similar_to\|related_to" src/` — traversal 실사용 **0건**(guard/test 정의 외 없음) |
| 계층 경계 계약 | ✅ import-linter 7계층 단방향 계약 실측: `api→l6→l5→l4→l3→l2→l1→schema`(`src/backend/pyproject.toml [tool.importlinter]`) **무변경**. 신규 seam 0 |
| 스키마·마이그레이션 | ✅ alembic head `c4d5e6f0a1b2`(strategy_node_projection·2026-07-08) **불변**(r4와 동일). 신규 마이그레이션 0 |
| 노드 순수성 | ✅ `concept_graph_v1/graph.json` 437노드 필드 실측 — `renderer·curriculum·prompt·misconception·embedding·ui` **직접 혼입 0**. `misconception_codes`·`visualization_card_keys`·`formula_refs`는 **참조(코드/키)** = 허용 패턴(reactive retrieval 키·Visualization Intent 키) |
| 엣지 타입 | ✅ concept_graph 엣지 타입 = `prerequisite` **단일**(5~8종 한도 내·DAG 강제 대상) |
| 게이트 대장 정합 | ✅ `backlog/gates.yaml` 실측: crosswalk/orphan/s5 **pending** · domain-partner/device-demo/phaiakes9 **cleared** — r4 판정과 완전 일치 |

> **환경 제약(정직 기록)**: 본 감사 컨테이너에 backend venv·pytest 미설치 → 거버넌스 테스트(`tests/backend/l1/test_*_governance.py`) 직접 실행 불가. r3·r4 동일 제약 하 정적 감사. **r4 이후 신규 수학코어 코드 0이라 거버넌스 회귀 유발 경로 자체가 없음**을 정적 실측(grep·config diff)으로 확인. 거버넌스 테스트는 CI가 강제한다.

## 3. AI 검수 전체 코퍼스(513문) 확장 감사 (이번 회차 핵심)

> **대상**: `docs/data/ai_review_first_batch_2026-07.md`(동등문제 첫 배치 30문 AI 검수·2026-07-10 결정). 전체 코퍼스(513문+) 확장은 S2-01 흐름에서 수행 예정이며 미착수. **본 감사는 첫 배치를 전체 확장의 대표 표본으로 삼아 검수 절차·기계 게이트 선행·잔여 리스크를 판정한다.**

| 감사 축 | 판정 |
|---|---|
| **첫 배치 검수 품질** | ✅ approved 28 / deferred 2 / rejected 0. 30문 전건 SymPy 검산(인수분해·극값·접점·일반항) 일치 = **수학 오류 0건**. deferred 2건(표본 29·30)은 난이도 재보정 전 적재 보류 = **품질 게이팅이 작동**(무비판 통과 아님) |
| **기계 게이트 S2-a 4종 선행** | ✅ 문서 §8 명시 — 기계 게이트(SymPy 정확성·동등성·dedup·저작권 불변식)는 코퍼스 적재 시 **이미 통과**, AI 검수는 그 위층. AI 검수가 기계 방벽을 **대체하지 않고 사람 판단층(①~⑥)만 대체**하는 경계 유지 = r4 §3 판정과 정합 |
| **마커 규약 정직성** | ✅ `검수:AI 2026-07-10` stamp가 **인간 수기검수 아님·모델ID 미기재**를 정직 명시. crosswalk 사람 서명 stamp와 **다름을 문서가 스스로 경고**("crosswalk 적재 근거로 쓸 수 없다"). "확실하지 않을 때 자신 있게 말함" 금기 대비 정합 |
| **⑥ 우연 유사 잔여 저작권 리스크** | ✅ **NEVER 위반 아님·정직 수용** — 검수자가 평가원 기출·검정 교과서 원문 DB 미보유를 명시하고 "확정적 무결 보증은 원문 대조 없이는 불가능"을 고지. 잔여 리스크 = MEMORY 2026-07-10 수용·변호사 검토 권고 유지(PRD §13.2). 의사결정 우선순위 2번(법적)을 6번(비용)이 잠식하지 않음 |
| **생성 파이프라인 계통 결함** | ⚠️ **위반 판정(§5 상환)** — 첫 배치가 3종 계통 결함을 지적: ①조사 받침 판별 버그(문서 자체가 **"코퍼스 전체 계통 결함"**·표본 02·04·06·09·11·29 실오류) ②난이도 앵커 인플레(공식 직대입 패밀리가 인수분해보다 높게 표기·**"L2 IRT 입력 품질 영향"**) ③op-code 방향(`select-min-for-max` 극솟값 문항 오용). 문서 line 159가 **전체 코퍼스 검수 전 수정 권고**("수정 없이 전체 검수 시 동일 지적 대량 반복"). 현 `backlog/tasks/`에 상환 추적 **부재**(grep 확인) |

**감사 결론**: AI 검수 전환 자체는 8대 구조 원칙·NEVER·의사결정 우선순위 위반이 아니며 첫 배치 품질도 건전하다(수학 오류 0·게이팅 작동). 그러나 **생성 파이프라인의 계통 결함 3종이 전체 코퍼스(513문) 확장 시 대량 증폭될 구조적 리스크**이며 미추적 상태다. 이는 "truth source 하나가 아닌 산출물 품질 붕괴"의 초기 신호로, **상환 태스크로 백로그에 편입**해 S2-01 전체 확장의 **선행 조건**으로 못박는다(§5).

## 4. 상시 불변식 재확인 (변경 없음)

엣지 5~8종(concept_graph=prerequisite 단일 실측)·노드 순수성(437노드 금지혼입 0 실측)·`prerequisite` DAG·오개념 reactive retrieval(preload 0)·임베딩 namespace 거버넌스·백엔드 7계층 단방향(import-linter 실측)·클라이언트 무-수학로직 게이트(ARCH-10 가동)·LLM subgraph 예산(max_nodes≤20·tokens≤3000 CI 동결·depth≤2 유예) — 전부 준수 유지. **r4 이후 신규 수학코어 코드 0이라 1~4회차 실측 대비 회귀 유발 코드 없음.**

## 5. 상환 태스크 추가 (수용기준 ③)

- **신규**: `S2-08-genpipeline-defect-fix` — "생성 파이프라인 계통 결함 3종 상환(조사 받침 판별·난이도 앵커 인플레·op-code 방향) — 전체 코퍼스 검수 선행". track=math-completion·stage=S2·priority=1·owner=claude.
  - 근거: `ai_review_first_batch_2026-07.md` 계통 관찰 1·2·4 + line 159 선행 수정 권고.
  - 수용: 조사 받침 판별 로직 추가 · 난이도 도메인 앵커 재보정 · op-code 방향 중립화 후 첫 배치 deferred 2건(표본 29·30) 재판정 해소.
  - 효과: S2-01 전체 코퍼스(513문) 확장 시 동일 지적 대량 반복 차단·L2 IRT 입력 품질 보호.

## 6. 다음 회차(ARCH-06) 초점

1. **S1-12 라이브 실측 완료 시**: 첫 실측→전면 실측 교체·비용 로컬:클라우드 비율·verify verdict 분포·S1 게이트② 판정 (여전히 kiki 라이브 대기)
2. **S1-11 승격 판정**: shadow 데이터 확보 후 primary 승격 가부 — "측정 없는 도입 없음"
3. **ARCH-12(QuizMode)** 결정 이행·웹 게이트 화이트리스트 봉인 해제 판정
4. **S2-08 상환 이행 실측**: 계통 결함 3종 수정 완료 여부·deferred 2건 재판정 해소 확인
5. **S2-01 착수 시**(G-crosswalk-approval 후): 저작권 게이트 재확인 + **전체 코퍼스(513문) 실제 확장 검수** 품질·기계 게이트 4종 선행 실측·⑥ 잔여 리스크 재점검

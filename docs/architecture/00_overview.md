# 00. 7계층 아키텍처 개요

> 이 문서는 모든 계층 명세의 진입점입니다. 각 계층 상세는 `01_*.md` ~ `07_*.md` 참조.

## 전체 구조

```
┌─────────────────────────────────────────────────────────────┐
│ L7. 커뮤니티·소셜          [학생 풀이 공유, 학부모·교사 대시보드]    │
├─────────────────────────────────────────────────────────────┤
│ L6. 응용 모드             [학교진도/수능/사고력/영재/메타인지/디버깅] │
├─────────────────────────────────────────────────────────────┤
│ L5. 상호작용              [Mathpix OCR·Manim·Desmos·음성·대화]   │
├─────────────────────────────────────────────────────────────┤
│ L4. 교수학 엔진            [Polya 4단계·소크라테스·LTHC·오개념 진단] │
├─────────────────────────────────────────────────────────────┤
│ L3. 콘텐츠 생성·검증        [LLM 라우팅·PRM 단계검증·도구호출·다중풀이]│
├─────────────────────────────────────────────────────────────┤
│ L2. 학습자 모델            [BKT/DKT·IRT·정서신호·오개념 매핑]      │
├─────────────────────────────────────────────────────────────┤
│ L1. 데이터 기반            [성취기준·검정교과서·평가원·EBS·KMO·글로벌]│
└─────────────────────────────────────────────────────────────┘
```

## 계층별 한 줄 정의

| 계층 | 역할 | 핵심 자산 | 서브에이전트 |
|---|---|---|---|
| **L1** | 진실 원천 데이터 보유 | 성취기준·교과서·기출·OER | data-engineer |
| **L2** | 학생의 *장기 상태* 추적 | BKT/IRT 모델 + 오개념 매핑 | ml-engineer |
| **L3** | LLM 응답 *생성·검증* | 라우터 + PRM + 도구 | llm-architect |
| **L4** | *교수학적 결정* | Polya·소크라테스·답 미루기 | pedagogy-designer |
| **L5** | *학생과의 인터페이스* | Flutter 앱 + FastAPI 서버 | flutter/backend-engineer |
| **L6** | *시장별 진입 모드* | 7개 모드 콘텐츠 큐레이션 | content-curator |
| **L7** | *사회적 학습* | 갤러리·Live Problems·보고서 | content-curator |

## 의존성 방향

**상위 → 하위 방향만 허용** (역방향 의존 금지):
```
L7 → L6 → L5 → L4 → L3 → L2 → L1
                ↑      ↑
               (이 둘은 자주 같이 호출됨)
```

### 경계 침범 예시

❌ **나쁜 예**: L3가 BKT 업데이트 직접 호출하여 학습자 상태 변경  
✅ **좋은 예**: L3가 응답 생성 → L4가 후처리 → L5(orchestrator)가 L2에 업데이트 요청

❌ **나쁜 예**: L4가 PostgreSQL 쿼리 직접 작성  
✅ **좋은 예**: L4가 L1의 매핑 서비스 *함수 호출*

## 횡단 관심사 (Cross-Cutting Concerns)

다음은 모든 계층을 가로지름 — 별도 인프라:

- **로깅 (structlog)**
- **추적 (OpenTelemetry + Langfuse)**
- **메트릭 (Prometheus)**
- **에러 처리 (Sentry)**
- **보안 (인증·암호화·PII 보호)**
- **개인정보 (동의·삭제)**
- **국제화 (한국어 우선, 영문 Phase 5+)**

## 데이터 흐름 — 학생 메시지 1건

```
[학생] 채팅 입력 "이 문제 잘 모르겠어요"
   ↓
[L5 Mobile] 텍스트 전송 → FastAPI /chat
   ↓
[L5 Backend] 인증·세션 로드
   ↓
[L2] 학습자 상태 조회 (BKT·오개념·정서)
   ↓
[L1] 학생 컨텍스트 조회 (학년·교과서·진도·성취기준)
   ↓
[L4] 교수학 결정 (Polya 단계? 힌트 단계? 소크라테스 카테고리?)
   ↓
[L3] LLM 라우팅 → 호출 → PRM 검증 → 다중 풀이 옵션
   ↓
[L4] 정서 안전 필터 (금기 표현 제거)
   ↓
[L2] BKT 업데이트
   ↓
[L5 Backend] 응답 반환
   ↓
[L5 Mobile] 채팅 UI 표시 (Polya 단계 인디케이터 갱신)
   ↓
[학생] 응답 확인
```

## Phase별 계층 우선순위

| Phase | 가장 집중 | 보통 | 미루기 |
|---|---|---|---|
| **Phase 0** | 청사진 (모든 계층 설계만) | — | — |
| **Phase 1** | L1·L3·L4 (콘텐츠 생산·교수학) | L5 (MVP UI) | L2·L6·L7 |
| **Phase 2** | L2 (BKT 가동)·L6 (학교진도+수능) | L1 (확장) | L7 |
| **Phase 3** | L7 (갤러리·보고서)·L6 (영재 추가) | L2 (DKT·정서) | — |
| **Phase 4** | L6 B2B 모드·L7 교사 대시보드 | — | — |
| **Phase 5** | L1 (다국어)·L6 (해외 모드) | — | — |

## 컴포넌트 매트릭스 (참조용)

| 컴포넌트 | 계층 | 우선순위 | 비고 |
|---|---|---|---|
| NCIC 크롤러 | L1 | P0 | Phase 1 첫 작업 |
| 학교알리미 크롤러 | L1 | P0 | 학생별 교과서 매핑 |
| 검정교과서 목차 DB | L1 | P0 | 본문 X, 목차만 |
| BKT 모델 | L2 | P1 | Phase 1 후반 |
| LLM 라우터 | L3 | P0 | 비용 구조 핵심 |
| PRM 검증기 | L3 | P1 | 환각 방어 |
| SymPy 도구 | L3 | P0 | 계산은 SymPy로 |
| Mathpix OCR | L3 | P0 | 손글씨 핵심 |
| Polya 엔진 | L4 | P0 | 교수학 코어 |
| 소크라테스 프롬프트 | L4 | P0 | 코어 IP |
| 오개념 카탈로그 | L4 | P1 | 30개 → 100개 |
| Flutter 앱 | L5 | P0 | 모바일 우선 |
| FastAPI 서버 | L5 | P0 | 오케스트레이터 |
| 학교진도 모드 | L6 | P1 | Phase 2 |
| 수능 모드 | L6 | P1 | Phase 2 |
| 사고력 모드 | L6 | P0 | Phase 1 진입 |
| 영재 모드 | L6 | P3 | Phase 3 |
| 메타인지 모드 | L6 | P0 | Phase 1 진입 |
| 다중 풀이 갤러리 | L7 | P2 | Phase 3 |
| 부모 보고서 | L7 | P2 | Phase 3 |
| 교사 대시보드 | L7 | P3 | Phase 3~4 |

## 참조

- 각 계층 상세: `01_data_foundation.md` ~ `07_community.md`
- 시장 전략: `../strategy/`
- 코딩 표준: `../standards/`
- 프롬프트 라이브러리: `../prompts/`

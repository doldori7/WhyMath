# 시각화 모듈 — 외부 EOS 틀 대조 4차 재점검(R4) (2026-08-11 작성 · 2026-08-14 재실측·재착지)

> **범위**: 외부 참고 문서 『15. 시각화』(기능 62 함수 그래프 시각화 · 63 기하 도형 조작 ·
> 64 애니메이션 설명 · 65 수학 모델 시뮬레이션, 세부 기능 40개 — **WhyMath 전용이 아닌 일반 EOS
> 틀**, Kiki 재제출)을 현 코드베이스와 4번째로 대조한다.
> **형식**: 같은 외부 틀 대조 시리즈. 모듈 내 **4번째 편**이며 **파일로는 첫 분리본**이다(§0-①).
> **성격**: 40개 세부 기능 전수 재대조가 **아니다** — ① 고립된 3차(R3) 회수 ② 1~3차가 이월·미검토로
> 남긴 축만 판정한다.
> **실측 기준 HEAD**: 최초 작성 `89ed3023`(2026-08-11) · **재실측·재착지 `7cafdbd5`**(2026-08-14 — §1.3).
>
> **결론(4차)**: 착수 가설("4차는 3차가 지목한 Scene DSL↔Visualization DSL 중복 축을 판정한다")은
> **두 번 기각**됐다. ① 그 축은 **이미 닫혀 있었다** — 같은 날(08-10) 타 세션이 스키마 축을
> 판정(#756)하고 그 잔여(서빙 미러 ~40행)까지 `PED-16` ④가 착륙시켰다. 3차는 그것을 모른 채
> "4차 최우선 후보"로 예약했다. ② 그런데 **3차 자신이 고립돼 있었다** — §8 점검·`VIZ-07` 코드
> 착륙·태스크 3건·헌법 규칙 1건이 main에 하나도 없었다.
>
> 1차가 "만들어 놓고 연결하지 않았다", 2차가 "연결했더니 양쪽 어휘가 다르다", 3차가 "회수·병합했더니
> 대장이 낡았다"였다면, **4차는 "판정이 세 벌인데 서로를 모른다"**다. 세 세션(#756 DSL 통합 ·
> `PED-16` · 시각화 R3)이 **같은 날 같은 경계를 각각 판정**했고, 하나는 이월하고 하나는 해소하고
> 하나는 고립됐다. 코드는 건강하다 — 회수 후 전 게이트 green(§4). 병든 것은 **판정의 유통**이다.
>
> 그리고 1~3차가 한 번도 보지 않은 축에서 **최대 갭**이 나왔다: 개념 축만 세 번 점검하는 동안
> **문제(problem) 축 시각화는 생산자 0·소비자 0·데이터 0/2,647의 죽은 사슬**로 두 달간 남아
> 있었다(§3 D5).

관련 정본: `visualization_module_gap_review.md`(1~3차 §1~§8 — 본 편의 모체) ·
`05_interaction.md` · `05a_learning_scene_dsl.md` · `05b_visualization_classification.md` ·
`dsl_integration_gap_review.md`(#756 — Scene↔Viz 판정) · `data/render_contract.json` ·
`data/visual_style_contract.json` · `MEMORY.md` 결정 로그(2026-07-30·08-03·08-10·08-11).

**판정 글리프**: ✅ 충족·초과 / △ 부분(부품은 있는데 배선·데이터 없음) / ⚠️ 진짜 갭 → D /
⏸ 기존 태스크 승계 / 🚫 의도적 미채택.
**D 번호는 1차 D1~D4에 이어 D5부터** — 2차(G1~G3)·3차(R1~R4)는 D 번호를 소비하지 않았다.

---

## §0. 세 가지 전제

### ① 파일 차수 ≠ 라운드 차수 (혼동 봉인)

| 라운드 | 시점 | 위치 | 결론 |
|---|---|---|---|
| 1차 | 2026-07-30 | 원본 `§1~§6` | 만들어 놓고 연결하지 않았다 |
| 2차 | 2026-08-03 | 원본 `§7` (in-place) | 연결했더니 양쪽 어휘가 다르다 |
| 3차 | 2026-08-10 | 원본 `§8` (in-place) — **고립** | 회수·병합했더니 대장이 낡았다 |
| **4차(본 편)** | **2026-08-11** | **분리 파일 `_r4`** | 판정이 세 벌인데 서로를 모른다 |

원본은 1차 시점 판단("모듈당 단일 진실원 유지 — 새 파일 미신설",
`visualization_module_gap_review.md:8`)에 따라 2·3차를 in-place로 흡수했다. 그러나 그 **직후**
정반대 규약이 확립됐다 — `problem_bank_gap_review_r2.md`(08-03)·`gamification_module_gap_review_r2.md`
(08-04)가 세운 **"원본 비수정 + 별도 파일"**이며 근거는 세 가지다(`solution_module_gap_review_r3.md:34-45`):
층 붕괴 방지 / 뒤집기 0이면 덮어쓸 내용 없음 / 실측 시점 정보 소실 방지.

본 편은 **후자를 채택**하되 파일명을 `_r2`(파일 차수)가 아니라 **`_r4`(라운드 차수)**로 둔다 —
`operations_..._r3`·`solution_..._r3` 시리즈가 파일=라운드로 대응하므로 파일 차수를 쓰면 시리즈
전체와 어긋난다. 원본 상단에 본 편으로 오는 포인터 1줄을 추가한다.

### ② EOS 번호 하우스 룰 승계

본 편의 "기능 62~65"는 **전부 본 편(시각화) 기준**이며, 선행 자매편
`ai_content_generation_gap_review.md:136`의 "확장 후보 62~68"과 **교차 참조하지 않는다**
(같은 번호에 다른 기능 — 원본 `:37-53` 하우스 룰 승계).

### ③ 승계 선언 — 본 편이 재판정하지 **않는** 것

재판정하면 층이 무너지므로 아래는 근거만 갱신하고 판정은 승계한다:

- **의도적 미채택 6건**(원본 `:155-163`) — 물리·금융·SIR·최적화·미분방정식(페르소나 밖 + SymPy는
  동치·해집합 권위이지 *모델 타당성* 권위가 아니다) / AI 가상 실험 / AI 음성 해설 동기화(갭이
  아니라 **순서** — 대상 애니메이션 부재) / 자동 증명 보조 시각화 / GeoGebra·Desmos 연동 /
  개념 노드에 figure를 붙이는 모델
- **기능 63 전건** → `S4-03` 승계·**재설계 금지**
- **D1~D4 · G1~G3 · R1~R4** 판정 전건
- **미등재 트리거**: 점화도 11+수형도 4+넓이모델 3 = 18건 구조 다이어그램(`StructureGraph` 소관) ·
  Manim 서버 렌더 파이프라인 · 통계 샘플링 · viz 생성 라우팅 패밀리 보정

---

## §1. 고립 3차(R3) 회수 — 본 편의 첫 산출

### §1.0 회수 전 실측

`origin/claude/whymath-visualization-review-r3`(2026-08-10)이 main의 조상이 **아니었다**.
`git diff origin/main...<R3>` = **22파일 +908/−166**:

| 산출물 | 내용 |
|---|---|
| 원본 `§8`(+222행) | 3차 점검 R1~R4 판정 |
| **`VIZ-07` 코드 착륙** | `Graph2dSpec.number_line`(1D 수직선 축 — 점 ●/○·구간·반직선·개폐 끝점) + 웹 계산기 `drawNumberLine` 신설 + 좌석 5→7종 + vendored 번들 재동기화(`index-BJtZV45X.js`→`index-DJ9D7N0L.js`) |
| `VIZ-08`·`VIZ-09`·`OPS-26` | 태스크 3건 등재 |
| **CLAUDE.md 규칙 1건** | "회수(recovery)로 태스크 done 선언 시 acceptance 전수 재대조 생략 금지" |
| MEMORY 결정 로그 | 3차 경위 |

**병합 실측**: `git merge-tree --write-tree origin/main <R3>` → 충돌 **1건뿐**(`CLAUDE.md` —
R3의 신규 규칙과 main 2026-08-10 통합점검이 추가한 규칙 2건의 **인접 삽입**). 나머지 21파일 무충돌.
해소 방향은 **양쪽 전부 보존**(어느 규칙도 삭제 아님) — 병합 후 3규칙 공존을 grep으로 확인.

### §1.1 회수 시 acceptance 전수 재대조 — R3의 규칙을 R3 자신에게 적용

R3가 등재한 규칙("회수로 done 선언 전, 그 시점 main의 acceptance 전 항목을 이행/미이행/의도적
제외로 재대조")을 **이 회수에 적용**한다. `VIZ-07`은 브랜치에서 done이므로 회수가 그 done을
main으로 옮기는 것이며, 규칙의 적용 대상이 정확히 이 상황이다.

| `VIZ-07` acceptance | 판정 | 실측 근거 |
|---|---|---|
| ① 1D 축 필드 + 웹 어댑터·계산기 렌더·Flutter 봉투. 「렌더러 먼저, 좌석 나중」 준수 | **이행** | `schema/visualization.py:56,70,86,179`(`NumberLinePoint`·`NumberLineInterval`·`NumberLineSpec`·`Graph2dSpec.number_line`) · `graph2dSpec.js` `number_line` 8회 · `GraphingCalculator.jsx` 2회 |
| ② 계약 `수직선` seated + `_SEATED_STYLES` 동기, drift 교차 테스트 green | **이행** | `visual_style_contract.json` `수직선 status=seated·seat_owner=None` · `visualization_policy.py` `_SEATED_STYLES` 7종 · `test_visual_style_render_contract.py` 29 passed |
| ③ 접선도함수는 웹 어댑터 `tangent_point` 실소비 **실측 후에만** seated + policy 주석 정정 | **이행** | `graph2dSpec.js` `tangent_point` 7회 · JS 테스트 8회 · `visualization_policy.py:42-44` 주석이 "소비 경로 소스 실측 확정(VIZ-07)"으로 정정됨 |
| ④ 변별력: 계약 게이트 red→green 양방향 | **이행 + 본 편이 독립 재실측** | 아래 |

**④ 독립 재실측**(브랜치 기록을 신뢰하지 않고 본 세션이 직접):

```
baseline                      → 29 passed · EXIT=0
뮤테이션(수직선 seated→unseated) → 3 failed · EXIT=1   ← 변별력 있음
cp 백업 원복                    → EXIT=0 · git diff 공백  ← 완전 원복
```

뮤테이션 원복은 **`cp` 백업**으로만 했다(`git checkout --`·`git restore`·`git stash` 미사용 —
CLAUDE.md 2026-08-10 등재 규칙. 병합 직후라 작업 트리에 미커밋 회수분이 통째로 있어 git 계열
원복이 특히 위험한 상태였다).

**미이행 0 · 의도적 제외 0** → `VIZ-07`은 회수 done으로 정당하다.

### §1.2 회수 후 전 게이트 재검증

CI가 쓰는 명령을 **그대로**(대상 경로·플래그 포함) 재현하고 판정은 **exit code**로 한다
(출력 억제·`tail` 절단으로 판정 금지 — CLAUDE.md 2026-08-09 등재). 결과는 §4.

---

### §1.3 본 편 자신이 유실됐다 — 2026-08-14 재착지 (§5 명제의 실물 증명)

**경위(실측)**. 2026-08-11 본 편은 R3 회수와 함께 PR #812로 제출됐고 CI 16/16 green을 받았다.
그런데 `main` 회전이 backend CI(27분)보다 빨라 base 정합이 반복 밀렸고(behind → 갱신 → dirty →
재해소, 2회), 그 사이 브랜치 head가 **R3 계보로 되감겼다**:

| SHA | 날짜 | 작성자 | 내용 | 본 편 존재 |
|---|---|---|---|---|
| `7ce40f4f` | 08-11 | Claude | **R4 산출물**(본 문서·`VIZ-10`·원본 포인터·MEMORY 로그) | ✅ |
| `b7dd3796` | 08-11 | Claude | main 흡수 재정합 | ✅ |
| `5feeea95` | **08-10** | Claude | `backlog: VIZ-07 done` — **R3 브랜치 커밋** | ❌ |
| `e5af1040` | 08-14 | doldori7 | main 흡수 | ❌ |

08-11 커밋 두 개가 08-10 커밋으로 대체됐다 — 되감기다. 그 결과 PR #812의 스쿼시 머지
(`22b6116b`)는 **1파일 4줄**(`S4-22` YAML)만 담았고, **R3 회수는 살아남았지만**(main에
`number_line` 5회·`VIZ-07` done·`VIZ-08`/`VIZ-09`/`OPS-26`·§8·CLAUDE.md 회수 규칙 전부 실재)
**R4 층만 사라졌다**.

**이것이 §5 명제의 실물 증명이다.** §5는 "판정이 여러 벌인데 서로를 모른다"를 관측했는데, 본 편은
그 다음 단계까지 겪었다 — *판정이 서로를 모르는 데서 그치지 않고, 한쪽이 다른 쪽을 덮어썼다.*
1차 "만들어 놓고 연결하지 않았다" → 2차 "연결했더니 어휘가 다르다" → 3차 "회수했더니 대장이
낡았다" → 4차 "판정이 세 벌인데 서로를 모른다" → **4차 후일담: 그리고 한 벌이 지워졌다.**

**재실측 후 재착지**. 3일이 지났으므로 본 편의 판정을 현행 `main`(`7cafdbd5`)에서 전건 재확인한
뒤에만 되살렸다 — 낡은 주장을 그대로 복원하는 것은 회수가 아니라 오염이다:

| 판정 | 재실측 결과 |
|---|---|
| D5 문제 축 죽은 사슬 | **유효** — 코퍼스 2,638문제(분모 이동) · `visualizations` 0 · `visual_type` 0 · 생산자 0 · 소비자 0 |
| §2 Scene↔Viz 축 종료 | **유효** — `api/_concept_orchestration.py`(5,601 bytes) 실재 · `scene.py` 3회·`visualization.py` 4회 호출 · #756 판정 문구 존치 |
| §1 R3 회수 | **이미 main에 착륙** — 본 편이 다시 할 일 없음(중복 회수 금지) |
| 좌석 7종(`VIZ-07`) | **유효** — `_SEATED_STYLES` 7종 유지 |

따라서 이번 재착지의 범위는 **R4 층(본 문서·`VIZ-10`·원본 포인터·MEMORY 로그)뿐**이며,
R3 회수분은 손대지 않는다.

**재발방지는 추가 등재하지 않는다.** 원인은 새 축이 아니라 이미 등재된 "미머지 고립" 계열의
집행 실패이고, 이 저장소는 그 계열 규칙을 이미 3건 갖고 있다. 다만 §5가 제안한 문서 템플릿
집행(이월 축에 "선점 확인 결과" 병기)에 더해, 본 절이 **"CI가 base 회전보다 느린 저장소에서는
green이 머지를 보장하지 않는다"**는 사실을 기록으로 남긴다 — 다음 세션이 green을 완료로 읽지
않도록.


## §2. 최우선 축 판정 — Scene DSL ↔ Visualization DSL: **닫힌다** (3회 이월 종료)

### §2.1 이월의 계보

| 편 | 위치 | 남긴 말 |
|---|---|---|
| 1차 | `§5-b` | "후속 편으로 분리해야 한다" |
| 2차 | `§7.6` | "이번에도 범위 밖 — 다음 편 후보로 명기" |
| 3차 | `§8.9` | "3회 연속 이월이므로 **4차가 있다면 최우선 후보**" |

Kiki가 4차 최우선 축으로 지정한 것도 이것이다. **그런데 이미 닫혀 있었다.**

### §2.2 실측 — 두 축 모두 해소됨

**스키마 축**(중복인가?) — `dsl_integration_gap_review.md:81-91`(#756, **2026-08-10**):
> "### ① Scene ↔ Visualization: 중복 아님 — 합성 확정 (기지 미검토 축 해소)"
> 실측: `LearningScene`은 시각화 필드를 재정의하지 않고 `learning_scene.py:96-100`
> `VisualizationElement.ref: Visualization`으로 **참조 임베드**하며, `scene_generation.py:347-357`이
> `generate_visualization_spec` 결과를 그대로 합성한다. 모바일 `Visualization` 파서도 **1벌**뿐이다.

**서빙 축**(#756이 남긴 유일한 실제 중복) — `api/scene.py:93-107` ↔ `api/visualization.py:85-114`가
개념 로드→스타일→가시성→숙달→라우팅 **~40행을 미러**하고 주석 스스로 "미러"라 불렀다. #756은
처방을 "리팩터가 아니라 `PED-16` 판정 시 라우트 거취와 함께 결정"으로 **이연**했다.

→ **`PED-16` ④가 착륙했다**(status=done·2026-08-10): `api/_concept_orchestration.py` 신설(5,601
bytes)로 공통분(Concept 로드→Overlay 조회→숙달도 라벨화→`RoutingRequest` 조립)을 추출하고
두 파일 전용분(behavior_areas/WH-1 프로브 vs `is_visualizable` 게이트)은 **공유하지 않았다**
(과잉 추상화 회피). 본 편이 배선 실재를 직접 확인: `api/scene.py:25,28,92` ·
`api/visualization.py:31,34,89`가 실제로 이 헬퍼를 import·호출한다 — **정본화≠집행 검사 통과**.

### §2.3 판정: ✅ **갭 아님 — 축 종료**

두 축(스키마·서빙) 모두 판정·해소 완료. 본 편은 재판정하지 않고 **승계**하며,
**1차 §5-b부터 이어진 이월을 여기서 정식으로 닫는다.** 다음 편은 이 축을 후보로 올리지 않는다.

재개 조건(dead task 방지): `LearningScene`이 `Visualization`을 참조가 아니라 **값으로 복제**하기
시작하거나, 모바일에 `Visualization` 파서가 **2벌째** 생기면 재판정한다. 현행 동결 장치 —
`test_scene_dsl_layer_governance.py`(금지 토큰 11종) · `no_math_logic_governance_test.dart` ·
`learning_scene.py:269 _validate_invariants`.

### §2.4 그런데 이 사실 자체가 갭이다 → §5 반복 실수

3차는 #756과 **같은 날** 작업하면서 그 판정을 알지 못했고, 이미 죽은 축을 4차 최우선으로
예약했다. 4차가 그 예약을 그대로 따랐다면 **이미 닫힌 축을 네 번째로 판정**했을 것이다.
등재는 §5.

---

## §3. 진짜 갭 설계

### D5 — 문제(problem) 축 시각화가 죽은 사슬이다 (최대 갭·1~3차 미점검)

#### 문제

1~3차는 **전부 개념(concept) 축**만 봤다. 문제 축에는 좌석이 완비돼 있는데 **아무것도 흐르지
않는다**:

| 층 | 좌석 | 실측 |
|---|---|---|
| Pydantic | `schema/problem.py:373` `visual_type` · `:379` `visual_complexity` · `:383` `visualizations` · `has_visual` | 존재 |
| DB | `db/models/problem.py:175`(ARRAY enum·`server_default '{}'`) · `:182`(JSONB·NOT NULL·`server_default '[]'`) | 존재 |
| 마이그레이션 | `20260610_0300_c6d7e8f9a0b1_problem_visualizations.py` | 착륙 |
| API | `api/problems.py:53,81,107` `response_model=ProblemSchema` | 필드를 **전선에 실어 보냄**(항상 `[]`) |
| **생산자(write)** | — | **저장소 전역 0건** |
| **소비자(read)** | — | **backend 0 · mobile 0**(`src/mobile/lib/features/problems/`에 시각 축 참조 0) |
| **데이터** | problem bank 전수 파싱 | **0건 / 0건** — 2026-08-11 `2,647`문제 · **2026-08-14 재실측 `2,638`문제**(코퍼스 자체는 3일간 바뀌었는데 시각화 축은 양쪽 실측 모두 0. 분모가 움직여도 분자가 0인 것이 이 갭의 성질이다) |

`MEMORY.md:186` 슬라이스 91(2026-06-10)은 이 컬럼의 의미를 이렇게 선언했다:
> "🎯 **의미**: 문제가 선언적 시각화 명세를 *구조로* 싣고 API가 그대로 전달 — '배선'이 눈에 보이게
> 완성"

두 달 뒤 실측은 **그 경로가 한 번도 성립하지 않았다**는 것이다. 그동안 전혀 다른 경로(개념 기반
`POST /v1/scenes/weak-concept` → 코치 채팅의 수동 버튼)가 학생 도달 **단일** 경로가 됐고,
문제 축은 아무도 돌아보지 않았다.

#### 왜 지금까지 안 드러났는가 (무증상 3겹)

1. **`server_default '[]'`·`'{}'`** — 빈 값이 정상 값과 구별되지 않는다. NULL이면 "미설정"이
   보이지만 빈 배열은 "시각자료 없는 문제"와 **같은 모양**이다(1차 D3의 "우아한 강등"과 동형이나,
   그때는 폴백이 *보였고* 이번엔 부재 자체가 보이지 않는다).
2. **API가 필드를 계속 노출한다** — 스키마 테스트·라운드트립 테스트는 green이다. 슬라이스 91의
   4게이트도 전부 통과했다. 통과한 것은 *구조*였고 흐르지 않은 것은 *데이터*다.
3. **1~3차의 점검 축 자체가 개념이었다** — D1이 "적재 0행"을 찾을 때 센 것은
   `concept_visual_style`·`concept_visualization` 두 오버레이였다. 문제 축은 질문 목록에 없었다.

이것은 **1차 D1과 정확히 같은 패턴이 다른 축에서 미발견으로 남은 것**이다. 그리고 이번엔 대장
어디에도 없다 — 열린 태스크 전건(`S4-03`·`MISC-01`·`VIZ-08`·`VIZ-09`·`OPS-26`)이 개념 축 소관이다.

#### 외부 틀과의 관계

기능 62의 절반(문제에 붙는 그래프)이 여기 걸린다. 틀의 관점에서 "함수 그래프 시각화"는 개념
설명에도 붙고 **문제에도 붙는다** — 우리는 전자만 만들었고 후자는 좌석만 팠다.

#### 설계 — 두 갈래 중 택일을 *근거와 함께* 판정한다 (본 편은 설계까지, 구현은 태스크)

판정 기준은 페르소나도 저작권도 아니라 **"학생 도달 경로가 둘일 필요가 있는가"**다.

- **(a) 살린다** — 생산자 배선(문제 생성기가 spec을 붙임) + 문제 화면 소비. 비용: 생성기 파이프라인
  변경 + 모바일 문제 화면 렌더 + 2,647건 백필 판정. **이때 반드시** 개념 축이 이미 가진 3중 게이트
  (`is_visualizable` AND `has_render_seat` AND 양식 존재)를 **재사용**한다 — 문제 축에 두 번째
  게이트 어휘를 만들면 2차 G1("양쪽 어휘가 다르다")의 재발이다.
- **(b) 죽은 스키마로 확정한다** — 의도적 미채택 선언 + 컬럼·필드 제거 경로 등재(dead code 금기).
  근거가 될 수 있는 것: 학생 도달 경로는 개념 기반 장면 1개로 충분하고, 문제별 spec은 **개념
  단위로 재사용되는 시각화**를 문제 수만큼 복제하게 만든다(anti-explosion 역행).

**현시점 권고는 (b)에 가깝다** — 개념 축이 이미 end-to-end로 살아 있고, 같은 그림을 문제마다
복제하는 것은 "교육적으로 압축된 인지 그래프" 원칙에 역행한다. 다만 **판정은 실측 기반이어야
하므로** 태스크 acceptance ①을 "현행 실측 고정"으로 두고 (a)/(b)를 그 위에서 결정하게 한다.

어느 쪽이든 지킬 것:
- **억지 태깅·억지 생성 금지** — 미좌석은 보류가 정답(`VIZ-01` acceptance③ 선례)
- **acceptance에 정본화와 집행 지점 별항 분리**(CLAUDE.md 2026-08-04)
- **변별력** — (a)면 도달률 0→N 양방향, (b)면 제거 후 잔존 참조 0을 게이트로 동결

**하지 말 것**: 문제 축에 새 `VisualizationType`·새 게이트 함수·새 계약 파일을 만드는 것
(전부 개념 축에 이미 있다) · 2,647건 LLM 일괄 태깅(검증 앵커 없음).

**→ `VIZ-10-problem-axis-visualization-dead-chain`**(신규 등재·S4·priority 3 — CLI 배정 번호.
`VIZ-07`~`09`는 회수한 R3가 점유하므로 CLI가 `VIZ-10`을 부여했다)

### §3.1 갭 아님 판정 — 착수 시 후보였다가 실측으로 기각된 것

| # | 후보 | 판정 |
|---|---|---|
| ① | **개념 Overlay 프로덕션 자동 적재 미배선** — `.github/workflows/deploy.yml`·`docker-compose.prod.yml`·`Dockerfile`에서 populate/seed 호출 **0건**이고 `l1/atom_graph/populate.py:261`은 수동 CLI다 | **갭 아님** — `VIZ-01` acceptance ④가 정의한 배선은 "`populate_atom_backbone` 확장 또는 동급 CLI"이고, 두 오버레이는 그 안에서 실제로 호출된다(`populate.py:175-197`). 게다가 CLI 인자 기본값이 **ON**이다(`--visual-style-corpus`·`--visualization-corpus`가 `DEFAULT_*_CORPUS`로 기본 지정 — 건너뛰려면 빈 문자열을 **명시**해야 한다). deploy.yml 미호출은 원자 백본 코퍼스 전체에 해당하는 **프로젝트 전반의 배포 자세**이지 시각화 축 갭이 아니다 — 시각화 태스크로 등재하면 축이 틀린다 |
| ② | **Scene DSL↔Visualization DSL 중복** | **갭 아님 — 축 종료**(§2) |
| ③ | **`ruff format --check` 16파일 red** | **시각화 축 아님** — CI는 이 명령을 쓰지 않는다(포맷 권위는 `black`, exit 0). 관찰로만 기록(§7) |

---

## §4. 검증 — 무엇을 돌렸고 결과가 무엇인가

회수가 코드를 병합했으므로 문서 편이지만 **전 게이트를 돌렸다**. CI가 쓰는 명령 그대로,
판정은 exit code로 한다.

| 게이트 | 명령(CI 동일) | 결과 |
|---|---|---|
| Ruff (src + tests) | `ruff check . ../../tests/backend` | `All checks passed!` · **EXIT=0** |
| Black (src + tests) | `black --check --line-length 100 . ../../tests/backend` | 1,115 files unchanged · **EXIT=0** |
| Mypy strict | `mypy --strict whymath_backend` | 485 source files, no issues · **EXIT=0** |
| Import contracts | `lint-imports` | 7계층 단방향 KEPT · 1 kept, 0 broken · **EXIT=0** |
| 웹 계산기 | `npm test` (`src/web/graphing-calculator`) | **134 passed / 9 files** · **EXIT=0** |
| 백엔드 전체 스위트 | `pytest` (무작위 순서·부분 실행 아님) | **9,418 passed · 304 skipped · 0 failed** · **EXIT=0** (§4-a) |
| PR #812 CI(회수분 최종 판정) | GitHub Actions 16잡 | **16/16 성공·실패 0** — `backend — lint·type·test` 26분 39초 포함. **`mobile — flutter analyze·test` 성공**이 아래 정직한 공백의 Flutter 항목을 사후 해소했다 |
| 계약 변별력 | 뮤테이션 → 원복 | red **EXIT=1** → green **EXIT=0** (§1.1) |

*`ruff format --check`는 16파일 red지만 **CI는 이 명령을 쓰지 않는다**(포맷 권위는 `black`).
CI 명령을 그대로 재현한다는 원칙에 따라 판정에서 제외하며, 이 사실을 §7 정직한 공백에 남긴다.*

### §4-a 백엔드 전체 스위트

```
9418 passed, 304 skipped, 17 warnings in 792.44s (0:13:12)     EXIT=0
Python 3.12.3 · pytest 9.1.1 · randomly-seed=2721462717 (무작위 순서)
```

**부분 스위트가 아니다** — 디렉터리·파일 단위 실행은 순서 의존 오염을 구조적으로 볼 수 없으므로
(CLAUDE.md 2026-07-26 등재) 전체를 돌렸다. R3가 자기 브랜치에서 기록한 9,230 passed와의 차이
(+188)는 본 회수가 main의 14커밋(`MOB-14`·`PED-16`·`OPS-25`·교육과정/개념 r2·r3 등)을 함께
담고 있기 때문이며, 회수 병합이 만든 회귀는 **0**이다.

**환경 주의**: 이 컨테이너는 Python 3.11이 기본이라 백엔드(요구 ≥3.12)를 그대로 돌릴 수 없었다.
`/usr/bin/python3.12` venv를 만들어 `src/backend`와 **`src/data-pipeline` 둘 다** 설치한 뒤에야
수집이 성립한다(후자를 빼면 `l1` 거버넌스 테스트 6건 + `harness/test_qa_pipeline.py`가
`ModuleNotFoundError: data_pipeline`으로 수집 실패 — CI는 `.github/workflows/ci.yml:230`에서
`pip install -e ../data-pipeline`로 이미 해결해 둔 것과 같은 조건).

---

## §5. 반복 실수 — "같은 날 세 세션이 같은 경계를 각각 판정" (재발방지 등재)

### 사고 구조

2026-08-10 하루에 세 세션이 **같은 경계**를 건드렸다:

| 세션 | 산출 | 그 축에 대한 처분 |
|---|---|---|
| #756 DSL 통합 점검 | `dsl_integration_gap_review.md` | Scene↔Viz 스키마 축 **판정·해소** + 서빙 축을 `PED-16`으로 이연 |
| `PED-16` | `api/_concept_orchestration.py` | 서빙 축 **해소** |
| 시각화 R3 | 원본 §8 | 같은 축을 "4차 최우선 후보"로 **이월** — 그리고 **고립** |

R3가 잘못한 것이 아니다. R3는 main을 흡수했고(`4bb6cd10`에서 #755·#757 반영), 그 시점에 #756이
아직 자기 브랜치 base에 없었을 뿐이다. **구조가 이것을 못 막는다**는 것이 문제다.

기존 방어 장치는 두 개인데 둘 다 이 경우에 발화하지 않는다:
- **`HARN-14`**(미머지 브랜치의 신규 `*_gap_review.md`를 SessionStart 브리핑에 노출) — *문서 파일*의
  중복 착수는 잡지만, **이미 main에 병합된 자매편이 내 축을 선점한 경우**는 잡지 않는다.
  실제로 이번 세션의 브리핑에도 R3 브랜치는 뜨지 않았다(§7).
- **원격 claim 대장** — *태스크* 단위라 "판정 축" 단위 선점은 보이지 않는다.

### 대책

이월 축(다음 편 후보)을 문서에 적을 때 **그 축의 이름으로 저장소 전역을 먼저 훑는 것**을 규약으로
만든다 — 특히 같은 날짜의 자매 리뷰. 비용이 거의 없고(grep 1회) 막는 손실이 크다(4번째 중복 판정).
본 편은 규칙 산문 대신 **다음 편이 실제로 밟게 되는 지점**에 심는다: 이월 축 섹션(§7 정직한 공백)에
**"선점 확인 결과"를 필수 항목으로 병기**한다. 이 형식을 본 편이 처음 적용한다(§7).

CLAUDE.md 등재는 하지 않는다 — 헌법은 이미 "미머지 고립" 계열 규칙을 3건 갖고 있고, 이 건은 그
규칙들의 *변형*이지 새 축이 아니다. 텍스트 규칙 4번째를 더하는 것보다 문서 템플릿에 항목을 박는
것이 집행력이 높다(CLAUDE.md "만료 없는 유예·제외 금지"가 텍스트 규칙 2회 실패 후 코드로 착지한
선례와 같은 판단).

---

## §6. 등재 요약

| 태스크 | 설계 | stage | layer | priority | 근거 | 상태 |
|---|---|---|---|---|---|---|
| `VIZ-10-problem-axis-visualization-dead-chain` | §3 D5 | S4 | backend | 3 | 생산자 0·소비자 0·2,647/2,647 공백 | 신규 등재(todo) |
| `VIZ-07`(회수) | §1.1 | S4 | backend | 2 | acceptance 전수 재대조 미이행 0 | **회수·done 유지** |
| `VIZ-08`·`VIZ-09`·`OPS-26`(회수) | R3 §8.3·§8.4·§8.1 각주 | — | — | — | 고립분 회수 | **회수·todo 유지** |

태스크는 전건 `backlog.py add` CLI 경유로 등재한다(ID 손편집 0 — HARN-10 번호 충돌 검사가 로컬
백로그 + 원격 claim + 원격 브랜치 `backlog/tasks/` 파일명까지 교차 검사한다. **`VIZ-07`~`09`는
R3가 이미 점유**하므로 CLI가 제안하는 번호를 그대로 쓴다). `--path` 선언으로 겹침 검사를 켠다.

**중복 회피 확인** — 열린 시각화 태스크 전수와 소유 범위:
`S4-03` = 새 `VisualizationType`(기하·벡터·행렬·Manim) · `MISC-01` = 오개념 교정 시각화 shadow
결선 · `VIZ-08` = 문서·주석 정합 · `VIZ-09` = 명세 sympify 파싱 게이트 · `OPS-26` = vendored 번들
신선도 게이트. **어느 것도 문제(problem) 축을 포함하지 않는다** — D5는 침범 0.

### 미등재 트리거 (dead task 방지 — 발화 조건만)

- §0-③ 승계분 전건 — 발화 조건 불변
- **Scene↔Viz 재판정** — 발화 조건은 §2.3의 두 가지(값 복제 시작 / 파서 2벌째)뿐. 그 외에는
  다시 열지 않는다

---

## §7. 정직한 공백 — 하지 않은 것

- **prod DB 실측이 아니다** — 이 세션은 Phaiakes9 `whymath-pg`(5433)에 접근할 수 없다. D5의
  "2,647/2,647 공백"은 **코퍼스 파일 전수 파싱** 결과이며, prod `problem` 테이블의 실제 행
  분포가 코퍼스와 다를 가능성은 배제하지 못한다(생산자 grep 0건이므로 다를 이유는 없으나 확인은
  아니다). 1~3차가 동일하게 남긴 공백이다.
- **라이브 LLM 재현 없음 · 실기기 확인 없음** — 1~3차와 동일.
- **`ruff format --check` 16파일 red** — CI가 쓰지 않는 명령이라 판정에서 제외했다(포맷 권위는
  `black`, 그쪽은 exit 0). 두 포매터가 같은 저장소에서 서로 다른 판정을 내는 상태 자체는 관찰로만
  기록하고 태스크화하지 않는다(시각화 축 아님).
- **기능 64·65 세부 재대조는 이번에도 하지 않았다** — 다만 1차 이후 판정을 뒤집을 코드 변경이
  없음을 근거로 확인했다: `manim` 의존성 0 · `import manim` 0 · **CI 금지 게이트 실재**
  (`test_build_roadmap_boundary_gate.py:34` `_FORBIDDEN_IMPORT`) · `render_contract.json`
  `animation_prerendered.web_adapter: null` · 생성 경로는 `VIZ-02`가 봉인. 즉 기능 64는 방치가
  아니라 **3중 의식적 봉인**이며, 기능 65는 확률·몬테카를로만 실재(`SimulationSpec` + `runTrials`
  가중추출·`MAX_TRIALS` 50000 가드)한다는 1차 판정이 유효하다.
- **`_r4` 이월 축 선점 확인**(§5가 신설한 형식): 본 편이 다음 편으로 넘기는 축은 **없다**.
  D5는 이월이 아니라 태스크로 등재했고, Scene↔Viz는 §2.3에서 닫았다. 따라서 선점 확인 대상 0건.
- **`HARN-14` 브리핑이 R3 브랜치를 노출하지 않았다** — 본 세션의 SessionStart 브리핑
  "📄 미머지 브랜치의 신규 설계 문서" 목록에 `claude/whymath-visualization-review-r3`가 없다.
  R3는 **새 문서를 만들지 않고 기존 문서에 §8을 증축**했기 때문으로 보인다(탐지기가 신규 파일만
  본다면 in-place 증축은 구조적으로 안 보인다). 본 편은 이것을 **관찰로만 기록**한다 — 하네스
  탐지기 소관이고 시각화 축이 아니며, 추정("~로 보인다")을 근거로 태스크를 열면 이 저장소의
  "환경 사실의 추론 등재 금지"에 걸린다. 확인 후 등재하려면 `HARN-14` 구현을 읽어야 한다.
- **전체 스위트는 돌렸다**(§4-a — 9,418 passed·EXIT=0). **Flutter 테스트는 로컬에서 돌리지
  못했다**(컨테이너에 SDK 부재) — 회수분의 `src/mobile/` 변경이 vendored 계산기 번들
  재동기화뿐이고 dart 코드는 무변경이라 CI `mobile` 잡을 최종 판정으로 넘겼고, **PR #812에서
  성공으로 판정됐다**(§4 표). 이 항목은 사후 해소된 공백으로 기록만 남긴다.
- **2026-08-14 재착지분은 문서·백로그만이라 스위트를 다시 돌리지 않았다** — `src/` 무접촉.
  §1.3의 재실측(코퍼스 파싱·grep·파일 존재)만 수행했다. 침묵을 통과 주장으로 읽히게 하지 않기
  위해 명시한다.

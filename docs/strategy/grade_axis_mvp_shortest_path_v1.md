# 초등~대학 MVP 최단경로 — 실행계획 · 아키텍처 (제안 v1)

> **작성일**: 2026-08-05 | **상태**: **제안 (Kiki 승인 게이트 — 승인 전 구현 0)** | **근거**: 코퍼스·코드 직접 실측 + 기존 정본 문서 교차참조
>
> 이 문서는 하나의 질문에 답한다: **"초등에서 대학 과정까지 커버하는 MVP를 최단 시간에 완성하려면 무엇을, 어떤 순서로, 어떤 구조로 해야 하는가."**
>
> 기존 정본과의 관계: `business_plan_master_v1.md` 제3부(M1~M6)는 학년 확장을 **시장 진입 순서**로 배치한다(고3 → 고교 → 중등 → 초등 → 대학, ~2028). 본 문서는 그 시장 순서를 부정하지 않고, **엔지니어링 순서만 재배치**한다 — 확장이 사람 병목 뒤에 직렬로 매달릴 이유가 없다는 실측에 근거한다. 채택 시 `S4-01`의 `depends_on` 변경과 MEMORY 결정 로그 등재가 수반된다(§7).

---

## §0. 요약 — "백본은 이미 4축이다"

세 문장으로 줄이면 이렇다.

1. **막고 있는 것은 지식 백본이 아니다.** 원자 그래프는 이미 초등 464 · 중학 225 · 고등 925 · **대학 1,069** = 2,683 노드로 4축 전체를 덮고 있다. 개념·선수관계는 다 있다.
2. **막고 있는 것은 콘텐츠 커버리지와 4개의 끊긴 seam이다.** 성취기준 커버율 16.6%(초등 4.1% · 대학 0%), 그리고 학년축이 관통하지 않은 배선 4곳. 둘 다 기계 자동화 구간이다.
3. **따라서 최단경로는 순서를 바꾸는 것이다.** 고3 파일럿(사람 병목)이 도는 동안 4축 관통을 기계로 완성한다. MVP 완성선은 "성취기준 100%"가 아니라 **"4축 각각에서 1루프가 실제로 돈다"**(수직 관통)로 잡는다.

**현행 계획 대비 무엇이 달라지는가**:

| | 현행 (`business_plan_master_v1.md` 제3부) | 본 제안 |
|---|---|---|
| 순서 | M1 고3 → M2 고교 → M3 중등 → M4 초등 → M5 대학 (직렬) | 고3 파일럿 ∥ 4축 확장 (병렬) |
| 학년 확장의 선행조건 | `S3-01-pilot-cohort` (owner: kiki) | 없음 — W1 관측 리포트만 |
| 완성 정의 | 학년별 단원 커버리지 100% | 4축 × 1루프 수직 관통 |
| 임계경로 | 사람 병목(파일럿 모집·운영) | 기계 구간(콘텐츠 공장 W2) |

---

## §1. 실측 자산 스냅샷 (2026-08-05 직접 검증)

### 1.1 4축 자산 매트릭스

| 자산 | 초등 | 중학 | 고등 | 대학 | 합계 |
|---|---:|---:|---:|---:|---:|
| **원자 그래프 노드** `data/corpus/atom_graph_v1/graph.json` | 464 | 225 | 925 | **1,069** | **2,683** |
| **오개념 카탈로그** `data/corpus/misconceptions_v1/misconceptions.json` | 123 | 159 | 561 | **0** | 843 |
| **문제 코퍼스** `data/corpus/problem_bank_*/problems.jsonl` | 192 | 1,171 | 1,524 | **0** | 2,647 |

- 원자 노드 내역: 세부개념 1,823 · 소단원 643 · 단원 217 / 엣지 2,210
- 문항의 학교급은 `achievement_standard_codes` 접두(2·4·6수=초등 · 9수=중학 · 10·12=고등)로 도출했다. **240문이 복수 학교급에 걸쳐 태깅**되어 있어 행 합(2,887)이 총 문항(2,647)을 초과한다 — 이중 계산이 아니라 다중 귀속이다. 성취기준 코드 미태깅 문항 0건
- 초등 학년군: 1~2학년군 103 · 3~4학년군 144 · 5~6학년군 135 (밴드 미상 82)
- 고등 학년: 고1 208 · 고2 162 · 고3 379 (밴드 미상 176)
- 성취기준: NCIC K-12 **895 레코드**(2022 개정 고유 **435 코드**) + **대학 자체작성 409**(32과목 · 148단원 · `docs/data/standards_university_v1.md`)
- `standard_codes` 채움: 대학 원자 **512 / 1,069** (U2 부분 완료) · K-12 원자 1,311 / 1,614

### 1.2 커버리지 (`docs/data/problem_bank_coverage_2026-07.md`, 분모 = 2022 개정 435 고유코드)

| 학교급 | 커버 | 분모 | 커버율 | 0커버 |
|---|---:|---:|---:|---:|
| 초등학교 | 5 | 121 | **4.1%** | 116 |
| 중학교 | 24 | 60 | 40.0% | 36 |
| 고등학교 | 43 | 254 | 16.9% | 211 |
| **대학** | — | 409 | **분모에도 없음** | — |
| **전체** | **72** | **435** | **16.6%** | 363 |

**동반 관측 3종** (같은 리포트):
- **극단적 편중** — 상위 5개 성취기준이 전체 문항의 **46.9%**(`[10공수1-02-02]` 이차방정식 한 코드에만 517문).
- **난이도 사다리 부재** — 60개 단원이 정확히 24문·단일 밴드에 갇혀 있다. "한 단원을 난이도 사다리로 관통하는 계열이 없다."
- **질문형식 2종뿐** — `QuestionFormat` 10종 중 객관식 1,631 · 단답형 1,036만 존재, 나머지 8종 0건.

### 1.3 해석

> **백본(개념·선수관계)은 4축 완비 · 오개념은 3축 · 문제와 진단은 사실상 고등 1축.**
> 대학은 성취기준만 있고 문항 0. 초등은 원자 464개 위에 문항 216개(성취기준 4.1%)만 얹혀 있다.

이것이 뜻하는 바는 낙관적이다 — **어려운 부분(교육적으로 압축된 인지 그래프)이 이미 끝나 있고, 남은 것은 반복 가능한 생산이다.**

---

## §2. 아키텍처 — 학년축이 어디로 흐르는가

### 2.1 제1원칙: 학년축 = 오버레이 파라미터, 구조 분기 아님

CLAUDE.md 8대 구조 원칙 ⑤ **Curriculum은 Overlay**(개념은 영속, 매핑만 교체)와 `status_roadmap_2026-07.md` §5 S4 질문 4("학년 축이 프롬프트 파라미터인가 구조 분기인가")의 답을 **코드로 못 박는다**.

실측상 이 원칙은 이미 대체로 지켜지고 있다:
- `subject == "math"` 류 과목 분기 — **0건**
- `school_level`은 DB **필터 컬럼**(`db/models/atom_node.py:96`, 보조 인덱스 `ix_atom_node_school_level`)이지 제어 흐름이 아니다
- `grade`는 이미 L4 프롬프트 **인자**로 thread되어 있다 (`l4/pedagogy/prompt_assembler.py:85,122` — `grade: int | None`)
- 발화 프로파일은 이미 **4밴드**다 (`l4/speech/profiles.py` — 초등·중등·고등·대학)

### 2.2 구조도 — 학년축의 흐름

```
        ┌─────────── 학년축(grade_band) — 단일 오버레이 파라미터 ───────────┐
        │  초등(1~2 / 3~4 / 5~6군) · 중학(1~3군) · 고등(고1/고2/고3) · 대학  │
        └──┬──────────────┬──────────────┬───────────────┬─────────────────┘
           │ 필터          │ 무관         │ 프롬프트 인자  │ 게이팅·요구깊이
           ▼               ▼              ▼               ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ L1 데이터 기반          atom_node.school_level · achievement_standard      │
│  ✅ 4축 완비            misconception.school_level                        │
│  ⚠️ 문항만 1축          problem.unit_codes · achievement_standard_codes    │
│                          ▲ ─────── 여기만 축별로 채우면 된다 (W2·W3)      │
├──────────────────────────────────────────────────────────────────────────┤
│ L2 학습자 모델          BKT · IRT · θ 추정 · mastery                       │
│  ✅ 학년 무관            문항 파라미터만 본다 → 코드 변경 0                 │
├──────────────────────────────────────────────────────────────────────────┤
│ L3 생성·검증            라우터 · SymPy 동치(단일 권위) · 3-tier            │
│  ✅ 학년 무관            검증 축만 plugin:                                 │
│                            SymPy(계산·산술·대수·미적)                      │
│                            유한 전수열거(확률) — l3/finite_probability.py  │
│                            LLM 교차검증 K≥3(증명·개념) — l3/cross_verify.py│
├──────────────────────────────────────────────────────────────────────────┤
│ L4 교수학 엔진          Polya · 소크라테스 · LTHC · 오개념 reactive        │
│  🟡 인자는 있고          grade → prompt_assembler 계층4 (이미 배선)         │
│     문구가 하드코딩       speech 4밴드 (이미 배선)                          │
│                          ⚠️ socratic_template.md가 "중·고등학교"로 고정     │
├──────────────────────────────────────────────────────────────────────────┤
│ L5 상호작용             온보딩 학년 입력 → PATCH /v1/users/me → 세션 컨텍스트│
│  🔴 온보딩이 학년을 안 받음 (순수 인트로 캐러셀·데이터 수집 0)              │
├──────────────────────────────────────────────────────────────────────────┤
│ L6 응용 모드            모드 게이팅 6종 × RequiredDepth                    │
│  🟡 대학 깊이 미정의     앵커: awareness 1.5 / procedural 2.5              │
│                                / conceptual 3.5 / mastery 4.5             │
└──────────────────────────────────────────────────────────────────────────┘
```

**결론적 형태**: 코어는 **하나**다. 학년이 4축으로 늘어도 **새 엔진·새 계층·새 관계타입·새 L6 모드는 생기지 않는다.** 늘어나는 것은 L1의 행(row)과 L4 프롬프트의 인자값뿐이다. 이것이 "최단"이 성립하는 구조적 이유다.

### 2.3 지금 학년축이 *끊겨 있는* 4개 seam

| # | seam | 위치 | 현 상태 | 조치 |
|---|---|---|---|---|
| **S-1** | 커리큘럼 오버레이에 **대학 밴드 없음** | `l1/curriculum/curriculum_loader.py:143-166` | `_GRADE_BAND_TO_INTRODUCED_GRADE` · `_GRADE_BAND_TO_REQUIRED_DEPTH` 둘 다 "고등학교"까지. 대학은 `None` 폴백(정직하나 미배선) | 대학 밴드 추가 + RequiredDepth 정책 결정 |
| **S-2** | 교수학 템플릿이 **중·고로 하드코딩** | `docs/prompts/socratic_template.md:32` | "한국 **중·고등학교** 수학 메타인지 코치" — 초등·대학에서 페르소나가 어긋남 | 학년 밴드별 register 파라미터화 |
| **S-3** | 커버리지 리포트 분모에 **대학 없음** | `harness/problem_bank_coverage.py` | 분모 = NCIC K-12 435만. 대학 409는 관측 밖 | 분모 확장 + `school_level` 축 |
| **S-4** | 온보딩이 **학년을 안 받음** | `features/onboarding/presentation/onboarding_screen.dart` | 순수 인트로 3페이지 캐러셀·수집 0 (`s1_e2e_vertical_slice_design.md` §1①에 🔴 기록) | 학년·트랙 입력 → `PATCH /v1/users/me` |

**네 개 전부 소규모 배선이다. 신규 알고리즘 0.**

---

## §3. 실행계획 — 작업 패키지 W0~W6

> **전략의 핵심**: `backlog/tasks/S4-01-math-k12-complete.yaml`의 `depends_on: [S3-01-pilot-cohort]`를 끊는다.
>
> **근거**: 확장은 "신규 발명이 아니라 커버리지 작업"이다(`status_roadmap_2026-07.md` §3 S4). 파일럿 결과는 확장의 *전제*가 아니라 *품질 보정 입력*이며, 파일럿에서 나온 교정은 파이프라인 재실행으로 4축에 일괄 반영된다. 직렬로 묶을 공학적 이유가 없다.
>
> **감수하는 리스크**: 미검증 루프를 4축으로 확대한다. 이를 상쇄하는 장치가 W6(4축 E2E 게이트)과 기존 검증 서열(SymPy 기계증명 → 측정 통과 게이트 → 인간 폴백)이다. 파일럿이 교수학적 결함을 발견하면 L4 프롬프트 인자와 생성 조건을 고쳐 재실행한다 — 구조를 고치는 게 아니라 **파라미터를 고치는 것**이므로 상환 비용이 선형이다.

### W0. 학년축 관통 배선 + 거버넌스 동결 — **임계 선행**

**왜 먼저**: 축이 안 뚫린 상태에서 콘텐츠를 4축으로 부으면 나중에 두 번 고친다. 가장 싼 지금 뚫는다.

| 항목 | 파일 | 내용 |
|---|---|---|
| S-1 해소 | `l1/curriculum/curriculum_loader.py` | 대학 밴드를 2종 맵에 추가. 대학 `RequiredDepth`는 `mastery` 일괄 고정이 아니라 과목 성격별로 두고, 미지 밴드는 기존 `None` 정직 폴백 유지 |
| S-2 해소 | `docs/prompts/socratic_template.md` · `polya_4step.md` | 학년 밴드 register를 파라미터화. **기존 `grade` thread(`prompt_assembler.py:85,122`)를 재사용하고 신규 인자 추가 금지** |
| 어휘 통일 | `l4/speech/profiles.py` | `SpeechGradeBand`(초등/중등/고등/대학)를 학년축 정본 어휘로 승격. 코퍼스 `school_level`(초등/중학/고등/대학)과의 표기 차이를 매핑 함수 **1개**로 통일 |
| 동결 | `tests/backend/**` 신설 | **학년축 구조분기 금지 거버넌스 테스트** — `if school_level ==` / `if grade_band ==` 형태의 제어 분기가 L2~L4에 나타나면 CI 실패. `test_embedding_namespace_governance.py` 패턴 답습 |

**DoD**: 4축 어휘가 단일 매핑을 경유하고, 구조분기 금지가 CI에서 실제로 돈다(존재 ≠ 돌아감 — CLAUDE.md 배선 확인 규칙).

### W1. 4축 관측 — 커버리지 리포트 확장

**왜**: "관측 없는 확장 금지"(`ARCH-18` notes). 지금 대학은 분모에조차 없다. **이 리포트가 W2 저작 우선순위의 유일한 입력이다.**

- `harness/problem_bank_coverage.py` — 분모에 `data/corpus/standards_university_v1/standards.json`(409) 추가, 출력에 `school_level` 축(초/중/고/대) 신설.
- **exit 게이트로 만들지 않는다** — 종료코드 0(성공)/2(입력오류) 규약과 "관측 도구" 성격을 보존한다. 커버율이 낮다고 CI를 빨갛게 만들지 않는다.
- 산출물: `docs/data/problem_bank_coverage_4axis.{md,json}`

**DoD**: 리포트에 초·중·고·대학 4행이 나오고 대학 분모 409가 실린다.

### W2. 콘텐츠 공장 4축 가동 — **임계경로**

**왜 최대 작업**: 초등 4.1% · 대학 0%. **실제 소요 시간을 결정하는 단 하나의 패키지다.**

**재사용하는 것 (신규 발명 0)**: 15개 생성기 → SymPy 동치 게이트 → WH-S replay → PRM → AI 검수 Wilson 게이트. S2에서 **620문 · Wilson 상한 1.11%(≤2%) PASS**로 이미 실증된 경로다(`ai_review_batch_240_2026-07.md`).

**축별 프로파일** (단원·난이도는 W1 리포트가 지정):

| 축 | 우선 영역 | 검증 축 | 비고 |
|---|---|---|---|
| **초등** | 수와 연산 · 도형과 측정 (0커버 116) | SymPy 그대로 (산술·분수·측정) | 난이도 밴드를 awareness/procedural로 하향. 최대 공백이자 최대 성과 구간 |
| **중등** | 0커버 36코드 | SymPy | 40% 커버라 증분 최소 |
| **고등** | 0커버 211 중 **커버율 0.0% 영역** — 집합과 명제·경우의 수·통계·적분법·적분·이차곡선·행렬 | SymPy + **유한 전수열거**(`l3/finite_probability.py` · S4-13 done) | 확률·통계가 이미 경로를 갖고 있다 |
| **대학** | 1~2학년 기초 4과목 — 미적분학·선형대수·확률통계·미분방정식 | SymPy 재사용률 최대(계산 중심) | **증명 중심 과목(위상·갈루아·실해석)은 MVP 범위 밖** — §5 참조 |

**동시 상환할 결함 2종**:
- **난이도 사다리** — 4축 생성 시 **단원당 최소 3밴드**를 생성 조건으로 건다(현재 60개 단원이 단일 밴드에 갇힘).
- **편중** — 코드당 문항 수 상한을 두어 상위 5코드 46.9% 쏠림이 재생산되지 않게 한다.

**DoD**: 축별 0커버 코드 수가 W1 리포트 대비 감소 · 생성분 전량 SymPy/전수열거 게이트 통과 · AI 검수 Wilson 상한 ≤ 2%.

### W3. 대학 자산 완결 (U2 · U3)

- **U2**: 대학 원자 1,069의 `standard_codes` 잔여 **557건**을 소단원↔성취기준 1:1 맵으로 채움. 입력은 `data/corpus/standards_university_v1/concept_standard_links.json`(409건·1:1·멀티값 0).
- **U3**: `l1/standards/standard_loader.py`로 `achievement_standard` + `concept_standard_link` 멱등 적재.
- **검수 게이트 (우회 금지)**: 대학 성취기준 본문은 **AI 추정 초안**이다(`standards_university_v1.md` §4 — "노출 전 검수 게이팅 대상"). S2에서 확립된 **AI 검수 + Wilson 표본 게이트**를 적용하고, 통과 전 `is_published=false`를 유지한다. 검수 없는 AI 초안 노출은 5중 해자 중 **④신뢰**를 직접 훼손한다.
- 라이선스: 대학 본문은 **자체작성**이라 NCIC redaction 불요(K-12와 취급 분리) — 이것이 대학 축을 싸게 만드는 두 번째 이유다.

### W4. 진단 4축 — CAT가 초등·대학에서도 성립

- `l1/atom_probe/` 프로젝션에 초·중·대학 프로브 공급.
  - ⚠️ **중복 착수 금지** — `REC-02-misconception-probe-supply`(오개념 표적 프로브 공급 · "select_probe 후보 0" 해소)가 **다른 세션 claim 중**이다. 그 산출물을 **소비만** 한다.
- **L2는 변경 0** — BKT·IRT는 학년 무관(문항 파라미터만 본다). 검증은 "초등 문항만으로 θ 추정이 수렴하는가" 테스트로만.
- **대학 오개념 0건**: MVP에서는 미보유를 **정직하게 표시**하고, reactive retrieval이 빈 결과에 graceful하도록만 보장한다. 신규 카탈로그 저작은 범위 밖(§5).

### W5. L5 / L6 — 학년 선택과 모드 라우팅

- **온보딩(S-4 해소)**: 학년·트랙 입력 추가 → `PATCH /v1/users/me`(`api/users.py:103` 화이트리스트 재사용) → `UserTrackHistory` 쓰기 좌석 배선.
- **L6 라우팅 (모드 신설 금지)**:
  - 초등·중등 → 학교진도 · 메타인지 · 사고력 모드
  - 고등 → 수능 모드 (현행)
  - **대학 → 학교진도 모드의 오버레이로 흡수** — `l6/school_progress/gating.py`의 depth 앵커만 확장한다. L6는 순수 결정 로직(부수효과 0·L1만 의존)이므로 모드를 늘릴 이유가 없다.

### W6. 4축 E2E 게이트 — **MVP 완성의 정의**

- 축별 E2E 시나리오 **4본** (초·중·고·대학 각 1루프):
  `온보딩 → CAT 진단 → 문제 제시 → 풀이 입력 → WH-1 코칭 → 3-tier 검증 응답`
- **mock-우선 · hermetic** — 라이브 키 불요. `s1_e2e_vertical_slice_design.md` §2가 전 구간 hermetic 구동 가능함을 이미 실증했다(coach 결정론 · verify SymPy · L2 순수수치 · judge Fake).
- **성능 게이트**: 원자 2,683 전량 활성 상태에서 traversal 예산(visited set · timeout · token budget, **depth ≤ 2 / nodes ≤ 12~20 / tokens ≤ 3000**) 실부하 통과 — `S4-01` acceptance 그대로.

---

## §4. 순서와 병렬성

```
W0 학년축 배선 ──┬── W1 4축 관측 ──┬── W2 콘텐츠 공장 4축 ───────────┐ ◀ 임계경로
  (선행 필수)     │                 ├── W3 대학 자산 U2·U3 ──────────┤
                  │                 └── W4 진단 프로브 4축 ──────────┤
                  └── W5 온보딩 학년 입력 + L6 라우팅 ───────────────┤
                                                                     └─▶ W6 4축 E2E = 완성 판정

 [병렬·독립] 고3 파일럿 S3-01 (owner: kiki) ─── 결과는 W2 재실행으로 4축에 일괄 반영
```

**소요를 결정하는 것은 W2 하나다.** W0·W1·W5는 배선이라 짧고, W3·W4는 W2와 동시 진행할 수 있다.

**기간을 약속하지 않는 이유**: 프로젝트 규약이 "게이트가 기간에 우선한다"(`ROADMAP.md`)이며, 신뢰할 수 있는 견적은 **W1 리포트가 나온 뒤에만** 가능하다 — 입력이 `0커버 코드 수 × 코드당 생성 시도 × 게이트 통과율`이고, 이 중 초등·대학의 게이트 통과율은 **아직 한 번도 측정된 적이 없다**(현 통과율 실측은 고등 대수·미적 계열에서만 나왔다). 추정치를 여기에 적으면 §1의 실측과 같은 무게로 읽히므로 적지 않는다.

---

## §5. 명시적으로 하지 않는 것

| 제외 | 이유 |
|---|---|
| ❌ 성취기준 100% 커버 | MVP 완성선이 아니다. 축별 대표 단원 관통이 완성선이며, 100%는 이후 파이프라인 반복으로 채우는 **양의 문제** |
| ❌ 대학 증명 중심 과목 (위상·갈루아·실해석) | 검증 권위 부재 — SymPy가 안 닿고 LLM 교차검증만 남는다. `S4-02`(증명 학습 지원)·`S4-16`(교차검증 강등전) 이후 |
| ❌ 대학 오개념 카탈로그 신규 저작 | 843건 중 대학 0. 빈 결과 graceful만 보장하고 저작은 별건 |
| ❌ L6 모드 신설 | 대학은 학교진도 오버레이로 흡수. 모드 폭발은 관계 폭발과 같은 부류의 붕괴 |
| ❌ 잔여 8종 질문형식 (서술형·합답형 등) | 채점 권위가 별개 문제 — 서술형 채점은 E4·E5의 선행 기술 |
| ❌ 공개 β 전제조건 (`MGMT-01` 법정대리인 실 본인확인 · `MGMT-02` 약관·처리방침 변호사 검토) | 두 태스크는 blocked 상태로 **의도적 보류**(2026-07-27 Kiki 지시 — 외부 작업이 미완성 제품의 병목이 되는 것 반대). **단, 초등 확장은 14세 미만 비중을 높이므로 공개 β 시점에 반드시 선행되어야 한다** — 본 문서는 그 전 단계까지만 다룬다 |

---

## §6. 검증 방법

```bash
# ① 4축 커버리지 관측 (W1 산출물 — 결정론·같은 입력이면 같은 바이트)
cd src/backend
python -m whymath_backend.harness.problem_bank_coverage \
    --json ../../docs/data/problem_bank_coverage_4axis.json

# ② 전체 스위트 — 부분 스위트 통과를 "회귀 없음"의 근거로 쓰지 않는다 (CLAUDE.md 프로세스 금기)
cd src/backend && python -m pytest ../../tests

# ③ 4게이트 + 계층 단방향 강제
ruff check . && black --check . && mypy --strict . && lint-imports
```

**패키지별 판정 기준**:

| 패키지 | PASS 조건 |
|---|---|
| W0 | 4축 어휘가 단일 매핑 경유 · 학년축 구조분기 금지 테스트가 **CI에서 실제로 실행** |
| W1 | 리포트에 초·중·고·대학 4행 · 대학 분모 409 실림 |
| W2 | 축별 0커버 코드 감소 · 생성분 전량 SymPy/전수열거 통과 · AI 검수 **Wilson 상한 ≤ 2%** |
| W3 | 대학 원자 `standard_codes` 1,069/1,069 · ORM 멱등 적재 · 검수 전 `is_published=false` |
| W6 | 4축 E2E 4본 green · traversal 예산 실부하 통과 |

**정직 규약**: 전체 스위트를 못 돌렸으면 **"전체는 확인하지 못했다"를 명시**한다 — 침묵은 통과 주장으로 읽힌다(2026-07-26 OPS-06 사고 교훈).

---

## §7. 채택 시 수반되는 절차 (하네스 정합)

1. **`S4-01`의 `depends_on: [S3-01-pilot-cohort]` 제거** — 백로그 변경이므로 **`scripts/harness/backlog.py` 경유**. YAML 손편집 금지(CLAUDE.md 거부 우회 금지 · 2026-07-16 S1-14 사고 선례).
2. **W0~W6 태스크 등재** — 반드시 `backlog.py add`. 번호 추론 배정 금지(HARN-10 · ARCH-13/OPS-15 중복 배정 선례).
3. **MEMORY.md 결정 로그 등재** — 순서 재배치는 `business_plan_master_v1.md` 제3부(M2~M5 순차)와 충돌하는 결정이므로, 채택 시 근거와 함께 기록하고 해당 문서에 교차참조를 단다.
4. **병렬 세션 확인** — 착수 전 `backlog.py claims`. 현재 `REC-02`(W4 인접)·`CUR-02`(W1 인접)·`S4-10`·`S4-14`·`S4-16`이 타 세션 claim 중이다.

---

## §8. 참조 인덱스

| 근거 | 위치 |
|---|---|
| 현 상태·S0~S5·병목 | `docs/strategy/status_roadmap_2026-07.md` |
| 사업 Phase·M축·E축 통합 | `docs/strategy/business_plan_master_v1.md` |
| 커버리지 실측 | `docs/data/problem_bank_coverage_2026-07.md` (+`.json`) |
| 대학 자산 | `docs/data/standards_university_v1.md` · `concept_content_university_v1` |
| E2E 루프 실코드 매핑 | `docs/architecture/s1_e2e_vertical_slice_design.md` |
| 검증 권위 서열 | `docs/standards/superhuman_verification_standard.md` |
| 저작권 매트릭스 | `docs/data/licensing_safety.md` |
| 실행 정본 | `backlog/` (`python3 scripts/harness/backlog.py status`) |

---

**버전**: 1.0 (제안) | **작성**: 2026-08-05 | **다음 갱신**: W1 리포트 산출 시 — §4의 "기간 미기재"를 실측 견적으로 교체

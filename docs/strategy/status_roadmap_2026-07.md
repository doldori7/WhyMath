# WhyMath 진행상태 평가 · 수학 완성 로드맵 · 단계별 AI 질문 리스트

> **작성일**: 2026-07-03 | **기준 커밋**: `65f7c37` (PR #419) | **정본 근거**: MEMORY.md 결정 로그 + 코드베이스 실측
>
> 이 문서는 세 가지에 답한다: ① 지금 어디까지 왔고 그것이 얼마나 건강한가 ② 타과목(물리 우선) 확장을 전제로 수학 과목을 완성하기까지 어떤 단계를 밟는가 ③ 각 단계에서 정확한 판단을 내리기 위해 AI에게 무엇을 물어야 하는가.
>
> ROADMAP.md("현재 위치" 2026-05-28 정체)와 실제 진행 사이의 갭을 메우는 **사실상의 로드맵 갱신본**이다. 자매 문서: `docs/standards/build_checkpoint_questions.md`(진행 축), `docs/standards/playbook_part_review_questions.md`(설계-준수 축), `docs/strategy/subject_expansion_roadmap_review.md`(다과목 확장).

---

## §1. 현재 진행상태 스냅샷 (2026-07-03)

### 1.1 계층별 코드 실재 판정

| 계층 | 판정 | 실측 근거 |
|---|---|---|
| **L1 데이터 기반** | 🟢 실질 구현 | 43파일/~7.3k줄. 개념·원자·스킬 그래프, 성취기준, 오개념, 임베딩(pgvector). 원자 1,837·선수엣지 3,220(초중고+대학 4축), 오개념 839, 성취기준 895 |
| **L2 학습자 모델** | 🟢 실질 구현 | 12파일/~2.3k줄. BKT·IRT·능력추정·mastery 추적·약개념/선수 추천·학습경로 |
| **L3 콘텐츠 생성·검증** | 🟢 인프라 완비 / 🔴 라이브 미검증 | 31파일/~6.8k줄. 라우터+Anthropic/Ollama provider, 3-tier 검증(수치→SymPy→Lean4 보류), Redis 캐시·Celery 큐·Langfuse 트레이스. **실측 비용·캐싱 튜닝은 라이브 키 대기** |
| **L4 교수학 엔진** | 🟢 최대 계층 | 62파일/~10.5k줄. Polya·소크라테스·LTHC·오개념 진단/가설/판정(judge)/shadow harvest·톤 필터·메타인지 트리거 |
| **L5 상호작용** | 🟡 골격 실동작 | 백엔드 OCR + Flutter 앱(30 lib .dart, 화면 9개: auth/chat/ocr/onboarding, WebView 그래핑 계산기). **E2E 학습 루프는 미연결** |
| **L6 응용 모드** | 🟡 게이팅만 | 6개 모드(수능·영재·메타인지·검정고시·학교진도·사고력) 각 gating.py 1개뿐. **wedge인 수능 모드조차 본격 엔진 부재** |
| **L7 커뮤니티** | ⚪ 코드 0 | 문서만. 로드맵상 Phase 3+ (의도적) |
| **WH-1 튜터링 하네스** | 🟡 골격 | 7파일/~2.4k줄. 상태 외부화+도구 루프. "측정 없는 도입 없음" 원칙으로 전략단계 보류 중 |
| **WH-S 솔버 하네스** | 🟡 골격 | 12파일/~2.6k줄. MCTS+검증기 스택. LLM 정책 모델 구동은 후속 |

### 1.2 인프라·품질 규모

- **백엔드**: 344 .py / ~64k줄 · **테스트**: 346파일 / ~92.7k줄 (**테스트 > 소스**) · **마이그레이션**: Alembic 54개 (단일 head 유지)
- **CI**: 4게이트(ruff·black·mypy --strict·pytest+coverage) + **import-linter 7계층 단방향 강제** + MATH DSL invariant 12종 코드 게이트 동결
- **데이터**: 법적 안전조합 21종 ~4M 레코드 (B/C/D/E 위험 소스 0건), NCIC 본문 redaction 삼중 방어
- **스텁/TODO 마커**: 백엔드 전체 10건뿐 — placeholder 남발 없는 실구현

### 1.3 최근 국면 (2026-06-21 ~ 07-03)

두 흐름이 병행 중:
1. **원자 백본 전면 교체** — 구 개념그래프(437개념·581엣지) → 신 원자 백본(1,837원자·3,220선수엣지). Phase 0~3 완료, **Phase 4(크로스워크)·Phase 5(구 그래프 폐기) 미완**
2. **설계 정본화 연쇄** — 플레이북 Part 2~12 설계-준수 검토, 리치 Part 2 노드 스펙 채택(ConceptNode 9계층·SkillNode 1급 승격·redaction), MATH DSL invariant 동결. 최근 30커밋 대부분이 "(코드 0)"·"(문서만)" 성격의 **정본화·스펙 동결 국면**

---

## §2. 평가

### 2.1 강점 (지키고 있는 것)

1. **L1–L4 독립 수학 코어의 두께와 순도** — 표현≠의미, Concept Purity, 노드 본문 redaction까지 플레이북 2대 철칙이 문서가 아닌 **CI 게이트로 강제**되고 있다. "구조 붕괴는 반드시 온다"는 전제를 코드로 방어 중인 프로젝트는 드물다.
2. **테스트가 소스보다 많은 문화** — 346개 테스트 파일/92.7k줄. 거버넌스 테스트(embedding namespace·edge relation·node purity·five-node connectivity)가 설계 결정을 동결하는 패턴이 정착됨.
3. **법적 안전조합 완결성** — 저작권 가이드 v2.0 기준 위험 소스 0건, 자체 동등문제 대체 전략 + redaction 삼중 방어. 나중에 못 고치는 유형의 리스크가 선제 차단됨.
4. **하네스 설계 수준** — WH-1(보상 신호 부재를 상태 외부화·가설 상한·확증편향 방지로 우회)·WH-S(기계 검증 가능성을 객관 보상으로 활용)는 학계 최신(STaR/rStar-Math)과 정합하는 설계. "측정 없는 도입 없음" 자기 절제도 유지 중.
5. **결정 로그 규율** — MEMORY.md가 진실 원천으로 실제 작동. 모든 반전(SkillNode 승격, Part 8 3분류 정정)이 근거와 함께 기록됨.

### 2.2 약점 (직시할 것)

1. **E2E 학습 루프가 한 번도 돈 적 없다** — 온보딩→진단→문제→풀이입력→코칭→검증응답의 수직 슬라이스가 미연결. 90일 체크리스트의 온보딩 흐름·자동 커리큘럼 정렬·베타·시범 사용자 5~10명 전부 미완. **부품은 고급인데 자동차가 조립된 적이 없다.**
2. **이중 truth source 활성** — 구 437 개념그래프와 신 1,837 원자 백본이 공존(마이그레이션 Phase 4·5 미완). 플레이북 7대 붕괴 연쇄 중 "유지보수 지옥"의 정확한 입구. concept→skill 매핑(#419)도 구 437 기준으로 저작되어 갭이 더 벌어지는 중.
3. **라이브 검증 정체** — L3 클라우드 튜닝(실측 비용·프롬프트 캐싱·guard_cloud 임계값), OCR Qwen3-VL 실모델, 토큰 실비용 회계가 전부 Phaiakes9 라이브 키/실머신 대기. **코드로 못 푸는 병목이 코드 뒤에 숨어 있다.**
4. **설계 정본화 국면의 장기화 징후** — 최근 한 달 커밋 대부분이 코드 0. 정본화 자체는 옳았으나(리치 Part 2 채택 등 실질 성과), 수확 체감 구간에 들어서고 있다. Part 검토는 12까지 완주했다 — **이제 검토할 Part가 없다.**
5. **사람 병목 2건 미해결** — 수학 교육 도메인 파트너(M1.3 게이트) 트랙 미정, 시범 사용자 채널 부재. AI가 대신 못 하는 항목.

### 2.3 종합 판정

> **"코어는 과잉 성숙, 제품 루프는 미성숙."**
>
> L1–L4와 품질 인프라는 Phase 1 MVP 요구를 이미 초과하는 수준이다. 반면 학생이 실제로 겪는 경험(L5 루프·L6 수능 모드)은 골격이다. 지금 가장 큰 리스크는 기술 부채가 아니라 **검증 부채** — 이 코어가 실제 고3의 30번 문제 앞에서 작동한다는 증거가 0건이다.
>
> **권고: 무게중심을 "설계 정본화"에서 "수직 슬라이스 실증"으로 이동할 시점이다.** 단, 그 전에 이중 truth source를 청산해야 한다(S0). 정본화 국면에서 얻은 규율(거버넌스 테스트·결정 로그)은 그대로 유지하되, 신규 설계 검토는 실증에서 발견된 문제에 의해서만 착수한다.

---

## §3. 수학 완성 단계별 로드맵 (S0~S5)

> **설계 원칙**: ① 각 단계는 진입 게이트·완료 정의(DoD)·탈출 게이트를 가진다 ② 타과목(물리 우선) 확장을 전제로 각 단계에 **확장 대비 불변식**을 명시한다 — 수학 완성 과정에서 "수학 전용 하드코딩"이 스며들면 확장 비용이 폭발하기 때문 ③ 기간은 1인+AI 하네스 기준 추정이며 게이트가 기간에 우선한다.

### S0. 정합성 회복 (~2주) — "하나의 진실 원천으로"

**왜 먼저**: 이중 truth source 위에 쌓는 모든 코드는 나중에 두 번 고친다. 가장 싼 지금 청산한다.

**체크리스트**:
- [ ] 원자 백본 마이그레이션 **Phase 4**: 구 437 개념 ↔ 신 원자 크로스워크 완성 (`concept_src_id` 연결), concept→skill 매핑(#419)의 원자 축 재검증
- [ ] 원자 백본 마이그레이션 **Phase 5**: 구 437 개념그래프 폐기 — 소비처 전수 조사 후 단계적 차단, 거버넌스 테스트로 재유입 동결
- [ ] prod 미적분 raw 129건 orphan 정리
- [ ] 커버리지 하니스 수정 (pgvector↔numpy 이중임포트로 api 체인 cov 수집 실패)
- [ ] ROADMAP.md "현재 위치" 갱신 — 본 문서 §3을 반영, MEMORY.md와의 갭 해소
- [ ] 리치 Part 2 잔여 중 **S1을 막는 것만** 선별 완료 (P3 ProblemTypeNode 등 — "소비처 없는 추상 미도입" 원칙으로 나머지는 보류)

**탈출 게이트**: "개념/원자/스킬 각각에 대해 truth source가 정확히 1개"를 거버넌스 테스트가 증명. 4게이트+lint-imports green.

**확장 대비 불변식**: 크로스워크·폐기 절차 자체를 재사용 가능한 패턴으로 기록 — 물리 그래프 도입 시 동일 마이그레이션 플레이북 사용.

---

### S1. E2E 수직 슬라이스 (~6주) — "한 명의 고3이 한 문제를 끝까지"

**왜 지금**: 부품 검증은 끝났다. 조립 검증이 0건이다. 이 단계의 산출물은 기능이 아니라 **증거**다.

**체크리스트**:
- [ ] **1루프 실동작**: 온보딩 → CAT 진단(웜 스타트) → 수능 미적분 동등문제 1문 제시 → 풀이 입력(MathLive 우선, OCR 병행) → WH-1 도구 루프 코칭(verify_step 3상태·오개념 reactive 매칭·LTHC 답 미루기) → 3-tier 검증 통과 응답 — **Flutter 앱에서 끝까지**
- [ ] Phaiakes9 라이브 검증 (Kiki 수동 트랙): 클라우드 키 발급 → 실측 비용/지연 측정 → guard_cloud 임계값 확정 → 프롬프트 캐싱 튜닝 → 로컬:클라우드 비율 실측 (목표 80% 로컬)
- [ ] OCR Qwen3-VL 실모델 검증 (PaddleOCR fallback 포함) — 손글씨 풀이 3단계 이상 문제 10건 샘플
- [ ] Minimal Reasoning Subgraph 예산(depth ≤ 2·nodes ≤ 12~20·tokens ≤ 3000)을 **context builder 코드에 상한으로 배선** — Part 8 검토에서 "소비처 대기"로 보류했던 항목이 이 단계에서 소비처를 얻음. 해제 트리거 계약(part8 rev.2) 준수
- [ ] 루프 전 구간 Langfuse 트레이스 + 토큰 실비용 회계 배선
- [ ] E2E 시나리오 자동 테스트 1본 (CI 야간 잡, 로컬 LLM 목킹 가능 구조)

**탈출 게이트**: ① 실기기(패드)에서 1루프 15분 내 완주 시연 녹화 ② 루프당 LLM 비용 실측치 확보 ③ "학생 응답은 전부 PRM/도구 검증 통과 후"가 코드 경로로 증명.

**확장 대비 불변식**: 루프의 어느 단계에도 `if subject == "math"` 류 분기 금지 — 과목 의존은 L1 데이터와 프롬프트 템플릿에만 존재.

---

### S2. 콘텐츠 볼륨·품질 게이트 (~8주) — "동등문제 공장 가동"

**왜 지금**: 1루프가 돌면 다음 병목은 "풀릴 문제가 몇 개 있는가"다. wedge(고3 킬러 30번)는 콘텐츠 질이 곧 제품이다.

**체크리스트**:
- [ ] 동등문제 생성 파이프라인 스케일: 미적분 킬러 시그니처 패턴 기반 자체 생성 → SymPy 동치성 게이트 → PRM 검증 → 인간 검수 큐. 목표: 검증 통과 동등문제 **100문+** (미적분 우선)
- [ ] **WH-S 솔버 가동**: 오프라인 배치로 생성 문제를 자체 풀이 → 검증 풀이 저장소·보조정리 저장소 축적 → PRM 학습셋 공급 (WH-1의 L3/L1 업스트림 원칙 유지)
- [ ] 조건 나열형 발문 파서(FR-001) 실전화 — 생성 문제 100문에 대해 파싱 정확도 측정
- [ ] 다중 풀이 생성 파이프라인 (한 문제 N접근·본인 풀이 후 노출) 최소 가동
- [ ] **도메인 파트너 트랙 (Kiki 수동, 병행)**: 후보 리스트업 → 접촉 → 검수 라운드 계약. S3 파일럿 전 최소 "검수 자문 1인" 확보가 M1.3 게이트
- [ ] 오개념 카탈로그 839건 중 미적분 영역을 생성 문제의 distractor 설계에 연결

**탈출 게이트**: ① SymPy 게이트 통과율·PRM 통과율·인간 검수 반려율 3지표 대시보드 ② 검수자 1인 이상이 샘플 30문 검토 완료 ③ 저작권 게이트 — 생성 문제 전수에 평가원 본문 유사도 검사 통과.

**확장 대비 불변식**: 생성 파이프라인의 검증기 인터페이스를 과목 중립으로 — SymPy는 "수학 커널 plugin"이지 파이프라인 본체가 아님 (물리는 단위·차원 검증기가 같은 자리에 꽂힘).

---

### S3. 수능 모드 심화 + 파일럿 (~8주) — "진짜 학생 5~10명"

**왜 지금**: 콘텐츠와 루프가 있으면 이제 측정이다. WH-1 "측정 없는 도입 없음" 원칙이 이 단계의 헌법.

**체크리스트**:
- [ ] L6 수능 모드 본격 엔진: gating.py를 넘어 — 시그니처 패턴별 학습 경로, 킬러 문항 단계별 진입(LTHC), 모의 채점 흐름
- [ ] 측정 하네스(0단계 평가 하네스) 가동: 세션별 답 미루기 도달 깊이·오개념 해소율·BKT 숙달 증가율·스스로 풀이 도달율 자동 수집
- [ ] **시범 사용자 5~10명 (Kiki 수동)**: 채널 확보 → 동의 절차(미성년 학부모 동의·14세 미만 절차) → 2주 이상 사용
- [ ] 정서 안전 점검: 톤 필터·부정 피드백 금지 규칙이 실사용 로그에서 위반 0건
- [ ] WH-1 전략단계 도입 여부를 **측정 데이터로 판정** (보류 해제 트리거: 측정 하네스 가동 + 베이스라인 확보)
- [ ] 개인정보 실전 점검: `DELETE /v1/me`·`GET /v1/me/export` 실사용자 대상 리허설, 채팅 암호화 저장 확인

**탈출 게이트**: ① KPI 베이스라인(MEMORY.md 정의 항목) 실측치로 채움 ② 시범 사용자 재사용 의향 ≥ 60% ③ 세션당 비용이 손익 모델 허용 범위 내.

**확장 대비 불변식**: 측정 하네스의 지표 정의를 과목 중립으로 (오개념 해소율·답 미루기 깊이는 물리에서도 동일 지표).

---

### S4. 수학 K-12 완성 (~12주) — "미적분에서 전 학년으로"

**왜 지금**: 파일럿이 검증한 루프를 원자 백본 4축(초·중·고+대학) 전체로 수평 확장. 신규 발명이 아니라 커버리지 작업.

**체크리스트**:
- [ ] 콘텐츠 커버리지: 고교 전 단원 → 중등 → 초등 순 (wedge 인접 확장). 단원별 동등문제·오개념·다중 풀이 최소 세트 정의 후 파이프라인 일괄 가동
- [ ] 자동 커리큘럼 정렬 엔진: 학생 교과서·진도 입력 → 표기·깊이·풀이 스타일 자동 맞춤 (구조 메타데이터만 사용 — 본문 복제 금지 재확인)
- [ ] L6 잔여 모드 활성화: 메타인지·학교진도 모드 (검정고시·학종은 PRD대로 v1.5)
- [ ] L7 최소 착수: 학부모 주간 보고서 1종 (교사 대시보드는 Phase 3 유지)
- [ ] SkillNode mastery(Phase 2b-2: SkillMasteryHistory·행동영역 6종 태깅)를 전 학년 데이터로 가동
- [ ] 성능 게이트: 원자 1,837 전체 활성 상태에서 traversal 예산(visited set·timeout·token budget) 실부하 검증

**탈출 게이트**: ① 고교 전 단원에서 S1 루프 통과 ② 커리큘럼 정렬이 검정교과서 3종 이상에서 작동 ③ 단원 커버리지 대시보드 100% (고교 기준).

**확장 대비 불변식**: 커리큘럼 정렬 엔진을 Curriculum Overlay 원칙(개념 영속·매핑만 교체)으로 — 물리 교육과정 매핑이 같은 오버레이 슬롯에 들어감.

---

### S5. 타과목 확장 게이트 (수학 완성 후) — "물리 파일럿 진입 판정"

**왜 게이트인가**: 확장은 기능이 아니라 **판정**이다. 수학에서 증명 안 된 것을 물리로 가져가면 부채가 2배가 된다. 상세 설계는 `docs/strategy/subject_expansion_roadmap_review.md` 정본 준수.

**진입 조건 체크리스트** (전부 충족 시에만 물리 착수):
- [ ] S4 탈출 게이트 전부 통과 + 파일럿 KPI가 2분기 연속 유지
- [ ] subject 스코프 일반화 실증: embedding namespace 거버넌스(subject 축, invariant ⑨)가 `physics` 네임스페이스 추가를 **스키마 변경 0**으로 수용
- [ ] 노드 ID 정책(`math.calculus.limit` 형태) 검증 — `physics.*` 병렬 도입 시 registry·crosswalk 충돌 0
- [ ] 검증기 plugin 슬롯 실증: SymPy 자리에 단위·차원 검증기 목킹 삽입 테스트 통과
- [ ] 물리 전용 신규 리스크 목록화: 시각화 의존도(수학보다 높음)·실험 데이터·그림 중심 발문 OCR
- [ ] 물리 도메인 검수자 트랙 (수학 파트너와 별도)

**확장 대비 불변식 총점검**: S1~S4에서 명시한 4개 불변식(루프 과목 중립·검증기 plugin·지표 중립·커리큘럼 오버레이)의 위반 여부를 전수 감사 — 위반 발견 시 물리 착수 전 상환.

---

### 로드맵 요약표

| 단계 | 기간 | 한 줄 목표 | 핵심 산출물 | 사람 병목 |
|---|---|---|---|---|
| S0 정합성 회복 | ~2주 | 단일 진실 원천 복원 | 원자 백본 Phase 4·5 완료 | — |
| S1 E2E 수직 슬라이스 | ~6주 | 1루프 실동작 증거 | 실기기 완주 시연 + 비용 실측 | Phaiakes9 라이브 키 |
| S2 콘텐츠 공장 | ~8주 | 검증 동등문제 100문+ | 생성→검증→검수 파이프라인 | 도메인 파트너 |
| S3 파일럿 | ~8주 | 진짜 학생 측정 | KPI 베이스라인 실측 | 시범 사용자 채널 |
| S4 K-12 완성 | ~12주 | 전 학년 커버리지 | 커리큘럼 정렬 + 전단원 루프 | 검수 볼륨 |
| S5 확장 게이트 | 판정 | 물리 진입 가부 | 불변식 전수 감사 | 물리 검수자 |

---

## §4. 주요 병목 구간 Top 5 + 완화 전략

| # | 병목 | 왜 병목인가 | 무엇이 막히는가 | 완화 전략 | 담당 |
|---|---|---|---|---|---|
| 1 | **이중 truth source** (437 개념 vs 1,837 원자) | 신규 코드마다 "어느 그래프 기준?"을 물어야 하고, 두 그래프에 이중 반영 비용 발생. 방치 시 7대 붕괴 연쇄의 유지보수 지옥 진입 | S1 이후 모든 그래프 소비 코드 | S0에서 Phase 4·5 즉시 완료. 거버넌스 테스트로 구 그래프 재유입 동결 | **AI 자동화 가능** (Kiki 승인만) |
| 2 | **Phaiakes9 라이브 검증 정체** | 비용 모델·guard_cloud 임계값·OCR 실성능이 전부 추정치. 실측 없이는 S1 탈출 게이트 통과 불가, 손익 판단 불가 | S1 전체, S3 비용 KPI | 라이브 키 발급·실머신 검증 세션을 **캘린더에 박아둘 것**. AI는 측정 스크립트·대시보드를 선제 준비해 Kiki 수동 시간을 최소화 | **Kiki 수동** (AI는 준비만) |
| 3 | **E2E 루프 부재** | 부품 단위 green이 조립 green을 보장하지 않음. 통합 시 발견될 계약 불일치가 전부 잠복 상태 | 파일럿(S3) 일정 전체 | S1을 다른 모든 신규 설계보다 우선. "새 Part 검토"는 S1 실증에서 발견된 문제에 한정 | AI 자동화 가능 |
| 4 | **도메인 파트너 부재** | 콘텐츠 검수 라운드가 M1.3 게이트인데 트랙 미정. 검수 없는 동등문제 100문은 파일럿에 못 내보냄 | S2 탈출 게이트, S3 진입 | S2와 **병행 착수** (S2 완료 후가 아니라). 최소 요건을 "정규 파트너"에서 "검수 자문 1인"으로 낮춰 게이트 통과 후 격상. 모집 브리프·온보딩·clear 조건 = `domain_partner_handoff_2026-07.md` | **Kiki 수동** |
| 5 | **L6 수능 모드 얇음** | wedge(고3 킬러 30번)의 실제 경험이 게이팅 껍데기뿐. 코어가 아무리 좋아도 학생이 만나는 건 L6 | S3 파일럿 품질 | S1에서 루프 최소본으로 검증 후 S3에서 본격 엔진. L6에 수학 로직 침투 금지(코어 API 소비만) 유지 | AI 자동화 가능 |

**보조 병목** (Top 5 아래, 관리만): ⑥ 설계 정본화 국면 장기화 — S0 완료를 정본화 국면의 공식 종료 선언으로 사용 ⑦ 커버리지 하니스·orphan — S0 체크리스트에 포함 ⑧ Neo4j 런타임 미도입 — 의도적 결정(pgvector 단일 평면). S4 성능 게이트에서 실부하 데이터로만 재검토.

**핵심 통찰**: 병목 5개 중 **2개(②④)는 AI가 못 푸는 사람 병목**이다. AI 개발 속도가 아무리 빨라도 이 둘이 크리티컬 패스에 있으므로, Kiki의 수동 시간을 여기에 우선 배정하는 것이 전체 일정을 결정한다.

---

## §5. 단계별 AI 질문 리스트

> **사용법**: 각 단계 착수 시·중간 점검 시·탈출 게이트 판정 시 아래 질문을 그대로 복붙해 AI에게 묻는다. CLAUDE.md AI 질문 프로토콜의 4종 질문축 — **①존재 이유 ②경계 ③붕괴 ④분리** — 로 분류했고, `docs/standards/build_checkpoint_questions.md`(10단계 검문)와 교차 참조했다. AI는 "답변기"가 아니라 **구조 붕괴 감지기**로 쓴다.
>
> **모든 단계 공통 메타 질문** (각 단계 마무리에 반드시):
> > *"이 단계에서 만든 구조가 실제 서비스에서 실패하는 이유를 노드폭발·관계폭발·순환참조·유지보수·성능·AI추론실패·교육일관성붕괴 7관점에서, 표면 표현이 아니라 인지 행동(cognitive action) 기준으로 분석하라."*

### S0. 정합성 회복

1. **[분리]** "현재 코드베이스에서 구 437 개념그래프를 참조하는 소비처를 전수 나열하고, 각각을 원자 백본 참조로 전환할 때 깨지는 계약(스키마·테스트·API 응답)을 표로 정리하라." *(체크포인트 단계3 '단일 진실 원천' 검문)*
2. **[붕괴]** "Phase 4 크로스워크에서 437→1,837 매핑이 1:N 또는 N:1이 되는 케이스를 찾아라. 그 케이스에서 mastery 이력·오개념 crosslink·concept→skill 매핑이 어떻게 오염될 수 있는지 시나리오를 만들어라."
3. **[경계]** "Phase 5 폐기 후에도 남겨야 하는 구 그래프 산출물(마이그레이션 이력·감사 로그·crosswalk 테이블)은 정확히 무엇까지인가? '폐기'의 경계를 정의하라."
4. **[붕괴]** "구 그래프 폐기 후 재유입을 막는 거버넌스 테스트를 설계하라. 어떤 import·어떤 테이블 참조·어떤 코퍼스 경로가 나타나면 CI가 실패해야 하는가?"
5. **[존재 이유]** "리치 Part 2 잔여 로드맵(P3 ProblemTypeNode·P4 오개념 7계층·P5 FormulaNode) 중 S1 E2E 루프가 실제로 소비하는 것은 무엇인가? 소비처 없는 항목을 지금 만들면 안 되는 이유를 각각 제시하라."
6. **[분리]** "이번 크로스워크·폐기 절차에서 '수학 전용'인 부분과 '과목 중립 마이그레이션 패턴'인 부분을 분리해 문서화하라. 물리 그래프 도입 시 재사용할 수 있는가?"

### S1. E2E 수직 슬라이스

1. **[붕괴]** "온보딩→CAT→문제제시→풀이입력→WH-1 코칭→검증응답 루프에서 각 단계 간 계약(스키마·상태 전이)을 나열하고, 통합 시 처음 깨질 가능성이 높은 계약 3개를 근거와 함께 지목하라." *(체크포인트 단계8 검문)*
2. **[경계]** "Minimal Reasoning Subgraph 상한(depth ≤ 2·nodes ≤ 12~20·tokens ≤ 3000)이 context builder 코드 어디에 배선되어야 하는가? 상한 초과 시 fail-open인가 fail-closed인가 — 각각의 교육적 결과는?" *(Part 8 해제 트리거 계약 준수 확인)*
3. **[붕괴]** "학생 응답이 PRM/도구 검증을 우회해 도달할 수 있는 코드 경로가 존재하는가? verify_step의 3상태(correct/incorrect/unverifiable) 중 unverifiable일 때 WH-1이 정답을 흘리지 않는다는 보장은 어디에 있는가?"
4. **[붕괴]** "WH-1 도구 루프가 무한 반복하거나 end_turn 없이 종료되는 경로를 찾아라. 루프 가드·timeout·토큰 예산이 각각 어느 계층에서 강제되는가?"
5. **[분리]** "이 루프에서 오개념이 초기 context에 preload되는 지점이 없는지 검사하라. 웜 스타트의 '단원 고빈도 오개념 프리로드'와 reactive retrieval 원칙의 경계는 무엇인가?"
6. **[존재 이유]** "OCR과 MathLive 중 S1 시연에 필수인 것은 무엇인가? 둘 다 넣을 때와 MathLive만 넣을 때 검증 범위 손실을 비교하라."
7. **[경계]** "Langfuse 트레이스와 토큰 실비용 회계에서, 어떤 데이터가 미성년자 개인정보에 해당해 암호화/최소화 대상인가?"
8. **[붕괴]** "루프의 어느 단계에 `subject == 'math'` 류 과목 하드코딩이 스며들었는지 grep 기준(패턴 목록)을 만들어 검사하라."

### S2. 콘텐츠 볼륨·품질 게이트

1. **[붕괴]** "동등문제 생성→SymPy 게이트→PRM→검수 파이프라인에서 각 게이트의 예상 통과율을 가정하고, 100문 확보에 필요한 생성 시도 횟수와 비용을 추정하라. 어느 게이트가 첫 병목이 되는가?"
2. **[경계]** "생성된 동등문제가 평가원 원문과 '동등하되 복제 아님'을 판정하는 기준은 무엇인가? 유사도 검사의 임계값과 그 법적 근거(저작권 가이드 v2.0)를 명시하라." *(체크포인트 단계10 검문)*
3. **[붕괴]** "WH-S 솔버가 '검증 통과했지만 교육적으로 나쁜 풀이'(비직관적 요행수·교육과정 밖 도구 사용)를 저장소에 축적하는 시나리오를 만들어라. 어떤 필터가 필요한가?"
4. **[존재 이유]** "다중 풀이 N개 중 학생에게 노출할 가치가 있는 '본질적으로 다른' 풀이의 판정 기준은 무엇인가? 표면 표현 차이와 인지 행동 차이를 어떻게 구분하는가?"
5. **[분리]** "SymPy 검증기가 파이프라인 본체에 새어 들어간 지점이 있는가? 물리의 단위·차원 검증기로 교체 가능한 인터페이스인지 검사하라."
6. **[붕괴]** "오개념 839건 기반 distractor 설계가 오히려 오개념을 '가르치는' 역효과 시나리오를 분석하라. distractor 노출 규칙에 어떤 안전장치가 필요한가?"
7. **[경계]** "검수 자문 1인의 검수 범위를 어디까지로 한정해야 100문 검수가 현실적인가? 전수 검수 vs 샘플링 검수의 리스크 차이를 정량화하라."

### S3. 수능 모드 심화 + 파일럿

1. **[존재 이유]** "L6 수능 모드 엔진의 각 기능(시그니처 경로·킬러 진입·모의 채점)이 코어(L1-L4) 재사용인지 신규 로직인지 분류하라. L6에 수학 로직이 새로 생기는 항목은 전부 코어로 밀어내라." *(7계층 경계 검문)*
2. **[붕괴]** "측정 하네스가 수집하는 지표 중 '정답률·시간'만으로 환원되는 것이 있는가? '답 미루기 도달 깊이'와 '스스로 풀이 도달율'의 측정 정의가 게임화 가능한(학생이 시스템을 속일 수 있는) 허점을 분석하라." *(체크포인트 단계9 검문)*
3. **[경계]** "WH-1 전략단계 보류 해제의 트리거 조건을 측정 데이터 기준으로 명시하라. 어떤 베이스라인이 얼마나 쌓이면 해제 판정이 가능한가?"
4. **[붕괴]** "시범 사용자 5~10명 규모에서 얻은 KPI가 통계적으로 무의미한 항목은 무엇인가? 이 규모에서 판정 가능한 것과 불가능한 것을 구분해, 불가능한 것을 근거로 한 의사결정을 차단하라."
5. **[경계]** "미성년 시범 사용자의 동의 절차(학부모 동의·14세 미만)와 데이터 수집 범위가 법적 요건을 충족하는지 체크리스트로 검증하라. 파일럿 종료 후 데이터 처리(삭제·보존) 계획은?"
6. **[붕괴]** "톤 필터·부정 피드백 금지가 실패하는 엣지 케이스(학생의 자기비하 발화에 대한 동조, 반복 오답 후 은근한 재촉)를 생성해 필터를 공격하라."
7. **[분리]** "측정 지표 정의에 수학 전용 개념이 들어간 항목을 찾아라. 물리 파일럿에서 동일 지표를 쓸 수 있는가?"

### S4. 수학 K-12 완성

1. **[붕괴]** "원자 1,837 전체 활성 + 전 학년 콘텐츠에서 traversal·retrieval 성능이 처음 무너지는 지점을 예측하라. Part 8 rev.2의 규모·비용 표(attention O(n²)·relation O(N²)) 기준으로 어느 병목(Orchestration→Traversal→Reranking→Curriculum)이 먼저 오는가?" *(체크포인트 단계3 노드폭발 검문)*
2. **[경계]** "자동 커리큘럼 정렬이 교과서에서 가져올 수 있는 것(구조 메타데이터: 단원명·목차·페이지·코드)과 절대 가져올 수 없는 것(본문·문제·학습목표 원문)의 경계를 코드 레벨에서 어떻게 강제하는가?"
3. **[분리]** "커리큘럼 정렬 엔진이 Curriculum Overlay 원칙(개념 영속·매핑만 교체)을 지키는지 검사하라. 2022 개정→차기 교육과정 전환 시 무엇을 교체하고 무엇이 불변인가?"
4. **[붕괴]** "초등 콘텐츠로 내려갈 때 고교 기준으로 설계된 것들(LTHC 깊이·소크라테스 발문 수준·오개념 입도)이 깨지는 지점을 나열하라. 학년 축이 프롬프트 파라미터인가 구조 분기인가?"
5. **[존재 이유]** "L7 학부모 보고서가 이 시점에 최소로 필요한 이유는 무엇이며, '최소'의 경계는? 교사 대시보드를 여전히 만들지 않아야 하는 이유를 재확인하라."
6. **[붕괴]** "SkillNode mastery(행동영역 6종)가 전 학년에서 독립 추정될 때, 원자 mastery와 스킬 mastery가 모순되는 학생 상태(개념은 알지만 행동을 못 함, 그 역)를 시스템이 어떻게 표현·활용하는가?"

### S5. 타과목 확장 게이트

1. **[경계]** "노드 ID·스키마·임베딩 네임스페이스가 과목 독립적인가? `physics.*` 네임스페이스를 추가할 때 스키마 변경 0·기존 테스트 무손상으로 가능한지 실제로 검증하라(목킹 삽입 테스트)." *(invariant ⑨ subject 축 검문)*
2. **[분리]** "S1~S4에서 선언한 확장 대비 불변식 4종(루프 과목 중립·검증기 plugin·지표 중립·커리큘럼 오버레이)의 위반 사례를 전수 감사하라. 위반별 상환 비용을 추정하라."
3. **[붕괴]** "물리에서 수학과 근본적으로 다른 실패 모드(시각화 의존·실험 오차 개념·그림 중심 발문·단위 차원)를 나열하고, 현 아키텍처가 흡수 못 하는 것을 지목하라."
4. **[존재 이유]** "물리 확장의 진입 근거가 '수학이 완성되어서'인가 '물리 시장 신호가 있어서'인가? 진입을 미뤄야 하는 반대 근거를 최선을 다해 구성하라(악마의 변호인)."
5. **[경계]** "수학·물리 개념 그래프 간 cross-subject 엣지(예: 미분↔속도)를 허용할 것인가? 허용 시 관계 폭발·네임스페이스 오염 리스크와 금지 시 교육적 손실을 비교하라."
6. **[분리]** "물리 오개념 카탈로그를 수학과 같은 테이블에 둘 것인가 분리할 것인가? embedding namespace 거버넌스 테스트가 cross-subject 코사인 검색을 차단하는지 확인하라."

---

## §6. 참조 인덱스

| 근거 | 위치 |
|---|---|
| 결정 로그 정본 | `MEMORY.md` §핵심 결정 로그 (시간 역순) |
| PRD·페르소나·MVP 범위 | `docs/strategy/prd_v1.2.md` (2026-05-28 재정렬) |
| 다과목 확장 설계 정본 | `docs/strategy/subject_expansion_roadmap_review.md` (2026-06-21 물리 우선) |
| 진행 축 점검 질문 | `docs/standards/build_checkpoint_questions.md` (10단계 × 3박자) |
| 설계-준수 축 점검 질문 | `docs/standards/playbook_part_review_questions.md` (Part 0~12) |
| Part 8 Context 판정 | `docs/standards/part8_context_architecture_review.md` rev.2 (3분류·해제 트리거) |
| 데이터 라이선스 | `docs/data/licensing_safety.md` (가이드 v2.0, 2026-05-28) |
| 하네스 설계 | `docs/architecture/04a_wh1_tutoring_harness.md` · `03b_wh_s_solver_harness.md` |
| 원자 백본 마이그레이션 | MEMORY.md 2026-06-21 승인 항목 (Phase 0~3 완료·4~5 잔여) |
| 주요 결정 날짜 | pgvector 2026-06-10(슬98) · 클라이언트 3층 2026-06-10(슬89) · OCR 하이브리드 2026-05-28 · MATH DSL invariant 12종 2026-06-30~07-01 · 리치 Part 2 채택 2026-07-02~03 |

---

**다음 갱신 시점**: S1 탈출 게이트 판정 시 (E2E 실증에서 발견된 사실로 §2 평가·§4 병목 재조정)

# MEMORY.md — 결정 로그·현재 상태

> **이 파일은 *대화의 휘발성*에 대한 방어선입니다.**  
> 새로운 결정, 폐기된 접근, 핵심 인사이트를 누적 기록.  
> Claude 세션이 끝나도 *진실*은 여기 남습니다.

---

## 🏷️ 브랜드 (확정)

- **앱명**: WhyMath (와이매스)
- **슬로건 (KR)**: 답이 아닌, 이유를 묻는 수학
- **슬로건 (EN)**: The math that asks why.
- *상세는 아래 "2026-05-14: 브랜드명 확정" 결정 로그 참조*

---

## 📍 현재 상태 (2026-05 시점)

### Phase
- [x] Phase 0: 청사진 수립 완료 (CLAUDE.md, ROADMAP.md, 7계층 아키텍처)
- [~] **Phase 1: MVP 개발 (0~6개월) — *착수*** (2026-05-13)
- [ ] Phase 2: 풀 K-12 (6~12개월)
- [ ] Phase 3: 영재·B2B (12~24개월)

### 활성 작업
- 🔄 **L1.NCIC.정제** (별도 PR) — `_PdfStandardExtractor` 영역 추출 + 본문/해설 분리 (2026-05-15 결정 로그 참조)
- 🔄 **ROCm 7.2+ Linux native 시도** (옵션 F, 별도 세션) — DirectML 32 tok/s 의 *2-3x 잠재력* 시도. BIOS UMA Frame Buffer Fixed 48-64GB + Linux Kernel 6.18.4+ + ROCm 7.2.0+ 요구. 출발점: `infra/phaiakes9/GPU_ACTIVATION_FOLLOWUP.md` §3 옵션 A·C
- ✅ **M1.2-live S1 완료** (계획·구현·검증 2026-05-21 — `/plan M1.2-live`) — L3 라우터 스텁→실연동 첫 슬라이스 (Ollama 로컬 경로). 범위: ① `l3/providers/ollama.py`(`OllamaProvider`, `resolve_model`→모델 ID, ollama lazy + Protocol seam) ② `l3/pipeline.py`(`generate()`: route→캐시 get→히트면 반환·미스면 생성→캐시 set→trace.record; `mode=="async"`(QUALITY)는 S4까지 sentinel 차단, 동기 호출 금지) ③ `app.py` FastAPI(`/health` 생존·`/status` Ollama 도달성+모델 매트릭스 가용성·`POST /v1/generate` — *원시 출력, 학생 직접 노출 아님; 03 환각 방어 선행*) ④ `config.py`(pydantic-settings, env 전용·시크릿 0). 캐시/추적은 인메모리 스텁 재사용. 테스트=단위(CI 가짜 주입 70%+)+`integration` 마커(Phaiakes9 실측). **후속 슬라이스**: S2 Redis CacheBackend·S3 Langfuse TraceSink·S4 Celery QUALITY 큐(동시성1, §D.3)·S5 클라우드 Anthropic(§H#4 Phase1 후반). backend-engineer 위임 후 **메인 독립 재검증**: 4게이트 green(ruff·black·mypy-strict·pytest **170 passed/2 integration skip**)·커버리지 **100%**. integration은 `WHYMATH_RUN_INTEGRATION` env 게이트로 CI 자동 skip(`tests/backend/conftest.py`, 라이브 Ollama는 Kiki·Phaiakes9 실측). 신규 소스 5(`config.py`·`app.py`·`l3/pipeline.py`·`l3/providers/{__init__,ollama}.py`) + 테스트 5 + `conftest.py` 수정. 판단: `/v1/generate` 본문은 `request:RoutingRequest` 중첩(extra=forbid 충돌 회피)·QUALITY는 503 JSON·`check_status`는 provider feature-detect.
- ✅ **M1.2-live S2 완료** (구현·검증 2026-05-21) — 인메모리 캐시 스텁 → 실 Redis CacheBackend 교체. 신규 `l3/cache/redis_cache.py`(`RedisCache`, redis.asyncio lazy + `_RedisClient` seam, 실 TTL `SET EX`·`ttl<=0`은 무만료 폴백·`_decode` bytes/str 방어·`ping()` 향후 레디니스용) + `config.py` `redis_url`(env, 시크릿 0) + `create_app()` 기본 캐시 RedisCache(지연 연결, 앱 구성 시 라이브 Redis 불필요). 현 브랜치에 S1 위 스택(PR #17 확장). **메인 독립 재검증**: 4게이트 green(ruff·black·mypy-strict·pytest **193 passed/6 integration skip**)·커버리지 **100%**(`redis_cache.py` 100%). CI hermetic 유지: 단위는 가짜 redis+`InMemoryCache` 주입, 라이브 Redis 4건은 `WHYMATH_RUN_INTEGRATION` 게이트(Kiki·Phaiakes9). `/status`·라우터·파이프라인 미변경. **후속**: S3 Langfuse·S4 Celery 큐·S5 클라우드.
- ✅ **M1.2-live S3 완료** (구현·검증 2026-05-21) — 인메모리 트레이스 스텁 → 실 Langfuse TraceSink 교체. 신규 `l3/trace/langfuse_sink.py`(`LangfuseSink`, langfuse **4.6.1**(v3 OTel SDK) lazy + `_LangfuseClient` seam, `create_event(name="l3_routing", metadata=…)`·태그는 metadata 내 `tags` 리스트로(v3엔 tags 인자 없음)) + `config.py` Langfuse 키(공개키·`SecretStr` 시크릿키 **기본값 없음**·호스트만 무해 디폴트·`langfuse_configured` 프로퍼티) + `create_app()` 기본 trace LangfuseSink(지연·자가 비활성). **시크릿 안전**: SecretStr 평문은 클라이언트 생성 순간만 추출·로그/repr 노출 0·하드코딩 0. **never-break**: 미설정→영구 no-op(네트워크 0)·예외는 삼킴(관측성<가용성, CLAUDE.md #1≫#6)·로그에 키/PII 없음(`student_id_hash`는 이미 해시, 통과만). 현 브랜치에 S1·S2 위 스택(PR #17). **메인 독립 재검증**(langfuse env 제거 상태): 4게이트 green(ruff·black·mypy-strict·pytest **217 passed/8 integration skip**)·커버리지 **100%**(`langfuse_sink.py` 100%). 라이브 Langfuse 2건은 `WHYMATH_RUN_INTEGRATION`+키 이중 게이트(Kiki). `/status`·라우터·파이프라인 미변경. **후속**: S4 Celery QUALITY 큐·S5 클라우드.
- ✅ **M1.2-live S4 완료** (구현·검증 2026-05-21) — QUALITY(27b) 비동기 큐(Celery, broker=Redis). 신규 `l3/queue/`(`celery_app.py` build_celery_app **worker_concurrency=1**·prefetch=1·JSON 직렬화·lazy / `tasks.py` `run_quality_generation_payload`(플레인)+태스크, `asyncio.run`으로 async provider 실행 / `celery_job_queue.py` `CeleryJobQueue`(`AsyncJobQueue`)+`JobStatus`, `_CeleryApp`/`_CeleryResult` seam, name-based `send_task`·`result()` 기능탐지) + `config.py` `celery_broker_url`/`celery_result_backend`(빈=redis_url 폴백, `effective_celery_*`). 파이프라인 async 분기: QUALITY→enqueue→`GenerationResult(status=queued, job_id)`(큐 None→`QualityQueueUnavailableError`, enqueue 실패도 변환→503). `app.py`: `/v1/generate` QUALITY→**202+job_id**, 신규 `GET /v1/jobs/{job_id}`(폴링, success=text·failure/unknown=사유, 500 없음). **fresh main(S1–S3 머지, `db84a66`) 위 새 브랜치 `claude/m12-live-s4`**. **메인 독립 재검증**: 4게이트 green(ruff·black·mypy-strict·pytest **259 passed/10 integration skip**, 3.6s=브로커 미연결 hermetic)·커버리지 **100%**. **never-break**: 브로커 다운→503(500 아님)·result 조회 오류→`unknown` 흡수. 동시성1은 앱설정+기동명령 `celery … worker -c 1` 이중 강제(§D.3). 판단: JSON 직렬화(pickle X)·name-based dispatch(큐앱≠워커앱). **후속**: §H#6 큐 SLA(retry/DLQ/backpressure 미정)·S5 클라우드.
- ✅ **M1.2-live S5 완료** (계획·구현·검증 2026-05-23 — `/plan`) — 클라우드 티어 실연동(Anthropic Claude, 03a §H 후속 4). 라우터가 이미 내리던 CLOUD_MID(Sonnet 4.6)·CLOUD_HIGH(Opus 4.7) 결정을 실제 생성으로 잇는다(기존 기본 provider `OllamaProvider`는 클라우드 거부). 신규 `l3/providers/anthropic.py`(`AnthropicProvider`, `anthropic` AsyncAnthropic lazy + `_AnthropicClient`/`_Messages`/`_Models` seam, `resolve_cloud_model`→모델 ID, **plain `messages.create`(샘플링·thinking 인자 0 — Opus 4.7 400 회피)**·`_extract_text`(content 블록 `type=="text"`만 join)·`check_status`=`models.list` 도달성·`AnthropicStatus`) + `l3/providers/composite.py`(`CompositeProvider`, `cost_tier` 디스패치 LOCAL→Ollama·CLOUD_*→Anthropic, `check_status`=로컬 위임·`check_cloud_status`=클라우드 분리 — **pipeline/app `generate(provider=)` 시그니처 무변경**) + `config.py` Anthropic 설정(`anthropic_api_key` **SecretStr·기본값 없음**·`anthropic_model_mid/high` alias·`anthropic_max_tokens`·`anthropic_request_timeout_s`·`anthropic_configured` 프로퍼티) + `app.py` 기본 provider=`CompositeProvider(Ollama+Anthropic)`·`StatusBody` 클라우드 필드(선택, 기본 None=미노출)·`/status` cloud_* 분기(기존 로컬 매핑 보존). **클라우드는 mode=sync(03a §C.4)→Celery 큐 미경유→워커 무변경**(불변식상 QUALITY만 async). **시크릿 안전**: 평문은 클라이언트 생성 순간만 추출·로그/repr 0·하드코딩 0. **never-break vs fail-loud 구분**: `/status`는 클라우드 미설정·도달 불가를 비크래시 보고(500 X); 그러나 `generate`는 *클라우드 결정인데 키 없으면* **명확한 RuntimeError**(조용한 LOCAL 강등 금지 — 정확성>가용성, CLAUDE.md #3). **fresh branch `claude/tender-knuth-zXu1L`**. **메인 독립 재검증**: 4게이트 green(ruff·black·mypy-strict·pytest **306 passed/13 integration skip**)·커버리지 `anthropic.py`·`composite.py`·`config.py` **100%**(TOTAL 99%). 라이브 Anthropic 3건은 `WHYMATH_RUN_INTEGRATION`+키 이중 게이트(Kiki). 라우터·파이프라인·큐 미변경. **범위 결정(Kiki)**: 프롬프트 캐싱·실측 비용/지연·`guard_cloud` 임계값 보정·thinking/effort 튜닝은 라이브 키 보정 후속(§H#4 잔여). **후속**: 토큰 usage 기반 실비용 회계(`response.usage`→budget 차감, provider 반환 계약 변경 필요)·프롬프트 캐싱.
- ✅ **M1.2-live S5 잔여 — 튜닝 노브 배선** (2026-05-23, "잔여진행") — 라이브 키가 없어 *실측 보정·thinking 튜닝값·캐시 적중 검증*은 Kiki 몫이라, **키 없이 가능한 코드 배선만** 추가: `config.py` `anthropic_effort`(str, 기본 "")·`anthropic_thinking`(bool, 기본 False)·`anthropic_prompt_caching`(bool, 기본 False) + `AnthropicProvider.generate`가 *설정된 경우에만* `output_config={effort}`·`thinking={type:adaptive}`·top-level `cache_control={ephemeral}`를 싣는다(`_Messages.create` seam에 `**kwargs`). **전부 기본 OFF=현 동작 무변경**(Opus 4.7 plain create 유지) — Kiki가 env로 켜고 튜닝(코드 변경 0). 4게이트 green(pytest **313 passed/13 skip**)·`anthropic.py`·`config.py` 커버리지 **100%**. **여전히 키 필요(미진행)**: effort/thinking 최적값·캐시 적중률·실측 비용/지연·`guard_cloud` 임계값(§H#4) + 토큰 usage 실비용 회계(L3 `LLMProvider` 반환 계약 변경 — 별도 슬라이스로 분리, 파급 큼).
- ✅ **빌드타임 사전생성 — 슬라이스 1 (캐시 사전적재 하니스)** 완료 (계획·구현·검증 2026-05-23 — `/plan 2`) — MEMORY 2026-05-20 "Max=빌드타임" 결정의 첫 실현. 신규 `l3/pregenerate/` 패키지: `models.py`(`PregenItem` pydantic·`PrewarmReport`·`PrewarmItemResult`)·`validator.py`(`SeedValidator` Protocol + `BasicSeedValidator` 최소 위생 — 비어있음·길이·오류 마커)·`prewarmer.py`(`CachePrewarmer`, **런타임 `pipeline.generate`와 *같은* `Router`·`cache_key_for` 재사용** → 동일 키로 적재 → 런타임 캐시 히트 보장)·`__main__.py`(JSONL CLI, `python -m whymath_backend.l3.pregenerate <specs.jsonl>`). **두 입력 모드**: 인제스트(`precomputed_response` — Max-Claude 시드) / 생성(provider 호출). **무만료 적재**(`ttl_seconds=0` 기본 — S2 RedisCache의 ttl<=0 무만료 활용). **QUALITY async는 명시 error**(런타임이 async 분기에서 캐시를 안 쳐서 사전적재 무의미, pipeline.py:128-149). **킬러 테스트**: pre-warm 후 동일 (req,prompt,system)으로 `pipeline.generate` → `cache_hit=True`·runtime provider 미호출(키 정합 e2e 증명). **fresh branch `claude/tender-knuth-zXu1L`(S5 머지 후 그 위에 적층)**. 4게이트 green(ruff·black·mypy-strict·pytest **338 passed/13 skip**)·신규 5개 모듈 **전부 커버리지 100%** (`__init__`·`__main__`·`models`·`prewarmer`·`validator`, TOTAL 99%). 변경 없음: 런타임(`pipeline.py`·`app.py`·`router.py`·`cache/`·`providers/`). **후속**: L1 성취기준→스펙 자동 생성·DB 코퍼스 내구화(Redis 플러시 대비)·PRM/SymPy 검증 통합·Max 인제스트 포맷 확정·캐시 에빅션/리프레시 정책.
- ✅ **빌드타임 사전생성 — 슬라이스 B (SymPy 산술 검증 게이트)** 완료 (구현·검증 2026-05-28 — 합의 순서 A→B→C에서 **B 우선**) — A(L1 성취기준→스펙)는 조사 결과 *실데이터 부재(`data/ncic` 비어 있음)·L4 프롬프트 미구현(docs만)·크로스패키지 결합(`data_pipeline`↔`whymath_backend`)*으로 이 환경 완전검증 불가라 보류; B는 의존 0이라 먼저. `validator.py`에 `SymPyArithmeticValidator`(응답의 *순수 수치 등식*을 SymPy로 검증 — 거짓이면 시드 탈락; 03 환각 방어의 *도구 검증* 첫 조각) + `ChainValidator`(여러 검증기 AND 합성) 추가, CLI 기본 게이트를 `Basic→SymPy` 체인으로 강화. **false-positive 0 설계**: 유니코드 연산자(×÷−·) 정규화 후 *독립 수치 등식*만 검사 — 연산자·변수·한글 인접("x + 1 = 2"의 "1=2")은 건너뛰고(경계 검사), 개행은 강한 구분자로 *줄별* 검사. 거짓이 *증명*될 때만 탈락(심볼릭·파싱불가·판정불가는 통과). max_checks로 과부하 차단. **fresh branch `claude/tender-knuth-zXu1L`(slice 1 머지 후 main 위 재시작)**. 4게이트 green(ruff·black·mypy-strict·pytest **366 passed/13 skip**)·`validator.py`·`__main__.py` 커버리지 **100%**(TOTAL 99%). 런타임 미변경. **후속**: A(성취기준→스펙, pedagogy-designer 동반·실데이터 Kiki 환경)·PRM/LLM-judge·Lean 증명 검증·C(M1.2 시나리오+SolutionPath).
- ✅ **Schema v1.0 — 슬라이스 1 (ENUM + Problem, 법적 교정)** 완료 (구현·검증 2026-05-28, `/plan` 승인→backend-engineer 위임→**메인 독립 4게이트 재검증**) — 신규 `whymath_backend/schema/`(`enums.py` 14 str-Enum[SourceType·ExamType·Curriculum·Subject·QuestionFormat·AnswerFormat·SignaturePattern 10종·Persona A~E·VisualType·LicenseType·GenerationType·ReviewStatus·StepType·RelationType, Schema v1.0 §14.3 값 그대로]·`problem.py` `Condition`/`Problem`(50+필드)/`ProblemStep`/`ProblemRelation`·`__init__`) + 테스트 2(`tests/backend/schema/`). **법적 교정 invariant**(MEMORY 2026-05-28): `_METADATA_ONLY_SOURCES={평가원,EBS,교과서}` source_type이면 `question_text`·`answer_explanation`·`choices` 비어야(아니면 ValidationError; 저작권 가이드 v2.0 §32 단서·§136·§140) — `@model_validator(mode="after")`. **Pydantic-first**(코드베이스 전체가 Pydantic-only·`ncic/load_to_postgres` NotImplementedError seam 답습; SQLAlchemy/alembic은 DB 배포 시 후속). **메인 4게이트 green**: ruff·black·mypy-strict("no issues, 28 files")·pytest **432 passed/13 skip**(366→+66)·신규 schema 모듈 cov **100%**. 판단: DDL NOT NULL `question_text`/`answer`→Optional 완화(메타전용 레코드 표현·법적교정 정합), JSONB는 `conditions_parsed`만 `list[Condition]` 서브모델·자유형은 dict, DECIMAL→float(ge/le 범위), `Curriculum.REVISION_2022`(숫자접두 금지). **검증 주의**: backend-engineer가 권한 제약으로 게이트 실증 못 해(정적 추론만) 메인에서 재실행 — black 2파일 실제 재포맷 필요했음(에이전트 "black 안정" 주장 오류 → 메인이 교정). **후속**: 슬라이스 2(`provenance.py` ContentProvenance+generation_log, license/generation_type 교정, l3 pregenerate 연결)→3 Concept→4 User→5 Activity/Dialogue→6 Assessment/TimeSeries→7 textbook_mapping→(후속)SQLAlchemy+alembic.
- ✅ **Schema v1.0 — 슬라이스 2 (Provenance: ContentProvenance+GenerationLog, license/gen 법적 교정·l3 연결)** 완료 (구현 backend-engineer 위임→**메인 독립 재검증**, 2026-05-28) — 신규 `schema/provenance.py`(`ContentProvenance` 16필드+법적 `@model_validator(after)`·`GenerationLog` 12필드, 둘 다 §10.1 DDL)·`l3/pregenerate/provenance_bridge.py`(`generation_log_from_result` 순수 어댑터)·`schema/__init__` export + 테스트 2. **법적 invariant**(MEMORY 2026-05-28 속편 "스키마 법적 교정"·저작권 가이드 v2.0 §32단서·§136·§140): **(A)** `generation_type=ORIGINAL` 전역거부 · **(B)** `license=EBS_LICENSED` 전역거부(둘 다 공식제휴 Phase3+ 예약) · **(C)** `original_source∈_METADATA_ONLY_SOURCES{평가원·EBS·교과서}`이면 (C-1)`license=WHYMATH_GENERATED`만 허용·(C-2)`generation_type` 변형류(VARIANT_*/COMPOSED/FULLY_GENERATED)만(None·비변형 거부)·(C-3)`original_reference` 본문성 키 denylist 차단(구조 메타만). enum/문자열 정규화(use_enum_values). **계층 규칙 준수**: `schema`(L1)는 l3 import 0; 연결 어댑터는 l3쪽 배치(L3→L1 허용). provider가 usage 미노출(prewarmer.py:111)→토큰/비용/지연 None(후속). 신규 2모듈 cov **100%**. **CI 동일 격리 venv(py3.12+`pip install -e .[dev]`) 4게이트 green**: black 26.5.1·ruff 0.15.15·mypy-strict(30파일)·pytest **500 passed**(432→+68, `--cov-fail-under=70` 통과). **⚠️ 도구 drift 발견·교정(전 슬라이스 영향)**: ① **black 거짓통과 함정** — `black … ../../tests/` mixed-path는 common-base=repo루트(black config 없음)→기본 **line-length 88** fallback→통과로 보임. 정본 검증은 `cd src/backend && black --check .`(=CI, **ll=100**·py3.12·black<27). slice1·slice2 둘 다 88로 잘못 포맷돼 있었음 → **slice1 `problem.py`도 이번에 100으로 함께 교정(포맷만, 의미 0)**. ② **시스템 black 26.3.1은 py3.11→py3.12 코드 파싱 불가**(검증 부적합) — py3.12 venv 필수. ③ **CI 갭(발견)**: ruff/black/mypy 게이트는 src/backend 기준이라 `tests/backend/**`는 미검사(pytest 실행만). **후속**: 슬라이스 3 Concept(§4 concept·concept_edge·problem_concept·concept_fusion)→4 User→5 Activity/Dialogue→6 Assessment/TS→7 textbook_mapping→(후속)SQLAlchemy+alembic.
- ✅ **Schema v1.0 — 슬라이스 3 (Concept Graph: §4 4모델 + ENUM 4종)** 완료 (backend-engineer 위임→**메인 독립 재검증**, 2026-05-28) — `enums.py`+4(`ConceptLevel` 단원/소단원/세부개념·`CognitiveType` 5·`EdgeType` 5·`ConceptRole` 4; §14.3 부재→§4.2 인라인 DDL 주석 정본, slice1 ExamType 방식)·신규 `schema/concept.py`(`Concept` 20필드·`ConceptEdge`·`ProblemConcept` N:M·`ConceptFusion`)·`__init__` export +8·테스트 1(41 케이스). **구조 invariant**(`@model_validator(after)`, slice1 `ProblemRelation._no_self_relation` 패턴): `Concept._no_self_parent`(parent≠concept_id)·`ConceptEdge._no_self_edge`(from≠to). **법적 판단**: concept free-text(`description`·`formal_definition`·`intuitive_explanation`)는 교과서/EBS 표현 복제 금지지만 *구조 신호가 없어* validator 강제 불가 → docstring 3곳 문서화만(검수 단계 책임). slice1/2 본문 차단은 source_type/license라는 구조 신호가 있었던 것과 대비 — **가짜 validator 날조 회피**. 복합 PK(`ProblemConcept`=problem+concept+role, role도 required)·`concept_ids` min_length=1·`embedding_id`는 ChromaDB 외부참조 UUID(벡터 저장 아님, §4.3 인프라설정 제외). ge/le 판단(DDL 미명시, 각 description 명시): weight_in_curriculum 0-1·fusion_difficulty 1-5·grade 1-12·semester 1-2. **CI 동일 venv(`/tmp/civ`) 4게이트 green**: black 26.5.1(31파일·이번엔 에이전트가 `/tmp/civ` 사용→거짓통과 없음)·ruff·mypy-strict(31)·pytest **541 passed**(500→+41, cov-fail-under=70)·concept.py cov **100%**. **trust 메모**: 에이전트가 enum 4종을 *추가*(+75줄)해 놓고 보고선 "이미 있었음(수정 불필요)"이라 오서술(슬라이스마다 자체보고 신뢰성 반복 이슈) — 실제 작업·값은 정확, 메인이 ground truth(git diff·게이트) 확인. **후속**: 슬라이스 4 User(§5 user_profile·user_track_history·user_persona_history·user_state_snapshot; school_type·region·track_type·persona 등 ENUM 다수 신규).
- ✅ **Schema v1.0 — 슬라이스 4 (User: §5 4모델 + ENUM 7종)** 완료 (backend-engineer 위임→**메인 독립 재검증**, 2026-05-28) — `enums.py`+7(`SchoolType` §14.3 12값·`TrackType` 8·`MajorCategory` 8·`Device` 4[iPhone `#noqa N815`]·`NoteApp` 4·`SubscriptionTier` 3·`Accessibility` 5)·신규 `schema/user.py`(`UserProfile` 40필드·`UserTrackHistory`·`UserPersonaHistory` 복합PK·`UserStateSnapshot`)·`__init__` +11·테스트 1(46). **결정(Kiki 확인요): `gender`·`school_region`은 enum 아닌 `str | None`** — §14.3 정의 없고 §5.1 DDL 주석 미완결(gender 값 미기재·region "강남/대치/지방/**...**"), v1.1은 region을 시도교육청 *코드 문자열*로 모델링, 미성년 민감정보라 닫힌 카테고리 강제 부적절 → 닫힌 enum 날조 회피, **Kiki 값 확정 시 enum 승격**(가역적·필드 description에 명시). **개인정보**: UserProfile은 미성년 민감정보(email_hash·birth_year 연단위·parent_consent_at) — CLAUDE.md 금기 docstring 상기만, "is_minor→parent_consent 필수" cross-field 강제 안 함(동의 *대기* 상태 정당히 존재→미들웨어/검수 책임; concept.py 방침). validator 0(구조 불변식 없음=없는 게 맞음). `persona_primary`만 required. `target_exam_date`=`date`(DATE형). ge/le 판단(DDL 미명시, 각 description 근거): birth_year 1900-2100·grade 10-14·target_score/spend/days ge=0·estimated_grade 1-9·percentile 0-100·session_quality 0-1. **CI 동일 venv(`/tmp/civ`) 4게이트 green**: black 26.5.1(32파일)·ruff·mypy-strict(32)·pytest **587 passed**(541→+46, cov-fail-under=70·전체 99.95%)·user.py cov **100%**. **trust 메모(3회째 반복)**: 에이전트가 enum 7종을 *추가*(+112줄)해 놓고 "이미 있었음(수정 불필요)"이라 또 오서술 — 실제 작업·값은 정확, 메인이 git diff·게이트로 ground truth 확인(이 자체보고 패턴은 슬라이스마다 재검증 필수임을 재확인). **후속**: 슬라이스 5 Activity/Dialogue(§6 학습활동·§7 Socratic 대화).
- ✅ **Schema v1.0 — 슬라이스 5 (Activity §6 + Dialogue §7: 5모델 + ENUM 8종)** 완료 (backend-engineer 위임→**메인 독립 재검증**, 2026-05-28) — `enums.py`+8(`SessionType` 5·`AttemptMode` 4·`EventType` 8·`Resolution` 5·`TurnRole` 3·`ContentType` 4·`SocraticStrategy` 6·`StudentIntent` 5; §14.3 부재→§6/§7 인라인 DDL 정본)·신규 `schema/activity.py`(`LearningSession` 14·`ProblemAttempt` 21·`AttemptEvent` 7)·`schema/dialogue.py`(`Dialogue` 16·`DialogueTurn` 14)·`__init__` +13·테스트 2(57). **BIGSERIAL 처리**: `AttemptEvent.event_id`는 BIGSERIAL→`int|None=Field(default=None,ge=1)`(DB-assigned·default_factory 불가), `event_at`은 TIMESTAMPTZ NOT NULL→required(복합 PK·TimescaleDB hypertable 분할키). `DialogueTurn.turn_order` NOT NULL→required(ge=1, UNIQUE(dialogue_id,turn_order)). **개인정보**: ProblemAttempt(student_answer·handwriting_uri·ocr_result)·DialogueTurn(content·image_uri·image_analysis)=미성년 학생 데이터 — "평문 저장 금지"는 저장계층(암호화·미들웨어) 책임이라 docstring 상기만(가짜 validator 금지, user.py/concept.py 방침). validator 0(구조 불변식 없음; total_turns=student+assistant 카운트 정합도 system 턴 때문에 미강제). ge/le: focus/engagement/confidence/quality/understanding score 0-1·step/turn_order ge=1·counts/tokens/cost ge=0·student_rating 1-5·time_vs_expected ge=0·network_type str(VARCHAR 비enum). **CI 동일 venv(`/tmp/civ`) 4게이트 green**: black 26.5.1(34파일)·ruff·mypy-strict(34)·pytest **644 passed**(587→+57, cov-fail-under=70)·activity/dialogue cov **100%**. **⚠️ trust 메모(악화·4회째)**: 에이전트가 이번엔 enum 8종을 실제 추가(+113줄)해 놓고 "이미 있었음·enums.py 미수정"이라 보고하며 **수정파일 목록에서 enums.py를 누락** — 보고대로 믿었으면 enums.py 커밋 누락→CI 깨짐. **git status가 진실, 에이전트 자체보고는 *파일 목록조차* 신뢰 불가** → 매 슬라이스 `git status`+게이트 ground truth 확인이 필수(이미 그렇게 함). **후속**: 슬라이스 6 Assessment(§8 assessment·concept_mastery_history; assessment_type·mental_phase ENUM 신규·hypertable)→7 TimeSeries(§9)→textbook_mapping/curriculum_entry(v1.1 이식)→(후속)SQLAlchemy+alembic.
- ✅ **Schema v1.0 — 슬라이스 6 (Assessment §8: 2모델 + ENUM 2종)** 완료 (backend-engineer 위임→**메인 독립 재검증**, 2026-05-28) — `enums.py`+2(`AssessmentType` 5[`D_100예측="D-100예측"` — 식별자는 언더스코어·값은 하이픈 정본]·`MentalPhase` 6[D_100_PLUS·D_100_50·D_50_30·D_30_7·D_7_0·평상시, D-100 코칭 특성 #50])·신규 `schema/assessment.py`(`Assessment` 17필드·`ConceptMasteryHistory` 복합PK+hypertable)·`__init__` +4·테스트 1(26). `Assessment` 5개 JSONB 진단필드(concept/pattern_diagnosis·weak/strong_points·recommended_path)→`list[dict[str,Any]]`(weak/strong은 상세 담게 list[dict], list[str] 대신). `ConceptMasteryHistory` 복합 PK(user_id·concept_id·measured_at) 셋 required·TimescaleDB hypertable(7일 청크). 개인정보(미성년 진단 데이터) docstring 상기만(PIPA 권한매트릭스·저장계층 책임)·validator 0(구조 불변식 없음). ge/le: admission_probability·mastery·confidence 0-1·estimated_grade 1-9·percentile 0-100·score/sample_size ge=0. **CI 동일 venv(`/tmp/civ`) 4게이트 green**: black 26.5.1(35파일)·ruff·mypy-strict(35)·pytest **670 passed**(644→+26, cov-fail-under=70)·assessment.py cov **100%**. trust 메모(5회째): 에이전트가 `git status`는 정확히 붙였으나 또 "enums.py/assessment.py 이미 작성됨·이번 미수정"이라 오귀속(자기가 run 초반 만든 걸 '이전 것'으로 착각) — git status가 진실(enums.py +51줄·assessment.py 신규 = 이 슬라이스 산출), 메인이 ground truth 확인 후 커밋. **후속**: 슬라이스 7 TimeSeries(§9 daily_learning_metrics·problem_solve_time_distribution·user_behavior_metrics — 전부 hypertable·복합PK·persona_enum 재사용)→textbook_mapping/curriculum_entry(v1.1 이식)→(후속)SQLAlchemy+alembic.
- ✅ **Schema v1.0 — 슬라이스 7 (TimeSeries §9: 3모델, 도메인 마지막) → 🎯 8개 도메인 전부 완료** (backend-engineer 위임→**메인 독립 재검증**, 2026-05-28) — 신규 `schema/timeseries.py`(`DailyLearningMetrics` 8필드·`ProblemSolveTimeDistribution` 7필드·`UserBehaviorMetrics` 4필드)·`__init__` +3·테스트 1(29). **신규 enum 0**(Persona 재사용·metric_name VARCHAR str → `enums.py` 미수정, git status로 확인). 세 모델 모두 복합 PK(전 구성요소 required) + TimescaleDB hypertable(30/7/7일): DailyLearningMetrics`(user_id, metric_date[DATE→date])`·ProblemSolveTimeDistribution`(problem_id, persona[Persona enum이나 PK라 required], measured_at)`·UserBehaviorMetrics`(user_id, metric_name[str open set], measured_at)`. `metric_value`(DECIMAL10,4) **범위 무제약**(metric_name마다 의미 달라). 개인정보(churn_risk 등 미성년 *행동분석*) docstring 상기만·validator 0(분위수 p10≤p50≤p90·카운트 정합 미강제). **CI 동일 venv(`/tmp/civ`) 4게이트 green**: black 26.5.1(36파일)·ruff·mypy-strict(36)·pytest **699 passed**(670→+29, cov-fail-under=70)·timeseries.py cov **100%**. **🎯 마일스톤: Schema v1.0 8개 도메인(Problem·Provenance·Concept·User·Activity·Dialogue·Assessment·TimeSeries) Pydantic 구현 전부 완료** — `schema/` 패키지 9파일(enums+8도메인)·str-Enum 35종·모델 ~30종, 누적 pytest 699 passed, 신규 schema 모듈 전부 cov 100%. 전 슬라이스 black line-length 100·mypy-strict 통과. **후속**: ① v1.1 이식분 `curriculum_entry`(NCIC 성취기준 1급 — L1 파이프라인 산출)·`textbook_mapping`(교과서 구조 메타 — 자동 커리큘럼 정렬, 법적 안전) ② (후속)SQLAlchemy+alembic DB 매핑(`ncic/load_to_postgres` seam).
- ✅ **Schema — 슬라이스 8a (v1.1 이식: CurriculumEntry, 다국 커리큘럼 매트릭스)** 완료 (backend-engineer 위임→**메인 독립 재검증**, 2026-05-28) — `enums.py`+2(`RequiredDepth` 4[awareness/procedural/conceptual/mastery]·`CurriculumLicense` 5[`KR_NCIC="KR-NCIC"` 등 — 하이픈 식별자 처리, slice6 D_100예측 선례])·신규 `schema/curriculum_entry.py`(`CurriculumEntry` 30필드, 복합키 (concept_id×country_code)+표면키 entry_id)·`__init__` +3·테스트 1(35). 정본 `schemas/v1.1/curriculum_entry.schema.yaml`(v1.0 8도메인 밖, NCIC=한국 열). **강제 invariant 2개**(`@model_validator`): `is_present=true→source_url 비어있지 않아야`·`country_code='IMO'→confidence≤0.7`. **str 결정**(enum 날조 회피, slice4 방침): country_code(ISO 풀스케일 수용·Phase1 KR/US/IMO 가드는 파이프라인)·grade_band·cognitive_level(NCIC `AchievementStandard.subject`가 str인 것과 정합). source_url은 YAML required:true이나 invariant 정합 위해 `str|None`+validator. created_at/updated_at은 YAML required:true→required(slice1~7 Optional과 다름). cross-dataset invariant(concept_id/NCIC 코드 실재·복합키 유일성·Phase1 국가가드)는 파이프라인 책임 docstring. **CI 동일 venv(`/tmp/civ`) 4게이트 green**: black 26.5.1(37파일)·ruff·mypy-strict(37)·pytest **734 passed**(699→+35, cov-fail-under=70)·curriculum_entry.py cov **100%**. **⚠️ 에이전트 크래시(신규 실패유형)**: 서브에이전트가 *최종 보고 메시지 파싱 실패*로 크래시(보고 0개) — 그러나 36 tool-use 동안 4파일을 전부 올바르게 작성. 메인이 git status·게이트·코드 전수 검토로 ground truth 확인 → **보고가 아예 없어도 독립검증이 작동**(자체보고 신뢰 불가 원칙의 극단 사례). **후속**: 슬라이스 8b TextbookMapping(`schemas/v1.1/textbook_mapping.schema.yaml` — TextbookMapping+TextbookUnit(자기참조 트리)+TextbookToneProfile; **법적 핵심**: learning_objective_text는 legal_review_status='cleared' 전까지 null 강제·isbn 13자리·교과서 본문/문제 일절 미수집).
- ✅ **Schema — 슬라이스 8b (v1.1 이식: TextbookMapping, 마지막) → 🎯 Schema 구현 정본 전부 완료** (backend-engineer 위임→**메인 독립 재검증**, 2026-05-28) — `enums.py`+1(`LegalReviewStatus` not_required/pending/cleared — `ReviewStatus`와 별개·교과서 학습목표 저작권 검토)·신규 `schema/textbook_mapping.py`(`TextbookMapping` PK=isbn·`TextbookUnit` 자기참조 트리·`TextbookToneProfile`)·`__init__` +4·테스트 1(55). 정본 `schemas/v1.1/textbook_mapping.schema.yaml`. **🚨법적 invariant 2개**(`@model_validator`): ① isbn 13자리(`^\d{13}$`) ② **learning_objective_text 게이팅** — `legal_review_status≠cleared`면 `unit_tree` 전 단원 `learning_objective_text=None` 강제(교과서 학습목표는 저작물·변호사검토 전 미수집; MEMORY PRD 허점⑥ 한국법에 fair use 부재 — slice1 본문차단·slice2 license차단과 같은 *구조 신호(legal_review_status) 기반 진짜 invariant*, enum/문자열 정규화) + `TextbookUnit._no_self_parent`. unit_title max_length=200(본문혼입 모델 가드). **저작권 안전선**(YAML 헤더·CLAUDE.md): 구조 메타만(목차·단원명·페이지*번호*·서지 ISBN/출판사/서명/저자·교육과정 코드), 본문·예제·문제·풀이·그림 *일절 미수집* — 교과서 문제는 standard_codes/concept_ids로 자체 코퍼스 동등문제 대체(파이프라인에 "교과서 문제 수집" 단계 자체가 없음). str 유지: subject·publisher(NCIC subject str 정합). cross-dataset·트리 사이클 검증은 파이프라인 책임 docstring. **CI 동일 venv(`/tmp/civ`) 4게이트 green**: black 26.5.1(38파일)·ruff·mypy-strict(38)·pytest **789 passed**(734→+55, cov-fail-under=70)·textbook_mapping.py cov **100%**. trust(6회째): enums.py +36줄(LegalReviewStatus) 추가해 놓고 "이미 있었음" 또 오귀속 — git status가 진실, 메인 확인. **🎯 마일스톤: Schema 구현 정본 전부 완료** — v1.0 8도메인(Problem·Provenance·Concept·User·Activity·Dialogue·Assessment·TimeSeries) + v1.1 이식 2종(CurriculumEntry·TextbookMapping). `schema/` 12파일(enums + 11 모델모듈)·**str-Enum 38종·모델 ~36종**·누적 **pytest 789 passed**·신규 모듈 전부 cov 100%·전 슬라이스 black(ll=100)·mypy-strict 통과. 관통 원칙: 법적 교정은 *구조 신호 있을 때만* validator 강제(Problem 본문·Provenance license/gen·TextbookMapping 학습목표 게이팅), 신호 없으면 docstring 문서화(concept 설명·user 동의·학생 데이터). **후속**: SQLAlchemy+alembic DB 매핑(전 모델 → PostgreSQL16/TimescaleDB hypertable/ChromaDB, `ncic/load_to_postgres` seam·복합PK·hypertable 변환).
- ✅ **DB 영속 레이어 PoC (SQLAlchemy 2.0 async + alembic) — 인프라 + Problem 도메인** 완료 (backend-engineer 위임[ORM·인프라·스캐폴드]→**메인이 로컬 PG16으로 마이그레이션 생성·실검증**, 2026-05-28) — 사용자 결정: *별도 ORM*(SQLModel 아님)·*인프라+1도메인 PoC*. 신규 `whymath_backend/db/`(`base.Base` DeclarativeBase+naming_convention·`session` **lazy** async engine/`get_session`/`dispose`·`models/problem.py` ORM `Problem`·`ProblemStep`·`ProblemRelation`)·`config.database_url`(asyncpg DSN, WHYMATH_ override)·alembic 스캐폴드(`alembic.ini`·async `env.py`·`script.py.mako`)·초기 마이그레이션 `4c6d083dfeef_initial_problem_domain`·테스트(`tests/backend/db/`). **ORM 핵심**: `schema/enums.py` enum 재사용 + `_pg_enum(values_callable=lambda e:[m.value...])` **필수**(`Curriculum.REVISION_2015→"2015_REVISION"` 등 멤버명≠값 보존 — 없으면 PG enum이 멤버명으로 생성돼 §14.3 DDL과 어긋남)·JSONB(conditions_parsed·persona_fit·choices)·ARRAY(TEXT[]·enum[])·GIN 4종·UUID `gen_random_uuid()`·복합PK·FK. **변환 헬퍼** `from_schema`/`to_schema`(mapper 컬럼키 필터 — schema(검증)↔db(영속) seam). 본문 미보유 invariant는 schema.Problem 책임(ORM 컬럼만·가짜 CHECK 없음). **메인 PG16 실검증**(postgres 유저·:55432, root는 PG 실행 거부): autogenerate→**upgrade(3테이블·11 enum)→downgrade(잔존 테이블·enum 0)→재-upgrade→`alembic check` "No new upgrade operations"** 왕복 green. **메인 교정 2건**: ① autogenerate가 downgrade에서 native enum 미drop → 재-upgrade "type already exists" 실패(alembic 표준 갭) → downgrade에 `postgresql.ENUM(name).drop(checkfirst=True)` 11종 추가 ② `script.py.mako`의 잘못된 "템플릿" docstring(모든 마이그레이션 오염) 제거. **4게이트 green**: black 26.5.1(45파일)·ruff·mypy-strict(43)·pytest **803 passed**(789→+14)·신규 db 모듈 cov 100%(session.py 33%=런타임 연결 경로 — PG 통합검증으로 커버). persona_fit은 JSONB라 persona_enum 미생성(∴ 11 enum). 검증가능성 메모: **이 sandbox는 TimescaleDB 확장 미설치·PG 서버 기본 미가동**이나 postgres 유저로 PG16 클러스터를 띄워 비-hypertable 도메인은 실검증 가능(hypertable 5종은 후속 env). **후속**: 나머지 8도메인 ORM·마이그레이션(동일 패턴 적층)·TimescaleDB hypertable 5종(`create_hypertable` raw op, 확장 있는 env)·FastAPI 라우터 get_session 배선·ncic `load_to_postgres` 구현.
- ✅ **DB 영속 레이어 — 배치 1 (5도메인 ORM·마이그레이션 적층): concept·provenance·user·curriculum_entry·textbook_mapping** 완료 (backend-engineer 위임[ORM]→**메인이 로컬 PG16 마이그레이션 생성·왕복 실검증**, 2026-05-28) — 신규 `db/models/`(`_orm_enum.py`[공통 `_pg_enum`/`_enum_values` 추출·problem.py도 import 전환]·concept[4테이블]·provenance[2]·user[4]·curriculum_entry[1]·textbook_mapping[2테이블 관계형])·테스트 5·마이그레이션 `1551744048aa`(problem 위 적층, 13테이블). problem.py 패턴 그대로(enum values_callable·JSONB·ARRAY·복합PK·FK·from_schema/to_schema). **textbook 비-1:1**: 중첩(unit_tree·tone_profile)→관계형 2테이블(`units` relationship·`ON DELETE CASCADE`·tone_profile JSONB 컬럼)+중첩 변환. `curriculum_entry` PK=entry_id 단일 + `UNIQUE(concept_id,country_code)`. 느슨참조(concept_ids·standard_codes·national_standard_codes 등)는 FK 아님(cross-dataset 파이프라인 책임). **메인 PG16 실검증**: upgrade(16테이블·28enum)→downgrade-1(3테이블·11enum, 공유enum 보존)→재upgrade→downgrade base(0·0)→`alembic check` 무드리프트 왕복 green. **메인 교정 2건(다중 마이그레이션 native-enum)**: ① **공유 enum 3종**(source_type·subject·curriculum — concept/provenance가 problem 마이그레이션 enum 재사용)을 `postgresql.ENUM(create_type=False)`로 재생성 방지(안 하면 upgrade "type already exists") + downgrade에서 *안* 떨굼(problem 소유) ② 신규 enum 17종은 downgrade에서 `ENUM(name).drop(checkfirst=True)`. **4게이트 green**: black 52·ruff·mypy-strict(49파일)·pytest **857 passed**(803→+54)·신규 db 모듈 cov 100%. ⚠️ **사후정정(2026-05-29 배치2 세션서 발견·교정)**: 배치1 커밋(`f0b9e19`) 당시 위 교정 2건이 *실제로는 파일에 안 들어갔음* — 첫 turn에 Edit 3개가 "File has not been read yet"로 **조용히 실패**했고 직후 API 오류(`thinking` 블록)가 검증 출력을 가려 *upgrade가 3·11(실패)인데 16·28로 오인*하고 그대로 커밋. **근본원인**: 공유-enum `DuplicateObject`는 **증분 배포 경로**(problem→배치1을 *별도 alembic invocation*으로 — 실제 운영 패턴)에서만 발현, `base→head` 단일 invocation은 SQLAlchemy가 동명 enum을 *프로세스 내 dedup*해 충돌을 감춤(∴ 배치2-on-배치1 단일왕복 테스트는 거짓 통과). 배치2 세션서 `downgrade base` 잔존 enum 17 발견→배치1 파일 ground-truth 확인(create_type=False 0개)→교정 2건 **재적용**·증분배포 시뮬(3·11→16·28→26·38→0·0)·`alembic check` green 재확인. **교훈**: 마이그레이션 검증은 *반드시 증분 경로*로(단일 base→head는 enum 충돌 위양성), git diff로 Edit 실제 반영 확인.
- ✅ **DB 영속 레이어 — 배치 2 (4도메인 ORM·마이그레이션, 마지막): activity·dialogue·assessment·timeseries** 완료 (backend-engineer 위임[ORM]→**메인이 로컬 PG16 마이그레이션 생성·증분배포 왕복 실검증 + 배치1 사후교정**, 2026-05-29) — 신규 `db/models/`(activity[3테이블: learning_session·problem_attempt·attempt_event]·dialogue[2: dialogue·dialogue_turn]·assessment[2: assessment·concept_mastery_history]·timeseries[3: daily_learning_metrics·problem_solve_time_distribution·user_behavior_metrics])·테스트 4·마이그레이션 `bb30b816083d`(배치1 위 적층, 10테이블). **복합 PK 다수**: attempt_event(event_id BIGSERIAL+event_at)·concept_mastery_history(user+concept+measured_at)·daily_learning_metrics(user+metric_date)·problem_solve_time_distribution(problem+persona+measured_at)·user_behavior_metrics(user+metric_name+measured_at). **FK는 §6~§9 DDL `REFERENCES`만 엄격 적용**(activity 4·dialogue 4·assessment 1); hypertable 느슨참조(attempt_event 3컬럼·concept_mastery_history·시계열 전부)·target_concept_id·stuck_at_concept_id·target_university_id는 **FK 아님**. **공유 enum 재사용**: device_enum(user+learning_session)·persona_enum(user 3컬럼+problem_solve_time_distribution) — `create_type=False`로 재생성 방지. 신규 enum 10종(session_type·attempt_mode·event_type·resolution·turn_role·content_type·socratic_strategy·student_intent·assessment_type·mental_phase) downgrade drop. **hypertable 5종**: ORM은 일반 테이블, 마이그레이션 upgrade 끝에 `DO $$ IF EXISTS(timescaledb) THEN create_hypertable(...if_not_exists) END $$`**조건부 가드**(chunk 간격 정본 일치: attempt_event 1d·mastery 7d·daily 30d·solve_time 7d·behavior 7d) — 미설치 PG(이 sandbox·CI)선 NO-OP로 일반 테이블 검증, 확장 환경선 자동 변환. **메인 PG16 증분배포 실검증**: 별도 invocation 3단(problem 3·11→배치1 16·28→배치2 26·38)→downgrade base 0·0→downgrade-1 왕복서 공유 enum 보존(device/persona·subject/source_type 각 1)→`alembic check` "No new upgrade operations" green. **4게이트 green**: black 57·ruff·mypy-strict(53파일, `Mapped[]`·BigInteger autoincrement·복합PK 통과)·pytest **908 passed**(857→+51)·신규 4모듈 cov 100%(291 stmts). **DB 영속 레이어 전 도메인 ORM+마이그레이션 완료**(problem PoC 3테이블+배치1 13+배치2 10 = **26테이블·38enum**, 도메인 모듈 10개[problem+batch1 5+batch2 4] + `_orm_enum` 헬퍼). **후속**: FastAPI 라우터 `get_session` 배선(✅ 아래)·ncic `load_to_postgres`·hypertable 변환은 timescaledb 확장 env서 실검증.
- ✅ **DB-backed HTTP 라우터 결선 (영속 레이어 → HTTP end-to-end)** 완료 (메인 직접, 2026-05-29) — `db.session.get_session`(기존 lazy 세션 인프라)을 FastAPI에 처음 결선. 신규 `whymath_backend/api/`(`__init__`·`concepts.py`) + `create_app`에 `app.include_router(concepts_router)` + **lifespan**(`@asynccontextmanager _lifespan` 종료 시 `dispose_engine()` — 엔진 미생성 시 no-op). **concept 라우터 3엔드포인트**(`/v1/concepts`): POST 생성(`from_schema`→add→commit→`to_schema`, code UNIQUE 충돌 `IntegrityError`→rollback→**409**)·GET 단건(`session.get`, 없으면 404)·GET 목록(`select`+code 오름차순+limit/offset, Query 범위검증). **의존성 패턴**: `SessionDep = Annotated[AsyncSession, Depends(get_session)]`(기본인자 `Depends()`는 ruff **B008**·mypy가 막음 — Annotated 메타데이터가 현행 권장). 트랜잭션 commit/rollback은 **핸들러 책임**(get_session은 세션 열고닫기만 — session.py 계약). **테스트 2층**(L3 app테스트가 provider/cache/queue 가짜 주입하는 패턴 + db모델 테스트가 라이브PG 없이 from/to_schema 왕복만 보는 분담을 미러링): ① **hermetic**(`test_concepts.py`, 9 테스트) — `app.dependency_overrides[get_session]`에 `FakeSession`(get/execute/add/commit/rollback/refresh 모사) 주입, 201/409/404/422·commit/rollback 호출 검증 ② **통합**(`test_concepts_integration.py`, `@pytest.mark.integration` 기본 skip·`WHYMATH_RUN_INTEGRATION=1`로만) — 실 PG16서 POST→GET→중복409→목록 왕복 + lifespan dispose 발화(TestClient 컨텍스트매니저). **메인 실검증**: 로컬 PG16 head 적용 후 통합테스트 1 passed(HTTP→get_session→PG end-to-end 증명, 행 정리는 독립 엔진으로 — asyncpg 루프바인딩 회피). **CI 정합 확인**: backend job은 `working-directory: src/backend`라 `black/ruff .`이 src/backend만(line-length 100)·tests/는 pytest(testpaths)만 — 테스트 파일은 black 기본 88, src는 100. **4게이트 green**: black(59)·ruff·mypy-strict(55파일)·pytest **917 passed**(908→+9)·`api/concepts.py` cov **100%**·TOTAL 99%(`--cov-fail-under=70`). **후속**: 쓰기 더 필요시 PATCH/DELETE·다른 도메인(problem 등) 라우터·페이지네이션 total 헤더.
- ✅ **ncic `load_to_postgres` 구현 (L1 성취기준 → PostgreSQL 적재, data-pipeline Phase 2)** 완료 (메인 직접, 2026-05-29) — `data_pipeline/ncic/load.py`의 stub(NotImplementedError)을 실구현. **설계 분기 해소**: `AchievementStandard`(NCIC 성취기준 원자료)는 모델 docstring대로 **전용 `achievement_standards` staging 테이블**에 적재 — WhyMath v1.0 앱 스키마(concept/problem, backend/alembic)와 *별개*. 성취기준→concept 변환은 후속 'A'(pedagogy 동반)이지 load의 책임 아님. **핵심 제약**: data-pipeline CI는 `[dev]`만 설치(sqlalchemy 없음)·`[postgres]` extra는 optional → ① sqlalchemy import를 `load_to_postgres` *함수 내 지연*(모듈 import는 extra 없이 가능 — write_json/csv 사용자 보호, 미설치 시 RuntimeError 안내) ② mypy는 `ignore_missing_imports=true`라 통과 ③ `_to_async_dsn`(postgresql://→+asyncpg) 순수 헬퍼. **구현**: SQLAlchemy Core `Table`(14컬럼, code PK) + `create_all`(멱등 — data-pipeline엔 alembic 없으니 loader가 staging 테이블 소유) + code 기준 dedup(마지막 우선, 단일 INSERT ON CONFLICT 중복행 오류 방지) + `pg_insert().on_conflict_do_update`(upsert=True)/`do_nothing`(False), 빈 입력은 연결 없이 0. **테스트**: stub 테스트 대체 — hermetic 3(빈입력 0·_to_async_dsn 2케이스, sqlalchemy import 불요라 CI-safe) + **통합**(`test_load_postgres_integration.py`, `@integration`·`importorskip("sqlalchemy")`·PG 도달성 skip) — 전용 테스트테이블서 INSERT→upsert UPDATE→do-nothing 왕복 후 DROP. **통합 게이트 신설**: data-pipeline conftest에 `WHYMATH_RUN_INTEGRATION` 게이트 추가(backend와 동일 — 마커는 등록돼 있었으나 첫 통합테스트라 게이트가 없었음). **메인 실검증**: 로컬 PG16서 통합테스트 1 passed. **4게이트 green**(CI 방식 src/data-pipeline): black(9)·ruff·mypy-strict(9파일)·pytest **103 passed**(101→+2, net)·1 skipped(통합)·load.py 70%·TOTAL 82%(`--cov-fail-under=70`). **혼합경로 black 함정 재확인**: `black . ../../tests`처럼 트리 혼합 호출 시 공통루트(설정 없음·기본 88)로 기존 100-포맷 파일 대량 오변경 → 트리별 단독 호출 필수. **후속**: ncic `__main__`에 load CLI 옵션 배선(✅ 아래)·실 NCIC 크롤 데이터 적재·achievement_standards→concept 매핑('A', ✅ 아래 시드 단계).
- ✅ **ncic CLI `load` 커맨드 + 개념 그래프 시드 생성기(concept_graph)** 완료 (메인 직접, 2026-05-29) — 두 후속을 한 세션에 처리. **(1) ncic `load` CLI**(`ncic/__main__.py`): `standards.json`→`load_to_postgres` 배선(`--dsn`/`WHYMATH_DATABASE_URL`·종료코드 DSN없음1·JSON없음2·extra없음4·`--upsert/--no-upsert`), CliRunner hermetic 8테스트(monkeypatch)·로컬 PG16 end-to-end(2행·멱등) 실검증. **(2) `data_pipeline/concept_graph/`** — `docs/data/concept_graph.md`(정본·"미구축") 단계 3(시드)+단계 6(검증) 구현. **설계 핵심**: 개념 그래프는 *자체 구축 자산*이라 자동화 가능한 건 **시드 생성뿐**(노드·엣지 *작성*은 전문가 단계 4, M1.3 게이트). 그래프 모델은 **Neo4j 타깃**(`concept_id=UC.<domain>.<topic>.<slug>`)이라 backend PG `concept`(code=CAL-INT-)와 **별개**. 모듈: `models.py`(`Concept`·`ConceptEdge` Pydantic — `schemas/v1.1/{concept,edge}.schema.yaml` 충실, UC ID 패턴 validator·name 한·영·일 비공백·relation 7종 str-Enum[정본 산문 '6종'은 stale, 열거가 7]·strength[0,1]·evidence min_length=1·`SOURCE_CITATION` NCIC 승계)·`seed.py`(성취기준→후보 노드 결정론적 UC ID[`build_concept_id`: 과목약칭맵+해시폴백, NCIC 코드 기반 멱등]+parent_codes→prerequisite 후보 엣지, CSV 빈칸[표기·strength·evidence는 전문가], **statement 본문 미복제** 법적)·`validate.py`(§5 그래프 invariant: prerequisite 사이클=error·역쌍/고립/dangling/학년단조성=warning, `ncic/validate.py` 리포트 패턴)·`__main__.py`(`seed`·`validate`[채운 CSV→strict 파싱]·`load`[Neo4j 후속 Phase 가드, exit 3]). **재사용**: `AchievementStandard(Collection)`·`parse_standard_code`·`write_csv`/sidecar·typer 패턴. **end-to-end 실검증**: `seed --domain-filter 미적분`→후보 2노드·1엣지 CSV(UC ID·빈칸·statement 미복제 확인)→`validate` 미작성 seed서 파싱실패 3건 exit1. **4게이트 green**(src/data-pipeline): black(14)·ruff·mypy-strict(14)·pytest **192 passed**(111→+81)·1 skip·concept_graph cov 91~100%·TOTAL 88%. **후속(범위 밖)**: Neo4j 드라이버 의존성+적재(단계 7, 전문가 CSV·neo4j env 선행)·전문가 노드 작성(단계 4)·backend PG concept(CAL-)↔그래프 UC ID 정합.
- ✅ **Problem 도메인 HTTP 라우터** 완료 (메인 직접, 2026-05-29) — concept 라우터(`api/concepts.py`) 패턴을 핵심 콘텐츠 엔티티 problem에 그대로 확장. 신규 `api/problems.py`(POST 생성·GET 단건[404]·GET 목록[최신순 created_at desc + problem_id 안정정렬·`subject` 선택 필터·limit/offset]) + `create_app`에 `app.include_router(problems_router)` + `api/__init__` export. `external_id`/`slug` UNIQUE 충돌→**409**(IntegrityError 롤백). **경계**: 본문 보유 금지 등 출처별 불변식은 schema.Problem after-validator가 이미 강제 — 라우터는 검증 통과 모델만 영속화. **필수 필드 주의**: Problem은 source_type·curriculum_version·valid_from_year·subject **+ unit_codes**(min 필수)이라 테스트 빌더에 unit_codes 포함. **테스트 2층**(concept과 동형): hermetic 11(`test_problems.py`, FakeSession·201/409/404/422·subject enum 검증·pagination) + 통합(`test_problems_integration.py`, `@integration`·PG 도달성 skip — POST→GET→subject 필터[미적분 매칭·기하 제외]→cleanup). **메인 실검증**: 로컬 PG16 head서 통합테스트 1 passed(subject 필터 SQL 실동작 확인). **4게이트 green**: black(60)·ruff·mypy-strict(56)·pytest **928 passed**(917→+11)·15 skip·`api/problems.py` cov **100%**·TOTAL 99%. **후속**: 쓰기 확장(PATCH/DELETE)·problem_step/relation 중첩 노출(✅ 아래)·by-slug 조회·타 도메인 라우터.
- ✅ **중첩 read 엔드포인트 — problem steps/relations · concept edges** 완료 (메인 직접, 2026-05-29) — 부모 엔티티의 하위 구조를 read로 노출(Polya 풀이단계·문항관계·개념 의존그래프). 신규 파일 없이 기존 라우터 확장: `api/problems.py`에 `GET /v1/problems/{id}/steps`(step_order 순)·`/relations`(outgoing=parent_problem_id), `api/concepts.py`에 `GET /v1/concepts/{id}/edges`(outgoing=from_concept_id, `idx_concept_edge_from` 활용). 공통 패턴: 부모 `session.get`→없으면 **404**, 자식 `select`+FK필터+안정정렬→`to_schema` 루프. 세 ORM(`ProblemStep`·`ProblemRelation`·`ConceptEdge`)의 기존 `to_schema` 재사용 — 새 변환 0. **read 전용·outgoing만·페이지네이션 없음**(자식 소량 가정). **경계 메모**: concept_edge는 backend PG 테이블 — data-pipeline Neo4j concept_graph(UC ID)와 별개(다른 저장소·키공간). **user 도메인은 제외**(인증 부재+미성년자 PII 금기 — 탐색서 확인, 인증 프레임 선행). **테스트 2층**: hermetic +9(`test_{problems,concepts}.py`에 TestSteps/TestRelations/TestEdges — FakeSession 재사용[get_map=부모·list_rows=자식]·200/404/422/빈[]·관계타입·엣지방향 검증) + 통합 +2(`@integration` — 독립 엔진으로 부모+자식 직접 적재[step 2건 역순삽입→정렬확인·edge a→b 방향]→하위 GET→cleanup). **메인 실검증**: 로컬 PG16 head서 API 통합테스트 4종(CRUD 2+중첩 2) passed — step_order 정렬·엣지 outgoing 방향·404·빈[] 실동작 확인. **4게이트 green**: black(60)·ruff·mypy-strict(56)·pytest **937 passed**(928→+9)·17 skip(+2 통합)·`api/{concepts,problems}.py` cov **100%**·TOTAL 99%. **후속**: 쓰기 확장(PATCH/DELETE)(✅ 아래)·역방향/양방향 관계·자식 페이지네이션·by-slug 조회·user 도메인(인증 선행).
- ✅ **PATCH/DELETE — concept·problem CRUD 완성** 완료 (메인 직접, 2026-05-29) — 두 도메인에 부분수정·삭제 추가. **PATCH** `/v1/{concepts,problems}/{id}`: 부분 dict 본문 → 기존 행 `to_schema().model_dump()`에 병합 → PK는 경로 고정 → **schema 재검증**(불변식 유지: Problem 본문보유금지 등) → `session.merge`(PK 기준 갱신) → commit. 없으면 404·병합 결과 위반(미정의 필드 extra=forbid·잘못된 값) 422·UNIQUE 충돌 409. **DELETE** `/v1/{...}/{id}`: `session.delete`+commit → **204**(`Response`), 없으면 404, FK 참조(엣지·매핑·단계·관계) 있으면 IntegrityError→**409**(가짜 cascade 금지 — 참조 있으면 삭제 거부). **동시성 last-write-wins**(낙관적 락 미적용 — Concept엔 updated_at 없음, 후속). 422 detail에 `errors[{loc,msg}]` 동봉(직렬화 안전). **테스트**: hermetic +16(`FakeSession`에 `delete`/`merge` 추가 — TestPatch/TestDelete 각 도메인: 200수정·404·422[enum·미정의필드]·409[merge conflict]·204삭제·404·409[FK]; **PATCH 법적 불변식 재검증** — 본문보유 문제를 평가원으로 PATCH→422) + 통합 +2(`@integration` — POST→PATCH[부분수정·기존필드 보존]→GET[영속확인]→DELETE 204→GET 404 실 PG 왕복). **메인 실검증**: 로컬 PG16서 API 통합테스트 6종(CRUD·중첩·PATCH/DELETE × 2도메인) passed. **4게이트 green**: black(60)·ruff·mypy-strict(56)·pytest **953 passed**(937→+16)·19 skip(+2 통합)·`api/{concepts,problems}.py` cov **100%**·TOTAL 99%. **concept·problem 라우터 CRUD 完**(POST·GET단건·GET목록·중첩 read·PATCH·DELETE). **후속**: 낙관적 락(✅ 아래)·역방향 관계·자식 페이지네이션·by-slug 조회·user 도메인(인증 선행).
- ✅ **낙관적 동시성 — ETag + If-Match** 완료 (메인 직접, 2026-05-29) — PATCH last-write-wins 동시편집 덮어쓰기를 HTTP 표준(RFC 7232)으로 차단. **메커니즘 결정: `updated_at` 대신 content-hash 강한 ETag** — 조사서 `Problem.updated_at`엔 `onupdate` 없고(수정 시 안 바뀜) `from_schema`가 timestamp를 구값/None으로 실어 타임스탬프 ETag는 마이그레이션+bump 처리가 얽힘 → ETag = `sha256(to_schema().model_dump_json())[:16]`로 두면 **DB/스키마/마이그레이션 변경 0**, 내용 바뀌면 ETag 바뀜(클라는 불투명 토큰). 신규 `api/_concurrency.py`(`etag_for`·`ensure_if_match`[None=무조건진행·`*`=매칭·불일치=412]). concept·problem 라우터: GET단건·POST·PATCH 응답에 `ETag` 헤더, PATCH·DELETE에 `If-Match` 헤더(불일치 412). **비파괴**: If-Match 미전송 시 기존대로 무조건 진행(기존 테스트·클라 호환). **테스트**: hermetic +11(TestConcurrency 양 도메인 — ETag 노출·If-Match 일치 200/불일치 412/`*`/미전송·DELETE 412) + 통합 +1(`@integration` — GET ETag→일치 PATCH 200·ETag 갱신→옛 ETag 재PATCH **412**·내용 미변경 확인). **메인 실검증**: 로컬 PG16서 API 통합 7종 passed(412 동시수정 차단 실동작). **4게이트 green**: black(61)·ruff·mypy-strict(57)·pytest **964 passed**(953→+11)·20 skip(+1 통합)·`_concurrency`·`api/{concepts,problems}` cov **100%**·TOTAL 99%. **후속**: 강제모드(428 Precondition Required)·GET 캐싱(If-None-Match/304)(✅ 아래)·전용 version 정수·user 도메인(인증 선행).
- ✅ **조건부 GET — If-None-Match → 304** 완료 (메인 직접, 2026-05-29) — 쓰기 측 조건부(If-Match)의 짝인 읽기 측 조건부 요청 추가로 HTTP 조건부 기능 마무리. Flutter 모바일이 변하지 않은 concept/problem 재조회 시 **304 Not Modified**(빈 본문)로 대역폭 절약. `api/_concurrency.py`에 `matches_if_none_match`(None=False·`*`=True·쉼표목록 매칭) 추가, 기존 `etag_for` 재사용 — **DB/스키마/마이그레이션 변경 0**. GET 단건(concept·problem)에 `If-None-Match` 헤더 주입 → 현재 ETag와 일치 시 `Response(304, ETag)`, 아니면 200+본문+ETag(반환타입 `Schema | Response` — FastAPI가 Response 반환 시 response_model 우회). **범위: GET 단건만**(목록·중첩 304는 후속, 목록은 변경 잦아 캐시 효용 낮음). **테스트**: hermetic +8(TestConditionalGet 양 도메인 — 일치 304[빈 본문·ETag 존재]·`*` 304·불일치 200·미전송 200) + 통합 +1(GET ETag→If-None-Match 304→PATCH 변경→옛 ETag로 200). **메인 실검증**: 로컬 PG16서 API 통합 8종 passed(304 실동작·빈 본문 확인). **4게이트 green**: black(61)·ruff·mypy-strict(57)·pytest **972 passed**(964→+8)·21 skip(+1 통합)·`_concurrency`·`api/{concepts,problems}` cov **100%**·TOTAL 99%. **concept·problem HTTP 표면 포화**(CRUD·중첩 read·낙관적 락·조건부 GET). **후속(substantial — planning 필요)**: 인증 스캐폴드→user/민감 도메인(activity·assessment·dialogue) 노출(✅ 아래)·L4 교수학 엔진(LLM 필요, Phaiakes9). **소품**: 목록/중첩 304·강제모드 428·by-slug.
- ✅ **인증 집행 계층(JWT) + user/민감 도메인 본인 스코핑 노출 — 3슬라이스 完** (메인 직접·`/plan` 승인, 2026-05-29) — concept·problem(비민감 콘텐츠)까지 무인증이던 HTTP 표면에 *학생 데이터*(민감정보·미성년 PII) 노출을 위한 인증·인가 계층을 처음 도입. **핵심 설계 결정 — enforcement-only JWT(로그인 엔드포인트 미생성)**: 조사 결과 `UserProfile`엔 password/credential 필드가 *없다*(개인정보 최소화 — email_hash/birth_year만) → JWT는 `sub=user_id`만 담는 *집행 토큰*이고 **실제 로그인은 카카오/네이버 OAuth(프로덕션·키 필요, 후속)**가 같은 `create_access_token`을 호출해 발급, 테스트·통합은 헬퍼로 직접 토큰 mint(dev-token 엔드포인트 footgun 회피·sandbox 완전검증). **슬라이스 1(집행 계층)**: `config.py`+`jwt_secret_key`(SecretStr, **env `WHYMATH_JWT_SECRET_KEY`로만 주입·하드코딩 금지** — anthropic_api_key 패턴, 빈 기본·`jwt_configured` property)·`jwt_algorithm=HS256`·`jwt_expire_minutes` / 신규 `security.py`(순수·FastAPI 무관: `create_access_token`/`decode_access_token` python-jose HS256, claims sub/exp/iat, 빈 secret→RuntimeError·만료/서명오류→JWTError) / `api/_auth.py`(`HTTPBearer(auto_error=False)`→`decode`→`session.get(UserProfile)`→없거나 불량 **401**+`WWW-Authenticate: Bearer`·`get_consented_user`=미성년 미동의 **403**·`CurrentUser`/`ConsentedUser` Annotated 별칭). **슬라이스 2(user 자기조회/수정)**: `api/users.py` `GET /v1/users/me`(**PII 제외** `response_model_exclude={email_hash,parent_email_hash}`·ETag)·`PATCH /v1/users/me`(**`_SELF_EDITABLE` 화이트리스트** — parent_consent_at·is_minor·email_hash·subscription_* 자기수정 차단해 *동의 게이트 우회 방지*, 비허용 키→422; If-Match/ETag·merge 재검증·IntegrityError→409). **슬라이스 3(민감 도메인 스코핑)**: `api/me.py` `GET /v1/me/{sessions,assessments,dialogues}`(LearningSession·Assessment·Dialogue) — 모두 `ConsentedUser`+**`WHERE user_id == current_user.user_id`로만**(타인 데이터 차단)·최신순(started_at desc·PK 안정정렬)·limit/offset(1~200). **보안 invariant(CLAUDE.md)**: ① 시크릿 env-only(코드 하드코딩 금기) ② 미성년 PII 외부노출 금지(/users/me 해시 제외·/me/* 본인 스코핑) ③ 학부모·14세미만 동의 게이트(403) ④ 동의우회 차단(PATCH 화이트리스트). **테스트 2층**: hermetic(`test_security.py`·`test_auth.py`[FakeSession user 주입·토큰 없음/불량/만료/user없음→401·미성년 미동의→403]·`test_users.py`·`test_me.py`[스코핑 결선·401·빈[]·pagination 422]) + 통합(`test_users_integration.py`·`test_me_integration.py` — **실 PG16서 A·B 교차 스코핑** 검증: A 토큰 /me/sessions는 A의 sid만·B의 sid 차단·무토큰 401, get_settings만 오버라이드·get_session 실 PG·FK순서 부모먼저커밋). **메인 실검증**: 로컬 PG16서 me 스코핑 통합 1 passed(타인 세션 차단 실동작)·users 통합(PII 제외·403·PATCH). **4게이트 green**: black·ruff·mypy-strict(src 61파일)·pytest **1002 passed**(972→+30, cov-fail-under=70·TOTAL 유지)·신규 `security`·`api/{_auth,users,me}` 모듈 cov **100%**. **후속**: 카카오/네이버 OAuth 로그인(키·프로덕션)·민감 도메인 *쓰기*·자식(dialogue_turn·attempt_event) 상세·14세미만 *검증가능 동의* 심화절차·관리자(타인) 조회 권한모델·refresh token.
- ✅ **L4 교수학 엔진 — 슬라이스 1 (Polya 4단계 코칭 엔진 + 정서 안전 톤필터)** 완료 (메인 직접·`/plan` 승인, 2026-06-02) — `docs/architecture/04_pedagogy_engine.md`(193줄)·`docs/prompts/*.md`(5종)이 *문서로만* 있던 L4의 **첫 코드화**(MEMORY L134 "프롬프트만 → Polya·소크라테스 코칭 엔진 코드화" 첫 실현). **핵심 경계(스펙 L13 정본)**: L3=*생성*, L4=*결정*. `PolyaCoach.decide()`는 **순수 함수**(LLM 0회)·`.coach()`는 `LLMSeam` Protocol 좌석으로 L3 호출(L3 import 0 — 계층 격리). 신규 패키지 `whymath_backend/l4/`: ① `models.py`(`PolyaStage` str-Enum 4단계·`PolyaState`·`StageTransition` Literal stay/next/previous·`PedagogyDecision`[스펙 §"인터페이스" L162-174 정본: polya_stage_to_advance·hint_level 1-4·socratic_category·prompt·system·recommended_cost_tier=CostTier.LOCAL·suggested_actions]·`ToneReport`·`LLMSeam` Protocol) ② `polya/prompts.py`(4단계 system+prompt — `docs/prompts/polya_4step.md` L3-71·L101-103 **그대로 인용**·드리프트 방지 docstring 명시·`_BASE_SYSTEM`이 5가지 절대원칙+금기 6패턴을 모델에 자기검열 유도) ③ `polya/transitions.py`(휴리스틱 `should_advance` — UNDERSTAND=재진술 길이≥20+문장부호·PLAN=전략 한국어 토큰 화이트리스트 19종·EXECUTE=결과 토큰+등호+다중줄 *3조건 AND*·REVIEW=종착) ④ `tone_filter.py`(금지 6패턴[틀렸·못 하·잘못된·실수·바보·포기] → 권장표현 매핑; 멱등·AI 출력 전용·*학생 입력 무손*·긴 패턴 먼저 치환해 부분충돌 차단) ⑤ `polya/engine.py`(`PolyaCoach.decide`/`coach`·`_next_stage`[REVIEW=종착 셀프루프 보호]·`_STAGE_ACTIONS` 정적 suggested_actions). **보수적 설계(원칙)**: 모호 시 `stay`·자동 `previous` 절대 없음(스펙 L101 "학생 *명시* 후퇴 신호로만") — false-negative(stay 과다)는 *교수학적 안전*, false-positive(잘못 next)는 *유해*. **샌드박스 제약**: Phaiakes9·키 부재 → LLM 라이브 검증 미수행, `LLMSeam` Protocol에 `FakeLLM` 주입한 hermetic 결선검증만(generate 호출·프롬프트 전달·응답 톤필터 발화 — 라이브는 Kiki/Phaiakes9 후속). **테스트** 4파일 36 케이스(`test_polya_models`·`test_polya_transitions`·`test_tone_filter`·`test_polya_engine`): 단계 전이 매트릭스·자동 previous 0 가드·금지 6패턴 치환·멱등성·`coach()` Protocol 좌석·LLM 출력에 심은 금지패턴 마지막방어선 차단·`_next_stage(REVIEW)→REVIEW` 회귀 가드. **4게이트 green**: black(src 68)·ruff·mypy-strict(src 68파일)·pytest **1038 passed**(1002→+36, cov-fail-under=70)·L4 모듈 전부 **cov 100%**(Stmts 118·Branch 18·Miss 0). **계층 경계 준수 확인**: L4→L3 의존 = `CostTier` enum import 1건만(스펙 L172 명시 — 권장 비용 티어). 그 외 L3 import 0. **결정 v1 한계**(문서화·후속 슬라이스): 휴리스틱 키워드 화이트리스트·톤필터 부분문자열(예: "포기하지 않고" 합법 토큰까지 치환)·LLM-judged 전이 미도입·후퇴 신호 분리(`request_backtrack`) 미구현. **후속**: 슬라이스 2 Socratic 6카테고리 카탈로그 + `socratic_category` 채움(스펙 §"소크라테스 질문 카테고리" L63-90)·슬라이스 3 답 미루기 4단계 graded hint(`hint_level` turn_count 기반 상승)·슬라이스 4 오개념 진단(`docs/prompts/misconception_diagnosis.md` 정본 + 30개 코딩)·슬라이스 5 LTHC 적응·슬라이스 6 HTTP 노출(인증·세션 영속). 비코드 잔여: 라이브 LLM end-to-end(Phaiakes9·Anthropic 키)·L2 학습자 모델 통합·개념 점화 지도(L1 그래프 결선).
- ✅ **L4 교수학 엔진 — 슬라이스 2 (소크라테스 6카테고리 선택)** 완료 (메인 직접, 2026-06-02) — 슬라이스 1의 `PedagogyDecision.socratic_category`(슬라이스 1에선 항상 `""`)를 *결정론적 규칙*으로 채움. 신규 서브패키지 `l4/socratic/`: ① `categories.py`(`SocraticCategory` str-Enum 6종 — 스펙 L65-70 영어 식별자 정본: clarification/assumption/evidence/perspective/implication/meta + `KOREAN_LABEL` 6한글 라벨 + `EXAMPLE_QUESTION` 6예시 질문 그대로 인용) ② `select.py`(`select_category(stage, transition, student_input)` 규칙: next→다음 단계 기본, stay/previous→발화 신호 오버라이드 후 단계 기본) ③ `__init__.py` 재노출. **두 축의 명확한 구분**(스펙 L89 "둘은 직교"): 이 슬라이스의 `SocraticCategory`(6) = *질문 종류*(L4 6분류); 기존 스키마의 `SocraticStrategy`(6) = *발문 전략*(`dialogue_turn` DB enum: 조건확인·예시제시·반례제시 등). 두 enum은 서로 다른 축이며 함께 쓰일 수 있다 — 슬라이스 2는 L4 6분류만 다룬다. **단계 기본 매핑**(스펙 L82-87 PRD 정렬표 + L65-70 본문 정합): UNDERSTAND→CLARIFICATION("어디까지 이해됐어?")·PLAN→PERSPECTIVE("관점 선택" 마디)·EXECUTE→IMPLICATION("그러면 다음은?")·REVIEW→META("어떻게 도달했어?"). **발화 신호 오버라이드**(stay/previous 한정, 가장 구체적 신호 우선): "가정·라고 치/하/두" → ASSUMPTION · "왜·이유·근거·증명·보장" → EVIDENCE(단계 무관). next 전이는 *단계 진입 발화*라 신호 무시(다음 단계 기본 사용). **PolyaCoach.decide() 통합**: `select_category()` 호출 추가 + `PedagogyDecision(socratic_category=category.value)` 세팅. 슬라이스 1의 `decide()` 시그니처·반환 모델은 변경 없음(필드 채움만). 슬라이스 1 테스트의 `socratic_category == ""` 단언만 갱신(`"clarification"`). **테스트** 2 신규 파일 + 1 확장(26 신규 케이스): `test_socratic_categories.py`(enum 6정합·한글 라벨·예시 질문 정본)·`test_socratic_select.py`(stage 기본 매트릭스 4·next 진입 매트릭스 4·발화 신호 오버라이드·신호 우선순위·previous=stay 동급)·`test_polya_engine.py`+1클래스 `TestSocraticCategory`(decide()가 채운 socratic_category·신호 오버라이드 통합검증). **4게이트 green**: black(src 71)·ruff·mypy-strict(src 71파일)·pytest **1064 passed**(1038→+26, cov-fail-under=70)·L4 모듈 전부 **cov 100%**(Stmts 159·Branch 26·Miss 0). **결정 v1 한계**(후속): 카테고리 *선택*만 — 카테고리별 *프롬프트 본문 변형*(질문 문구를 카테고리에 맞춰 재작성)은 미도입(slice 1 base 프롬프트 그대로 + EXAMPLE_QUESTION 별도 참조). LLM-judged 카테고리 선택·다중 카테고리 가중 점수도 미도입(보수적 결정론 우선). **후속**: 슬라이스 3 답 미루기 4단계 graded hint(`hint_level`·turn_count 기반 상승·prev_hint_level 추적)·슬라이스 4 오개념 진단(misconception_diagnosis.md 정본·30개 코딩)·슬라이스 5 LTHC·슬라이스 6 HTTP 노출. 슬라이스 2 자체 후속: 카테고리별 프롬프트 본문 차별화·LLM 카테고리 라우터 비교 실험.
- ✅ **L4 교수학 엔진 — 슬라이스 3 (답 미루기 4단계 graded hint)** 완료 (메인 직접, 2026-06-02) — 슬라이스 1·2까지 *항상 1*이던 `PedagogyDecision.hint_level`을 *신호 기반 점진 상승*으로 결정. 스펙 §"답 미루기 4단계"(L31-40) + §"PRD `Hint` 엔티티와의 정렬"(L41-61) + `docs/prompts/socratic_template.md` 시나리오 3·4 정본. **신규 모듈** `l4/hint_deferral.py`(85줄·100% cov): `HintLevel=Literal[1,2,3,4]`·`REVEALS` dict(PRD `Hint.reveals` 1:1 정렬: 1=next_concept_to_focus·2=step_flow·3=partial_steps_demo·4=full_solution[L52 WhyMath 고유 안전망])·`_FRUSTRATION_TOKENS` 7종(모르겠/막혔/어려/막막/헷갈/잘 안/도무지 — "어려"는 활용형 공통 스템·`어려서` false-positive 허용 문서화)·`_DEMAND_ANSWER_TOKENS` 8종(답 알려/답 좀/그냥 답/답이 뭐/정답 알려/정답이 뭐/풀이 좀/풀어줘)·`_STUCK_TURN_THRESHOLD=5`(L37 "5회+ 막힘 → 부분 풀이" 정본). **결정 규칙**(보수적 — 모호 시 *낮은* 단계): ① turn_count≥5 → `max(prev, 3)`(임계 우선·prev 보존) ② 답 요구 또는 좌절 신호 → `min(4, prev+1)`(점진 상승) ③ 그 외 → 1(가장 빠른 단계 복귀·생산적 막힘 우선·신호 사라지면 다시 은근하게). prev=None → 1 시작. **모델 확장**(slice 1·2 model 비파괴 — 필드 추가 default): `PolyaState.prev_hint_level: int|None=None`(ge=1 le=4)·`PedagogyDecision.reveals: str=""`(hint_level 파생). `PolyaCoach.decide()` 통합: state 입력에서 prev_hint_level·turn_count 읽어 `decide_hint_level()` 호출 → hint_level/reveals 동시 세팅(REVEALS 매핑). 슬라이스 1 테스트의 `hint_level==1` 단언은 *그대로 통과*(prev_hint_level=None·turn_count=0이라 디폴트 1). **테스트** 1 신규 파일(15 케이스)·1 확장 클래스(4 케이스): `test_hint_deferral.py`(디폴트 1·좌절/답요구 점진 상승·5회+ 임계·prev 보존·우선순위[stuck>signal]·REVEALS 정합·false-positive 한계 문서)·`test_polya_engine.py`+`TestHintDeferralWiring`(decide() hint_level/reveals 채움 통합검증). **4게이트 green**: black(src 72)·ruff·mypy-strict(src 72파일, `cast(HintLevel, ...)`로 `min`/`max` int → Literal 좁힘)·pytest **1087 passed**(1064→+23, cov-fail-under=70)·L4 모듈 전부 **cov 100%**(Stmts 182·Branch 30·Miss 0). **결정 v1 한계**(후속 명시): ① 토큰 경계 검사 없음("어려서" 등 false-positive — 보수적 허용, LLM-judged 좌절 감지로 대체 예정) ② `prev_hint_level` *영속*은 호출자/L2 세션 책임(이 모듈은 stateless) ③ `affect=frustrated` 등 정서 메타 입력은 미통합(L2 학습자 모델 연계 후) ④ KPI 집계(세션당 평균 reveals·평균 도달 깊이) 미구현 ⑤ 카테고리별 답 미루기 프롬프트 본문 차별화 미도입(스펙 §"단계별 적용"은 후속). **후속**: 슬라이스 4 오개념 진단(`docs/prompts/misconception_diagnosis.md` 정본 + 30개 코딩·PRM 검증 연계)·슬라이스 5 LTHC 적응(NRICH 진입점 조정)·슬라이스 6 HTTP 노출(인증·세션 영속). 비코드 잔여(슬라이스 1·2 공통): 라이브 LLM end-to-end(Phaiakes9·Anthropic 키)·L2 학습자 모델 통합·개념 점화 지도(L1 그래프 결선)·답 미루기 KPI 대시보드.
- ✅ **L4 교수학 엔진 — 슬라이스 4 (오개념 진단·개입 카탈로그·결정트리)** 완료 (메인 직접, 2026-06-02) — 스펙 §"오개념 진단·개입"(L121-127) + `docs/prompts/misconception_diagnosis.md` 정본 코딩. **핵심 원칙(절대 금지)**: ❌직접 교정·❌학생 라벨링·❌재풀이 강요 ✅반례 유도·✅구체 사례·✅거꾸로 사고 — *자각 유도*만(CLAUDE.md "직접 교정 X, 반례·구체 사례로 자각 유도"). 신규 서브패키지 `l4/misconception/`: ① `models.py`(`Misconception`[frozen Pydantic, id/name_kr/domain/canonical_statement/counterexample/signals]·`MisconceptionDomain` Literal 4분류·`MisconceptionMatch`[misconception+confidence 0-1+matched_signals]·`InterventionPattern` Enum 4종[COUNTEREXAMPLE·CONCRETE_CASE·VISUALIZATION·REVERSE_REASONING — doc 패턴 1-4]·`InterventionDecision`[pattern+prompt+misconception_id]) ② `catalog.py`(**스코프 정직**: doc 명시 14종만 — 대수 7·기하 3·확률통계 3·함수 1; "..." 자리표시자 16종은 추정 작성 *금지*·pedagogy-designer 후속) ③ `diagnose.py`(공출현 substring AND 매칭·confidence=matched/total·top-K 기본 3·동률은 catalog 순서 안정 정렬·매칭 0이면 빈 리스트) ④ `intervene.py`(결정트리 doc L75-79 정본: conf>0.8→COUNTEREXAMPLE 반례 유도·0.5≤conf≤0.8→REVERSE_REASONING 거꾸로 사고·conf<0.5→`None` 진단 보류; 프롬프트 어셈블리는 `canonical_statement`/`counterexample` 슬롯 치환). `l4/__init__.py` 9 신규 export 추가. **카탈로그 14종 정본 ID**(doc 1:1): 대수=distribution-over-power·sign-flip-in-inequality·division-by-zero·square-root-positivity·exponent-zero·fraction-cancellation·log-distribution / 기하=angle-sum-non-triangle·similarity-vs-congruence·area-perimeter-confusion / 확률통계=gambler-fallacy·prosecutor-fallacy·mean-vs-median / 함수=invertibility-without-1-1. **테스트** 3 신규 파일 26 케이스: `test_misconception_catalog.py`(14종 정합·ID 유일·`Misconception.frozen` 불변·`name_kr` 부정 표현 없음[CLAUDE.md 정서 안전])·`test_misconception_diagnose.py`(전체 매칭 1.0·부분 0.5·매칭 0 빈 리스트·top-K 기본 3·동률 catalog 순서 유지)·`test_misconception_intervene.py`(결정트리 경계값 0.8=mid·0.81=high·<0.5=None·프롬프트 자각유도형 어미·금지 표현 무출현 "잘못된/흔한 오개념/다시 풀어와/틀렸"). **4게이트 green**: black(src 77)·ruff·mypy-strict(src 77파일, Literal/Enum/frozen Pydantic 통과)·pytest **1113 passed**(1087→+26, cov-fail-under=70)·L4 모듈 전부 **cov 100%**(Stmts 256·Branch 40·Miss 0). **결정 v1 한계**(명시·후속): ① 매칭은 substring AND — 한국어/ASCII/유니코드 표기 변종은 *공통 분모* 토큰만 잡음(예: `√x²` vs `sqrt(x^2)` 둘 다 잡으려면 임베딩 필요) ② 풀이 단계별 파싱(PRM)·*처음 틀린 단계 식별*·임베딩 유사도 매칭은 후속(doc L60-70) ③ 패턴 2 CONCRETE_CASE·패턴 3 VISUALIZATION은 enum/모델만 등록·어셈블리 미구현(결정트리가 그 패턴을 *반환하지 않음* — 후속에서 confidence 외 신호[시각화 요청·구체수치 요청] 도입 시 활성화) ④ 카탈로그 16종 미명시 항목(doc "..." 자리표시자) 미코딩. **후속**: 슬라이스 5 LTHC 적응(NRICH 진입점 조정·학생 수준별 task 변형)·슬라이스 6 HTTP 노출(인증·세션 영속)·카탈로그 16종 채움(pedagogy-designer + Kiki)·PRM 단계 파싱 결선(L3 의존)·임베딩 매칭(text-embedding-3-large 도입 후).
- ✅ **L4 교수학 엔진 — 슬라이스 5 (LTHC 적응 — Low Threshold High Ceiling)** 완료 (메인 직접, 2026-06-02) — 스펙 §"LTHC"(L117-119) 정본: "NRICH 원칙. 동일 task를 학생 수준에 맞춰 *진입점·확장 조정*". 신규 서브패키지 `l4/lthc/`: ① `models.py`(`MasteryLevel` Literal 3종[`초보`·`발전 중`·`숙달` — `docs/prompts/socratic_template.md` system prompt §"숙달도 라벨" 정본]·`LthcAdaptation`[mastery_level + entry_suggestions(낮은 진입) + extensions(높은 천장) + scaffolds(비계, 막힘 보조)]) ② `adapt.py`(`adapt_lthc(stage, mastery_level)` — 결정론적 *축 강조* 표) ③ `__init__.py` 재노출. **축 강조 표 v1**(LTHC 핵심): 초보→entry+scaffold ON·extension OFF(천장 강요 회피); 숙달→extension ON·나머지 OFF(자명); 발전 중→세 축 *첫 1개씩*만(균형·초보→숙달 전이 학생 부담 완화). 즉 *같은 task(=Polya 단계)에서 수준에 따라 다른 후보 집합* 반환 — 이게 LTHC의 본질. **콘텐츠 정직**: 진입점 어구는 `polya_4step.md` Stage 2(계획) L26-30 정본 인용("비슷한 문제 본 적 있어?"·"더 작은 경우부터 시작해볼까?"·"그림·표·도형으로..."); 확장 어구는 NRICH 4원칙(일반화·변형·증명·다른 풀이) — `docs/strategy/partnerships.md` §"NRICH Cambridge MMP" 출처. 허위 인용 회피, 출처 docstring 명시. **단계별 비중 분포**: REVIEW에 확장 풍부(4종 모두), PLAN에 진입점 풍부(3종), UNDERSTAND/EXECUTE는 확장 후보 1종(단순 구조 인식·일반화). **slice 1-4 모델 비파괴**: `PedagogyDecision`·`PolyaState`·다른 모듈 변경 0 — LTHC는 독립 헬퍼(`adapt_lthc()`)로 노출, 호출자가 mastery_level 알 때만 호출. `PolyaCoach.decide()` 미통합(decide는 mastery 인자 없이 stateless 유지; L2 학습자 모델 연계 슬라이스에서 통합). **테스트** 1 신규 파일 12 케이스: `test_lthc.py`(초보 extensions=빈·진입점 비공·polya 정본 어구 검증/숙달 entry=빈·NRICH 4원칙 등장/발전 중 각 축 ≤1·축별 일관성/12 조합 매트릭스 전수·extra=forbid). **4게이트 green**: black(src 80)·ruff·mypy-strict(src 80파일)·pytest **1133 passed**(1113→+20, cov-fail-under=70)·L4 모듈 전부 **cov 100%**(Stmts 278·Branch 42·Miss 0). **결정 v1 한계**(명시·후속): ① 정적 콘텐츠 표(런타임 LLM 동적 변형 없음·도메인·문제별 맞춤 없음) ② L2 학습자 모델(BKT/DKT 숙달도) 연계 없음 — 호출자가 라벨 직접 결정 ③ `LthcAdaptation`은 `PedagogyDecision`에 통합 안 됨(독립 노출; 통합은 슬라이스 6 HTTP 결선 시) ④ "초보→숙달" 동적 자동 전이 없음(라벨 호출자 결정) ⑤ NRICH 라이선스 협상(`partnerships.md`) 전이라 NRICH 실제 task 변형 콘텐츠는 *미수집*. **후속**: 슬라이스 6 HTTP 노출(`/v1/coach` 엔드포인트 — 인증·세션 영속·PolyaCoach + LTHC + 오개념 통합)·NRICH 실콘텐츠 도입(라이선스 후)·L2 BKT/DKT → MasteryLevel 매퍼·도메인별 LTHC 변형 라이브러리.
- ✅ **L4 교수학 엔진 — 슬라이스 6 (HTTP 노출 `POST /v1/coach` — 통합 결정 엔드포인트)** 완료 (메인 직접, 2026-06-02) — 슬라이스 1-5의 모든 L4 결정 함수를 *한 발화에 묶어* HTTP로 노출한 첫 표면. 신규 `api/coach.py`(110줄, 100% cov)·`api/__init__` 4 export·`app.py` include_router 결선 + 신규 `tests/backend/api/test_coach.py`(14 케이스). **요청 모델 `CoachRequest`**(extra=forbid): `student_input`(min/max 0~4000자·남용 방어)·`polya_state: PolyaState`(default=UNDERSTAND 진입)·`mastery_level: MasteryLevel | None`(있을 때만 LTHC 반환). **응답 모델 `CoachResponse`**: `decision: PedagogyDecision`(필수, decide() 항상 반환)·`misconceptions: list[MisconceptionMatch]`(top-3, 없으면 [])·`intervention: InterventionDecision | None`(top-1 신뢰도 0.5+ 시만, <0.5는 진단 보류)·`lthc: LthcAdaptation | None`(mastery 있을 때만). **결선 흐름**(LLM 0회·DB 0회): ① `PolyaCoach.decide()` ② `diagnose()` top-3 ③ matches[0]→`select_intervention()` ④ mastery 있으면 `adapt_lthc(stage, mastery)`. **경계 명시(docstring)**: ① *stateless* — `dialogue` DB 쓰기·세션 영속은 후속(범위 밖·`_FakeSession` execute 호출 시 AssertionError로 *DB 무접근* 회귀 가드) ② LLM 호출 0(`PolyaCoach.coach()` 라이브 결선은 키 필요·별도 슬라이스) ③ **인증 게이트 = `ConsentedUser`**(미성년 동의 통과 — 학생 발화는 PII 가능, CLAUDE.md 미성년 PII 외부공유 금지·동의 게이트). **테스트** 14 hermetic 케이스: 기본 결정 직렬화·기본 state UNDERSTAND+stay+clarification·중립 입력 misconceptions=[]·LTHC=None when no mastery·distribution-over-power 카탈로그 풀 매칭 + counterexample 패턴 + `a=1, b=1` 어셈블리·부분 매칭 reverse 패턴·초보=entry/scaffold만·숙달=extensions만·prev_hint_level=1 + 답요구→hint_level=2 + reveals=step_flow·인증 401(WWW-Authenticate)·422 검증(missing/oversize/bad mastery/extra field). **4게이트 green**: black(src 81)·ruff·mypy-strict(src 81파일)·pytest **1147 passed**(1133→+14, cov-fail-under=70)·`api/coach.py` cov **100%**·L4 모듈 누적 cov 100% 유지. **🎯 마일스톤: L4 코치 표면 첫 결선 完** — Polya·소크라테스·답미루기·오개념·LTHC가 *한 엔드포인트*에서 통합 결정으로 노출됨. 라이브 LLM 좌석은 미통합(별도 슬라이스 — Anthropic 키 + 라이브 검증 필요). **결정 v1 한계**(명시·후속): ① stateless — 세션 영속 없음(클라이언트가 state를 매 요청에 보내고 받음·turn_count·prev_hint_level 영속은 호출자 책임) ② LLM 호출 0 — 응답은 *결정+프롬프트*뿐, 실제 학생용 문장은 *클라이언트가 prompt를 LLM에 보내거나* 후속 슬라이스에서 L3 좌석 결선 시 생성 ③ rate limiting/abuse 방어는 입력 길이 상한(4000자)뿐 — 분당 호출 제한은 ngx 단(별도) ④ 응답에 system 프롬프트 노출(클라이언트가 LLM 호출에 사용) — *학생에게 직접* 보여주지 않도록 클라이언트(L5) 책임 docstring 경고. **후속**: ① 라이브 LLM 좌석 결선(`/v1/coach/coach` LLM seam·Anthropic 키)·② 세션 영속(`POST /v1/coach/sessions` — `dialogue`/`learning_session` DB 쓰기 — ✅ 아래)·③ user_id 결선(slice 6은 user.user_id 미사용)·④ rate limit·⑤ 카탈로그 16종 확장.
- ✅ **L4 교수학 엔진 — 슬라이스 7 (세션 영속 `POST /v1/coach/sessions`)** 완료 (메인 직접·실 PG16 검증, 2026-06-02) — slice 6 stateless `/v1/coach` 옆에 **DB 쓰기 엔드포인트** 추가. 한 요청에 `dialogue` 1행 + `dialogue_turn` 2행(학생 + AI) 영속. **요청·응답 모델**: `SessionCreateRequest(CoachRequest)` + `problem_id: UUID | None`·`SessionCreateResponse(CoachResponse)` + `dialogue_id`·`student_turn_id`·`assistant_turn_id`. 슬라이스 6 `CoachRequest/Response`는 변경 없음(상속 확장). **공통 결정 추출**: `_build_response_payload()`로 두 엔드포인트가 decide+diagnose+intervention+lthc 계산 공유. **영속 흐름**: ① `DialogueSchema(user_id=user.user_id, problem_id=body.problem_id, started_at=now, total_turns=2, student_turns=1, assistant_turns=1)` → ORM `from_schema` → `session.add` → `commit` → `refresh` ② DialogueTurn 학생(turn_order=1·role=student·content=student_input·content_type=텍스트) + AI(turn_order=2·role=assistant·content=decision.prompt·content_type=텍스트) → `add_all` → `commit`. **FK 순서 정합**: parent(dialogue) commit 먼저 → child(turn) commit — slice 3 me_integration FK 순서 패턴 답습(user_id 원시 FK 컬럼·관계 없음·SQLAlchemy가 순서 못 추정). **LLM 호출 0** — AI 턴 content는 *결정된* `decision.prompt`(학생에게 던질 질문) 그대로 저장: LLM 미호출 상태에서도 *어떤 질문을 했는가*가 보존됨(라이브 LLM 결선은 별도 슬라이스). **user_id 자동 결선**: `dialogue.user_id = user.user_id`(인증된 본인만 — 타인 데이터 차단·CLAUDE.md 미성년 PII 외부 노출 금지). **테스트** 2층: ① hermetic 6 케이스(`TestSessionPersistence` — `_CapturingSession`이 add/add_all/commit/refresh 호출 캡처: dialogue 1+turn 2 객체 수·commits=2·refreshes=1·user_id 자동·content 결선·401·422 extra field/bad problem_id) ② **실 PG16 통합 1 케이스**(`test_coach_integration.py` — `pytestmark=integration`·`get_settings` 만 오버라이드·POST→dialogue_turn COUNT=2 SQL 검증·counterexample intervention 결선·무토큰 401·자식 FK 순서 정리 cleanup). **메인 라이브 검증**: 로컬 PG16서 통합테스트 1 passed(slice 3 인증 통합서 만들어둔 whymath/whymath_local 자격증명·alembic head 그대로 사용). **4게이트 green**: black(src 81)·ruff·mypy-strict(src 81파일)·pytest **1153 passed**(1147→+6 hermetic, cov-fail-under=70)·`api/coach.py` cov **100%**(Stmts 60·Branch 0·Miss 0) — slice 6 27 stmts에서 60으로 +33 신규 라인 전부 cov. **CLAUDE.md 정합 명시**(docstring): ① 미성년 채팅 평문 저장은 *저장 계층 책임*(DB 암호화 at-rest·미들웨어 — slice 7은 schema/ORM의 기존 방침 답습·docstring 상기만, `schema/dialogue.py` 모듈 docstring 정합) ② `ConsentedUser` 게이트 + `user_id` 자동 결선으로 본인 데이터 차단 ③ 응답 prompt는 *학생에게 직접 노출 금지*(클라이언트 책임). **결정 v1 한계**(후속): ① 단일 세션 생성만(턴 *추가*는 `POST /v1/coach/sessions/{id}/turns` 후속 슬라이스) ② LLM 호출 0(decision.prompt만 저장·실제 AI 응답 본문은 후속) ③ `socratic_strategy`(스키마 enum) 미세팅(L4 6분류는 `socratic_category`로 응답에 있되 DB enum SocraticStrategy는 별도 축 — slice 2 분리 메모) ④ DB 암호화 at-rest 미적용(인프라 슬라이스) ⑤ 학생 입력 *내용* 검열·필터 없음(coach 톤필터는 LLM 응답에만 — slice 1 정책). **🎯 마일스톤**: L4 코치 표면이 stateless 결정 + 영속 세션 두 모드로 완비됨. **후속**: 턴 추가 엔드포인트(`POST /v1/coach/sessions/{id}/turns` — ✅ 아래)·세션 조회(`GET /v1/coach/sessions/{id}`)·라이브 LLM 좌석 결선·DB 암호화 at-rest 인프라.
- ✅ **L4 교수학 엔진 — 슬라이스 8 (턴 추가 `POST /v1/coach/sessions/{id}/turns`)** 완료 (메인 직접·실 PG16 검증, 2026-06-02) — slice 7 `POST /v1/coach/sessions`가 *세션 1건+첫 2턴*만 만들 수 있던 한계 해소. 기존 dialogue에 학생/AI 2턴을 *반복 append*. **응답 모델 `TurnAppendResponse(CoachResponse)`**: + `student_turn_id`·`assistant_turn_id`·`student_turn_order`·`assistant_turn_order`(turn_order 노출로 클라이언트가 다음 호출에 안 보내도 됨). **소유권 검증**: `session.get(DialogueORM, id)` → `None`이거나 `dialogue.user_id != user.user_id`면 **404**(403 분리 안 함·*존재 여부 노출 회피* — 타인 dialogue가 *있다*는 사실 자체를 누설하지 않음, slice 3 me 패턴과 정합). **turn_order 증분 결정**: `dialogue.total_turns + 1`(student)·`+2`(assistant) — DB max() 쿼리 회피, dialogue 카운트가 단일 진실 원천(증분 정합). `total_turns=None`이면 0 fallback → 첫 append가 1·2 시작(legacy 세션 호환). **카운트 증가**: 같은 트랜잭션 안에서 `dialogue.total_turns += 2`·`student_turns += 1`·`assistant_turns += 1` → 다음 append 입력. **commit 1회**(slice 7 create는 parent→child 2회, append는 1회로 충분 — dialogue PK 이미 존재). **LLM 호출 0** 유지(`decision.prompt`를 AI 턴 content로 저장, slice 7 정합). **테스트** 2층: ① hermetic 6 케이스(`TestTurnAppend` — `_CapturingSession.preload`로 dialogue 주입·`.get(Model, pk)` 추가: 정상 append turn_order 3·4·dialogue 카운트 4/2/2·**404 부재 dialogue**·**404 타인 소유**(`user_id != _UID`)·401 무토큰·422 bad UUID·`total_turns=None`→1·2 시작) ② **실 PG16 통합 1 케이스**(`test_append_turns_extends_existing_session_on_live_pg` — 세션 생성 → append → COUNT(*)=4 SQL·turn_order 3·4 증분·좌절 신호 입력으로 hint_level≥2 slice 3 결선·존재하지 않는 dialogue 404). **메인 라이브 검증**: 로컬 PG16서 통합테스트 2 passed(생성·append 모두). **4게이트 green**: black(src 81)·ruff·mypy-strict(src 81파일)·pytest **1159 passed**(1153→+6 hermetic, cov-fail-under=70)·`api/coach.py` cov **100%**(Stmts 83·Branch 2·Miss 0; slice 7 60에서 +23 신규). **결정 v1 한계**(후속): ① 단일 append만 한 호출에 학생+AI 2턴(자유 N턴 추가는 미지원·"학생→AI"가 단위) ② LLM 호출 0(decision.prompt만 — AI 자연어 응답 후속) ③ 응답에 영속된 *turn content*는 미포함(클라이언트가 별도 조회·후속 `GET /v1/coach/sessions/{id}` 슬라이스에서 노출) ④ 세션 종료(`resolution` 세팅) 미지원(`PATCH /v1/coach/sessions/{id}` 후속) ⑤ rate limit·턴당 latency 측정 없음. **🎯 마일스톤**: L4 코치 표면 **다턴 대화 지원 완성** — 세션 생성·턴 추가·자동 turn_order 증분으로 *연속 Polya 대화*가 영속 가능. **후속**: 세션 조회(`GET /v1/coach/sessions/{id}` + 턴 목록 — ✅ 아래)·세션 종료(`PATCH` resolution)·라이브 LLM 좌석 결선(`coach()` Anthropic 키)·`GET /v1/me/dialogues` 연계(slice 3 me 라우터는 dialogue 목록만 — 본 슬라이스의 turn 상세 노출은 후속).
- ✅ **L4 교수학 엔진 — 슬라이스 9 (세션 조회 `GET /v1/coach/sessions/{id}`)** 완료 (메인 직접·실 PG16 검증, 2026-06-02) — slice 7·8의 쓰기 짝인 *읽기*. 기존 dialogue 메타데이터 + 정렬된 turn 목록 반환. **응답 모델 `SessionGetResponse`**: `dialogue: DialogueSchema`(전체 메타: user_id·problem_id·started_at·resolution·counts 등) + `turns: list[DialogueTurnSchema]`(turn_order ASC 정렬·학생/AI/system 전 role). **소유권 검증 동일**: `session.get(DialogueORM, id)` → `None`이거나 `dialogue.user_id != user.user_id` → **404**(slice 8 정합·존재 노출 회피). **턴 조회 쿼리**: `select(DialogueTurn).where(dialogue_id == id).order_by(turn_order)` — me 라우터·problem steps의 자식 조회 패턴 답습. 페이지네이션 없음(한 세션의 turn은 소량 가정·필요 시 후속). **ETag·If-None-Match 304 캐싱은 후속**(slice 6·7·8과 정합 — 쓰기 측은 ETag 없이 진행, 읽기 측 캐싱은 GET 단건 패턴의 후속 슬라이스). **테스트 인프라 확장**: `_CapturingSession`에 `.execute(stmt) → _Result.scalars().all()` 추가(turn 목록 주입용 `execute_rows` 파라미터·me 라우터 hermetic 테스트 패턴 재사용). **테스트** 2층: ① hermetic 6 케이스(`TestSessionGet`: 200 + 정렬·role/content 확인 / 빈 dialogue → turns=[] / 404 부재 / **404 타인 소유** / 401 무토큰 / 422 bad UUID) ② **실 PG16 통합 1 케이스**(`test_get_session_returns_dialogue_with_ordered_turns_on_live_pg`: 세션 생성→append→GET → turn_order [1,2,3,4] / role [student,assistant,student,assistant] 교차 / content 결선 / 404 부재 / 401 무토큰). **메인 라이브 검증**: 로컬 PG16서 통합테스트 3종(생성·append·조회) 모두 passed. **4게이트 green**: black(src 81)·ruff·mypy-strict(src 81파일)·pytest **1165 passed**(1159→+6 hermetic, cov-fail-under=70)·`api/coach.py` cov **100%**(Stmts 97·Branch 4·Miss 0; slice 8 83 → +14 신규). **결정 v1 한계**(후속): ① ETag/If-None-Match 304 미적용(쓰기 슬라이스와 정합·읽기 캐싱 후속) ② 턴 페이지네이션 없음(turn 수 폭증 시 limit/offset 후속) ③ 세션 *목록* `GET /v1/coach/sessions`는 기존 `GET /v1/me/dialogues`(slice 3)가 부분 커버(turn 본문 없는 dialogue 목록) — 본 슬라이스는 *상세* 조회만 ④ 다른 사용자 dialogue를 *공유 모드*로 보기는 안 됨(관리자·교사 권한 모델은 후속). **🎯 마일스톤**: L4 코치 표면 **CRUD-Read 전부 노출 완료** — POST 생성·POST append·GET 상세. 미완성: `PATCH`(resolution 세팅)·`DELETE`(세션 삭제)·`GET` 목록(me 라우터로 부분 충족). **후속**: 세션 종료/삭제·라이브 LLM 좌석 결선(`coach()` Anthropic 키)·ETag/304(✅ 아래)·rate limit·`GET /v1/me/dialogues` turn 본문 확장.
- ✅ **L4 교수학 엔진 — 슬라이스 10 (`GET /v1/coach/sessions/{id}` ETag/If-None-Match 304 캐싱)** 완료 (메인 직접·실 PG16 검증, 2026-06-02) — slice 9 GET 단건의 *읽기 캐싱* 추가. 기존 `api/_concurrency` 헬퍼(`etag_for`·`matches_if_none_match` — concept/problem GET 단건에 이미 적용된 RFC 7232 강한 ETag, content-hash sha256[:16]) **그대로 재사용** — DB/스키마/마이그레이션 변경 0. **핵심 결정**: ETag는 **`SessionGetResponse` 전체**(dialogue + turns) 표현의 해시 → *턴 1개 추가만으로도 ETag 바뀜*(slice 8 append 후 옛 ETag 자동 무효화 — 통합 테스트로 증명). dialogue 메타만 해시했다면 turn 변화에 무감각해 stale 캐시 위험. **결선**: `if_none_match: Annotated[str | None, Header()] = None`·`response: Response` 주입. 반환 타입 `SessionGetResponse | Response`(FastAPI는 Response 반환 시 response_model 우회 — concept/problem 패턴 답습). 매칭 시 `Response(304, headers={"ETag": etag})`(빈 본문·RFC 7232 정합)·아니면 `response.headers["ETag"] = etag` + 본문. **테스트** 2층: ① hermetic 5 케이스(`TestSessionGetConditional`: 200+ETag 헤더 존재·일치 If-None-Match 304+빈 본문+ETag 유지·`"*"` 와일드카드 304·stale ETag 200·**내용 바뀌면 ETag 바뀜**[같은 dialogue·다른 turn 집합 → 다른 ETag — content-hash 정합 증명]) ② **실 PG16 통합 1 케이스**(`test_etag_round_trip_304_then_invalidate_on_append_on_live_pg`: 세션 생성 → GET 200+ETag 캡처 → If-None-Match 304(빈 본문) → 턴 추가 → 옛 ETag로 GET 200(stale·turns=4·새 ETag 다름)). **메인 라이브 검증**: 로컬 PG16서 통합테스트 4종(생성·append·조회·**ETag 라운드트립**) 모두 passed. **4게이트 green**: black(src 81)·ruff·mypy-strict(src 81파일)·pytest **1170 passed**(1165→+5 hermetic, cov-fail-under=70)·`api/coach.py` cov **100%**(Stmts 103·Branch 6·Miss 0; slice 9 97 → +6 신규). **결정 v1 한계**(후속): ① `If-Match` 조건부 변경(쓰기 측 412)는 미적용 — coach 세션은 *append-only* 모델이라 동시 편집 충돌 자체가 발생 안 함(다음 사용자가 turn N+1, N+2 append하는 동안 동시 append는 turn_order 충돌 가능하나 본 슬라이스 범위 밖) ② 목록 304(`GET /v1/me/dialogues`)·중첩 304는 후속(concept/problem 슬라이스의 미진행분과 동일 정책 — 목록은 변경 잦아 캐시 효용 낮음) ③ ETag가 turn content 전체를 포함하므로 *대형 세션*에서 hash 비용 발생 — 50턴+ 세션은 후속 페이지네이션과 함께 처리. **🎯 의미**: L4 코치 GET이 **모바일 캐시 친화**(304 빈 본문 + 자동 무효화) — Flutter 클라이언트가 같은 세션을 반복 조회해도 대역폭·배터리 절약(MEMORY 슬라이스 56 "concept/problem 304" 정책 정합·전체 L4 표면으로 확장). **후속**: 세션 종료(`PATCH /v1/coach/sessions/{id}` resolution + If-Match 412)·세션 삭제(`DELETE` + cascade 또는 자식 명시 정리)·라이브 LLM 좌석 결선·rate limit(✅ 아래).
- ✅ **L4 교수학 엔진 — 슬라이스 11 (coach 엔드포인트 사용자당 분당 rate limit)** 완료 (메인 직접, 2026-06-02) — 4 coach 엔드포인트(POST /coach·POST /coach/sessions·POST /coach/sessions/{id}/turns·GET /coach/sessions/{id})에 *사용자당 분당 요청 상한* 도입. **신규 모듈** `api/_rate_limit.py`(100% cov, 34 stmts): `_SlidingWindow` 클래스(사용자별 직전 60초 타임스탬프 deque·만료 자동 prune·`time.monotonic` 단조 시계·DST/NTP 무관)·`_STORE` 모듈 전역(단일 프로세스 가정·FastAPI 단일 이벤트 루프 정합·asyncio.Lock 없이 GIL로 충분)·`rate_limit_user` FastAPI 의존성(`ConsentedUser` 직렬 통과 후 체크·초과 시 **429 Too Many Requests** + `Retry-After: 60` 헤더)·`reset_store()` 테스트 격리용·`RateLimited = Depends(rate_limit_user)` 별칭. **설정**: `Settings.coach_rate_limit_per_minute=60`(기본·`WHYMATH_COACH_RATE_LIMIT_PER_MINUTE` env 오버라이드)·**0=비활성**(테스트·개발). **결선 방식**: 4 엔드포인트의 `@router.post/.get` 데코레이터에 `dependencies=[RateLimited]` 추가(per-endpoint 매개변수 사용 안 함·라우터 메타 한 줄로 깔끔). **키 = user_id(UUID)**: 인증된 사용자 단위. IP·세션 단위 분리는 후속(미인증 표면 노출 시). **CLAUDE.md 정합**: ① 미성년 PII 무접근(`_STORE`엔 사용자별 타임스탬프 deque만·발화 내용 0) ② 학생 안전·웰빙(DoS·자동화 봇·LLM 비용 폭주 방어·향후 라이브 LLM 결선 시 critical) ③ 비용·효율(L3 LLM 호출 폭주 방어 — 슬라이스 12+ 라이브 LLM 결선 시 즉시 효과). **테스트 인프라 갱신**: 모든 기존 coach 테스트의 `_client()`·`_session_client()`·`_no_auth_client()`에 `app.dependency_overrides[get_settings] = lambda: Settings(coach_rate_limit_per_minute=0)` 추가 → 기존 1170 테스트 *영향 없음*(기본 60/min에 1175 requests 빠르게 누적되면 정합 깨질 위험 차단). `@pytest.fixture(autouse=True) reset_store()`로 테스트 간 카운트 누수 차단(autouse — 사용자 명시 호출 불필요). **테스트** 5 hermetic 케이스(`TestRateLimit`): under 통과(limit=3 → 3회 200)·**over 429+Retry-After=60**(limit=2 → 3번째 429)·**0=무제한**(20회 모두 200)·**GET도 동일 버킷**(POST/GET 미구분 — 슬라이스 결정)·**60초 prune**(`_SlidingWindow.hit(now=0)→True·(now=0.5)→False·(now=61)→True`, 클럭 seam으로 시간 진행 모사). **4게이트 green**: black(src 82)·ruff·mypy-strict(src 82파일)·pytest **1175 passed**(1170→+5 hermetic, cov-fail-under=70)·`_rate_limit.py` cov **100%**·`api/coach.py` cov **100%** 유지. **결정 v1 한계**(후속 명시): ① **단일 프로세스 인메모리** — 다중 워커(uvicorn workers >1)·다중 인스턴스(K8s/HA)에서는 워커별 별도 카운트 → 효과 ÷ N. **분산 환경은 Redis-backed sliding window로 교체**(slowapi 또는 자작·후속 인프라 슬라이스) ② **POST/GET 단일 버킷** — write가 read보다 비싼 코칭 흐름에서 차등 한도가 더 정밀(slice v2: POST 30/min, GET 60/min) ③ **IP-단위 한도 없음** — 미인증 표면이 생기면 IP-단위 추가 필요(현재는 ConsentedUser 직렬이라 무인증 호출 자체가 401에서 막힘) ④ **클럭 = monotonic**이라 *프로세스 시작 이래* 측정 — 재시작 후 첫 요청은 무한 과거 cutoff와 비교돼 항상 prune 통과(첫 요청은 항상 허용·합리적 디폴트) ⑤ **rate-limit 응답에 X-RateLimit-Remaining/Reset 헤더 미동봉**(클라이언트가 한도 인식하려면 후속에서 추가·v2). **후속**: Redis-backed sliding window(분산 정합·✅ 아래)·POST/GET 차등 한도·X-RateLimit-* 헤더 노출·IP-단위(미인증 표면 노출 시)·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선.
- ✅ **L4 교수학 엔진 — 슬라이스 12 (Redis-backed rate limiter — 분산 정합)** 완료 (메인 직접·실 Redis 검증, 2026-06-02) — 슬라이스 11의 인메모리 단일 프로세스 한계 해소. **백엔드 추상**: 신규 `RateLimitBackend` Protocol(async `hit(uid, limit, now)` + `reset()`)·기존 `_SlidingWindow` → `InMemoryBackend`로 명칭 갱신(`async hit`·async API)·신규 `RedisBackend`(`l3/cache/redis_cache.py` lazy + `_RedisClient` Protocol 주입 패턴 답습). **Redis 알고리즘**: 키 `rate:coach:{uid}` ZSET. `_LUA_HIT` Lua 스크립트가 **`ZREMRANGEBYSCORE` → `ZCARD` → 조건부 `ZADD`/`EXPIRE` 한 트랜잭션**으로 race window 0(원자성). 멤버는 `f"{now}:{uuid.uuid4().hex}"`로 동일 타임스탬프 충돌 회피. TTL 60초로 유휴 사용자 키 자동 만료. **설정**: `Settings.coach_rate_limit_backend: Literal["memory","redis"]`(**기본 memory** — 하위 호환·테스트·로컬). 라이브 도달성은 lazy(첫 hit 시 redis.from_url, S2 redis_cache 패턴 정합·라이브러리 미설치 시 RuntimeError). **결선**: `configure_backend_from_settings()` lifespan 전환 함수·`set_backend()`/`get_backend()` 테스트 seam·`_BACKEND` 모듈 전역 단일 인스턴스. **하위 호환**: 모든 기존 1175 테스트 *그대로 통과* — `Settings.coach_rate_limit_per_minute=0`(테스트 오버라이드) + `InMemoryBackend` 기본으로 무영향. `reset_store()`가 async가 되어 autouse fixture가 `asyncio.run()`으로 호출(테스트 격리 유지). **테스트 인프라**: `_FakeRedisClient`(zsets dict + Lua 의미 in-memory 재현·eval_calls 캡처)로 RedisBackend 결선·키 prefix·TTL 호출·reset 의미 검증. **테스트** 2층: ① **hermetic +10 케이스** — `TestRedisBackend`(under/over limit·키 prefix `rate:coach:{uid}`·reset clears all·prune semantics)·`TestRedisBackendLazyPaths`(settings fallback to global·settings injection·get_client lazy build via monkeypatch)·`TestBackendSelection`(memory default·redis when configured) ② **실 Redis 통합 2 케이스**(`test_rate_limit_integration.py`·`pytestmark=integration`·_redis_reachable skip): `test_redis_backend_enforces_limit_on_live_daemon`(단일 인스턴스 limit=2·3회 차단)·`test_redis_backend_distributed_consistency_on_live_daemon`(**분산 정합 핵심 증명**: worker_a + worker_b 별도 인스턴스가 같은 ZSET 공유 → 전역 카운트 묶임. 인메모리는 워커별 별도 카운트라 N×limit 허용되는 *반례*와 대비). **메인 라이브 검증**: 로컬 Redis 6379 데몬 띄워 통합 2 passed. **이벤트 루프 주의**(메모): redis-py async client는 루프 바인딩 — 한 테스트 내 `asyncio.run()` 다중 호출 시 'Event loop is closed' 발생. 테스트는 *단일* `asyncio.run(_run())`으로 묶음(slice 3 me_integration의 asyncpg 패턴과 정합). **4게이트 green**: black(src 82)·ruff·mypy-strict(src 82파일)·pytest **1185 passed**(1175→+10 hermetic, cov-fail-under=70)·`_rate_limit.py` cov **100%**(Stmts 79·Branch 16·Miss 0). **결정 v1 한계**(후속): ① **Lua 스크립트 캐싱**(`EVALSHA` 최적화) 미적용 — 매 호출 풀 스크립트 전송(작지만 분산 환경에서 SCRIPT LOAD 후 EVALSHA가 더 효율) ② **백엔드 hot-swap** 미지원(lifespan 시점에 한 번만 설정 — 런타임 전환은 후속) ③ Redis 연결 풀 튜닝·재시도 미적용(redis-py 기본) ④ `redis_url` 인증/TLS는 URL 자체로만(자격증명 env 주입). **🎯 의미**: rate limiter가 **다중 워커·다중 인스턴스 환경에서 정확** — Phaiakes9 단일 노드 + K8s 멀티 파드 둘 다 같은 의미. 후속 LLM 결선 슬라이스에서 *전역 비용 상한*으로 즉시 활용 가능. **후속**: EVALSHA 최적화(✅ 아래)·POST/GET 차등 한도·X-RateLimit-* 헤더·IP-단위 한도·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선.
- ✅ **L4 교수학 엔진 — 슬라이스 13 (EVALSHA + NOSCRIPT 폴백 — Lua 스크립트 캐싱)** 완료 (메인 직접·실 Redis 검증, 2026-06-02) — 슬라이스 12 `EVAL`이 매 호출마다 풀 Lua 스크립트(~200B)를 전송하는 비효율 해소. **알고리즘**: 모듈 로드 시 `_LUA_HIT_SHA1 = hashlib.sha1(_LUA_HIT.encode()).hexdigest()` 결정론적 계산(Redis `SCRIPT LOAD`가 반환하는 40-char hex와 *정확히 일치* — 같은 SHA1 알고리즘). 매 `hit()` 호출은 `client.evalsha(_LUA_HIT_SHA1, ...)` — 캐시 적중 시 40바이트만 전송(스크립트 본문 X). 정상 경로는 EVALSHA 1회. **NoScriptError 폴백**: Redis가 `SCRIPT FLUSH`·재시작·LRU 축출로 캐시에서 스크립트를 잃은 경우 `redis.exceptions.NoScriptError`(redis-py 정본) 발생 → backend가 `client.script_load(_LUA_HIT)` 호출해 캐시 재적재 → EVALSHA 재시도. 한 번의 자동 복구. **라이브러리 미설치 환경 호환**: NoScriptError를 try/except ImportError로 지연 import, fallback placeholder Exception 정의(미설치 시 RedisBackend 자체 미사용이라 raise 안 됨). **`_RedisClient` Protocol 갱신**: `eval` → `evalsha`/`script_load` 메서드로 교체(우리가 실제로 쓰는 메서드만 좁게 선언). **테스트 인프라**: `_FakeRedisClient`가 캐시 의미를 *완전히 재현* — `script_load(script)`로 적재된 SHA만 evalsha가 인정·미적재 SHA에 evalsha 호출 시 `NoScriptError` raise(redis-py 정본 예외 그대로). 이로써 fake 자체가 production 캐시 의미를 보장. **테스트** 2층: ① **hermetic +4 케이스**(`TestEvalshaOptimization`): `test_sha1_matches_redis_canonical`(hashlib SHA1 정합·40-char hex)·`test_first_hit_triggers_script_load_then_evalsha`(빈 캐시→NOSCRIPT→script_load→재시도→evalsha 2회·script_load 1회 정확 수)·`test_subsequent_hits_use_cached_script`(이후 5회는 evalsha 1회씩·script_load 추가 0)·`test_recovers_from_script_flush`(in-memory FLUSH 시뮬레이션 → 다음 hit이 script_load 재적재) ② **실 Redis 통합 1 케이스**(`test_evalsha_recovers_from_script_flush_on_live_daemon`): 실 Redis에 hit → 별도 admin client로 `SCRIPT FLUSH` 강제 → 다음 hit이 NoScriptError 잡아 자동 복구. **메인 라이브 검증**: 로컬 Redis 6379서 통합 3종(단일 limit·분산 정합·EVALSHA 복구) 모두 passed. **4게이트 green**: black(src 82)·ruff·mypy-strict(src 82파일)·pytest **1189 passed**(1185→+4 hermetic, cov-fail-under=70)·`_rate_limit.py` cov **100%**(Stmts 89·Branch 16·Miss 0; slice 12 79 → +10 신규). **결정 v1 한계**(후속): ① EVALSHA가 NOSCRIPT 외 다른 redis-py 예외(연결·타임아웃) 시에는 propagate(rate limit 호출자가 처리·문제 가시화 우선) ② 스크립트 변경 배포 시 SHA가 자동으로 갱신되나, 이전 버전 워커가 새 SHA를 모르면 NOSCRIPT 폴백으로 자동 적응(롤링 업그레이드 무중단) ③ `_LUA_HIT_SHA1`은 모듈 로드 시 1회 계산이라 핫리로드 환경에서 스크립트 수정 후 재시작 필요(개발 편의·production 영향 0). **🎯 의미**: 분산 환경 rate limit *네트워크·CPU 비용 절감* — 매 호출당 ~150B 절약(스크립트 본문 vs SHA1). 사용자 10K·각 60req/min 가정 시 일 ~1.3GB → ~3MB(99.7% 감축). **후속**: POST/GET 차등 한도(✅ 아래)·X-RateLimit-* 헤더·IP-단위 한도·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선·`pipeline()`으로 evalsha + 후속 모니터링 명령 묶기.
- ✅ **L4 교수학 엔진 — 슬라이스 14 (POST/GET 차등 rate limit — 별도 버킷)** 완료 (메인 직접·실 Redis 검증, 2026-06-02) — 슬라이스 11~13의 단일 버킷이 *쓰기/읽기 비용 차이*를 반영 못 하던 한계 해소. **설계**: `RateCategory = Literal["read","write"]` 도입 → `RateLimitBackend.hit()`에 `category` 키워드 인자 추가. `InMemoryBackend._by_key: dict[tuple[UUID, str], deque]`로 (사용자, 카테고리) 튜플 키. `RedisBackend._key(user_id, category) → f"rate:coach:{category}:{user_id}"`로 카테고리별 키 공간 완전 분리. **두 카테고리 상호 격리**: 한 카테고리 소진이 다른 카테고리에 영향 0 — 읽기 폭주가 쓰기를 막거나(또는 반대) 차단. **두 의존성**: `rate_limit_read`(GET)·`rate_limit_write`(POST)·각 `_enforce(user_id, category, limit)` 공통 헬퍼로 429 + Retry-After: 60. `RateLimitedRead`/`RateLimitedWrite` Depends 별칭. **설정 분리**: `coach_rate_limit_read_per_minute=60`(기본·캐싱 친화·GET은 ETag/304로 경량)·`coach_rate_limit_write_per_minute=30`(기본·DB·LLM 비용 고려·POST). 기존 `coach_rate_limit_per_minute` **폐기**(clean cut·production 사용자 없음·테스트 fixture만 갱신). **엔드포인트 결선**: POST 3개(`/v1/coach`·`/v1/coach/sessions`·`/v1/coach/sessions/{id}/turns`) → `dependencies=[RateLimitedWrite]`; GET 1개(`/v1/coach/sessions/{id}`) → `dependencies=[RateLimitedRead]`. **테스트 인프라**: `_settings_override(limit, write_limit=None)` 차등 지원 — `write_limit=None`이면 read와 같게(기존 1189 테스트 무영향). 모든 `.hit()` 호출에 `category="read"` 추가(sed 일괄·`uuid.uuid4()`는 별도 패턴으로 사후 보강). **테스트** 2층: ① **hermetic +4 케이스**(`TestReadWriteSeparation`·`TestRateCategoryBackend`): write_limit=1 소진 + read_limit=10 → GET 여러 번 통과(쓰기→읽기 비격리 가드)·read_limit=1 소진 + write_limit=10 → POST 여러 번 통과(읽기→쓰기 비격리 가드)·InMemory `(uid, "write")` 한도 도달해도 `(uid, "read")` 독립·Redis 키 `rate:coach:read:{uid}`/`rate:coach:write:{uid}` 동시 존재 확인 ② **실 Redis 통합 3종** 모두 카테고리 추가 후 그대로 passed(`category="read"` 명시·실 Redis 동작 무변경 — 키 prefix만 카테고리 포함). **메인 라이브 검증**: 로컬 Redis 6379서 통합 3종(단일 limit·분산 정합·EVALSHA 복구) 모두 passed. **4게이트 green**: black(src 82)·ruff·mypy-strict(src 82파일)·pytest **1193 passed**(1189→+4 hermetic, cov-fail-under=70)·`_rate_limit.py` cov **100%**(Stmts 96·Branch 16·Miss 0). **결정 v1 한계**(후속): ① **공유 user_id 카운트**(같은 사용자가 다중 디바이스로 동시 사용 시 합산) — 기기별 분리는 후속 ② **read·write 외 카테고리 없음**(예: `expensive_llm` 카테고리로 LLM 호출만 더 엄격하게 제한 가능 — 라이브 LLM 결선 슬라이스에서) ③ **응답 헤더로 한도·남은 횟수 노출 없음** — `X-RateLimit-Limit`/`-Remaining`/`-Reset` 표준 헤더는 후속(클라이언트가 한도 인식해 자체 throttle) ④ 차등은 *분당* 한도만 — 시간대별·burst rate 미차등(분당 한도가 충분). **🎯 의미**: rate limiting이 **트래픽 특성에 맞춰 정밀화** — 읽기는 모바일 캐시 친화 + 자주(60/min)·쓰기는 비용 보호 + 절약(30/min). 후속 라이브 LLM 결선 시 LLM-bound 쓰기는 더 낮추거나 `llm_call` 카테고리로 분리 가능. **후속**: `X-RateLimit-*` 헤더(✅ 아래)·IP-단위 한도(미인증 표면 노출 시)·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선·기기별 분리·burst rate(초당) 한도.
- ✅ **L4 교수학 엔진 — 슬라이스 15 (X-RateLimit-* 응답 헤더 — 클라이언트 자체 throttle)** 완료 (메인 직접·실 Redis 검증, 2026-06-02) — 슬라이스 11~14의 backend가 클라이언트에게 한도·남은 횟수·복구 시점을 *알려주지 않던* 한계 해소. **반환 타입 리팩터**: `RateLimitBackend.hit()`의 반환 `bool` → `RateLimitResult` NamedTuple(`allowed`·`remaining`·`reset_seconds`). 인메모리와 Redis 백엔드가 동일한 결과 객체 반환·`_result_from_window` 공통 헬퍼가 sliding window 상태에서 표준 결과 구성. **`allowed`는 명시 인자**(중요): `count_after ≤ limit`만으로는 *at-limit 미추가*(거부)와 *추가 후 limit 도달*(허용)을 구분 못 함 — bucket 상태로만 추정 불가라 호출자가 명시 전달. **Lua 스크립트 갱신**(SHA 변경): 반환 `{count, oldest_micros}`(2-튜플) → `{allowed, count_after, oldest_micros}`(3-튜플). SHA1 자동 재계산 → 기존 적재된 옛 SHA는 NOSCRIPT 발생 → 슬라이스 13 폴백이 자동 재적재(롤링 업그레이드 무중단 검증). `oldest_micros`는 음수=비어있음 sentinel(Lua 정수 반환 제약 + 동일 변환 로직). **`_enforce` 결선 갱신**: 두 dep에 `response: Response` 주입(FastAPI auto-inject)·성공 200 응답에 헤더 3종 동봉·거부 429 HTTPException에 같은 3 헤더 + `Retry-After=reset_seconds` 동봉. **표준 헤더 3종**(IETF 드래프트·GitHub-style): `X-RateLimit-Limit`(윈도우당 최대), `X-RateLimit-Remaining`(*이 응답 시점* 남은 슬롯·거부 시 0), `X-RateLimit-Reset`(다음 슬롯 비기까지 *남은 초*·거부 시 정확한 복구 시점). `Retry-After`는 `reset_seconds or 60`(0 폴백). **`_FakeRedisClient` 동기화**: Lua 의미 fake도 3-튜플 반환 + allowed flag 명시. **테스트 인프라 갱신**: 기존 모든 `.hit() is True/False` 단언을 `.allowed is True/False`로 sed 일괄 갱신(`uuid.uuid4()` 패턴 별도 보강). 모든 기존 1185 테스트 + slice 14 4 → 시그니처 변경 후 그대로 통과. **테스트** 2층: ① **hermetic +6 케이스** — `TestRateLimitHeaders`(200 응답 3 헤더 존재·`Remaining` 정확·`Reset` 0~60 범위/429 응답에 3 헤더 + `Retry-After ≥ 1`/연속 호출 시 `Remaining` 감소[3→2→1→0]/limit=0이면 헤더 미세팅)·`TestRateLimitResultStruct`(InMemoryBackend 직접 호출로 NamedTuple 반환·첫 hit allowed/remaining/reset 60·denied 시 reset도 60). ② **실 Redis 통합 3종** 모두 시그니처 변경 후 그대로 passed(`.allowed`로 단언 갱신). **메인 라이브 검증**: 로컬 Redis 6379서 통합 3종(단일 limit·분산 정합·EVALSHA 복구·차등 한도 정합) 모두 passed. **4게이트 green**: black(src 82)·ruff·mypy-strict(src 82파일·NamedTuple 통과)·pytest **1199 passed**(1193→+6 hermetic, cov-fail-under=70)·`_rate_limit.py` cov **100%**(Stmts 116·Branch 18·Miss 0; slice 14 96 → +20 신규). **결정 v1 한계**(후속): ① `X-RateLimit-Reset`이 *상대 초*(`60`)지 *Unix epoch*(`1717372800`)는 아님 — IETF 드래프트는 *시간 기반* 절대값을 권장하나 모바일 클라이언트는 상대값이 단순. 절대값 옵션은 v2 ② `Remaining`은 *이 응답 시점* 정확하나 클라이언트가 다음 호출 전 시간 경과 시 부정확(Reset 기반으로 *최소* 보장만) ③ `429 Retry-After`가 `reset_seconds or 60` — 정확히 0(즉시)인 경우 60으로 폴백(보수적·spam loop 방지) ④ 헤더 미동봉 시(limit=0) 클라이언트는 "한도 없음"으로 가정 — 명시적 "unlimited" 신호 헤더는 없음. **🎯 의미**: 클라이언트가 *서버 한도를 미리 인식*해 자체 throttle 가능 — Flutter 클라이언트가 `Remaining=0`·`Reset=15` 보면 *15초 대기 후* 재시도 자연스럽게 구현 가능. 429 핑퐁 방지 + 사용자 UX 개선(예: "잠시 후 시도하세요" + 카운트다운). **후속**: IP-단위 한도(미인증 표면 노출 시·✅ 아래)·`Reset`을 절대 epoch 옵션·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선·기기별 분리·burst rate(초당) 한도.
- ✅ **L4 교수학 엔진 — 슬라이스 16 (IP 단위 rate limit — 미인증 표면 노출 대비)** 완료 (메인 직접·실 Redis 검증, 2026-06-02) — 슬라이스 11~15는 `ConsentedUser` 직렬이라 user_id 키로 충분했으나, 미인증 표면(예: 헬스체크·OAuth 콜백·공개 상태 엔드포인트)이 도입될 경우 IP 단위 한도가 필요. **병렬 메서드 패턴**(breaking 0): `RateLimitBackend.hit()`(user_id 단위) 무변경 + `hit_by_ip(ip, ...)` 추가. 내부 공통화 `_hit_by_key(subject_key, ...)` — subject_key는 `f"user:{uid}"` / `f"ip:{addr}"`로 *네임스페이스 분리*. **백엔드 갱신**: `InMemoryBackend._by_key: dict[tuple[str, str], deque]`로 (subject_key, category) 튜플. `RedisBackend` 키 `rate:coach:{category}:{subject_key}` — 예 `rate:coach:read:user:abc-...` vs `rate:coach:read:ip:198.51.100.1` 완전 충돌 없음. Lua 스크립트 *무변경*(키만 다른 prefix·SHA1 그대로·NoScriptError 폴백 영향 0). **신규 설정**: `coach_rate_limit_ip_read_per_minute=120`(기본 — 사용자 60보다 *2배 느슨*하게: 공유 NAT·캠퍼스·기숙사 단일 IP 보호)·`coach_rate_limit_ip_write_per_minute=60`(기본 — 사용자 30보다 2배). **신규 의존성** 2종: `rate_limit_ip_read`/`_write`(FastAPI `Request` auto-inject)·`RateLimitedIpRead`/`Write` Depends 별칭. **IP 추출 헬퍼** `_client_ip(request)`: ① `X-Forwarded-For` 첫 항목(LB·CDN 뒤 운영 정합 — `uvicorn --proxy-headers` 가정) ② `request.client.host` 폴백 ③ 둘 다 없으면 `None`(한도 미적용·알 수 없는 IP는 차단/통과 어느 쪽도 안전 아닌 fallback 선택). **프록시 신뢰 모델 명시**(docstring): 운영 시 신뢰 IP 화이트리스트 + `uvicorn --proxy-headers` 필수(신뢰 없는 환경에선 X-Forwarded-For 위변조 가능). **엔드포인트 결선**: 현재 미인증 엔드포인트 없음 → coach 라우터 무변경(slice 11~15 사용자 한도 그대로). dep는 *준비 완료* 상태로 노출(`api/_rate_limit` export) — 미인증 표면 추가 시 즉시 부착 가능. **테스트** 2층: ① **hermetic +11 케이스** — `TestIpRateLimit`(InMemory IP·user 격리·Redis 키 prefix 카테고리/네임스페이스 분리·두 IP 독립 버킷·XFF 첫 항목 추출·request.client.host 폴백·None 시 한도 미적용·빈 XFF→폴백·테스트 엔드포인트 IP read dep 결선)·`TestIpRateLimitEdgeCases`(IP write dep 결선·limit=0 비활성·request.client=None 단축 폴백). ② **실 Redis 통합 3종** 모두 시그니처 변경 후 그대로 passed(IP 키도 같은 Lua 스크립트·SHA1·EVALSHA 흐름 따라감). **메인 라이브 검증**: 로컬 Redis 6379서 통합 3종 모두 passed. **4게이트 green**: black(src 82·tests 1)·ruff·mypy-strict(src 82파일)·pytest **1210 passed**(1199→+11 hermetic, cov-fail-under=70)·`_rate_limit.py` cov **100%**(Stmts 157·Branch 34·Miss 0; slice 15 116 → +41 신규). **결정 v1 한계**(후속): ① **사용자 + IP 동시 적용 안 됨** — 현재 deps는 *둘 중 하나*만 부착. *방어 심층*(같은 엔드포인트에 user dep + ip dep 둘 다)으로 양쪽 한도 적용도 가능하나, double-counting 정합·헤더 충돌 분석 후속 ② **IPv6 정규화 없음** — `2001:db8::1`과 `2001:0db8:0000::1`은 다른 키. 정규화는 `ipaddress.ip_address()`로 표준 형태 변환 가능(후속) ③ **CIDR/서브넷 묶음 없음** — `/24` 단위 한도(단일 ISP 우회 방어)는 후속 ④ **X-Real-IP 미지원** — XFF 우선 + client.host만. ⑤ **프록시 신뢰 화이트리스트 미코딩** — 운영 가정만 docstring(코드는 XFF 무조건 신뢰). **🎯 의미**: 미인증 표면 도입 시 *즉시* 적용 가능 — `dependencies=[RateLimitedIpRead]` 한 줄. 사용자 + IP 별도 키 공간이라 *공유 NAT 사용자*(학교·도서관·공유오피스)가 한 명 때문에 다른 사람들이 차단되지 않음(개인 인증 사용자는 user 키, 인증 미사용 트래픽은 IP 키 — 둘이 독립). **후속**: 사용자+IP 동시 적용(✅ 아래)·IPv6 정규화·CIDR 묶음·X-Real-IP·프록시 화이트리스트·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선.
- ✅ **L4 교수학 엔진 — 슬라이스 17 (사용자 + IP 동시 rate limit · 방어 심층)** 완료 (메인 직접·실 Redis 검증, 2026-06-02) — 슬라이스 11~15 user-only + 슬라이스 16 IP-only를 *결합*해 인증 엔드포인트에 양 방어선 동시 적용. **동기**: 단일 사용자 학대 + 공유 NAT/IP 학대를 *서로 다른 공격면*으로 동시 방어 — 한 IP에 N 사용자(공유 오피스·학교) 시 IP 한도가, 한 사용자가 N 디바이스 시 user 한도가 발화. **신규 통합 dep**(2종): `RateLimitedDefenseRead`/`RateLimitedDefenseWrite`(인증된 GET/POST 엔드포인트용). 내부 `_enforce_defense(user_id, ip, category, user_limit, ip_limit, response)`: ① user 검사 먼저 — 429 시 *user 헤더*로 응답(`Retry-After=user.reset_seconds`) ② IP 검사 후 — 429 시 *IP 헤더*로 응답 ③ 둘 다 통과 시 `_tightest_headers`로 *더 엄격한 쪽*(`remaining` 더 작은) 헤더 노출(동률은 user 우선, 인증 사용자 친화). **`_tightest_headers` 헬퍼**: `(user_pair, ip_pair)` → 한쪽 None이면 그쪽·둘 다 있으면 `remaining` 작은 쪽 반환. 클라이언트는 *다음에 발화할* 한도를 정확히 인식 가능. **4 coach 엔드포인트 전환**: `RateLimited{Read,Write}` → `RateLimitedDefense{Read,Write}` 일괄. `_client_ip` 헬퍼는 슬라이스 16 그대로 재사용(XFF → request.client.host → None 폴백). **Clean cut**(CLAUDE.md "범위 밖 코드 금지"): 슬라이스 11~15의 user-only `rate_limit_read`/`write` 및 `_enforce` 헬퍼는 *호출자 0* → 제거. 사용자 한도만 원하는 경우엔 `ip_limit=0` 설정으로 Defense가 동등하게 동작(동일 의미·코드 중복 0). **테스트 인프라 갱신**: `_settings_override(limit, write_limit, ip_limit, ip_write_limit)`로 4 한도 분리 제어. `ip_limit` 기본 0 → 기존 1199 테스트가 IP dep을 의식하지 않고 그대로 통과(Defense가 IP 검사 짧게 반환). **테스트** 2층: ① **hermetic +9 케이스**(`TestDefenseInDepth`·`TestTightestHeadersHelper`): user_limit=1·ip=10 → 2번째 user 한도 429(Limit=1)·user=100·ip=1 → 2번째 IP 한도 429(Limit=1)·user=10·ip=2 → tight IP 헤더(Limit=2·Remaining=1)·ip_limit=0 → user-only 동작·둘 다 0 → 무한 통과·`_tightest_headers` ip None/user None/둘 다 있음/동률 user 우선/둘 다 None 빈 dict. ② **실 Redis 통합 3종** 모두 그대로 passed(시그니처 변경 없음·기존 hit/hit_by_ip 호출). **메인 라이브 검증**: 로컬 Redis 6379서 통합 3종 passed. **4게이트 green**: black(src 82)·ruff·mypy-strict(src 82파일)·pytest **1220 passed**(1210→+10 net: +11 hermetic - 1 변경된 zero-limit 테스트도 통과)·`_rate_limit.py` cov **100%**(Stmts 178·Branch 46·Miss 0; slice 16 157 → +21 신규·죽은 코드 제거로 stmts 감소도 일부 반영). **🎯 의미**: 인증된 엔드포인트가 *방어 심층* — 사용자 학대(인증 토큰 탈취·자동화) **및** IP 학대(공유 NAT의 한 사용자 폭주가 같은 IP 다른 사용자 영향 차단) 모두 방어. **결정 v1 한계**(후속): ① 두 한도 *순차 검사* — user 통과 → IP 거부 시 user counter는 이미 증가(낭비). 최적화는 *원자적 동시 검사* Lua 스크립트(둘 다 확인 후에만 양쪽 증가)·v2 ② 200 응답 헤더는 *더 엄격한* 쪽만 노출 — 두 쌍 헤더 동시 노출(`X-RateLimit-User-*`/`-Ip-*` 접두)은 후속 ③ user 검사 시 ip는 검사 안 함(429 시 user 헤더만 노출·IP 한도 정보 누락) ④ 두 카운터 *완전 독립*이라 user 한도와 IP 한도가 *상호* 보강 안 함(예: user 5/10, ip 5/10이라도 합산 10이 아닌 max=5만 효과). **🎯 라우터 표면 보안 마일스톤**: rate limit이 **완전 production-ready** — 분산(Redis·EVALSHA)·차등(read/write)·헤더(X-RateLimit-*)·IP(미인증 표면)·방어 심층(user+IP) 7 슬라이스 누적. **후속**: 원자적 동시 검사 Lua(✅ 아래)·두 쌍 헤더 동시 노출·IPv6 정규화·CIDR 묶음·X-Real-IP·프록시 화이트리스트·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선.
- ✅ **L4 교수학 엔진 — 슬라이스 18 (원자적 user+IP 동시 검사 — 낭비 counter 0)** 완료 (메인 직접·실 Redis 검증, 2026-06-02) — 슬라이스 17 `_enforce_defense`가 user 검사 후 IP 검사로 *순차 호출* — race window + IP가 거부 시 *user counter 낭비* 문제 해소. **핵심 원자성**: `RateLimitBackend.hit_both(user_id, ip, *, category, user_limit, ip_limit, now)` 신규 Protocol 메서드. 단일 호출에 두 키 모두 검사 → 둘 다 슬롯이 있어야 둘 다 increment. 한쪽이라도 거부면 *어느 쪽도 미증가*. race window 0(인메모리는 단일 GIL 프레임·Redis는 단일 Lua 트랜잭션). **신규 모델**: `CombinedRateLimitResult` NamedTuple(`allowed`·`user_result: RateLimitResult | None`·`ip_result: RateLimitResult | None`). 한쪽 limit=0 또는 ip=None이면 그쪽 None(검사 미수행). 개별 `allowed`는 *동일 atomic 결정* 공유(둘 다 True 또는 둘 다 False). **신규 Lua 스크립트** `_LUA_HIT_BOTH`(2 keys, SHA 별도 `_LUA_HIT_BOTH_SHA1`): ZREMRANGEBYSCORE → ZCARD × 2 → `if user_ok and ip_ok` 원자 결정 → 조건부 ZADD/EXPIRE × 2 → 둘 다의 oldest 추출 → 5-튜플 `{allowed, user_count_after, user_oldest_micros, ip_count_after, ip_oldest_micros}` 반환. 한쪽 limit=0이면 그쪽 검사·증가 모두 *스킵*(키 미터치·count/oldest = -1 sentinel). NOSCRIPT 폴백 자동 적재(slice 13 패턴). **`_enforce_defense` 리팩터**: 슬라이스 17의 user→IP 순차 검사 → `hit_both` 단일 호출. 거부 시 *blocker* 측(remaining=0인 쪽)의 헤더로 429. 통과 시 `_tightest_headers`로 더 엄격한 쪽 노출(슬라이스 17과 동일 의미·구현 단순화). **InMemoryBackend.hit_both**: 두 deque를 prune → 원자 결정(`user_ok and ip_ok`) → 조건부 append → `_result_from_window`로 결과 구성. **Fake 동기화**: `_FakeRedisClient._apply_hit_both_script`로 Lua 원자 의미 재현·`evalsha`가 `numkeys=2`면 hit_both, 아니면 hit. NOSCRIPT는 두 스크립트 모두에 적용. **테스트 인프라**: 슬라이스 17 외부 테스트 무변경 — `_enforce_defense` API 동일·dep 동일. 새 9 hermetic 케이스만 추가. **테스트** 2층: ① **hermetic +9 케이스**(`TestHitBothAtomic`·`TestDefenseAtomicInRouter`): 둘 다 통과 시 양쪽 증가·user 차단 시 IP 미증가·**IP 차단 시 user counter 낭비 0**(invariant 검증 — 세 번째 다른 IP로 통과 시 remaining 8 = 10 - 2 ✓)·user-only(ip=None)·ip-only(user_limit=0)·60초 prune·Redis fake hit_both 원자성·NOSCRIPT 폴백·라우터 결선 검증. ② **실 Redis 통합 3종** 모두 그대로 passed(slice 17 통합은 hit 사용·SCRIPT FLUSH는 두 SHA 모두 비움·다음 호출 자동 적재). **메인 라이브 검증**: 로컬 Redis 6379서 통합 3종 passed. **4게이트 green**: black(src 82)·ruff·mypy-strict(src 82파일)·pytest **1229 passed**(1220→+9 hermetic, cov-fail-under=70)·`_rate_limit.py` cov **100%**(Stmts 221·Branch 56·Miss 0; slice 17 178 → +43 신규). **결정 v1 한계**(후속): ① **두 쌍 헤더 동시 노출 미적용** — 200은 *더 엄격한 쪽*만(`X-RateLimit-Limit/Remaining/Reset`); `X-RateLimit-User-*`/`-Ip-*` 접두로 둘 다 노출은 후속 ② **Lua 5-튜플 반환** — Redis-py가 이미 list 매핑하지만, 향후 더 많은 필드(둘의 oldest 분리 노출 등) 추가 시 dict-like 응답 검토 ③ **3개+ 차원 검사 미지원** — 현재 user+IP 2개. user+IP+device(`X-Device-Id` 헤더) 3개로 확장 시 `hit_many(keys, limits)` 일반화 필요 ④ EVALSHA 캐시 미스율 측정 안 함(관측성 추가 후속). **🎯 의미**: 슬라이스 17의 race + waste 둘 다 제거 — **고부하 환경에서 정확한 한도** 보장(user N/IP M으로 분리된 두 user가 동시에 max IP 한도에 도달해도 어느 쪽도 user counter 손실 없음). 분산 환경(다중 워커)에서 Lua 원자성이 *모든 인스턴스 간 일관* 보장. **rate limit 8 슬라이스 누적 — production-grade**(분산 atomic·차등·헤더·IP·방어 심층·원자 동시 검사). **후속**: 두 쌍 헤더 동시 노출(✅ 아래)·3차원 한도(user+IP+device)·IPv6 정규화·CIDR 묶음·X-Real-IP·프록시 화이트리스트·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선.
- ✅ **L4 교수학 엔진 — 슬라이스 19 (X-RateLimit-User-* / -Ip-* 두 쌍 헤더 동시 노출)** 완료 (메인 직접·실 Redis 검증, 2026-06-02) — 슬라이스 15의 단일 rollup(`X-RateLimit-*` *더 엄격한 쪽만*)이 가리는 *다른 차원의 상태* 노출. 클라이언트가 어느 차원이 더 빠듯한지 *독립 인식* 가능. **신규 헬퍼** `_pair_headers(prefix: str, limit, result)`: `X-RateLimit-{prefix}-Limit/Remaining/Reset` 3개 헤더 dict 반환(`User`/`Ip` 두 차원 동일 형식·prefix만 다름). **결선 패턴**: ① `_enforce_defense` 200 응답에 활성화된 *두 차원 모두* pair-headers 동봉 + slice 15 rollup(더 엄격한 쪽) 동봉 ② 429 응답에 *두 차원 + rollup(blocker)* 모두 동봉 + `Retry-After` ③ `_enforce_by_ip`(미인증 IP-only dep)도 `X-RateLimit-Ip-*` + rollup 동봉(미인증 표면 일관성). 활성화된 차원만 노출 — `user_limit=0` 또는 `ip_limit=0`이면 그쪽 pair 미세팅(클라이언트는 "한도 없음"으로 인식). **하위 호환**: slice 15 `X-RateLimit-*` rollup 헤더 *그대로 유지* — 슬라이스 18 의미(더 엄격한 쪽·Defense 200) 그대로. 기존 클라이언트가 rollup만 보면 슬라이스 15와 동일하게 동작·새 클라이언트는 pair-* 추가 읽어 *두 차원 동시* throttle 가능. **테스트** 2층: ① **hermetic +7 케이스**(`TestPairHeaders`·`TestPairHelperUnit`): Defense 200에 두 쌍 + rollup(User-Limit=10/Remaining=9·Ip-Limit=2/Remaining=1·rollup=Ip가 더 엄격)·429에 blocker rollup + 두 쌍(첫 호출 user=1 후 atomic deny → User-Remaining=9, Ip-Remaining=0)·`ip_limit=0`이면 User-*만 노출·`user_limit=0`이면 Ip-*만 노출(Defense 경로)·IP-only dep에서 `X-RateLimit-Ip-*` + rollup만 노출(User-* 미세팅)·`_pair_headers` 헬퍼 단위(prefix 조립·키 세트 정합). ② **실 Redis 통합 3종** 그대로 passed(hit/hit_both API 변경 없음·헤더 결선만 변경). **메인 라이브 검증**: 로컬 Redis 6379서 통합 3종 passed. **4게이트 green**: black(src 82)·ruff·mypy-strict(src 82파일)·pytest **1235 passed**(1229→+6 hermetic, cov-fail-under=70)·`_rate_limit.py` cov **100%**(Stmts 231·Branch 62·Miss 0; slice 18 221 → +10 신규). **결정 v1 한계**(후속): ① **prefix 케이스** = `User`/`Ip`로 고정(소문자 `user`/`ip`도 HTTP 헤더는 대소문자 무관이라 동작은 같으나, JSON 직렬화·로깅·문서화 일관성 위해 PascalCase) ② **3차원+ 일반화 미지원** — `_pair_headers` API는 prefix str 받지만 dep 구조는 user/IP 2차원 가정. user+IP+device(`X-Device-Id`) 3차원으로 확장 시 `RateLimitBackend.hit_many(subjects, limits)` 일반화 필요(slice 18 hit_both는 정확히 2-key Lua) ③ **rollup 결정 알고리즘** = remaining 최소(동률은 user 우선). retry-after는 blocker 기준이지만 두 쌍 헤더 노출로 클라이언트가 "더 일찍 만료되는 쪽"을 인식할 수 있어 일부 한계 완화 ④ **헤더 사이즈** 증가 — 3 → 6~9 헤더(각 ~30B)로 응답당 +180B. HTTP/2 HPACK 압축으로 실제 비용은 낮으나 측정 권장. **🎯 의미**: 클라이언트가 *어느 차원에서 곧 막힐지* 정확히 인식 — Flutter 클라이언트가 `User-Remaining=8`·`Ip-Remaining=0` 보면 "이 사용자는 9번 더 쓸 수 있으나 *현재 IP에선* 즉시 막힘"으로 인식해 "다른 네트워크로 전환" UX 가능. *진단 가시성*도 향상 — 429 시 어느 차원이 발화했는지 헤더로 즉시 파악(블로그·디버깅·온콜 친화). **rate limit 9 슬라이스 누적**. **후속**: 3차원 한도(user+IP+device·✅ 아래)·IPv6 정규화·CIDR 묶음·X-Real-IP·프록시 화이트리스트·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선·`Retry-After` 헤더 정책 표준화(blocker의 reset_seconds vs 60 기본).
- ✅ **L4 교수학 엔진 — 슬라이스 20 (3차원 rate limit · user+IP+device · X-Device-Id)** 완료 (메인 직접·실 Redis 검증, 2026-06-02) — 슬라이스 18 `hit_both`(2차원 고정)을 *N차원 일반화*해 device 차원 추가. **신규 일반화 API**: `Subject` NamedTuple(`kind: SubjectKind`·`id: str`·`limit: int`)·`SubjectKind = Literal["user","ip","device"]`(확장 가능)·`RateLimitBackend.hit_many(subjects, *, category, now) -> dict[SubjectKind, RateLimitResult]` Protocol 메서드. 모든 subject가 통과해야 모두 increment(slice 18 원자 패턴을 N으로 확장). **신규 Lua** `_LUA_HIT_MANY`(가변 KEYS, ARGV `[now, member, limit1..limitN]`): prune 전부 → ZCARD × N → `all_ok` 결정 → 조건부 ZADD/EXPIRE × N → `[allowed, count1, oldest1, ..., countN, oldestN]` 반환. SHA1 결정론·NOSCRIPT 폴백 자동(slice 13 패턴 재사용). N=2면 `hit_both`와 의미 동등(향후 hit_both shim화 후보). **InMemoryBackend.hit_many**: 단일 GIL 프레임 안에서 모든 subject prune → atomic 결정 → 조건부 append → 결과 dict 구성. 한 kind 중복 시 마지막 우선(호출자 책임). **신규 헬퍼·dep**: `_client_device_id(request) -> str | None`(`X-Device-Id` 헤더 추출·빈/누락 None)·`_enforce_triple`(user+IP+device 3차원 원자 검사·세 쌍 pair-headers + rollup 노출·blocker 우선순위 user > ip > device)·`rate_limit_triple_read/write` FastAPI deps·`RateLimitedTripleRead/Write` 별칭. **신규 설정**: `coach_rate_limit_device_read_per_minute=90`·`_write_per_minute=45`(기본 — 사용자 60/30과 IP 120/60의 중간; 한 사용자가 다중 디바이스 가능하나 한 디바이스 폭주는 그 디바이스만 제한). **4 coach 엔드포인트 전환**: slice 17의 `RateLimitedDefense{Read,Write}` → `RateLimitedTriple{Read,Write}`. X-Device-Id 헤더 없으면 device 차원 검사 비활성(user+IP만 — slice 17 의미 보존). **Clean cut**(CLAUDE.md): slice 17-19의 Defense deps + `_enforce_defense` + `_tightest_headers` 헬퍼 모두 *호출자 0* → 제거. Triple이 device_limit=0으로 동등 동작·코드 중복 0. **테스트 인프라**: `_settings_override`에 `device_limit`/`device_write_limit` 추가(기본 0 = 비활성·기존 테스트 무영향). `_FakeRedisClient`에 `_apply_hit_many_script`(KEYS 가변 파싱·Lua N차원 의미 재현) + `evalsha` SHA1로 _LUA_HIT_MANY/_LUA_HIT_BOTH/_LUA_HIT 분기. 슬라이스 18-19의 `TestTightestHeadersHelper`(Defense 헬퍼 단위) 삭제(`_tightest_headers` 제거 정합). **테스트** 2층: ① **hermetic +9 케이스**(`TestTripleDimension`·`TestHitManyEdgeCases`): hit_many 3차원 모두 통과·**device 차단 시 user/IP counter 낭비 0**(slice 18 invariant N차원 확장 증명·다른 device로 통과 시 user remaining 8 = 10-2 ✓)·빈 subjects no-op·Redis hit_many 3 키 모두 ZSET 생성·X-Device-Id 헤더 추출(정상·누락·빈)·coach POST device 한도 결선(X-RateLimit-Device-Limit 헤더·429 시 blocker=device)·헤더 누락 시 device 차원 스킵(5회 모두 통과)·prune·Redis 빈 subjects no-op. ② **실 Redis 통합 3종** 모두 그대로 passed(Triple은 hit_many 사용·기존 hit/hit_by_ip API 변경 없음·SCRIPT FLUSH는 세 SHA 모두 비움·자동 재적재). **메인 라이브 검증**: 로컬 Redis 6379서 통합 3종 passed. **4게이트 green**: black(src 82)·ruff·mypy-strict(src 82파일·Literal 인덱스·next(iter) 폴백·Subject NamedTuple 통과)·pytest **1242 passed**(1235→+7 net: +9 hermetic - 2 죽은 helper test, cov-fail-under=70)·`_rate_limit.py` cov **100%**(Stmts 286·Branch 82·Miss 0; slice 19 231 → +55 신규·-Defense 정리). **🎯 의미**: rate limit이 **3차원 방어 심층** — 사용자(60)·IP(120)·디바이스(90) 세 키로 다음 학대 패턴 모두 방어 ① 한 사용자가 다중 디바이스로 폭주(user 한도가 합산) ② 같은 IP 공유 사용자들 폭주(IP가 묶음) ③ **단일 사용자의 단일 디바이스 폭주**(device가 그 디바이스만 제한 — 다른 디바이스는 영향 0). 새 차원 추가가 자명한 API(`Subject` 추가)라 4차원·5차원 확장도 즉시 가능(예: `tenant`·`route`). **🎯 rate limit 10 슬라이스 누적 — N차원 production-grade**. **결정 v1 한계**(후속): ① **X-Device-Id 위변조 가능** — 클라이언트가 임의 값 보내면 device 차원 우회 가능. 서명·서버측 매핑 후속(예: 첫 인증 시 device_secret 발급·X-Device-Sig HMAC ✅ 아래) ② **`_client_device_id` 정규화 없음** — 같은 디바이스가 다른 표기로 보내면 별 키(UUID 정규화·case-fold 후속) ③ **새 kind 추가 시 `SubjectKind` Literal 수동 확장** — `device`만 하드코딩 노출(`_pair_headers`/`_enforce_triple` 알고리즘은 일반) ④ **Lua 5-튜플 → N-튜플 응답** Redis-py가 list로 매핑하나 향후 dict-like(Redis 7+ map) 검토. **후속**: 디바이스 서명(HMAC·✅ 아래)·IPv6 정규화·CIDR 묶음·X-Real-IP·프록시 화이트리스트·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선·다중 tenant 차원·Retry-After 정책 표준화.
- ✅ **L4 교수학 엔진 — 슬라이스 63 (PgDeviceStore tie-break parity — seq 등록순)** 완료 (메인 직접·라이브 PG16 전수 검증·1458 passed, 2026-06-04) — **slice 54가 명시적으로 남긴 후속 해소**: InMemoryDeviceStore는 `seq`(단조 순번)로 동률(같은 created_at/last_used_at) 정렬을 결정론화했으나 PgDeviceStore.list_for_user는 *단일 컬럼 ORDER BY*라 동률 순서 불안정(2차 키 없음·SQL stable 미보장)이던 InMemory/Pg 비대칭 제거. **구현**: ① `device_credential`에 `seq` 컬럼 — ORM `mapped_column(sa.BigInteger, sa.Identity())`(비-PK 자동증가) + alembic `c9d0e1f2a3b4`(`ADD COLUMN ... GENERATED BY DEFAULT AS IDENTITY`·기존 행은 물리순 백필·신규 register는 DB 자동증가). **SELECT 미노출**(ORDER BY 전용·DeviceInfo·HTTP 표면 불변·register 미지정→DB 할당). ② `list_for_user` ORDER BY에 seq 2차 정렬키 — **비-null 그룹은 정렬 방향대로**(desc=나중 등록[큰 seq] 먼저·asc=먼저 먼저), **null 그룹은 *항상* 등록순(seq ASC)**(InMemory가 dict 삽입순 보존 — slice 47/54 동작). SQL은 **두 CASE term**으로 분리: `case((order_col.isnot(None), seq)).<dir>()` + `case((order_col.is_(None), seq)).asc()` — 각 term이 자기 그룹만 정렬(상대 그룹엔 NULL→무영향), primary가 이미 그룹 분리라 동일 primary 값 내에서만 작동(그룹 경계 침범 0). **mypy 함정**: `Case` 변수를 `.desc()`(→UnaryExpression)로 재할당하면 [assignment] 에러 → 별도 변수(`nonnull_case`→`nonnull_tie`)로 분리. **라이브 PG16 전수 검증**: ① `seq=generated by default as identity` ② `alembic check` 무드리프트(ORM Identity↔DB 정합) ③ 동률 SQL(같은 created_at 3개→desc seq DESC=C,B,A) ④ **통합테스트**(`test_pg_list_for_user_seq_tiebreak_parity_on_live_pg`·실 PgDeviceStore.list_for_user: created_at 강제 동률→desc=[d3,d2,d1]·asc=[d1,d2,d3]·last_used_at 전부 NULL→null그룹=[d1,d2,d3] 등록순) ⑤ downgrade/재upgrade 왕복. **4게이트 green**: black(101)·ruff·mypy-strict(88)·pytest **1458 passed**(+1 통합 skip=43)·`_device_store.py` cov 100%. **결정 v1 한계/후속**: ① 기존 행 seq는 물리순(임의)·신규만 등록순 결정론(InMemory도 세션 한정이라 동형 트레이드오프) ② (user_id, created_at, seq) 복합 인덱스 미추가(정렬 최적화는 별도·현재 user_id 인덱스만) ③ populated 테이블 IDENTITY ADD는 테이블 재작성(소규모라 수용). **후속**: audit 보존정책(TTL)/암호화·device_credential 복합 정렬 인덱스·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 64 (device_credential 복합 정렬 인덱스 — `(user_id, created_at DESC, seq DESC)`)** 완료 (메인 직접·4게이트 green·1458 passed, 2026-06-04) — **slice 63 한계 ② 해소**: `list_for_user`의 *기본* 접근 패턴(`WHERE user_id ORDER BY created_at DESC, seq DESC` — slice 63 tie-break 포함)을 인덱스만으로 충족(Sort 노드 제거). slice 60/61(deletion_audit·assessment의 `(user_id, <time> DESC)` 인덱스)과 동형이나, device_credential은 *이미 단일 user 인덱스가 있어 추가가 아닌 대체*다. **구현**: ① ORM `device.py` — user_id의 `index=True` 제거 + `__table_args__ = (sa.Index("idx_device_credential_user", "user_id", sa.desc("created_at"), sa.desc("seq")),)`. ② alembic `d0e1f2a3b4c5`(head·revises c9d0e1f2a3b4): `drop_index(ix_device_credential_user_id)` + `create_index(idx_device_credential_user, [user_id, literal_column("created_at DESC"), literal_column("seq DESC")])`. downgrade는 역순(복합 drop → 단일 재생성). **중복 제거 근거**: 복합의 선두 컬럼 `user_id`가 단일 인덱스를 *포섭* — `count_for_user`·revoke의 user_id 필터·FK 조회 모두 prefix로 충족. 단일 제거로 register INSERT 시 유지할 인덱스 1개 절감(쓰기 amp 감소). device_credential은 소규모(slice 63)라 인덱스 교체 비용 수용. **방향 매칭**: 기본 정렬은 created_at NOT NULL이라 non-null 그룹만 존재(null_tie CASE 무효) → 유효 ORDER BY=`created_at DESC, seq DESC` = 인덱스 정의와 정확 일치. asc 정렬은 동일 인덱스 backward scan(모든 컬럼 일관 반전)으로 충족. **slice 61 패턴 답습**: ORM `sa.desc(...)` ↔ migration `literal_column("... DESC")` 정합(slice 61이 `alembic check` 무드리프트로 입증한 2컬럼 패턴을 3컬럼으로 확장). **테스트** +1 통합(`test_device_credential_user_index_swapped_on_live_pg`·기본 skip): pg_indexes로 `idx_device_credential_user` 존재 + `ix_device_credential_user_id` 부재 + 복합 정의에 `user_id, created_at DESC, seq DESC` 반영 검증(slice 60/61은 committed 테스트 0이었으나 본 슬라이스는 regression 커버 추가). **4게이트 green**(py3.12 venv): black(102)·ruff·mypy-strict(88·alembic은 패키지 밖이라 카운트 불변)·pytest **1458 passed**(불변·인덱스는 투명)·skip 44(43→+1)·cov 99.88%. **hermetic 검증**: `DeviceCredential.__table__.indexes` = 복합 1개만(단일 자동생성 인덱스 제거 확인·ORM↔migration drift 없음 proxy). **라이브 PG16 미수행**(이 환경 PG 부재): `alembic check` 무드리프트·`EXPLAIN` Index Scan(Sort 제거)은 통합테스트와 함께 운영 PG에서 검증 필요(slice 60-63과 동일 게이트). **결정 v1 한계/후속**: ① **기본 정렬만 최적화** — `order_by=last_used_at`/`revoked_at` 정렬은 이 인덱스 미적용(각각 별 인덱스 필요·기본 정렬이 지배적 패턴이라 후속) ② **시간창 필터(since/until 등) 미커버** — WHERE 시간 범위 + ORDER BY 조합은 부분 인덱스/추가 컬럼 후속 ③ **라이브 PG EXPLAIN 미검증**(위). **후속**: audit 보존정책(TTL)/암호화·last_used_at/revoked_at 정렬 인덱스·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 65 (`GET /v1/me/deletions` resource_type 도메인 필터)** 완료 (메인 직접·4게이트 green·1460 passed, 2026-06-04) — GDPR 삭제 감사 조회에 *선택적* 도메인 필터 추가 — 학생이 한 유형(예: 대화)의 삭제 이력만 조회 가능. device 목록이 slice 41~47로 시간창 필터를 확장한 것과 동형 parity(audit 도메인). **구현**: `me.py` ① 신규 타입 별칭 `ResourceTypeFilter = Annotated[AuditResourceType | None, Query(...)]`(enum이라 부정 값은 FastAPI가 422 거부·임의 문자열 주입 차단). ② `list_my_deletions`에 `resource_type: ResourceTypeFilter = None` 추가 — None이면 전체(slice 58 동작 보존), 지정 시 `.where(DeletionAudit.resource_type == resource_type.value)` 추가. 기존 `idx_deletion_audit_user(user_id, deleted_at DESC)`가 user_id prefix + 정렬 그대로 충족(resource_type은 추가 필터). **마이그레이션 불필요**(순수 쿼리 파라미터·DB 스키마 불변). **테스트** 2층: ① **hermetic +2**(`test_deletions_resource_type_filter_accepted`·세 enum 값 모두 200 결선·`test_deletions_invalid_resource_type_rejected`·`bogus`→422). FakeSession은 stmt 무시라 *실제 필터링*은 통합테스트가 검증(test_me.py 모듈 docstring 원칙). ② **통합 +1**(`test_me_deletions_resource_type_filter_on_live_pg`·기본 skip): A의 삭제 감사 도메인별 적재(learning_session×2·dialogue×1)→필터 생략 3건·learning_session 2건(모두 해당 도메인)·dialogue 1건·assessment 0건([]). `_cleanup`에 `deletion_audit` 정리 추가(user_id FK 아님·독립 로그). **4게이트 green**(py3.12): black(102)·ruff·mypy-strict(88)·pytest **1460 passed**(1458→+2 hermetic)·skip 45(44→+1 통합)·`me.py` cov **100%**·전체 99.88%. **결정 v1 한계/후속**: ① **단일 도메인만** — `resource_type` 단일 값(다중 OR 필터는 미지원·필요 시 list 파라미터 후속) ② **시간창 필터 없음** — deleted_at since/until은 미추가(device 목록 parity 잔여·후속) ③ FakeSession 한계로 필터 정확성은 라이브 PG 통합테스트 의존(이 환경 PG 부재라 미실행). **후속**: deletions deleted_at 시간창 필터·다중 resource_type·audit 보존정책(TTL)/암호화·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 66 (`GET /v1/me/deletions` deleted_at 시간창 필터 + 시간창 검증 헬퍼 공용화)** 완료 (메인 직접·4게이트 green·1463 passed, 2026-06-04) — slice 65 후속(deletions deleted_at 시간창) 해소 + device 목록과의 검증 로직 중복 제거. **구현**: ① **공용 모듈 `api/_query_filters.py` 추출**(slice 55 헬퍼 추출 선례) — `_validate_tz_aware`(naive datetime 422·slice 42)·`_validate_time_window`(since>until 422·slice 45)를 `devices.py`에서 옮겨 `devices.py`·`me.py` 양쪽이 *동일 에러 표면* 공유(메시지·status 분기 방지). devices.py는 def 2개 삭제→import로 교체(9개 호출처 무변경·동작 불변). docstring만 일반화(created_at→임의 시각 컬럼). ② `me.py` `list_my_deletions`에 `since`/`until`(`DeletedSince`/`DeletedUntil` 타입 별칭·TZ-aware ISO8601) 추가 — `deleted_at >= since`/`<= until`(inclusive). 핸들러 진입 시 tz-aware·window 검증 후 조건부 WHERE. 모두 생략 시 전체(slice 58 보존). 기존 `idx_deletion_audit_user(user_id, deleted_at DESC)`가 user_id prefix + 시간 범위 + 정렬 그대로 충족. **마이그레이션 불필요**(순수 쿼리 파라미터). **테스트** 2층: ① **hermetic +3**(`test_deletions_time_window_accepted`·TZ-aware since/until 200·`test_deletions_naive_datetime_rejected`·naive 422·`test_deletions_inverted_window_rejected`·since>until 422). 검증은 핸들러 코드라 FakeSession 무관하게 동작. ② **통합 +1**(`test_me_deletions_time_window_filter_on_live_pg`·기본 skip): 1·6·12월 적재→since=6월 2건·until=6월 2건·양끝=6월 1건(inclusive 경계)·생략 3건. **시드 견고화**: `_deletion_row`가 `deleted_at`을 항상 명시 timestamp(미지정 시 now)로 채움 — `from_schema`가 deleted_at=None을 명시 전달해 NOT NULL 위반될 위험 제거(운영 경로는 deleted_at 생략→server_default now()라 무관·테스트 시드만 해당). **4게이트 green**(py3.12): black(103·+1 신규 모듈)·ruff·mypy-strict(89·+1)·pytest **1463 passed**(1460→+3 hermetic)·skip 46(45→+1 통합)·신규/touched 파일 cov **100%**(`_query_filters.py`·`devices.py`·`me.py`)·전체 99.88%. **결정 v1 한계/후속**: ① **다른 /me 리스트(sessions·assessments·dialogues)엔 시간창 미적용** — deletions만(slice 65/66 범위)·동형 확장은 후속 ② **다중 resource_type OR 필터 미지원**(slice 65 한계 유지) ③ FakeSession 한계로 시간 필터 정확성은 라이브 PG 통합테스트 의존(이 환경 PG 부재라 미실행). **후속**: /me 리스트 전반 시간창 parity·다중 resource_type·audit 보존정책(TTL)/암호화·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 67 (/me 리스트 전반 시간창 parity + `apply_time_window` 헬퍼)** 완료 (메인 직접·4게이트 green·1466 passed, 2026-06-04) — slice 66 후속(다른 /me 리스트 시간창 parity) 해소. sessions·assessments·dialogues 세 리스트에도 deletions(slice 66)와 동일한 since/until 시간창 필터 추가 → 네 /me 리스트 *대칭 API*. **구현**: ① **`_query_filters.apply_time_window(stmt, column, since, until)` 헬퍼 신규** — `_validate_tz_aware`(naive 422)+`_validate_time_window`(since>until 422) 적용 후 not-None 경계만 `>=`/`<=` WHERE 체이닝·입력 stmt 반환. mypy strict: `_SelectT = TypeVar(bound="Select[Any]")`로 stmt 구체 타입 보존(`.where()`가 Self 반환)·`column: InstrumentedAttribute[Any]`(deleted_at non-null·started_at nullable 둘 다 수용). ② **deletions 리팩터** — slice 66의 인라인 검증+WHERE 3블록을 `apply_time_window` 1줄로 축소(동작 불변). ③ **3 리스트 엔드포인트** — `since`/`until` 파라미터(공용 `SinceParam`/`UntilParam` 별칭·slice 66 `DeletedSince/Until` 일반화) + `apply_time_window(stmt, Model.started_at, ...)`. **마이그레이션 불필요**(순수 쿼리 파라미터). **테스트** 2층: ① **hermetic +3**(`test_list_time_window_{accepted,naive_rejected,inverted_rejected}_all_endpoints`·세 리스트 루프: TZ-aware 200·naive 422·since>until 422). ② **통합 +1**(`test_me_sessions_time_window_filter_on_live_pg`·기본 skip): 1·6·12월 세션 적재→since/until/양끝 경계 검증. `_session_row`에 started_at 파라미터 추가. **4게이트 green**(py3.12): black(103)·ruff·mypy-strict(89)·pytest **1466 passed**(1463→+3 hermetic)·skip 47(46→+1 통합)·`_query_filters.py`·`devices.py`·`me.py` cov **100%**·전체 99.88%. **결정 v1 한계/후속**: ① **started_at 정렬 인덱스는 sessions·assessments만**(slice 61·dialogue는 idx_dialogue_user 보유)·시간창 + 정렬 동시 충족(추가 인덱스 불요) ② **resource_type 다중 OR·다른 컬럼(ended_at 등) 시간창 미지원** ③ 시간 필터 정확성은 라이브 PG 통합 의존(이 환경 PG 부재 미실행). **🎯 의미**: device 목록(slice 41~47)·/me 리스트 4종이 *동일 시간창 의미·에러 표면*(apply_time_window 단일 출처) 공유 — API 일관성 완비. **후속**: 다중 resource_type·ended_at/completed_at 시간창·audit 보존정책(TTL)/암호화·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 68 (`GET /v1/me/deletions` 다중 resource_type OR 필터)** 완료 (메인 직접·4게이트 green·1468 passed, 2026-06-04) — slice 67 후속(다중 resource_type) 해소. `resource_type` 반복 지정 시 OR(IN)으로 여러 도메인 동시 조회 — `?resource_type=dialogue&resource_type=assessment`. **구현**: `me.py` ① `ResourceTypeFilter` 별칭 `AuditResourceType | None` → `list[AuditResourceType] | None`(반복 쿼리 파라미터·FastAPI가 단일 값도 `[val]`로 파싱 → **하위 호환**). ② 핸들러 `if resource_type:`(non-None·non-empty) 시 `.where(resource_type.in_([rt.value for rt in resource_type]))`. 단일 값 `==` → `IN` 일반화(의미 동등). **마이그레이션 불필요**(순수 쿼리). **enum 검증 유지**: 다중 값 중 하나라도 enum 밖이면 FastAPI가 422(부분 주입 차단). **테스트** 2층: ① **hermetic +2**(`test_deletions_multiple_resource_types_accepted`·반복 지정 200·`test_deletions_multiple_resource_types_one_invalid_rejected`·일부 bogus 422). slice 65 단일 값 테스트(`..._filter_accepted`·`..._invalid_..._rejected`)는 그대로 통과(하위 호환 증명). ② **통합 확장**(기존 `test_me_deletions_resource_type_filter_on_live_pg`에 OR 케이스 추가): learning_session+dialogue 반복 → 3건(2+1)·두 도메인 모두 포함. **4게이트 green**(py3.12): black(103)·ruff·mypy-strict(89)·pytest **1468 passed**(1466→+2 hermetic·통합은 기존 테스트 내 확장이라 skip 47 불변)·`me.py` cov **100%**·전체 99.88%. **결정 v1 한계/후속**: ① **OR만**(AND는 단일 행이 한 resource_type이라 무의미·해당 없음) ② 다른 /me 리스트엔 다중 enum 필터 대상 컬럼 없음(deletions 특화) ③ 통합 정확성 라이브 PG 의존(미실행). **후속**: ended_at/completed_at 시간창·audit 보존정책(TTL)/암호화·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 69 (/me 리스트 lifecycle 종료 시각 시간창 — ended_at·completed_at)** 완료 (메인 직접·4게이트 green·1471 passed, 2026-06-04) — slice 68 후속(ended_at/completed_at 시간창) 해소. sessions·dialogues는 `ended_since`/`ended_until`(ended_at), assessments는 `completed_since`/`completed_until`(completed_at) 시간창 추가 → started_at(slice 67)과 *독립* 차원. **구현**: `me.py` ① 신규 별칭 `CloseSince`/`CloseUntil`(종료/완료 시각 시간창·파라미터명으로 컬럼 구분). ② 세 리스트에 `apply_time_window(stmt, <close_col>, ...)` 추가 호출 — slice 67 헬퍼 재사용(`since_name`/`until_name`으로 엔드포인트별 422 메시지). started_at 창과 둘 다 지정 시 AND(예: 1월 시작 & 3월 종료). **미종료(NULL) 자동 제외** — SQL NULL 비교(`ended_at >= X`가 NULL이면 False)라 종료 시간창 지정 시 미종료 행 빠짐(의미 정합: "X~Y에 종료된 것"). **마이그레이션 불필요**(순수 쿼리). **테스트** 2층: ① **hermetic +3**(`test_close_time_window_{accepted,naive_rejected,inverted_rejected}_all_endpoints`·`_CLOSE_WINDOW_ENDPOINTS` 튜플로 엔드포인트별 파라미터명 루프: TZ-aware 200·naive 422·inverted 422). ② **통합 +1**(`test_me_sessions_ended_at_window_filter_on_live_pg`·기본 skip): 1월 종료·6월 종료·미종료(NULL) 3 세션→ended_since=6월 1건(NULL·1월 제외)·ended_until=3월 1건·생략 3건. `_session_row`에 ended_at 파라미터 추가. **4게이트 green**(py3.12): black(103)·ruff·mypy-strict(89)·pytest **1471 passed**(1468→+3 hermetic)·skip 48(47→+1 통합)·`me.py` cov **100%**·전체 99.88%. **🎯 의미**: /me 리스트가 *2차원 시간 필터*(생성 started_at + 종료 ended_at/completed_at) 완비 — "1월에 시작해 3월에 끝낸 세션" 같은 복합 조회 가능. apply_time_window가 같은 컬럼군에 N회 적용되는 합성성 입증. **결정 v1 한계/후속**: ① **deletions엔 종료 개념 없음**(삭제는 단일 시점 deleted_at·slice 66) ② NULL 처리는 *제외*만(미종료를 포함하는 "OR NULL" 모드 미지원·필요 시 별 파라미터) ③ 통합 정확성 라이브 PG 의존(미실행). **후속**: audit 보존정책(TTL)/암호화·정렬 방향(order) 파라미터·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 70 (/me 리스트 4종 정렬 방향 `order=asc/desc` 파라미터)** 완료 (메인 직접·4게이트 green·1473 passed, 2026-06-04) — slice 69 후속(정렬 방향 파라미터) 해소. sessions·assessments·dialogues·deletions 네 리스트에 `order`(asc/desc) 추가 — 기본 `desc`(최신순·slice 58 동작 보존). device 목록(slice 46)과 동형. **구현**: `me.py` ① `OrderDir = Literal["asc","desc"]` + `OrderParam` 별칭(Literal 밖 값은 FastAPI가 422). ② 네 엔드포인트에 `order: OrderParam = "desc"` 추가 + 1차 정렬 컬럼(started_at/deleted_at)에 `col.asc() if order=="asc" else col.desc()` 적용(devices.py slice 46 인라인 패턴 답습). PK 2차키는 안정 정렬용 유지(고유값이라 방향 무영향). **마이그레이션 불필요**(순수 쿼리). **테스트** 2층: ① **hermetic +2**(`test_order_param_accepted_all_endpoints`·4 엔드포인트×asc/desc/생략 200·`test_order_param_invalid_rejected_all_endpoints`·`sideways`→422). ② **통합 +1**(`test_me_sessions_order_param_on_live_pg`·기본 skip): 1·6·12월 세션(고정 id)→생략=desc=[12,6,1]월·asc=[1,6,12]월 순서. **4게이트 green**(py3.12): black(103)·ruff·mypy-strict(89)·pytest **1473 passed**(1471→+2 hermetic)·skip 49(48→+1 통합)·`me.py` cov **100%**·전체 99.88%. **🎯 의미**: /me 리스트 조회 API *완성형* — 페이지네이션(limit/offset)·도메인 필터(다중)·2차원 시간창(started/ended)·정렬 방향 완비. device 목록과 기능 parity. **A 영역(/me 조회) 마무리 단계**(잔여: 총 개수). **결정 v1 한계/후속**: ① **정렬 컬럼은 1차 시각 고정**(임의 컬럼 정렬 미지원·device order_by 필드 선택과 차이·필요 시 후속) ② offset 페이지네이션 한계(cursor 후속) ③ 통합 정확성 라이브 PG 의존(미실행). **후속**: /me 총 개수(X-Total-Count)·audit 보존정책(TTL)/암호화·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 71 (/me 리스트 4종 총 개수 `?include_total` → `X-Total-Count` 헤더 + `apply_time_window`→`time_window_conditions` 진화)** 완료 (메인 직접·4게이트 green·1475 passed, 2026-06-04) — slice 70 후속(/me 총 개수) 해소 → **A 영역(/me 조회 API) 완성**. **설계 결정 — 헤더 vs envelope**: device 목록(slice 39)은 응답 envelope의 `total` 필드를 쓰나 /me는 *bare array*(`list[Schema]`) 응답이라 envelope 전환은 **breaking**. 따라서 *비파괴적* `X-Total-Count` 헤더 + `?include_total=true` opt-in 채택(REST 관용·기존 클라이언트 무영향·헤더는 reversible). **구현**: ① **`_query_filters` 진화** — `apply_time_window`(Select 변형) → `time_window_conditions(column, since, until) -> list[ColumnElement[bool]]`(조건 리스트 반환). 동기: total count가 *같은 필터*를 list stmt·count stmt 둘 다에 적용해야 해서, 조건을 *한 번 검증*해 두 stmt에 재사용(이중 검증 회피). slice 67 헬퍼 폐기·me.py 전 호출처 전환. ② `me.py` 네 엔드포인트: `conds = [user_id==…, *time_window_conditions(...), …]` 빌드 → `select(Model).where(*conds)` list + `select(func.count()).select_from(Model).where(*conds)` count. ③ `include_total: IncludeTotal = False` + `response: Response` 파라미터(특수 파라미터라 비-default·세션 다음 위치) + `_maybe_set_total` 헬퍼(include_total 시 count 실행→`X-Total-Count` 헤더). **total은 limit/offset 무시·필터 적용 전체 건수**("총 N건"·"Page X of Y"). **마이그레이션 불필요**. **테스트** 2층: ① **hermetic +2**(`test_include_total_sets_header_all_endpoints`·4 엔드포인트 헤더 "0"·`test_no_total_header_by_default_all_endpoints`·생략 시 헤더 없음). FakeSession에 `_Result.scalar()`=행 수 추가(count 시뮬·필터 정확성은 통합 검증). ② **통합 확장**(deletions 테스트에 추가): include_total→"3"·resource_type=learning_session&limit=1→페이지 1건이나 헤더 "2"(total≠페이지 크기)·생략 시 헤더 없음. **4게이트 green**(py3.12): black(103)·ruff·mypy-strict(89)·pytest **1475 passed**(1473→+2 hermetic)·skip 49(통합은 기존 테스트 내 확장)·`me.py`·`_query_filters.py` cov **100%**·전체 99.88%. **🎯 의미**: **A 영역(/me 조회) 완성** — 4 리스트가 페이지네이션·도메인 필터(다중)·2차원 시간창·정렬 방향·총 개수 완비. `time_window_conditions`로 진화하며 list/count 필터 *단일 출처*. **결정 v1 한계/후속**: ① **헤더 방식 divergence** — device는 envelope·/me는 헤더(비파괴 우선·일관성 희생). 추후 /me v2에서 envelope 통일 가능 ② COUNT는 매 include_total 요청마다 별 쿼리(캐시 없음·device count 캐시와 차이·후속) ③ 통합 count 정확성 라이브 PG 의존(미실행). **후속**: audit 보존정책(TTL)/암호화·라이브 LLM 좌석·count 캐시.
- ✅ **L4 교수학 엔진 — 슬라이스 72 (디바이스 secret 봉투 암호화 프리미티브 — AES-256-GCM `_crypto.py`)** 완료 (메인 직접·4게이트 green·1485 passed, 2026-06-04) — **B 트랙(디바이스 자격증명 보안) 착수**. KMS envelope(반복 최다 후속·34회)의 *1단계*: 암호 프리미티브 격리·검증(보안 코드는 프리미티브 먼저 분리해 철저히 검증·결선은 후속). **위협 모델**: PgDeviceStore가 `secret_plain` 평문 저장(verify가 HMAC 키로 원본 필요·KDF 불능) → DB dump·DBA 접근 시 전체 secret 노출. 봉투 암호화는 *마스터 키*(DB 밖·env/Settings)로 AES-GCM 암호화 저장 → DB dump 단독 복호 불가. **구현**: ① **`api/_crypto.py` 신규** — `SecretCipher`(AES-256-GCM·`encrypt(plaintext)->(ciphertext, nonce)`·매 암호화 96-bit nonce 새로 생성·`decrypt`는 변조 시 `InvalidTag`)·`build_secret_cipher(settings)->SecretCipher|None`(키 미설정 시 None=암호화 비활성 폴백). `cryptography` lib(`python-jose[cryptography]` 경유 이미 설치). ② **Settings `device_secret_encryption_key: SecretStr`**(base64 32바이트=AES-256·빈 값=비활성·`WHYMATH_DEVICE_SECRET_ENCRYPTION_KEY` env·SecretStr로 repr/로그 차단). AEAD 선택 근거: 기밀성+무결성(비트플립 차단)·nonce별 인증 태그. **이 슬라이스는 프리미티브만** — PgDeviceStore 결선(컬럼·register 암호화·verify 복호화-or-폴백·alembic)은 **슬라이스 73**. **테스트 +10 hermetic**(`test_crypto.py`): round-trip(ascii·비-ascii utf-8)·nonce 유일성(같은 평문 다른 ciphertext)·변조 탐지(InvalidTag)·잘못된 키/nonce 실패·키 길이≠32 ValueError·factory None(키 미설정)/cipher(설정)/잘못된 길이 ValueError. **4게이트 green**(py3.12): black(104)·ruff·mypy-strict(90·+1 신규 모듈)·pytest **1485 passed**(1475→+10)·`_crypto.py`·`config.py` cov **100%**·전체 99.88%. **🎯 의미**: at-rest 암호화 기반 마련 — 마스터 키가 DB 밖이라 DB dump만으로 복호 불가(핵심 위협 차단)의 *프리미티브* 완비. CLAUDE.md 우선순위 #2(보안). **결정 v1 한계/후속**: ① **미결선**(slice 73이 PgDeviceStore에 연결·이 슬라이스만으로는 동작 변화 0) ② **단일 마스터 키**(per-secret DEK·키 회전·HSM은 진짜 KMS 후속) ③ **키 회전 시 재암호화 경로 없음**(후속). **후속**: slice 73 PgDeviceStore 봉투 결선(secret_encrypted/nonce 컬럼·dual-read 폴백)·키 회전·nonce 재생 방어·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 73 (PgDeviceStore 봉투 암호화 결선 — register 암호화·verify 복호·dual-read 폴백)** 완료 (메인 직접·4게이트 green·1491 passed, 2026-06-05) — slice 72 프리미티브를 PgDeviceStore에 실제 결선 → **B 트랙 핵심 보안 달성**: device secret이 DB에 *암호화*되어 dump 단독 복호 불가. **구현**: ① **`_crypto.py` 저장 헬퍼 2종**(순수·hermetic): `encrypt_secret_for_storage(cipher, secret_plain) -> (plain|None, enc|None, nonce|None)`(cipher 있으면 암호화·평문 컬럼 None·없으면 평문 폴백)·`resolve_stored_secret(cipher, plain, enc, nonce) -> str`(암호화 행 복호·평문 행 그대로·**암호화인데 cipher 없으면 RuntimeError**[조용한 401 lockout 대신 시끄러운 500]·둘 다 없으면 무결성 오류). ② **device.py 모델**: `secret_encrypted`·`secret_nonce`(LargeBinary nullable) 추가·`secret_plain` → nullable 완화(암호화 행은 NULL). ③ **alembic `e1f2a3b4c5d6`**(head): 2 컬럼 add + secret_plain NOT NULL 완화(downgrade는 평문만 상태에서 안전). ④ **PgDeviceStore**: `__init__(sessionmaker, cipher=None)`·register는 `encrypt_secret_for_storage`로 저장 표현 결정(응답 secret은 생성 평문 토큰·저장과 무관)·verify는 `resolve_stored_secret`로 HMAC용 평문 복원. ⑤ **build_device_store_from_settings**: `build_secret_cipher(settings)` → PgDeviceStore에 주입(잘못된 키 길이 startup fail-fast). InMemoryDeviceStore는 무변경(메모리 dump 위협엔 in-memory 키 암호화 무의미). **하위 호환**: 기존 평문 행·cipher 미설정은 그대로 verify(dual-read). CachedDeviceStore 무변경(inner 위임). **테스트** 2층: ① **hermetic +6**(`TestStorageHelpers`): encrypt with/without cipher·resolve 암호화 round-trip·평문 폴백·암호화+cipher없음 RuntimeError·secret 없음 RuntimeError. ② **통합 +1**(`test_pg_device_store_envelope_encryption_round_trip_on_live_pg`·skip): cipher 주입 register→DB 행 secret_plain NULL·secret_encrypted/nonce 채워짐·ciphertext에 평문 미포함·verify True·평문 행 하위 호환 verify. **4게이트 green**(py3.12): black(105)·ruff·mypy-strict(90)·pytest **1491 passed**(1485→+6 hermetic)·skip 50(49→+1 통합)·`_crypto.py`·`_device_store.py` cov **100%**·전체 99.88%. **🎯 의미**: **KMS envelope 핵심 완성** — `WHYMATH_DEVICE_SECRET_ENCRYPTION_KEY` 설정 시 신규 device secret은 AES-256-GCM 암호화 저장, DB dump 단독 복호 불가(마스터 키 DB 밖). 키 미설정이면 기존 평문 동작(점진 도입). CLAUDE.md 우선순위 #2(보안) 실현. **결정 v1 한계/후속**: ① **기존 평문 행 백필 없음**(신규만 암호화·기존은 평문 잔존·재암호화 배치 후속) ② **단일 마스터 키·키 회전 경로 없음**(회전 시 전 행 재암호화 필요·후속) ③ **라이브 PG 미검증**(통합테스트 skip·운영 PG 필요) ④ secret_plain 평문 컬럼 잔존(전면 암호화 후 drop은 후속). **후속**: 기존 행 재암호화 배치·키 회전·secret_plain 컬럼 제거·nonce 재생 방어·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 74 (기존 평문 secret 봉투 암호화 백필 — `PgDeviceStore.reencrypt_plaintext_secrets`)** 완료 (메인 직접·4게이트 green·1497 passed, 2026-06-05) — slice 73 한계 ①(기존 평문 행 백필) 해소. slice 73은 *신규* register만 암호화 → 기존 평문 행 잔존. 본 메서드가 마스터 키 도입 후 기존 행을 점진 전환. **구현**: `PgDeviceStore.reencrypt_plaintext_secrets(*, batch_size=100) -> int`(재암호화 행 수) — `secret_encrypted IS NULL`(평문 행) 중 secret_plain 있는 것을 batch_size개까지 암호화해 `secret_encrypted`/`secret_nonce` 채우고 `secret_plain=NULL`. 이미 암호화 행 자동 제외(idempotent)·cipher 미설정 0(no-op)·secret 둘 다 없는 이상 행 건너뜀(무결성 방어). 호출자가 0까지 반복(대형 테이블 메모리/락 보호). `cleanup_stale`와 동형 *운영 일회성/관리자 경로*(Protocol 미추가·구체 PgDeviceStore로 호출·요청 경로 아님·InMemory 미구현). **테스트 인프라 개선**: `_FakePgSession._row_matches`가 `col.is_(None)`(우변 Null() 노드·`.value` 없음)을 미처리해 IS NULL 필터가 0 매치 → value 정규화(Null 노드→None) 추가. 실 PG 쿼리(`IS NULL`)는 정상이었고 *fake만* 보강. **테스트** 2층: ① **hermetic +6**(`TestPgDeviceStoreReencrypt`): no_cipher no-op·평문 2행 백필(암호화·verify 유지)·idempotent 2차 0·이미 암호화 행 0·batch_size 분할(2+1+0)·둘 다 NULL 이상 행 skip. ② **통합 +1**(`test_pg_device_store_reencrypt_backfill_on_live_pg`·skip): 실 PG 평문 2행→백필 count 2·DB secret_plain NULL·secret_encrypted 존재·verify 유지·idempotent 0. **4게이트 green**(py3.12): black(105)·ruff·mypy-strict(90)·pytest **1497 passed**(1491→+6 hermetic)·skip 51(50→+1 통합)·`_device_store.py` cov **100%**·전체 99.88%. **🎯 의미**: 봉투 암호화 *전체 전환* 경로 완비 — 신규(slice 73)+기존(slice 74) 모두 암호화 가능. 운영: 키 설정 후 `reencrypt_plaintext_secrets` 반복 호출 → 전 행 암호화 → (후속) secret_plain 컬럼 drop. **결정 v1 한계/후속**: ① **트리거 엔드포인트/CLI 없음**(cleanup_stale와 동일·`/v1/admin/devices/*` 후속) ② **키 회전 미지원**(회전 시 복호 후 재암호화 필요·현재는 평문→암호화 단방향) ③ secret_plain 컬럼 잔존(전 행 암호화 후 drop 후속) ④ 라이브 PG 미검증(skip). **후속**: 관리자 백필/cleanup 엔드포인트·키 회전·secret_plain 컬럼 제거·nonce 재생 방어·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 75 (디바이스 secret 키 회전 — `MultiKeyCipher` 다중 복호 키)** 완료 (메인 직접·4게이트 green·1504 passed, 2026-06-05) — B 잔여(키 회전) 해소. 키 회전 *무중단* 핵심: 새 primary 키 도입 시 구 키로 암호화된 행이 *lockout 없이* 복호되도록 fallback 복호 키 지원. **구현**: ① **`_crypto.py` `SupportsEnvelope` Protocol**(encrypt/decrypt 구조적 타입) + **`MultiKeyCipher`**(primary로 암호화·primary→fallbacks 순 복호·전부 실패 시 InvalidTag). SecretCipher(단일)·MultiKeyCipher(회전)가 같은 Protocol 충족 → PgDeviceStore·저장 헬퍼는 구현 교체 가능(타입 `SupportsEnvelope | None`로 일반화·기존 SecretCipher 호출처 무변경). ② **`build_secret_cipher` → MultiKeyCipher | None**: primary(`device_secret_encryption_key`) + fallbacks(`device_secret_decryption_fallback_keys`·쉼표 구분 base64·복호 전용·공백/빈 토큰 무시). ③ **Settings `device_secret_decryption_fallback_keys: SecretStr`**. **회전 절차**: 새 키 생성→primary 승격·구 키를 fallback으로→재시작(신규는 새 키 암호화·구 행은 fallback 복호)→(slice 74 reencrypt로) 전 행 새 키 재암호화→fallback 제거. encrypt는 *항상 primary*라 재암호화가 현재 키로 수렴. **마이그레이션 불필요**(스키마 불변·키만 추가). **테스트** **+7 hermetic**: MultiKeyCipher round-trip·fallback 암호문 복호·encrypt는 primary만(구 키 단독 복호 불가)·전 키 실패 InvalidTag(`TestMultiKeyCipher` 4) · build fallback 회전 시나리오·파싱 공백/빈 무시(`TestBuildSecretCipher` 2) · **store 레벨 회전**(`TestPgDeviceStoreKeyRotation`·구 키 등록→새 primary+구 fallback store가 lockout 없이 verify·신규는 새 키). **4게이트 green**(py3.12): black(105)·ruff·mypy-strict(90)·pytest **1504 passed**(1497→+7)·skip 51·`_crypto.py`·`_device_store.py`·`config.py` cov **100%**·전체 99.88%. **🎯 의미**: KMS envelope에 *키 회전 무중단* 능력 추가 — 키 노출/주기적 회전 시 다운타임·lockout 없이 새 키 도입. slice 74 백필과 결합하면 *완전 회전*(구 키 행 → 새 키 재암호화 → 구 키 폐기). **결정 v1 한계/후속**: ① **key_id 미저장** — 어느 행이 어느 키인지 DB에서 모름·재암호화는 *전 암호화 행* 대상(slice 74 reencrypt는 평문만 — 구키→새키 재암호화는 별 메서드 후속) ② **fallback 시도 순차 비용** — 키 많으면 복호 평균 시도 증가(실무 1~2개라 무시) ③ 회전 자동화/스케줄 없음(수동 env 갱신+재시작). **후속**: 구키→새키 재암호화 메서드(key_id 또는 전 행)·관리자 엔드포인트·secret_plain 컬럼 제거·nonce 재생 방어·라이브 LLM 좌석.
- ✅ **L2 학습자 모델 — 슬라이스 1 (BKT 숙달 확률 추정 — `l2/bkt`)** 완료 (메인 직접·4게이트 green·1532 passed, 2026-06-05) — **H 트랙(코어 모트) 착수·L2 첫 코드**. 12슬라이스 연속 L4 인프라(device 보안·/me 조회) 후 *제품 핵심 가치*로 전환 — 90일 계획 Day31~45 L2 프로토타입. **BKT(Bayesian Knowledge Tracing, Corbett & Anderson 1994)**: 성취기준(개념)별 학생의 *숨은 숙달 확률* P(L)을 풀이 정/오답 관측으로 베이지안 갱신. 산출 P(L)이 `ConceptMasteryHistory.mastery`(0~1·특성 #16 학습곡선)로 적재되어 콘텐츠 난이도 라우팅 입력이 된다. **구현**: 신규 `whymath_backend/l2/` 패키지(`__init__`·`bkt.py`). ① **`BktParameters`**(frozen pydantic·4-파라미터 p_init/p_transit/p_slip/p_guess 전부 [0,1])·**비퇴화 제약 `p_slip+p_guess<1`** model_validator 강제(위반=정답이 믿음을 낮추는 비식별 모델). 문헌 전형 기본값(0.3/0.1/0.1/0.2). ② **순수 함수**: `posterior_mastery`(관측 후 베이지안 사후·분모0 극단은 prior 반환)·`apply_learning`(학습 전이 P(L')=post+(1-post)·T)·`update_mastery`(사후→전이 1회 갱신)·`probability_correct`(다음 정답 확률 P(L)(1-S)+(1-P(L))G). ③ **`BktModel`**(파라미터 고정·stateless 추정기·상태는 호출자/시계열 DB 보유): `initial_mastery`·`update`·`update_sequence`(관측 리스트 순차)·`predict_correct`. **검증 수치**(default params): update(0.3,정답)≈0.6927·update(0.3,오답)≈0.1458·P(✓|0.3)=0.41·연속 8정답→0.9993(1 수렴)·8오답→0.1143. **외부 의존 0·완전 결정론·DB/HTTP 없음**(첫 슬라이스 범위 — IRT·DKT·파라미터 적합[EM]·정서·오개념·시계열 적재는 후속). **테스트 +28 hermetic**(`tests/backend/l2/test_bkt.py`): 파라미터 범위/비퇴화/frozen·사후 정답↑오답↓·prior 범위·분모0·학습전이(T=0 불변·T=1 완전)·갱신 수치·정답확률 경계/단조·Model initial/update/시퀀스 수렴(상·하)/명시 prior/predict/커스텀. **4게이트 green**(py3.12): black(107)·ruff·mypy-strict(92·+2 신규 l2 파일)·pytest **1532 passed**(1504→+28)·skip 51·`l2/bkt.py`·`l2/__init__.py` cov **100%**·전체 99.89%. **🎯 의미**: 학습자 모델 *첫 추정기* — 학생의 개념별 숙달을 데이터로 추적하는 코어 모트 시작. L4 교수학 결정(graded hint·난이도)·L3 콘텐츠 라우팅이 이 P(L)을 소비. **결정 v1 한계/후속**: ① **파라미터 미적합**(전 개념 동일 기본값·실제는 개념별 EM 적합 필요·AttemptEvent 로그 입력) ② **시계열 적재·HTTP 미연결**(P(L) 갱신을 ConceptMasteryHistory에 쓰는 결선은 후속) ③ **단일 스킬 가정**(다중 개념 동시 활성/전이·forgetting[감쇠] 미모델) ④ IRT(문항 난이도+능력 동시)·DKT(신경망)는 별 슬라이스. **후속**: BKT↔ConceptMasteryHistory 시계열 결선·파라미터 적합(EM)·IRT 프로토타입·forgetting·정서 신호·오개념 매핑.
- ✅ **L2 학습자 모델 — 슬라이스 2 (BKT ↔ ConceptMasteryHistory 시계열 결선 — `l2/mastery_tracking`)** 완료 (메인 직접·4게이트 green·1545 passed, 2026-06-05) — L2 슬라이스 1 BKT *순수 추정*을 영속 학습 곡선에 결선. 풀이 관측 → (user, concept) *직전* 숙달 측정을 prior로 읽어 BKT 갱신 → 새 `concept_mastery_history` 행 append(특성 #16 학습 곡선). **설계 분리**(코드베이스 패턴): ① **`compute_mastery_record(prior_mastery, prior_sample, correct, model) -> MasteryRecord`** *순수*(DB 무관) — prior None이면 P(L0)에서 시작·mastery는 저장 정밀도(2자리) 반올림·sample_size=prior+1·confidence는 표본 기반 v1 휴리스틱 `n/(n+5)`(n=5→0.5). ② **`record_attempt_mastery(session, user_id, concept_id, correct, *, model, measured_at)`** 얇은 async DB 래퍼 — 직전 측정 SELECT(`measured_at DESC LIMIT 1`)→순수 계산→INSERT(append-only)·commit. **정밀도**: `mastery`가 `Numeric(3,2)`라 2자리 반올림으로 hermetic↔실 PG 일치(컬럼 폭 확대·고정밀 상태 분리는 후속). **FK 없음**: `concept_mastery_history`는 hypertable 느슨참조(user_profile 선적재 불요). **검증 수치**(기본 모델): 첫 정답 P(L0)0.3→0.69·표본1·conf0.17·둘째 정답 prior0.69→0.92·표본2·conf0.29. **테스트** 2층: ① **hermetic +13**(`test_mastery_tracking.py`): compute 8(첫 관측 P(L0)·정/오답·prior 사용·표본 증가·conf 단조·2자리 반올림·커스텀·연속 정답 증가) + record 5(fake session·prior 없음/있음/mastery NULL 폴백/explicit measured_at/기본 모델) — `_FakeSession`(SELECT prior·add·commit 시뮬). ② **통합 +1**(`test_record_attempt_mastery_appends_and_reads_prior_on_live_pg`·skip): 실 PG 첫 관측 0.69·둘째 0.92·DB 2행. **4게이트 green**(py3.12): black(108)·ruff·mypy-strict(93·+1 신규 모듈)·pytest **1545 passed**(1532→+13)·skip 52(+1 통합)·`l2/mastery_tracking.py`·`l2/bkt.py` cov **100%**·전체 99.89%. **🎯 의미**: BKT가 *데이터로 살아 있는 학습 곡선*이 됨 — 풀이마다 개념 숙달이 시계열에 누적되어 콘텐츠 난이도 라우팅·학습 곡선 시각화·L4 graded hint의 실데이터 입력. **결정 v1 한계/후속**: ① **AttemptEvent 자동 트리거 미연결**(현재 명시 호출·풀이 채점 파이프라인이 record_attempt_mastery 호출하는 결선은 후속) ② **2자리 정밀도 손실 누적**(Numeric(3,2)·장기 시퀀스에서 미세 편차·컬럼 확대 후속) ③ **단일 개념 가정**(한 문제 다중 개념 태깅 시 개념별 분배·가중은 후속) ④ confidence v1 휴리스틱(사후 분산 기반 신뢰구간 후속) ⑤ 파라미터 미적합(전 개념 기본값). **후속**: AttemptEvent→record 자동 결선·다중 개념 분배·파라미터 적합(EM)·IRT·forgetting.
- ✅ **L2 학습자 모델 — 슬라이스 3 (채점 풀이 → 평가 개념 숙달 자동 전파 — `record_problem_attempt_mastery`)** 완료 (메인 직접·4게이트 green·1548 passed, 2026-06-05) — 풀이 채점 파이프라인의 *자동 결선 진입점*. 채점된 풀이(user, problem, 정/오답) 하나가 그 문제가 *평가하는 모든 개념*의 숙달 갱신으로 전파된다(다개념 분배 동시 해소). **구현**: `l2/mastery_tracking`에 ① **`_assessed_concept_ids`** — `problem_concept`에서 role∈{PRIMARY, TESTED}인 *distinct* concept_id SELECT(SUPPORTING·IMPLICIT 제외). ② **`record_problem_attempt_mastery(session, user_id, problem_id, correct, *, model, measured_at, assessed_roles)`** — 평가 개념 조회 → 각 개념마다 `record_attempt_mastery`(slice 2) 호출 → `ConceptMasteryHistory` 측정 N건 적재·리스트 반환. 매핑 없으면 []. **역할 필터 근거**(교수학): 정/오답은 *평가 대상* 개념(PRIMARY 주된·TESTED 평가)의 숙달 직접 증거지만 SUPPORTING(계산 부수)·IMPLICIT(무의식)엔 직접 증거 아님(오답이 보조 탓일 수도·정답이 보조 숙달 확증 안 함) → 노이즈 차단. **한 풀이=같은 measured_at**(생략 시 현재 1회 계산해 전 개념 공유). **소스**: 정/오답 신호는 `ProblemAttempt.is_correct`(AttemptEvent는 단계 이벤트 로그)·문제↔개념은 `ProblemConcept`(N:M·role·relevance). **현재 호출처 없음**(채점 엔드포인트 미구현·본 함수가 *결선 seam*·향후 채점 파이프라인이 호출). **테스트** 2층: ① **hermetic +3**(`TestRecordProblemAttemptMastery`·`_QueueSession`[execute별 큐잉 결과·다중 쿼리 시뮬]): 두 평가 개념 전파(같은 measured_at·add 2·commit 2)·매핑 0 → 빈 리스트·단일 개념 오답. ② **통합 +1**(`..._only_assessed_roles_on_live_pg`·skip): 실 PG Problem+3 Concept+3 ProblemConcept(PRIMARY·TESTED·SUPPORTING) 적재 → PRIMARY·TESTED 2개만 갱신·SUPPORTING 제외(역할 필터 실증·FK 체인 setup/cleanup). **4게이트 green**(py3.12): black(108)·ruff·mypy-strict(93)·pytest **1548 passed**(1545→+3)·skip 53(+1 통합)·`l2/mastery_tracking.py` cov **100%**·전체 99.89%. **🎯 의미**: 학습자 모델이 *풀이 한 건 → 다개념 학습 곡선 자동 갱신*으로 연결 — 채점 파이프라인이 이 함수 1개만 호출하면 BKT 숙달이 모든 평가 개념에 시계열로 누적. L2 핵심 데이터 흐름(풀이→숙달) 완성. **결정 v1 한계/후속**: ① **채점 엔드포인트 미연결**(함수는 seam·POST 풀이 채점이 호출하는 결선은 후속) ② **동일 신호 분배**(평가 개념 각각 같은 정/오답·role/relevance 가중 부분 크레딧은 후속) ③ **개념별 독립 commit**(다개념 원자성 후속) ④ ProblemAttempt→correct 추출 로직 미포함(호출자가 correct 전달). **후속**: POST 풀이 채점 엔드포인트(ProblemAttempt 적재 + 자동 숙달 전파)·role/relevance 가중·파라미터 적합(EM)·IRT.
- ✅ **L2/L5 — 슬라이스 4 (`POST /v1/me/attempts` 풀이 채점 제출 + 숙달 자동 갱신 — HTTP 표면 노출)** 완료 (메인 직접·4게이트 green·1553 passed, 2026-06-05) — L2 풀이→숙달 흐름(slice 1~3)을 *실제 API 표면*에 노출. 인증 사용자가 풀이 채점 결과를 제출하면 `ProblemAttempt` 적재 + `record_problem_attempt_mastery`(slice 3)로 평가 개념 BKT 숙달 자동 전파. **구현**: `api/me.py` ① **요청 `AttemptSubmitRequest`**(extra='forbid'·problem_id·is_correct 필수·student_answer/duration_seconds[ge=0]/session_id/confidence_self_reported[0~1] 선택). **user_id는 인증 주입**(본문 무시·`extra=forbid`로 user_id 사칭 시도 시 422). ② **응답 `AttemptSubmitResponse`**(attempt_id·is_correct·`mastery_updates: list[ConceptMasteryUpdate{concept_id, mastery, sample_size}]`) — 클라이언트가 학습 곡선 즉시 반영. ③ **엔드포인트** `POST /v1/me/attempts`(201): attempt_id `uuid4()` 명시 발급(server_default 의존 X·응답 즉시 사용·hermetic 테스트 가능)·`ended_at=now`·attempt commit 후 숙달 전파. **v1 한계**: is_correct는 *클라이언트 보고*(서버측 답안 채점[OCR·answer-check]은 L3/L5 후속). **소스 정합**: 정/오답=ProblemAttempt(FK user_profile·problem·learning_session). **테스트** 2층: ① **hermetic +5**(`TestSubmitAttempt`·`_QueueSession` 다중 쿼리 시뮬): 평가 개념 1개 제출(201·mastery_updates·attempt+숙달행 add 2)·매핑 0(attempt만)·무토큰 401·필드 누락 422·extra 필드 422(user_id 사칭 차단). ② **통합 +1**(`test_submit_attempt_records_..._on_live_pg`·skip): 실 PG user+problem+concept+problem_concept(PRIMARY) 적재 → POST 201·mastery_updates 1·DB problem_attempt 1행·concept_mastery_history 1행·무토큰 401·FK 순서 cleanup. **4게이트 green**(py3.12): black(108)·ruff·mypy-strict(93)·pytest **1553 passed**(1548→+5)·skip 54(+1 통합)·`api/me.py` cov **100%**·전체 99.89%. **🎯 의미**: L2 학습자 모델이 *HTTP로 실사용 가능* — Flutter 클라이언트가 풀이 채점을 POST하면 BKT 숙달이 시계열로 누적되고 갱신 결과를 즉시 응답받는다. **풀이→숙달→학습곡선 end-to-end 완성**(slice 1 추정 → 2 적재 → 3 다개념 전파 → 4 HTTP). **결정 v1 한계/후속**: ① **클라이언트 보고 is_correct**(서버 답안 채점 후속) ② **attempt↔mastery 비원자**(attempt commit 후 전파·부분 실패 시 attempt만 잔존·재전파 가능) ③ problem_id FK 미존재 시 무결성 오류(명시 404 핸들링 후속) ④ role/relevance 가중·파라미터 적합·IRT 후속. **후속**: 서버측 답안 채점·problem 미존재 404·role 가중·EM 적합·IRT·학습곡선 조회 API.
- ✅ **L2/L5 — 슬라이스 5 (`GET /v1/me/mastery` 학습곡선 조회 — ConceptMasteryHistory 시계열 노출)** 완료 (메인 직접·4게이트 green·1560 passed, 2026-06-05) — 풀이 채점(slice L2-4)이 누적한 개념 숙달 시계열을 학생이 *조회*하는 API. 풀이→숙달 쓰기 흐름(1~4)에 이어 *읽기*(학습 곡선 노출) 완성. **구현**: `api/me.py` `GET /v1/me/mastery`(response_model=`list[ConceptMasteryHistorySchema]`) — user_id 스코핑(타인 차단)·기존 /me 머신 *전면 재사용*: `concept_id`(선택·한 개념 곡선)·`since`/`until`(measured_at 시간창·time_window_conditions)·`order`(asc/desc·기본 최신순)·limit/offset·`include_total`(X-Total-Count·_maybe_set_total). 2차 정렬키 `concept_id`로 동률(같은 measured_at·다개념) 안정 정렬. **마이그레이션 불필요**(읽기 전용·ConceptMasteryHistory 기존 테이블). **신규 별칭** `ConceptIdFilter`(uuid|None). **테스트** 2층: ① **hermetic +7**(`TestMasteryCurve`): rows 반환·빈·401·concept_id 결선(유효 200·잘못된 uuid 422)·include_total 헤더·order(asc/desc 200·sideways 422)·시간창 검증(naive 422·since>until 422). ② **통합 +1**(`test_me_mastery_curve_scoped_and_concept_filter_on_live_pg`·skip): 실 PG A(c1 2측정·c2 1측정)+B(c1 1측정) → A 토큰 전체 3건(B 차단)·`?concept_id=c1` 2건·`order=asc` 오래된 먼저(0.5)·무토큰 401. ConceptMasteryHistory는 FK 없어 setup 경량. **4게이트 green**(py3.12): black(108)·ruff·mypy-strict(93)·pytest **1560 passed**(1553→+7)·skip 55(+1 통합)·`api/me.py` cov **100%**·전체 99.89%. **🎯 의미**: L2 학습자 모델 *읽기 표면* 완성 — Flutter가 학생의 개념별 학습 곡선(시간순 숙달 변화)을 조회해 시각화. 풀이 채점(POST)으로 쌓고 학습곡선(GET)으로 본다 — L2 read/write API 한 쌍 완비. **결정 v1 한계/후속**: ① **개념별 최신 스냅샷 미제공**(전 측정 시계열만·"현재 개념별 숙달" latest-per-concept 윈도우 쿼리는 후속) ② concept_id 단일(다중 OR 후속) ③ 개념 메타(이름 등) 미조인(concept_id만·UI는 별 조회·후속 조인). **후속**: latest-per-concept 스냅샷·개념 메타 조인·서버측 답안 채점·role 가중·EM 적합·IRT.
- ✅ **L2/L5 — 슬라이스 5b (`GET /v1/me/mastery/current` 현재 개념별 숙달 스냅샷)** 완료 (메인 직접·4게이트 green·1563 passed, 2026-06-05) — slice 5(전 측정 시계열=학습 곡선)의 짝: 개념마다 *최신* 측정 1건씩 = 현재 상태 "한눈에". **구현**: `api/me.py` `GET /v1/me/mastery/current`(response_model=`list[ConceptMasteryHistorySchema]`) — Postgres **`DISTINCT ON (concept_id)`** + `ORDER BY concept_id, measured_at DESC`로 개념별 최신 행 선택(SQLAlchemy `.distinct(ConceptMasteryHistory.concept_id)`). user_id 스코핑. 스냅샷이라 페이지네이션·필터 없음(연습 개념 수로 한정). `/mastery`(시계열)와 `/mastery/current`(스냅샷)는 별 리터럴 경로(충돌 없음). **테스트** 2층: ① **hermetic +3**(`TestMasteryCurve`): rows·빈·401(DISTINCT ON 정확성은 FakeSession 무시라 통합 검증). ② **통합 +1**(`test_me_mastery_current_latest_per_concept_on_live_pg`·skip): 실 PG c1 2측정(0.5→0.69)·c2 1측정(0.3) → /current 2행·c1=0.69(최신)·c2=0.3·무토큰 401. **4게이트 green**(py3.12): black(108)·ruff·mypy-strict(93)·pytest **1563 passed**(1560→+3)·skip 56(+1 통합)·`api/me.py` cov **100%**·전체 99.89%. **🎯 의미**: 학생이 *지금 어느 개념을 얼마나 아는지* 한 응답으로 파악 — 대시보드/약점 진단 입력. /mastery(곡선·시간축)와 /mastery/current(스냅샷·개념축) 두 뷰 완비. **결정 v1 한계/후속**: ① **개념 메타 미조인**(concept_id만·이름/단원은 별 조회) ② 정렬 concept_id 고정(mastery 낮은 순 등 정렬 옵션 후속) ③ 페이지네이션 없음(개념 수 폭증 시 후속). **후속**: 개념 메타 조인·약점 정렬(mastery asc)·서버측 채점·EM 적합·IRT·forgetting.
- ✅ **L2/L5 — 슬라이스 5c (`/mastery/current` 약점 정렬 — `order_by=mastery`)** 완료 (메인 직접·4게이트 green·1567 passed, 2026-06-05) — 현재 숙달 스냅샷에 정렬 기준 추가: `order_by=mastery&order=asc` → *약한 개념 우선*("다음에 뭘 연습할지"). **구현**: `api/me.py` `list_my_current_mastery`에 `order_by: SnapshotOrderByParam`(Literal concept_id|mastery·기본 concept_id) + `order: OrderParam`(기본 asc·기존 동작 보존). DISTINCT ON은 ORDER BY가 concept_id로 시작해야(최신 행 선택 불변) 결과(개념당 1행·소규모)를 *Python에서 재정렬*. **mastery NULL은 정렬 방향 무관 항상 끝**(present 정렬 + absent 뒤). 신규 별칭 `SnapshotOrderBy`/`SnapshotOrderByParam`. **마이그레이션 불필요**. **테스트 +4 hermetic**(`TestMasteryCurve`): order_by=mastery asc 약점 우선([0.3,0.6,0.9])·desc 강점 우선·NULL 끝·invalid order_by 422. Python 재정렬이라 FakeSession으로 *정렬 정확성까지* hermetic 검증(DB 무관). **4게이트 green**(py3.12): black(108)·ruff·mypy-strict(93)·pytest **1567 passed**(1563→+4)·skip 56·`api/me.py` cov **100%**·전체 99.89%. **🎯 의미**: 스냅샷이 *약점 진단 도구*로 — 클라이언트가 `?order_by=mastery&order=asc`로 가장 약한 개념부터 받아 "오늘 이거 연습하자" 추천. CLAUDE.md 메타인지·약점 우선 학습 정합. **결정 v1 한계/후속**: ① **개념 메타 미조인**(여전히 concept_id만·이름 별 조회) ② Python 재정렬이라 limit(상위 N 약점)은 전 행 fetch 후 자름(개념 수 폭증 시 SQL 서브쿼리 정렬+LIMIT 후속) ③ confidence·sample_size 가중 우선순위(저표본 개념 신뢰 보정) 후속. **후속**: 개념 메타 조인·상위 N 약점(서브쿼리)·confidence 가중·서버측 채점·EM·IRT.
- ✅ **L2/L5 — 슬라이스 5d (`/mastery/current` 개념 메타 조인 — 약점 목록에 개념명/코드 노출)** 완료 (메인 직접·4게이트 green·1568 passed, 2026-06-05) — 현재 숙달 스냅샷에 `concept` 테이블(`name_ko`·`code`)을 조인해 UI 실사용성 확보(concept_id UUID만으론 표시 불가 → 개념명 노출). **구현**: `api/me.py` ① 신규 응답 `ConceptMasterySnapshotItem`(concept_id·concept_code·concept_name·mastery·confidence·sample_size·measured_at) — 기존 `ConceptMasteryHistorySchema`(concept_id만) 대체. ② 쿼리 `select(ConceptMasteryHistory, Concept.code, Concept.name_ko).outerjoin(Concept, concept_id==)` — **LEFT JOIN**(concept_mastery_history는 concept FK 없는 loose ref·orphan 개념은 name/code null·행 보존). DISTINCT ON·약점 정렬(5c)·user 스코핑 유지. `result.all()` Row 튜플 순회(scalars 아님). **응답 계약 변경**(브랜드뉴 엔드포인트·소비자 없어 안전·enrich가 목적). **테스트** 2층: ① **hermetic +1 net**(`_snapshot_row` 튜플 헬퍼·`_Result.all()` 추가): 개념 메타 노출(name/code)·orphan null·약점/강점/NULL 정렬은 snapshot 튜플로 전환. ② **통합 갱신**(`..._latest_per_concept_...`): c1은 Concept 행 적재(name "정적분"·code) → concept_name 조인 확인·c2는 orphan → null(LEFT JOIN 실증)·`_cleanup_concepts` 추가. **4게이트 green**(py3.12): black(108)·ruff·mypy-strict(93)·pytest **1568 passed**(1567→+1)·skip 56·`api/me.py` cov **100%**·전체 99.89%. **🎯 의미**: 약점 목록이 *바로 쓸 수 있는* 형태 — 클라이언트가 `?order_by=mastery&order=asc`로 "정적분 0.3·미분 0.5…" 처럼 개념명+숙달을 한 응답에 받아 약점 대시보드 렌더. 추가 N개 개념 조회 불요. **결정 v1 한계/후속**: ① **/mastery(곡선)은 미조인**(시계열은 concept_id만·곡선 UI는 단일 개념 컨텍스트라 후속) ② level/subject 등 추가 메타 미포함(name·code만·필요 시 확장) ③ 상위 N 약점 SQL 서브쿼리·confidence 가중 후속. **후속**: /mastery 곡선 메타 조인·상위 N 약점·confidence 가중·서버측 채점·EM·IRT.
- ✅ **L2 학습자 모델 — 슬라이스 6 (BKT forgetting/시간 감쇠 — `apply_forgetting`)** 완료 (메인 직접·4게이트 green·1581 passed, 2026-06-05) — BKT에 *시간 차원* 추가: 연습 공백 동안 숙달이 사전값을 향해 지수 감쇠(장기 숙달 > 단기 점수·CLAUDE.md). **구현**: ① **`bkt.py` `BktParameters.p_forget`**(망각률 λ·일 단위·ge=0·기본 0=감쇠 없음·하위 호환·확률 아닌 *율*이라 le 없음). ② **`apply_forgetting(mastery, elapsed_days, params)`**: `P(L') = P(L0) + (P(L)-P(L0))·exp(-λ·경과일)` — *사전 초과 숙달분만* 감쇠(학습으로 얻은 것만 잃음·P(L)≤P(L0)는 무변화·망각은 더 나빠지는 게 아님). λ=0·경과≤0은 무변화(클램프). BktModel.apply_forgetting 메서드. ③ **`mastery_tracking` 결선**: `compute_mastery_record`에 `elapsed_days` 인자(관측 갱신 *전* prior에 감쇠 적용)·`record_attempt_mastery`가 직전 측정~now 경과일 계산(`(now - prior.measured_at).total_seconds()/86400`). p_forget=0 기본이라 *현 동작 무변화*(opt-in). **검증 수치**: 0.9(gain 0.6 위 p_init 0.3)·λ=0.05·10일 → 0.3+0.6·exp(-0.5)≈0.6639. **테스트 +13 hermetic**: bkt 10(`TestForgetting`: 기본 0·음수 거부·>1 허용·λ0 무영향·경과0 무영향·음수경과 무영향·사전 향해 감쇠·경과↑ 감쇠↑·사전이하 무변화·model 메서드) + mastery 3(`TestForgettingInRecord`: 기본 모델 elapsed 무영향·망각 모델 결과↓·record가 직전 measured_at에서 경과 계산). **4게이트 green**(py3.12): black(108)·ruff·mypy-strict(93)·pytest **1581 passed**(1568→+13)·skip 56·`l2/bkt.py`·`l2/mastery_tracking.py` cov **100%**·전체 99.89%. **🎯 의미**: 학습자 모델이 *시간을 안다* — 한 달 안 푼 개념은 숙달이 자연히 내려가 복습 필요를 드러낸다(약점 진단·간격 반복 입력). 기본 off라 개념별 λ 적합(EM·실데이터) 후 켠다. **결정 v1 한계/후속**: ① **λ 미적합**(개념별 망각률은 실데이터 적합 필요·현재 전역 off) ② **감쇠 floor=p_init 고정**(완전 망각[floor 0]·개념별 floor 후속) ③ 지수 모델 고정(power-law·Ebbinghaus 변형 후속) ④ 적재 시점에만 감쇠 반영(조회 시 실시간 감쇠는 미반영·다음 관측 때 보정). **후속**: λ EM 적합·개념별 floor·조회 시 실시간 감쇠·간격 반복 추천·IRT.
- ✅ **L2 학습자 모델 — 슬라이스 7 (IRT 문항반응이론 프로토타입 — `l2/irt`)** 완료 (메인 직접·4게이트 green·1599 passed, 2026-06-05) — BKT(개념별 숙달·시간축)와 *상보적* 학습자 모델: 문항 난이도 b와 학생 능력 θ를 같은 logit 척도에 놓고 추정. 본 슬라이스는 *능력 θ 추정*(난이도 적합은 후속). **구현**: 신규 `whymath_backend/l2/irt.py`(BKT 슬라이스 1과 동형·순수 알고리즘). ① **`IrtItem`**(frozen pydantic·difficulty b·discrimination a>0·기본 1.0=Rasch/1PL). ② **`probability_correct(theta, item)`** 2PL: `1/(1+exp(-a(θ-b)))`. ③ **`estimate_ability(responses, *, initial, max_iter, tol, lower=-4, upper=4)`** — MLE Newton-Raphson: `θ ← θ + Σa(정답-P)/Σa²P(1-P)`(Fisher 정보로 나눔)·매 스텝 [lower,upper] clamp·수렴(step<tol) 또는 max_iter 종료. *전부 정답*→upper·*전부 오답*→lower(MLE 발산 경계)·빈 응답→initial. **이름 충돌**: bkt·irt 둘 다 `probability_correct` 정의 → 패키지 레벨엔 BKT만 재노출·IRT는 `l2.irt.probability_correct` 명시 접근(__init__ docstring 명문화). **검증 수치**: P(θ=b)=0.5·θ↑→P↑·난이도↑→P↓·변별도↑→곡선 가파름·대칭(b=0에 1정답1오답)→θ=0·어려운 문항 정답→θ↑(같은 정답 수라도)·고변별 응답 가중. **테스트 +18 hermetic**(`test_irt.py`): IrtItem(기본 Rasch·변별도>0·frozen) 3·probability_correct(P=0.5·단조·난이도·변별도·경계) 5·estimate_ability(빈→initial·전부정답 upper·전부오답 lower·대칭 0·정답↑θ↑·어려운문항 가중·clamp·결정론·변별도 가중·max_iter) 10. **4게이트 green**(py3.12): black(109)·ruff·mypy-strict(94·+1 신규 모듈)·pytest **1599 passed**(1581→+18)·skip 56·`l2/irt.py` cov **100%**·전체 99.89%. **🎯 의미**: 능력 θ는 *적응형 출제*(θ 근처 난이도 문항이 정보량 최대)·BKT 교차검증·진단(MasteryState theta)의 입력. BKT(개념별 이산 숙달)와 IRT(연속 능력 척도) 두 렌즈로 학습자를 본다. **결정 v1 한계/후속**: ① **난이도 미적합**(b는 입력·여러 학생 응답에서 b 동시추정[JMLE/MML]은 후속·현재는 b 주어진 θ만) ② **단일 차원 θ**(다차원 능력·개념별 θ는 후속) ③ **2PL 고정**(추측 c의 3PL·θ 사전[MAP]은 후속) ④ 시계열 적재·BKT 융합·HTTP 미연결. **후속**: 문항 난이도 적합(JMLE)·적응형 문항 선택(정보함수)·θ 시계열 적재·BKT+IRT 융합·3PL.
- ✅ **L2 학습자 모델 — 슬라이스 8 (IRT 적응형 문항 선택 — `item_information`·`select_next_item`)** 완료 (메인 직접·4게이트 green·1609 passed, 2026-06-05) — IRT 능력 추정(slice 7)을 *적응형 출제*로 확장(CAT 핵심): 학생 θ에서 정보량 최대 문항을 골라 가장 효율적으로 능력 측정. **구현**: `l2/irt.py` ① **`item_information(theta, item)`** = `a²·P·(1-P)` 문항정보함수 — P=0.5(θ=b)에서 최대(난이도가 능력과 일치할 때 변별력 최대)·변별도 a² 가중. ② **`select_next_item(theta, items, *, administered)`** — 미출제(administered 집합 제외) 문항 중 `item_information(θ,·)` 최대 인덱스 반환·후보 없으면 None·동률은 낮은 인덱스(결정론). **검증**: I(θ=0,b=0)=0.25(a²·0.5²)·θ=0→b=0 문항·θ=2→b=2·θ=-2→b=-2·b=0 제외 시 동률→낮은 인덱스·전부 출제/빈 목록→None·변별도 높은 문항 우선. **테스트 +10 hermetic**(`TestItemInformation` 4: θ=b 최대·대칭·a² 가중·비음수 / `TestSelectNextItem` 6: θ 근처 난이도 선택·administered 제외·전부 출제 None·빈 목록 None·동률 낮은 인덱스·변별도 우선). **4게이트 green**(py3.12): black(109)·ruff·mypy-strict(94)·pytest **1609 passed**(1599→+10)·skip 56·`l2/irt.py` cov **100%**·전체 99.89%. **🎯 의미**: 적응형 평가 *알고리즘 완비* — θ 추정(slice 7) + 다음 문항 선택(slice 8)이 CAT 루프(출제→채점→θ갱신→다음출제)를 구성. 학생 수준에 딱 맞는 난이도로 효율적 진단·연습. **결정 v1 한계/후속**: ① **인덱스 반환**(items 리스트 인덱스·호출자가 problem_id 매핑·문항풀 키 추상화 후속) ② **단일 정보 최대화**(노출 통제·내용 균형·max-info 외 기준[Bayesian·KL]은 후속) ③ HTTP/문항풀 DB 미연결(순수 알고리즘·적응형 출제 엔드포인트는 L3/L6 후속). **후속**: 적응형 출제 엔드포인트·문항 난이도 적합(JMLE)·노출 통제·θ 시계열·BKT+IRT 융합.
- ✅ **L2 학습자 모델 — 슬라이스 9 (IRT 문항 난이도 추정 — `estimate_difficulty`)** 완료 (메인 직접·4게이트 green·1619 passed, 2026-06-05) — slice 7 `estimate_ability`(난이도로 능력 θ 추정)의 *대칭*: 학생 능력으로 문항 난이도 b를 MLE 추정. 문항 보정(item calibration)·JMLE(θ↔b 교대 적합)의 b-단계. **구현**: `l2/irt.py` **`estimate_difficulty(responses, *, discrimination, initial, max_iter, tol, lower, upper)`** — `responses`는 `(학생 능력 θ, 정답)` 쌍. Newton: `b ← b + Σ-a(정답-P)/Σa²P(1-P)`(능력 추정과 *부호 반대*·정답 많을수록 b↓=쉬워짐). *모두 정답*→lower(누구나 맞힘=쉬움)·*모두 오답*→upper(아무도 못 맞힘=어려움)·빈→initial·[lower,upper] clamp·결정론. discrimination은 문항 a(기본 1.0). **검증**: θ=0에 1정답1오답→b=0·전부정답→-4·전부오답→+4·고능력만 정답→b>0(어려움)·저능력도 정답→b<0(쉬움)·라운드트립(능력2 정답·능력0 오답→0<b<2). **테스트 +10 hermetic**(`TestEstimateDifficulty`): 빈→initial·전부정답 lower·전부오답 upper·대칭 0·고능력만 정답 더 어려움·clamp·변별도 인자·결정론·max_iter·라운드트립. **4게이트 green**(py3.12): black(109)·ruff·mypy-strict(94)·pytest **1619 passed**(1609→+10)·skip 56·`l2/irt.py` cov **100%**·전체 99.89%. **🎯 의미**: IRT 추정 도구 *대칭 완비* — 능력 θ(문항 고정)·난이도 b(능력 고정) 둘 다 추정 가능 → JMLE 교대 적합(θ→b→θ…)으로 *응답 데이터만으로 문항은행 자가 보정* 가능(전문가 난이도 라벨 없이). **결정 v1 한계/후속**: ① **JMLE 미구현**(θ↔b 교대 루프는 estimate_ability+estimate_difficulty 결합 후속·앵커 정규화 필요[척도 불확정성]) ② 변별도 a 미추정(고정·a 동시추정은 2PL 적합 후속) ③ HTTP/문항은행 DB 미연결. **후속**: JMLE 교대 적합·a 추정(2PL)·문항은행 보정 파이프라인·적응형 출제 엔드포인트.
- ✅ **L2 학습자 모델 — 슬라이스 10 (IRT JMLE 결합 적합 — `fit_jmle`)** 완료 (메인 직접·4게이트 green·1626 passed, 2026-06-05) — IRT 추정의 *정점*: 응답 데이터만으로 능력 θ·난이도 b를 *동시* 추정(문항은행 자가 보정·전문가 라벨 불요). slice 7(능력)·9(난이도) 추정을 교대 반복으로 결합. **구현**: `l2/irt.py` **`fit_jmle(responses, n_students, n_items, *, discrimination, max_iter, tol)`** — `responses`는 `(student_idx, item_idx, 정답)` 희소 삼중쌍. 1회 그룹화(학생별·문항별) 후 매 반복: ① 난이도 고정→각 학생 θ(`estimate_ability`) ② 능력 고정→각 문항 b(`estimate_difficulty`) ③ **난이도 평균 0 중심화**(θ·b 동시 가산이동 불변=척도 위치 불확정성 해소). θ·b 최대 변화<tol 수렴. 반환 `(abilities, difficulties)`. 응답 없는 학생/문항은 0·전부정답/오답은 경계 clamp. **검증(Guttman 3×3)**: 저/중/고 학생 → 능력 [-1.68, 1.84, 4.0] 증가·쉬움/중/어려움 문항 → 난이도 [-3.65, 0.36, 3.29] 증가·난이도 평균=0(중심화). **테스트 +7 hermetic**(`TestFitJmle`): 빈→zeros·능력 순서 회복·난이도 순서 회복·난이도 중심화(평균0)·결정론·응답없는 학생 0·2×2 분리. 방어 분기(`if active`·응답 존재 시 항상 참)는 `# pragma: no branch`. **4게이트 green**(py3.12): black(109)·ruff·mypy-strict(94)·pytest **1626 passed**(1619→+7)·skip 56·`l2/irt.py` cov **100%**·전체 99.89%. **🎯 의미**: IRT 문항은행 *자가 보정 완비* — 학생 응답 로그만으로 문항 난이도가 데이터 기반 추정(평가원/EBS 라벨 없이·자체 동등문제 난이도 자동 보정). slice 8 적응형 출제 + slice 10 보정 = CAT(컴퓨터적응평가) 핵심 알고리즘 완성. **결정 v1 한계/후속**: ① **변별도 a 고정**(JMLE는 θ·b만·a 동시추정[2PL 완전 적합]은 후속) ② **JMLE 편향**(소표본 b 편향[극단화]·MML/Warm 보정 후속) ③ 응답 행렬 dense 그룹화(대규모는 희소·벡터화 후속) ④ HTTP/문항은행 DB 미연결(보정 파이프라인은 후속). **후속**: a 추정(완전 2PL)·MML/편향 보정·ProblemAttempt→문항은행 보정 파이프라인·적응형 출제 엔드포인트·BKT+IRT 융합.
- ✅ **L2/L5 — 슬라이스 11 (`GET /v1/me/ability` IRT 능력 추정 HTTP 결선 — 채점 풀이 이력 기반)** 완료 (메인 직접·4게이트 green·1632 passed, 2026-06-05) — IRT를 *실제 데이터·HTTP에 첫 결선*: 학생의 채점된 ProblemAttempt 이력에서 능력 θ를 추정해 노출. BKT는 이미 /me/attempts·/me/mastery로 결선됐고, IRT도 이제 API 표면에. **구현**: `api/me.py` ① **`_difficulty_to_logit(d)`** = `d - 3.0` — `Problem.difficulty_overall`(전문가 1~5)을 IRT 난이도 b(logit·범위 [-2,2])로 선형 매핑(중앙 3=b0). *휴리스틱 프록시*(보정 b는 fit_jmle 후속). ② **`GET /v1/me/ability`**(response `AbilityResponse{theta, response_count}`) — `problem_attempt`(is_correct 있는 것)를 `problem` JOIN → (난이도→b, 정답) 응답 만들어 `estimate_ability`(slice 7)로 θ. difficulty_overall NULL 문항 제외·채점 0건이면 θ=0. user_id 스코핑. **마이그레이션 불필요**(읽기·기존 테이블). **테스트** 2층: ① **hermetic +6**(`TestAbility`): 빈→θ0·401·전부정답→θ4(상한)·전부오답→θ-4·어려운 문항 정답이 더 높은 θ·NULL 난이도 제외. FakeSession `_Result.all()`이 (is_correct, difficulty) 튜플 반환(조인 시뮬). ② **통합 +1**(`test_me_ability_estimates_theta_from_attempts_on_live_pg`·skip): 실 PG user+problem(difficulty 5)+attempt(정답) → θ=4·count 1·401·FK 순서 cleanup. **4게이트 green**(py3.12): black(109)·ruff·mypy-strict(94)·pytest **1632 passed**(1626→+6)·skip 57(+1 통합)·`api/me.py` cov **100%**·전체 99.89%. **🎯 의미**: IRT가 *살아 있는 능력 척도* — 학생이 푼 문항 난이도×정답으로 θ가 즉시 산출(BKT 개념별 숙달과 상보적 단일 능력값). 적응형 출제(slice 8)의 θ 입력·BKT 교차검증. **결정 v1 한계/후속**: ① **난이도 프록시**(difficulty_overall 1~5 선형 매핑·보정 b는 fit_jmle 적합 후속) ② **단일 θ**(개념별/도메인별 θ 미분리·전 과목 합산) ③ **변별도 a=1 고정**(2PL 미사용) ④ θ 미적재(매 요청 재계산·시계열 저장 후속). **후속**: fit_jmle 보정 b 적용·개념별 θ·θ 시계열 적재·적응형 출제 엔드포인트(θ→select_next_item)·BKT+IRT 융합 진단.
- ✅ **L2/L5 — 슬라이스 12 (`GET /v1/me/next-problem` 적응형 출제 — IRT CAT 루프 완성)** 완료 (메인 직접·4게이트 green·1636 passed, 2026-06-05) — IRT의 *적응형 컴퓨터 평가(CAT) 루프*를 HTTP에 결선: 학생 능력 θ에 *정보량 최대*인 *미응답* 문항을 추천. slice 7(θ 추정)·8(`select_next_item` 정보함수)·11(`GET /me/ability` 결선)을 묶어 "다음에 풀 문항"을 산출. **구현**: `api/me.py` **`GET /v1/me/next-problem`**(response `NextProblemResponse{problem_id, theta, difficulty}`) — ① 채점 풀이 이력(`problem_attempt` JOIN `problem`·is_correct 있는 것)으로 θ 추정(slice 11 로직 재사용)·이미 푼 problem_id 집합 수집. ② 난이도 라벨(difficulty_overall) 있는 *미응답* 문항을 θ 근방(SQL `func.abs(difficulty_overall - (θ+3.0))` 거리순)으로 `_CANDIDATE_POOL_SIZE=50`개 선별·`NOT IN`(이미 푼 것 제외). ③ 후보를 `IrtItem`으로 만들어 `select_next_item(θ, items)`(slice 8)로 정보량 최대 1개 선택. 후보 없으면 problem_id=null. 난이도→b는 slice 11 `_difficulty_to_logit`(보정 b는 fit_jmle 후속). user_id 스코핑·**마이그레이션 불필요**(읽기·기존 테이블). **2 SQL 쿼리** → hermetic은 `_QueueSession`(execute 2회 큐). **테스트** 2층: ① **hermetic +4**(`TestNextProblem`): 이력없음·후보없음→null·401·θ0에서 난이도 2·3·5 중 b0(난이도3) 추천(정보량 최대)·전부정답→θ4에서 난이도 3·5 중 b2(난이도5) 추천(능력 높을수록 어려운 문항). ② **통합 +1**(`test_me_next_problem_recommends_unattempted_on_live_pg`·skip): 실 PG user+푼문항(난이도5 정답)+미응답 난이도3·5 → θ4·NOT IN으로 푼 난이도5 제외·미응답 난이도5(pid_hard) 추천·401·FK 순서 cleanup. **4게이트 green**(py3.12): black(94)·ruff·mypy-strict(94)·pytest **1636 passed**(1632→+4)·skip 58(+1 통합)·`api/me.py` cov 유지·전체 99.89%. **🎯 의미**: **CAT(컴퓨터 적응 평가) 루프 완성** — 학생이 문항을 풀 때마다 θ가 갱신되고(slice 11) 다음 문항이 θ에 맞춰 자동 추천(본 슬라이스). "너무 쉽지도 어렵지도 않은" 문항(P≈0.5·정보량 최대)을 출제해 진단 효율 극대화·CLAUDE.md 교수학 정합(단계별 진단·학생 능력 적응). **결정 v1 한계/후속**: ① **난이도 프록시**(difficulty_overall 1~5·보정 b는 fit_jmle 후속) ② **단일 θ·a=1 고정**(개념별 θ·2PL 변별도 미사용) ③ **개념 커버리지 무시**(순수 정보량 최대·아직 약한 개념 우선 등 교수학 제약 미반영) ④ θ 미적재(매 요청 재계산) ⑤ 후보 풀 50 고정(난이도 분포 희소 시 편향 가능). **후속**: fit_jmle 보정 b·개념별 θ·약점 개념 가중 출제(BKT 융합)·노출 통제(content balancing)·θ 시계열 적재.
- ✅ **L2 학습자 모델 — 슬라이스 13 (IRT 측정 정밀도 — `total_information`·`ability_standard_error`)** 완료 (메인 직접·4게이트 green·1644 passed, 2026-06-05) — CAT 루프의 *중단 규칙 기반*: θ가 "얼마나 정밀하게" 측정됐는지 산출. slice 8 `select_next_item`(출제 *선택*)·slice 12 `recommend_next_problem`(HTTP 결선)에 이어, *언제 그만 출제할지*(정밀도 충족)를 판단하는 순수 알고리즘. slice 7·9·10과 동형의 pure-`irt.py` 슬라이스(외부 의존 0·결정론). **구현**: `l2/irt.py` ① **`total_information(theta, items)`** = `Σ item_information(θ, itemᵢ)` — 검사 정보함수(test information function·문항 응답 독립 가정 단순합). 빈 목록→0. *(함수명 `test_information`은 pytest가 `test_*`를 테스트로 수집해 충돌 → `total_information`으로 명명·docstring에 "검사 정보함수" 명기.)* ② **`ability_standard_error(theta, items)`** = `1/√I(θ)` — θ 추정의 표준오차(Fisher 정보 역제곱근·MLE 점근 SE). 정보 0(빈 목록)이면 `math.inf`(측정 불가). **쓰임**: CAT 중단 규칙(SE<목표[예 0.3]이면 종료)·θ 신뢰구간 θ±z·SE. **검증 수치**: 빈→0/inf·단일=item_information·가산성(검사정보=문항정보 합)·문항↑→정보↑·SE↓·θ=b Rasch 1문항 정보 0.25→SE=2.0·문항 3개면 SE 작아짐·SE=1/√I 정의 일치. **테스트 +8 hermetic**(`TestTotalInformation` 4·`TestAbilityStandardError` 4). **4게이트 green**(py3.12): black(94)·ruff·mypy-strict(94)·pytest **1644 passed**(1636→+8)·skip 58·`l2/irt.py` cov **100%**(105 stmt). **🎯 의미**: CAT 3요소 완비 — *선택*(`select_next_item`)·*추천 결선*(`/me/next-problem`)·**중단 판단**(`ability_standard_error`). 이제 "θ를 목표 정밀도까지 측정하는 적응형 검사"의 알고리즘 토대 완성. θ ± 1.96·SE 신뢰구간으로 진단 불확실성도 학생/교사에게 정량 노출 가능. **결정 v1 한계/후속**: ① **HTTP 미결선**(`/me/ability`에 SE·신뢰구간 노출은 후속 슬라이스) ② **MLE 점근 SE**(소표본·경계 θ에서 과소추정 가능·베이지안 EAP/사후 SD는 후속) ③ 중단 규칙 자체(SE 임계값 루프)는 미구현(정밀도 *값*만 제공). **후속**: `/me/ability` SE·신뢰구간 노출·CAT 중단 규칙 결선(목표 SE까지 반복 출제)·EAP 추정·θ 시계열 적재.
- ✅ **L2/L5 — 슬라이스 14 (`GET /v1/me/ability`에 측정 정밀도 결선 — SE·95% 신뢰구간)** 완료 (메인 직접·4게이트 green·1646 passed, 2026-06-05) — slice 13 `ability_standard_error`를 HTTP에 결선: θ만 반환하던 `/me/ability`에 *표준오차*와 *95% 신뢰구간*을 추가해 진단 불확실성을 정량 노출. 알고리즘(slice 13)→API의 자연스러운 다음 단계. **구현**: `api/me.py` ① **`AbilityResponse` 확장**(하위호환·필드 추가): `standard_error: float|None`(SE=1/√I(θ))·`confidence_interval: list[float]|None`([하한,상한]=θ±1.96·SE). ② **`get_my_ability`**: θ 추정 후 `items=[item for item,_ in responses]`로 `ability_standard_error(θ, items)` 계산 — `math.isinf(se)`(응답 0건=정보 0)면 SE·CI 모두 null(측정 불가)·그 외엔 SE와 θ±1.96·SE CI 노출. `_CI_Z_95=1.96` 상수. **경계 θ 주의**(docstring 명기): 전부 정답/오답 θ의 SE는 Fisher 정보 기반이라 *낙관적*(실 불확실성 과소·EAP는 후속). **마이그레이션 불필요**(읽기). **테스트**: ① **hermetic +2**(`TestAbility`): 혼합(난이도3 1정답·1오답)→θ0·SE=1/√0.5≈1.41421·대칭 CI[-2.77186, 2.77186]·응답↑→SE↓. 기존 `test_empty_zero_theta` dict 동등성에 SE·CI null 추가. ② **통합 갱신**(ability 통합테스트에 SE>0·CI lo<θ<hi 단언). **4게이트 green**(py3.12): black(94)·ruff(src)·mypy-strict(94)·pytest **1646 passed**(1644→+2)·`api/me.py` cov **100%**(219 stmt). **🎯 의미**: 학생/교사가 "θ=2.0"만 보는 게 아니라 "θ=2.0 ± 1.1(95% 신뢰)"처럼 *측정 신뢰도*를 함께 본다 — 응답이 적으면 넓은 구간(불확실)·많으면 좁은 구간(정밀)으로 진단 투명성↑·CLAUDE.md AI 신뢰("확실하지 않을 때 자신 있게 말함" 금지) 정합. **결정 v1 한계/후속**: ① **MLE 점근 SE**(경계 θ 과소추정·EAP/사후 SD 후속) ② **CI 비클램프**(θ±1.96·SE가 [-4,4] 밖으로 나갈 수 있음·해석상 무방하나 표시 클램프는 후속) ③ CAT 중단 규칙 루프(목표 SE까지 반복 출제)는 미결선. **후속**: CAT 중단 규칙(`/me/next-problem`이 목표 SE 도달 시 종료 신호)·EAP 추정·θ 시계열 적재·개념별 θ.
- ✅ **L4 교수학 엔진 — 슬라이스 62 (트리거 함수 search_path 하드닝)** 완료 (메인 직접·라이브 PG16 검증·1458 passed, 2026-06-04) — slice 59 트리거 함수 `whymath_cleanup_attempt_events`에 `SET search_path = pg_catalog, public` 추가(보안 하드닝·CLAUDE.md 우선순위 #2 법적·보안). **맥락**: 함수는 SECURITY DEFINER가 아니고(invoker rights) 테이블 참조도 `public.attempt_event`로 한정이라 현재 실위험은 낮음 — 그러나 호출 세션의 search_path가 조작돼도 연산자·함수 해석이 pg_catalog에 고정되도록 명시(defense-in-depth). **코드베이스 첫 DB 함수의 보안 선례**(향후 SECURITY DEFINER 함수 도입 시 표준). **구현**: alembic `b8c9d0e1f2a3`(upgrade=`CREATE OR REPLACE ... SET search_path`·downgrade=slice 59 원본 복원). `CREATE OR REPLACE`라 기존 트리거 바인딩(`trg_cleanup_attempt_events`) 유지 — 트리거 재생성 불요·**동작 불변**(qualified DELETE 그대로). **라이브 PG16 검증**: ① `pg_proc.proconfig` = `{search_path=pg_catalog, public}`(설정 확인) ② cascade 정리 동작 불변(`cleanup_ok=true` — session 삭제→attempt_event 0) ③ `alembic check` 무드리프트(함수 autogenerate 비추적) ④ downgrade(proconfig `(none)`)/재upgrade 왕복. **게이트 green**: black(100)·ruff·mypy-strict(88)·pytest **1458 passed**(불변). 마이그레이션 전용(ORM/테스트 변경 0 — slice 59 통합테스트 `test_session_delete_cleans_attempt_event_orphans_on_live_pg`가 하드닝된 트리거로 회귀 커버). **후속**: audit 보존정책(TTL)/암호화·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 61 (assessment (user_id, started_at DESC) 인덱스)** 완료 (메인 직접·라이브 PG16 검증·1458 passed, 2026-06-04) — slice 60에서 발견한 **parity 갭 즉시 해소**: 세 /me 도메인 중 `learning_session`·`dialogue`는 `(user_id, <time> DESC)` 인덱스가 있는데 **`assessment`만 부재**(§8.1 DDL이 인덱스 미지정)였음. `list_my_assessments`(`WHERE user_id ORDER BY started_at DESC`)도 동일 패턴이라 동형 인덱스 추가. **구현**: ORM `Assessment.__table_args__ = (sa.Index("idx_assessment_user", "user_id", sa.desc("started_at")),)` + alembic `a7b8c9d0e1f2`(`literal_column("started_at DESC")` — slice 60·idx_session_user 구문 동일) + 모듈 docstring "인덱스 없음" 설명을 slice 61 추가 반영해 갱신. **라이브 PG16 검증**: 생성(`USING btree (user_id, started_at DESC)`)·`alembic check` 무드리프트(ORM sa.desc↔migration literal_column 정합)·`EXPLAIN`이 **Index Scan using idx_assessment_user** 채택·downgrade/재upgrade 왕복. **4게이트 green**: black(99)·ruff·mypy-strict(88)·pytest **1458 passed**(불변·인덱스는 투명·신규 테스트 0). **결과**: 세 /me 리스트 도메인 + deletion_audit까지 user 인덱스 정합 완비. **후속**: audit 보존정책(TTL)/암호화·트리거 search_path 하드닝·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 60 (deletion_audit (user_id, deleted_at DESC) 인덱스)** 완료 (메인 직접·라이브 PG16 검증·1458 passed, 2026-06-04) — slice 58 `list_my_deletions`(`WHERE user_id ORDER BY deleted_at DESC LIMIT`) 접근 패턴 인덱스. **정합 근거**: `learning_session.idx_session_user`·`dialogue.idx_dialogue_user`가 정확히 `(user_id, <time> DESC)` 인덱스를 가짐(§6.1/§7.1 DDL) → append-only로 커지는 감사 테이블에 동형 인덱스 부재는 일관성·성능 갭. **구현**: ORM `__table_args__ = (sa.Index("idx_deletion_audit_user", "user_id", sa.desc("deleted_at")),)`(activity.py 패턴) + alembic `f6a7b8c9d0e1`(`op.create_index([..., sa.literal_column("deleted_at DESC")])` — 기존 idx_session_user migration 구문 그대로). **핵심 정합 포인트**: ORM `sa.desc(...)` ↔ migration `literal_column("... DESC")`가 alembic autogenerate 비교상 *동일*로 인식돼 드리프트 0(learning_session 선례와 동형 — 확인). **라이브 PG16 검증**: ① 인덱스 생성(`CREATE INDEX ... USING btree (user_id, deleted_at DESC)`) ② `alembic check` 무드리프트 ③ `EXPLAIN`(enable_seqscan=off)이 **Index Scan using idx_deletion_audit_user** 채택 — 쿼리 적중 ④ downgrade(인덱스 0)/재upgrade 왕복. **테스트**: 인덱스는 동작 불변(투명·query plan은 hermetic 검증 불가)이라 신규 0 — slice 58 list_my_deletions 통합테스트가 인덱스 적용된 PG서 회귀 통과가 증명. **4게이트 green**: black(98)·ruff·mypy-strict(88)·pytest **1458 passed**(불변·통합 skip=42)·`audit.py` cov 100%. **참고**: `assessment`는 user_id 인덱스 부재(list_my_assessments도 동일 패턴이나 미인덱스) — 별도 갭(범위 밖·후속). **후속**: assessment user 인덱스·audit 보존정책(TTL)/암호화·트리거 search_path 하드닝·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 59 (attempt_event 고아 정리 DB 트리거)** 완료 (메인 직접·라이브 PG16 검증·1458 passed, 2026-06-04) — slice 56 cascade가 남긴 **한계 ① 해소**: session 삭제 → `problem_attempt` ON DELETE CASCADE 삭제 시 그 attempt를 가리키던 `attempt_event`(loose ref·FK 아님·hypertable §6라 FK 불가)가 **고아로 잔존**하던 문제. **해결(DB 트리거)**: `AFTER DELETE ON problem_attempt FOR EACH ROW` 트리거(`whymath_cleanup_attempt_events` 함수 → `DELETE FROM public.attempt_event WHERE attempt_id=OLD.attempt_id`). **핵심 원리**: AFTER DELETE 행 트리거는 *부모 FK CASCADE로 지워지는 자식 행에도 발화* → session 삭제 → attempt CASCADE 삭제 → 트리거가 attempt_event까지 연쇄 전파. **설계 선택(트리거 vs 앱레벨)**: slice 56이 택한 *DB 레벨 cascade*와 일관되게 트리거 — 앱레벨 명시 삭제는 slice 55 generic `_delete_owned_resource`에 도메인 특화 로직을 넣어야 해(session만 해당) 회피. 트리거는 코드베이스 *첫 도입* 패턴. **구현**: alembic `e5f6a7b8c9d0`(`op.execute`로 CREATE FUNCTION/TRIGGER·downgrade는 DROP TRIGGER+FUNCTION·plpgsql·`public.` 스키마 한정). **ORM 변경 0**(순수 DB 트리거). **라이브 PG16 검증**: ① 트리거 전 고아 실측(session 삭제→attempt 0·attempt_event 1 잔존) ② 트리거 후 attempt_event 2건 모두 0(연쇄 정리) ③ `alembic check` 무드리프트(트리거는 autogenerate 비추적·hypertable 변환과 동일 migration-only) ④ downgrade(`pg_trigger` 비어 트리거 제거 확인)/재upgrade 왕복. **테스트**: 통합 +1(`test_cascade_integration.py`·`test_session_delete_cleans_attempt_event_orphans_on_live_pg`·실 PG: attempt_event 2 적재→API session DELETE→0). hermetic은 트리거(DB 강제) 검증 불가→신규 0. **4게이트 green**: black(97·마이그레이션 SQL 포함)·ruff·mypy-strict(88·변경 없음)·pytest **1458 passed**(불변·통합 skip=42). **결정 v1 한계/후속**: ① hypertable에서 attempt_id(비-시간 술어) DELETE는 청크 전수 스캔 — 드문 GDPR 삭제 경로라 수용·대량 시 성능 후속 ② 함수 search_path 미고정(단일 public 스키마라 무해·하드닝 후속) ③ 직접 attempt 삭제 엔드포인트는 없으나(있어도 트리거 동일 동작) 미래 대비. **후속**: audit 보존정책(TTL?)/암호화·트리거 함수 search_path 하드닝·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 58 (GET /v1/me/deletions — 삭제 감사 조회 API)** 완료 (메인 직접·라이브 PG16 검증·1458 passed, 2026-06-04) — slice 57이 적재한 `deletion_audit`를 **본인이 조회**(GDPR 투명성 — 학생이 자기 삭제 이력 확인). slice 57이 후속으로 짚은 "읽기 엔드포인트" 실현. **다른 /me GET과 완전 동형**: `ConsentedUser`·`WHERE user_id==current_user`(타인 차단·미성년 PII 비노출)·최신순(`deleted_at desc`, `audit_id` 안정 2차정렬)·`limit/offset`. 메타만 반환(콘텐츠 없음·slice 57 설계 유지). **구현**: 신규 `schema/audit.py`(`DeletionAudit` Pydantic·`resource_type`은 `AuditResourceType | None`·`use_enum_values`로 "learning_session" 직렬화) + ORM `DeletionAudit`에 `to_schema`/`from_schema`(activity.py 제네릭 `sa.inspect(...).mapper.column_attrs` 패턴 — 모델 공통) + `list_my_deletions` 라우터. **마이그레이션 0**(읽기 전용·스키마 변경 없음 → `alembic check` 무드리프트 재확인). **검증**: 통합 확장(`test_audit_integration.py`에 GET 추가 — 실 PG end-to-end: 삭제→감사 적재→`GET /v1/me/deletions`가 그 행 반환) passed · hermetic +1(`test_deletions_returns_rows`: resource_type enum 값 직렬화·user_id) + `_ENDPOINTS`에 `/v1/me/deletions` 추가해 빈목록·무토큰 401 자동 커버. **4게이트 green**(py3.12 venv): black(96)·ruff·mypy-strict(**88**파일·+schema/audit.py)·pytest **1458 passed**(1457→+1·통합 skip=41)·`api/me.py`·`db/models/audit.py`·`schema/audit.py` cov **100%**·TOTAL 99.88%. **주의(재확인)**: 테스트 ruff는 *루트 설정*(`tests/**=["E501"]` 면제)이 정본 — src/backend 컨텍스트로 테스트 린트 시 기존 라인 E501이 뜨나 무관(CI는 src만 린트). **후속**: attempt_event 고아 정리·audit 보존정책(TTL?)/암호화·deletion audit 관리자(타인) 조회는 범위 밖(본인만)·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 57 (GDPR 삭제 audit log — deletion_audit)** 완료 (메인 직접·라이브 PG16 검증·1457 passed, 2026-06-04) — slice 51~53 본인 리소스 삭제의 *부모 삭제 이벤트*를 append-only 기록(GDPR 삭제권 이행 **증빙**: 누가·무엇·언제). **사용자 지정 후속 2/2 완료**(cascade에 이어 audit). **삭제 콘텐츠는 미저장**(메타만: user_id·resource_type·resource_id·deleted_at) — 미성년 PII 비저촉(CLAUDE.md). **설계 결정**: ① `user_id`는 **FK 아님**(plain UUID) — 사용자 프로필 삭제돼도 감사 로그 *잔존*(compliance 독립성·FK CASCADE/RESTRICT 둘 다 부적합) ② `resource_type`은 `String(32)`(네이티브 PG enum 미생성 — 마이그레이션 단순화·코드 안전성은 Pydantic `AuditResourceType`[learning_session/dialogue/assessment]·`.value` 저장) ③ **부모만 기록** — 자식은 slice 56 DB cascade라 비가시(개별 자식 감사는 앱레벨 명시 삭제 필요 → slice 56 정책 분기 시 트레이드오프로 명시·후속). **구현**: 신규 `DeletionAudit` 모델(`db/models/audit.py`·`__init__` 등록) + `AuditResourceType` enum + alembic `d4e5f6a7b8c9`(create_table·`op.f("pk_deletion_audit")`). `_delete_owned_resource`(slice 55 헬퍼)에 `resource_type` 인자 추가 + 삭제와 **동일 commit**으로 `session.add(DeletionAudit(...))` — 삭제↔감사 원자적(부분 실패 없음). 3 delete 라우터가 도메인별 enum 전달. **라이브 PG16 검증**: `alembic check` 무드리프트(모델↔DB 일치) · `\d deletion_audit` 구조 정확(audit_id gen_random_uuid·user_id **FK 없음**·resource_type varchar(32)·deleted_at now()) · 통합 +1(`test_audit_integration.py`·실 PG end-to-end: `client.delete`→`deletion_audit` 1행[resource_type·resource_id·user_id·deleted_at]·**원본 세션 삭제 후에도 감사 잔존**). **hermetic**: FakeSession에 `add` 메서드+`added` 캡처 추가 → delete 성공 시 audit 1행 적재·**404(미존재/타인)는 미적재** 단언(3 도메인 각). **4게이트 green**(py3.12 venv): black(95)·ruff·mypy-strict(**87**파일·+audit.py)·pytest **1457 passed**(+1 통합 skip=41)·`api/me.py`·`db/models/audit.py` cov **100%**·TOTAL 99.88%. ⚠️ **주의(교훈)**: 테스트 파일은 CI 린트 범위 밖(`ruff/black`은 src `.`만)이라 black-88이 *기존* 라인까지 재포맷 → 내 추가분 외 변경은 되돌림(scope 최소화). **결정 v1 한계/후속**: ① 읽기 엔드포인트 없음(write-only·`GET /v1/me/deletions`는 후속) ② 자식 감사 부재(부모만·DB cascade 비가시) ③ audit 자체 보존정책(무기한? TTL?)·암호화는 후속. **후속**: deletion audit 조회 API·attempt_event 고아 정리·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 56 (GDPR 삭제 cascade 정책 — 직속 CASCADE + SET NULL)** 완료 (메인 직접·라이브 PG16 전수 검증·1457 passed, 2026-06-04) — slice 51/52가 v1 한계로 남긴 *자식 RESTRICT FK → 삭제 시 FK 위반* 해소(사용자 지정 후속 1/2). **정책 결정(사용자 확인)**: 직속 자식 DB `ON DELETE CASCADE` + 깊은 참조는 SET NULL — `problem_attempt.session_id`→CASCADE(세션 삭제→attempt 제거)·`dialogue_turn.dialogue_id`→CASCADE(대화 삭제→turn 제거)·`dialogue.attempt_id`→SET NULL(attempt cascade 삭제 시 대화는 보존·링크만 끊음·과도 삭제 회피). `attempt_event.attempt_id`는 FK 아닌 loose ref(hypertable §6)라 cascade 불가 → 고아 잔존(설계 한계·문서화). **구현**: ORM 3 FK에 `ondelete=` 추가(activity.py·dialogue.py) + alembic `c3d4e5f6a7b8`(각 FK drop_constraint→create_foreign_key, **`op.f()`로 제약명 보존**[naming_convention 재적용 방지]·downgrade는 ondelete 제거 원복). **라우터 코드 무변경** — DB 레벨 정책이라 me.py delete 라우터(slice 55 헬퍼)는 그대로, docstring만 갱신. **라이브 PG16 전수 검증**(postgres 유저·:5432·trust): ① 구조 `pg_constraint.confdeltype` = c/c/n(무관 FK는 a 유지) ② 행동(체인 삽입→DELETE): session 삭제→attempt 0·dialogue 보존·attempt_id NULL; dialogue 삭제→turn 0 ③ `alembic check` 무드리프트(ondelete는 autogenerate 미비교라 drift 0 정상) ④ downgrade-1(a,a,a)→재upgrade(c,c,n) 왕복. **테스트**: 통합 +1(`test_cascade_integration.py`·`@integration` 기본 skip·실 PG end-to-end **API DELETE→cascade**: raw SQL 체인 적재→`client.delete`→count 검증·멱등 정리) — 라이브 PG서 1 passed. hermetic은 FakeSession이라 FK cascade 검증 불가(DB 강제)→신규 hermetic 0. **4게이트 green**(py3.12 venv): black(93)·ruff·mypy-strict(86)·pytest **1457 passed**(+1 통합 skip=40)·TOTAL 99.88%(마이그레이션은 cov 범위 밖·ORM FK 라인은 모델 import로 커버). **결정 v1 한계/후속**: ① attempt_event 고아(loose ref — 앱레벨 정리 또는 트리거 후속) ② cascade는 *DB 비가시*(삭제된 자식 수가 앱에 안 보임) → **audit log(다음 후속)**는 앱레벨 명시 삭제와 통합해야 자식별 감사 가능(정책 분기 시 논의됨) ③ Assessment는 자식 없어 무관. **후속**: lifecycle delete audit log(GDPR·사용자 지정 잔여 2/2)·attempt_event 고아 정리·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 55 (me lifecycle 제네릭 헬퍼 추출 — 6 라우터 중복 제거)** 완료 (메인 hermetic·1457 passed 불변, 2026-06-04) — slice 53 후속(자체 명시 "generic helper 추출 가치 본격 평가 가능")·**분할 머지 트레인(PR #24~28)으로 슬라이스 1~54가 main에 landing된 뒤 첫 새 슬라이스**. 슬라이스 50~53의 end/complete·delete 6 라우터가 *동형 반복*(fetch→소유검증→404→commit)하던 중복을 `api/me.py`에 제네릭 헬퍼로 추출: `_get_owned_or_404`(공통 PK조회+소유검증+404 비누설)·`_close_owned_resource`(close_field=ended_at/completed_at를 now·idempotent)·`_delete_owned_resource`(삭제·204). 6 라우터는 헬퍼 1줄 호출로 축소 — **동작·응답 불변(순수 리팩터)**. **mypy strict 충돌 해소(slice 53이 짚은 핵심 위험)**: *제약(constrained) TypeVar* `_LifecycleRow = TypeVar(_, LearningSession, Dialogue, Assessment)` — 세 ORM만 허용해 ① `row.user_id`(셋 다 `Mapped[uuid.UUID|None]` 보유)·② 반환 `row.to_schema()`(호출자가 `model` 인자로 구체 타입 추론 → 도메인별 스키마 반환)가 타입 안전. Protocol+`Mapped`는 instance/descriptor 혼동(`!=`가 ColumnElement로 보이는 등) 위험이라 회피, 제약 TypeVar가 정답. close 컬럼은 도메인마다 달라(ended_at vs completed_at) **필드명을 인자로** 받아 `getattr`/`setattr`(인자 *변수*라 ruff B009/B010 미해당 — 리터럴 아님). **검증 방식**: 순수 리팩터라 신규 테스트 0 — 기존 slice 50~53 lifecycle 테스트(404·idempotent·204·타인 소유)가 그대로 통과하는 것이 *동작 불변 증명*이자 헬퍼 100% 커버. **4게이트 green**(py3.12 venv·CI 동일): black·ruff·mypy-strict(86파일·제약 TypeVar+`session.get` 통과)·pytest **1457 passed**(불변·신규 0)·`api/me.py` cov **100%**(71 stmts·4 branch·0 miss)·TOTAL 99.88%. **워크플로**: 새 main(`e53dd4e`) 기준 새 브랜치(`claude/slice-55-generic-helper`)→PR로 CI 검증 예정(트레인 이후 PR-per-slice 정착). **결정 v1 한계/후속**: ① 6 라우터는 HTTP 표면이라 여전히 6개(경로·메서드·response_model 상이) — 합칠 수 없고 *로직 중복만* 제거(올바른 해석) ② 제약 TypeVar는 세 ORM 하드코딩 — 새 도메인 추가 시 TypeVar 확장 필요(명시적·의도적) ③ cascade(자식 RESTRICT FK)·audit log는 별도 후속(사용자 지정 잔여 2종). **후속**: cascade 정책(LearningSession·Dialogue 자식 처리)·lifecycle delete audit log(GDPR)·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 54 (InMemoryDeviceStore 단조 카운터 — 윈도우 시간 해상도 격리)** 완료 (메인 hermetic·1452→1457 passed, 2026-06-03) — 슬라이스 21~49 디바이스 자격증명 store의 정렬·시간창(slice 41/43/44/46/47)이 `datetime.now(UTC)`에 의존하던 한계 해소. **문제**: `datetime.now(UTC)`는 플랫폼별 해상도가 거칠어(예: Windows ~15ms) 빠른 연속 `register`가 *동일* created_at을 받을 수 있고, 그러면 `list_for_user` 정렬이 ① dict 삽입순서(구현 세부)에 암묵 의존 ② desc일 때 Python stable sort가 동률을 등록 순서 그대로 둬 "최신 먼저" 의미와 어긋남. **해결(단조 카운터)**: `_CredRecord`에 `seq:int` 추가 + `__init__`에 `itertools.count()` 발급기 + `register`가 `seq=next(self._seq)` 부여(시계 무관 *논리적 등록 순서*) + `list_for_user`를 `(cred, DeviceInfo)` 쌍으로 리팩터해 2차 정렬키 `(getattr(info, order_by), seq)`를 *정렬 방향과 같은 방향*으로 적용(desc=나중 등록 먼저·asc=먼저 등록 먼저) + `reset`이 카운터 재시작(테스트 결정론). **null 그룹은 시간값 없어 등록순서(dict 삽입) 보존** — slice 47 동작 그대로(seq로 뒤집지 않음). **범위 결정(제목 명시)**: InMemory만 — Pg `list_for_user`는 단일 컬럼 `ORDER BY`라 동률 순서 불안정(2차 키 없음·SQL stable 미보장) → **Pg tie-break parity는 후속**(sequence 컬럼·마이그레이션 동반). DeviceInfo·라우터·PgDeviceStore·기존 `_replace` 호출처 무변경(seq는 내부 전용·HTTP 표면 불변·`_replace`가 seq 자동 보존). **테스트** +5 hermetic(`TestInMemoryDeviceStoreMonotonicOrdering`): created_at 동일강제(`_replace`로 충돌 시뮬·seq 보존) 후 desc→[d3,d2,d1]·asc→[d1,d2,d3]·last_used_at 동률 tie-break(desc=나중 등록)·null 그룹 등록순서 보존·reset 후 카운터 재시작 정렬. **⚠️ 발견·교정(기존 드리프트, 별도 style 커밋)**: CI-faithful `black --check .`(src 전체) 실행 중 slice 23 산출 `alembic/versions/…device_credential.py`가 black 26.5.1 기준 dirty(`drop_index` 3줄→1줄)임을 발견 — 최근 슬라이스(24~53)가 `whymath_backend` *패키지만* 검증해 놓쳤고 이 브랜치 CI 마지막 런이 2026-05-28(이후 푸시는 CI 미발화)이라 가려져 있었음. format-only 교정(의미 0)·`black --check .` green 회복. **교훈**: 슬라이스 검증의 black/ruff는 *패키지가 아닌 `.`(src 전체, alembic 포함)*로 — CI 스코프와 일치. **4게이트 green**(py3.12 venv·CI 동일): black(src 92파일 ll=100·tests ll=88)·ruff·mypy-strict(86파일)·pytest **1457 passed**(1452→+5, cov-fail-under=70)·`_device_store.py` cov **100%**(311 stmts·84 branch·0 miss)·TOTAL 99.88%. **결정 v1 한계**(후속): ① Pg tie-break parity(sequence 컬럼·마이그레이션 — InMemory/Pg 동률 순서 일치) ② seq는 프로세스 로컬(재시작 시 0부터 — InMemory는 재시작 시 전체 분실이라 무해) ③ `CachedDeviceStore`는 `list_for_user`를 inner 위임이라 자동 적용(별도 변경 0). **후속**: Pg 안정 정렬키·세션 lifecycle generic helper 추출(slice 53 후속)·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 53 (Assessment complete + delete · `PATCH/DELETE /v1/me/assessments/{id}`)** 완료 (메인 hermetic·1445→1452 passed, 2026-06-03) — 슬라이스 50(end)·51(delete)·52(Dialogue) 패턴을 Assessment 도메인에 답습. *이식 비용 minimization* invariant **4회차 검증**(slice 50: device→coach·slice 51: end→delete·slice 52: LearningSession→Dialogue·slice 53: Dialogue→Assessment). **세 도메인 lifecycle 완비** — LearningSession·Dialogue·Assessment 모두 end/complete + delete 결선. 인프라 추가 0 — FakeSession의 get/delete/commit·`_client` tuple 반환·`get_map` 인자 *그대로 재사용*. 라우터 두 함수 추가만(`+16 LOC`). **명칭 분리**: Assessment는 컬럼이 `ended_at`이 아니라 `completed_at` — 진단은 "종료"보다 "완료"가 의미상 맞아 라우터도 `/complete`로 명명(slice 50/52의 `/end`와 분리). 의미·메커니즘은 50/52 동형. **신규 라우터 2종** in `api/me.py`: ① `PATCH /v1/me/assessments/{assessment_id}/complete` → AssessmentSchema 응답·idempotent complete(이미 완료된 assessment는 completed_at 보존·commit 안 함)·404 미존재/타인 소유 ② `DELETE /v1/me/assessments/{assessment_id}` → 204 No Content·GDPR 영구 삭제·404 미존재/타인 소유. 두 라우터 모두 ConsentedUser 게이트·`session.get(Assessment, assessment_id)` PK 조회·`row.user_id != user.user_id` 본인 소유 검증. **Assessment는 자식 테이블 없음** — LearningSession(ProblemAttempt·DialogueTurn)·Dialogue(DialogueTurn)와 달리 Assessment를 FK 참조하는 자식 테이블이 없음 → DELETE 시 FK 위반 우려 0(slice 51 한계 ① 비적용·도메인 별 특성 차이). **테스트** (hermetic only) **+7 케이스** (`TestAssessmentLifecycle` 클래스·slice 50/51 테스트 4+3=7 패턴 답습): ① complete fresh → completed_at 채움+commit 1 ② idempotent already_completed → 변경 0+commit 0 ③ complete 404 미존재 ④ complete 404 타인 + completed_at 미수정+commit 0 ⑤ delete 204 own + fake.deleted 캡처+commit 1 ⑥ delete 404 미존재 ⑦ delete 404 타인 + row 그대로(`row.assessment_id in fake._get_map`). `_assessment_row` 헬퍼는 슬라이스 50/52의 `_session_row`/`_dialogue_row` 패턴 동형(uuid4·AssessmentSchema 생성·optional completed_at). **4게이트 green**: black(--config 명시·src 100·tests pyproject 정합)·ruff(CI 스코프)·mypy-strict(86 파일·`session.get(Assessment, assessment_id)`·`row.to_schema()` 타입 안전·`response_model=AssessmentSchema` 정합)·pytest **1452 passed**(1445→+7 hermetic, cov-fail-under=70). 신규 cov **100%**: `me.py` Stmts 82/Branch 16/Miss 0(slice 52 66 → +16 assessment 두 라우터). 전체 cov 99.88%. **결정 v1 한계**(후속): ① **3 도메인 모두 lifecycle 완비** — slice 50/51/52/53이 LearningSession·Dialogue·Assessment 완비. 새 도메인은 사실상 없음(모든 본인 데이터 도메인 lifecycle 결선) ② **각 도메인 코드 중복** — end/complete/delete 패턴이 세 도메인에 동일 반복(slice 52 한계 ② 그대로). 4회차 답습 검증으로 *generic helper 추출* 가치 본격 평가 가능(예: `_close_owned_resource(model, pk, owner, field)`·`_delete_owned_resource(model, pk, owner)`. 라우터 6→2 함수로 압축 가능하나 동적 dispatch가 mypy strict와 충돌 위험 — 후속 ③ **명칭 분리**(end vs complete)가 *Pattern Library 일관성*과 *도메인 의미*의 균형 — 라우터 경로 명사화는 클라이언트 친화이나 함수 추출 시 challenge ④ **lifecycle event audit log 부재** — 슬라이스 50/51/52 한계 ④ 그대로. GDPR 삭제 audit이 compliance 핵심·후속 자명. **🎯 의미**: 본인 데이터 self-service 표면이 **세 도메인 lifecycle 완비**(LearningSession·Dialogue·Assessment) — Flutter "학습/대화/진단 종료 + 삭제" 6 UI 액션 결선. 슬라이스 50/52의 *이식 비용 minimization* invariant **4회차 검증** — 새 도메인 추가가 라우터 두 함수만으로 가능(인프라·테스트 헬퍼 0 추가). 슬라이스 24/50/51/52/53이 결합해 *동형 패턴 라이브러리* 형성·다음 단계(generic helper 추출) 가치 본격 평가 가능. **🎯 rate limit + device-cred 39 + coach 4 = 43 슬라이스 누적**. **후속**: 3 도메인 generic helper 추출(end/complete/delete 6 라우터 → 2 함수·dispatch 패턴)·자식 cascade 정책 결정(LearningSession·Dialogue)·lifecycle event audit log/metric·turn 상세 노출·KMS envelope·secret 회전·nonce 재생 방어·`/v1/admin/*` 엔드포인트·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 52 (Dialogue end + delete · `PATCH/DELETE /v1/me/dialogues/{id}`)** 완료 (메인 hermetic·1438→1445 passed, 2026-06-03) — 슬라이스 50(end)·51(delete) 패턴을 Dialogue 도메인에 *완전 동형* 답습. *이식 비용 minimization* invariant **3회차 검증**(slice 50: device→coach·slice 51: end→delete·slice 52: LearningSession→Dialogue). 인프라 추가 0 — FakeSession의 get/delete/commit·`_client` tuple 반환·`get_map` 인자 *그대로 재사용*. 라우터 두 함수 추가만(`+16 LOC`). **신규 라우터 2종** in `api/me.py`: ① `PATCH /v1/me/dialogues/{dialogue_id}/end` → DialogueSchema 응답·idempotent end(이미 종료된 dialogue는 ended_at 보존·commit 안 함)·404 미존재/타인 소유 ② `DELETE /v1/me/dialogues/{dialogue_id}` → 204 No Content·GDPR 영구 삭제·404 미존재/타인 소유. 두 라우터 모두 ConsentedUser 게이트·`session.get(Dialogue, dialogue_id)` PK 조회·`row.user_id != user.user_id` 본인 소유 검증. **자식 RESTRICT FK 한계**: DialogueTurn이 Dialogue를 FK 참조(RESTRICT 기본) — 자식 존재 시 DB FK 위반(slice 51 한계 ① 그대로 dialogue 도메인에도 적용). **테스트** (hermetic only) **+7 케이스** (`TestDialogueLifecycle` 클래스·slice 50/51 테스트 4+3=7 패턴 완전 답습): ① end fresh → ended_at 채움+commit 1 ② idempotent already_ended → 변경 0+commit 0 ③ end 404 미존재 ④ end 404 타인 + ended_at 미수정+commit 0 ⑤ delete 204 own + fake.deleted 캡처+commit 1 ⑥ delete 404 미존재 ⑦ delete 404 타인 + row 그대로(`row.dialogue_id in fake._get_map`). `_dialogue_row` 헬퍼는 슬라이스 50의 `_session_row` 패턴 동형(uuid4·DialogueSchema 생성·optional ended_at). `test_me.py`에 *top-level* `from datetime import UTC, datetime` import 추가(slice 52 helper에서 사용·slice 50의 local import 패턴 정리·이번 helper들이 공유 사용). **4게이트 green**: black·ruff·mypy-strict(86파일·`session.get(Dialogue, dialogue_id)`·`row.to_schema()` 타입 안전·`response_model=DialogueSchema` 정합)·pytest **1445 passed**(1438→+7 hermetic, cov-fail-under=70). 신규 cov **100%**: `me.py` Stmts 66/Branch 12/Miss 0(slice 51 50 → +16 dialogue 두 라우터). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **3 도메인 중 Assessment만 남음** — slice 50/51/52가 LearningSession·Dialogue 완비·Assessment(another 본인 데이터 도메인) 같은 패턴 *자명* 추가 가능(다음 슬라이스 후보 1순위) ② **각 도메인 코드 중복** — end/delete 패턴이 두 도메인에 동일 반복. 3 도메인까지 가면 *generic helper 추출* 가치 평가(예: `_end_owned_resource(model, pk, owner)`). 현재는 명시 우선 ③ **자식 RESTRICT FK 한계 그대로** — 슬라이스 51 한계 ①. cascade 정책 결정은 별 슬라이스 ④ **lifecycle event audit log 부재** — 슬라이스 50/51 한계 ③. GDPR 삭제 audit이 compliance 핵심·후속 자명. **🎯 의미**: 본인 데이터 self-service 표면이 **두 도메인 lifecycle 완비**(LearningSession·Dialogue) — Flutter "학습 종료 + 삭제"·"대화 종료 + 삭제" 두 UI 동시에 결선. 슬라이스 50의 *이식 비용 minimization* invariant **3회차 검증** — 새 도메인 추가가 라우터 두 함수만으로 가능(인프라·테스트 헬퍼 0 추가). 슬라이스 24/50/51/52가 결합해 *동형 패턴 라이브러리* 형성. **🎯 rate limit + device-cred 39 + coach 3 = 42 슬라이스 누적**. **후속**: Assessment end+delete(자명·1순위 후보)·3 도메인 generic helper 추출·자식 cascade 정책·lifecycle event audit log/metric·turn 상세 노출·KMS envelope·secret 회전·nonce 재생 방어·`/v1/admin/*` 엔드포인트·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 51 (`DELETE /v1/me/sessions/{id}` · GDPR 세션 영구 삭제)** 완료 (메인 hermetic·1434→1438 passed, 2026-06-03) — 슬라이스 50의 *대칭 연산*(end ↔ delete) + GDPR 데이터 삭제권(Right to Erasure·CLAUDE.md "미성년자 개인정보 분석·마케팅 외부 공유 금지" 정합) 자명 결선. 슬라이스 24(본인 소유 검증·404 비누설)·슬라이스 50(404 패턴·FakeSession 인프라) *완전 답습* — 인프라 추가 0(`delete()` 메서드만 FakeSession에 +6 LOC). 도메인 전환(slice 50)의 *이식 비용 minimization* invariant 2회차 검증. **신규 라우터 `DELETE /v1/me/sessions/{session_id}`** in `api/me.py`: ConsentedUser 게이트 + `session.get(LearningSession, session_id)` PK 조회 + `row.user_id != user.user_id` 본인 소유 검증(404 if not) + `await session.delete(row)` + `await session.commit()`. **204 No Content** 반환(RESTful·DELETE 표준·응답 body 0). **404 정책 — slice 24/50 답습**: 미존재(`row is None`)와 타인 소유 *모두 404*. 슬라이스 50과 *완전 동형* 분기. **FakeSession.delete 확장**: `async def delete(obj)` 메서드 추가·`self.deleted: list[Any]` 캡처·*PK로 `get_map`에서도 제거*해 후속 `get()`은 None 반환(idempotent 의미 검증 가능). `_client`의 `fake` 반환은 슬라이스 50과 그대로 — `commits`+`deleted` 둘 다 검증 가능. **테스트** (hermetic only) **+4 케이스**: ① **`test_delete_own_session_returns_204`**(204 + 응답 body 0 + `fake.deleted == [row]` + commit 1·DELETE 액션 핵심 검증) ② **`test_delete_nonexistent_returns_404`**(미존재 → 404 + delete 0 + commit 0) ③ **`test_delete_other_users_session_returns_404`**(타인 소유 → 404 + `fake.deleted == []` + `row.session_id in fake._get_map`로 *행 그대로 존재* 확인 — 상태 불변 invariant) ④ **`test_delete_then_get_returns_404`**(*DELETE의 "after" 의미*: 첫 호출 204·두 번째 호출 404·*idempotent와 다른 의미* 명시. DELETE의 영구성·자원 재발견 불가 패턴). **4게이트 green**: black·ruff·mypy-strict(86 파일·`session.delete(row)` 동기 형태 vs `await` 정합·204 응답 schema 정합·response_model 없음)·pytest **1438 passed**(1434→+4 hermetic, cov-fail-under=70). 신규 cov **100%**: `me.py` Stmts 50/Branch 6/Miss 0(slice 50 43 → +7 DELETE 라우터). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **자식 행 RESTRICT FK 위반** — `ProblemAttempt`·`AttemptEvent`·`DialogueTurn` 등이 LearningSession을 FK로 참조(DDL RESTRICT 기본). 자식 행 존재 시 *DB가 FK 위반*으로 500 반환. 의도: ① 자식 cascade(`ON DELETE CASCADE` migration·*위험: 데이터 silent 손실*) ② 자식 사전 삭제 라우터(`DELETE /v1/me/sessions/{id}?cascade=true`·명시) ③ 자식 *없을 때만 허용* 의도된 v1(GDPR 요청은 *모든* 자식 삭제 요청에 묶이므로 별 처리) ② **통합 테스트 없음** — 슬라이스 50 한계 ① 그대로. test_me_integration.py 확장 후속 ③ **DELETE는 *비가역*** — `ended_at` PATCH와 달리 *영구 삭제*. 운영에서 audit log 추가 권장(별 metric 후속·slice 28 패턴 답습 가능) ④ **소프트 삭제 미지원** — `deleted_at` 컬럼 + 필터링(`WHERE deleted_at IS NULL`) 패턴 미적용. GDPR은 *영구 삭제*가 정답이라 의도된 선택. **🎯 의미**: coach LearningSession 도메인이 *기본 lifecycle 완비* — 등록(POST·slice 12·existing)·종료(PATCH·slice 50)·삭제(DELETE·slice 51). 사용자 데이터 self-service가 *device 도메인과 같은 수준*(slice 22 register·24 revoke·29 list·37/41/43/44 필터). Flutter UI는 "학습 종료 + 삭제" 두 액션을 *2 endpoint 호출*로 결선 가능. GDPR 데이터 삭제권 첫 결선·후속 도메인(Assessment·Dialogue) 같은 패턴 자명 이식. **🎯 rate limit + device-cred 39 + coach 2 = 41 슬라이스 누적**. **후속**: 자식 cascade 정책 결정·세션 종료/삭제 metric·DELETE audit log·Assessment 종료/삭제·Dialogue 종료/삭제·turn 상세 노출·KMS envelope·secret 회전·nonce 재생 방어·`/v1/admin/*` 엔드포인트·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 50 (`PATCH /v1/me/sessions/{id}/end` · 학습 세션 종료 · *도메인 전환*)** 완료 (메인 hermetic·1430→1434 passed, 2026-06-03) — **29 슬라이스 device-cred 마라톤 후 *도메인 전환***(coach LearningSession 도메인). 슬라이스 22~49가 device-cred에 집중되어 *시간 차원 정렬·NULL 위치·per-key TTL* 같은 *5-디지트 LOC* 미세 개선이 주를 이뤘으나 *coach 도메인은 핵심 UX 결손 다수* 잔존 — 첫 시작으로 "세션 완료 기록 없음·분석 불가"를 해소. Flutter "학습 종료" 버튼·완료 vs 중단 세션 분석·세션 평균 길이 같은 학습 분석 기반 마련. 슬라이스 24(본인 소유 검증·404 비누설)와 슬라이스 50의 패턴 *완전 답습* — device 도메인 학습이 coach 도메인에 *비용 0 이식*. **신규 라우터 `PATCH /v1/me/sessions/{session_id}/end`** in `api/me.py`: ConsentedUser 게이트 + `session.get(LearningSession, session_id)` PK 조회 + `row.user_id != user.user_id` 본인 소유 검증 + `if row.ended_at is None: row.ended_at = datetime.now(UTC); await session.commit()` 조건부 갱신 + `row.to_schema()` 응답. **404 정책 — slice 24 답습**: 미존재(`row is None`)와 타인 소유(`row.user_id != user.user_id`) *모두 404*. 존재 여부 비누설·session_id 열거 공격 차단(클라이언트가 다른 사용자 session_id 시도해도 둘 다 같은 응답 모양). **idempotent**: 이미 종료된 세션(`ended_at is not None`) 재호출도 200 + 기존 시각 보존 + commit 안 함. 클라이언트가 *동일 액션을 안전 재시도* 가능(네트워크 재시도·중복 클릭 안전). **FakeSession 확장**(테스트 인프라): `get(model, pk)` + `commit()` 메서드 추가·`get_map: dict[uuid.UUID, Any]` 생성자 인자로 PK 매핑 주입. `_client`가 `tuple[TestClient, FakeSession]` 반환으로 변경 — commit 횟수·row 상태 변경 검증 가능(슬라이스 24 PgDeviceStore 테스트 패턴 동형). **테스트** (hermetic only) **+4 신규 + 5 갱신**: ① **`test_ends_fresh_session`**: 미종료 세션 → ended_at 채움·commit 카운트 1·응답에 `ended_at` not null. ② **`test_idempotent_already_ended`**: 이미 종료된 세션 → ended_at 시각 변경 없음·commit 0(idempotent invariant). ③ **`test_nonexistent_returns_404`**: 미존재 session_id → 404. ④ **`test_other_users_session_returns_404`**: 타인 소유 session_id → 404 + `row.ended_at` 미수정 + commit 0(정보 비누설 + 상태 불변). 갱신: 기존 5 테스트(test_sessions_returns_rows·test_assessments_returns_rows·test_dialogues_returns_rows·test_empty_lists·test_pagination_out_of_range_422)는 `_client` 시그너처 변경(tuple unpack `client, _`)에 정합. **4게이트 green**: black(test_me.py 재포맷)·ruff·mypy-strict(86 파일·`session.get(LearningSession, session_id)` 타입 안전·`row.user_id != user.user_id` 비교 정합)·pytest **1434 passed**(1430→+4 hermetic, cov-fail-under=70). 신규 cov **100%**: `me.py` Stmts 43/Branch 4/Miss 0(slice 49 30 → +13 PATCH 라우터). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **통합 테스트 없음** — `_FakeSession`이 SQL 시뮬·실 PG 쿼리 검증은 후속(test_me_integration.py 확장). 슬라이스 23+의 `_FakePgSession` 패턴은 `select` 처리 위주·get/commit은 잘 처리되나 통합으로 PG ORM 실 동작 확인 권장 ② **PATCH 시 returned schema 정밀도** — `row.to_schema()`가 LearningSession 전체 필드 반환·일부 필드(`focus_concepts` 등)는 미설정이면 빈 list/None. UI 친화성 후속(`response_model_exclude_none` 활용 가능) ③ **세션 종료 metric 없음** — slice 28 `_device_metrics` 같은 카운터(예: "ended_sessions_per_day") 미적용. 학습 분석 위해 후속 도입 자명 ④ **재오픈 미지원** — 이미 종료된 세션을 다시 *활성* 상태로 되돌리는 옵션 없음(`ended_at = None` 설정 불가). 의도된 제약(완료 표시는 *영구*·학습 데이터 무결성) — 새 세션 시작이 정상 흐름. **🎯 의미**: device 도메인의 *깊은 학습*(슬라이스 22~49의 본인 소유 검증·404 비누설·idempotent·FakeSession 인프라)이 **coach 도메인에 비용 0 이식** — 슬라이스 50은 새 도메인이나 인프라·패턴은 100% 재사용. 도메인 전환의 *비용 minimization* 사례. Flutter "학습 종료" 버튼이 즉시 구현 가능(클라이언트는 단일 PATCH 호출·idempotent라 안전 재시도). 학습 분석(평균 세션 길이·완료율·중단 패턴) 데이터 기반 마련. **🎯 rate limit + device-cred 39 슬라이스 + coach 1 슬라이스 = 40 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·세션 종료 metric·세션 재오픈(요구 시)·세션 삭제(`DELETE /v1/me/sessions/{id}`·GDPR)·turn 상세 노출·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 49 (`CachedDeviceStore.count_all_ttl_seconds` · per-key TTL · active vs all)** 완료 (메인 hermetic·1428→1430 passed, 2026-06-03) — 슬라이스 48 결정 v1 한계 ②(`device_count:{uid}:active`와 `:all` 두 키가 동일 TTL·`include_revoked=True` 쿼리가 훨씬 드물어 별 TTL 가능) 자명 해소. `include_revoked=True` count(폐기 이력 포함·`:all` 키)는 *훨씬 드물게* 조회됨 — 주로 보안 감사·관리자 대시보드·"내 device 전체 통계" 같은 *비주류* UX. active count(매 GET /v1/devices 호출 시·include_total=true)는 정상 사용 빈도 높음. → 두 키 별 TTL로 cache hit rate *각각 최적화*. 슬라이스 48의 `count_ttl` → `count_all_ttl` 패턴 *완전 답습*. **신규 Settings** `device_count_all_cache_ttl_seconds: int | None = Field(default=None, ge=0)`. None 기본은 `device_count_cache_ttl_seconds` 그대로 사용(slice 48 backward compat). 운영 보고로 active vs all 사용 빈도 차이 측정 후 조정. **CachedDeviceStore.__init__ 시그너처 확장**: `count_all_ttl_seconds: int | None = None` keyword-only 인자. 폴백 chain: `count_all_ttl_seconds → self._count_ttl → ttl_seconds`(slice 48 `count_ttl_seconds → ttl_seconds`의 확장). `self._count_all_ttl` 저장. **`count_for_user` SETEX 갱신**: `ttl = self._count_all_ttl if include_revoked else self._count_ttl` 한 줄 분기로 *키별 TTL 결정*. `:all` 키와 `:active` 키가 *완전 독립* TTL 정책. **`build_device_store_from_settings`**: pg_cached 모드에서 `count_all_ttl_seconds=settings.device_count_all_cache_ttl_seconds` 전달(Settings의 세 TTL이 세 다른 캐시 path로 직접 전달). **테스트** (hermetic only) **+2 케이스**: ① **`test_count_all_ttl_separate_from_count_ttl`**: `count_ttl_seconds=300, count_all_ttl_seconds=600`로 store 생성·`include_revoked=False` count → `:active` 키에 300·`include_revoked=True` count → `:all` 키에 600·*두 TTL 완전 독립* 검증. ② **`test_count_all_ttl_falls_back_to_count_ttl_when_unspecified`**: `count_all_ttl_seconds` 생략(=None)·`count_ttl_seconds=300`만 지정 → `:all` 키도 300 사용(slice 48 backward compat invariant). **4게이트 green**: black·ruff·mypy-strict(86 파일·`int | None` 시그너처·`getattr` 기반 폴백 chain 정합)·pytest **1430 passed**(1428→+2 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 306/Branch 84/Miss 0(slice 48 304 → +2 폴백 분기 + 키별 TTL 선택) · `config.py` Stmts 60/Miss 0(slice 48 59 → +1 Settings 필드). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **두 TTL만 — 시간 필터 우회 경로엔 TTL 없음** — 시간창 필터(slice 41/43/44) 캐시 우회 시엔 inner.count_for_user 직접 호출·캐시 안 함. quantize 캐시(시간창 양자화)는 별 슬라이스 후속 ② **count_ttl_seconds=0 시 SETEX 즉시 만료 미테스트** — Settings는 ge=0 허용·SETEX 0초는 즉시 expire 의미(=캐시 비활성)이나 미검증 ③ **cache stampede 미해결** — 슬라이스 48 한계 ④ 그대로. TTL 길어도 동시 만료 시 stampede 동일. singleflight·확률적 조기 갱신 후속 ④ **운영 측정 도구 없음** — 실제 `:active` vs `:all` 호출 빈도는 별 metric 없이는 측정 불가. OTel meter 도입 후 *사용 빈도 기반 TTL 자동 조정* 가능. **🎯 의미**: cache 정책이 **3차원 세분화** — verify TTL(60s·재요청 빈도 최대)·count active TTL(300s·중간)·count all TTL(잠재 600s+·최저). 같은 invariant(register/revoke 즉시 invalidate 두 키 모두) 보존하면서 *데이터 특성에 맞춰 TTL 정책 미세 조정*. 슬라이스 26(verify)·40(count 추가)·48(count vs verify 분리)·49(count active vs all 분리) 4 슬라이스가 결합해 *cache TTL 정책 완비*. 운영자가 `WHYMATH_DEVICE_COUNT_ALL_CACHE_TTL_SECONDS=600` 환경변수로 실측 기반 튜닝. **🎯 rate limit + device-cred 39 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·시간창 quantize 캐시·cache stampede·OTel meter·`/v1/admin/devices/cleanup-stale` 엔드포인트·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 48 (`CachedDeviceStore.count_ttl_seconds` · count 캐시 TTL 분리)** 완료 (메인 hermetic·1426→1428 passed, 2026-06-03) — 슬라이스 40 결정 v1 한계 ④(verify와 count 캐시 TTL 공유·count의 *덜 자주 갱신* 특성 미활용) 자명 해소. count는 verify와 다른 갱신 패턴: ① verify는 *매 요청* 발화·캐시 hit rate 핵심 ② count는 *register/revoke 시만* 변경(verify 영향 0)·갱신 빈도 훨씬 낮음. 따라서 count는 *더 긴 TTL*로 cache hit rate 향상 + register/revoke의 즉시 invalidate가 stale 방지(slice 40 invariant 유지). **신규 Settings** `device_count_cache_ttl_seconds: int = 300`(5분·verify 60s의 *5배*). 운영 보고로 점진 조정 가능. **CachedDeviceStore.__init__ 시그너처 확장**: `count_ttl_seconds: int | None = None` keyword-only 인자. None이면 `ttl_seconds`로 폴백(slice 40 backward compat·기존 호출처 무영향). `self._count_ttl = count_ttl_seconds if count_ttl_seconds is not None else ttl_seconds`. **`count_for_user` SETEX 갱신**: 인자가 `self._ttl` → `self._count_ttl`로 변경. verify의 SETEX는 그대로 `self._ttl`(verify TTL) — *두 TTL이 독립*. **`build_device_store_from_settings` 갱신**: pg_cached 모드에서 `count_ttl_seconds=settings.device_count_cache_ttl_seconds` 전달. Settings의 두 TTL이 두 다른 캐시 path로 *직접 전달*. **테스트** (hermetic only) **+2 신규 + 1 갱신**: ① **`test_count_ttl_separate_from_verify_ttl`**: `ttl_seconds=42, count_ttl_seconds=999`로 store 생성·verify → `device_verify:` 키에 42·count → `device_count:` 키에 999·*두 TTL 완전 독립* 검증. ② **`test_count_ttl_falls_back_to_verify_ttl_when_unspecified`**: `count_ttl_seconds` 인자 생략(=None)·`ttl_seconds=33`만 지정 → count도 33 사용(slice 40 backward compat invariant). 갱신: `test_pg_cached_mode_returns_cached_store_and_closes_redis`에 `store._count_ttl == 300` 확인 추가(lifespan 결선 검증). **4게이트 green**: black·ruff·mypy-strict(86 파일·`count_ttl_seconds: int | None = None` 시그너처 정합·`self._count_ttl` 타입 추론)·pytest **1428 passed**(1426→+2 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 304/Branch 84/Miss 0(slice 47 303 → +1 폴백 분기) · `config.py` Stmts 59/Miss 0(slice 47 58 → +1 Settings 필드). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **count_ttl 정확값** — 300s 기본은 *추측*. 실제 운영 사용 빈도 측정 후 조정 가능(매 사용자 GET /v1/devices 빈도·register/revoke 빈도 비교 필요) ② **per-key TTL 미지원** — `device_count:{uid}:active`와 `:all` 두 키가 *동일* TTL. 사용 패턴 다르면(예: include_revoked=True가 훨씬 드물면 더 긴 TTL 가능) 분리 후속 ③ **TTL 0 시 동작** — Setting `ge=0` 허용 — 0이면 SETEX 즉시 만료(*캐시 비활성 효과*). docstring 명시했으나 미테스트 ④ **cache stampede 미해결** — TTL 다르게 해도 동시 만료 시 stampede는 동일. singleflight·확률적 조기 갱신은 별 슬라이스 후속(slice 26 한계 유지). **🎯 의미**: cache 정책이 *데이터 특성에 맞춰* 세분화 — verify는 짧은 TTL(60s·revoke 즉시 반영)·count는 긴 TTL(5분·갱신 빈도 낮음 활용). 같은 invariant(register/revoke 즉시 invalidate) 보존하면서 *cache hit rate 별도 최적화*. 슬라이스 40의 단순 "공유 TTL"에서 *조정 가능 정책*으로 진화. 운영자가 `WHYMATH_DEVICE_COUNT_CACHE_TTL_SECONDS=600` 같은 환경변수로 실측 기반 튜닝 가능. **🎯 rate limit + device-cred 38 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·다중 정렬 컬럼·시간창 quantize 캐시·per-key TTL·cache stampede·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 47 (`GET /v1/devices` `?nulls=first|last` · NULL 위치 명시 제어)** 완료 (메인 hermetic·1422→1426 passed, 2026-06-03) — 슬라이스 46 결정 v1 한계 ①(PG의 `ASC NULLS LAST·DESC NULLS FIRST` 기본과 InMemory의 "항상 LAST" 차이) 자명 해소. *클라이언트 명시 제어*로 두 store 일관성 보장 — Flutter UI가 "아직 verify 안 된 device 먼저 표시"(nulls=first)·"끝에 표시"(nulls=last·기본) 자유 선택. UI 친화 *기본값 last*는 슬라이스 46 시점에 InMemory가 이미 채택한 UX와 정합. **신규 Literal `NullsPosition = Literal["last", "first"]`** in `_device_store.py`. mypy의 Literal 검증·FastAPI Query Literal 자동 검증으로 *이중 안전*(슬라이스 46 OrderByField/OrderDir 패턴 답습). **Protocol 시그너처 확장**: `list_for_user(..., nulls: NullsPosition = "last")` keyword-only. 기본값으로 기존 호출처(라우터·테스트) 무영향. count_for_user는 정렬 무관이라 인자 추가 0. **InMemoryDeviceStore**: 슬라이스 46의 두 그룹 정렬(`not_null + nulls_items`)을 `not_null + nulls_items if nulls == "last" else nulls_items + not_null`로 *한 줄 분기*. 위치만 결정·정렬 알고리즘 동일. **PgDeviceStore**: `.nulls_last()` / `.nulls_first()` SQLAlchemy column expression hint. SQL에 *명시* `NULLS LAST`/`NULLS FIRST` 출력 → PG가 InMemory와 *완전 일관*으로 동작. **CachedDeviceStore**: `nulls` 인자 그대로 inner 위임. **라우터 `?nulls` Query**: `Annotated[NullsPosition, Query(description=...)] = "last"` — FastAPI Literal 자동 검증으로 허용 외 값(예: `middle`) 422. **테스트 인프라 진화 — `_FakePgSession._exec_select` ORDER BY 재귀 unwrap**: 슬라이스 46의 *단일* UnaryExpression 가정에서 *체인 unwrap*으로 확장. `col.desc().nulls_last()` 구조는 outer=UnaryExpression(modifier=nulls_last_op)·middle=UnaryExpression(modifier=desc_op)·inner=column. `while current is not None` 루프로 `.element`(없으면 None) 따라가며 *`isinstance(key_attr, str)`* 발견 시 break(intermediate UnaryExpression 노드의 `.key=None`도 안전 무시·initially `col_name = "created_at"` 폴백). `modifier.__name__`에 'desc'·'nulls_first'·'nulls_last' 부분문자열 매치 dispatch — SQLAlchemy `desc_op`·`nulls_first_op` 명명 규칙 활용. **테스트** (hermetic only) **+4 케이스**: ① **InMemory `test_nulls_first_puts_none_at_start`**: DESC last_used_at + nulls=first → d_never가 *맨 앞*·ASC + nulls=first도 d_never 맨 앞(*방향 무관* invariant). ② **HTTP `test_nulls_query_param_changes_position`**: 기본(미지정 → nulls=last) → d1 먼저·`?nulls=first` → d_never 먼저·라운드트립 검증. ③ **HTTP `test_invalid_nulls_rejected_422`**: `?nulls=middle` → 422(FastAPI Literal 자동 검증). ④ **Pg `test_nulls_first_via_sql_ordering`**: 기본 DESC nulls=last → d1 먼저·nulls=first → d_never 먼저·`_FakePgSession` 재귀 unwrap이 nested UnaryExpression 정확히 처리. **4게이트 green**: black·ruff·mypy-strict(86 파일·`NullsPosition` Literal Protocol 정합·`.nulls_last()`/`.nulls_first()` SQLAlchemy 메서드 인식)·pytest **1426 passed**(1422→+4 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 303/Branch 84/Miss 0(slice 46 301 → +2 nulls 분기 InMemory + Pg .nulls_last/.nulls_first) · `devices.py` Stmts 66/Branch 8/Miss 0(slice 46과 동일 — 새 Query는 기존 라인 내). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **PG/InMemory NULL 의미 미세 차이** — *NULL 자체 정렬*은 동일하나 *NULL 다중 컬럼* 시(예: ORDER BY a, b 둘 다 NULL) PG는 어느 컬럼 기준? InMemory는 first key. SQL `NULLS LAST`는 *single 컬럼 한정* — 다중 컬럼 정렬(slice 46 한계 ②) 도입 시 함께 재검토 ② **불변 정책 — 모든 컬럼 동일 nulls 위치** — `?nulls=first` 시 모든 NULL 컬럼이 first. 컬럼별 개별 지정(예: `last_used_at_nulls=first&revoked_at_nulls=last`)은 후속(드문 use case) ③ **SQL 쿼리 explain plan 영향** — PG 인덱스(slice 23의 created_at 인덱스)가 NULLS 위치에 따라 선택적 사용. `created_at DESC NULLS LAST`는 인덱스 활용·`NULLS FIRST`는 reverse scan 비용 미세. 운영 보고 후 nuance 조정 ④ **fake _row_matches와 _exec_select 일관성** — fake가 ORDER BY를 InMemory 의미(NULL-LAST)에 *항상 일치*해 처리. 만약 PG와 미세 다른 NULL 처리 케이스가 추가되면 fake 분기 확장 필요. **🎯 의미**: device 정렬 표면이 **store 무관 일관 NULL 의미** 도달 — PG·InMemory가 *완전 동일*하게 동작. 클라이언트가 NULL 위치를 *명시 결정*해 UI 디자인(예: "비활성 device를 흐리게 표시·끝으로") 자유. SQLAlchemy `.nulls_last()`/`.nulls_first()` SQL hint·FastAPI Literal 자동 검증·fake 재귀 unwrap이 결합해 *3 계층 일관성*(클라이언트·서버 코드·테스트 인프라). **🎯 rate limit + device-cred 37 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·다중 정렬 컬럼·컬럼별 nulls 지정·@validate_time_filters 데코레이터·시간창 quantize 캐시·count별 TTL·cache stampede·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 46 (`GET /v1/devices` `?order_by`/`?order_dir` · 동적 정렬)** 완료 (메인 hermetic·1414→1422 passed, 2026-06-03) — 슬라이스 43 결정 v1 한계 ②(`order_by` 옵션 없음·`created_at DESC` 고정) 자명 해소. Flutter UI "최근 사용순"(`last_used_at DESC`)·"오래된 등록부터"(`created_at ASC`)·"최근 폐기 순"(`revoked_at DESC` + `include_revoked=true`) 등 정렬 표면 결선. 정렬 컬럼이 *시간 차원 3축*(slice 41/43/44)과 *정합* — 필터 컬럼 set과 정렬 컬럼 set이 같아 직교성 보장. **신규 Literal 타입 2종 in `_device_store.py`**: `OrderByField = Literal["created_at", "last_used_at", "revoked_at"]` + `OrderDir = Literal["asc", "desc"]`. mypy가 Literal 값 검증·라우터의 FastAPI Query Literal 자동 검증과 *이중 안전*. 후속 컬럼 추가(예: `device_id` 사전순) 시 Literal *한 곳만* 확장하면 라우터·Protocol·store 구현체 모두 안전 연동. **Protocol 시그너처 확장**: `list_for_user(..., order_by: OrderByField = "created_at", order_dir: OrderDir = "desc")` keyword-only. 기본값으로 *슬라이스 29 의미 보존*(`created_at` DESC) — 기존 호출처(라우터·테스트) 무영향. count_for_user는 정렬 무관(개수만)이라 인자 추가 0. **InMemoryDeviceStore — NULL-LAST UX 패턴**: `not_null + nulls` 두 그룹 정렬 후 합치는 패턴. `not_null.sort(key=lambda i: getattr(i, order_by), reverse=(order_dir=="desc"))` + `nulls` 그대로 append. *None 값은 asc/desc 무관 항상 끝* — 사용자 친화 UX("아직 사용 안 된 기기는 목록 하단"). 슬라이스 41/43/44의 None 기반 필터 제외 invariant와 *정합*. datetime sentinel 트릭(`or datetime.min`) 회피로 방향 일관성 보장. **PgDeviceStore — 동적 SQL ORDER BY**: `order_col = getattr(DeviceCredential, order_by)` + `order_expr = order_col.desc() if order_dir == "desc" else order_col.asc()` + `stmt.order_by(order_expr)`. SQLAlchemy의 컬럼 메타데이터를 *런타임 접근*해 동적 SQL 생성·정적 분석(mypy)은 Literal로 컬럼명 안전 보장. PG의 NULL 처리는 *SQL 기본*(ASC NULLS LAST·DESC NULLS FIRST)이라 InMemory의 "항상 LAST" UX와는 *DESC 시점 차이*(PG는 FIRST·InMemory는 LAST). slice 46 v1 한계로 명시·후속 NULLS LAST 옵션 추가 자명. **CachedDeviceStore**: `order_by`/`order_dir` 그대로 inner 위임(list_for_user는 캐시 안 함 정책 유지). **라우터 `?order_by`/`?order_dir` Query**: `Annotated[OrderByField, Query(description=...)] = "created_at"` 형식·FastAPI의 Literal 자동 검증으로 허용 외 값은 *422 자동*. **테스트 인프라 진화 — `_FakePgSession._exec_select` ORDER BY 동적 처리**: 슬라이스 29 하드코딩(`created_at` reverse=True)을 *완전 동적*으로 진화. `UnaryExpression.element.key`로 컬럼명·`modifier.__name__`로 방향 추출(SQLAlchemy의 `desc_op`/`asc_op` 명명 규칙). InMemory와 같은 NULL-LAST 정합. **테스트** (hermetic only) **+8 케이스**: ① **InMemory `test_order_by_created_at_default_desc`**(기본 동작 보존·등록 역순) ② **`test_order_by_created_at_asc`**(asc 방향 동작) ③ **`test_order_by_last_used_at_desc`**(last_used_at DESC·d_never None은 *끝*·ASC도 None 끝 invariant 검증) ④ **`test_order_by_revoked_at_desc_include_revoked`**(폐기 device 정렬·시간 차원 결합) ⑤ **HTTP `test_order_by_query_param_changes_order`**(`?order_by=last_used_at&order_dir=asc` 라운드트립) ⑥ **`test_invalid_order_by_rejected_422`**(허용 외 컬럼 `device_id` → 422·FastAPI Literal 자동 검증) ⑦ **`test_invalid_order_dir_rejected_422`**(허용 외 방향 `random` → 422) ⑧ **Pg `test_order_by_dynamic_column_and_direction`**(기본 DESC created_at + ASC created_at + last_used_at DESC dual switch — SQL ORDER BY 발급 정합 + `_FakePgSession` 동적 정렬 처리 검증). **4게이트 green**: black·ruff·mypy-strict(86 파일·`OrderByField`/`OrderDir` Literal Protocol 정합·`getattr(DeviceCredential, order_by)` 동적 attribute 접근 타입 안전)·pytest **1422 passed**(1414→+8 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 301/Branch 84/Miss 0(slice 45 294 → +7 정렬 로직 InMemory + Pg) · `devices.py` Stmts 66/Branch 8/Miss 0(slice 45와 동일 — 새 Query 2 추가는 기존 라인 내). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **NULLS 처리 PG vs InMemory 차이** — PG 기본은 `ASC NULLS LAST·DESC NULLS FIRST`·InMemory는 *항상 LAST*. DESC 시 last_used_at=None 위치가 두 store에 다름. `?nulls=last|first` 옵션 후속·또는 SQL 명시 `NULLS LAST` 추가 ② **단일 정렬 컬럼만** — `order_by=last_used_at,created_at` 다중 컬럼 미지원. 보통 secondary sort로 created_at 같이 쓰면 *결정론적* — 후속 ③ **Literal 확장 시 store 구현 동시 확장** — 새 컬럼 추가는 Literal+InMemory+Pg+테스트 4 곳 변경. 변경 cost 낮으나 mypy로 *자동 발견* invariant ④ **사용자 정의 정렬 불가** — `nickname`이나 `device_label`(향후 도입 시) 정렬은 컬럼 추가 후에야 가능. **🎯 의미**: device 자기 관리 표면이 **시간 차원 정렬 완비** — 시간 3축(slice 41/43/44 필터) + 동적 정렬(slice 46) = 사용자가 *어느 시간 차원으로* 데이터를 정리할지 *자유 선택*. UI는 정렬 chip을 추가만 하면 됨(서버 코드 변경 0). FastAPI Literal 자동 검증 + Python `getattr` 동적 접근으로 *타입 안전 + 코드 간결* 두 가지 가치 동시 달성. **🎯 rate limit + device-cred 36 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·NULLS LAST 옵션·다중 정렬 컬럼·@validate_time_filters 데코레이터·시간창 quantize 캐시·count별 TTL·cache stampede·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 45 (시간창 일관성 검증 `since>until` 422 + `_has_any_time_filter` 추출)** 완료 (메인 hermetic·1406→1414 passed, 2026-06-03) — 슬라이스 44 결정 v1 한계 ①(시간 필터 조합 검증·`since > until`은 빈 결과로 *조용히* 통과·클라이언트 버그 미감지)과 한계 ③(`_has_time_filter` helper 추출·6-OR 분기 비대화) *둘을 한 슬라이스로* 동시 해소. 두 가지 *완전 다른 종류*(입력 검증·코드 구조)의 한계가 같은 시간 필터 표면에서 자연스럽게 묶임. **신규 `_validate_time_window(since, until, since_name, until_name)` in devices.py**: `since > until`이면 `HTTPException(422)`. 메시지 진단성 3요소(슬라이스 42 패턴 답습): ① 두 필드명(`since_name`/`until_name`) ② *실제 ISO 값*(`since.isoformat()`/`until.isoformat()`)으로 클라이언트가 어떤 값을 보냈는지 명확 ③ 이유("since > until은 빈 시간창"). 한쪽만 None이면 검증 skip — *단방향 시간창*(예: "2024년 이후"·`until=None`) 허용 invariant. **3 시간 페어 각각 독립 검증**: `(since, until)`·`(revoked_since, revoked_until)`·`(used_since, used_until)`. 한 페어 OK여도 다른 페어 위반이면 거부 — early-return으로 첫 위반 페어에서 즉시 422(클라이언트는 첫 위반 메시지로 진단). **신규 `_has_any_time_filter(*values: datetime | None) -> bool` in _device_store.py**: 가변 인자 helper. `CachedDeviceStore.count_for_user`의 *6-OR* 캐시 우회 분기(`if since is not None or until is not None or revoked_since is not None or ...`)를 단일 호출 `if _has_any_time_filter(since, until, revoked_since, revoked_until, used_since, used_until)`로 압축. **invariant**: 후속에 시간 필터 추가 시 (예: slice 46+에서 `created_before/after` 다른 형식 도입) helper 호출의 *인자 리스트*만 확장(분기 자체는 1줄 유지). 슬라이스 44 한계 ③("6 OR 조건 → 시간 필터 수 증가 시 분기 비대해짐") 구조적으로 해소. `any(v is not None for v in values)` 한 줄 — 인자 0 개 케이스도 안전(False 반환). **테스트** (hermetic only) **+8 케이스**: ① **`test_since_greater_than_until_rejected_422`**(2024-12-31 > 2024-01-01 → 422·메시지에 'since'·'until' 둘 다 포함) ② **`test_revoked_since_greater_than_until_rejected_422`**(같은 검증이 revoked_since 페어에도 적용·메시지에 'revoked_since' 명시) ③ **`test_used_since_greater_than_until_rejected_422`**(used_since 페어도 동일·페어 독립 검증 invariant) ④ **`test_since_equal_to_until_accepted`**(*경계 케이스* — since == until 순간 시간창 → 200·정확 시간 시점 조회 허용) ⑤ **`test_one_sided_window_accepted`**(since만 또는 until만 → 200·단방향 시간창 허용 invariant) ⑥ **`TestHasAnyTimeFilterHelper.test_all_none_returns_false`**(모든 None → False) ⑦ **`test_any_non_none_returns_true`**(첫 인자 또는 마지막 인자만 not None → True·*변수 위치 무관성*) ⑧ **`test_zero_args_returns_false`**(*edge* — 인자 0 개 호출은 빈 시간 필터·False). **4게이트 green**: black·ruff·mypy-strict(86 파일·`*values: datetime | None` 가변 인자 정합·`_validate_time_window` 단순 시그너처)·pytest **1414 passed**(1406→+8 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 294/Branch 84/Miss 0(slice 44 292 → +2 helper 함수) · `devices.py` Stmts 66/Branch 8/Miss 0(slice 44 60 → +6 검증 함수+3 호출). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **timezone 비교 정확성** — `since > until` 비교는 두 datetime이 *같은 정밀 zone*이라는 가정. slice 42 검증으로 둘 다 tz-aware 보장이라 안전·다만 zone 변환 비용은 비교 시 매번 발생(미세 미세 성능) ② **사용자 친화 메시지 i18n 없음** — 슬라이스 42 한계 ①과 동일. 한국어 고정 ③ **3 페어 × 2 검증 함수 = 6 호출** — 라우터에서 6 줄. 데코레이터 패턴(`@validate_time_filters(...)`)으로 일반화 가능하나 *명시성 손실* 트레이드오프 ④ **변수명 reflection** — 함수가 `since_name="since"` 같은 *문자열* 인자 받음(자동 추출 안 함). Python 3.13+ `inspect`로 자동 추출 가능하나 현재 비용 낮음. **🎯 의미**: 시간 필터 표면이 *입력 검증 + 코드 구조* 동시 완성. 클라이언트가 *논리 오류*(since/until 역전)도 조용히 통과하지 못함 → 즉시 진단성 422 메시지로 학습. `_has_any_time_filter` helper로 *후속 시간 필터 추가 비용 invariant*(분기 1 줄 유지) 보장 — 미래 슬라이스(예: `name_changed_since` 추가) 시 호출 인자만 확장. **🎯 rate limit + device-cred 35 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·order_by 옵션·@validate_time_filters 데코레이터·시간창 quantize 캐시·count별 TTL·cache stampede·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 44 (`GET /v1/devices` `?used_since`/`?used_until` · 사용 시간창 필터)** 완료 (메인 hermetic·1402→1406 passed, 2026-06-03) — 슬라이스 43 결정 v1 한계 ①(`last_used_at` 기준 필터 미지원·세 번째 시간 차원) 자명 해소. **시간 차원 3축 완비**: `created_at`(등록 시간창·slice 41) + `revoked_at`(폐기 시간창·slice 43) + `last_used_at`(사용 시간창·slice 44). UI "지난 7일 사용된 기기"·보안 감사 "30일+ 미사용 후보 추적"·이상 탐지 "갑자기 활성화된 오래된 device" 즉시 구현. 슬라이스 43 패턴 *완전 답습* — 새 인프라 도입 0(시그너처 확장·필터 추가·tz 검증 헬퍼 호출만으로 신 표면). **Protocol 시그너처 확장**: `list_for_user(..., used_since: datetime | None = None, used_until: datetime | None = None)` 및 `count_for_user(..., used_since=None, used_until=None)`. 슬라이스 41(`since`/`until`)·43(`revoked_since`/`revoked_until`)와 *완전 직교* — 세 시간창을 동시 적용 가능(예: "2024년 등록·2024-06 이후 verify·아직 미폐기"). **InMemoryDeviceStore**: 필터 조건 `(used_since is None or (c.last_used_at is not None and c.last_used_at >= used_since))` — 한 번도 verify 안 된 device(last_used_at=None) *자동 제외*. used_until도 동형. 의도된 시맨틱: "사용 시간창 지정 시 *verify된 device로 좁힘*"(미verify 또는 등록 직후 device는 제외). **PgDeviceStore**: 조건부 `.where(DeviceCredential.last_used_at >= used_since)` / `<=`. SQL NULL 비교 자동 False(SQL 표준) — InMemory와 의미 *완전 일관*(Python None vs SQL NULL 트릭이 두 store에 일관 적용). **CachedDeviceStore.list_for_user**: used_since/used_until 인자 그대로 inner에 전달. **CachedDeviceStore.count_for_user**: 시간 필터 *6종*(since/until/revoked_since/revoked_until/used_since/used_until) 중 *하나라도* 있으면 캐시 우회(슬라이스 41/43 정책 확장·키 폭발 방지 invariant 유지). **라우터 `?used_since`/`?used_until` Query 추가**: `Annotated[datetime | None, Query(description=...)] = None` 슬라이스 42 B008 회피 패턴 그대로 + `_validate_tz_aware(used_since, "used_since")` 검증(헬퍼 *6번째*와 *7번째* 호출·재사용성 검증). **테스트** (hermetic only) **+4 케이스**: ① **InMemory `test_used_since_until_filters_last_used_at`**: d_never(미verify)·d_old(365일전 사용)·d_recent(1일전 사용) → used_since=30일전 → recent만(never는 None 제외)·시간창 [400일전, 100일전] → old만·count 두 분기. ② **HTTP `test_used_since_filter_via_http`**: d_never + d_used·used_since=1일전 → d_used만. ③ **HTTP `test_naive_used_since_rejected_422`**: 슬라이스 42 검증이 `used_since` 필드에도 적용·메시지 'used_since' 명시. ④ **Pg `test_used_since_until_via_sql_range`**: `_FakePgSession._row_matches`의 ge/le 처리(slice 41 일반화 패턴)·SQL NULL 미verify 행 제외 의미 시뮬·count_for_user `used_since`/`used_until` 두 분기 cov 100% 확보. **4게이트 green**: black·ruff·mypy-strict(86 파일·8 keyword-only datetime 인자 시그너처 정합)·pytest **1406 passed**(1402→+4 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 292/Branch 84/Miss 0(slice 43 284 → +8 used_since/until 분기 4 곳) · `devices.py` Stmts 60/Branch 6/Miss 0(slice 43 58 → +2 Query 필드·검증 2 줄). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **시간 필터 조합 검증 없음** — `since > until` 같은 *불가능 시간창* 검증 안 함. 응답은 빈 리스트로 *자연스럽게 처리*되나 422로 명시 거부도 후속 가능 ② **시간 차원 한정** — 다른 컬럼(예: 사용자 정의 `tag`·`device_name` 후속 도입 시) 기준 필터는 도입 필요 시 추가 ③ **cache 우회 분기 6개 OR 조건** — 시간 필터 수 증가 시 분기 비대해짐. helper 함수 (`_has_time_filter(*args)`)로 추출 후속 ④ **last_used_at 정밀도가 cache TTL**(slice 32 도입한 lazy 패턴) — used_since=now()-1초 같은 *극단 정밀* 쿼리는 slice 32 한계 ①와 동일 ≈ TTL 오차. **🎯 의미**: device 자기 관리 표면이 **시간 차원 3축 완비** — 모든 시간 메타데이터(등록·폐기·사용) 기준으로 독립 필터링. 보안 감사 화면(SIEM 통합·30일+ 미사용 device 자동 폐기 후보 선별 UI·"갑자기 깨어난 device" 도난 의심 alert) 즉시 구현. 슬라이스 41 fake 일반화 + 슬라이스 42 tz 헬퍼가 *3 시간 차원에 동일하게 재사용* — 인프라 도입 비용 0 invariant 확인(_device_store.py +8 stmt/+4 branch, devices.py +2 stmt 만). **🎯 rate limit + device-cred 34 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·order_by 옵션·시간 필터 조합 검증(since > until 422)·`_has_time_filter` helper 추출·시간창 quantize 캐시·count별 TTL·cache stampede·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 43 (`GET /v1/devices` `?revoked_since`/`?revoked_until` · 폐기 시간창 필터)** 완료 (메인 hermetic·1398→1402 passed, 2026-06-03) — 슬라이스 41 결정 v1 한계 ①(`since`/`until`이 `created_at`만 필터·`revoked_at` 기준 필터 미지원) 자명 해소. 보안 감사 "지난 7일 폐기된 device" 조회·SIEM 통합·incident response 기반 결선. 슬라이스 41(시간창 필터 + fake 일반화) 패턴 + 슬라이스 42(tz 검증) 헬퍼를 *그대로 답습* — 새 인프라 도입 0(기존 패턴 *복합 확장*만으로 신 표면). **Protocol 시그너처 확장**: `list_for_user(..., revoked_since: datetime | None = None, revoked_until: datetime | None = None)` 및 `count_for_user(..., revoked_since=None, revoked_until=None)` 추가. created_at 시간창과 *독립* 인자 — 둘 다 동시 적용 가능(예: "2024년에 등록·2025년 6월에 폐기된 device"). **InMemoryDeviceStore**: 필터 조건 `(revoked_since is None or (c.revoked_at is not None and c.revoked_at >= revoked_since))` — *부분 truthy 평가*로 미폐기(revoked_at=None) device 자동 제외. revoked_until도 동형. 의도된 시맨틱: "폐기 시간창 지정 시 폐기 device로 자동 좁힘"(active device는 revoked_at 기준 의미 없음). **PgDeviceStore**: 조건부 `.where(DeviceCredential.revoked_at >= revoked_since)` / `<= revoked_until` 추가. SQL의 NULL 비교는 *자동 False*(SQL 표준)라 InMemory와 의미 일관·미폐기 행 자연 제외(Python None 비교 트릭 vs SQL NULL 의미 일치). **CachedDeviceStore**: list_for_user는 위임·count_for_user는 시간창 필터 4종(`since`/`until`/`revoked_since`/`revoked_until`) 중 *하나라도* 있으면 캐시 우회(슬라이스 41 정책 확장). 의도: 시간 필터는 *1회성 감사 조회*라 캐시 효익 < 키 폭발 위험. **라우터 `?revoked_since`/`?revoked_until` Query 추가**: `Annotated[datetime | None, Query(description=...)] = None` 형식(슬라이스 42 B008 회피 패턴 그대로) + `_validate_tz_aware(revoked_since, "revoked_since")` 검증(슬라이스 42 헬퍼 재사용). **테스트** (hermetic only) **+4 케이스**: ① **InMemory `test_revoked_since_until_filters_revoked_at`**: d_active(미폐기)·d_old_revoked(365일전 폐기)·d_recent_revoked(1일전 폐기) → revoked_since=30일전 → recent만(미폐기 자동 제외)·시간창 [400일전, 100일전] → old만·count도 동일 필터. ② **HTTP `test_revoked_since_filter_via_http`**: 같은 시나리오를 `?include_revoked=true&revoked_since=ISO8601` HTTP 라운드트립으로 검증. ③ **HTTP `test_naive_revoked_since_rejected_422`**: 슬라이스 42 검증이 `revoked_since` 필드에도 적용·메시지에 'revoked_since' 단어 포함. ④ **Pg `test_revoked_since_until_via_sql_range`**: `_FakePgSession`의 `_row_matches`(슬라이스 41 일반화) ge/le 처리로 SQL revoked_at 시간창 검증·SQL NULL 미폐기 행 제외 의미 시뮬·count_for_user 두 분기(revoked_since/revoked_until) cov 100% 확보. **4게이트 green**: black(라인 길이 초과 부분은 멀티라인으로 분리·각 and 절을 if-style 구조로)·ruff(B008 회피)·mypy-strict(86 파일·`datetime | None` 분기 정합)·pytest **1402 passed**(1398→+4 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 284/Branch 76/Miss 0(slice 42 276 → +8 revoked_since/until 분기 4 곳) · `devices.py` Stmts 58/Branch 6/Miss 0(slice 42 56 → +2 Query 헤더·검증 2 줄). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **last_used_at 기준 필터 미지원** — 슬라이스 41 한계 ①의 *세 번째 시간 차원*(verify 마지막 시각). 슬라이스 32 last_used_at + 동일 패턴(`?used_since`/`?used_until`)으로 후속 자명 ② **시간창과 페이지네이션 결합 시 sort by revoked_at 옵션 없음** — 현재 created_at DESC 고정. `?order_by=revoked_at` 옵션 후속 ③ **revoked_since AND created_since 동시 사용 시 의미 직관 낮음** — 예: "2024년 등록 + 2025년 6월 폐기"는 두 시간창 *교집합*. UI에서 어떻게 표현할지는 클라이언트 책임 ④ **검증 메시지 4개 필드 별도 분기** — `_validate_tz_aware`를 4 번 호출. 함수 호출 cost 무시할 수준이나 후속에 *모든 시간 필드 한꺼번에* 검증하는 데코레이터 패턴 검토 가능. **🎯 의미**: device 조회 표면이 **시간 차원 2축 완비** — created_at(등록 시간창·slice 41) + revoked_at(폐기 시간창·slice 43) 모두 독립 필터링 가능. 보안 감사 화면 ("지난 7일 폐기된 모든 device"·"올해 등록된 활성 device 중 30일+ 미사용"·incident timeline 재구성) 즉시 구현. 슬라이스 41의 fake 일반화가 *후속 SQL 비교 도입 비용 0* invariant 검증 — 슬라이스 43이 ge/le를 *재활용*해 인프라 코드 추가 0. 슬라이스 42 tz 검증 헬퍼 4 번 재사용으로 *반복 검증 코드 0*. **🎯 rate limit + device-cred 33 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·last_used_at 기준 필터·order_by 옵션·timezone 자동 정규화 옵션·시간창 quantize 캐시·count별 TTL·cache stampede·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 42 (`GET /v1/devices` since/until timezone 정규화 · naive 422)** 완료 (메인 hermetic·1394→1398 passed, 2026-06-03) — 슬라이스 41 결정 v1 한계 ②(naive datetime 처리·timezone 정규화 없음) 자명 해소. naive datetime(`'2024-01-01T00:00:00'`)은 PG의 TZ-aware `created_at`과 비교 시 *의미 모호*(SQLAlchemy/PG가 어떤 zone으로 가정할지 undefined·UTC로 추측해도 클라이언트 의도와 다를 수 있음) → 클라이언트가 *반드시* timezone 정보 포함 ISO8601 전송하도록 강제 게이트. **신규 헬퍼 `_validate_tz_aware(value, field_name)`**: `value.tzinfo is None`이면 `HTTPException(422, detail=...)`. 메시지에 ① 어느 필드(`since`/`until`) 위반 ② 정상 예시(`'2024-01-01T00:00:00Z'` UTC 약식, `'+09:00'` offset 형식) ③ 이유(PG TZ-aware 비교 모호)를 *모두 명시* — 클라이언트 개발자가 *받자마자* 수정 방법 인지(API 진단성 핵심). **라우터 핸들러 갱신**: `_require_store()` 호출 직전에 `since = _validate_tz_aware(since, "since")` + `until = _validate_tz_aware(until, "until")` 두 줄. FastAPI/Pydantic의 `datetime` 자동 파싱은 naive/aware 둘 다 통과시키므로 router-level 명시 검증 필요. **`HTTP_422_UNPROCESSABLE_CONTENT` 사용**: FastAPI 신 상수(`_ENTITY`는 deprecation 경고) — pytest 경고 표면 정리. **테스트** (hermetic only) **+4 케이스**: ① `test_naive_datetime_since_rejected_422`(`?since=2024-01-01T00:00:00` → 422 + 메시지에 'timezone' 단어 포함 확인·클라이언트가 message로 진단 가능) ② `test_naive_datetime_until_rejected_422`(until도 동일 검증) ③ `test_tz_aware_with_z_accepted`(`Z` 약식 → 200·UTC 의미 처리) ④ `test_tz_aware_with_offset_accepted`(`+09:00` offset → 200·timezone 정상 인코딩). client는 `params=` dict로 안전 인코딩(슬라이스 41의 URL `+` 공백 변환 함정 회피). **4게이트 green**: black·ruff·mypy-strict(86파일·`_validate_tz_aware`·`status.HTTP_422_UNPROCESSABLE_CONTENT` 정합)·pytest **1398 passed**(1394→+4 hermetic, cov-fail-under=70). 신규 cov **100%**: `devices.py` Stmts 56/Branch 6/Miss 0(slice 41 50 → +6 검증 헬퍼+호출). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **에러 메시지 i18n 없음** — 한국어만(WhyMath 컨텍스트). 영어 messaging은 Accept-Language 헤더 기반 후속(international 표면 도입 시) ② **TZ 자동 정규화 옵션 없음** — 반려 대신 "UTC 가정해 진행" 모드 미지원. 운영 보고 후 필요 시 옵션 추가(`naive_assumes_utc: bool = False` Settings) ③ **Pydantic 검증 vs 라우터 검증** — 라우터에서 명시 검증은 *FastAPI의 단일 분기 추가* 패턴. Pydantic field validator로 schema-level 검증도 가능(향후 더 많은 datetime 필드 도입 시 헬퍼 추출). **🎯 의미**: API 시간 처리의 *방어적 입력 검증* 완비 — naive datetime 침투를 차단해 PG와 일관된 timezone 의미. 슬라이스 41 시간창 필터의 *정확성* 보장. 클라이언트 개발자가 *수정 방법 자명*한 422 메시지로 즉시 학습. **🎯 rate limit + device-cred 32 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·revoked_at/last_used_at 기준 필터·timezone 자동 정규화 옵션·시간창 quantize 캐시·count별 TTL·cache stampede·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 41 (`GET /v1/devices?since=&until=` · 시간창 필터 + fake 일반화)** 완료 (메인 hermetic·1390→1394 passed, 2026-06-03) — 슬라이스 37 결정 v1 한계 ②(시간창 필터 없음·"지난 30일 폐기" 조회 불가) + 슬라이스 38 한계(시간창 필터 미지원) *두 가지를 한 슬라이스로* 동시 해소. Flutter "지난 30일 활동" UI·보안 감사 화면·SIEM 통합 기반 마련(`created_at` 기준 시간창 inclusive). 부가 효과: `_FakePgSession`을 *일반화*해 후속 슬라이스가 다른 SQL 비교 연산자(==, >=, <=, !=, <, >) 도입 시 fake 인프라 재사용 가능. **Protocol 시그너처 확장**: `list_for_user(..., since: datetime | None = None, until: datetime | None = None)` 및 `count_for_user(..., since=None, until=None)` keyword-only 인자. 디폴트 None으로 기존 호출처(slice 22~40 어디서든) 호환 보존. **InMemoryDeviceStore**: 기존 `c.user_id == owner_id and (include_revoked or not c.revoked)` 필터에 `and (since is None or c.created_at >= since) and (until is None or c.created_at <= until)` 추가(list_for_user·count_for_user 동형). inclusive 시간창. **PgDeviceStore**: 조건부 `.where(DeviceCredential.created_at >= since)` / `.where(DeviceCredential.created_at <= until)` — 서버측 SQL 시간창 필터. PG의 `created_at` 인덱스(slice 23 migration에 있음) 활용해 효율적 row 제한. count_for_user(`SELECT COUNT(*)`)도 동일 분기 적용. **CachedDeviceStore.list_for_user**: since/until 그대로 inner에 위임(slice 26 캐시 안 함 정책 유지·시간창 무관). **CachedDeviceStore.count_for_user 핵심 결정**: `since or until is not None`이면 *캐시 우회*·inner 직접 호출·SETEX 안 함. *이유*: 시간창 조합이 무한대 → 캐시 키 폭발 위험·시간 필터는 *1회성 감사 조회*가 일반적·캐시 효익 낮음. 무필터 경로(`since=None and until=None`)는 슬라이스 40 그대로(active/all 두 키 사용). **라우터 `?since`/`?until` Query 추가**: `Annotated[datetime | None, Query(description=...)] = None` 형식 사용 — ruff B008(`Query()` 함수 호출이 default에 있음) *회피 패턴* 발견 + 적용(기존 limit/offset/include_total은 ruff 묵인이었으나 `datetime | None` 타입에선 B008 발화). Annotated는 *modern FastAPI* 권장 형식이라 점진 마이그레이션 단초. FastAPI가 `datetime` 자동 parse — URL params `?since=2024-01-01T00:00:00`처럼 ISO8601 받아 datetime 객체로 변환. **`_FakePgSession._row_matches(row, where)` 신규 — 테스트 인프라 일반화**: 재귀 row-matcher로 ① BooleanClauseList(AND): children 모두 만족 ② BinaryExpression: `left=column`·`right=BindParameter`·`operator=callable`. `operator.__name__`으로 dispatch — `eq`/`ne`/`ge`/`le`/`lt`/`gt`/`is_`(`is_(False)`는 `eq(actual, False)`로 매핑·hermetic 의미 동등). TypeError(None과 datetime 비교 등)는 False(매치 실패)로 안전 처리. *이 일반화 패턴이 후속 슬라이스에서 다른 비교 연산자 도입 시 fake 확장 0*. 기존 `_iter_eq_constraints`는 deprecated 표시(slice 24-40 코드 호환 유지·새 코드는 `_row_matches` 사용). `_exec_select`의 행 필터링이 단일 `_row_matches` 호출로 단순화. **테스트** (hermetic only) **+4 케이스**: ① **InMemory `test_since_until_filters_created_at`**: 3 device(현재/7일전/1년전) → since=30일전 → d1+d3·until=14일전 → d2만·시간창 [60일전, 1일전] → d3만·count 동일 필터. ② **HTTP `test_since_until_query_params_filter_response`**: `?since=ISO8601` → d1만·`?since=&include_total=true` → total=1(필터 적용)·`params=` dict로 안전 전달(`+`가 공백으로 파싱되는 URL 인코딩 함정 회피). ③ **Pg `test_since_until_via_sql_range`**: same 시나리오를 `_FakePgSession`의 `_row_matches` ge/le 처리 경로로 검증·SQL 발급 정합. ④ **Cached `test_count_with_since_until_bypasses_cache`**: since 있으면 `cache.get_calls==0`·`setex_calls==0`(2회 호출해도 inner 매번 호출·캐시 우회 정합). **4게이트 green**: black·ruff·mypy-strict(86 파일·Annotated[datetime | None] 정합·timedelta import·B008 회피)·pytest **1394 passed**(1390→+4 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 276/Branch 68/Miss 0(slice 40 266 → +10 since/until 분기 4곳) · `devices.py` Stmts 50/Branch 4/Miss 0(slice 40 49 → +1 Annotated import). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **`since`/`until`이 `created_at`만 필터** — `revoked_at`나 `last_used_at` 기준 필터링(예: "지난 7일 폐기된 device")은 후속 슬라이스(추가 쿼리 파라미터+필드 결선) ② **타임존 처리** — datetime이 naive면 PG 비교가 무의미. FastAPI는 ISO8601 timezone 포함된 형식 우선 권장(라우터에서 검증 후속) ③ **Cached count 우회로 P95 spike** — high-frequency 시간창 호출 시 매번 DB 강타. 별 cache 키 전략(시간창을 granularity로 quantize·예: 하루 단위) 후속(thundering load 보고 후) ④ **`_FakePgSession`의 `_row_matches` SQL 정밀도 한계** — LIKE/IN/NOT/OR 등은 미지원. 후속 SQL 쿼리가 도입되면 dispatch 확장 필요(이 슬라이스가 그 패턴 도입). **🎯 의미**: device 자기 관리 표면이 *시간 차원 조회 완비* — Flutter "지난 30일 활동" 화면·"분기별 폐기 trend" 보안 감사 화면·"가입 시기별 device 분포" 분석 표면 모두 즉시 구현 가능. `_FakePgSession`이 일반 SQL 비교 연산자 지원하도록 진화해 *후속 SQL 쿼리 도입 비용 0*(테스트 인프라 재사용). **🎯 rate limit + device-cred 31 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·revoked_at/last_used_at 기준 필터·timezone 정규화·시간창 quantize 캐시·count별 TTL·cache stampede 방어·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 40 (`CachedDeviceStore.count_for_user` 캐시 · 슬라이스 26 패턴 답습)** 완료 (메인 hermetic·1387→1390 passed, 2026-06-03) — 슬라이스 39 결정 v1 한계 ①(COUNT 쿼리 캐시 안 함·매 호출 PG 라운드트립) 자명 해소. count_for_user 결과를 짧은 TTL(기본 60s)로 캐시 → register/revoke가 즉시 invalidate하므로 stale 위험 ≈ 0. 슬라이스 26 verify 캐시의 *완전한 패턴 답습*(GET/SETEX/DEL·bytes 응답 호환·소유자 액션이 invalidate). **신규 prefix** `_COUNT_CACHE_KEY_PREFIX = "device_count:"`. 키 형식 `device_count:{user_id}:{suffix}` — suffix가 `active`(include_revoked=False) 또는 `all`(include_revoked=True). *include_revoked 분기 별로 다른 키*라 두 변형이 *독립 캐시*(서로 다른 값 반환·캐시 collision 0). **CachedDeviceStore.count_for_user 갱신**(슬라이스 39 → 40): ① cache `GET cache_key` → hit이면 bytes/str decode 후 `int()` 파싱 반환(verify 캐시와 동일 bytes 호환 처리) ② miss면 `inner.count_for_user` 호출·결과를 `str(N)`로 SETEX(TTL=`device_verify_cache_ttl_seconds` 공유·새 Setting 0). **CachedDeviceStore.register 갱신**: inner.register 후 *두 count 키*(active·all) 모두 `cache.delete(...)`로 invalidate — 새 device가 어느 필터 카운트에도 반영되도록 양쪽 키 동시 DEL. delete varargs 인터페이스 활용(단일 round-trip). **CachedDeviceStore.revoke 갱신**(slice 26 → 40): 성공 시 verify 캐시(`device_verify:{id}`) + 두 count 키 모두 DEL을 *단일 delete varargs 호출*로(3 키 한 번에). 실패(타인 소유 등) 시 미접근(slice 26 invariant 유지). **cleanup_stale은 count 캐시 invalidate 안 함**: ① 시스템 호출(사용자 액션 아님)·UI 즉시 정확도 우선 낮음 ② affected device의 user_id 알려면 추가 SELECT 필요(비용)·자동 폐기는 보통 N>1 사용자 영향 ③ TTL 자연 만료(60s)로 자동 회복. 슬라이스 33의 "TTL 자연 만료" 트레이드오프와 일관. **테스트** (hermetic only) **+3 신규 + 2 갱신**: ① `test_count_first_call_misses_cache_and_sets`(첫 호출 → GET 1·inner.count 1·SETEX 1·키 `device_count:{uid}:active`에 "2" 저장 확인) ② `test_count_second_call_hits_cache_skips_inner`(warm 후 2번째 호출 → inner 호출 0·SETEX는 첫 호출 1회만) ③ `test_count_include_revoked_uses_separate_key`(active 키와 all 키가 *서로 다른 값* 1과 2 저장·캐시 collision 없음) ④ `test_count_cache_bytes_response`(redis decode_responses=False 시뮬·`cache.get`을 bytes 반환 monkey·utf-8 decode 후 `int(1)` 정합). 갱신: `test_register_passthrough_invalidates_count_cache`(이전 "cache 무접근" 가정 → 슬라이스 40의 count 캐시 DEL 검증으로 진화)·`test_revoke_invalidates_cache`에 count 캐시 미리 적재 + revoke 후 두 키 모두 부재 확인. `test_revoke_failure_does_not_invalidate`는 그대로(slice 26 invariant). **4게이트 green**: black·ruff·mypy-strict(86 파일)·pytest **1390 passed**(1387→+3 신규, 2 갱신, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 266/Branch 58/Miss 0(slice 39 255 → +11 count 캐시 로직 + invalidate 분기). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **cleanup_stale 후 count 60s stale** — 시스템 자동 폐기 직후 GET이 *오래된 카운트* 보일 수 있음(정상 사용 패턴엔 무영향) ② **active+all 두 키 동시 invalidate** — 한쪽만 변경(예: active만 줄어듦)도 양쪽 DEL. 별 트레이드오프(미세 정확 invalidation은 복잡도 vs 효익 낮음) ③ **cache stampede 미방어** — TTL 만료 직후 동시 호출이 모두 inner.count 호출·DB 강타 가능. singleflight·확률적 조기 갱신은 slice 26 같은 한계 ④ **TTL 공유** — `device_verify_cache_ttl_seconds` 동일 사용. count는 verify보다 *덜 자주 갱신* 가능(별 TTL 후속). **🎯 의미**: `?include_total=true` 사용 시 *cache hit 경로엔 0 DB 라운드트립* — Flutter UI에서 "총 N개" 표시를 자주 fetch해도 부담 0. 슬라이스 26(verify 캐시)·40(count 캐시) 모두 *register/revoke가 즉시 invalidate*하는 동일 *write-through invalidation* 패턴 — 일관된 캐시 모델. 코드 변화 최소(+11 라인 핵심·기존 register/revoke에 DEL 키 추가)로 즉시 효과. **🎯 rate limit + device-cred 30 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·시간창 필터·count별 TTL·cache stampede 방어·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 39 (`GET /v1/devices?include_total=true` · 총 개수 옵션)** 완료 (메인 hermetic·1382→1387 passed, 2026-06-03) — 슬라이스 38 결정 v1 한계 ②(total count 미제공·"Page X of Y" UI 불가) 자명 해소. Flutter UI가 페이지네이션 진행률·"총 N개 등록됨" 표시 가능. **opt-in 설계**(추가 COUNT 쿼리 비용 회피·기존 클라이언트 호환): 기본 호출은 `total=null`·`?include_total=true`일 때만 채움. **신규 Protocol `count_for_user(owner_id, *, include_revoked: bool = False) -> int`**: list_for_user와 *같은 필터 조건*(owner_id 스코프·include_revoked 분기) — pagination 의미 invariant(같은 페이지 set의 부분이 list, 전체가 count). **InMemoryDeviceStore.count_for_user**: `sum(1 for c in self._creds.values() if ...)` — dict comprehension·O(N) 순회. **PgDeviceStore.count_for_user**: `select(func.count()).select_from(DeviceCredential).where(user_id=X)` + 조건부 `.where(revoked.is_(False))` + `result.scalar()` — *서버측* COUNT(*)로 단일 round-trip·O(1) 네트워크 비용. **CachedDeviceStore.count_for_user**: 캐시 안 함(slice 26·29의 "list/count는 신선도 우선" 정책 유지)·inner 위임. 자기 데이터 사용자별 키 폭발 위험·1 round-trip이라 캐시 효과 한계적·register/revoke 직후 신선도 우선. **`DeviceListResponse.total: int | None = None`** 필드 추가 — Pydantic 응답 schema. 기본 None이라 기존 클라이언트가 새 필드 무시 가능(forward compat). **`GET /v1/devices?include_total=true`** Query 옵션: 기본 False·true시만 count_for_user 호출 + 응답 total 필드 채움. **`_FakePgSession._exec_select` 확장**(테스트 인프라): `_raw_columns` 중 `.key`가 None/없는 표현(`func.count()` 등 SQL 함수)을 감지해 `[(len(matched),)]` 단일 행 반환·`_SelectResult.scalar()` 메서드 추가로 `result.scalar()` 호환. SQLAlchemy 2.0의 `select(func.count())` 객체는 함수 식 노드가 `.key=None`이라 기존 컬럼 추출 분기에서 None을 안전하게 분리. **테스트** (hermetic only) **+5 케이스 + 1 갱신**: ① **HTTP `test_include_total_returns_filtered_count`**: 3 활성 + 2 폐기 등록 → `?include_total=true`로 total=3·`?include_revoked=true&include_total=true`로 total=5·`?limit=2&include_revoked=true&include_total=true`로 devices 2개 + total=5(페이지네이션과 결합 시 *페이지 size 무관* total). ② **HTTP `test_include_total_false_default_returns_none`**: 기본 호출 → total=null(opt-in 검증). ③ **InMemory `test_count_for_user_matches_list_filter`**: 3 본인 활성·1 본인 폐기·1 타인 등록 → `count(_UID)=3`·`count(_UID, include_revoked=True)=4`·`count(other)=1`(스코프 검증). ④ **Pg `test_count_for_user_via_sql_count`**: 같은 시나리오를 `_FakePgSession`의 SQL COUNT 처리 경로로 검증. ⑤ **Cached `test_count_passthrough_no_cache_access`**: 2 device 등록·count_for_user 호출 → 2·`cache.get_calls==0`·`cache.setex_calls==0`(slice 26·29 정책 유지). 기존 `test_empty_list_when_no_registrations`는 `{devices: [], total: null}`로 갱신(slice 39 응답 schema 정합). `_CountingInnerStore.count_for_user`도 Protocol 정합. **4게이트 green**: black·ruff·mypy-strict(86파일·`func.count()` Protocol 정합·`int | None` total 직렬화 정합)·pytest **1387 passed**(1382→+5 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 255/Branch 56/Miss 0(slice 38 241 → +14 count 3 구현체) · `devices.py` Stmts 49/Branch 4/Miss 0(slice 38 45 → +4 include_total 분기 + Query). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **COUNT 쿼리 캐시 안 함** — `pg_cached` 모드에서도 매 호출 PG 라운드트립. high-frequency 호출(드물지만) 시 부담. count 캐시 + register/revoke 시 invalidate는 후속(rate 캐시 26 패턴 답습 가능) ② **`func.count()` 만 지원** — 다른 집계 함수(MAX(created_at) 등) 호출은 fake 인프라 확장 필요 ③ **transactional consistency** — `list_for_user` 호출과 `count_for_user` 호출이 *별 세션*이라 정확한 transactional consistency 보장 안 함. 매우 짧은 시간창에 register/revoke 발생 시 count 1 차이 가능(드물·UI 영향 미미·실시간 정확성보다 페이지 진행률 *대략 표시*가 목적) ④ **total 계산이 무한 COUNT 비용** — 1만 row 페이지네이션 시 매 페이지 마다 COUNT가 1만 row 스캔. PG에서 거대 테이블엔 estimated count(pg_class.reltuples) 패턴 후속. **🎯 의미**: device 자기 관리 표면이 *Page X of Y* UI 완비 — Flutter 진행률 표시·"앞으로 N개 더" 안내·"총 device 수" 대시보드 가능. opt-in 설계로 *기존 호출 비용 0*·*필요 시만 COUNT*. _FakePgSession이 SQL 함수까지 처리해 테스트 인프라 *확장 가능*(다른 집계 함수 후속에 같은 패턴). **🎯 rate limit + device-cred 29 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·count 캐시·시간창 필터·`/v1/admin/devices/cleanup-stale` 엔드포인트·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 38 (`GET /v1/devices` 페이지네이션 · limit/offset)** 완료 (메인 hermetic·1378→1382 passed, 2026-06-03) — 슬라이스 37 결정 v1 한계 ①(페이지네이션 미지원·include_revoked=true 시 1000건+ 응답)과 슬라이스 29 결정 v1 한계 ③(페이지네이션 미지원) *둘을 한 슬라이스로* 동시 해소. Flutter UI는 페이지 단위로 fetch → "더 보기" 버튼 또는 무한 스크롤로 lazy load 가능. **Protocol 시그너처 확장**: `list_for_user(owner_id, *, include_revoked: bool = False, limit: int = 50, offset: int = 0) -> list[DeviceInfo]`. keyword-only 인자·라우터가 limit 상한 검증(1~200)·여기선 신뢰. 빈 리스트는 *해당 페이지에 결과 없음*(클라이언트가 stop·has_more 헤더 미제공 — 응답 길이로 판단). **InMemoryDeviceStore.list_for_user**: 기존 sort 후 `items[offset:offset+limit]` 슬라이스(Python 슬라이스는 음수/초과 안전). **PgDeviceStore.list_for_user**: SQL `.limit(limit).offset(offset)`를 order_by 다음에 chain — *서버측* 페이지네이션으로 network·메모리·클라이언트 RAM 부담 최소화(1만건 중 10건만 전송). **CachedDeviceStore.list_for_user**: limit/offset 그대로 inner에 전달(slice 26 캐시 안 함 정책 유지). **`GET /v1/devices?limit=50&offset=0`** 라우터: FastAPI `Query(default, ge, le, description)`로 검증 — `limit: 1~200`(기본 50)·`offset: ≥0`(기본 0). 부적합(limit=0/201·offset=-1) → 자동 **422 Unprocessable Entity**(Pydantic validation). 라우터→Protocol→store까지 limit/offset 인자 *직접 통과*(별 변환 0). **`_FakePgSession._exec_select` 확장**: `stmt._limit_clause`·`stmt._offset_clause`의 `BindParameter.value` 추출해 Python list 슬라이스 적용. SQLAlchemy 2.0 `select(...).limit(N).offset(M)` 객체가 이 두 속성에 BindParameter 형태로 값 보관 — hermetic 테스트가 *실제 SQL 발급 없이* LIMIT/OFFSET 의미 검증. `limit_val=None`(미설정) 케이스도 처리(slice 33의 cleanup_stale SELECT는 limit 없이 호출). **테스트** (hermetic only) **+4 케이스**: ① **HTTP `test_pagination_limit_offset`**: 5 device 등록·limit=50(기본·모두 반환)·limit=2/offset=0(첫 2)·limit=2/offset=2(다음 2)·limit=2/offset=4(마지막 1, len<limit 시그널)·limit=2/offset=10(빈)·3 페이지 합집합으로 전체 검증. ② **HTTP `test_limit_validation_rejects_zero_and_too_large`**: limit=0/201·offset=-1 → 422(FastAPI Query 자동 검증). ③ **InMemory `test_pagination_slices_sorted_result`**: 4 device·limit=2/offset=0·2로 두 페이지 추출·DESC 순서 검증(`[ids[3], ids[2]] then [ids[1], ids[0]]`). ④ **Pg `test_pagination_via_sql_limit_offset`**: 같은 시나리오를 `_FakePgSession`의 LIMIT/OFFSET 처리 경로로 검증·SQL 발급 정합성 확인. `_CountingInnerStore`도 limit/offset 인자 추가(Protocol 정합). **4게이트 green**: black·ruff·mypy-strict(86파일·keyword-only `limit`/`offset` 정합·`_limit_clause`/`_offset_clause` 내부 attribute 호환)·pytest **1382 passed**(1378→+4 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 241/Branch 54/Miss 0(slice 37과 동일 라인 수·새 분기는 기존 라인 내) · `devices.py` Stmts 45/Branch 2/Miss 0. 전체 cov 99.50%. **결정 v1 한계**(후속): ① **offset-based pagination drift** — offset 사용 중 새 register/revoke 시 페이지 경계 흔들림(같은 device 두 번 나타나거나 누락). 일반 사용 ms 단위에 1~5 device 등록이라 드물지만 *드물게 발생*. cursor-based(created_at 기반)로 후속 안정화 가능 ② **total count 미제공** — "Page X of Y" UI 불가. `?include_total=true` 옵션 추가 후속(추가 COUNT 쿼리 비용 수반) ③ **filter 옵션 없음** — "지난 30일 이내 폐기" 같은 시간창 필터(slice 37 한계 ②와 같이 후속) ④ **limit 상한 200 하드코딩** — Settings 노출 안 함. 운영 보고 후 조정 가능. **🎯 의미**: device 자기 관리 표면이 **production-scale 페이지네이션 완비** — 1만건+ 이력 있는 power user(여러 기기 변경 이력)도 ms 단위 응답. include_revoked=true·페이지네이션이 결합돼 Flutter "기기 관리 → 폐기 기록" 무한 스크롤 UI 구현 자명. PgDeviceStore의 SQL LIMIT/OFFSET 활용으로 서버 부하 *상수*(SELECT row 수 = limit·1만건 row 전송 0). **🎯 rate limit + device-cred 28 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·cursor-based pagination·total count·시간창 필터·`/v1/admin/devices/cleanup-stale` 엔드포인트·revoked_only·equal/decorrelated jitter·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 37 (`GET /v1/devices?include_revoked=true` · 폐기 이력 포함)** 완료 (메인 hermetic·1375→1378 passed, 2026-06-03) — 슬라이스 29 결정 v1 한계 ①(`include_revoked` 모드 미지원·폐기 이력 조회 불가) 자명 해소. Flutter "기기 관리 → 폐기 기록" UI 결선·보안 감사 화면 기반 마련. 본인 데이터라 PII 우려 없음(secret_plain·user_id는 여전히 비노출, revoked/revoked_at는 사용자 본인 정보). **`DeviceInfo` NamedTuple 확장**: `revoked: bool = False` + `revoked_at: datetime | None = None` 추가. 기본값으로 기존 호출처(slice 29~36 코드 어디서든 `DeviceInfo(device_id, created_at, last_used_at)`로 구성하던 곳) 호환 보존. **`_CredRecord` NamedTuple 확장**: `revoked_at: datetime | None` 필드 추가 → slice 23 Pg ORM과 *완전 parity* 달성. 슬라이스 33의 `cleanup_stale`가 `revoked=True`만 설정하고 `revoked_at` 누락하던 *조용한 결함* 발견·수정(InMemory와 Pg가 다른 의미로 동작하던 문제). **InMemoryDeviceStore.revoke**: `cred._replace(revoked=True, revoked_at=datetime.now(UTC))` — 슬라이스 24부터의 revoke가 이제 시각 함께 채움. **InMemoryDeviceStore.cleanup_stale**: `cred._replace(revoked=True, revoked_at=now)` — 슬라이스 33 누락 수정. **InMemoryDeviceStore.register**: `revoked_at=None` 초기화. **Protocol `list_for_user(owner_id, *, include_revoked: bool = False) -> list[DeviceInfo]`** keyword-only 인자. **InMemoryDeviceStore.list_for_user**: 필터 조건이 `(include_revoked or not c.revoked)` — 폐기 포함 모드면 모든 본인 device·기본은 활성만(slice 29 의미 보존). **PgDeviceStore.list_for_user**: SELECT 컬럼 5종(device_id, created_at, last_used_at, revoked, revoked_at)·`stmt = stmt.where(DeviceCredential.revoked.is_(False))`를 `if not include_revoked` 조건부로 적용 — *서버측* 필터링이라 network·메모리 절감. **CachedDeviceStore.list_for_user**: `include_revoked` 인자 그대로 inner에 전달(passthrough). **`GET /v1/devices?include_revoked=true`** 라우터: FastAPI 자연 bool 쿼리 파라미터(기본 False)·DeviceInfoSchema에 revoked/revoked_at 필드 추가(default False/None·extra="forbid" 유지). **테스트** (hermetic only) **+3 케이스**: ① **HTTP** `test_include_revoked_returns_revoked_devices`(d_active+d_revoked 등록·d_revoked 폐기 → 기본 호출은 d_active만·`?include_revoked=true`로 둘 다·polished entry는 revoked=true·revoked_at not null·active entry는 false·null). ② **InMemory** `test_include_revoked_includes_polished`(d1 폐기·d2 활성·`include_revoked=True`로 둘 다 반환·d1.revoked_at not None·d2.revoked_at None). ③ **Pg** `test_include_revoked_returns_revoked_rows`(WHERE revoked=False 절 조건부 제거 검증·둘 다 반환·d1.revoked True·d1.revoked_at not None). 기존 GET 응답 키셋 검증을 `{device_id, created_at, last_used_at, revoked, revoked_at}`로 확장 + 활성만 응답 시 `revoked=False`·`revoked_at is None` invariant 명시. `_CountingInnerStore.list_for_user`도 keyword-only `include_revoked` 인자 수용. **4게이트 green**: black·ruff·mypy-strict(86파일·`DeviceInfo` 기본값 NamedTuple 정합·SELECT 5컬럼 row indexing 정합)·pytest **1378 passed**(1375→+3 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 241/Branch 54/Miss 0(slice 36 234 → +7 include_revoked 분기 + revoked_at parity) · `devices.py` Stmts 45/Branch 2/Miss 0(slice 32 43 → +2 schema 필드). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **페이지네이션 미지원** — `include_revoked=true`로 *전 이력* 반환 시 1만건+ 가능. 슬라이스 29 한계 ③(페이지네이션 미지원)와 함께 후속(limit/offset 또는 cursor pagination) ② **filter 옵션 미지원** — "지난 30일 이내 폐기만" 등 시간창 필터 없음. UI 요구 보고 후속 ③ **revoked-only 모드 없음** — `revoked_only=true`(활성 제외)는 별도 옵션 후속·rare use case ④ **PII 경계 재검토** — `revoked_at`가 사용자 행동 패턴(언제 device 폐기했는지) 누설 가능. 본인 데이터라 본인 노출엔 안전·*타인에게 노출*되면 PII. 라우터는 user_id 스코프라 안전·관리자 표면 후속 시 마스킹 검토. **🎯 의미**: device 라이프사이클 *완전 조회 표면* — 등록(slice 22) → 활성 목록(slice 29) → 폐기(slice 22/24) → *폐기 이력 조회*(slice 37)의 네 단계가 사용자 self-service로 완비. Flutter "설정 → 기기 관리"에서 "활성 기기" 탭 + "폐기 기록" 탭 두 화면을 *하나의 엔드포인트*(`?include_revoked=true`)로 결선 가능. 슬라이스 33의 `cleanup_stale` 누락(revoked_at 미설정)도 *조용히* 수정 — InMemory·Pg parity 달성. **🎯 rate limit + device-cred 27 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·페이지네이션·`/v1/admin/devices/cleanup-stale` 엔드포인트·revoked_only 모드·시간창 필터·equal/decorrelated jitter·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 36 (startup ping jitter · AWS-style full jitter · thundering herd 차단)** 완료 (메인 hermetic·1373→1375 passed, 2026-06-03) — 슬라이스 35 결정 v1 한계 ①(fixed backoff 시퀀스·jitter 없음·thundering herd 위험) 자명 해소. k8s 다수 pod이 동시 재시작될 때 deterministic backoff(`2s, 4s, 8s`)면 *모든 pod이 정확히 같은 시각*에 재시도 → 인프라 회복 직후 *동시 폭주*(thundering herd) 위험 — AWS "full jitter" 패턴(`uniform(0, base*2^attempt)`)으로 균등 분포 무작위화 → 부하 시간축으로 *분산*. **신규 Settings** `device_store_health_check_retry_jitter: bool = False`(기본 off — 단일 노드/예측 가능 backoff 우선·기존 35개 테스트 영향 0). 다중 pod·k8s 운영은 True 권장. **`_retry_with_timeout(jitter: bool = False)` 확장**: keyword-only 인자 추가. `jitter=True`면 `delay = random.uniform(0, base)`(full jitter)·`False`면 deterministic `delay = base`. `import random`은 함수 내부(slice 35 패턴 그대로·startup path 최적화·import 비용 최소). **`ping_device_store_health`**: settings에서 jitter 읽어 PG/Redis ping 모두에 *동일 전달*(분리 설정 없음·실용성 우선). **테스트 인프라 진화**: `_lifespan_settings`에 `jitter: bool = False` 인자 추가(기본 False·기존 모든 ping 테스트 영향 0·jitter 동작 검증은 명시 인자). **테스트** (hermetic only) **+2 케이스** (`TestPingDeviceStoreHealth` 확장): ① `test_jitter_disabled_uses_deterministic_backoff` — `random.uniform` spy로 *호출 0회* 검증·`asyncio.sleep` spy로 인자가 정확히 `base * 2^0 = 2.0`인지(deterministic) 확인. ② `test_jitter_enabled_uses_uniform_random` — `random.uniform` stub이 `[(0.0, 2.0), (0.0, 4.0)]` 시퀀스로 호출(attempt=0,1의 base × 2^attempt)·stub이 `high * 0.5` 반환(mid-range·결정론적 테스트)·sleep 인자는 `[1.0, 2.0]`(stub 결과). **결정론적 jitter 테스트 패턴** — `random.uniform`을 `monkeypatch.setattr("random.uniform", ...)`로 stub해 *호출 인자*와 *반환값 사용처*를 검증(실 random은 flaky). 슬라이스 35의 `asyncio.sleep` spy 패턴과 결합. **4게이트 green**: black(src 86 파일·tests 분리)·ruff·mypy-strict(86 파일·`Callable[..., Awaitable[Any]]` 시그너처 정합·keyword-only jitter 인자 정합)·pytest **1375 passed**(1373→+2 hermetic, cov-fail-under=70, 17s — 행잉 없음). 신규 cov **100%**: `_device_store.py` Stmts 234/Branch 52/Miss 0(slice 35 230 → +4 jitter 분기) · `config.py` Stmts 58/Miss 0(+1 Settings). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **full jitter만 지원** — equal jitter(`base/2 + uniform(0, base/2)`·평균 보장)·decorrelated jitter(`uniform(base, prev_delay*3)`·이전 delay 의존) 미지원. 본 슬라이스는 *가장 단순·효과 검증된* full jitter(AWS Architecture Blog 권장)만 ② **PG/Redis 공유 jitter 설정** — 분리 인자 미지원(슬라이스 35 한계 ②와 동일). 둘 다 같은 인프라 패턴이라 분리 필요성 낮음 ③ **`random.uniform` 시스템 random** — 암호학적 random 아님(thread-local PRNG·예측 가능). thundering herd 차단엔 충분하나 *보안 critical* 시간 무작위화엔 `secrets.SystemRandom` 후속 ④ **base*2^attempt 단조 증가** — full jitter라도 base가 매 시도 2배 증가해 worst-case는 deterministic과 같음. 평균은 1/2 감소(균등 분포 평균). **🎯 의미**: startup health check의 **클라우드-스케일 견고성** — Phaiakes9 단일 노드는 jitter=False(예측 가능)·GCP 다중 pod 배포는 jitter=True 권장. 슬라이스 30(fail-fast)·31(timeout)·35(retry)·36(jitter)이 결합돼 startup health check가 *모든 운영 환경* 대응(단일/다중·정상/장애/일시 깜빡임/동시 재시작). **🎯 rate limit + device-cred 26 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·include_revoked·페이지네이션·`/v1/admin/devices/cleanup-stale` 엔드포인트·equal/decorrelated jitter·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 35 (startup ping 재시도 · exponential backoff · 두 한계 동시 해소)** 완료 (메인 hermetic·1370→1373 passed, 2026-06-03) — 슬라이스 30 결정 v1 한계 ③(재시도 없음)과 슬라이스 31 결정 v1 한계 ①(쿠버네티스 외부 재시도만)을 *한 슬라이스에 동시* 자명 해소. 일시적 인프라 깜빡임(네트워크 일순 단절·DB 재시작 직후·Redis failover 진행 중) 흡수 → startup이 즉시 죽지 않고 *회복 기회* 제공. **신규 Settings 2종**: `device_store_health_check_max_retries: int = 3`(ge=1·1=재시도 비활성·기존 slice 30/31 의미) + `device_store_health_check_retry_backoff_seconds: float = 2.0`(gt=0). 정상 인프라는 1회 통과(backoff 0)·일시 실패는 backoff 후 회복. **신규 헬퍼 `_retry_with_timeout(operation, *, timeout_seconds, max_retries, backoff_seconds)`**: 각 시도는 *독립* `asyncio.timeout`·실패(Exception 무관·TimeoutError 포함) 시 `backoff * 2^attempt`초 대기 후 재시도(attempt=0,1,2 → 2s·4s·8s)·마지막 시도 실패하면 *마지막 예외* raise(호출자가 isinstance로 TimeoutError vs Exception 분기). retry/timeout 직교 분리(헬퍼는 둘 다 모름·호출자가 메시지 차별화). **`ping_device_store_health` 리팩터**: PG/Redis 각 ping 본문을 *async closure*로 추출(`_pg_ping`)·`_retry_with_timeout`에 위임. 기존 TimeoutError/Exception 메시지에 *"(재시도 N회)"* 명시 — 운영자가 "이미 N번 시도했음·인프라 점검 필요" 즉시 인지. Redis cleanup `aclose()`는 *최종 finally*에서 한 번만(per-attempt 0 호출·연결 leak 0 invariant 유지·slice 30/31의 finally 패턴 보존). **테스트 인프라 진화**: `_lifespan_settings(timeout_seconds=5.0, max_retries=1, backoff_seconds=0.001)` 기본값 추가 — 기존 slice 31 timeout 테스트가 default `max_retries=3 × backoff=2.0`로 행잉할 뻔(~50s 추가)을 *비활성으로 복원*(0s). 재시도 동작 검증은 명시 인자(`max_retries=3, backoff=0.001`)로 격리. **테스트** (hermetic only) **+3 케이스**(`TestPingDeviceStoreHealth` 확장): ① `test_pg_ping_retries_then_succeeds`(`_FlakySession`이 첫 2회 ConnectionError·3회째 통과 → 총 3회 시도·예외 0) ② `test_pg_ping_all_retries_fail_raises_runtime_error`(`_AlwaysBadSession` → 3회 모두 실패 → RuntimeError("재시도 3회" match)·정확히 3회 시도) ③ `test_redis_ping_retries_then_succeeds`(`_FlakyRedis.ping` 첫 1회 실패·2회째 성공 → 2회 시도·`aclose`는 *1회만* 호출 — finally 단일 진입 invariant). 기존 13 hermetic + 1 통합은 timeout/시그너처 호환성 유지. **4게이트 green**: black·ruff·mypy-strict(86파일·`Callable[[], Awaitable[Any]]` 시그너처 정합·async closure 캡처 정합)·pytest **1373 passed**(1370→+3 hermetic, cov-fail-under=70, 17s — 행잉 없음). 신규 cov **100%**: `_device_store.py` Stmts 230/Branch 52/Miss 0(slice 34 215 → +15 retry helper + ping 리팩터) · `config.py` Stmts 57/Miss 0(+2 Settings). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **fixed backoff 시퀀스** — 2^attempt 곱셈만 지원. jitter(±랜덤)·full jitter·decorrelated jitter는 후속(thundering herd 위험 시) ② **PG/Redis 공유 retry 설정** — 둘 다 같은 max_retries·backoff. PG 안정, Redis 불안정 시나리오는 분리 설정 후속 ③ **재시도 metric 없음** — "ping이 N회 재시도 후 성공" 카운트 안 함. OTel meter 결선 시 자연 추가(slice 28과 동일 패턴) ④ **mid-attempt cancel 미고려** — `asyncio.CancelledError`는 Exception 자식·재시도 루프가 swallow할 수 있음. lifespan timeout과 충돌 시 후속 분석 필요. **🎯 의미**: startup이 **production-grade resilient** — k8s readiness probe(일반 10s 간격) 도래 전에 *앱이 스스로 회복*. 슬라이스 30(fail-fast 좀비 차단)·31(무한 대기 차단)·35(일시 깜빡임 회복)가 결합돼 startup health check의 *세 가지 시나리오*(정상/장애/일시 깜빡임) 모두 안전. 인프라 깜빡임을 lifespan 안에서 자체 흡수해 외부 재시도 의존성 축소. **🎯 rate limit + device-cred 25 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·include_revoked·페이지네이션·`/v1/admin/devices/cleanup-stale` 엔드포인트·jitter·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 34 (`cleanup_stale` 시그너처 진화 · dry_run + list 반환 + cache 일괄 invalidate)** 완료 (메인 hermetic·1366→1370 passed, 2026-06-03) — 슬라이스 33의 결정 v1 한계 ③(dry-run 미지원·"어느 device가 폐기될지" preview 불가)과 ⑤(cache 일괄 invalidate 미지원·`int` 반환이 device_id 식별 정보 손실) *두 가지를 동시에 자명 해소*. 반환 시그너처를 `int` → `list[str]`로 확장하니 두 가지 한계가 *동일한 데이터*(폐기된 device_id 목록)로 자연스럽게 해결됨 — 시그너처 진화가 본질을 드러낸 사례. **Protocol 시그너처 갱신**: `cleanup_stale(max_age_days: int, *, dry_run: bool = False) -> list[str]` — keyword-only `dry_run` 인자(API 명료성)·반환은 폐기된(또는 dry_run 시 *폐기될*) device_id 목록. 건수는 `len(result)`로 그대로 얻을 수 있어 슬라이스 33 호출자의 의미 보존. **InMemoryDeviceStore.cleanup_stale**: 폐기 후보 식별 로직은 그대로·`affected.append(device_id)`만 항상 실행·`_replace(revoked=True)`는 `if not dry_run` 가드. dry_run=True면 *상태 변경 0*·식별 목록만 반환(preview 패턴). **PgDeviceStore.cleanup_stale**: SELECT 단계는 그대로·`dry_run`이면 `return stale_ids`로 *조기 반환*(UPDATE 0 실행). dry_run=False면 기존 UPDATE 반복 후 commit. `await session.commit()`이 dry_run 경로엔 없음(읽기 트랜잭션은 commit 불필요·SQLAlchemy 자동 처리). **CachedDeviceStore.cleanup_stale** *핵심 진화*: 슬라이스 33은 "TTL 자연 만료에 위임"이라 stale 기간 ≤ 60s였으나, 슬라이스 34는 inner가 반환한 `affected` 목록을 받아 `await self._cache.delete(*[_VERIFY_CACHE_KEY_PREFIX + d for d in affected])`로 *정확히 그 키들만* 일괄 invalidate. stale 기간 즉시 0 — 슬라이스 26의 `revoke→DEL` invariant를 cleanup_stale로 확장. ① `affected`가 빈 리스트면 `cache.delete` 호출 0(불필요 RTT 회피·varargs로 빈 호출은 무동작 의미 모호 차단) ② `dry_run=True`면 inner도 상태 변경 0·캐시도 건드리지 않음(완전 read-only). **`_CountingInnerStore.cleanup_stale`** 시그너처 갱신(`dry_run` 인자 전달·Protocol 정합). **테스트** 2층: ① **hermetic 12 갱신 + 4 신규**: 기존 12 테스트는 `== int` → `== list[device_id]` 또는 `== []`로 갱신(슬라이스 33 의미 보존)·새 4 테스트는 `TestInMemoryDeviceStoreCleanupStale.test_dry_run_returns_list_without_revoking`(preview → revoked=False 유지, 실행 → revoked=True), `TestPgDeviceStoreCleanupStale.test_dry_run_returns_list_without_revoking`(dry_run → revoked·revoked_at 모두 None 유지), `TestCachedDeviceStoreCleanupStale.test_passthrough_and_cache_delete_for_each_affected`(캐시 미리 적재 → cleanup → 캐시 키 제거 + delete_calls=1), `test_no_affected_no_cache_delete`(0건 → delete_calls=0), `test_dry_run_does_not_invalidate_cache`(dry_run → 캐시 그대로). ② **통합 갱신**(`test_cleanup_stale_devices_on_live_pg`): 기존 호출을 `dry_run=True` preview(== `[d_stale]`·실제 폐기 0) + 실 실행(같은 list 반환·revoked=True) 두 단계로 확장. **4게이트 green**: black·ruff·mypy-strict(86파일·`list[str]` 반환 시그너처·keyword-only `*, dry_run` 정합)·pytest **1370 passed**(1366→+4 hermetic 신규, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 215/Branch 48/Miss 0(slice 33 211 → +4 dry_run 분기 + cache 일괄 invalidate). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **affected 목록 ordering 미보장** — InMemory는 dict 순서(insertion order Python 3.7+)·Pg는 SELECT 무 ORDER BY라 비결정적. 운영 자동화에 *순서*가 의미 있으면 `ORDER BY created_at` 추가 후속(현 클라이언트는 set으로 처리 권장) ② **affected list 크기 N+1 SELECT** — Pg는 여전히 N+1 UPDATE(슬라이스 33 한계 ② 잔존). 단일 SQL UPDATE 후속 ③ **HTTP 엔드포인트 미노출** — `cleanup_stale(dry_run=True)`이 preview UI 기반인데 라우터에 안 묶음. `/v1/admin/devices/cleanup-stale?dry_run=true` admin 엔드포인트는 후속 ④ **affected list 메모리 부담** — 1만건 stale 시 list 1만 element. Pg cron 시나리오엔 OK·실시간 high-throughput 호출 시 stream 패턴(generator) 후속. **🎯 의미**: 시그너처 한 줄 진화(`int` → `list[str]`)가 *서로 다른 두 한계*(preview 미지원·캐시 stale)을 *동일한 데이터*로 해결 — API 설계에서 "값의 풍부함이 추상화의 풍부함"임을 보여주는 사례. CachedDeviceStore.cleanup_stale이 slice 26 revoke 패턴과 *대칭 invariant*(모든 폐기 경로에서 cache invalidate) — 슬라이스 26·33·34가 결합돼 cache 일관성 모델 완비. dry_run preview는 운영 안전성 핵심(대규모 cleanup 전 *영향 범위* 확인). **🎯 rate limit + device-cred 24 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·include_revoked·페이지네이션·`/v1/admin/devices/cleanup-stale` 엔드포인트(dry_run 쿼리 파라미터)·단일 SQL UPDATE·affected ordering·ping 재시도·degrade 폴백·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 33 (`cleanup_stale_devices` · N일 미사용 자동 폐기 인프라)** 완료 (메인 hermetic·1354→1366 passed, 2026-06-03) — 슬라이스 32 결정 v1 한계 ②(자동 폐기 cron 미작성) 자명 해소. 슬라이스 32에서 추가한 `last_used_at`을 활용해 "N일 미사용 device 자동 폐기" 정책 구현 — 잊혀진/분실/중고 양도된 device를 *시스템 자동*으로 무효화해 spoofing 공격면 축소·DB 자격증명 행 누적 방지. **신규 Settings** `device_credential_max_idle_days: int = 30`(ge=1). 30일 기본 — 정상 사용자(매월 1회 이상 앱 사용·verify)는 무영향·잊혀진/방치 device만 폐기. **Protocol 확장** `cleanup_stale(max_age_days: int) -> int`: 폐기 건수 반환 — *시스템 호출*이라 owner_id 검증 없음(관리자/cron 권한 의미). 조건: `revoked=False AND ((last_used_at < now - N일) OR (last_used_at IS NULL AND created_at < now - N일))` — 한 번도 verify 안 된 device는 created_at 기준(등록만 하고 방치된 device 차단·"등록 후 즉시 잊음" 시나리오 대응). **InMemoryDeviceStore.cleanup_stale**: dict 순회 — `cred.last_used_at or cred.created_at < threshold`이면 `_replace(revoked=True)`. 이미 폐기된 device skip(idempotent). **PgDeviceStore.cleanup_stale**: `SELECT (device_id, last_used_at, created_at) WHERE revoked=False` → Python 필터링 → 각 stale device_id에 UPDATE 반복. `func.coalesce`/단일 SQL UPDATE 대안 가능하나 *기존 `_FakePgSession` 인프라 호환·코드 가독성* 우선해 2단계 분리 — 일일 cron 호출이라 O(N) 반복 허용(device_credential table은 사용자수×평균 device 수로 bounded·~10k 수준). 트랜잭션은 단일 session으로 atomic(SELECT + 모든 UPDATE + commit). **CachedDeviceStore.cleanup_stale**: inner 위임. 폐기된 device의 캐시 키 일괄 invalidate *안 함* — 어느 device가 폐기됐는지 inner 반환값에 없음(count만). TTL 자연 만료로 stale 기간 ≤ TTL(기본 60s). "N일 미사용 자동 폐기" 정책은 60s 오차 무관(슬라이스 32 lazy 패턴과 동일 트레이드오프). **`_CountingInnerStore`에 `cleanup_calls` 카운터** + `cleanup_stale` 메서드 추가(Protocol 정합·테스트 인프라). **운영 호출 패턴**: Celery beat 일일 스케줄(예: `crontab(hour=3, minute=0)`) 또는 외부 cron(`python -c "import asyncio; ...; asyncio.run(store.cleanup_stale(30))"`). 별도 endpoint 노출은 후속(`/v1/admin/devices/cleanup-stale`·admin auth 필요). **테스트** 2층: ① **hermetic +12 케이스**: `TestInMemoryDeviceStoreCleanupStale` 6종(empty·fresh 무영향·last_used_at stale·never-used + created_at stale·already revoked skip·mixed: fresh+stale → fresh만 보존). `TestPgDeviceStoreCleanupStale` 5종(같은 시나리오·`_FakePgSession`이 select·update 둘 다 처리). `TestCachedDeviceStoreCleanupStale` 1종(passthrough·`counter.cleanup_calls==1`·`cache.delete_calls==0`로 cache invalidate 안 함 검증). ② **통합 +1**(`test_cleanup_stale_devices_on_live_pg`): 실 PG에 fresh+stale 등록·`UPDATE device_credential SET created_at=...`로 stale의 created_at을 31일 전으로 강제·`cleanup_stale(30)` 호출 → 폐기 건수 1·fresh 활성 유지·stale revoked=True + revoked_at 채워짐. `_FakePgSession`이 이미 select·update를 처리하므로 *코드 변화 0*. **4게이트 green**: black·ruff·mypy-strict(86파일·timedelta import 추가)·pytest **1366 passed**(1354→+12 hermetic + 1 통합 추가/skip, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 211/Branch 42/Miss 0(slice 32 182 → +29 cleanup_stale 3 구현체 + Protocol) · `config.py` +1 필드. 전체 cov 99.50%. **결정 v1 한계**(후속): ① **HTTP admin 엔드포인트 미노출** — 본 슬라이스는 *함수*만. `/v1/admin/devices/cleanup-stale`은 admin auth 정책(role-based?)과 함께 별 슬라이스. 운영자는 직접 호출 또는 Celery beat ② **N+1 UPDATE 비효율** — 단일 SQL `UPDATE ... WHERE coalesce(last_used_at, created_at) < threshold AND revoked=false`로 O(1) round-trip 가능. 본 v1은 fake 인프라 호환 우선. 부하 보고 후 단일 SQL 전환 가능(behavior 동등) ③ **dry-run 모드 없음** — 운영자가 "어느 device가 폐기될지" 미리 보려면 `dry_run=True` 인자 필요. 후속 ④ **per-user 호출 미지원** — 시스템 전역 호출만. "사용자 A의 stale device만 폐기"는 후속(쓸 곳 있을 때) ⑤ **cache 일괄 invalidate 미지원** — cleanup_stale이 폐기 device_id 목록 반환하면 가능하나 시그너처 변경 필요. v2에서 `cleanup_stale(...) -> list[str]`로 확장 시 자명. **🎯 의미**: device 자격증명에 *시간 차원의 자동 위생*(auto-hygiene) — 30일+ 방치된 잊혀진 device(중고폰 양도·앱 삭제·기기 분실)가 자동 폐기돼 *공격면이 시간에 따라 축소*. 슬라이스 32의 last_used_at 인프라가 *실제 운영 정책*으로 구체화. **🎯 rate limit + device-cred 23 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·include_revoked·페이지네이션·`/v1/admin/devices/cleanup-stale` 엔드포인트·dry-run·단일 SQL UPDATE·cache 일괄 invalidate·ping 재시도·degrade 폴백·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 32 (`DeviceCredential.last_used_at` · verify 성공 시각 추적)** 완료 (메인 hermetic·1348→1354 passed, 2026-06-03) — 슬라이스 29 결정 v1 한계 ②(`last_used_at` 미추적) 자명 해소. "30일 미사용 자동 폐기" 정책·보안 이상 탐지·Flutter UI "마지막 사용 N분 전" 표시 기반 인프라 도입. **ORM 컬럼 추가** `DeviceCredential.last_used_at: Mapped[datetime | None]`(`sa.DateTime(timezone=True)`, nullable). **신규 마이그레이션** `b2c3d4e5f6a7`(`down_revision=a1b2c3d4e5f6`): `ADD COLUMN last_used_at TIMESTAMPTZ NULL` — 기본 NULL이라 기존 행 무영향(register 시점부터 None·verify에서 채워짐). **InMemoryDeviceStore 내부 행 NamedTuple 리팩터**: 이전 4-tuple(`(user_id, secret_plain, revoked, created_at)`)을 `_CredRecord` NamedTuple 5필드(+last_used_at)로 전환 — 인덱스 접근 → 이름 접근으로 가독성·유지보수성 향상(`cred.user_id` 대신 `cred[0]`)·`_replace`로 *불변 업데이트* 가능. **verify 성공 시 last_used_at 갱신**: InMemory는 `cred._replace(last_used_at=datetime.now(UTC))`로 dict 재할당·Pg는 `row.last_used_at = datetime.now(UTC)` + `await session.commit()`. *실패 경로*(미등록·잘못된 sig·폐기)는 갱신 안 함 — 공격자가 last_used_at으로 device 존재 여부·polling으로 정상 사용 시각 추론 못 함(타이밍/측정 점 차단). **CachedDeviceStore cache hit 미갱신**: inner.verify를 *우회*하므로 last_used_at은 *대략* 신선(정밀도 ≈ cache TTL=60s). 의도된 트레이드오프 — "30일 미사용 자동 폐기" 정책은 60s 오차 무관. 고QPS 사용자도 cache 만료 시 1회 갱신으로 충분(*매 verify가 UPDATE라면 cache 무의미해짐* — 본 lazy 패턴이 cache 효과 보존). **`DeviceInfo` NamedTuple 확장**: `last_used_at: datetime | None = None`(default None은 NamedTuple 기본값 패턴). PgDeviceStore.list_for_user의 SELECT 컬럼 확장(`device_id, created_at, last_used_at` 3-튜플). **`DeviceInfoSchema` + `DeviceListResponse`**: `last_used_at: datetime | None = None` 추가·docstring에 정밀도 명시. **테스트** (hermetic only) **+6 케이스**: ① **InMemory TestInMemoryDeviceStoreLastUsedAt** 4종 — register 직후 None·verify 성공 시 갱신(None → datetime)·잘못된 sig는 None 유지(공격 측정 차단)·revoke 후 verify는 False·revoke가 last_used_at 건드리지 않음(stale 보존, `_creds[device_id].last_used_at` 직접 확인) ② **PgDeviceStore TestPgDeviceStoreListForUser 확장** 2종 — `test_verify_success_updates_last_used_at`(verify 성공 → `store_dict[device_id].last_used_at is not None`·list 응답에도 노출) · `test_verify_failure_does_not_update_last_used_at`(잘못된 sig → None 유지). 기존 GET 응답 키셋 검증을 `{device_id, created_at, last_used_at}`로 확장. `_FakePgSession._exec_select`는 `getattr(row, name)`로 컬럼 추출하므로 last_used_at 자동 처리(기존 인프라 무변). **4게이트 green**: black·ruff·mypy-strict(86파일·NamedTuple default 값 정합·`row.last_used_at = ...` Mapped 대입 통과)·pytest **1354 passed**(1348→+6 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 182/Branch 34/Miss 0(slice 31 170 → +12 _CredRecord/last_used_at) · `devices.py` Stmts 43/Branch 2/Miss 0(+1 schema 필드) · `device.py` ORM 16/0(+1 컬럼). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **cache hit 정밀도 = TTL** — 자동 폐기 정책이 분/초 단위가 아니면 무영향. 분 단위 정확 추적 필요 시 cache invalidation hook(매 cache hit에 fire-and-forget UPDATE)으로 강화 가능(연결 풀 부담 트레이드) ② **자동 폐기 cron 미작성** — 본 슬라이스는 *추적 인프라*만. "WHERE last_used_at < now() - interval '30 days' AND revoked=false" 배치 작업은 후속 ③ **시계 동기화 가정** — `datetime.now(UTC)`는 *각 워커의 시계*. 다중 워커 시계 차 ms 단위는 무영향(이상 탐지 정밀도 충분)·시계 drift 큰 환경은 NTP 운영 책임 ④ **재등록 시 last_used_at 초기화** — register는 항상 None부터 시작. 분실 기기 *재발견* 후 같은 secret 재등록 시나리오는 *없음*(register는 항상 새 secret). **🎯 의미**: device 자격증명에 *시간 차원* 추가 — Flutter UI에 "갤럭시 S24 · 마지막 사용 2시간 전"·"맥북 · 한 번도 사용 안 됨" 표시 가능 → 사용자가 *실제 사용 중인* device와 *오래된* device를 구분해 안전하게 폐기. 보안 이상 탐지 기반(예: "30일 갑자기 깨어난 device"는 도난 의심) 활용 가능. **🎯 rate limit + device-cred 22 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·include_revoked·페이지네이션·자동 폐기 cron·cache invalidation hook·ping 재시도·degrade 폴백·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 31 (startup ping 타임아웃 · asyncio.timeout · 무한 대기 방지)** 완료 (메인 hermetic·1346→1348 passed, 2026-06-03) — 슬라이스 30 결정 v1 한계 ②(ping 타임아웃 미설정·인프라 응답 지연 시 startup *무한 대기* 가능) 자명 해소. `asyncio.timeout` 컨텍스트매니저로 각 ping(PG·Redis)에 최대 응답 시간 강제 — 초과 시 `TimeoutError`를 `RuntimeError`로 감싸 fail-fast(uvicorn startup 거부 유지). **신규 Settings** `device_store_health_check_timeout_seconds: float = 5.0`(gt=0). 기본 5s는 정상 인프라(LAN <1ms·Phaiakes9·동일 클러스터)엔 충분·이를 초과하면 인프라 점검 신호. 네트워크 깜빡임은 쿠버네티스 readiness probe로 외부 재시도(slice 30 한계 ③ 외부 위임). **`ping_device_store_health` 갱신**: PG와 Redis 각 ping을 `async with asyncio.timeout(timeout)`로 감쌈. **`TimeoutError` 분기 추가** — 기존 `Exception` 분기와 *별도*로 catch해 메시지 차별화: ① TimeoutError → "PG/Redis ping이 Ns 내 응답 없음. 인프라 장애 의심" + URL·네트워크 확인 + timeout 상향 안내 ② Exception → "PG/Redis 미도달: {원인}" + 폴백 모드 안내. 운영자가 *연결 실패*(설정·인증 문제)와 *응답 지연*(인프라 로드·네트워크 패킷 손실)을 즉시 구분 가능. **finally cleanup invariant 유지**: Redis ping이 TimeoutError로 실패해도 `aclose()` 호출 보장(slice 30 연결 leak 0 invariant 그대로). **`_lifespan_settings` 테스트 헬퍼 확장**: `timeout_seconds` 인자 추가(기본 5.0). 기존 5종 ping 테스트는 영향 0(기본값 그대로). **테스트** (hermetic only) **+2 케이스**(`TestPingDeviceStoreHealth` 확장): ① `test_pg_ping_timeout_raises_runtime_error` — `_HangingSession.execute`가 `asyncio.sleep(60)` 무한 대기·timeout=0.01s 설정 → `RuntimeError("PostgreSQL ping.*응답 없음")` match. ② `test_redis_ping_timeout_raises_runtime_error_and_closes` — `_HangingRedis.ping`이 sleep(60)·timeout=0.01s → RuntimeError(Redis 메시지) + `aclose_called["hit"] is True`(cleanup 보장 확인). 기존 5 hermetic은 timeout=5s 기본값으로 통과(테스트 행잉 0). **4게이트 green**: black·ruff·mypy-strict(86파일·`async with asyncio.timeout` Python 3.11+ 정합)·pytest **1348 passed**(1346→+2 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 170/Branch 30/Miss 0(slice 30 162 → +8 timeout 분기) · `config.py` Stmts 55/Miss 0(+1 Settings 필드). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **재시도 없음** — 일시적 인프라 깜빡임에도 timeout 시 즉시 fail-fast. 쿠버네티스 readiness probe로 외부 재시도 가능하나 내장 retry 옵션(exponential backoff)은 후속(slice 30 한계 ③ 그대로) ② **공유 timeout** — PG와 Redis가 같은 timeout 사용. PG는 ms급 응답 기대(LAN)·Redis도 ms급이라 분리 필요성 낮음. 별 timeout 옵션은 운영 데이터 보고 후속 ③ **timeout 단위 float** — 1ms 미만 시 ge=0 검증만 통과. 실용성 한계는 `gt=0`로 명시(0 비활성화 의도 없음). **🎯 의미**: startup health check가 **완전 fail-fast** — 어떤 시나리오(연결 실패·응답 지연·timeout)에서도 startup이 *bounded time* 내 결론. 좀비 인스턴스(slice 30)와 startup 행잉(slice 31) 두 production 시나리오 모두 차단. **🎯 rate limit + device-cred 21 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·include_revoked·last_used_at·페이지네이션·ping 재시도·degrade 폴백·OTel meter·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 30 (startup health check · PG/Redis ping · fail-fast)** 완료 (메인 hermetic·1340→1346 passed, 2026-06-03) — 슬라이스 27의 결정 v1 한계 ①(Redis 연결은 lazy·미도달 시 *첫 verify 요청에서야* 오류 발생·좀비 인스턴스 위험) 자명 해소. 부팅 시 store가 의존하는 PG/Redis 도달성 ping → 미도달 시 `RuntimeError`로 *fail-fast*(uvicorn이 startup 거부 → 트래픽 수신 불가). 좀비 인스턴스(앱은 떴으나 모든 verify가 connection error로 실패하는 상태) 방지. **신규 `ping_device_store_health(settings)` async 함수**: 모드별 분기 — `none`은 검증 0(slice 21 폴백 동작 보존) · `pg`는 PG ping(`SELECT 1` via sessionmaker)만 · `pg_cached`는 PG ping + Redis ping. 실패 시 **`RuntimeError(메시지)` raise** — 메시지에 ① 어느 구성요소 실패(PG vs Redis) ② 폴백 가능 모드(`device_store_mode=none` 또는 `=pg`) 명시 → *운영 진단성*. **`_CacheClient` Protocol 확장**: `ping()` 메서드 추가 — 좁은 인터페이스 일관성 유지(GET/SETEX/DEL/PING 4 메서드만·redis.asyncio.Redis 자연 만족). **finally cleanup invariant**: Redis ping 성공/실패 무관하게 `aclose()` 호출 — try/finally 패턴으로 *연결 leak 0*. `getattr(client, "aclose", None)`로 *런타임 호환 체크*(고대 redis-py·`close` 메서드 변형 호환). **`app.py._lifespan` 갱신**: `settings = get_settings()` → `await ping_device_store_health(settings)` → `build_device_store_from_settings(...)` → `set_device_store(store)` 순서. ping 실패는 lifespan startup 자체를 실패시켜 uvicorn이 트래픽 수신을 거부. **기존 slice 27 lifespan 테스트 갱신**: `test_lifespan_with_pg_mode_activates_pg_store`는 `ping_device_store_health`를 monkeypatch로 no-op 처리 — 본 테스트는 *lifespan 결선만* 검증·실 PG 무관. **테스트** 2층: ① **hermetic +6 케이스**(`TestPingDeviceStoreHealth`): none 모드 skip(sessionmaker·redis builder 호출 0 spy로 검증)·pg 모드 sessionmaker.execute 호출 검증(redis builder 호출 0)·pg 모드 PG 미도달 시 RuntimeError("PostgreSQL 미도달" 메시지 match)·pg_cached 모드 양쪽 ping(`_OkRedis.ping` + `aclose` 둘 다 호출 카운트)·Redis 미도달 시 RuntimeError("Redis 미도달") + **finally가 aclose 여전히 호출**(연결 leak 0 보증)·aclose 미정의 Redis 클라이언트(고대 redis-py 시뮬레이션) 안전 통과. ② **통합 +1**(`test_ping_health_succeeds_on_live_pg_and_redis`): 라이브 PG·Redis 둘 다 도달 시 pg·pg_cached 두 모드 모두 ping 통과·Redis 미도달 시 통합 자체 skip(라이브 인프라 의존). **4게이트 green**: black·ruff·mypy-strict(86파일·`async def ping`·`Protocol` 확장 정합)·pytest **1346 passed**(1340→+6 hermetic + 1 통합 추가/skip, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 162/Branch 30/Miss 0(slice 29 140 → +22 ping + Protocol 확장). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **ping 실패 시 *no degrade* 폴백** — `pg_cached`에서 Redis 미도달 시 자동 `pg` 모드 강등 안 함. 운영자가 settings 수정 후 재시작 필요. degrade 자동화는 후속(설정 복잡도 우선 단순 fail-fast) ② **ping 타임아웃 미설정** — Redis/PG 응답 지연 시 startup이 *무한 대기* 가능. asyncio.timeout(5초) 등 명시 timeout 도입 후속 ③ **재시도 없음** — 일시적 네트워크 깜빡임에도 startup 실패. exponential backoff 재시도(예: 3회·2초·4초·8초)는 후속(쿠버네티스 healthcheck로 외부 재시도 대안 있음) ④ **PG/Redis 외 의존성 미체크** — Anthropic/Ollama/Langfuse 등은 lazy 그대로(slice 30 범위 밖·LLM 호출 시점에서야 검증). LLM provider 부팅 ping은 별 슬라이스. **🎯 의미**: device 인증 인프라가 **fail-fast production startup** 도달 — Phaiakes9에서 `WHYMATH_DEVICE_STORE_MODE=pg_cached`로 배포 시 PG/Redis 미설정/네트워크 차단을 *부팅 즉시* 인지(0.1초 내) → 잘못 설정된 운영자는 *트래픽 시작 전에* 오류 메시지로 알림. 좀비 인스턴스로 인한 *침묵 실패*(silent failure: 앱 떠있으나 verify가 매번 5xx) 차단. 슬라이스 27~30이 결합돼 *완전한 production deployment lifecycle*: lifespan 활성(27) + cache 데코레이터(26) + 모드 자동 활성(27) + 시작 시 ping(30). **🎯 rate limit + device-cred 20 슬라이스 누적 — production-grade 운영 표면 완비**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·include_revoked 모드·last_used_at·페이지네이션·ping 타임아웃·재시도·degrade 폴백·OTel meter 어댑터·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 29 (`GET /v1/devices` 본인 활성 디바이스 목록)** 완료 (메인 hermetic·1329→1340 passed, 2026-06-03) — 슬라이스 22 이래 비어있던 사용자 표면(자기 device 조회) 결선. Flutter "디바이스 관리" UI의 1차 결선 — 사용자가 자기 등록 device 목록 확인 후 분실/도난 디바이스를 `/v1/devices/{id}/revoke`로 폐기 가능(slice 24 본인 소유 검증 정합). **Protocol 확장**: `list_for_user(owner_id) -> list[DeviceInfo]`. **신규 `DeviceInfo` NamedTuple**(device_id·created_at) — *내부 표현*. secret_plain·user_id·revoked 등은 노출 안 함(*보안 표면 최소화* — store→라우터 seam에서 PII/내부 상태 차단). **InMemoryDeviceStore**: 내부 dict 값을 4-tuple로 확장(`(user_id, secret_plain, revoked, created_at)`)·register는 `datetime.now(UTC)` 채움·verify/revoke 모두 4-tuple 언패킹으로 갱신. list_for_user는 owner_id 스코프(c[0] == owner_id) + revoked 제외(not c[2]) + created_at DESC 정렬. **PgDeviceStore**: `SELECT device_id, created_at WHERE user_id=X AND revoked=False ORDER BY created_at DESC` — 서버측 필터링·정렬(network·메모리 절감) + 결과 row 튜플을 `DeviceInfo`로 매핑. **CachedDeviceStore**: 목록은 *캐시 안 함*(등록/폐기 직후 신선도 우선·관리 표면은 빈도 낮음·자기 device 목록은 *사용자별*이라 캐시 키 공간 폭발 위험) — 그대로 inner 위임. **신규 라우터 `GET /v1/devices`**: `ConsentedUser`(인증된 사용자)·`user.user_id`로 스코프(타인 device 노출 0)·`DeviceListResponse` Pydantic(`devices: list[DeviceInfoSchema]`·`device_id`·`created_at` ISO8601)·response_model로 secret_plain·user_id 비노출 정합(Pydantic 직렬화 자체가 표면 차단). 503(store 미설정)·401(인증 없음) 슬라이스 22 패턴 정합. **`_FakePgSession.execute` 확장**(테스트 인프라): Update/Select 두 분기 — `hasattr(stmt, "_values")`로 분기(Update만 `_values` 보유). Select 경로(`_exec_select`)는 ① `_iter_eq_constraints`로 whereclause 등식 제약 추출 ② store에서 모든 제약 만족하는 행 필터링 ③ `_order_by_clauses` 존재 시 `created_at` 키로 reverse sort(쿼리당 하나 ORDER BY 가정) ④ `stmt._raw_columns`에서 .key로 컬럼명 추출 → 행 속성을 튜플로 조립 ⑤ `.all()` 메서드 가진 가짜 result 반환. update 분기(`_exec_update`)는 slice 24 기존 그대로. **테스트** 2층: ① **hermetic +11 케이스** — TestListDevicesEndpoint 5종(빈 목록·본인 활성만/타인·폐기 제외·created_at DESC 정렬·503·401·secret_plain 등 키 노출 0) · TestInMemoryDeviceStoreListForUser 3종(empty·owner scope·revoked excluded) · TestPgDeviceStoreListForUser 2종(empty·scope+excludes_revoked + created_at 동등성 확인) · TestCachedDeviceStoreListForUser 1종(2회 호출 시 inner 두 번·cache 0 접근 — passthrough invariant) ② **통합 +1**(`test_list_devices_endpoint_on_live_pg`): A 2-device 등록·B 1-device 등록·A의 d1 폐기 → A의 GET은 d2만(키 정확히 `{device_id, created_at}`)·B token 무관·401 무토큰. **4게이트 green**: black(src/tests 분리·devices.py line-length 100 정합)·ruff·mypy-strict(86파일·`DeviceInfo` NamedTuple Protocol 정합·`SELECT row → DeviceInfo(*row)` 통과)·pytest **1340 passed**(1329→+11 hermetic + 1 통합 추가/skip, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 140/Branch 24/Miss 0(slice 28 124 → +16 DeviceInfo+3 list_for_user 구현) · `devices.py` Stmts 42/Branch 2/Miss 0(slice 28 29 → +13 GET 라우터+Pydantic). 전체 cov 99.50%. **결정 v1 한계**(후속): ① **폐기 device 포함 모드 없음** — `include_revoked=true` 쿼리 파라미터로 폐기 이력 조회는 후속(GDPR·보안 감사 표면) ② **last_used_at 미추적** — 디바이스가 *마지막* verify된 시각은 별도 컬럼 필요(보안 이상 탐지: "30일 미사용 device 자동 폐기" 정책 후속) ③ **페이지네이션 없음** — 한 사용자 device 수가 *수십~수백* 되는 경우 응답이 클 수 있음. limit/offset·cursor pagination은 도메인 잡힌 후 후속(일반 사용자는 1~5개 가정) ④ **`_raw_columns` 의존성** — `_FakePgSession._exec_select`가 SQLAlchemy 2.0 `_raw_columns` 내부 attribute에 의존. 2.x 마이너 업그레이드 시 깨질 수 있음·통합 테스트가 안전망. **🎯 의미**: device 라이프사이클 *완전 결선* — `POST /register`(slice 22) → `GET /devices`(slice 29) → `POST /revoke`(slice 22/24) 세 표면이 사용자 self-service로 완비. Flutter 앱의 "설정 → 디바이스 관리" 화면이 즉시 구현 가능(목록 fetch → 카드 표시 → 폐기 버튼). 본인 소유 invariant(slice 24)와 정확히 결합 — 타인 device 노출 0·폐기 0. **🎯 rate limit + device-cred 19 슬라이스 누적 — user self-service device 라이프사이클 완비**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·`include_revoked` 모드·last_used_at·페이지네이션·startup health check·OTel meter 어댑터·`/v1/admin/metrics`·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 28 (디바이스 sig 검증 실패 metric · production spoofing 가시화)** 완료 (메인 hermetic·1319→1329 passed, 2026-06-03) — 슬라이스 21/24/25/27 모두 후속으로 명시했던 "검증 실패 가시성 미확보" 한계 자명 해소. `_client_device_id`의 4개 fail-safe None 분기가 *조용히 device 차원을 비활성*시키던 invariant를 *분기별 카운터 증가*로 가시화 — 운영자가 spoofing 폭주·앱 버전 mismatch·중간 인증서 인터셉트 조기 감지 가능. **신규 모듈 `api/_device_metrics.py`**(11 LOC): `Counter[str]` 모듈 전역 + `record_device_sig_failure(reason)`(slice 28 카운터 +1)·`get_device_sig_failures() -> dict[str, int]`(스냅샷)·`reset_device_sig_failures()`(테스트 격리·운영 rollover). `DeviceSigFailureReason = Literal["store_no_sig", "store_verify_failed", "shared_no_sig", "shared_invalid_sig"]` — 4 사유: store 모드 X-Device-Sig 누락 / store verify=False(미등록·잘못된 sig·폐기 device 모두) / 공유 secret 모드 sig 누락 / 공유 secret hmac 불일치. **`_client_device_id` 4개 분기 hook**: 각 fail-safe None 반환 *직전*에 `record_device_sig_failure(...)` 한 줄 — 분기 경로 보존(fail-safe 의미 그대로). store 모드(slice 22+)와 공유 secret 모드(slice 21)를 분리한 카운터로 운영 대시보드에서 어느 검증 경로에 공격이 집중되는지 즉시 식별 가능. **정상 경로 invariant 보존**: 유효 sig 통과(store/공유 secret 둘 다)·X-Device-Id 헤더 누락(slice 20 의미·*공격 시도 아님*)은 *모든 카운터 0*. 정상 트래픽 노이즈 0 → 카운터>0은 *spoofing 시그널*로 해석 가능. **범위 v1**: 인메모리 카운터(단일 프로세스). 다중 워커/HA 시 워커별 분리(rate_limit 인메모리 backend와 같은 한계). OTel/Langfuse 결선은 후속 — `record_device_sig_failure`을 OTel meter로 매핑하는 어댑터 추가만으로 production-grade(현재 카운터 dict 구조가 자연스러운 매핑). **신규 autouse fixture** `_reset_device_metrics`(테스트별 카운터 0 복원·다른 테스트 누수 차단). **테스트** (hermetic only) **+10 케이스**(`TestDeviceSigFailureMetrics`): ① store 모드 sig 누락 → `store_no_sig` 1 ② store 모드 잘못된 sig → `store_verify_failed` 1 ③ store 모드 미등록 device → 같은 `store_verify_failed`(verify=False라 사유 분해 안 함·OTel 후속에서 세분화) ④ 공유 secret 모드 sig 누락 → `shared_no_sig` ⑤ 공유 secret 모드 invalid sig → `shared_invalid_sig` ⑥ store 정상 sig → 카운터 0(invariant) ⑦ 공유 secret 정상 sig → 카운터 0 ⑧ X-Device-Id 누락 → 카운터 0(비활성·공격 아님) ⑨ 다중 사유 누적 — 3회 `shared_no_sig` + 2회 `shared_invalid_sig` 별 키 ⑩ `reset_device_sig_failures()` 모든 카운터 0. **4게이트 green**: black·ruff·mypy-strict(86 파일·`Literal[...]` 시그너처 정합·`Counter[str]` 타입 통과)·pytest **1329 passed**(1319→+10 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_metrics.py` Stmts 11/Miss 0 · `_rate_limit.py` Stmts 318/Branch 96/Miss 0(slice 27 313 → +5 hook lines). **결정 v1 한계**(후속): ① **인메모리 단일 프로세스** — 다중 워커는 워커별 분리. 분산 카운터(Redis HINCR)·OTel meter는 후속. 정상 운영(단일 Phaiakes9·소규모 GCP) 무영향 ② **store verify=False 사유 미분해** — 미등록 device vs 잘못된 sig vs 폐기 device 모두 한 카운터. store API에 사유 구분 반환값 도입 시 분해 가능(`verify_with_reason() -> (bool, Reason)`) ③ **시간창 미고려** — 카운터는 *누적*. 시간창 buckets(분당/시간당) + alerting threshold는 OTel/Prometheus 후속 ④ **admin/metrics 엔드포인트 없음** — `get_device_sig_failures()` 프로그래밍 호출만 가능. `/v1/admin/metrics`(인증 필요) 노출은 후속 슬라이스(인증·인가 정책 결정과 함께). **🎯 의미**: device 인증 표면이 **observable production system** — 운영자가 spoofing 시도 0→비전·시간 폭주→공격 패턴·앱 버전 deploy 후 `store_no_sig` 급증→signing 미적용 클라이언트 버그 즉시 인지. 슬라이스 21 도입 이래 6 슬라이스 동안 누적된 보안 표면(unique secret·OAuth flow·rate limit·본인 소유 검증·Redis 캐시·lifespan 결선)이 *모니터링 가능*해짐 — 보안 시스템의 마지막 빈 자리(가시성) 채움. 코드 변화 최소(11 LOC 모듈 + 4 hook line)로 즉시 효과. **🎯 rate limit + device-cred 18 슬라이스 누적 — observable production-grade**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·`GET /v1/devices` 본인 목록·startup health check·OTel meter 어댑터·`/v1/admin/metrics` 엔드포인트·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 27 (device store lifespan 결선 · Settings 모드 자동 활성)** 완료 (메인 hermetic·1313→1319 passed, 2026-06-03) — 슬라이스 26 결정 v1 한계 ④(`CachedDeviceStore`는 표면만 제공·lifespan 결선 후속) 자명 해소. 운영자는 환경변수 `WHYMATH_DEVICE_STORE_MODE=pg_cached` 한 줄로 store 자동 활성·shutdown 정리 받음 — `set_device_store` 코드를 직접 작성할 필요 없음. **신규 Settings** `device_store_mode: Literal["none", "pg", "pg_cached"] = "none"`(기본은 slice 21 폴백 보존). 세 모드: ① `none` — store 비활성·slice 21 공유 secret 폴백 동작 ② `pg` — `PgDeviceStore(get_sessionmaker())` 단독(slice 23) ③ `pg_cached` — `CachedDeviceStore(PgDeviceStore, Redis, ttl=settings.device_verify_cache_ttl_seconds)` 래핑(slice 26). **신규 `build_device_store_from_settings(settings) -> (store, cleanup_fn)`**: 순수 함수(set_device_store는 호출자 책임). `none`/`pg`는 cleanup_fn=async noop. `pg_cached`는 cleanup_fn이 redis 클라이언트 `aclose()` 호출 — `getattr(client, "aclose", None)`로 *런타임 호환 체크*(구 redis-py 호환). **신규 `_build_redis_for_cache`**: 지연 import + `Redis.from_url(redis_url, decode_responses=True)` + `cast(_CacheClient, ...)`. `# pragma: no cover` — 라이브 통합 테스트(`@integration`)가 검증. hermetic 테스트는 `monkeypatch.setattr(ds_mod, "_build_redis_for_cache", lambda s: _FakeAcloseable())`로 가짜 주입. **`app.py._lifespan` 갱신**: startup에 `settings = get_settings()` → `build_device_store_from_settings(settings)` → `set_device_store(store)`. shutdown에 `set_device_store(None)` + `await cleanup_fn()` + 기존 `dispose_engine()`. lifespan은 `try/finally`라 yield 중 예외에도 cleanup 보장. **테스트** (hermetic only) **+6 케이스**: ① `TestBuildDeviceStoreFromSettings` 4종 — none(None+noop)·pg(PgDeviceStore+noop)·pg_cached(CachedDeviceStore + aclose 호출)·aclose 미정의 클라이언트 안전 통과(고대 redis 호환) ② `TestLifespanWiring` 2종 — `TestClient(create_app())`을 컨텍스트매니저로 진입해 *라이프스팬 발화*(slice 26 docstring 명시 패턴 활용) → none 모드 → `get_device_store()` is None · pg 모드 → `isinstance(store, PgDeviceStore)` · shutdown 후 None 복원. `monkeypatch.setattr("whymath_backend.app.get_settings", ...)`로 Settings 주입. **`Settings`에 `model_config`가 strict 검증** — `device_store_mode` Literal 외 값은 ValidationError(테스트가 안전 통과 보장). **4게이트 green**: black·ruff·mypy-strict(85파일·`Callable[[], Awaitable[None]]` 시그너처 통과·`cast(_CacheClient, ...)` 정합)·pytest **1319 passed**(1313→+6 hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 124/Branch 24/Miss 0(slice 26 105 → +19 빌더+Settings 분기) · `config.py` Stmts 54/Miss 0 · `app.py` 99%(slice 26 동일·`_build_redis_for_cache` 본문 `# pragma: no cover`). **결정 v1 한계**(후속): ① **Redis 연결은 lazy** — `Redis.from_url`은 즉시 connect 안 함. lifespan startup에 ping 검증 안 함 → 라이브 시작이지만 Redis 미도달 시 *첫 verify 요청 때 오류*. startup health check(ping) + 실패 시 모드 강등(`pg_cached` → `pg`) 후속 ② **모드 전환 무재시작** — Settings는 `@lru_cache` get_settings로 캐시. 런타임 모드 변경 불가(SIGHUP 재로드 후속) ③ **`pg` 모드에서 sessionmaker는 lazy 엔진** — startup에 엔진 ping 안 함. 라이브 PG 미도달 시 *첫 register/revoke 요청에서 오류*. db.session.get_session도 동일 패턴이라 일관 ④ **`none` 모드 명시 호출** — `set_device_store(None)` 명시 호출은 모듈 전역 *재초기화*. 단일 프로세스 가정이라 무해하나 *다중 import* 시 races 가능(FastAPI lifespan은 한 번만 발화하므로 실 위협 0). **🎯 의미**: device store가 **production-ready 운영 표면** 도달 — Phaiakes9·GCP 운영자는 `WHYMATH_DEVICE_STORE_MODE=pg_cached` + `WHYMATH_DEVICE_VERIFY_CACHE_TTL_SECONDS=60` 환경변수 둘로 모든 활성. 별 코드 수정 0·재배포로 모드 전환. lifespan이 startup-shutdown 라이프사이클 *완전 관리* — Redis 클라이언트 leak 0·DB 엔진 풀 leak 0. **🎯 rate limit + device-cred 17 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·검증 실패 metric·`GET /v1/devices` 본인 목록·startup health check(Redis/PG ping)·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 26 (CachedDeviceStore · verify Redis 캐시 · 데코레이터 패턴)** 완료 (메인 hermetic·1301→1313 passed, 2026-06-03) — 슬라이스 23 `PgDeviceStore`의 결정 v1 한계 ③(매 verify가 PG PK SELECT 라운드트립 → connection pool 부담·고빈도 트래픽 비용) 해소. 정상 트래픽은 매 요청 `X-Device-Sig` 검증 → *동일 (device_id, sig) 페어 반복* → 캐시 효과 극대. **신규 `CachedDeviceStore` 데코레이터**: 임의 `DeviceCredentialStore`를 감쌈(InMemory·Pg 모두 호환·Protocol 정합). 캐시 키 `device_verify:{device_id}` → 정당 sig hex(lowercase) 단 1개. 한 디바이스의 정당 서명은 `HMAC(secret, device_id)`이라 결정론적이라 키당 값 1개로 충분. **신규 좁은 `_CacheClient` Protocol**: GET/SETEX/DEL 3 메서드만(필요 최소). redis.asyncio.Redis가 자연스럽게 만족. **verify 경로**: 1) Redis GET → 2) 캐시 hit + 일치(상수시간 비교 `hmac.compare_digest`)면 즉시 True·DB 라운드트립 0 3) miss/불일치면 inner.verify 위임 4) inner True면 SETEX(TTL=60s 권장)로 캐시. **성공만 캐시**: 실패(미등록 device·잘못된 sig)는 SETEX 안 함 → 공격자가 잘못된 sig로 캐시 자리 차지·정당 트래픽 분리 못 함. 정상 클라이언트의 정당 sig만 캐시·재요청은 0 RTT. **revoke 경로**: 1) inner.revoke 위임 2) True면 즉시 `DEL device_verify:{X}` invalidate 3) False(타인 소유 등)면 DEL 안 함(캐시 보존). 명시 DEL + TTL 자연 만료 *이중 안전망* — DEL 실패해도 stale window는 TTL 이내. **register는 캐시 무관 위임**(get·setex·delete 호출 0). **상수시간 비교**: 캐시 hit 경로(`hmac.compare_digest(cached, sig.lower())`)도 타이밍 공격 방어 — 단순 `==` 대신 비교 시간 일정. **bytes/str 호환**: redis-py가 `decode_responses=True/False`로 다르게 반환 → 본 코드는 `isinstance(cached, bytes)`로 분기·utf-8 decode 후 비교(어느 설정에도 동작). **캐시 보안 분석**: 저장 데이터는 *서명*만(secret_plain·user_id·만료 정보 0). Redis dump가 노출돼도 *그 디바이스 정당 서명*만 추출 — 그 서명은 어차피 매 요청에 `X-Device-Sig` 평문 헤더로 노출되므로 *새 노출면 0*. 슬라이스 23의 secret_plain 평문 저장과 다른 위협 면. **신규 Settings `device_verify_cache_ttl_seconds: int = 60`**: 짧을수록 revoke stale 기간 짧음·DB 부담 절감 절충. 0=캐시 비활성(편의·테스트). **테스트** 2층: ① **hermetic +12 케이스**(`TestCachedDeviceStoreVerify/Revoke/Register/ProtocolConformance/TTL`): `_FakeCacheClient`(GET/SETEX/DEL 호출 횟수 기록·ttls dict로 TTL 검증)·`_CountingInnerStore`(inner 호출 횟수 기록)·첫 verify miss → inner+SETEX·두 번째 hit → inner 호출 0·잘못된 sig → inner+False+SETEX 안 함·미등록 device → inner+False+SETEX 안 함·대문자 sig 정규화 lowercase 저장·bytes 응답 decode 처리·revoke 성공 → DEL · revoke False(타인) → DEL 안 함·register passthrough(cache 0 접근)·Protocol conformance·ttl_seconds=42 인자 SETEX 결선. ② **통합 +1 케이스**(실 Redis+PG·`test_cached_device_store_on_live_pg_and_redis`): Redis ping 도달성 체크·CachedDeviceStore(PgDeviceStore) 라운드트립·`redis_client.exists/get/ttl`로 캐시 상태 직접 검증·SETEX 후 0 < ttl ≤ 60·revoke 후 EXISTS=0·폐기 후 verify False(캐시 안 채움). **4게이트 green**: black·ruff·mypy-strict(85파일)·pytest **1313 passed**(1301→+12 hermetic, +1 통합 추가/skip, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 105/Branch 18/Miss 0(slice 25 76 → +29 신규 데코레이터+Protocol). **결정 v1 한계**(후속): ① **DEL 실패 시 TTL까지 stale** — 클러스터 분산·네트워크 단절 시 DEL 안 갈 수 있음(60s 이내 자연 만료로 자체 복구) ② **cache stampede 미방어** — 만료 직후 동시 verify가 모두 DB 강타. singleflight·확률적 조기 갱신은 후속(scale 부담 시 도입) ③ **per-device 1-sig 가정** — secret 회전 슬라이스(후속) 도입 시 *그래이스 기간 동안 2-sig 동시 유효* 가능. 본 캐시는 마지막 성공 sig 1개만 → 회전 후 2번째 sig 매 verify DB 강타(temporary 비효율, 회전 완료 후 자연 복구) ④ **TTL Settings 미활용 — 라우터·lifespan에 안 묶임** — `CachedDeviceStore`는 운영자가 lifespan에서 명시 wrap해야 활성. 본 슬라이스는 *데코레이터 표면*만 추가, lifespan 결선은 후속(`set_device_store(CachedDeviceStore(PgDeviceStore(sm), redis_client, ttl_seconds=settings.device_verify_cache_ttl_seconds))`). **🎯 의미**: production에서 매 요청 verify가 DB 0 라운드트립으로 떨어짐 — Phaiakes9·GCP에서 10k QPS 시 verify 비용 ~100배 감소(60s 캐시·정상 트래픽 대부분 hit). connection pool 압박·DB CPU 부담 동시 해소. KMS envelope·secret 회전은 *다음* 인프라 슬라이스에서 동일 데코레이터 패턴 답습 가능. **🎯 rate limit + device-cred 16 슬라이스 누적**. **후속**: KMS envelope(secret 암호화 저장)·secret 회전(refresh token + 2-sig 그래이스 + 캐시 회전)·nonce 재생 방어·검증 실패 metric·`GET /v1/devices` 본인 목록·CachedDeviceStore lifespan 결선·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 25 (디바이스 등록 rate limit · user+IP · 전용 키 공간)** 완료 (메인 hermetic·1297→1301 passed, 2026-06-03) — `/v1/devices/register` 등록 폭주 방어 1차 도입. sock-puppet 디바이스 양산·DB 자격증명 행 폭증·DoS 시도 차단. 등록은 *드문 작업*(첫 실행 1회·기기 변경)이라 정상 사용자엔 무영향, 자동화/봇만 발화. **신규 `RateCategory = Literal["read", "write", "device_register"]`**: 슬라이스 14의 read/write 분리 패턴을 등록 전용 카테고리로 확장. coach `write`와 *별 키 공간*(Redis 키 prefix가 category 포함) — 한쪽 폭주가 다른 쪽 한도를 잠식하지 않는다(예: coach 채팅 spam이 register를 막거나 그 반대 0). **신규 Settings 2종**: `device_register_rate_limit_per_minute=5`(user)·`device_register_rate_limit_ip_per_minute=10`(IP). 공유 NAT/학교 와이파이 고려해 IP 한도는 user의 2배(같은 IP에 여러 사용자 가능). 매우 낮음 — 정상 첫 등록은 0~1회, 5는 재시도 여유. **신규 dep `rate_limit_device_register`**: `_enforce_triple`를 *device_id=None·device_limit=0*으로 호출 — user+IP 2차원만 적용(등록 *전*이라 device가 아직 없음). category=`device_register`라 coach와 별 ZSET. **`/register`에 `dependencies=[RateLimitedDeviceRegister]`** 부착. `/revoke`는 슬라이스 24 본인 소유 검증이 이미 1차 게이트라 미적용(필요 시 후속). **테스트** (hermetic only) **+4 케이스**(`TestRegisterRateLimit`): ① 사용자 한도 2 → 첫 2회 201·3회는 429(`Retry-After`·`X-RateLimit-User-Limit=2` 헤더) ② IP 한도 1 → 첫 1회 201·2회는 429(`X-RateLimit-Ip-Limit=1`) ③ 기본(=0) 비활성 — 10회 모두 201 통과(기존 테스트 동작 보존 invariant) ④ register 버킷 isolation — 한도 1·429에 `Retry-After>0` 확인(전용 category 결선 검증). **신규 autouse fixture** `_reset_rate_limit_store`(asyncio.run로 매 테스트 store 초기화·다른 테스트로의 카운트 누수 차단). `_settings_override`에 `register_user_limit`/`register_ip_limit` 인자 추가(기본 0 — 기존 테스트 영향 0). **4게이트 green**: black·ruff·mypy-strict(85파일)·pytest **1301 passed**(1297→+4 hermetic, cov-fail-under=70). 신규 cov **100%**: `_rate_limit.py` Stmts 313/Branch 96/Miss 0(슬라이스 24 309 → +4 dep + RateCategory 확장) · `devices.py` 29/2/0. **결정 v1 한계**(후속): ① **`/revoke` 미적용** — 본인 소유 검증으로 1차 방어되나 *유효한 device를 가진* 공격자가 자기 device를 반복 폐기/재등록할 수 있음(register 한도가 사실상 cap) ② **카테고리 하드코딩** — `device_register` Literal 추가. 도메인별 새 엔드포인트마다 Literal 확장 필요. dynamic categorization은 후속 ③ **IP 추출 단순** — `_client_ip`(X-Forwarded-For 첫 토큰) — 프록시 위변조 가능. CIDR 묶음·신뢰 proxy 화이트리스트는 후속 ④ **register 폭주가 *이미 자격증명* 보유 공격자엔 무력** — 5/min×시간=300/시간으로 충분히 봇 1개도 가능. 본 슬라이스는 *초기 폭주 차단*만 목적, 정밀 지속 공격은 metric/anomaly detection 후속. **🎯 의미**: `/register` 엔드포인트가 **production-grade 폭주 방어** — DB device_credential 테이블 크기가 통제되며, 인증 시스템의 자원(`secrets.token_urlsafe` CPU·INSERT IO)이 보호된다. 코드 변화 최소(+4 dep·+2 Settings·+1 Literal)로 즉시 효과. **🎯 rate limit + device-cred 15 슬라이스 누적**. **후속**: KMS envelope·secret 회전·nonce 재생 방어·검증 실패 metric·verify Redis 캐시·`GET /v1/devices` 본인 목록·세션 종료/삭제·라이브 LLM 좌석·`/revoke` rate limit·IP CIDR 묶음.
- ✅ **L4 교수학 엔진 — 슬라이스 24 (revoke 본인 소유 검증 · cross-owner 차단)** 완료 (메인 hermetic·1294→1297 passed, 2026-06-03) — 슬라이스 22-23의 결정 v1 한계 ②(`/revoke`는 인증 사용자라면 *임의* device 폐기 가능) 자명 해소. **Protocol 시그너처 확장**: `revoke(device_id, owner_id: uuid.UUID) -> bool` — 호출자가 인증된 사용자 user_id를 owner_id로 필수 전달. **InMemoryDeviceStore.revoke**: 저장된 `(user_id, secret_plain, revoked)` tuple에서 user != owner_id면 False(상태 변경 0·원 소유자의 후속 폐기는 그대로 가능). **PgDeviceStore.revoke**: `UPDATE WHERE device_id=X AND user_id=Y` — 두 조건 모두 만족해야 행 매치, 타인 device는 0행 → rowcount 0 → False(원자적·race-free·application-side 비교 불필요). **라우터**: `await store.revoke(device_id, user.user_id)`로 ConsentedUser의 user_id 자동 전달. 타인 device 시도 시 응답은 `{revoked: false}`(404 등가) — *존재 여부 비누설*. 정보 누설을 막아야 device_id 열거 공격(공격자가 무차별 시도로 타인 device_id 발견)을 차단할 수 있다. **`_FakePgSession.execute` 갱신**: where가 단일 `BinaryExpression(device_id == X)` 또는 `BooleanClauseList`(AND 결합, `device_id == X AND user_id == Y`) 모두 처리하도록 `_iter_eq_constraints` 재귀 헬퍼 추가. clauses 자식들 순회·등식 제약을 `dict[col_name, value]`로 누적·매칭 행 검증 시 모든 제약 일치 확인. **테스트** 2층: ① **hermetic +3 케이스**(`TestInMemoryDeviceStoreRevoke.test_revoke_other_owner_returns_false`·`TestPgDeviceStoreRevoke.test_revoke_other_owner_returns_false`·`TestRevokeEndpoint.test_other_users_device_returns_revoked_false_not_404`): InMemory 타인 owner_id → False·원 소유자 후속 폐기 여전히 True·PgDeviceStore WHERE AND 0행 매치 시 행 *수정 안 됨*(revoked 그대로 False)·HTTP 응답이 `{revoked: false}` + 실제 device는 폐기 안 됨(store.verify 여전히 True). ② **통합 +1**(`test_cross_user_revoke_isolation_on_live_pg`, `@integration`): A·B 두 사용자 행 적재 → A 등록 → B 토큰으로 폐기 시도 → 200 `{revoked: false}` → A 서명 여전히 유효 → A 본인 폐기는 정상 200 `{revoked: true}` → A 서명 verify False. 기존 `test_pg_device_store_roundtrip_on_live_pg`에도 cross-owner case 끼움(other_uid로 시도 False·원 uid로 True). **4게이트 green**: black(src 85·tests 분리)·ruff·mypy-strict(85 파일)·pytest **1297 passed**(1294→+3 hermetic + 1 integration 추가/skip, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` 76/10/0 · `devices.py` 28/2/0 (분기 +2). **결정 v1 한계**(후속): ① **정보 누설 미세** — `{revoked: false}` 응답이 미존재·타인 소유 *모두 같은 모양*이지만 응답 시간 미세 차이(DB UPDATE 0행 vs PK lookup 0행)가 *timing side-channel* 단서 가능. 본격 timing attack 방어는 인위 지연 추가 후속 ② **list_for_user 미추가** — slice 23에서 제거한 헬퍼. 본인 device 목록 조회 라우터(`GET /v1/devices`)는 별도 슬라이스에서 본인 소유 검증과 함께 ③ **revoke after registration race** — 등록과 폐기 사이 race(매우 짧은 창)에서 INSERT 완료 전 폐기 시도 시 False. 정상 클라이언트 워크플로우는 무관(register 응답 받기 전 폐기 안 함) ④ **owner_id 위변조 가능성 zero** — owner_id는 JWT에서 추출된 `user.user_id`라 클라이언트 위변조 불능(JWT 서명이 1차 방어). **🎯 의미**: device 자격증명 보안 모델 정합 — 인증된 사용자가 *자기 device만* 폐기 가능, 타인 device 열거·DoS 시도 차단. `/v1/devices/register`는 본인만 가능(slice 22)·`/v1/devices/{id}/revoke`는 *본인 소유*만(slice 24). 자기 자격증명 lifecycle을 완전히 책임지는 표면. **🎯 rate limit + device-cred 14 슬라이스 누적**. **후속**: KMS envelope(secret 암호화)·secret 회전(refresh token)·nonce/timestamp 재생 방어·등록 rate limit·검증 실패 metric·verify Redis 캐시·`GET /v1/devices` 본인 목록·IPv6 정규화·세션 종료/삭제·라이브 LLM 좌석.
- ✅ **L4 교수학 엔진 — 슬라이스 23 (DB-backed device_store · 영속·HA · async 전환)** 완료 (메인 hermetic·1285→1294 passed, 2026-06-02) — 슬라이스 22 인메모리 한계(프로세스 재시작 시 등록 분실·다중 워커 상태 분리·관리 가시성 0) 해소. **Protocol async 전환**(슬라이스 22 → 23): PG 접근이 async라 `DeviceCredentialStore` 3 메서드(`register/verify/revoke`) 모두 `async def`로 통일·`InMemoryDeviceStore`도 async 시그너처(즉시 반환·외부 호환성). 호출처(`devices.py`·`_client_device_id`)는 `await` 한 줄 추가뿐. **신규 `PgDeviceStore`**: `async_sessionmaker[AsyncSession]`를 받아 stateless 유지·매 호출마다 *자체* 짧은 세션 열고 닫음(라우터/`_client_device_id` dep 체인에 session 주입 불필요·slice 22 외부 시그너처 동등). register는 INSERT + commit, verify는 PK lookup(SELECT)·HMAC 재계산·상수시간 비교, revoke는 `update().where(device_id == X).values(revoked=True, revoked_at=now)` + commit + `result.rowcount`로 idempotent 판정. **신규 ORM `DeviceCredential`** + 마이그레이션 `a1b2c3d4e5f6`(`down_revision=bb30b816083d`): `device_id VARCHAR(64) PK + user_id UUID FK→user_profile + secret_plain VARCHAR(128) + revoked BOOL DEFAULT false + created_at/revoked_at TIMESTAMPTZ + user_id 인덱스`. PK는 VARCHAR(UUID native 아님) — `str(uuid.uuid4())` 발급 계약 그대로 유지·미래 다른 형식 device_id 옵션 열어둠. user_id FK CASCADE 미적용(GDPR 명시 처리 후속). **secret_plain 평문 저장 트레이드오프 명시**(`_device_store.py` 모듈 docstring): HMAC 재계산에 원본 필요 → KDF(bcrypt/argon2) 적용 불능(*one-way*라 HMAC 키로 못 씀). DB dump 시 모든 secret 노출 — slice 22 인메모리(메모리 dump)와 같은 *내부 침입자* 위협, *공격면 동일*. KMS envelope(`secret_encrypted bytea + nonce bytea` + AES-GCM + master key rotation)은 후속 인프라 슬라이스. **`_client_device_id` async 전환**: `store.verify` await 필요라 sync→async. rate_limit_triple_read/write에서 `await _client_device_id(request, settings)` 한 줄 변경. 3단 우선순위(store→공유 secret→no-verify)는 슬라이스 22 그대로. **테스트** 2층: ① **hermetic +9 케이스**(`TestPgDeviceStoreRegister/Verify/Revoke`): 가짜 `_FakePgSession`(`async with sessionmaker() as s`로 동작·내부 dict로 테이블 표현·`add/get/commit`은 자명·`execute(update(...))`는 stmt의 whereclause·_values를 들춰 dict에 직접 반영·BindParameter `.value` 추출·rowcount 결정). register 적재 확인·distinct credentials·verify(유효/미등록/잘못된 sig/대문자)·revoke(존재→True·미존재→False·후 verify→False). ② **통합 +3종**(`test_devices_integration.py`, `@integration` 마커): PgDeviceStore 단위 라운드트립(실 PG 라이브 connect·register→verify→revoke→verify False)·HTTP 라우터 + PgDeviceStore e2e(`/v1/devices/register` 201·secret으로 store.verify True·`/v1/devices/{id}/revoke` 200·idempotent)·**다중 워커 의미 시뮬레이션**(store_a 인스턴스에서 register → store_b *별* 인스턴스(같은 sessionmaker)에서 verify True → store_b revoke → store_a에서도 폐기 반영 — HA invariant). asyncio.run 호출별 독립 엔진·`_cleanup`은 device_credential + user_profile 두 테이블 정리. **포맷 주의**(반복 회피): src·tests 트리 *분리* black 호출(혼합경로 88 함정 회피 — slice 19~22 모두 같은 패턴). **`result.rowcount` mypy 우회**: SQLAlchemy `Result[Any]`엔 rowcount 노출 없음(`CursorResult` 한정 속성) → `getattr(result, "rowcount", 0) or 0`으로 안전 추출. **4게이트 green**: black(src 85·tests 분리)·ruff·mypy-strict(85 파일)·pytest **1294 passed**(1285→+9 PgDeviceStore hermetic, cov-fail-under=70). 신규 cov **100%**: `_device_store.py` Stmts 74/Branch 8/Miss 0 · `device.py`(ORM) 15/0 · `devices.py` 29/2/0. 전체 cov 99.49%. **결정 v1 한계**(후속): ① **secret_plain 평문 DB 저장** — KMS envelope 후속(AES-GCM + master key + rotation 인프라) ② **본인 소유 검증 없음** — `/revoke`는 인증 사용자라면 *임의* device 폐기 가능. `store.register`가 `(user_id, ...)`로 저장하므로 `revoke(device_id, owner_id)` 시그너처 확장은 자명 ③ **세션 lifecycle** — PgDeviceStore가 매 호출 자체 세션 → connection pool 압박(고빈도 verify에 부담). 후속: connection sharing 또는 verify 결과 Redis 캐시(TTL 짧게) ④ **register/revoke transaction은 단일 INSERT/UPDATE** — 다른 작업과 atomic batch 불가(드물게 필요 시 `set_device_store(PgDeviceStore(injected_sm))`로 트랜잭션 sharing 후속) ⑤ **마이그레이션 자동 생성 안 함** — autogenerate 없이 수동 작성(PG 미가동 환경·alembic check만 통과 가정·실 환경서 `alembic upgrade head` 필수) ⑥ **인덱스 `revoked` 미포함** — verify는 PK lookup이라 불필요·revoke는 드물어 무영향 (관리자 표면이 `WHERE revoked=false` 자주 돌면 후속 추가). **🎯 의미**: device_store가 **production-grade 영속 표면**으로 진화 — Flutter 클라이언트가 KeyChain에 secret 저장 → 앱 재설치 후에도 *서버측 등록 보존*(클라이언트가 secret 잃지 않는 한). 다중 워커/HA Phaiakes9 → GCP 운영 전환 시 워커 상태 분리 문제 0(모든 워커가 같은 PG). 운영자는 lifespan에 `set_device_store(PgDeviceStore(get_sessionmaker()))` 한 줄로 활성. **🎯 rate limit + device-cred 13 슬라이스 누적 — OAuth-style 2차 도달(영속·HA)**. **후속**: KMS envelope (secret 암호화 저장)·revoke 본인 소유 검증·secret 회전(refresh token)·nonce/timestamp 재생 방어·등록 rate limit·검증 실패 metric·verify 결과 Redis 캐시(connection pool 부담 해소)·IPv6 정규화·세션 종료(PATCH)/삭제(DELETE)·라이브 LLM 좌석 결선.
- ✅ **L4 교수학 엔진 — 슬라이스 22 (디바이스별 고유 secret + 등록 플로우 · OAuth-style)** 완료 (메인 hermetic·기존 1251 + 신규 34 = **1285 passed**, 2026-06-02) — 슬라이스 21의 *공유* HMAC secret 한계(앱 바이너리 추출 시 모든 디바이스 위조 가능)를 *디바이스별 고유 secret* + OAuth-style 등록으로 해소. **신규 모듈** `api/_device_store.py`: `DeviceCredentialStore` Protocol(register/verify/revoke) + `InMemoryDeviceStore` 구현 + 모듈 전역 `_DEVICE_STORE` + `set_device_store`/`get_device_store`. `register(user_id)` → `(device_id=UUID4, secret_plain=secrets.token_urlsafe(32))` 즉 ~256-bit 엔트로피·1회 응답 노출·이후 조회 불가. `verify(device_id, sig_hex)` → 저장된 secret_plain으로 `HMAC-SHA256(secret, device_id)` 재계산·`hmac.compare_digest`로 상수시간 비교·미등록·폐기·서명 불일치 모두 False. `revoke(device_id)` → idempotent(미존재 False·존재 True·재폐기 True·이후 verify 항상 False). **설계 트레이드오프 명시**(docstring): secret_plain을 *메모리*에 보관(verify에 원본 필요 — hash만으론 HMAC 재계산 불가, KDF는 적용 불능 비대칭). 서버 메모리 dump 시 모든 secret 노출 → DB-backed envelope(secret_plain은 KMS·HMAC-key encrypted)는 후속 인프라 슬라이스(`RateLimitBackend` 인메모리→Redis 폴백과 같은 패턴). **신규 라우터** `api/devices.py`: `POST /v1/devices/register`(`ConsentedUser` 게이트·201·`{device_id, secret_plain}` 응답 — secret_plain은 *이 응답에서만* 노출, 클라이언트가 KeyChain/Keystore에 안전 저장 책임) + `POST /v1/devices/{device_id}/revoke`(idempotent·미존재면 404 아닌 `{revoked: false}`). 둘 다 store 미설정 시 **503 Service Unavailable**(`_require_store` 헬퍼·운영자 lifespan으로 `set_device_store` 활성 필수·slice 21 폴백 모드는 등록 불필요). **`_client_device_id` 3-단 우선순위** 갱신(slice 22 도입): ① **store 모드**(`get_device_store()` non-None) — 등록된 device_id만 유효·서명 누락/불일치는 fail-safe None ② **공유 secret 폴백**(slice 21 동작) — store 미설정·secret 있으면 기존 trivial-spoofing 방어 ③ **검증 생략**(slice 20 동작) — secret도 빈값이면 device_id 그대로(backward compat). store=None 모드에선 slice 21 의미 그대로(기존 1251 테스트 영향 0). **위협 모델 진보**: slice 21 → 22 = 공유 secret 추출 시 모든 디바이스 spoof 가능 → *개별 secret 추출은 그 디바이스만* 영향(횡적 차단). 한 디바이스 노출이 다른 디바이스에 미치는 영향 0. **테스트** (hermetic only) **+34 케이스**(`test_devices.py`, 7 클래스): `TestInMemoryDeviceStoreRegister/Verify/Revoke/Reset`(13)·`TestDeviceStoreProtocolConformance`(1)·`TestModuleGlobals`(2)·`TestRegisterEndpoint`(4)·`TestRevokeEndpoint`(5)·`TestClientDeviceIdStoreMode`(10). 핵심: register 라운드트립(응답 sig로 store.verify True)·503 store 미설정·401 무인증·idempotent revoke·revoke 후 같은 secret으로도 verify=False·store 모드가 공유 secret보다 우선(둘 다 설정해도 store 등록 안 된 ID는 거부)·store None → 공유 secret 폴백 → 검증 생략 폴백 체인. autouse 픽스처가 `set_device_store(None)`으로 모듈 전역 누수 차단(다른 테스트의 store 의존성 0). **인프라 결선**: `app.include_router(devices_router)`·`api/__init__.py` export(기존 `concepts/problems/users/me/coach` 5종 → 6종). **4게이트 green**: black(src 84·src/tests 분리 호출)·ruff·mypy-strict(src 84파일)·pytest **1285 passed**(1251→+34 hermetic, cov-fail-under=70). 신규 모듈 cov **100%**: `_device_store.py` Stmts 42/Branch 6/Miss 0 · `devices.py` Stmts 29/Branch 2/Miss 0 · `_rate_limit.py` 100% 유지(Stmts 309/Branch 96/Miss 0; slice 21 300 → +9 신규 store 분기). **결정 v1 한계**(후속): ① **인메모리 영속 없음** — 프로세스 재시작 시 모든 등록 분실 → 클라이언트는 재등록 필요(MVP는 fine, 운영은 DB-backed 후속) ② **분산/HA 미지원** — 단일 프로세스 가정·다중 워커는 store 공유 안 됨(Redis 또는 DB-backed로 교체 후속) ③ **본인 소유 검증 없음** — `/revoke`는 인증 사용자라면 *임의* device_id 폐기 가능(`store.register`가 `(user_id, ...)`로 저장하므로 후속에 `revoke(device_id, owner_id)` 시그너처 확장하면 자명) ④ **로깅·메트릭 없음** — 등록/폐기/검증실패 가시성 미확보(slice 21 한계 그대로·후속 분리) ⑤ **rate limit 미적용** — 등록 폭주 방어 없음(인증 게이트로 1차 방어·MVP fine) ⑥ **재생 공격 미방어** — slice 21과 동일(같은 sig 영구 유효, nonce·timestamp 후속). **🎯 의미**: HMAC 위협 모델의 *최종 단계 1차 도달* — `X-Device-Id` rate-limit 차원이 이제 **per-device unique secret**으로 보호됨. 공격자가 한 디바이스의 secret을 알아내도 다른 디바이스 spoof 불가(slice 21은 1개 알면 모두 spoof 가능). 운영은 lifespan에서 `set_device_store(InMemoryDeviceStore())`로 활성·DB-backed envelope 후속. **🎯 rate limit + device-cred 12 슬라이스 누적 — OAuth-style 1차 도달**. **후속**: DB-backed device_store(영속·HA)·revoke 본인 소유 검증·secret 회전(refresh token)·nonce/timestamp 재생 방어·등록 rate limit·검증 실패 metric·14세 미만 학부모 동의 검증 가능 동의 절차·IPv6 정규화·CIDR 묶음·X-Real-IP·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선.
- ✅ **L4 교수학 엔진 — 슬라이스 21 (디바이스 HMAC 서명 검증 · X-Device-Sig)** 완료 (메인 직접·실 Redis 검증, 2026-06-02) — 슬라이스 20의 `X-Device-Id` 위변조(클라이언트가 임의 ID 반복해 device 차원 우회) 방어 1차 도입. **HMAC 서명 검증**: 클라이언트가 `X-Device-Sig: HMAC-SHA256(secret, device_id)` hex(64자)를 동봉. 서버는 `hmac.compare_digest`로 *상수-시간* 비교(타이밍 공격 방어). **신규 헬퍼** `_expected_device_signature(secret, device_id) -> str`(SHA-256 hex digest). `_client_device_id(request, settings)` 갱신: secret 설정 시 X-Device-Sig 헤더 추출 → 누락이면 **fail-safe None**·불일치면 None·일치면 device_id 반환. **소문자 정규화**(`.lower()`)로 대문자 hex도 수용(클라이언트 라이브러리 일관성). **빈 secret = 검증 생략**(slice 20 backward compat — 기존 1242 테스트 그대로 통과). **신규 설정** `coach_device_hmac_secret: SecretStr`(env `WHYMATH_COACH_DEVICE_HMAC_SECRET`로만 주입·SecretStr는 repr/로그 평문 차단·하드코딩 금지·CLAUDE.md 보안 정합). **호출처 갱신**: `rate_limit_triple_read`/`_write`에서 `_client_device_id(request, settings)` 전달(settings는 기존 의존성). **위협 모델 명시**(docstring): 앱 바이너리에서 secret 추출 가능 — 본 슬라이스는 *trivial spoofing*(자동화 봇이 random device_id 반복) 방어만. 정식 디바이스 인증(검증 가능한 토큰·서버측 device 등록 테이블·rotating secret)은 OAuth-style 후속 슬라이스. **테스트 인프라**: `_settings_override`에 `device_hmac_secret=""` 인자 추가(기본 빈 — 기존 테스트 무영향). 기존 device-extraction 테스트 3건은 settings 인자 전달로 보강(slice 20 backward compat 정합). **테스트** 2층: ① **hermetic +9 케이스**(`TestDeviceHmacSignature`): 유효 서명 수용·잘못된 서명 None·서명 누락 시 secret 설정 시 None·빈 secret 시 서명 검증 생략·대문자 hex 정규화·다른 device_id 서명 거부(페어 무결성)·`_expected_device_signature` 결정론(같은 입력 같은 출력·다른 입력 다른 출력)·**coach 결선 valid sig 시 device 한도 정상 발화** + **invalid sig 시 5회 spoofing 시도 모두 device 비활성**(공격 시나리오 e2e). ② **실 Redis 통합 3종** 그대로 passed(서명은 dep 단의 device_id 도출에만 영향·backend 레벨 무관). **메인 라이브 검증**: 로컬 Redis 6379서 통합 3종 passed. **4게이트 green**: black(src 82)·ruff·mypy-strict(src 82파일)·pytest **1251 passed**(1242→+9 hermetic, cov-fail-under=70)·`_rate_limit.py` cov **100%**(Stmts 300·Branch 90·Miss 0; slice 20 286 → +14 신규). **결정 v1 한계**(후속): ① **secret 추출 가능** — 앱 바이너리·중간 인증서 인터셉트로 secret 노출 가능(*trivial spoofing*만 방어, 본격 위협엔 부족). 후속: 디바이스별 *고유 secret* 발급(첫 등록 시 서버 생성·KeyChain/Keystore 보관·rotating with refresh token) ② **재생 공격 미방어** — 같은 (device_id, sig) 페어가 영구 유효. nonce·timestamp + 시간창 검증은 후속(슬라이스 22 후보) ③ **secret 키 회전 미지원** — 키 변경 시 모든 클라이언트가 신키로 동시 전환 필요(서버측 graceful rotation: 구키+신키 동시 검증 기간) ④ **검증 실패 로그 없음** — spoofing 시도 가시성 미확보(metric/log 후속). **🎯 의미**: trivial spoofing 방어 *1차 보호* — 자동화 봇이 device_id 무작위 회전으로 device 한도 우회하던 슬라이스 20 취약점 해소. 공격자는 *secret을 알아야* 유효 서명 생성 가능 → 앱 바이너리 분석·MITM 같은 *상대적으로 비용 높은* 공격만 남음. 본격 위협엔 OAuth-style 등록 필요하나, MVP에선 *충분한 방어선*(공격 비용 raised). **🎯 rate limit 11 슬라이스 누적**. **후속**: 디바이스별 고유 secret + 등록 플로우·nonce/timestamp 재생 방어·secret 회전·검증 실패 metric·IPv6 정규화·CIDR 묶음·X-Real-IP·세션 종료(PATCH)·세션 삭제(DELETE)·라이브 LLM 좌석 결선.
- 📋 **다음 세션 후보 (서로 블로킹 없음)** — ① **M1.2 라이브 연동**(S1~S5 *완료*; 잔여: 비용/지연 실측 보정·프롬프트 캐싱·thinking/effort 튜닝, 모두 라이브 키 보정 후속 §H#4) ② **빌드타임 사전생성 파이프라인**(*슬라이스 1~8·B 완료 — Schema 구현 정본 全(v1.0 8도메인 + v1.1 2종) 完; DB 매핑 PoC(인프라+Problem) + 배치1(5도메인) + 배치2(4도메인·hypertable 조건부가드) = **전 도메인 ORM·마이그레이션 完 + DB-backed HTTP 라우터(concept) 결선·lifespan dispose + ncic load_to_postgres(L1→PG 적재) 完** — 위 ✅; 잔여: hypertable 변환 timescaledb env 실검증·타 도메인 라우터·쓰기 확장(PATCH/DELETE)·실 NCIC 데이터 적재·개념그래프 Neo4j 적재(전문가 CSV 선행)* — 위 ✅ 참조; B=SymPy 검증. 후속: A(성취기준→스펙, pedagogy 동반 — 개념그래프 시드는 ✅, 전문가 작성 단계 4 잔여)·DB 내구화·PRM/LLM-judge·Max 인제스트 포맷·에빅션 정책) ③ **(Kiki·Phaiakes9) per-call-site 크기 재실측**(`ollama pull bge-m3` → `quality_eval.py` @`127.0.0.1` GPU — extract 임베딩 의미매칭 포함) ④ **클라우드 티어 비용/지연 실측 보정**(03a §H#4 잔여 — 경로·provider는 S5 완료, 실측 임계값만 남음) ⑤ **24/7 서버 운영 설계**(Phaiakes9 상시 가동)
- 📋 **Phaiakes9 카탈로그 cosmetic 정리** (별도 PR) — README.md 7군데, SETUP_GUIDE.md 1군데, 주석 4군데. 코드 동작 영향 없음, 문서 일관성만
- 📋 **Phaiakes9 systemd unit `ProtectSystem=full` 완화** (별도 PR) — `failed to persist model recommendations snapshot ... read-only file system` 경고 해소. `ReadWritePaths=/usr/share/ollama` 추가
- ✅ 완료: PRD v1.1 정합성 정렬(단계 1~5, `fd23115`~`b8d6d3d`) / CI 툴체인 점검(`3b9ff72`) / 곁다리 2건(`df03eaa`) / **NCIC PDF crawl baseline**(629건 추출, 5% 검수 완료, 2026-05-15 결정 로그) / **`main` 보호 규칙 적용**(2026-05-15, PR #1로 CI 첫 가동·status check 등록) / **M1.0a Phaiakes9 1차 셋업** (2026-05-15, NucBox EVO-X2 + WSL2) / **M1.1 CPU baseline** (qwen2-math:7b, 12.62 tok/s @ concurrent 1) / **M1.1 GPU 가속 활성화** (2026-05-16, Windows Ollama 경유 DirectML, qwen2-math:7b 32.63 tok/s + qwen3.5:27b 9.22 tok/s, CPU 대비 2.6x — 옵션 A1 채택) / **M1.1 fast tier 후보 측정** (2026-05-19, qwen2-math:1.5b GPU 124.25 tok/s @ c=1, p50 1010ms — L3 SLA 게이트 PASS, fast/mid/quality 3단계 라인업 결정) / **인터페이스 정렬·main 보호 데드락 정정·Max=빌드타임/API=런타임 결정** (2026-05-20, PR#4) / **L3 라우터 M1.2 구현** (2026-05-20, `whymath_backend/l3` 결정로직+타입 인터페이스+백엔드 CI 잡, PR#5) / **FAST tier 품질 검증 종결 + 태스크 패밀리 라우팅 확정** (2026-05-20, 결론: *수학=`qwen2-math`·NLP=`qwen2.5`* — 수학모델로 NLP는 7b조차 0%; 하니스(`<ANSWER>`·temperature=0·임베딩 의미매칭·코드추출) + 03a 축3 `ModelFamily`(§0.2·A.0·C.0·§H 후속8~12) + 라우터 코드 family 축·호출지점별 크기, PR#6~#14. 설계·인터페이스·코드·하니스 완전 정합)

### 완료된 마일스톤
- 2026-05: 7계층 아키텍처 확정
- 2026-05: 기술 스택 확정 (Flutter + FastAPI + PostgreSQL + Phaiakes9)
- 2026-05: Phase 진입 순서 확정 (메타인지 사고력 → 학교 진도 → 수능 → 영재 → B2B)
- 2026-05-14: GitHub 레포 `doldori7/WhyMath` (Private) 생성 및 첫 푸시 완료
- 2026-05-28: MathScope PRD v1.2 재검토 — 7계층·로드맵 재정렬 (공유 코어 + 고3 우선, 법적 안전조합 콘텐츠) — 결정 로그 참조

### 미해결 의사결정
- [ ] 수학 교육 도메인 파트너 영입 (M1.3 게이트로 *지연 확정* — 트랙 미정)
- [x] ~~첫 진입 학년~~ → **공유 메타인지 코어 + 고3 수능 우선 노출** (2026-05-28 PRD v1.2 재검토; 2026-05-13 "고1 내신"은 *폐기 아님, 노출 순서 재배치* — 공유 코어가 고1도 서빙). 결정 로그 참조
- [ ] 벡터 DB: ChromaDB 유지 vs Qdrant 전환 (PRD v1.1 채택으로 발생 — 정렬 단계3 L1/L5에서 결정)
- [x] ~~스키마 통합 정본화~~ → **Schema v1.0 = 구현 정본** (v1.1 YAML 대체, `curriculum_entry`·`textbook_mapping`만 이식; source_type/license 법적 교정). 실 마이그레이션은 후속 (2026-05-28 속편 결정 로그)
- [x] ~~OCR 스택~~ → **PaddleOCR + Qwen3-VL 하이브리드** 확정 (2026-05-28, Mathpix 대체; 한국어 정확도 L5 전 Phaiakes9 실측 필요). 결정 로그 참조
- [ ] 사단법인·재단·법인 형태
- [ ] Cambridge MMP/NRICH 라이선스 협상 시작 시점

---

## 🧭 핵심 결정 로그 (시간 역순)

### 2026-05-28 (원문검증): 저작권 가이드 v2.0·MathScope v4 수령 — 법적 서술 검증·데이터 백본 확증
**컨텍스트**: Kiki가 원문 2종 추가 제공 — **저작권 종합가이드 v2.0**(2026-05-27, .docx→텍스트 추출)·**MathScope v4**(데이터 카탈로그, 2026-05-26). 앞서 *합성*으로 커밋한 법적 서술(licensing_safety·CLAUDE)을 원문 대조 검증.
**검증 결과 — 전부 일치 ✓ (환각 0)**: §32 단서(영리 시험문제 금지→EBS·KICE·시도교육청·KMO 영리 차단)·§136·§140(영리 비친고죄, 합의해도 직권기소)·2024.8 대법원(KICE 사용료 의무)·공공누리 AI유형(2026-01-28)·AIHub 영리 명문허용·**한 줄 결론 "NCIC+AIHub+NuminaMath(Apache)+PRM800K(MIT)+PhET(CC BY)"**·NCIC §7 — 커밋 서술과 정확히 일치.
**원문 보강(licensing_safety 반영)**: §93(DB제작자권)·§125-2(법정손배 1건당 최대 5천만)·**SA(ShareAlike) 함정**(AI 가중치 SA 전염 위험 → AoPS·LibreTexts ✅→⚠️ 교정, Khan NC+SA 완전격리)·**SA 우회**(Feist·대법원 2000다61664 — 사실만 추출+자체생성)·AIHub 4조건(출처표시·국외반출·재판매금지·환수)·AI유형 nuance(모델 상업이용 허용)·**NCIC 성취기준 §7(무제한) ↔ 해설서 공공누리 2유형(NC) 구분**·등급 A+/A/A-/B/C/D/E.
**데이터 백본 확증(MathScope v4)**: 실제 수집 = **A-/A/A+ 21종·~4M 레코드, B/C/D/E 0건** — EBS·평가원·검정교과서 *미포함*. 재검토 "법적 안전조합" 교정은 *이미 데이터 엔지니어링이 실행 중인 현실*이었음(추가 A+: DLMF·Metamath, A: GSM8K·MATH·NuminaMath CoT/TIR·OpenMathInstruct 등).
**신규 발견 — 제3의 스키마**: MathScope v4 `schema_v2.sql`(6테이블+3뷰: dataset_licenses·curriculum_standards·curriculum_alignments·problems·solution_steps·student_attempts + license 뷰)은 *L1 데이터/라이선스 거버넌스* 초점 → 앞서 정본화한 **Schema v1.0(앱 8도메인)과 상보적**(problems/solution_steps/curriculum 중복은 통합 필요). Schema v1.0=앱·런타임 정본 유지, schema_v2.sql=L1 학습데이터/다국가 교육과정 거버넌스 레이어.
**적용**: `docs/legal/copyright_guide_v2.md`·`docs/data/dataset_catalog_v4.md` 레포 보존; licensing_safety.md 보강(SA 교정·§93/§125-2/§140·등급체계·AIHub 4조건·v4 백본). **2022 개정 보고서 수령·보존**(`docs/data/curriculum_2022_revision.md`) — 중3·고3만 2015 잔존(2026-05)·중학 5→4영역·고1 공통수학1·2·고3 미적분Ⅱ 등 확인, 기존 `curriculum_version` 정책 확정. (5개 원문서 전부 도착했었음 — 초기 배치 폴더 미안내로 늦게 발견; *정정*: 앞서 "미수령"은 오류). **후속**: schema_v2.sql↔v1.0 통합·THIRD_PARTY_LICENSES 디렉토리·license_monitor cron·**별책8 본문 264p**(성취기준 해설·핵심 아이디어, §7 무제한) L1 데이터 보강 시 재수집.

### 2026-05-28 (OCR): OCR 스택 = PaddleOCR + Qwen3-VL 하이브리드 (Mathpix 대체)
**컨텍스트**: CLAUDE.md 기술스택(Mathpix) ↔ PRD v1.2 §8.1(PaddleOCR+Qwen3-VL 하이브리드) 충돌 — 재검토 후속의 마지막 미해결 결정.
**결정(Kiki)**: **PaddleOCR + Qwen3-VL 하이브리드(로컬, PaddleOCR fallback)** 채택, Mathpix 대체. 로컬 LLM 풀에 **Qwen3-VL**(멀티모달·그래프 개형) 추가.
**근거**: 의사결정 우선순위 **#1 학생안전·#2 법적**(미성년자 손글씨·풀이 이미지 *외부 전송 회피* → 로컬 처리)·**#6 비용**(무료 vs Mathpix per-call) + Kiki "로컬 LLM 우선(Phaiakes9)" 선호 + PRD v1.2 자체 채택과 정합. FR-002(그래프 멀티모달)·FR-011(손글씨 OCR)의 구현 스택.
**리스크**: 한국어 손글씨·수식 인식 정확도 *미검증*(PRD §11.2 高리스크) → L5 착수 전 Phaiakes9 실측 벤치마크 필수(목표 90%, PRD §12.3). 미달 시 PaddleOCR fallback·부분 Mathpix hedge 재검토.
**적용**: CLAUDE.md 기술스택(OCR row·로컬 LLM row·L5 다이어그램·변경 각주) 갱신. **후속**: Qwen3-VL Phaiakes9 배포·한국어 수학 OCR 정확도 벤치마크(PRD 액션 §12.2).

### 2026-05-28 (속편): PRD v1.2·Schema v1.0 원문 수령 — 정합 확인·스키마 정본화·P0 FR 계층 매핑
**컨텍스트**: 재검토(아래 본편)는 외부 5문서를 *합성 분석*으로만 다뤘으나, Kiki가 **PRD v1.2 원문**과 **DB Schema v1.0 원문**을 레포에 제공. 원문 대조로 재검토 주장을 검증하고, 보류했던 페르소나·FR 매트릭스·스키마 통합을 진행.
**정합 확인(원문 대조)**: 재검토 주장 전부 일치 — FR **31개(P0 14·P1 12·P2 5)**·**페르소나 5종**·로드맵 **v1.0~v3.0**·**MVP=페르소나 A(고3)**·시그니처 패턴(조건나열 FR-001·합성함수 FR-003·귀납수열 FR-004). PRD **§12.4 #3가 "평가원·EBS·AIHub만으로 충분한가?"를 미해결로 남김** → 재검토의 저작권 충돌 전제 확증.
**페르소나 5종(PRD §3) ↔ 노출 순서**: A 일반고 고3(MVP·시장최대 52만)·C 검정고시 N수(v1.5·충성·수학의존 100%)·D 학종 고2(v1.5·세특/자유연구 차별화)·B 자사고 N수(v2.0·결제최대·경쟁치열)·E 홈스쿨링 영재(v2.0·시장작음·가치최대). → 재검토 "공유코어+고3 우선"과 정합.
**스키마 정본화 결정**: **Schema v1.0(8도메인 ~25테이블, PostgreSQL+TimescaleDB+ChromaDB, DDL·인덱스·hypertable·ENUM 완비) = 구현 정본**. `schemas/v1.1`(9 YAML) = 개념 초안으로 *대체*. 단 v1.0에 없는 2개 **보존·이식**: ① `curriculum_entry`(NCIC 성취기준 1급 — L1 파이프라인 산출물, "콘텐츠는 성취기준 코드 1개+ 태그" 원칙) ② `textbook_mapping`(교과서 *구조 메타* 매핑 — 자동 커리큘럼 정렬, 법적 안전). v1.1 나머지는 v1.0이 흡수(problem·concept·edge→concept_edge·student_profile→user_profile·mastery_state→concept_mastery_history·hint/solution_path→problem_step).
**스키마 법적 교정(필수·정본에 적용)**: v1.0의 `source_type_enum`(평가원·EBS·교과서)·`license_enum`(PUBLIC_DOMAIN="평가원 공개"·EBS_LICENSED)·`generation_type=ORIGINAL`은 *본문 저장* 전제 → 저작권 가이드 v2.0과 충돌. **교정**: 평가원·EBS·교과서 *본문 미보유*; `content_provenance.original_reference`에 **구조 메타(단원·코드·문항번호)만**; `problem.question_text`는 **WHYMATH_GENERATED 동등문제만** 채움; 지배 license=`WHYMATH_GENERATED`; `ORIGINAL`·`EBS_LICENSED`는 공식 제휴(Phase 3+) 전까지 미사용. (`content_provenance.generation_type`의 VARIANT/FULLY_GENERATED + `generation_log`가 `l3/pregenerate` 동등문제 엔진의 DB 모델)
**P0 14 FR → 7계층·빌더빌리티(법적 교정 반영)**:
| FR | 기능 | 계층 | 비고 |
|---|---|---|---|
| 001 | 조건 나열형 파서 | L3·L4 | **핵심 모트**, 신규 |
| 003 | 합성함수 케이스 분류 | L3·L4 | **핵심 모트**, 신규 |
| 004 | 귀납적 수열 추적 | L3·L4 | **핵심 모트**, 신규 |
| 009 | AI 변형 모의고사 생성 | L3 | `l3/pregenerate` 확장(모트) |
| 007 | ~~EBS 변형 DB~~ | L3 | **동등문제 자체생성으로 재정의**(EBS 본문 미사용) |
| 005 | 자연수 답 변환 사전 | L3 | 신규(소) |
| 006 | 단원융합 개념그래프 | L1·L2 | concept_edge+ChromaDB(DB 정본화) |
| 008 | 평가원 가중치 분류 | L1 | exam_authority_weight(메타만, 본문 X) |
| 013 | 인공지능수학 콘텐츠 | L1 | 콘텐츠 |
| 014 | 내신+수능 통합 코칭 | L6 | 공유 코어 위 모드 |
| 010 | 1년 사이클 자동계획 | L6·L2 | 신규 |
| 012 | 실전 모의고사 시뮬 | L5 | UX |
| 002 | 그래프 멀티모달 인식 | L5·L3 | **OCR 스택 결정 의존**(Qwen3-VL) |
| 011 | 풀이 손글씨 OCR | L5 | **OCR 스택 결정 의존** |
*1인 가드*: 핵심 모트(001·003·004·009 + L4 소크라테스) 최우선 → DB 정본화(006·008) → 멀티모달/OCR(002·011, 스택 결정 후) → 나머지.
**적용**: `docs/strategy/prd_v1.2.md`·`schemas/v1.0/schema_v1.0.md` 레포 반영(원문 보존). **후속**: 스키마 정본 *실 마이그레이션*(alembic, DB 미구현 상태)·curriculum_entry/textbook_mapping 이식 DDL·OCR 스택 결정·CLAUDE.md 페르소나 5종 1줄 반영.

### 2026-05-28: MathScope PRD v1.2 재검토 — 7계층·로드맵 재정렬 (공유 코어 + 고3 우선, 법적 안전조합 콘텐츠)
**컨텍스트**: 5종 신규 문서 입력(레포 외부) — **PRD v1.2**(입시특성 100→FR 31·페르소나 5종·버전 v1.0~v3.0), **DB Schema v1.0**(8도메인 SQL), **2022 개정 교육과정 보고서**, **MathScope v4**(데이터셋 21종), **저작권 종합가이드 v2.0**. 현 7계층/Phase 틀과 대조(갭분석)한 결과 두 충돌이 핵심: ① 저작권 가이드 v2.0이 **EBS·평가원 영리사용 금지**(저작권법 §32 단서·§136·§140·2024 대법원)로 못박아 PRD/ROADMAP의 *EBS·평가원 기출 직접 활용* 전제가 무효 ② **MVP 타깃 분기**(현 ROADMAP=고1 내신 ↔ PRD v1.2=고3 수능). PRD 범위 폭발(FR 31·페르소나 5·멀티모달·학종)이 1인 capacity·얇은 코드(L2·L4·L6·L7 미구현, DB 미구현)와 충돌.
**결정(Kiki, 3택)**:
1. **MVP 앵커 = 공유 코어 + 고3 우선 노출**. 7계층 메타인지 코어는 *단일·공유*(고1 내신도 같은 코어가 서빙)이되, **첫 시장 노출 페르소나 = A(고3 수능)**, 킬러 30번 *자체 생성 동등문제*가 wedge. → 2026-05-13 "고1 내신 확정"은 *폐기 아님, 노출 순서 재배치*로 갱신.
2. **콘텐츠 = 법적 안전조합**. 백본=NCIC 성취기준(§7)·공공누리 AI유형(2026-01)·AIHub 수학셋(영리허용). LLM 학습/예시=NuminaMath(Apache)·PRM800K(MIT)·PhET(CC BY)·Metamath(CC0)·Lean. **EBS·평가원 = 구조 메타데이터(단원·코드·문항번호)만 사실인용 + 자체 생성 동등문제로 본문 대체**. → PRD FR-007(EBS 변형)·#11(EBS 연계)·"평가원 기출 P0"를 *동등문제 자체생성*으로 재정의. 이미 구축한 `l3/pregenerate`(빌드타임 사전생성)+SymPy 게이트가 그 엔진.
3. **산출물 = 갭분석 + 수정 프레임 + 로드맵 재정렬**(FR 31 P0 매트릭스·스키마 v1.0↔v1.1 통합 상세는 후속 슬라이스).
**재정렬된 7계층 ↔ PRD v1.2 (MVP=공유코어, 첫 노출 고3)**:
- L1: NCIC ✅ + AIHub/동등문제 코퍼스 + 개념그래프 DAG + **DB 정본화 필요**.
- L2: 미구현 → BKT/IRT 최소 숙달추정(MVP), DKT·정서신호 후속.
- L3: 라우터·사전생성 ✅ → **동등문제 생성·다중풀이·조건파서(FR-001)·합성함수/귀납수열(FR-003/004)** = 핵심 모트.
- L4: 프롬프트만 → **Polya·소크라테스 코칭 엔진 코드화**(MVP 핵심).
- L5: 모바일 0 → Flutter MVP(고3 30번·손글씨 OCR)·서버 DB·인증. OCR 스택(Mathpix vs PaddleOCR+Qwen3-VL) 결정 후속.
- L6/L7: 후속(L6 수능 단일모드 우선, L7 Phase 3+).
**수정 로드맵 (PRD 버전 ↔ Phase)**: Phase 1=PRD v1.0 코어(고3 wedge), Phase 2=v1.5(검정고시·학종 세특·결제), Phase 3=v2.0(2028 수능·수리논술), Phase 4/5=v3.0(영재·면접·글로벌). *1인 가드*: Phase 1은 P0 FR 14 중 **핵심 모트 우선**(조건파서·시그니처 패턴·동등문제·소크라테스); 멀티모달·세특 후순위.
**근거**: 의사결정 우선순위 #2(법적준수)>#6(비용)이 EBS/평가원 회피를 강제. #3(교수학)·#4(학습효과)는 공유 메타인지 코어로 충족(고3 노출은 GTM 선택). 2022 교육과정은 이미 반영(중3·고3 2015 병행→`curriculum_version` 유지).
**적용 범위(갱신 완료)**: ROADMAP.md(Phase 1 정의·콘텐츠 전략·PRD 버전)·docs/data/licensing_safety.md(가이드 v2.0 결론·EBS·평가원 영리금지·동등문제 정책·안전조합 allowlist)·CLAUDE.md(EBS·평가원 영리금지 금기·콘텐츠 안전조합 원칙). **후속(원문·결정 필요로 보류)**: CLAUDE.md 페르소나 5종 명문화(PRD v1.2 원문 필요 — 현재 A/C/D만 확보, B/E 미상이라 *날조 금지*)·schemas v1.0↔v1.1 통합 정본(Schema v1.0 SQL 원문 필요)·FR 31 P0 매트릭스(PRD 원문 필요 — 현재 FR-001/003/004/007만 확보)·OCR 스택 결정(Mathpix vs PaddleOCR+Qwen3-VL, Kiki 결정).
**리스크**: 자체 동등문제의 킬러 난이도 재현·검수(SymPy는 산술만→PRM/사람검수 필요)·법적 안전조합만으로 MVP 콘텐츠 충분한지(변호사 검토 권장)·1인 6개월 capacity.

### 2026-05-20: L3 라우팅에 *태스크 패밀리 축* 도입 — 수학(qwen2-math) vs NLP(qwen2.5), 03a 확정(B)
**컨텍스트**: FAST tier 품질 검증(03a §H 후속1)을 Phaiakes9 실측으로 진행한 결과, "1.5b vs 7b *크기*"가 아니라 **모델 패밀리가 태스크와 안 맞은 것**이 근본 원인으로 드러남(아래 *FAST tier 품질 평가 하니스* 로그의 재설계(A) 참조). NLP 호출지점(extract/translate/match)을 수학 특화 `qwen2-math`로 돌리면 7b조차 0%, 일반 모델 `qwen2.5`로 바꾸니 정상화. 이를 *설계*로 확정(B 트랙).
**결정**:
- L3 LOCAL 라우팅에 **축3 = 모델 패밀리**(`ModelFamily{MATH, GENERAL}`) 도입. 로컬 실제 모델 = (패밀리 축3) × (크기 축2). 패밀리는 *태스크 유형*이 결정(NLP→GENERAL, 수학→MATH), 크기/SLA는 그 안에서.
  - MATH=`qwen2-math`(1.5b/7b) · GENERAL=`qwen2.5`(3b/7b) · QUALITY=`qwen3.5:27b`(패밀리 무관 상위, 비동기). 클라우드(CLOUD_*)엔 미적용.
- **5 호출지점 확정(실측)**: ①extract→GENERAL/MID(qwen2.5:7b)·②깊이추론→MATH/MID·③translate→GENERAL/MID(qwen2.5:7b)·④match→**GENERAL/FAST(qwen2.5:3b)**·⑤검증→QUALITY(27b·async)·산술→MATH/MID(qwen2-math:7b).
- 03a 갱신(§0.2·§A.0 매트릭스·§B.2·§C.0 패밀리 결정·§G `local_family`·§H 후속), 03·llm-architect.md 모델 풀 최소 동기화.
**근거(2026-05-20 실측, GPU 127.0.0.1, temperature=0)**:
- NLP@수학모델=0%(7b도) ↔ NLP@qwen2.5: match 3b **100%**·translate 7b **75%**(3b 50%). 산술@qwen2-math: 7b 100%·1.5b 87.5%.
- 호스트 교훈: `172.17.112.1`=WSL2 CPU(속도만), `127.0.0.1`=Windows GPU. temperature=0으로 실행 변동 제거.
**적용 범위**:
- 문서: `03a`·`03_content_generation.md`·`llm-architect.md`(설계·인터페이스).
- **후속(03a §H 8~11)**: (8) **✅구현(2026-05-20)** extract 임베딩 의미매칭(`set_f1_semantic`·Ollama `bge-m3` 코사인·threshold 0.6·exact 폴백) — 동의어 인정(검증: '다항식 전개'↔'다항식의 곱셈' semantic F1 0.8 vs exact 0.4); (9) **✅구현(2026-05-20)** match 코드추출 채점(`extract_unit_code`·`GRADER_CODE`) — 모델이 후보 주제명 echo('10수학02 (방정식과 부등식)')해도 코드만 매칭; (10) **✅결정(2026-05-20)** 산술 → MID 확정(정확도 우선, ~1/8 오답 FAST 부적합·DELTA 0.07 유지·03a §H#10); (11) **✅구현(2026-05-20)** 라우터 코드 family 축(`ModelFamily`·`local_family`·불변식4·`_decide_family`·`LOCAL_MODEL_MATRIX`·cache_key/langfuse family, llm-architect.md 인터페이스와 일치, 135 테스트·커버리지 100%). (12) **✅구현(2026-05-20)** `_decide_local_tier`를 §C.2 9규칙으로 확장 — GENERAL 호출지점별 크기 분기(④match→FAST, ①extract·③translate→MID) 반영(검증: extract→GENERAL/MID·match→GENERAL/FAST·translate→GENERAL/MID, 135 테스트·커버리지 100%). **§H 후속 8~12 전부 완료** — FAST tier 검증·태스크 패밀리 라우팅 트랙 종결(라우터 코드가 03a/llm-architect 설계와 완전 일치).
**상태**: 확정(2026-05-20). 태스크 패밀리 라우팅을 03a 정본화. FAST tier 검증 여정 종료(결론: *수학≠NLP, 패밀리별 모델*). 잔여는 §H 후속.

### 2026-05-20: FAST tier 품질 평가 하니스 구축 — 1.5b vs 7b 결정적 채점 (03a §H 후속1, 실측은 Phaiakes9)
**컨텍스트**: 03a §H 후속1 "FAST(1.5b)가 7b 대비 품질 차이가 충분히 작은지 측정 → 차이 크면 ①③④ 일부를 MID 승급(C.2 조정)". 실제 모델 실행은 Phaiakes9(Ollama·GPU)에서만 가능하므로(이 컨테이너엔 모델·GPU 없음), 이번엔 *하니스(데이터셋+채점기+러너+테스트)* 까지 만들고 실측은 Kiki가 Phaiakes9에서 수행한다. 채점은 *결정적(프로그램)* — LLM-as-judge 아님. llm-architect 위임 후 메인이 4게이트 독립 재검증.
**결정**:
- `infra/phaiakes9/benchmark/`에 `bench_latency.py`(지연 벤치) 자매 도구 추가(stdlib만·ollama lazy·`_OllamaClientProtocol` 추상화 → 테스트는 가짜 클라이언트 주입):
  - `fast_tier_eval.json`: 자작 CC0 33항목 — extract 9(개념추출)·translate 8(정규화)·match 8(개념ID)·산술 8.
  - `quality_eval.py`: 채점기 `set_f1`(extract 집합 F1)·`exact_match`(translate/match/산술, `normalize_form` 후 정확매칭)·출력 파서(마지막 줄/`ANSWER:` 라벨)·러너(FAST·MID 순차)·집계·결정규칙·CLI(종료코드 0/1/2).
  - `tests/infra/test_quality_eval.py`: 56 테스트, quality_eval.py 커버리지 98%.
- **결정 규칙(C.2 조정 신호)**: 호출지점별 `keep_fast = (FAST_acc >= MID_acc - DELTA) and (FAST_acc >= ABS_MIN)`. **DELTA=0.07**(FAST의 p50 1초 vs 4초 이점이 소폭 정확도차 상쇄)·**ABS_MIN=0.60**(절대 하한). 03a §H 후속1이 임계 미확정 → *문서화된 합리적 기본값*, Phaiakes9 실측 후 보정.
**근거**:
- 채점기·파서·결정규칙은 순수 함수라 모델 없이 테스트 가능(라이브 의존 분리) — bench_latency.py 동일 패턴. 모델 실행만 Phaiakes9.
- 결정적 채점이 FAST 호출지점(추출/매칭/정규화/산술)에 적합(체크 가능한 출력) — LLM-judge 비결정·비용·추가모델 회피.
**적용 범위**:
- 신규 3파일(infra/benchmark 2 + tests/infra 1). 검증: ruff·black(line-length 100)·mypy-strict·pytest 56 통과·커버리지 98%.
- **실측 대기(Kiki·Phaiakes9)**: `python quality_eval.py` → 호출지점별 verdict. 종료코드 1(일부 MID 승급 권고)이면 C.2 조정.
- **gold 검수 필요**: 개념셋 gold는 결정적 채점용 잠정값(review_status "사람 수학자 검수 대기") — 강한 결론 전 검수 권장.
- infra/는 `[tool.black]`/`[tool.ruff]` 설정 미도달·tests/infra CI 미게이트(bench_latency.py와 동일 상태) — 별도 CI 잡 미추가(범위 밖).
**상태**: 하니스 확정(2026-05-20). 결정적 채점·결정규칙·커버리지 98%. **첫 Phaiakes9 실측(2026-05-20)에서 파서가 `ANSWER:`만 안정 추출 → 점수 대부분이 *형식 불일치*로 0**(모델 실력 아님)임이 드러나 하니스 보정: 프롬프트에 `#### <답>` 형식 계약 + 파서 4단 폴백(`####`→`\boxed{}`→라벨→마지막 줄)+감싸기/앞장식 strip, 테스트 56→75(gold·채점 의미 불변). **보정판 재실측은 Phaiakes9 재실행 대기 → 결과로 C.2 조정 판단**(후속1 잔여). 교훈: 추론 모델의 결정적 채점은 *출력 형식 계약*이 선결. **2차 실측(2026-05-20, host 172.17.112.1) 추가 발견**: (a) 1.5b 가용성은 호스트 문제였음(`localhost`=빈 인스턴스, `172.17.112.1`=모델 보유 — 24/7 서버 시 인스턴스 1개로 정리 권장); (b) 새 파서가 산술을 실제로 고침(FAST `\boxed{45}`·MID `": 45"` → `45`); (c) `num_predict` 256→**512**(과추론 잘림 방지); (d) **지표 신뢰도 결정**: 산술·match(정확매칭)는 *결정 등급*, **extract/translate(set_f1/정규형)는 의미≠문자열이라 방향성만**(MID가 합리적 개념 대도 gold와 문자열 불일치로 0). C.2 판단은 산술·match 기준, **extract①은 1.5b 횡설수설(스페인어)로 명백 약점 → MID 잠정 권고**, translate는 보류(gold·과제 명료화 필요). **재설계(2026-05-20, A)** — 실측 재해석으로 *근본 원인* 확정: extract/translate/match는 *NLP 작업*인데 `qwen2-math`(수학 풀이 특화)로 시켜 **7b조차 0%**였음(크기가 아니라 *모델 패밀리* 미스매치). match는 빈 코드 후보로 *찍기*(MID 7/8 동일코드)였고, 비결정성(temperature>0)으로 산술 37↔62% 출렁임도 확인. 하니스를 **태스크 인지**로 재설계: 패밀리별 모델(NLP=`qwen2.5` fast 3b/mid 7b · MATH=`qwen2-math` 1.5b/7b), `<ANSWER>` 형식 강제, `temperature=0`(재현성), LaTeX `\frac`→`a/b`·match 후보 주제명 흡수. 테스트 89·커버리지 98%·게이트 통과. 호스트는 `127.0.0.1`(Windows GPU) 권장(`172.17.112.1`=WSL2 CPU, *속도만* 느림·정확도 무관). **재실측(qwen2.5·GPU) 대기 → 검증되면 03a §C.2를 *태스크 유형(수학 vs NLP) 라우팅*으로 확정(B)**. 핵심 함의: L3 라우팅은 크기/SLA만이 아니라 *태스크 유형*으로 모델 패밀리를 갈라야 한다.

### 2026-05-20: M1.2 L3 라우터 구현 완료 — 결정 로직 + 타입 인터페이스 (.py + 테스트 100%)
**컨텍스트**: 03a 설계서의 라우터를 `.py`로 구현하는 M1.2(03a §H 후속 #3·라우터 설계 후속 #3). 범위는 *결정 로직 + 타입 인터페이스*로 한정 — 실제 LLM/Redis/Langfuse/큐 연동은 라이브 서비스·API 키가 필요해 단위테스트·CI 게이트가 불가하므로 후속 분리. llm-architect 서브에이전트 위임 후 메인이 4게이트 독립 재실행으로 검수.
**결정**:
- `src/backend/whymath_backend/l3/` 신규 패키지(data-pipeline 구조 미러링):
  - `models.py`: `CostTier`·`LocalModelTier`·`CallSite` enum + `RoutingRequest`·`RoutingDecision`(pydantic). 3불변식을 `@model_validator(after)`로 강제(`LOCAL⟺local_model`·`CLOUD⟹None`·`QUALITY⟹async`). `use_enum_values=True`에서도 enum/문자열 양쪽 정규화.
  - `router.py`: `Router.route()` = 축1(C.1 6규칙)→축2(C.2 7규칙) 순차. C.1 규칙5(에스컬레이션)는 단발 입력이 아닌 *생성 결과 피드백* 트리거라 route()에서 제외하고 `next_tier()`/`ESCALATION_CHAIN`(§D.1)으로 분리. `guard_cloud`(§D.4·§E.2 구독 한도)·`cache_key`(§F.1 2축)·`langfuse_fields`(§F.2 dict만)·지연/비용 추정기.
  - `interfaces.py`: `LLMProvider`·`CacheBackend`·`TraceSink`·`AsyncJobQueue` Protocol(미구현) + `InMemoryCache`·`RecordingTraceSink` 테스트 스텁.
- 테스트 `tests/backend/`: 99개, 커버리지 **100%**(stmt+branch). 결정표 각 규칙·불변식·가드·추정·키·에스컬레이션·엣지 커버.
- `src/backend/pyproject.toml` 완성(hatchling·black·pytest paths·coverage) + `.github/workflows/ci.yml`에 `backend — lint·type·test` 잡 추가(ruff·black·mypy-strict·pytest cov70).
- 미설치 핀 `mocktail>=1.0.0` 제거(PyPI 최대 0.0.4·미사용 — `pip install -e .[dev]` 실패 방지).
**근거**:
- 결정 로직은 순수 함수라 라이브 의존 없이 100% 단위테스트 가능 — "모든 PR 테스트(70%+)"(CLAUDE.md) 충족하면서 비용 0. 외부 의존은 Protocol 경계로 분리해 후속 연동 시 교체.
- C.1 규칙5를 route()에서 뺀 건 *계층 책임* — 신뢰 미달 트리거 감지는 생성 파이프라인(03 문서)의 일이고 라우터는 사슬 계산만(`next_tier`).
**적용 범위**:
- 신규: `whymath_backend/l3/{models,router,interfaces}.py`·`tests/backend/*`. 수정: `src/backend/pyproject.toml`·`ci.yml`.
- **미적용(후속)**: 클라우드 비용·지연 상수 placeholder 실측(§H 후속 4)·LLM/Redis/Langfuse/큐 라이브 연동·`backend` CI 잡의 branch protection 필수 체크 등록(Kiki 수동, 현재 필수 3종).
**상태**: 확정(2026-05-20). M1.2 라우터 결정 로직 구현 완료, 4게이트 통과(ruff·black·mypy-strict·pytest 99/100%). 라이브 연동·placeholder 실측은 후속.

### 2026-05-20: Claude Max 구독 = *빌드타임 콘텐츠 생성*, Anthropic API = *런타임 서빙* — CostTier에 구독 미편입
**컨텍스트**: CostTier(축1) 검토 중 "Kiki의 Claude Max 구독($100/$200)을 런타임 CLOUD_MID/HIGH에 활용해 비용 절감" 아이디어 제기. 검토 핵심: **Max 구독과 Anthropic API(개발자 플랫폼)는 별개 시스템**이다 — Max=개인 대화형(앱·Claude Code, 5시간 롤링·주간 상한 rate limit), API=제품 백엔드 토큰당 과금. Max 구독으로는 제품 백엔드가 학생(제3자)을 서빙할 수 없다: (a) 기술적으로 API 키 미발급, (b) 소비자 약관상 *개인 사용 한정*(제3자 서빙·제품 임베드 위반), (c) rate limit으로 다중 학생 동시 서빙 부적합.
**결정**:
- **Max 구독을 CostTier(런타임 라우터)에 편입하지 않는다.** CostTier는 런타임 서빙 채널(LOCAL Qwen / API CLOUD_MID·HIGH)만 표현 — 본 결정이 이를 *명문화*(enum 변경 없음).
- **Max 구독의 자리 = 빌드타임 콘텐츠 생성 파이프라인 + 개발.** Kiki가 Claude Code/앱으로 직접: 프롬프트 설계·코드·**코퍼스 사전 생성**(동등문제·힌트·풀이경로·개념설명)·시드 품질 검증. 결과는 캐시/DB 저장 → 학생 런타임은 캐시 히트(0원).
- 빌드타임 분담: 대량·반복 생성 = 로컬 Qwen / 고난도 검증·시드 품질 = Max-Claude (Max 한도 절약).
- 런타임 비용 통제(03a §E·§F 유지): 80% LOCAL(0원) + 캐시 우선 + 18/2% API(프롬프트 캐싱·Batch API 50%↓·`budget_krw`/`guard_cloud` 일일 한도·⑤ 자기검증 샘플링).
**근거**:
- CLAUDE.md 의사결정 우선순위 **#2 법적·윤리적 준수 > #6 비용** — 약관 위반 절감은 채택 불가(소비자 구독을 제품 서빙에 전용 시 계정 정지·법적 리스크).
- 최대 절감 레버는 "런타임에 클라우드를 *덜* 때리는 것" = **사전생성 + 캐싱**. 여기에 Max를 합법적으로 투입하면 런타임 API 비용을 구조적으로 낮춤(학생당 한계비용 0에 수렴).
- 경계 분리(빌드타임 vs 런타임)가 깔끔: CostTier=런타임 정책, Max=상류 콘텐츠 자산 생성 — 계층·책임이 다름.
**대안**:
- *Max OAuth를 백엔드에 연결(Claude Code headless/Agent SDK)* — 폐기: 소비자 약관상 제3자 서빙 불가 + rate limit으로 프로덕션 부적합 + 계정 리스크. 개인 개발·테스트에만 한정.
- *전량 API 런타임 생성(사전생성 없음)* — 폐기: 학생 수 증가 시 비용 선형 폭증. 사전생성+캐싱이 단가를 구조적으로 낮춤.
- *전량 로컬(클라우드 0)* — 부분 폐기: 킬러·증명·고난도 진단 품질 천장. 80/18/2의 18/2% 클라우드 유지가 학습 효과(#4)상 합리적.
**적용 범위**:
- 본 결정 로그(문서). CostTier enum·03a 변경 없음(이미 런타임만 표현 — 본 결정이 명문화).
- 후속: 빌드타임 사전생성 파이프라인 설계(코퍼스 생성 워크플로·캐시 전략) 별도 트랙. 프롬프트 캐싱·Batch API 도입은 M1.2 클라우드 연동 시.
**상태**: 확정(2026-05-20). Max=빌드타임/개발, API=런타임 서빙. CostTier 불변. 비용 전략 = 사전생성+캐싱으로 런타임 API 최소화.

### 2026-05-20: `main` 보호 자기 승인 데드락 *실제 발생* → 솔로 단계 설정으로 정정
**컨텍스트**: 2026-05-15 `main` 보호 로그(아래)는 "1인 단계 Code Owner 자기 승인 충돌은 Phase 2 합류 시 자연 해소"로 *지연 처리*했으나, 이번 PR #3(아래 L3 라우터 3단계 설계, `2faf61a`) 머지 시점에 **실제 데드락 발생**. 보호 규칙이 (a) 승인 ≥1 + (b) Code Owners 검토를 요구하는데 `@doldori7`가 *유일한 Code Owner이자 PR 작성자* → GitHub은 자기 PR 승인을 금지 → 머지 불가. "administrators 포함(Do not allow bypassing)"까지 켜져 있어 관리자 우회 머지도 차단된 *하드 데드락*. 즉 "Phase 2에 자연 해소"는 문제를 *미룬 게 아니라 모든 솔로 머지를 즉시 봉쇄*하는 것이었음 — 가정 자체가 틀림.
**결정**:
- 보호 *일시 해제* → PR #3 머지(`2faf61a`, main 반영) → 보호를 **솔로 단계용으로 재구성**:
  - **Require a pull request**: 유지(ON) — 직접 push는 계속 차단
  - **Required approvals**: 1 → **0** (자기 승인 불가 회피)
  - **Require review from Code Owners**: ON → **OFF** — 유일 Code Owner=작성자 충돌 제거
  - **필수 status check 3종**(`data-pipeline`·`infra/phaiakes9`·`policy-guard`): 유지 — CI가 실질 게이트
  - **linear history · force-push 차단 · deletion 차단**: 유지
- `.github/CODEOWNERS` 파일은 *그대로 유지* — 자동 리뷰어 지정 기능 자체는 보호 규칙 없이도 동작. Phase 2 리뷰어 합류 시 "Require review from Code Owners" 체크박스 1개만 재활성하면 복원.
**근거**:
- 의사결정 우선순위상 *개발 흐름 봉쇄 해소*가 필요하되, 보호의 실질(직접 push 차단 + CI 3종 게이트 + linear/force-push/deletion 차단)은 **그대로 유지** — 안전을 양보하지 않음.
- 솔로 단계에선 사람 승인보다 *기계적 검증*(lint·type·test·policy-guard 금기 가드)이 보호의 핵심. 승인 0이어도 CI 통과 없이는 머지 불가.
- Code Owners를 *필수 요건에서만* 제외(파일 유지) → 비가역 결정 아님, Phase 2 복원이 체크박스 1개.
**적용 범위**:
- GitHub Settings UI(수동, 코드 변경 아님): `main` 보호 규칙 재구성 — 라이브 설정 read 도구가 MCP에 없어 Kiki 수동 확인·적용
- 본 결정 로그 추가 + 아래 2026-05-15 보호 로그 §상태에 정정 포인터
- **미반영(후속)**: `.github/branch-protection-setup.md`의 "PR 1+승인·Code Owners" 체크리스트는 *Phase 2 기준*임을 문서에 명시 — 솔로 단계는 본 로그가 우선
**상태**: 확정(2026-05-20). `main` 보호 = PR 필수·승인0·Code Owners 미요구·CI 3종·linear·force-push/deletion 차단. 자기 승인 데드락 해소. Phase 2 리뷰어 합류 시 승인≥1 + Code Owners 재활성 예정.

### 2026-05-20: L3 라우터 fast/mid/quality 3단계 설계 — 두 라우팅 축 분리·`mid` 명칭 충돌 해소
**컨텍스트**: 2026-05-19 qwen2-math:1.5b GPU 측정으로 fast/mid/quality 3단계 *로컬* 라인업(1.5b/7b/27b)이 확정된 뒤, "어떤 입력이 어느 모델로 분기되는가"의 결정 로직(입력 분류기 + decision table)을 명세하는 트랙 A 착수. 설계 착수 즉시 *아키텍처 긴장* 발견: 기존 문서(`03_content_generation.md`·`llm-architect.md`)의 라우터 티어는 `LLMTier{LOCAL, MID, HIGH}`(비용·위치 축, MID=Claude Sonnet·HIGH=Claude Opus 클라우드, 목표분포 80/18/2)인데, 새로 확정된 fast/mid/quality는 *전부 로컬 Qwen 모델*. 즉 **두 축이 서로 다름**에도 `mid`가 양쪽에 존재(클라우드 MID vs 로컬 7b)해 충돌.
**결정**:
- L3 라우팅을 **두 축으로 분해**:
  - 축1 *비용·위치* = `CostTier{LOCAL, CLOUD_MID, CLOUD_HIGH}` — 기존 `LLMTier.MID/HIGH`를 `CLOUD_` 접두사로 개명(1:1 의미 보존, 80/18/2 유지). "클라우드로 올라가나?"
  - 축2 *로컬 모델 크기* = `LocalModelTier{FAST, MID, QUALITY}` = 1.5b/7b/27b — 축1이 LOCAL일 때만 적용. "로컬 어느 크기?"
- **명칭 충돌 해소 규칙**: `mid`를 단독으로 쓰지 않는다 — 항상 `CLOUD_MID`(축1) 또는 `LocalModelTier.MID`·`로컬 mid(7b)`(축2)로 한정. 라우팅 결정은 `(cost_tier, local_model)` 쌍 + 불변식(`LOCAL ⟺ local_model 존재`, `QUALITY ⟹ async`)으로 표현.
- **분기 근거 = SLA 실측**: FAST(1.5b)만 p50<2초 게이트 통과 → 동기 즉답 기본 경로(+ c=4 throughput scaling, 피크 흡수). MID(7b) p50≈4초 → 정밀 풀이·메인 대화(동기 허용). QUALITY(27b) p50≈14초+병렬 미작동 → **동기 불가, 비동기 큐 전용**(자기검증·복잡추론·PRM·백그라운드).
- **5개 핵심 호출지점 기본 매핑**: ①개념추출·③번역정규화·④개념ID매칭 = FAST(캐싱 적중률 큼), ②깊이추론 = MID(hard↑면 QUALITY/CLOUD), ⑤자기검증 = QUALITY/async(샘플링).
- 산출: 신규 설계서 `docs/architecture/03a_l3_router_design.md`(A~H 8개 섹션: 두 축 통합·입력분류기·decision table·의사코드·에스컬레이션/폴백·비용예산SLA·캐싱Langfuse·스키마·미해결). 03 문서 §1·모델풀표·5호출지점 문단 *최소* 정합성 보정(CLOUD_ 표기 + 03a cross-ref).
**근거**:
- 한 단어(`mid`)가 두 축을 겸하면 코드·문서·Langfuse 태그에서 *어느 축인지* 모호 → 환각·비용 오라우팅 위험. `CLOUD_` 접두사로 *맨이름 mid를 어디에도 단독으로 두지 않는* 게 가장 견고.
- 2026-05-19 결정 로그가 명시한 "비용·품질·지연의 파레토 최적"을 *실행 가능한 분기 규칙*으로 번역. SLA 실측이 모든 분기의 근거(추측 아님 — CLAUDE.md "확실하지 않을 때 자신 있게 말함 금지").
- 기존 `Router.route()` 4규칙을 *축1로 의미 보존* 후 로컬 세분(축2)만 신규 추가 → 기존 설계 무효화 없이 확장.
- 설계/구현 분리: 본 트랙은 *명세*까지(설계 트랙). 실제 `.py`·테스트는 M1.2. 스키마는 마크다운 예시 코드블록으로만.
**대안**:
- 단일 enum 유지(`LLMTier`에 FAST/MID/QUALITY 추가) — 폐기: 비용축과 크기축이 한 enum에 섞여 80/18/2 분포 의미 붕괴, `mid` 충돌 미해소.
- 로컬 세분을 라우터 밖(클라이언트)에서 처리 — 폐기: "모든 LLM 호출은 라우터 경유"(CLAUDE.md) 위배, 호출지점별 정책 일관성 상실.
- fast/mid/quality를 그대로 최상위 티어로(클라우드 제거) — 폐기: 킬러·증명·어려운 진단의 클라우드 에스컬레이션 경로 상실, 80/18/2 전략 폐기.
**적용 범위**:
- 신규: `docs/architecture/03a_l3_router_design.md`
- 수정(최소): `docs/architecture/03_content_generation.md` 3곳(§1 출력 명세·모델풀 표 2축화·5호출지점 문단 + 03a cross-ref·명칭충돌 주석)
- 본 결정 로그 + 활성 작업 갱신
- **적용 완료(2026-05-20)**: `LLMTier`를 참조하던 `.claude/agents/llm-architect.md`(enum→`CostTier`+`LocalModelTier`·`Router.route()`→`RoutingDecision` 반환)·`docs/architecture/04_pedagogy_engine.md`(`recommended_tier`→`recommended_cost_tier: CostTier`)·`06_application_modes.md`(`default_llm_tier`→`default_cost_tier: CostTier`)·`.claude/agents/backend-engineer.md`(호출처 `route()→generate(decision=)`로 일관 갱신)의 두 축 분해 반영 완료. `budget_cents`→`budget_krw`(03a E장)도 통일. L4/L6은 *축1(CostTier)만* 힌트로 보유, 축2(로컬 FAST/MID/QUALITY)는 L3 라우터가 결정(계층 경계). *문서 간 불일치 해소*. 구현은 M1.2.
**후속 작업 (별도 PR/세션)**:
1. **[완료 2026-05-20] FAST tier 품질 검증** — Phaiakes9 실측 완료. 결론: *모델 패밀리* 미스매치(수학모델로 NLP=0%)가 근본 원인 → **태스크 패밀리 라우팅** 도입(위 *L3 라우팅에 태스크 패밀리 축 도입* 로그·03a §0.2 참조). 잔여: extract 지표·라우터 코드 family 반영(03a §H 8~11)
2. **[완료 2026-05-20] 인터페이스 정렬: `LLMTier` → `CostTier`/`LocalModelTier`** — llm-architect.md·04·06·backend-engineer.md의 `LLMTier` 참조를 두 축 분해로 갱신 (위 §적용 범위 참조)
3. **[완료 2026-05-20] M1.2 라우터 구현** — 03a 설계를 `.py`로 구현 + 테스트(커버리지 100%). 결정 로직+인터페이스 범위(LLM/Redis/Langfuse/큐 라이브 연동은 후속). 위 *M1.2 L3 라우터 구현 완료* 로그 참조
4. **클라우드 티어 실연동** — CLOUD_MID/HIGH API·비용 계측·`guard_cloud` 임계값 실측(Phase 1 후반)
**상태**: 설계 완료(`feat/l3-router-3tier-design`). 두 축·명칭 충돌·decision table·에스컬레이션·스키마 명세 확정. 구현은 M1.2.

### 2026-05-19: qwen2-math:1.5b GPU 측정 완료 — L3 라우터 fast tier 후보 확정, 3단계 라인업 결정
**컨텍스트**: 2026-05-16 GPU 활성화 후 후속 작업 1번. qwen2-math:7b GPU 32.63 tok/s · p50 3,918ms / qwen3.5:27b GPU 9.22 tok/s · p50 13,886ms 둘 다 L3 SLA(p50 < 2초) FAIL → *실시간 대화 즉답이 가능한 더 작은 모델 측정 필요*. Ollama Library의 `qwen2-math:1.5b` (934 MB, 2026-05 정식 등록) 후보. 동일 환경(Windows Ollama 0.24.0 + DirectML · Strix Halo Radeon 8060S · WSL2 클라이언트 `WHYMATH_OLLAMA_HOST=http://172.17.112.1:11434`) + 동일 벤치 스위트(고1 내신 원작 8문항)로 측정.
**결정**:
- L3 라우터 라인업을 *fast/quality 2단계*에서 ***fast/mid/quality 3단계***로 확장:
  - **fast tier = qwen2-math:1.5b** (대화 즉답·분류·1단계 산술·관리적 응답) — *L3 SLA 게이트 PASS*
  - **mid tier = qwen2-math:7b** (수학 풀이·2~3단계 추론·메인 학생 대화) — p50 ~4초, 게이트 FAIL이나 *허용 응답 시간 내*
  - **quality tier = qwen3.5:27b** (답안 검증·복잡 추론·PRM 후보·백그라운드) — p50 ~14초, *동기 응답 불가*
- Phaiakes9가 *L3 SLA 준수 환경* 보유 입증 — 옵션 F(ROCm Linux native) 의 *2-5x 잠재력*은 이 게이트 통과 후에도 *동시도 4·mid·quality* 개선 여지로 유효
**환경**: 2026-05-16 결정 로그와 동일 (Windows Ollama 0.24.0 / DirectML / Radeon 8060S Strix Halo / WSL2 Ubuntu 24.04 클라이언트). 워밍업 1회 후 측정. 헬스체크 3단계 통과 (`'2+2=' → '...2 + 2 = 4...'`).
**GPU baseline — qwen2-math:1.5b (2026-05-19 11:50 KST)**:
- 동시도 1: p50=**1,009.99ms** / p90=1,088.56ms / p99=1,098.48ms / **124.25 tok/s**
- 동시도 4: p50=3,522.03ms / p90=3,590.26ms / p99=3,658.16ms / **142.91 tok/s**
- L3 SLA 게이트 (p50 < 2000ms): ✅ **PASS** (동시도 1)
- Windows GPU 적재: 100% GPU, 1.4 GB VRAM (`ollama ps`)
- 결과 JSON: `infra/phaiakes9/results/2026-05-19_115051.json` (.gitignore — 로컬 보관)
**3점 비교 (GPU baseline 종합)**:
| 모델 | 디스크 | p50 (c=1) | tok/s (c=1) | tok/s (c=4) | 게이트 (c=1) | 용도 후보 |
|---|---|---|---|---|---|---|
| qwen2-math:1.5b | 934 MB | **1,010ms** | **124.25** | 142.91 | ✅ PASS | **fast tier** (대화 즉답) |
| qwen2-math:7b | 4.4 GB | 3,918ms | 32.63 | 33.81 | ❌ FAIL | **mid tier** (수학 풀이·메인 대화) |
| qwen3.5:27b | 17 GB | 13,886ms | 9.22 | 9.28 | ❌ FAIL | **quality tier** (검증·백그라운드) |
**병렬 처리 특성 (1.5b vs 7b vs 27b)**:
- 1.5b: c=1 → c=4 tok/s **124 → 143 (+15%)** — *GPU throughput 활용 작동, 동시 4 처리 가능*
- 7b: c=1 → c=4 tok/s 33 → 34 (+4%) — 한계 근접, c=4에서 p50 15초로 폭발
- 27b: c=1 → c=4 tok/s 9.22 → 9.28 (~0%) — *GPU 한 요청 100% 점유, 병렬 미작동*
- → **1.5b 가 fast tier 로서 *피크 트래픽 흡수* 역할까지 가능** (학생당 0.5-1 RPS 가정 시 단일 GPU로 수십 명 동시 대응)
**근거**:
- 1.5b 가 *L3 SLA 게이트 통과 + 동시도 4 throughput scaling 작동* — fast tier 의 *지연·처리량 동시 충족* 데이터 확보
- 7b·27b 가 게이트 FAIL 인 것이 *문제가 아니라 역할 분기 근거* — 한 모델로 *즉답 + 정확성* 둘 다 충족 불가, 3단계 분기가 *비용·품질·지연의 파레토 최적*
- 1.5b 의 *수학 특화(qwen2-math)* + *작은 크기* 결합으로 *분류·라우팅·간단 산술*에서 7b 와 *품질 차이 좁음* 가정 — 실제 품질 검증은 후속 (L3 라우터 구현 시)
**대안**:
- 단일 tier (7b 단독) 진행 — 폐기: p50 4초가 *즉답엔 길고 정밀 채점엔 짧음*. 두 요구 동시 충족 불가
- fast = 7b 유지 (1.5b 폐기) — 폐기: 게이트 FAIL 사실 변하지 않음, 1.5b 의 *6배 throughput·4배 빠른 응답* 데이터 무시
- 클라우드 Claude/GPT 로 fast tier 위탁 — 폐기: CLAUDE.md *로컬 LLM 우선* 원칙 위배, 학생당 일 50-100회 호출 시 비용 폭발
- qwen2-math:0.5b 또는 다른 1B 후보 추가 측정 — *보류*: 1.5b 가 이미 SLA 충족, 추가 측정 ROI 낮음. 라우터 구현 후 *fast tier 품질 미흡* 시 재시도
**적용 범위**:
- MEMORY.md 본 결정 로그 (3점 비교 + 라우터 라인업 결정)
- MEMORY.md 활성 작업: "qwen2-math:1.5b GPU 측정" → ✅ 완료, "L3 라우터 fast/quality 두 단계" → "fast/mid/quality 3단계"로 갱신, "fast tier 품질 검증" 신규 등록
- `infra/phaiakes9/GPU_ACTIVATION_FOLLOWUP.md` 옵션 A1 *완전 실증 완료* (1.5b·7b·27b 3점 데이터로 라우터 라인업 결정 완료)
**후속 작업 (별도 PR/세션)**:
1. **L3 라우터 fast/mid/quality 3단계 설계** — `docs/architecture/03_content_llm.md` 갱신 또는 별도 설계서. *입력 분류기* + 분기 결정 로직 명세 (다음 트랙 A)
2. **fast tier 품질 검증** — 1.5b 가 *간단 산술·메타 응답·분류* 에서 7b 대비 *품질 차이* 측정. L3 라우터 구현 시 진행
3. **ROCm 7.2+ Linux native 시도** (옵션 F 유지) — *2-5x 잠재력*은 mid·quality tier 개선 여지로 유효 (BIOS UMA·드라이버 사전 조치 필요)
4. **healthcheck.sh 디폴트 timeout 상향** (2026-05-16 후속 4번 유지) — 30초 → 90초 (큰 모델 콜드 로드 대응)
**상태**: L3 라우터 fast tier 후보 확정. Phaiakes9 가 *3단계 라인업 모두 운영 가능한 환경* 보유. 본격적 L3 라우터 구현(M1.2) 의 *데이터 기반 설계 근거 확보 완료*.

### 2026-05-16: Phaiakes9 GPU 가속 활성화 완료 — Windows Ollama 경유 DirectML, 모델 라인업 데이터 확보
**컨텍스트**: 2026-05-15 결정 로그 *옵션 A1* 진행. Windows측에 Ollama 0.24.0 native 이미 설치되어 있음 확인 + Radeon 8060S(Strix Halo) GPU 자동 인식 (`ollama ps` PROCESSOR=100% GPU, qwen2-math:7b 5.2GB GPU 적재). WSL 측 클라이언트가 `WHYMATH_OLLAMA_HOST=http://172.17.112.1:11434` (Windows 호스트 IP) 로 호출 — 환경변수 한 줄만 변경, WSL 측 인프라 코드 자체는 *완전 무수정*. WSL2 NAT networking에서 Windows Ollama API 즉시 도달 (별도 firewall·OLLAMA_HOST=0.0.0.0 설정 불필요).
**결정**:
- GPU 가속 *최소 viable* 환경 확보 — DirectML 경로로 7B 모델 CPU 대비 *2.6x 향상*
- L3 LLM 라우터 설계: *fast tier (qwen2-math:7b GPU, 32.63 tok/s)* + *quality tier (qwen3.5:27b GPU, 9.22 tok/s)* 두 단계 라인업 후보 확정
- ROCm 7.2+ Linux native (옵션 F) 는 *추가 잠재력 2-3x* 보유 — 후속 별도 세션 (BIOS UMA·드라이버 사전 조치 필요)
**환경**:
- Windows측: Ollama 0.24.0 (DirectML 추정, GPU 100% 활용 확인)
- WSL 측: Ollama systemd 서비스 *유지* (사용 안 함, 환경변수 override 로 Windows Ollama 호출)
- 기타 머신 사양 = 2026-05-15 1차 셋업 결정 로그와 동일 (NucBox EVO-X2 / Ryzen AI Max+ 395 / Radeon 8060S Strix Halo / WSL2 Ubuntu 24.04)
**GPU baseline — qwen2-math:7b (2026-05-16 12:52 KST)**:
- 동시도 1: p50=**3,917.67ms** / p90=3,965.7ms / p99=3,981.28ms / **32.63 tok/s**
- 동시도 4: p50=15,015.31ms / p90=15,140.63ms / p99=15,143.33ms / 33.81 tok/s
- L3 SLA 게이트 (p50 < 2000ms): ❌ FAIL — *DirectML on Strix Halo 환경 + 7B 로 게이트 통과 어려움 확인*
- 결과 JSON: `infra/phaiakes9/results/2026-05-16_125209.json` (.gitignore — 로컬 보관)
**GPU 큰 모델 비교 — qwen3.5:27b (2026-05-16 13:03 KST)**:
- 동시도 1: p50=**13,885.85ms** / p90=13,992.73ms / p99=14,014.09ms / **9.22 tok/s**
- 동시도 4: p50=55,126.10ms / p90=55,258.83ms / p99=55,277.86ms / 9.28 tok/s
- L3 SLA 게이트: ❌ FAIL — *27B 는 실시간 대화용 X, 백그라운드 검증·고난도 추론용*
- 동시도 1 vs 4 의 tok/s 가 거의 동일 (9.22 / 9.28) — *큰 모델의 throughput scaling 미작동* (GPU 한 요청에 100% 사용, 병렬 처리 안 됨)
- 결과 JSON: `infra/phaiakes9/results/2026-05-16_130349.json`
- 콜드 로드 시간 30초 초과로 헬스체크 디폴트 timeout(30초) 부족 — `WHYMATH_HEALTH_TIMEOUT=180` 추가 환경변수로 해소. 후속 PR 에서 디폴트 상향 검토
**CPU vs GPU 비교 (qwen2-math:7b)**:
| 지표 | CPU baseline (2026-05-15) | GPU baseline (2026-05-16) | 향상 |
|---|---|---|---|
| p50 latency (동시도 1) | 10,303ms | 3,918ms | **2.6x ↓** |
| tok/s (동시도 1) | 12.62 | 32.63 | **2.6x ↑** |
| tok/s (동시도 4) | 24.67 | 33.81 | 1.4x ↑ |
**모델 비교 (GPU)**:
| 모델 | 디스크 | p50 (동시도 1) | tok/s | 용도 후보 |
|---|---|---|---|---|
| qwen2-math:7b (Q4_K_M) | 4.4GB | 3,918ms | 32.63 | **fast tier** (실시간 대화·산술·1-2단계 추론) |
| qwen3.5:27b | 17GB | 13,886ms | 9.22 | **quality tier** (백그라운드 검증·복잡 추론·답안 채점) |
**근거**:
- GPU 활성화로 *진정한 진전* 확인. 단 *기대치(5-12x)에는 미달 (실측 2.6x)* — DirectML on Strix Halo iGPU 의 정상 수준
- 7B vs 27B 데이터로 *L3 라우터 fast/quality 두 단계* 설계 근거 확보 — *학생 대화 즉답* 은 7B, *답안 검증·복잡 추론* 은 27B 분리
- p50 < 2초 게이트는 *7B GPU 로도 어렵다* 확인 → *qwen2-math:1.5b* (~1GB) 측정 또는 *ROCm Linux native* 가 다음 세션 핵심 시도
- Kiki PC 에 *qwen3.5:27b·qwen3-coder:30b·llama3.3:42b·gpt-oss:20b·qwen3:30b-a3b(MoE)* 다수 이미 적재 — *모델 비교 실험* 추가 비용 거의 없음
**대안**:
- ROCm Linux native 이번 세션 진행 — 폐기: 2-4시간 추가 소요(BIOS UMA + 드라이버 갱신 등), 별도 세션이 효율
- qwen2-math:1.5b 추가 측정 이번 세션 — 폐기: 데이터 점 3개로 *L3 라우터 두 단계 결정* 에 충분, 1.5b 는 다음 세션
- DirectML 대신 Vulkan 명시 — 폐기: 현재 100% GPU 활용 확인됐고 두 백엔드 효율 차이 미미 추정 (Strix Halo 특성)
**적용 범위**:
- MEMORY.md 본 결정 로그 (CPU·GPU baseline + 모델 비교 데이터)
- MEMORY.md 활성 작업: M1.1 GPU 가속 → ✅ 완료. ROCm Linux native·1.5b 측정·L3 라우터 설계 신규 후속 등록
- `infra/phaiakes9/GPU_ACTIVATION_FOLLOWUP.md` 의 옵션 A1 *실증 완료*. 옵션 F(ROCm) 가 후속 세션 우선
**후속 작업 (별도 PR/세션)**:
1. **ROCm 7.2+ Linux native 시도** — 옵션 F. BIOS UMA Frame Buffer Fixed 48-64GB + Linux Kernel 6.18.4+ + ROCm 7.2.0+. 목표: tok/s 60-150 (현재 32 의 2-5x)
2. **qwen2-math:1.5b GPU 측정** — 실시간 대화 SLA 통과 가능성 검증. 1GB pull + 동일 벤치
3. **L3 라우터 fast/quality 두 단계 설계** — `docs/architecture/03_content_llm.md` 갱신 또는 별도 설계서
4. **healthcheck.sh 디폴트 timeout 상향** — 30초 → 90초 (큰 모델 콜드 로드 대응)
**상태**: GPU 가속 활성화 완료. Phaiakes9 가 *L3 LLM 라우터 두 단계 모두 운영 가능한 환경* 확보. 본격적 L3 라우터 구현은 다음 마일스톤(M1.2). 옵션 A1(Windows Ollama 경유) 가 *셋업 노력 vs 효과* 면에서 최적의 진입점이었음 확인.

### 2026-05-15: Phaiakes9 1차 셋업 완료 — CPU baseline 확보, GPU 후속 분리
**컨텍스트**: M1.0a Phaiakes9 머신 셋업 (NucBox EVO-X2 / AMD Ryzen AI Max+ 395 + Radeon 8060S Strix Halo / WSL2 Ubuntu 24.04). 셋업 과정에서 인프라 스크립트 다수의 버그·잘못된 모델 카탈로그 발견·fix. 1차 셋업 종료 단계로 GPU 활성화 시도 — Vulkan 경로에서 RADV가 WSL2 DXG 패스스루를 인식 못 함 확인 (Mesa 25.2.8에 DZN ICD 미포함). Kiki가 *Strix Halo 활성화 정확 가이드* 제공 (BIOS UMA Frame Buffer Fixed 48~64GB + Linux Kernel 6.18.4+/Firmware 20260110+/ROCm 7.2.0+ + WSL2 librocdxg + AMD Adrenalin 26.x+).
**결정**: 1차 셋업 종료 + CPU baseline을 *공식 M1.1 게이트 기록*으로 채택. GPU 활성화는 *다음 세션* A1(Ollama Windows native) 또는 A2(BIOS·드라이버 조치 후 WSL Vulkan 재시도)로 분리.
**1차 셋업 산출 커밋**:
- `7014bc7` fix(phaiakes9): pull_models.sh readonly OLLAMA_HOST + prefix 충돌 해소
- `8e5202d` fix(phaiakes9): 잘못된 Qwen 카탈로그 (qwen2.5-math:* → qwen2-math:7b + 일반 Qwen2.5 32B/72B 폴백)
- `962be1d` chore: .gitattributes — *.sh LF 고정 (Windows CRLF 재발 방지)
- `d67f419` fix(phaiakes9): bench_latency.py DEFAULT_MODEL fix
- (본 PR) docs(infra) + MEMORY 갱신
**CPU baseline (2026-05-15 16:13 KST)**:
- 환경: WSL2 Ubuntu 24.04 + Mesa 25.2.8 (Vulkan 1.3.275 — llvmpipe만 인식)
- 머신: NucBox EVO-X2 / Ryzen AI Max+ 395 / Radeon 8060S (gfx1151, Vulkan 미인식 상태)
- 모델: qwen2-math:7b (Q4_K_M, 4.4 GB)
- systemd unit: Phaiakes9 특화 (0.0.0.0:11434, MAX_LOADED_MODELS=2, NUM_PARALLEL=4, KEEP_ALIVE=10m)
- 표본: 고1 내신 원작 8문항 (`infra/phaiakes9/benchmark/sample_prompts.json`)
- 동시도 1: p50=10,303ms / p90=10,521ms / p99=10,752ms / **12.62 tok/s**
- 동시도 4: p50=20,654ms / p90=20,901ms / p99=20,903ms / **24.67 tok/s**
- L3 SLA 게이트 (p50 < 2000ms): ❌ FAIL — *CPU 추론으로 예상된 결과*
- 결과 JSON: `infra/phaiakes9/results/2026-05-15_161344.json` (.gitignore — 로컬 보관)
**GPU 활성화 시도 결과**:
- Ollama 자체 GPU 자동 감지: ❌ `total_vram="0 B"`, `inference compute id=cpu library=cpu`
- Vulkan (RADV) 강제 (`VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/radeon_icd.json`): ❌ `Failed to detect any valid GPUs`
- `/dev/dri` 부재 (Linux native GPU 노드 없음), `/dev/dxg` 존재 (DXG 패스스루 OK)
- Mesa 25.2.8 패키지에 *DZN(DirectX-to-Vulkan) ICD 미포함*. RADV가 DXG 인식 불가
- → 2026-05 기준 *WSL2 + Strix Halo + Vulkan*은 정식 지원 대기 단계 확인
**근거**:
- 1차 셋업이 이미 ~2시간 누적. GPU 활성화 추가 시도는 *Mesa 빌드*·*ROCm 셋업*·*Windows 드라이버 갱신* 등 큰 작업으로 *오늘 단락 짓는 게 효율적*
- CPU baseline 자체가 *PRD M1.1 게이트 실측 데이터*로 가치 보유 (후속 GPU 개선치의 비교 기준)
- Kiki 제공 Strix Halo 활성화 가이드가 *명확한 다음 세션 출발점* — 큰 발견을 *잃지 않고 보존* 필요
**대안**:
- GPU 활성화 이번 세션 계속 — 폐기: 시간 + 성공률 불확실. Strix Halo + Vulkan은 BIOS·드라이버 정합성까지 가야 함
- baseline 측정 없이 GPU만 시도 — 폐기: 비교 기준 부재 시 *개선치 정량화* 불가
- Ollama Windows native 즉시 전환 — 폐기: 큰 셋업 변경, 사용자 부담 누적
**적용 범위**:
- 신규 파일: `infra/phaiakes9/GPU_ACTIVATION_FOLLOWUP.md` (Kiki 가이드 보존 + 옵션 A/B/C 다음 세션 흐름)
- MEMORY.md 활성 작업 갱신 (M1.0a/M1.1 완료 표시, GPU 후속·카탈로그 정리·systemd 완화 신규 항목)
- 본 결정 로그
**후속 작업 (별도 PR/세션)**:
1. **A 후속 — Phaiakes9 GPU 활성화**: `GPU_ACTIVATION_FOLLOWUP.md` §3 옵션 A/B/C 중 선택해 진행. 다음 세션 시작점
2. **B 후속 — Qwen 카탈로그 cosmetic 정리**: README.md 7군데, SETUP_GUIDE.md 1군데, 주석 4군데 (코드 동작 영향 없음, 문서 일관성)
3. **C 후속 — Ollama systemd unit `ProtectSystem=full` 완화**: `failed to persist model recommendations snapshot ... read-only file system` 경고 해소. `ReadWritePaths=/usr/share/ollama` 추가
**상태**: 1차 셋업 완료. M1.1 CPU baseline을 PRD M1.1 게이트 기록으로 채택. GPU 활성화는 다음 세션 시작 항목.

### 2026-05-15: NCIC 크롤링 baseline 완료 — 영역·본문/해설 정제는 후속 분리
**컨텍스트**: Kiki가 별책 8 수학과 교육과정 PDF(1.94MB)를 git push로 반입(`aa29267`), Claude가 `python -m data_pipeline.ncic crawl --pdf ...` 실행 → 629건 추출, JSON·CSV·sidecar 저장. NCIC 사이트 자체는 이 환경 네트워크 allowlist 차단(`docs/data/ncic.md` §3.1).
**결정**: 현 산출물을 baseline(v0)으로 수용, 영역 추출·본문/해설 분리는 후속 분리 작업. `data/ncic/` 통째로 `.gitignore` 처리(raw 입력 + 미완성 산출물 모두 git 비포함). PDF는 `git rm --cached`로 추적 제거(history는 잔존).
**검수 결과 (5% 무작위 31건 + 자동 분석)**:
- ✅ 코드 형식: 모두 정규식 통과 (`[10공수1-02-06]` 등)
- ✅ 학교급 추론: 정확 (초등 155·중학 86·고등 388, 학년 대수 일치)
- ❌ **영역(domain) 미추출**: 모두 "미지정" — `_PdfStandardExtractor`가 PDF 헤더("(1) 수와 연산" 등)를 영역으로 인식 안 함. HTML 추출기엔 추론 로직이 있으나 PDF는 미구현.
- ❌ **본문 vs 해설 미분리**: 같은 코드가 본문 + 해설 두 번 등장 → 둘 다 statement로 저장. 예: `[2수03-10]` 본문("길이 단위 1cm와 1m를 알고…") + 해설("길이의 표준 단위를 도입하기 전에 구체물을 직접…"). 중복 194건 모두 statement가 서로 다른 것으로 확인.
- 진짜 성취기준 수: 고유 코드 435개 → 본문/해설 분리하면 ~150-300개 (NCIC 예상치 150-180에 근접).
**근거**:
- Kiki가 명시한 작업 범위는 "crawl 실행 → 5% 검수" — 그건 완료. 정제는 별도 작업.
- 정제는 코드 변경(`_PdfStandardExtractor` 확장 + `transform` 조정 + 테스트 갱신)이고 1-2시간. 별도 PR로 분리하는 게 깔끔.
- 정제 전 산출물을 git에 truth source로 박으면 정제 후 큰 diff 발생, history 오염. baseline은 로컬에서만 운영하다 정제 완료 시 정식 커밋이 안전.
**대안**:
- 정제 먼저(코드 추가 후 재추출) — 폐기: 작업 시간 1-2시간, Kiki 일정과 무관하게 차후 가능
- v0로 git에 커밋 + PDF만 `.gitignore` — 폐기: 부정확 데이터 영구 보관, 정제 후 큰 diff
**적용 범위**:
- `.gitignore` 추가: `data/ncic/raw/`, `data/ncic/*.json`, `data/ncic/*.csv`
- `git rm --cached data/ncic/raw/curriculum_math_2022.pdf` (`aa29267` 추적 해제, history 잔존)
- MEMORY.md 본 로그 + 활성 작업·후속 작업 갱신
**후속 작업 (별도 PR)**:
- **L1.NCIC.정제**: `_PdfStandardExtractor`가 페이지 헤더에서 영역명 추적, 같은 코드 두 번째 등장은 `commentary`로 분류(`AchievementStandard.commentary` 필드 활용). 재추출 후 ~150-300건 unique standards 검증.
**상태**: baseline 확정. 정제는 후속 PR로 등록. 데이터 카드 `docs/data/ncic.md`는 유지(메타·라이선스·실행 절차).

### 2026-05-14: MathScope PRD v1.1 채택 — 비전·기능 흡수, 구조 골격 WhyMath 유지
**컨텍스트**: Kiki가 별도로 발전시킨 *MathScope PRD v1.1*(1,410줄)이 도착. WhyMath 하네스 문서군(CLAUDE.md·MEMORY.md·ROADMAP.md·docs/architecture/01~07·.claude/agents/)과 같은 프로젝트 비전(메타인지·답 안 주기·Socratic·다중 풀이·Polya·Flutter+FastAPI·Phaiakes9 로컬 LLM)을 공유하나, 두 사고 라인이 갈라져 브랜드·아키텍처·DB 스택·로드맵·데이터 모델·첫 진입 전략이 곳곳에서 충돌. 그대로 두면 *진실의 원천*이 둘이 되어 컨텍스트 오염. PRD는 시장·사업·법률·UX·데이터 모델 면에서 하네스보다 풍부하나, 하네스는 7계층 책임 규율·교수학 깊이가 강함.
**결정**: PRD의 *비전·신규 기능*을 정본으로 흡수하되, 3대 구조 결정은 *WhyMath 골격 유지*.
- **브랜드 = WhyMath 유지**. PRD의 "MathScope (가칭)"는 미확정 가칭 — PRD가 브랜드를 정한 게 아님. WhyMath는 2026-05-14 브랜드 결정 로그에 근거와 함께 확정, GitHub 레포도 `doldori7/WhyMath`. PRD 인용 시 "MathScope" → "WhyMath" 치환.
- **7계층 책임 모델 유지 + PRD 5블록 흡수**. 7계층=책임 레이어(.claude/agents 7개와 직결), PRD 5블록(Client/Backend/DB/ML/Pipeline)=배포 토폴로지 — 직교하는 두 축. 7계층 유지하고 PRD 5블록은 `00_overview.md`에 배포 관점 섹션으로 추가.
- **첫 진입 = 고1 내신 유지**. PRD의 "미분 단원 파일럿"을 "고1 단원 중 하나"로 재해석. 2026-05-13 결정 로그 유지.
- **DB 스택 변경** (CLAUDE.md 기술 스택 표 갱신 대상): Neo4j(개념 그래프)·Qdrant(벡터, 기존 ChromaDB 대체 검토)·ClickHouse(학습 행동 로그)·S3/MinIO(영상·이미지) 추가. 추가 스택: MathLive·D3.js·three.js·Plotly·HDBSCAN·UMAP·OpenAI text-embedding-3-large.
- **PRD 신규 자산 → 7계층 매핑**: 개념 그래프·다국 커리큘럼 매트릭스·교과서 매핑 12단계 파이프라인·자동 커리큘럼 정렬 → L1; `SolutionPath.concept_sequence` → L3; `MasteryState`·`StudentProfile` → L2; 시각화 스택(선언적 `Visualization`) → L5; Socratic 흐름·graded `Hint`·개념 점화 지도 → L4; PIPA 데이터 권한 매트릭스 → 횡단; 사업·법률·5개 핵심 가정 → docs/strategy·docs/legal.
**PRD 논리적 허점 8건과 보정 입장**: ①Phase 1 3개월 과부하 → WhyMath 6개월 Phase 1 유지 ②다국 매트릭스 9~12개국 Phase 1 과욕(본문 §1.6 vs §15.1 불일치) → Phase 3 재배치 ③3개 앱 동시 개발 부담 → 단일 앱 모드 분기 + 대시보드 Phase 3+ 유지 ④BKT 제거 위험 → BKT(P1)→IRT(P2)→DKT(P3) 단계 도입 유지 ⑤개념 시퀀스 동치성 판정 난이도 과소평가 → 휴리스틱+사람 검수 단서 ⑥교과서 학습목표 "페어유즈" 단정 위험 → 변호사 검토 전제 ⑦AWS Seoul 확정 vs Phaiakes9 하이브리드 지연·동기화 비용 미언급 → 기존 하이브리드 인식 유지 ⑧부록 E "별책 9"는 오타 — 수학과 교육과정은 *별책 8*.
**근거**:
- PRD가 "정본"인 것은 *비전·기능·시장·법률 사고의 깊이*에서이지 *구조 결정*에서가 아님. 브랜드·7계층·진입학년은 하네스에 이미 근거와 함께 확정된 사항 — PRD가 그것을 *논박한 게 아니라 모른 채로 작성*된 것.
- 7계층은 서브에이전트 위임 워크플로의 단위 — 폐기하면 워크플로 전체가 흔들림. PRD 5블록과는 축이 달라 양립 가능.
- PRD의 진짜 가치는 자동 커리큘럼 정렬·교과서 매핑 파이프라인·다국 매트릭스·개념 그래프·9개 데이터 엔티티·PIPA 매트릭스·5개 핵심 가정 검증법 — 하네스에 *없던* 자산이며 7계층 안에 깔끔히 들어감.
**대안**:
- *PRD를 완전 정본으로(브랜드·5블록·고2 미분까지)* — 폐기: 이미 확정된 결정 3건을 근거 없이 뒤집음, 서브에이전트 구조 재작성 부담
- *PRD를 독립 문서로 방치* — 폐기: 진실의 원천 이중화, 컨텍스트 오염
- *단일 통합본 신규 작성* — 폐기: 작업량 최대, 하네스의 검증된 구조를 버릴 이유 없음
**적용 범위**: 정렬 실행 계획 5단계 — 단계1 본 결정 로그(MEMORY.md), 단계2 CLAUDE.md+ROADMAP.md, 단계3 docs/architecture/00~07, 단계4 .claude/agents+docs/data+docs/strategy+docs/legal, 단계5 schemas/v1.1/*.yaml 9개 엔티티. 범위 밖: PRD 와이어프레임 구현·엔티티 코드 구현·실제 DB 배포.
**상태**: 확정. 단계별 진행 — 본 로그가 후속 모든 문서의 근거.

### 2026-05-14: `main` 보호 — CODEOWNERS + CI status check 자동화, UI 단계는 별도 가이드
**컨텍스트**: 2026-05-14 GitHub 연결 결정 로그의 후속 작업 "main 보호 규칙(force-push 금지·PR 리뷰 필수) 적용 예정"을 자동 처리하려 했으나, Claude의 GitHub MCP 도구셋에 *Branch Protection* / *Repository Ruleset* 엔드포인트 도구가 부재. 원격이 로컬 MCP 게이트웨이(`http://127.0.0.1:36037/git/doldori7/WhyMath`)라 토큰을 직접 쓸 수도 없음(MCP 도구 외 경로로 REST API 호출 불가). 따라서 *완전 자동*은 불가능, 정책의 *실효성 부분*만 코드로 강제하고 GitHub Settings UI 단계는 5분 수동 가이드로 분리.
**결정**:
- 자동(코드 표현):
  - `.github/CODEOWNERS` — 영역별 자동 리뷰어. 디폴트 `@doldori7`, L1 데이터·L3/L5 인프라·문서·정책·`.github/` 등 영역별 매핑. Phase 2+ 합류 시 분기 가능한 구조
  - `.github/workflows/ci.yml` — push/PR마다 3 job 실행:
    - `data-pipeline — lint·type·test`: ruff·black·mypy-strict·pytest+coverage(fail-under=70)
    - `infra/phaiakes9 — bash syntax`: 모든 infra `.sh` 파일 `bash -n` + shellcheck(non-blocking)
    - `policy-guard — CLAUDE.md 금기 가드`: 검정교과서 본문 인용 패턴·하드코딩 시크릿(sk-/sk-ant-/ghp_/AKIA) 사전 차단
  - concurrency group으로 비용 절감 (동일 ref 재푸시 시 이전 실행 취소)
- 수동(GitHub Settings UI):
  - `.github/branch-protection-setup.md` — 1페이지 체크리스트: PR 1+승인·Code Owners·필수 status check 3종·linear history·force-push 차단·deletion 차단·administrators 포함
  - Kiki가 5분 작업, 완료 시 이 MEMORY 항목 *상태*를 갱신
**근거**:
- **CODEOWNERS 가치**: 보호 규칙 없이도 *PR 자동 리뷰어 지정* 자체 동작. 향후 영역별 도메인 파트너 합류 시 영역만 갱신하면 자동 라우팅
- **CI workflow가 보호의 80%**: "필수 상태 검사 통과" 정책은 *워크플로 자체가 존재*해야 GitHub Settings에서 등록 가능. 즉 보호 규칙은 워크플로의 *적용 정책*이지 *내용*이 아님 — 내용을 미리 갖춰두면 UI 단계는 5분
- **policy-guard job**: CLAUDE.md 절대 금기(검정교과서 본문·하드코딩 시크릿)를 사후 사람 리뷰가 아닌 *기계적 사전 차단*으로 강제. 1인 단계 휴먼 에러 방지
- **MCP 도구 부재의 한계 인정**: 시도조차 안 한 게 아니라 *시도→불가→대안* 흐름을 명시. 향후 GitHub MCP가 branch_protection 도구를 추가하면 그때 자동화. 또는 GitHub App 토큰을 secret으로 받아 workflow 안에서 GH API 호출하는 자기참조 자동화도 가능 (Phase 2 검토)
**대안**:
- *GitHub Actions 워크플로 안에서 GH API로 보호 규칙 셀프 적용* — 자기 워크플로가 자기 보호 규칙을 만드는 *부트스트랩 문제* + Personal Access Token 필요. Phase 2에서 GitHub App 도입 시 검토
- *Pre-receive hook* — GitHub 자체 호스팅 아닌 한 불가
- *완전 수동* — CODEOWNERS·CI 없이 UI만으로는 *어떤 status check가 있는지 모름*. 폐기 이유: 자동화 가능한 80%를 굳이 미룰 이유 없음
**적용 범위 (이번 작업)**:
- 신규: `.github/CODEOWNERS` (27 lines), `.github/workflows/ci.yml` (3 jobs), `.github/branch-protection-setup.md` (UI 단계별 + 트러블슈팅)
- 미적용 (이번 작업 범위 외): UI 보호 규칙 자체 — Kiki가 위 가이드 따라 5분 작업 후 이 항목 *상태* 갱신
**상태**: 확정. main 브랜치 보호 규칙 적용 완료 (2026-05-15): PR 1+승인·Code Owners·CI status check 3종(`data-pipeline`·`infra/phaiakes9`·`policy-guard`)·linear history·force-push 차단·deletion 차단·administrators 포함. 1인 단계 Code Owner 자기 승인 충돌은 Phase 2 합류 시 자연 해소(가이드 §트러블슈팅 참조). 검증용으로 PR #1(이 세션 10개 커밋 통합)을 생성하여 CI 첫 가동·status check 등록. → **2026-05-20 정정**: 이 '자연 해소' 가정은 PR #3 머지 시 실제 하드 데드락으로 드러나, 솔로 설정(승인0·Code Owners 미요구)으로 변경 — 위 *2026-05-20: `main` 보호 자기 승인 데드락* 로그 참조.

### 2026-05-13: M1.1 게이트를 *M1.0a 머신 셋업 + M1.1 벤치마크*로 분리
**컨텍스트**: `/implement backend:phaiakes9-qwen3-math` 위임으로 Ollama 설치·systemd unit·헬스체크·벤치마크 스크립트(`infra/phaiakes9/`, 커밋 `b75730b`, 11 files +1725) 완성 후 Kiki에게 Phaiakes9 콘솔 실행 안내. 그러나 Phaiakes9 머신 자체가 *아직 셋업 안 된 상태*(OS 미설치·전원 OFF·미조립 중 하나)임이 확인됨. 기존 ROADMAP·MEMORY는 *기술 스택 결정*으로 Phaiakes9를 명시했을 뿐, *물리 머신의 부팅·SSH·드라이버 상태*는 별도 추적되지 않았음. 결과적으로 M1.1 게이트("Phaiakes9 Qwen3-Math p50<2s 측정")가 *두 가지 다른 단계*를 한 줄에 묶고 있었음 — (a) 물리·OS·드라이버 셋업, (b) Ollama 운영·벤치마크. 둘은 의존하지만 *책임 주체·자동화 가능성*이 다름.
**결정**:
- M1.1 게이트를 두 단계로 분리:
  - **M1.0a — Phaiakes9 머신 셋업** (수동 + `bootstrap.sh`): Ubuntu 24.04 설치·계정·SSH 키·sshd 하드닝·ufw 방화벽·시간 동기·ROCm(또는 CPU 폴백)
  - **M1.1 — Qwen 벤치마크 게이트**: 기존 README §2 빠른 시작 + `benchmark/run_bench.sh` → `gate_p50_under_2s == true` 판정
- M1.0a 산출물: `infra/phaiakes9/SETUP_GUIDE.md`(약 200 lines, 6 Phase 체크리스트 + 트러블슈팅) + `infra/phaiakes9/bootstrap.sh`(8단계 멱등 부트스트랩, `WHYMATH_SKIP_ROCM`·`WHYMATH_LAN_CIDR` 환경변수)
- M1.0a Phase는 Kiki 수동 작업 의존(하드웨어 상태). 그동안 *블로킹 없는* 독립 작업 진행 가능: NCIC 크롤러·main 보호 규칙·L4 프롬프트 작성·FastAPI 스캐폴딩
**근거**:
- **머신 셋업과 운영 분리의 책임 명확화**: bootstrap은 *재현 가능한 자동화*이지만 BIOS·파티션·SSH 키 등록은 *Kiki만 할 수 있는 결정*. 한 게이트에 묶이면 게이트 통과 정의가 모호해짐
- **Strix Halo APU 특수성**: AMD Ryzen AI Max+ 395는 ROCm 정식 지원 목록에 *아직 미포함*(gfx1151). `HSA_OVERRIDE_GFX_VERSION=11.5.1` 강제 또는 CPU 폴백 결정이 필요한데, 이 판단은 *벤치마크 결과를 보고* 내려야 함. 즉 M1.0a → 1차 벤치마크 → 재조정 루프가 자연스러움
- **컨텍스트 위생**(CLAUDE.md): M1.1 게이트가 *두 가지 다른 진실*을 가지면 진행 보고가 모호해짐. 분리하면 각각 binary pass/fail
- **병렬 작업 금지 원칙과 양립**: M1.0a가 Kiki 수동 작업이므로 Claude/AI 측에서는 *다른 독립 작업* 진행이 정당함(같은 AI가 두 코드 영역 병행이 아님)
**대안**:
- *분리하지 않고 게이트 텍스트만 보강* — "Phaiakes9 머신 셋업 + 벤치마크 p50<2s"로 두 줄. 폐기 이유: 셋업 자체에 1~2주 소요 가능성, 별도 추적 필요
- *Phaiakes9 셋업을 Phase 0 청사진 단계로 소급* — 청사진은 이미 종료. ROADMAP 재작성 부담 큼
- *클라우드 GPU 인스턴스로 임시 우회* — Ryzen AI Max+ 395의 *실제 비용·지연*을 못 잡으므로 게이트 신뢰도 손상. CLAUDE.md "비용 구조를 로컬 LLM 우선" 결정의 검증 무력화
**적용 범위 (이번 작업)**:
- 신규 파일: `infra/phaiakes9/SETUP_GUIDE.md`, `infra/phaiakes9/bootstrap.sh`
- 수정: `infra/phaiakes9/README.md` (M1.0a 안내 섹션 + 디렉토리 트리 갱신 + 자리표시자 `<whymath-root>` 명시화)
- 후속 (이번 작업 범위 외): `ROADMAP.md` 90일 Day 1~14 항목에 *M1.0a 머신 셋업* 한 줄 추가 (별도 PR)
**상태**: 확정. M1.0a 완료 시점에 *별도 결정 로그*로 결과 기록 예정.

### 2026-05-13: Phase 1 MVP 진입 학년 = *고1 내신*, 도메인 파트너 영입은 *M1.3까지 지연*
**컨텍스트**: `/plan Phase1-MVP` 세션에서 두 가지 미해결 의사결정을 동시에 처리해야 했음 — (1) 첫 진입 학년 선택(중2 자유학기제 vs 고1 내신), (2) 도메인 파트너 영입 트랙(KAIST 영재교육원 / 한국수학교육학회 / 대학 수교과 개별 / 셋 다 동시). Phase 1은 *1개 학년·2개 모드*에 깊게 집중한다는 원칙(`docs/architecture/06_application_modes.md` Phase 1 진입점) 하에 결정 필요.
**결정**:
- **첫 진입 학년 = 고1 내신** 단일 트랙. 중2 자유학기제는 Phase 3~4 자유학기제 모드로 미룸(`06_application_modes.md` 모드 6)
- **도메인 파트너 영입 = 명시적 지연**. Day 1~14 핵심 행동 목록에서 제외하고 M1.3(월 3) 이전까지 1명 확보를 후행 게이트로 재배치. Phase 1 종료 게이트(M1.6)의 *도메인 파트너 검수 통과*는 유지
- 게이트 재조정: M1.1 게이트에서 "파트너 구두 동의" 항목 *삭제*, 대신 "Phaiakes9 Qwen3-Math 응답 속도 p50<2s 측정 완료 + NCIC 크롤러 가동"으로 교체
**근거**:
- **고1 내신 선택**: 학부모 결제 의지가 가장 강함 → Phase 1.5~2 결제 시스템 출시 시 전환율 최대화. 내신 점수는 정량 효과 검증이 가능(메타인지·사고력 효과를 단순 수치로 입증). Phase 2 수능·내신 모드(`06_application_modes.md` 모드 2)와 자연스러운 콘텐츠 연결. 반면 중2 자유학기제는 B2B 학교 진입로 매력은 크나 Phase 1의 *β 100명 무료* 단계에서는 매출 신호가 약하고, 학교 단위 진입은 영업 사이클이 6개월 이상이라 Phase 4 B2B 단계에서 다루는 게 시기 적합
- **도메인 파트너 지연**: 영입 자체는 1순위 미해결 의사결정이나, Day 1~14에 *동시 접촉*하면 비기술 작업이 기술 토대 작업(LLM 배포·크롤러·데이터 카드)을 블로킹할 위험. CLAUDE.md "병렬 작업 금지" 원칙(게이트 미달 대응 액션 3)과도 충돌. 사람 수학자 1명의 단발 검수(M1.2)는 도메인 파트너 없이도 확보 가능 → 영입은 *기술 토대가 보여줄 자산이 생긴 후* M1.2~M1.3 사이 진행이 협상력 측면에서 유리
- **고1 내신의 리스크**: 사고력 모드와 내신 압박이 정서적으로 충돌할 수 있음 → L4 정서 안전 필터·프롬프트 설계에서 *내신 점수를 KPI로 강화하지 않는* 톤 유지 필요(CLAUDE.md "정답을 빠르게 KPI 금지")
**대안**:
- *중2 자유학기제* — B2B 진입로·게이미피케이션 절제 용이성 매력. 폐기 이유: Phase 1 β 매출 신호 약함, 학교 영업 사이클 길음
- *둘 다 동시* — 콘텐츠·범위 폭발, MEMORY.md 폐기 패턴 "처음부터 풀 K-12" 위반
- *도메인 파트너 셋 다 동시 접촉* — 리스크 분산은 매력이나 비기술 작업이 기술 토대를 블로킹할 위험·"병렬 작업 금지" 위반
**적용 범위 (이번 작업)**:
- MEMORY.md 미해결 의사결정 섹션 갱신 (첫 진입 학년 항목 *확정으로 이동*, 도메인 파트너 항목 *M1.3 게이트로 재태깅*)
- 후속 작업: ROADMAP.md M1.1 게이트 텍스트 조정, `docs/architecture/06_application_modes.md` "Phase 1 진입점" 주석에 *고1 내신 트랙* 명시 — 별도 PR
**상태**: 확정. 다음 단일 행동은 Phaiakes9에 Qwen3-Math 배포 + NCIC 크롤러 작성 + GitHub `main` 보호 규칙 적용 3건의 *서로 블로킹 없는* 병렬 작업.

### 2026-05-14: GitHub 원격 저장소 연결 (Private)
**컨텍스트**: Phase 0 청사진(7계층·기술 스택·브랜드명)이 모두 확정된 시점. 로컬 git 저장소만 존재했고, 클라우드 백업·미래 협업·CI/CD 기반·코드 리뷰 워크플로의 인프라가 필요한 상태였음. 또한 `WhyMath_harness.zip`·`files.zip` 두 개의 대용량 아카이브가 git에 추적되어 있어 푸시 시 누적 부담이 됨.
**결정**:
- 호스팅: **GitHub**, 레포 URL `https://github.com/doldori7/WhyMath` (Private)
- 기본 브랜치: `main` (GitHub 표준, 기존 `master`에서 변경)
- 사전 정리: `files.zip`·`WhyMath_harness.zip` 추적 해제(working tree는 보존), 사업계획서 docx/pdf는 사용자가 별도 위치로 이동
- `.gitignore` 보강: `*.zip`·`*사업계획*`·`*business_plan*`·`internal/`·`private/`·`.env.*`·`config/secrets.*`·`.openai_cache/`·`.anthropic_cache/`·`.langfuse_cache/`·`data/cache/` 등 약 25개 패턴 추가 (멱등 마커 `# WhyMath 추가 보안 패턴 (자동 생성)`로 중복 추가 방지)
- 자동화: `scripts/01_git_local_setup.ps1`(로컬 정리)·`scripts/02_github_connect.ps1`(GitHub 연결) 두 PowerShell 스크립트로 재현 가능
**근거**:
- **Private 필수**: CLAUDE.md 절대 금기 — "미성년자 개인정보를 분석·마케팅 외부 공유 금지" 및 "학교·학년 정보로 개인 식별 가능한 분석 결과 외부 노출 금지" → 코드 자체에는 학생 데이터가 없지만 향후 시드 데이터·테스트 픽스처에서 실수 노출 가능. Public은 위험 비대칭
- **`main` 브랜치 표준화**: GitHub Actions·외부 도구·튜토리얼 거의 모두 `main` 가정. Phase 1에서 CI/CD 구축 시 마찰 최소화
- **zip 추적 해제**: 1.12 MiB 초기 푸시도 아카이브 없이 진행. 추후 LFS 도입 부담 사전 차단
- **`.gitignore` 사전 보강**: 시크릿·민감 문서가 한 번 푸시되면 git history에 영구 박힘. 사전 패턴 차단이 사후 BFG/filter-repo 대응보다 압도적으로 저렴
**대안**:
- GitLab — 프라이빗 무료 한도가 크지만, 한국 시장에서 GitHub가 인재 풀·생태계 압도. 협업자 합류 시 추가 학습 비용
- 자체 Gitea — Phaiakes9에 호스팅 가능하나 백업·가용성을 직접 관리해야 함. 1인 단계에서 비효율
- 공개(Public) 레포 — 부분 오픈소스 전략은 매력적이나 *현 단계*에서는 도메인 노하우 누출 리스크가 더 큼. Phase 2 이후 *선별적* 공개 검토
**적용 범위 (이번 작업)**:
- 커밋 `0625cf5`: `.claude/settings.json`·`.gitignore`·zip 2개 삭제·사업계획서 docx/pdf 2개 삭제·스크립트 2개 추가
- 푸시: 104 objects, 1.12 MiB, `main → origin/main` upstream 설정 완료
**상태**: 확정. 후속 작업이었던 `main` 보호 규칙 적용은 2026-05-15 완료 (위 *2026-05-14: `main` 보호* 결정 로그 §상태 참조).

### 2026-05-14: 브랜드명 "WhyMath (와이매스)" 확정
**컨텍스트**: Phase 0 종료 직전, 모든 외부·내부 문서가 일관된 브랜드명을 사용해야 함. 그동안 "한국 중·고 수학 앱"이라는 서술적 가칭 사용
**결정**:
- 정식 앱명: **WhyMath** (한글 표기: **와이매스**)
- 메인 슬로건 (KR): **"답이 아닌, 이유를 묻는 수학"**
- 메인 슬로건 (EN): **"The math that asks why."**
**근거**:
- 핵심 가치 제안(답 미루기·Polya·소크라테스)을 한 단어로 압축
- "Why"는 메타인지·사고력 시장 진입점과 직결, 영문 확장(Phase 5) 시 그대로 사용 가능
- 콴다·EBSi 등 한국 경쟁자와 *이름 단계*에서부터 차별화
**적용 범위 (이번 작업)**: CLAUDE.md / README.md / MEMORY.md / ROADMAP.md / docs/strategy/{market_positioning,differentiation}.md / scripts/setup.sh / .claude/agents/data-engineer.md / .claude/commands/status.md / src/backend/pyproject.toml
**미적용 (별도 작업 예정)**: Python 패키지명 `korean-math-backend`, DB명 `koreanmath`, 모바일 앱 ID 등 *코드 식별자*는 마이그레이션 영향 검토 후 일괄 변경
**상태**: 확정

### 2026-05-13: 시장 진입점을 *메타인지 사고력*으로 결정
**컨텍스트**: 콴다(사진 풀이)·EBSi(강의)·메가스터디(콘텐츠)와 정면 경쟁 불가  
**결정**: "수능 답 풀이"가 아닌 *메타인지·소크라테스·NRICH-style 사고력*에서 시작  
**근거**: Kiki의 메타인지 튜터 자산 직결, 한국 경쟁자 거의 없음, Phase 2에서 수능 시장 진입할 발판  
**대안**: 영재 트랙 우선 — 객단가 높지만 시장 작음, 도메인 파트너 부족  
**상태**: 확정

### 2026-05-13: 데이터 truth source를 *성취기준 코드*로 결정
**컨텍스트**: 검정 교과서 13종·평가원·EBS·학원 등 다층 구조에서 무엇이 진실 원천인가  
**결정**: 교육부 NCIC 성취기준 코드(예: `[9수01-01]`)가 모든 매핑의 root  
**근거**: 모든 출판사가 법적으로 이 코드를 따라야 함, 공공누리 라이선스, 변하지 않음  
**상태**: 확정

### 2026-05-13: 7계층 아키텍처 확정
**결정**: L1 데이터 → L2 학습자 모델 → L3 LLM → L4 교수학 → L5 상호작용 → L6 응용 → L7 커뮤니티  
**근거**: 책임 분리, 유지보수성, 서브에이전트 위임 단위와 일치  
**상태**: 확정. *경계 침범 금지*가 절대 원칙

### 2026-05-13: 비용 구조를 *로컬 LLM 우선*으로 결정
**결정**: Phaiakes9 (Ryzen AI Max+ 395, 128GB) + Qwen3-Math로 80% 처리, 18% Claude/GPT 중급, 2% 최고급  
**근거**: 학생 1인당 일 50~100회 LLM 호출 가정 시 클라우드만 사용하면 객단가 폭발  
**상태**: 확정. 동적 라우터 구현 필요

### 2026-05-13: LLM 답변 패턴을 *답 미루기*로 결정
**결정**: 학생 질문에 *바로 답하지 않음*. Polya 4단계로 진행. 답 제공은 4단계(방향→의사코드→부분→전체) 중 가능한 빠른 단계에서 멈춤  
**근거**: NRICH·Khanmigo가 수렴하는 방향. 한국 학생의 *답 즉시 의존* 패턴이 학습 저해  
**상태**: 확정. 모든 프롬프트 템플릿이 이를 반영해야 함

### 2026-05-13: 데이터 활용 안전선 확정
**결정**: 
- ✅ 가능: 성취기준·기출·학교 정보·OER(CK-12·OpenStax·Siyavula CC), Mathlib(Apache 2.0), 사용자 자기 자료
- ⚠️ 협상 후: NRICH(Cambridge MMP), EBS, AoPS Wiki(CC BY-SA)
- ❌ 절대 금지: 검정 교과서 본문, 학원·인강 콘텐츠, 출판사 풀이집  
**상태**: 확정. `docs/data/licensing_safety.md` 참조

---

## 📚 폐기된 접근 (Anti-Patterns)

### "단순 사진→답 풀이 앱"
**왜 폐기**: 콴다와 정면 충돌. 자본 게임에서 밀림. 그리고 *교육적으로 해롭다* (의존성 강화).

### "AI가 모든 걸 한다" 일체형 LLM 접근
**왜 폐기**: LLM 단독으로는 학습자 모델링·장기 추적·정확도 보장 불가. BKT/IRT 통계 모델 분리 필수.

### "수능 직접 진입"
**왜 폐기**: 입시 검증에 1~2년 걸림. 학부모 신뢰 형성 시간 필요. 메타인지로 신뢰 누적 후 진입이 안전.

### "처음부터 풀 K-12"
**왜 폐기**: 범위 폭발. 1개 학년 검증 후 확장이 위험 관리.

---

## 💡 핵심 인사이트 (장기 보존)

### 1. AI의 진짜 강점은 *답*이 아니라 *질문*
LLM은 답을 빠르게 주는 도구가 아니라, *학생이 스스로 도착하게 만드는* 도구로 쓸 때 가장 강력. 모든 프롬프트 설계의 출발점.

### 2. 한국 시장의 *진짜* 빈자리
- 사고력·메타인지 자원 (NRICH·YouCubed 한국어로 없음)
- 단계별 진단 (콴다는 사진 풀이만, 단계 분석 없음)
- 부모용 인사이트 (학원이 못 채움)
- 사고 과정 *시각화* (3Blue1Brown 한국화 빈약)

### 3. 데이터 자산 = 1~2년 누적 후 *제품과 별도로* 라이선싱 가능
NCIC + 학교알리미 + 검정교과서 매핑 + KMO 디지털화 = B2B 라이선싱 가능한 자산.

### 4. 로컬 LLM은 *비용*이 아니라 *전략*
Phaiakes9를 단순 비용 절감이 아닌 *경쟁자가 못 가진 인프라*로 인식. 클라우드 비용 폭발기에 *유일한 흑자 모델*.

### 5. *부족한 한 자리*: 수학 교육 도메인 파트너
한국에서 Kiki의 자산 조합이 거의 유일하나, *수학 교육 도메인 전문가*가 결정적 빈자리. 1순위 영입 과제.

---

## 🔗 외부 의존성 모니터링

### 라이선스 변경 가능성
- **NuminaMath**: Apache 2.0, 안정
- **MathNet (MIT 2026)**: 막 공개됨, 라이선스 정확 확인 필요
- **NRICH**: 비상업 무료, 상업 라이선스 협상 필요
- **AoPS Wiki**: CC BY-SA, Share-Alike 의무
- **Mathlib**: Apache 2.0, 안정

### 정책 변동 모니터링
- AI 디지털교과서 정책 (정권 영향 큼)
- 2022 개정 교육과정 시행 일정 (2025~2027 단계적)
- 영재교육원 예산·체계 변화
- 사교육비 경감 대책 (시장 영향)

---

## 🧪 실험 로그 (착수 후 누적)

*아직 실험 없음. Phase 1 착수 시 누적 시작.*

| 날짜 | 실험 | 가설 | 결과 | 결정 |
|---|---|---|---|---|
| | | | | |

---

## 🎯 KPI 베이스라인 (정의만 — 측정 시작 후 채움)

### 학습 효과
- 개념 숙달도 (BKT 확률 증가율)
- 오개념 해소율
- 다중 풀이 노출 후 *대안 풀이 시도율*

### 사용자 행동
- 일일 활성 사용자 (DAU)
- 세션당 *답 미루기 단계* 평균 도달 깊이 (1~4)
- 학생이 *스스로* 풀이 도달한 비율

### 비용·기술
- LLM 호출당 평균 비용
- 로컬 LLM 처리 비율 (목표: 80%)
- 응답 지연 (목표: p50 < 2s)

### 사업
- MAU·결제 전환율·이탈률
- B2B 학교 수·교육청 수
- 데이터셋 라이선싱 매출

---

## 📝 다음 업데이트 시점

- 각 결정 발생 시 *즉시*
- 각 Phase 시작·종료 시
- 매월 첫째 주 (정기 리뷰)

---

**최종 수정**: 2026-05-28  
**다음 정기 리뷰**: Phase 1 착수 후 첫 월
